# 软件研发功能迭代记录与任务协作系统

## 系统设计文档 V1.0.2（技术选型 / 模块划分 / 系统安全）

**文档版本：** V1.0.2

**文档状态：** 已完成第二轮审核整改，待开发基线冻结

**对应文档：**

- [《功能设计 V1.1》](功能设计v1.1.md)：业务对象、规则、流程与交互规格；
- [《技术设计 V1.2.2》](技术设计v1.2.2.md)：实施级技术方案、数据库约束、事务处理与运维规范（本文与技术设计须同步生效）。

**文档定位：**

本文是连接产品功能与技术实现的**概要设计**，重点回答三个问题：

1. 用什么技术实现；
2. 系统如何划分为可独立维护的模块；
3. 如何保证系统安全。

本文不展开具体 SQL、接口字段、代码片段等实施细节，只描述**选型理由、边界与机制**，为后续详设和开发提供稳定依据。

---

# 0. V1.0.2 审核整改摘要

本版本在 V1.0.1 整改基础上完成第二轮一致性与安全性修订。V1.0.1 已落实的 Workflow、无 Outbox、ActivityProjection、Session 与安全头等决策继续有效；V1.0.2 进一步消除实现歧义，核心变更如下：

| 审核问题 | V1.0.2 决策 |
|---|---|
| 分层图把单域服务与 Workflow 串联 | Controller 按命令类型二选一调用单域 Use Case 或跨域 Workflow；Workflow 只调用领域 Public Port / Service |
| 代码依赖、Port 依赖与数据库 FK 依赖混杂 | 分为代码依赖图与数据库 FK 依赖表；代码只沿无环 Public Port 调用，跨域写统一由 Workflow 编排 |
| TaskGroupMember 与 `task_groups.main_task_id` 双真相 | 主任务身份只存于 TaskGroupMember 的 `role=MAIN`，删除 `task_groups.main_task_id` |
| ExternalLinks 项目归属 FK 与去重不完整 | 项目链接直接使用 `project_id` 作为归属；实体链接使用类型化关联表与复合 FK，并以数据库 UNIQUE 去重 |
| 审计哈希链可能在并发下分叉 | 明确按链作用域锁定链头、单调序号、唯一约束、确定性序列化、密钥轮换与并发测试 |
| CSRF 首次登录和页面刷新无法重新取得明文 Token | 增加匿名/已登录均可调用的签发端点；每次签发轮换 Token 哈希，登录成功再次轮换 Session 与 Token |
| Zod 无法单独描述完整 HTTP 契约 | Zod 是数据 Schema 唯一真相，Route Registry 是路由元数据唯一真相；共同生成 OpenAPI 与客户端 |
| 搜索 PoC 过晚且样本不足 | 阶段 0 完成中文可行性 PoC；阶段 4 按 3～5 年容量、并发与金标查询集完成最终验收 |
| CSP 仍为条件决策 | 生产采用 nonce CSP，禁止 `script-src/style-src 'unsafe-inline'`；AntD nonce 兼容性为阶段 0 阻断项 |
| MFA 与原始审计读取权限闭环不足 | 补齐受限注册、密钥加密、恢复码、防重放；原始审计使用独立只读角色，普通运行时角色无 SELECT |
| 浮动版本及其他文义问题 | Nginx 固定 1.30.x；Drizzle 最低 0.45.2 并锁定补丁版本；NestJS 12 迁移风险纳入阶段 0 ADR；统一用户 `auth_version` 会话失效语义；前后端仅做领域命名对齐；GitHub URL 仅允许 HTTPS；修正接口描述 |

---


## 0.1 本期建设范围

本期交付以下能力：

| 能力 | 说明 |
|---|---|
| 用户登录及两类角色 | 系统管理员、项目成员 |
| 项目创建、初始成员选择和成员管理 | 创建者自动成为成员，管理员管理成员 |
| 模块及功能档案 | 模块负责分类，功能负责长期档案 |
| 功能级任务和模块级任务 | 模块级任务可影响多个功能 |
| 任务指派、完成、取消、恢复及历史查询 | 任务状态变更不物理删除，全部可追溯 |
| 三段式迭代记录 | 为什么改、改了什么、效果如何 |
| 草稿、发布、版本、作废和恢复 | 已发布记录修改生成新版本，误作废可审计恢复 |
| 遗留问题转任务 | 从迭代记录一键生成跟进任务 |
| 任务合并、来源分支及解除合并 | 主任务 + 来源任务分支 |
| GitHub 多链接关联 | 支持多个 Issue / PR / Commit 链接 |
| 全局搜索、项目动态、基础通知和操作审计 | 检索、动态、站内通知、审计留痕 |

---

# 1. 技术选型

## 1.1 总体形态

本系统采用**模块化单体 + 前后端分离 SPA + 单一关系数据库**。

```text
浏览器
  │ HTTPS
  ▼
反向代理（Nginx / 静态资源 / API 路由）
  ├── React 单页应用（浏览器端）
  └── 后端应用（NestJS）
          └── 关系数据库（PostgreSQL）
```

选择该形态的原因：

- 本期规模为国内单企业、单实例，约 10～300 名用户，无多租户与高并发弹性需求；
- 单一数据库事务可保证「任务完成、迭代记录发布、审计写日志」等关键操作的一致性；
- 部署、备份、升级与回滚路径简单，1～3 人团队可长期维护；
- 为后续 GitHub 集成、SSE 通知、容量扩展保留清晰演进路径。

## 1.2 分层选型

| 层次 | 选型 | 选择理由 |
|---|---|---|
| 开发语言 | TypeScript（严格模式） | 前后端语言统一；类型反馈有利于 AI 辅助与多人协作 |
| 工程组织 | pnpm monorepo | 单仓管理前后端与共享包，统一 lockfile 与脚本 |
| 前端框架 | React + Vite | 内部重交互 SPA，无 SEO / SSR 需求 |
| UI 组件库 | Ant Design | 覆盖表格、树、抽屉、弹窗、表单等重交互场景 |
| 前端数据缓存 | TanStack Query | 服务端数据缓存、失效与重试策略统一 |
| 前端表单 | React Hook Form + Zod | 表单状态管理与运行时校验 |
| 后端框架 | NestJS（Express 适配器） | 模块、依赖注入、Guard / Pipe / Interceptor 约定清晰，认证生态成熟 |
| API 形态 | REST JSON，`/api/v1` | 边界明确，便于测试与后续集成 |
| API 契约 | Code-first：共享 Zod 数据 Schema + 类型化 Route Registry → OpenAPI 3.1 → 客户端 | Zod 是数据结构唯一真相，Route Registry 是 HTTP 操作元数据唯一真相；OpenAPI 与客户端均为生成物 |
| 数据库 | PostgreSQL | 事务、约束、行锁、JSONB、相似度检索支持完善 |
| 数据访问 | Drizzle ORM + 显式迁移 | 类型安全，同时保留 SQL、锁与索引控制力 |
| 认证 | 服务端 Session + HttpOnly Cookie | 支持主动失效，适合内部系统 |
| 会话存储 | PostgreSQL | 本期不为 Session 单独引入 Redis |
| 通知 | 数据库通知表 + 前端轮询 | 本期不实现 Outbox；通知在业务事务内写入，提交后由轮询读取 |
| 搜索 | PostgreSQL + PGroonga + 只读搜索投影 | V1 支持中文短词、完整英文缩写、完整代码标识符和完整编号；阶段 4 再按预计峰值容量验收 |
| 审计 | 应用层业务审计 + 哈希链 + 只追加 | 记录业务语义；目标为“可追溯、抗篡改”，不声称不可抵赖 |
| 测试 | Vitest + PostgreSQL 集成测试 + Playwright | 覆盖单元、真实约束、并发、权限矩阵与关键路径 |
| 部署 | 多阶段镜像 + Docker Compose + Nginx | pin digest、非 root、独立迁移任务、资源限制、健康检查、备份恢复演练 |
| 日志 | pino 输出到 stdout | 由容器日志驱动轮转与采集 |

## 1.3 选型原则

1. **优先可验证**：选型应能让类型、数据库约束、自动化测试快速校验 AI 或人工产出；
2. **优先可靠而非最轻**：本系统核心是数据不丢、不串项目、不产生半完成状态；
3. **避免过度设计**：不引入当前规模不需要的分布式组件；
4. **契约先行**：HTTP 契约可生成、可校验，前后端不强耦合数据库结构。

## 1.4 版本基线

采用“固定主版本 + 锁定补丁版本和镜像 digest”的双层策略，不使用“当前版本/其他依赖选稳定版”这类会随日期失效的表述。

| 组件 | 主版本基线 | 锁定与升级策略 |
|---|---|---|
| Node.js | 24.x | `package.json` engines 与 CI 固定 24.x；Node 24 进入 Maintenance 后仍可在主版本内升安全补丁，迁移到 26 LTS 另走 ADR |
| pnpm | 11.x | `packageManager` 固定，`pnpm-lock.yaml` 必须提交 |
| NestJS | 11.x（锁定 11.2.3） | 阶段 0 已完成 11.2.3 与 12.0.1 的兼容性 PoC；ADR-026 记录验证证据并决定本期继续暂留 11.x，升级触发条件与工作清单见该 ADR |
| React | 19.x | 与 Vite 8、AntD 6 的兼容矩阵在阶段 0 验证 |
| Vite | 8.x | 使用 Rolldown；验证 AntD、TanStack Query 和测试插件兼容性 |
| Ant Design | 6.x | 阶段 0 验证 CSS-in-JS 与 CSP 方案 |
| TanStack Query | 5.x | 不引入跨页面服务端实体副本 |
| React Hook Form | 7.x | 8 仍为 beta，不采用 |
| Zod | 4.x | 作为 API 契约唯一运行时 Schema 来源 |
| Drizzle ORM | >=0.45.2、<0.46 | 最低使用 0.45.2，并在 manifest 与 lockfile 中锁定实际补丁版本；升级补丁须通过迁移生成、回滚与锁行为测试，1.0 GA 后再评估主版本 |
| OpenAPI | 3.1 | 生成物提交到仓库用于 diff 检查；客户端重新生成作为 PR 门禁 |
| PostgreSQL | 18.x | 生产升级到当前安全补丁版本（评审时基线 18.6）；补丁升级先通过迁移与恢复演练 |
| Nginx | 1.30.x（评审基线 1.30.4） | 生产固定实际补丁版本与镜像 digest；切换 1.32.x 等新 stable 系列必须走 ADR 与回归 |

配套约定：

- CI 与生产镜像固定实际补丁版本，并记录 `package.json`、lockfile、基础镜像 digest 与 Git commit SHA；
- 不允许 `latest`、`next`、`beta`、`rc` 进入生产依赖；
- 依赖升级由 Renovate / Dependabot 创建 PR，人工确认后合并，不自动合并；
- 每次主版本升级必须新增 ADR，并先验证冷启动、构建、类型、集成、E2E、安全头与备份恢复；
- 依赖漏洞扫描作为上线阻断项，高危漏洞未修复或有明确风险接受记录时不得上线。

### 1.4.1 HTTP 契约唯一来源

完整 HTTP 契约由两类互补且各自唯一的代码来源组成，不把 Zod 数据 Schema 误称为完整路由契约：

- `packages/api-contract` 中的 Zod Schema Registry 唯一定义 path/query/header/body 及成功、错误响应的数据结构；
- 类型化 Route Registry 唯一定义 method、path、operationId、状态码、Content-Type、安全要求、CSRF、幂等键、安全重放响应 Schema/字段和并发版本头，并引用上述 Zod Schema；所有非安全 HTTP 写方法默认 `idempotencyRequired`，普通 GET、HEAD 与纯校验路由为 `none`。只有 [ADR-023](docs/adr/ADR-023.md) allowlist 中的单次安全凭据流程可登记 `securityFlow`（例如 `issueCsrfToken` 的 `GET` 不得按普通 GET 登记为 `none`），且必须登记单次消费/自然幂等机制与客户端恢复路径；其他豁免必须显式登记原因并经新 ADR 评审。`idempotencyRequired` 的摘要覆盖不可复用的幂等契约版本、解析后的 path/query/body、规范 Content-Type 与全部行为相关请求头，使用版本头的路由必须包含 `If-Match`，并使用带版本专用密钥的 HMAC-SHA-256；每个可缓存的 2xx 响应必须以带版本的 `idempotencyReplayPolicy` 登记互斥的 `body`（精确 Schema ref + 穷尽安全字段）或 `noBody`，并以 `replayAuthorizationPolicy` 登记响应暴露资源的类型化最小引用与当前读取授权器；响应头不得重放。请求、重放或结果授权策略变化必须升级契约版本，旧 Key 在新契约下返回 409；
- Controller 仅绑定 Route Registry 的 operationId 并调用 Use Case / Workflow，不重复手写请求 DTO、响应 DTO 或 OpenAPI Decorator；CI 校验 Registry 与 Controller 一一对应；
- OpenAPI 3.1、TypeScript 客户端及测试样例均由 Schema + Registry 生成，生成后有未提交差异即阻断；
- 对外契约只使用可无损映射到 OpenAPI 3.1 的 Zod 子集；`transform`、依赖运行时环境的 `refine` 等不可表达逻辑只能放在服务端领域校验中；
- 服务端对请求与响应都执行对应 Zod Schema 校验，客户端校验不能替代服务端校验。

---
## 1.5 明确不引入

本期不建设：

- 微服务、多租户平台、Kubernetes；
- Redis 缓存、Elasticsearch、WebSocket 实时协作；
- Event Sourcing、CQRS 框架、自研权限 DSL；
- GitHub API / Webhook 深度集成（本期仅解析并保存链接，不在本期实现 Outbox/Worker）；
- 中文分词服务、Elasticsearch/OpenSearch 或独立搜索服务；
- 附件对象存储。

---

## 1.6 搜索方案与验收指标

采用 **PostgreSQL + PGroonga + 规范化搜索投影**。V1 支持中文短词、完整
英文缩写、完整代码标识符和完整编号，不保证任意英文/代码子串搜索，也
不提供正则表达式搜索。

实现要求：

- 搜索投影保存 `project_id`、实体类型、实体 ID、标题、规范摘要、原始文本与 `normalized_search_text`；
- 规范化：Unicode NFKC、转小写、去除首尾空白、合并连续空白、统一常见全角/半角标点；业务字段另存原文；
- 索引：`USING pgroonga (normalized_search_text)`，默认 opclass 为 `pgroonga_text_full_text_search_ops_v2`；可评估覆盖索引；
- 查询：参数化，使用 `normalized_search_text &@~ app.pgroonga_query_escape($1)`，限制最短查询长度（建议 2 个字符）、最大长度与分页；用户输入必须转义；
- 权限：搜索投影与 `project_id` 关联，查询前使用当前用户可访问项目集合过滤；禁止按全库返回后再前端过滤。

普通搜索禁止直接使用未转义 `&~`，也禁止把用户输入透传给 Groonga 高级
查询语法；正则搜索不属于 V1 入口。搜索采用两阶段门禁：

**阶段 0：技术可行性 PoC**

- 使用不少于 1000 条真实脱敏或高保真仿真中文数据，以及不少于 100 条经人工标注期望结果的查询；
- 固定 Top-K（阶段 0 使用 Recall@20）与相关性金标，查询覆盖中文短词、编号、英文、混合字符、全半角标点和无结果场景；
- Recall@20 ≥ 90%，并证明 PGroonga 索引被目标查询计划使用；同时验证 PostgreSQL 18.6 官方基线中扩展构建、迁移和默认计划。18.6 探针镜像的构建、扩展、`0000-0002 -> 0003-0005` 迁移、逐迁移回滚、旧 `pg_trgm` contract 清理、默认计划和逻辑恢复已通过；`020_pgroonga.sql` 与 `0003-0005` 已通过真实 PostgreSQL 集成测试，SearchQueryService 服务层与真实 PostgreSQL 权限过滤/参数化查询测试已落地，搜索 API/Controller、生产备份恢复仍待后续纵切片。

**阶段 4：容量与安全最终验收**

| 指标 | 目标 |
|---|---|
| 数据规模 | 不低于 300 用户、5 年容量模型预计峰值的 1.2 倍，且搜索投影不得少于 100,000 条 |
| 查询金标 | ≥ 200 条代表性查询，固定版本、标注期望 Top-20 结果并纳入仓库 |
| 中文短查询召回率 | Recall@20 ≥ 90%（查询长度 ≥ 2 个中文字符） |
| 查询响应 | 30 个并发搜索请求、持续 10 分钟；预热后 P95 < 500ms、P99 < 1s，并单独记录冷缓存结果 |
| 权限过滤 | 任何查询不得返回当前用户无权访问项目的数据 |
| 跨项目串联 | 0 条 |
| 错误处理 | 空查询、超长查询、特殊字符查询返回明确结果，不报错且不放大资源 |

若后续 V2 要求任意英文/代码子串搜索，再通过新 ADR 评估
`TokenNgram`、`TokenBigramSplitSymbolAlphaDigit` 或独立搜索引擎；不得为
满足演示而临时引入未验证的第三方镜像或未转义的正则查询。

---
## 1.7 生产部署约束

生产环境只允许 Nginx 暴露 HTTP/HTTPS 端口；PostgreSQL 与 API 仅存在于 Compose 内部网络。

| 约束 | 设计要求 |
|---|---|
| 可复现性 | 镜像使用不可变 tag + digest；记录 Node/PostgreSQL 实际补丁版本、lockfile、Git commit SHA |
| 运行时身份 | 所有容器使用非 root 用户；文件系统按需只读；应用禁止以 migration owner 运行 |
| 资源限制 | 为 db、api、web 设置 CPU / 内存上限；数据库 `max_connections` 与 API 连接池由阶段 0/4 压测确定，不设置无限连接 |
| 迁移 | 独立 `migrate` 一次性任务；API 启动不自动迁移；准备好后才启动 API |
| 健康检查 | pod/进程心跳不查数据库；就绪检查必须验证数据库连接与 migration version 匹配 |
| 网络 | PostgreSQL 不映射公网端口；外部访问只经 Nginx；Nginx 只信任内部代理并设置 `X-Forwarded-*` |
| Secrets | 使用 `/run/secrets/*` 文件；PostgreSQL 使用 `POSTGRES_PASSWORD_FILE`；Session、幂等 fingerprint、审计与 TOTP 使用独立版本化 keyring；环境变量只放非敏感版本 selector，不放生产密钥 |
| 日志 | pino JSON 到 stdout；日志驱动设置大小与文件数；日志不入库敏感字段，审计与日志分离 |
| 备份 | `pg_dump --format=custom`，校验 SHA-256，加密后异机/对象存储；每日备份，保留策略明确 |
| RPO / RTO 目标 | 本期目标 RPO ≤ 24 小时、RTO ≤ 2 小时；实际值必须通过恢复演练确认，不按“生成了文件”验收 |
| 恢复 | 每月至少将备份恢复到临时数据库并运行 smoke test；最近成功备份/恢复时间可查询 |
| 升级 | expand 迁移先行，破坏性字段删除推迟到 contract migration；代码回滚不等于数据库回滚 |
| 扫描 | 依赖漏洞扫描、镜像扫描、Secret 扫描、权限矩阵与安全头检查为上线阻断项 |

---
# 2. 模块划分

## 2.1 划分原则

1. **按业务领域划分**：每个模块对应一个清晰业务域，如任务、迭代记录、合并、审计；
2. **模块间单向依赖**：禁止跨模块直接访问内部 Repository，模块对外只暴露应用服务；
3. **高内聚、低耦合**：涉及同一组业务规则的数据与逻辑收拢在同一模块；
4. **前后端共用领域语言**：前端 feature 与后端领域模块尽量使用相同术语，但页面可组合多个 feature，Workflow 也可组合多个后端模块，不要求页面与模块一一对应。

## 2.2 业务功能地图

```text
系统
├── 身份与权限
│   ├── 用户登录
│   └── 系统管理员 / 项目成员
│
├── 项目
│   ├── 项目创建、初始成员、成员管理
│   └── 项目归档
│
├── 模块与功能档案
│   ├── 模块（分类）
│   └── 功能（长期档案、当前说明、归档）
│
├── 任务
│   ├── 功能级任务
│   ├── 模块级任务（影响多功能）
│   ├── 指派、完成、取消、恢复
│   └── 历史查询与保留
│
├── 任务合并
│   ├── 主任务 + 来源分支
│   └── 解除合并
│
├── 迭代记录
│   ├── 三段式内容
│   ├── 草稿 / 发布 / 版本 / 作废 / 恢复
│   └── 遗留问题转任务
│
├── 外部链接（GitHub）
│   └── 多链接关联与解析
│
└── 支撑能力
    ├── 全局搜索
    ├── 项目动态
    ├── 基础通知
    └── 操作审计
```

## 2.3 后端模块划分

| 模块 | 职责 | 对应本期能力 |
|---|---|---|
| IdentityModule | 用户、登录、Session、系统管理员标识 | 用户登录、角色 |
| ProjectsModule | 项目、项目成员；提供 ProjectAccessQueryPort；不负责创建“未分类模块” | 项目创建、初始成员、成员管理 |
| ModulesModule | 模块分类、归档 | 模块档案 |
| FeaturesModule | 功能、当前说明、归档 | 功能档案 |
| TasksModule | 任务、状态流转、影响功能；对外提供任务领域服务与端口 | 功能级/模块级任务、状态 |
| TaskGroupsModule | 主任务、来源分支、解除合并 | 合并与来源分支 |
| ChangeRecordsModule | 迭代记录、草稿、版本、作废与可审计恢复；不直接创建任务 | 三段式记录、版本、作废、恢复 |
| ExternalLinksModule | GitHub HTTPS URL 规范化、链接实体、类型化关联与去重；目标归属由 ExternalLinkWorkflow 校验 | GitHub 多链接 |
| WorkflowModule | 跨模块组合命令：项目创建、任务完成+记录发布、遗留问题转任务、外部链接关联、合并/解除后的审计通知活动 | 跨模块事务编排 |
| SearchProjectionModule | 写入规范化搜索投影；SearchQueryService 执行权限过滤与检索 | 全局搜索 |
| ActivityProjectionModule | 写入脱敏项目动态投影；ActivityQueryService 执行权限过滤与读取 | 项目动态 |
| NotificationsModule | 站内通知写入、未读、标记已读；实现通知写入端口 | 基础通知 |
| AuditModule | 业务审计写入、并发安全哈希链、远端锚定；写适配器与原始审计只读查询使用独立数据库角色 | 操作审计 |
| HealthModule | 存活/就绪检查 | 运维 |

本期不建立 OutboxModule、不创建 `outbox_events` 表。通知、本地审计和投影均由业务事务直接写入。远端审计检查点/明细归档是读取已提交数据的独立幂等运维任务，使用确定性对象键、重试与失败告警，不属于逐业务事件投递；未来出现 GitHub API 推送等逐事件外部副作用时，才通过新 ADR 引入 Outbox。

---
## 2.4 后端分层

领域模块内部遵循固定分层。Controller 根据命令边界二选一调用单域 Use Case 或跨域 Workflow，二者不是上下级串联关系：

```text
HTTP Controller
  ├─ 单域命令 → Domain Use Case
  │                └─ Domain Service / 本域 Repository
  └─ 跨域写命令 → Workflow Use Case
                   ├─ TransactionManager / UnitOfWork
                   ├─ 领域 Command Port / Public Service
                   └─ Audit / Notification / Activity / Search WritePort

Repository → Drizzle / SQL、锁、持久化 → PostgreSQL
                                         └─ CHECK / FK / UNIQUE 最终防线
```

分层约束：

- Controller 不直接访问数据库；
- 单域 Use Case 负责本域权限、状态机、幂等与领域不变量，不调用 Workflow；
- **跨域写规则**：任何同时修改两个及以上领域聚合的命令只能由 WorkflowModule 编排；Workflow 依赖领域公开的 Command Port / Public Service，领域模块不得反向依赖 Workflow；
- **跨域只读规则**：单域 Use Case 仅可沿无环代码依赖图调用其他模块公开、无副作用的 QueryPort，用于权限和归属校验；不得调用其他模块内部 Application Service 或 Repository；
- Workflow 通过同一 TransactionManager / UnitOfWork 向所有参与 Repository 与 WritePort 传递事务上下文，禁止各模块自行开启嵌套事务；
- Audit、Notification、Activity、Search 的写入分别通过 `AuditWritePort`、`NotificationWritePort`、`ActivityWritePort`、`SearchProjectionWritePort` 注入；端口定义位于应用层 contracts，适配器实现不得被领域模块直接导入；
- Search 与 Activity 的读取通过各自 Query Service 对外提供，并在入口调用 `ProjectAccessQueryPort`；读 Port 不得产生业务写入；
- 数据库事务内不得调用网络服务；
- Repository 不决定业务状态流转，事务边界不放在 Repository；
- 模块间表级外键由统一迁移定义；数据库 FK 依赖不等同于代码 import 依赖，两类依赖分别记录和检查。

---
## 2.5 前端结构

```text
src/
├── app/            应用级入口、Provider、全局路由
├── pages/          路由页面，只做页面组合与参数传递
├── features/       按业务域组织的组件、hooks、API 使用层
├── shared/         通用样式、无业务语义的小件
└── generated/api/  OpenAPI 自动生成的客户端与类型
```

固定依赖方向：

```text
app → pages → features → shared / generated
```

- `pages` 负责路由、页面布局和组合；不写业务规则；
- `features` 包含业务组件、hooks、表单片段与数据访问；禁止 import `pages`；
- `shared` 不反向引用 `features`；
- `generated` 只由生成工具修改，禁止手工编辑；
- ESLint / dependency-cruiser 配置 `no-restricted-imports` 或等价边界规则，CI 自动阻断反向依赖。

前端状态边界保持不变：TanStack Query 管理服务端状态，React Hook Form 管理表单草稿，URL 管理可分享筛选状态，React 局部状态管理弹窗等一次性 UI，不建立服务端实体副本。

所有 API 调用通过生成客户端进入；组件不直接裸写 `fetch` / `axios`。

---
## 2.6 模块依赖方向

统一约定：**`A → B` 表示 A 在代码层依赖 B 的公开 Port 契约**。该箭头只表示 TypeScript import / DI 依赖，不表示数据库外键；适配器实现不出现在领域代码依赖中。

```text
领域代码依赖（只读校验 Port，无跨域写调用）

Identity

Projects        → IdentityQueryPort
Modules         → ProjectAccessQueryPort
Features        → ProjectAccessQueryPort, ModuleQueryPort
Tasks           → ProjectAccessQueryPort, ModuleQueryPort, FeatureQueryPort
TaskGroups      → TaskQueryPort
ChangeRecords   → ProjectAccessQueryPort, ModuleQueryPort, FeatureQueryPort, TaskQueryPort（仅发布校验）
ExternalLinks   → Common（目标归属由 ExternalLinkWorkflow 校验）

跨域编排（上层）

WorkflowModule  → 各领域 CommandPort / QueryPort,
                  AuditWritePort, NotificationWritePort,
                  ActivityWritePort, SearchProjectionWritePort,
                  TransactionManager

横切写适配器

AuditModule              → Database, Common
NotificationsModule      → Database, Common
ActivityProjectionModule → Database, Common
SearchProjectionModule   → Database, Common

横切查询入口

SearchQueryService   → Database, ProjectAccessQueryPort
ActivityQueryService → Database, ProjectAccessQueryPort
AuditQueryService    → AuditReadRole, AdminAuthorizationPort
NotificationQueryService → Database, ActorContext
```

数据库 FK 依赖单独记录，不改变上述代码依赖方向：

| 子表 | FK 父表 |
|---|---|
| modules | projects |
| features | projects、modules |
| tasks / task_feature_impacts | projects、modules、features |
| task_groups / task_group_members | projects、tasks |
| change_records | projects、tasks（`task_id` 可空） |
| project_external_links / task_external_links / feature_external_links / change_record_external_links | projects 以及各自目标表 |
| search_projection / activity_projection / notifications / audit_logs | projects（允许系统级记录的表按约束允许 `project_id` 为空） |

关键依赖约束：

1. **Projects 不依赖 Modules**。`POST /projects` 由 WorkflowModule 组合 Projects + Modules，创建项目、初始成员和“未分类模块”，避免 Projects ↔ Modules 循环；
2. **ChangeRecords 不依赖 Tasks 的写服务或 Repository**。`change_records.task_id` 是可空多对一关联：每条记录至多关联一个同项目任务，同一任务可关联多条记录。本模块可沿只读 `ModuleQueryPort` / `FeatureQueryPort` 校验 scope 与恢复门禁，并仅在独立发布已关联任务的记录时使用 `TaskQueryPort` 验证任务为 DONE；恢复时可空 `task_id` 只表示历史来源，不参与门禁。任务完成 + 本次最多一条记录发布由 `TaskCompletionWorkflow` 编排，既有记录不阻断；遗留问题转任务由 `LeftoverTaskWorkflow` 编排，避免领域写依赖循环；
3. **修改其他领域业务聚合的跨域写入只能由 Workflow 发起**；领域模块之间仅允许沿上图调用公开且无副作用的 QueryPort；审计、通知、Activity 与 Search 通过横切 WritePort 使用命令的同一事务，不视为领域聚合调用；
4. **领域模块不依赖 Audit/Notifications/Activity/Search 实现**，只依赖应用层稳定 WritePort；应用启动时注入适配器；
5. **Search 和 Activity**读取专用投影表，并通过 `ProjectAccessQueryPort` 在数据库查询前过滤权限，不跨模块拼装内部表；两种投影都保存由业务状态同事务派生的 `visibility_scope/source_status/source_row_version`，Activity 另保存 `source_entity_type/id`。普通成员只查 MEMBER；管理员只有显式 VOID 搜索筛选才查对应 Search ADMIN_ONLY，Activity 的 ADMIN_ONLY 也只对管理员开放。派生字段不得取代业务表状态真相；
6. ExternalLinks 对任务、功能、记录的关联属于跨域命令，由 ExternalLinkWorkflow 先调用目标 QueryPort 验证归属，再写类型化关联表；数据库复合 FK 仍是最终防线；
7. 模块之间不得访问其他模块 Repository；表级 FK 只出现在数据库依赖表和统一迁移中；
8. `ProjectAccessQueryPort` 由 ProjectsModule 提供适配器；调用方只 import 应用层 Port 契约，不 import ProjectsModule 内部实现；
9. CI 分别检查代码 import 图无环、Port 只读/写语义、禁止跨模块 Repository 访问，并以 PostgreSQL 集成测试验证 FK 依赖。

---
## 2.7 数据库跨项目约束

除项目根表 `projects` 本身外，所有“属于某个项目”的业务表必须包含 `project_id` 并以 `FOREIGN KEY (project_id) REFERENCES projects(id)` 锚定项目。会被其他项目内表引用的父表另提供 `UNIQUE (project_id, id)`，子表使用复合外键，禁止仅靠应用层判断跨项目关系。

典型设计：

```text
modules:             UNIQUE(project_id, id)
features:            UNIQUE(project_id, id)
tasks:               UNIQUE(project_id, id), project_id, module_id, feature_id
change_records:      UNIQUE(project_id, id), project_id, task_id
task_groups:         UNIQUE(project_id, id), project_id
task_group_members:  project_id, group_id, task_id, role, detached_at
```

### 2.7.1 任务组唯一真相

`task_groups` 不保存 `main_task_id`，任务是否属于组及其 MAIN / SOURCE 身份只存在于 `task_group_members`：

- `FOREIGN KEY (project_id, group_id) REFERENCES task_groups(project_id, id)`；
- `FOREIGN KEY (project_id, task_id) REFERENCES tasks(project_id, id)`；
- `UNIQUE (project_id, group_id, task_id)` 防止同组重复成员；
- 对 `(project_id, group_id) WHERE role='MAIN' AND detached_at IS NULL` 建部分唯一索引，保证每个活跃组至多一个主任务；创建/合并 Workflow 必须在提交前保证恰有一个 MAIN，并由可延迟约束触发器或等价提交期校验兜底；
- 对 `(project_id, task_id) WHERE detached_at IS NULL` 建部分唯一索引，保证任务至多属于一个活跃组；
- 合并、解除与主任务查询一律读取成员表，不在 tasks 或 task_groups 冗余 `group_id/main_task_id`。

### 2.7.2 ExternalLinks 归属与去重

不使用四个可空 owner 字段，也不为项目根复制 `project_owner_id`。链接规范化数据与类型化关联表分离：

```text
external_links
  id, project_id, normalized_url, display_url, provider, kind
  FK project_id → projects(id)
  UNIQUE(project_id, id)
  UNIQUE(project_id, normalized_url)

project_external_links
  project_id, link_id
  FK project_id                    → projects(id)
  FK (project_id, link_id)         → external_links(project_id, id)
  UNIQUE(project_id, link_id)

task_external_links
  project_id, task_id, link_id
  FK (project_id, task_id)         → tasks(project_id, id)
  FK (project_id, link_id)         → external_links(project_id, id)
  UNIQUE(project_id, task_id, link_id)

feature_external_links / change_record_external_links
  使用同样的 project_id + 目标 id + link_id 复合 FK 与 UNIQUE
```

要点：

- `normalized_url` 由标准 URL 解析器生成，主机名小写、移除 fragment、规范默认端口；同项目相同规范 URL 只存一条；
- 去重必须依赖上述 UNIQUE 并使用 upsert，不能采用“先查再插”的竞态实现；
- 通用多态 `source_type / source_id` 无法建立类型化复合 FK，本期禁止使用；
- 搜索投影、活动投影、通知、审计同样携带 `project_id`；
- 数据库约束是最终防线，应用层仍必须执行当前用户权限与同项目校验；
- 集成测试必须直接尝试插入跨项目关系、重复链接、第二个 MAIN 和同一任务加入第二个活跃组，并断言失败。

ExternalLinks 权威决策见 [ADR-022](docs/adr/ADR-022.md)。

---
# 3. 系统安全

## 3.1 安全目标

本期安全目标按优先级：

1. 防止未授权访问与越权操作，防止 IDOR 与跨项目串联；
2. 保护密码、Session、密钥、审计快照与用户敏感信息；
3. 防止 CSRF、XSS、登录爆破、SSRF、依赖攻击等常见风险；
4. 关键操作可追溯、抗篡改。

**承诺边界：** 在 PostgreSQL 应用角色与数据库管理员权限分离、哈希链与远端只读归档落地后，本文只承诺“可追溯、可检测篡改”，不承诺对数据库所有者或宿主机管理员具备不可抵赖能力。前者属于组织级安全边界，需单独制度与审计。

## 3.2 身份认证与密码

- 采用服务端 Session + HttpOnly Cookie，支持主动失效；
- 密码使用 **Argon2id**。初始安全下限：`m=19 MiB, t=2, p=1`；生产不得低于该值，按生产机器基准向上调优（建议单次登录 100～300 ms）；
- 哈希参数随密码哈希持久化，升级 Argon2id 参数后允许渐进重哈希；
- 登录成功后重新生成 Session 并轮换 CSRF Token，防止 Session Fixation；
- 登录失败按“账号 + IP + 全局速率”限制，并限制并发 Argon2 计算，防止哈希计算耗尽 CPU；
- **系统管理员强制启用 TOTP MFA**。管理员通过密码后必须显式获得 MFA 注册/验证受限会话，Session 状态没有默认完整态；完成注册确认时在同一事务启用因子、签发恢复码、撤销受限会话，并轮换为完整 Session 与新 CSRF，完成前不得访问任何业务或管理接口；
- TOTP Secret 使用独立密钥加密后入库，密钥仅由 `/run/secrets/*` 提供；Secret、二维码 URI、验证码和恢复码不得写入日志、审计快照或备份明文；
- 注册成功后签发高熵一次性恢复码，数据库只存带独立 salt 与参数的 Argon2id 编码哈希，不允许普通 SHA-256；恢复码使用后立即作废。TOTP/恢复码校验限制尝试次数与时钟窗口，并持久化最后接受的 time-step，禁止同一验证码重放；
- MFA 重置必须由另一名已完成 MFA 的系统管理员执行；仅剩一名管理员时使用离线恢复流程。重置会撤销该用户全部 Session，并记录不可省略原因与审计事件；
- 用户管理、成员管理、项目/模块/功能归档或恢复、查看原始审计、作废 PUBLISHED 或恢复 VOID 迭代记录等高风险操作需要**密码 + 当前 TOTP** 重新认证；成功后以同一服务端事务时间原子刷新 Session 的 `reauthenticated_at` 与 `mfa_verified_at`，高风险门禁要求两者均在 5 分钟内；
- 用户表维护单一全局失效计数 `auth_version`；Session 签发时保存其快照。用户停用、修改密码或管理员强制退出时，必须在同一事务中递增 `auth_version` 并撤销该用户全部 Session；每次鉴权都比较当前值与签发快照，不一致立即拒绝并清除 Cookie。

## 3.3 会话与凭据保护

- Cookie 写入密码学安全的随机 Token（建议 32 字节，Base64URL）；
- 数据库只保存 Token 的哈希（SHA-256 或 HMAC），**不保存原始 Token**；审计日志引用 Token 哈希前缀，不记录完整 Token；
- Cookie 使用 `__Host-session`，固定 `HttpOnly; Secure; SameSite=Lax; Path=/`，**不设置 Domain**；
- Session 保存 `userId / authVersionAtIssue / createdAt / lastSeenAt / idleExpiresAt / absoluteExpiresAt`；CSRF Token 哈希保存于独立的短期 token 集合，单个 Session 最多保留 4 个有效哈希以支持刷新和多标签页；单次请求后按节流策略更新 `lastSeenAt`；
- 空闲超时建议 8 小时，绝对超时建议 7 天；高危操作额外使用短期重认证状态；
- Session 不保存项目成员列表，成员关系在每次资源鉴权请求中实时查询；
- Session 表不纳入业务数据备份；如采用物理全库备份，恢复脚本必须在 API 启动前使全部旧 Session 失效；
- 生产密钥优先读取 `/run/secrets/*` 文件；不使用 JWT 或与用途不符的长效密钥；
- 日志、异常与审计快照统一脱敏，不输出 Token、密码、CSRF Token、完整敏感正文与完整 Cookie。

## 3.4 授权与访问控制

权限模型保持“系统管理员 + 项目成员”两类角色。所有资源鉴权遵循：

1. 不信任客户端传入的 `projectId`；
2. Repository 查询必须携带 `ActorContext`，按资源真实归属判断；
3. 跨资源操作对每个资源分别授权并校验同项目；
4. 对无权访问的资源统一返回 404，避免资源枚举；对明确的管理功能返回 403；
5. 被移除成员的所有现有会话立即失去由该成员关系派生的项目权限，但该用户的其他项目访问不受影响；系统管理员仍按全局管理员身份访问，是否注销全局会话由独立策略决定；
6. 停用用户的 Session 按无效处理，所有受保护/业务路由及使用停用凭据的登录返回 401；仅 CSRF 签发与无效 Session 的同源登出按匿名安全语义执行，前者不恢复身份，后者只清 Cookie；
7. 不做“只读项目成员”等额外角色，避免权限模型膨胀。

接口级权限矩阵以 [docs/permissions.md](docs/permissions.md) 为权威记录，并在 Route Registry 与数据驱动测试中落地，至少覆盖下表。ADR-023 的九个 `securityFlow` operationId 必须按各自 Session/challenge 前置状态逐项覆盖，不得合并成笼统的“登录”权限。项目创建时创建者必须作为初始成员且不可在创建表单中取消；创建后可由系统管理员按普通成员规则移除，`projects.created_by` 保持不可变且不产生额外权限（[ADR-012](docs/adr/ADR-012.md)）：

| 操作 | 匿名 | 同项目活跃成员 | 其他项目活跃成员 | 已移除成员 | 系统管理员 |
|---|---|---|---|---|---|
| 读取项目及下级数据 | 401 | 允许 | 404 | 404 | 允许 |
| 读取 VOID 迭代记录详情 | 401 | 404 | 404 | 404 | 允许 |
| 新建/编辑模块、功能、任务、草稿记录 | 401 | 项目内允许 | 404 | 404 | 允许 |
| 任务完成、重新打开、取消、恢复、合并、解除合并 | 401 | 项目内允许 | 404 | 404 | 允许 |
| 查看项目动态 | 401 | 项目内允许（脱敏投影） | 404 | 404 | 允许 |
| 成员管理 | 401 | 403 | 404 | 404 | 允许 + 密码 + TOTP 重认证 |
| 项目、模块或功能归档/恢复 | 401 | 403 | 404 | 404 | 允许 + 密码 + TOTP 重认证 |
| 查看原始审计 | 401 | 403 | 403 | 403 | 允许 + 密码 + TOTP 重认证 |
| 作废 PUBLISHED / 恢复 VOID 迭代记录 | 401 | 403 | 403 | 403 | 允许 + 密码 + TOTP 重认证 + 原因 |
| 停用用户 | 401 | 不适用 | 不适用 | 不适用 | 允许 + 密码 + TOTP 重认证 |

矩阵必须按“接口 × 动作 × 资源状态 × 操作者”生成自动化测试，不能只靠人工清单。

## 3.5 CSRF 防护

本期固定采用 **同步 Token（Synchronizer Token）**，不使用朴素双提交 Cookie。首次登录、登录后轮换与页面刷新统一遵循下列闭环：

1. SPA 首次加载调用同源 `GET /api/v1/auth/csrf`。未登录时服务端创建仅用于认证流程的匿名预认证 Session；已登录时复用当前 Session；Fetch Metadata 与同源可读策略必须通过，GET 的 Origin/Referer 若存在则精确校验，缺失本身不拒绝；
   匿名 Session 不含用户身份、最长存活 10 分钟，按 IP + Cookie + 全局限流并由定时清理任务回收，不能在未验证密码时升级为业务会话；
2. 每次签发生成 32 字节随机明文 Token，只返回一次并设置 `Cache-Control: no-store`；数据库只保存 Token 哈希、Session ID、签发时间和过期时间，不保存明文；
3. 每个 Session 最多保留 4 个未过期 Token 哈希，支持多标签页；超额时删除最早项。Token 随 Session 绝对到期，且最长有效期不超过 8 小时；
4. 登录请求只接受预认证 Session Cookie 与其 `X-CSRF-Token`；普通认证、MFA 受限或完整管理员 Session 调用登录返回 409，必须先登出/清 Cookie 再建立新预认证状态。登录成功后必须轮换 Session ID、删除全部预认证 Token，并在同一响应返回绑定新 Session 的新 Token；
5. 页面刷新或内存 Token 丢失时重新调用该签发端点取得新 Token；仅当服务端明确表示 CSRF 校验失败且业务/安全状态尚未消费时，客户端最多重新签发并重试一次。普通幂等/版本化路由保留原 Key/If-Match，`securityFlow` 不发送业务幂等键并按 ADR-023 恢复，防止无限重试；
6. 除无效/已撤销 Session 的登出恢复分支外，所有非安全方法请求携带 `X-CSRF-Token`；服务端对其哈希执行常量时间比较。有效 Session 首次登出仍须 CSRF；若 Session 已无效、已撤销或首次响应丢失，同源 Origin/Referer 与 Fetch Metadata 通过后只清 Cookie、返回 204，不执行状态写；
7. 同时精确校验外部可见 Origin；`Origin` 缺失时校验 `Referer`；两者均缺失时拒绝非安全请求；
8. 登录、有效 Session 首次登出、MFA 注册/校验/恢复和所有管理操作同样执行 CSRF 校验；无效 Session 登出是唯一例外；
9. GET / HEAD 不执行业务状态变更；CSRF 签发端点只创建安全元数据，不执行业务命令，且响应不得跨域读取；
10. `SameSite=Lax`、同源部署和 Fetch Metadata 校验作为纵深防御，不替代同步 Token。

## 3.6 输入输出与浏览器安全

### XSS

- Markdown 原文保存，渲染使用 `react-markdown + rehype-sanitize`，不启用原始 HTML；
- GitHub 链接协议仅允许 `https`，外链添加 `rel="noopener noreferrer"`；
- API 返回 JSON，不拼接 HTML；错误不返回 SQL、堆栈、Secret、内部路径。

### 安全响应头

| 响应头 | 设计值 |
|---|---|
| `Strict-Transport-Security` | 专用生产域及其全部子域均确认只支持 HTTPS 后使用 `max-age=31536000; includeSubDomains`；否则先使用不含 `includeSubDomains` 的策略并完成域名清单评审 |
| `Content-Security-Policy` | 每个 HTML 响应动态生成：`default-src 'self'; script-src 'self' 'nonce-{RANDOM}'; script-src-attr 'none'; style-src 'self' 'nonce-{RANDOM}'; style-src-attr 'none'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-src 'none'; frame-ancestors 'none'; base-uri 'none'; object-src 'none'; form-action 'self'` |
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `DENY` |
| `Referrer-Policy` | `no-referrer` |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=(), payment=(), usb=()` |
| `/api/v1/**`、会话和用户信息 | `Cache-Control: no-store` |
| `index.html` | `Cache-Control: no-store`，防止动态 nonce 被共享缓存复用 |
| 带哈希静态资源 | `public, max-age=31536000, immutable` |

**CSP 与 Ant Design CSS-in-JS 决策：**

- 生产不允许 `script-src 'unsafe-inline'` 或 `style-src 'unsafe-inline'`，也不允许把 CSP 兼容性问题降级为静默放宽策略；
- HTML 入口层为每个响应生成至少 128 bit 随机 nonce，同时写入 CSP 响应头和受控 bootstrap 元数据；nonce 不进入日志，不得跨响应复用；
- SPA 启动时读取该 nonce 并传给 Ant Design CSS-in-JS 的 StyleProvider / CSP 配置，所有运行时 `<style>` 标签必须带相同 nonce；
- 阶段 0 先以 Report-Only 运行 E2E，再切换强制 CSP 并验证登录、弹窗、表格、主题、懒加载和错误页；
- 若所选 AntD/Vite 组合无法满足 nonce CSP，阶段 0 直接阻断，必须通过新 ADR 更换样式产物方案或组件版本，不得自动回退 `unsafe-inline`。

CSP 权威决策见 [ADR-021](docs/adr/ADR-021.md)。

### CORS / 同源

- 前端与 API 同源部署，不启用跨域 Credential；
- Nginx 不设置宽泛 `Access-Control-Allow-Origin`；
- 只有未来出现明确跨域调用方时，才在服务端按白名单、子路径和方法配置 CORS 并重新评审。

### 可信代理

- 仅 Nginx 可在可信网络内设置 `X-Forwarded-For / X-Forwarded-Proto / X-Forwarded-Host`；
- NestJS 必须明确设置 `trust proxy` 跳数，不信任客户端直传头；
- 生产只允许 Nginx 暴露 HTTP/HTTPS 端口，API 与 PostgreSQL 不暴露公网端口。

## 3.7 SSRF 防护

本期只解析并保存 GitHub URL，不保存 GitHub Token、不调用 GitHub API，也不由服务端抓取远程内容，从源头规避 SSRF。即使仅做 URL 解析，也执行（权威决策见 [ADR-022](docs/adr/ADR-022.md)）：

- 仅允许 `https://`；
- 使用标准 URL 解析器并精确校验 `github.com`；本期不接受 `api.github.com`、通配子域、IP 字面量、用户名/密码或非 443 端口；
- 保存前移除 fragment 并按第 2.7.2 节生成 `normalized_url`；
- 拒绝用户控制代理地址、请求头与重定向；
- 未来启用请求时，必须增加 DNS 私网/环回地址拒绝、超时、响应体上限、专用低权限 Token 和逐跳重定向校验。

## 3.8 审计与追溯

审计记录业务动作而非复制每一次 SQL UPDATE：

```text
PROJECT_CREATED、PROJECT_MEMBER_REMOVED、TASK_COMPLETED、
TASK_MERGED、TASK_DETACHED、CHANGE_RECORD_PUBLISHED、
CHANGE_RECORD_VERSION_CREATED、CHANGE_RECORD_VOIDED、CHANGE_RECORD_RESTORED、
AUDIT_LOG_READ 等
```

每条审计至少记录 actor、action、target_type/target_id/project_id、脱敏前后快照、reason、request_id、IP、User-Agent、时间、`prev_hash`、`record_hash`。

抗篡改机制：

- 哈希链按 `chain_scope` 分链：项目事件使用不可变 `project_id`，无项目的系统级事件使用固定 `SYSTEM`；每条记录保存 `chain_scope / sequence_no / key_version / prev_hash / record_hash`；
- `audit_chain_heads` 为每个 scope 保存 `last_sequence_no / last_hash / key_version`。首次建链先 `INSERT ... ON CONFLICT DO NOTHING`，随后在业务事务内 `SELECT ... FOR UPDATE` 锁定唯一链头；单个事务涉及多个 scope 时必须按 scope 字节序统一加锁，避免死锁；
- 锁定链头后分配 `sequence_no = last_sequence_no + 1`，将 `chainScope / sequenceNo / keyVersion / prevHash / eventPayload` 组成固定字段信封并使用 UTF-8、确定性 JSON（JCS）编码，再计算 `record_hash = HMAC-SHA256(对应版本密钥, JCS信封)`，不使用无长度边界的字符串拼接；
- 插入 audit_logs 后在同一事务更新链头；业务写、审计插入或链头更新任一失败均整体回滚。禁止先提交业务再补审计；
- 数据库设置 `UNIQUE(chain_scope, sequence_no)`、`UNIQUE(chain_scope, record_hash)`，并对非首条记录设置 `UNIQUE(chain_scope, prev_hash)`，防止重复序号和链分叉；
- 密钥只存在于 `/run/secrets/*`，不保存在数据库。轮换时先用旧密钥写入 `AUDIT_KEY_ROTATED` 标记（只含新版本号，不含密钥），下一条起使用新版本；旧密钥以只读方式保留用于历史校验；
- 集成测试必须覆盖至少 100 个同 scope 并发业务事务、不同 scope 并发、事务回滚、链头初始化竞争、密钥轮换和备份恢复，断言无分叉、无序号缺口且所有业务事件恰有一条审计（见 [ADR-008](docs/adr/ADR-008.md)）；
- 迁移所有者（migration owner）、审计写角色（audit_writer）、原始审计读角色（audit_reader）与归档角色分离；
- 普通 `app_runtime` 无 audit_logs 的 `SELECT / UPDATE / DELETE / TRUNCATE / ALTER / REFERENCES`；AuditWriteAdapter 在当前 UnitOfWork 内使用 audit_writer，后者仅可 INSERT audit_logs 及 SELECT/UPDATE audit_chain_heads；
- AuditQueryService 使用独立只读连接和 audit_reader；只有完成 AdminAuthorizationPort 的 MFA 与重认证校验后才能调用，普通业务 Repository 不能取得该连接；
- 任何原始审计读取本身写入 `AUDIT_LOG_READ`，记录操作人、时间、请求 ID、查询范围；
- 审计数据必须纳入备份；至少每小时将 `scope / last_sequence_no / last_hash / key_version / record_count / generated_at` 检查点写入支持对象锁/WORM 的远端存储，每日导出加密审计明细。归档凭据只允许新建对象，不允许覆盖或删除；
- 恢复演练同时校验数据库链、当前链头、最近远端检查点和归档明细；不一致时恢复环境不得上线；
- 不使用“通用审计触发器 + 应用审计”双写，避免重复与语义冲突；
- 项目动态由脱敏 `ActivityProjection` 提供，不得直接向普通成员开放原始审计前后快照。

**目标表述：** 上述机制用于“可追溯、可检测篡改”。在数据库所有者、宿主机或应用运行时被完全控制时，仍可能伪造本地记录；远端不可变检查点用于缩短未锚定窗口并检测事后重写，但不把该能力表述为绝对不可抵赖。

## 3.9 安全验收清单

- [ ] Argon2id 参数不低于 OWASP 当前基线，生产机器基准测试与登录限流通过；
- [ ] 系统管理员 MFA 受限注册、TOTP Secret 加密、恢复码、防重放、重置吊销会话与审计全部通过；
- [ ] 未完成 MFA 的管理员只能进入注册流程，不能访问业务接口；高风险管理员操作的密码 + TOTP 重认证生效；
- [ ] Session 存入数据库的是 Token 哈希，非原始 Token；
- [ ] `__Host-session` 未设置 Domain，Secure/HttpOnly/SameSite/Path 生效；
- [ ] Session 备份恢复语义与失效机制通过验证；
- [ ] 普通成员被移除后，旧 Session 立即失去成员关系派生的项目权限，但不影响其他项目；系统管理员仍保留全局权限；
- [ ] 接口级权限矩阵自动化测试全部通过；
- [ ] 匿名首次登录、登录后 Session/Token 轮换、页面刷新、多标签页与 Token 过期场景下的 CSRF 闭环通过；
- [ ] CSRF 同步 Token、Origin/Referer 缺失拒绝及 Fetch Metadata 策略通过测试；
- [ ] nonce CSP 在强制模式下生效且 script/style 均无 `unsafe-inline`；HSTS、frame-ancestors、Permissions-Policy、no-store、同源 CORS 生效；
- [ ] XSS、危险 Markdown、非 HTTPS/畸形 GitHub URL 与未来 SSRF 防护测试通过；
- [ ] 至少 100 个同 scope 并发业务事务、初始化竞争、回滚、密钥轮换和恢复后的审计链验证通过，链无分叉且审计读行为被记录（见 [ADR-008](docs/adr/ADR-008.md)）；
- [ ] 普通 app_runtime 无原始审计 SELECT；audit_writer 不能修改/删除历史记录；audit_reader 只读且只能由授权查询服务使用；
- [ ] 每小时远端不可变检查点、每日加密明细归档与恢复演练通过；
- [ ] 依赖漏洞扫描、镜像扫描以及生产 Secret 文件读取通过。

---
# 4. 交付质量约束

开发过程执行以下可验证门禁：

1. **架构基线先行**：阶段 0 必须跑通 monorepo、数据库迁移、Session/CSRF、Zod + Route Registry 的 OpenAPI/客户端生成、nonce CSP、中文搜索可行性 PoC 与容器构建；
2. **契约不漂移**：Zod 是数据 Schema 唯一真相，Route Registry 是 HTTP 操作元数据唯一真相；`openapi.yaml` 与客户端均为生成物，CI 检查 Registry/Controller 一一对应及生成物无未提交差异；
3. **依赖边界自动检查**：dependency-cruiser / ESLint 检查代码 import/Port 依赖图；数据库 FK 图由迁移测试检查。出现循环依赖、跨域写绕过 Workflow、跨模块 Repository 访问或 `features → pages` 即失败；
4. **事务边界可测试**：跨模块工作流必须通过 PostgreSQL 集成测试验证“全部成功或全部回滚”，并覆盖幂等、乐观锁与并发冲突；
5. **权限矩阵数据化**：接口 × 动作 × 资源状态 × 操作者的预期结果以测试数据形式维护，防止只写文档不执行；
6. **安全入测**：CSRF 首次登录/刷新/多标签、Session 哈希、成员即时移除、MFA 全生命周期、nonce CSP、HTTPS URL、XSS、SSRF、审计链并发与角色最小权限均纳入自动化测试；
7. **生产约束阻断**：非 root 镜像、固定 Nginx 1.30.x 补丁与 digest、独立迁移任务、资源限制、健康检查、备份加密、RPO/RTO、恢复演练、依赖漏洞扫描为上线阻断项；
8. **搜索双门禁**：阶段 0 用金标集验证中文可行性；阶段 4 按 5 年峰值容量和并发验证召回、响应时间与权限过滤，未达标不得声明“已支持中文检索”；
9. **人工把关**：数据库迁移、权限规则、认证配置、容器与编排变更必须人工评审；
10. **变更同步**：本文、技术设计、ADR、接口权限矩阵与测试矩阵同步更新，不允许只改代码不改设计。

---
# 5. 架构决策记录（ADR 引用）

ADR 的编号、状态、背景、决策与影响只在 [docs/adr/README.md](docs/adr/README.md) 及其链接的正式记录中维护。本文引用 ADR-001～ADR-026，不再重复保存决策全文。原系统设计中的 CSP 与 ExternalLinks 编号分别统一为 ADR-021、ADR-022；ADR-018～ADR-020 保留技术设计主线含义；一次性认证流程的幂等例外、迭代记录可审计恢复、搜索方案与 NestJS 版本基线定案分别顺延为 ADR-023、ADR-024、ADR-025、ADR-026。变更已接受决策时必须新增 ADR，不得复用或重排编号。

---
# 6. 模块职责与边界概览

## 6.1 模块职责表

| 模块 | 职责 | 本期核心能力 | 主要依赖 |
|---|---|---|---|
| IdentityModule | 用户、登录、Session、系统管理员标识 | 登录、会话失效、MFA | 无（基础模块） |
| ProjectsModule | 项目、项目成员；提供 ProjectAccessQueryPort；不创建默认模块 | 项目创建（数据聚合）、成员增删、归档 | IdentityQueryPort |
| ModulesModule | 模块分类、归档与恢复；未分类允许编辑名称/描述且 kind 不变 | 列表 / 创建 / 编辑 / 归档 / 恢复模块；普通读取含归档历史，高风险操作复用管理员重认证 | ProjectAccessQueryPort |
| FeaturesModule | 功能档案 | 模块内列表 / 详情 / 项目内关键词候选 / 创建 / 编辑当前说明及标签 / 管理员归档恢复；历史只读，恢复仅自身 | ProjectAccessQueryPort、ModuleQueryPort、ModuleReadPort、ProjectCodePort（编号基础设施）、FeatureCandidatesQueryPort、审计及活动/搜索公开写端口 |
| TasksModule | 任务、状态流转、影响功能 | 功能级 / 模块级任务、指派、完成 / 取消 / 恢复 | ProjectAccessQueryPort、ModuleQueryPort、FeatureQueryPort |
| TaskGroupsModule | 任务合并与分支；TaskGroupMember 是唯一归属及角色真相 | 合并、来源分支、解除合并 | TaskQueryPort |
| ChangeRecordsModule | 迭代记录状态机、版本、作废与可审计恢复；不访问其他领域 Repository | 三段式记录、草稿 / 发布 / 版本、作废 / 恢复 | ProjectAccessQueryPort、ModuleQueryPort、FeatureQueryPort、TaskQueryPort（仅发布校验） |
| ExternalLinksModule | HTTPS URL 规范化、链接实体、类型化关联、数据库唯一去重 | GitHub 多链接 | Database、Common；目标 QueryPort 仅由 Workflow 调用 |
| WorkflowModule | 跨域组合命令编排与统一 UnitOfWork | 项目创建、任务完成+记录发布、遗留问题转任务、外部链接关联、合并后副作用 | 领域 Command/Query Port + 横切 WritePort |
| SearchProjectionModule | 规范化搜索投影写入；SearchQueryService 负责检索 | 全局搜索 | 写：Database、Common；读：ProjectAccessQueryPort |
| ActivityProjectionModule | 脱敏项目动态写入；ActivityQueryService 负责读取 | 项目动态 | 写：Database、Common；读：ProjectAccessQueryPort |
| NotificationsModule | 通知写入、未读、已读 | 基础通知 | Database、Common |
| AuditModule | 审计写入、并发安全哈希链、远端锚定、原始审计查询 | 操作审计 | 写：audit_writer；读：audit_reader + AdminAuthorizationPort |
| HealthModule | 存活 / 就绪检查 | 运维 | Database |

## 6.2 边界约定

1. 模块对外只暴露 Application Service 或 Port；其他模块不得直接访问其 Repository；
2. 单域命令由所属领域模块完成；跨模块命令由 WorkflowModule 统一编排；
3. 审计、通知、活动、搜索投影写入均以 WritePort 注入；Search/Activity 读取通过 QueryService + ProjectAccessQueryPort，项目动态不直接开放原始审计；
4. 本期所有本地审计、通知、活动、搜索投影与业务聚合在**同一个数据库事务**中写入，提交后前端轮询读取，不引入异步投影；远端审计归档只读取已提交数据，不参与业务事务；
5. `POST /projects` 的“创建项目 + 初始成员 + 未分类模块”由 ProjectBootstrapWorkflow 完成；
6. `POST /tasks/{taskId}/complete` 的“完成任务 + 创建/发布记录 + 审计 + 通知 + 活动 + 搜索投影”由 TaskCompletionWorkflow 完成；
7. 遗留问题转任务由 Workflow 调用 ChangeRecords 读取与 Tasks 创建，ChangeRecords 不依赖 Tasks Repository 或写服务；
8. GitHub 链接只接受 HTTPS；ExternalLinkWorkflow 验证目标归属，类型化关联表通过 `project_id` 复合外键防止跨项目串联，并通过 UNIQUE + upsert 去重；
9. 搜索只读专用投影，不拼装各领域内部的表；投影由 Workflow 与业务命令在同一事务内维护；
10. 前端约束：`app → pages → features → shared / generated`，features 不得引用 pages。

## 6.3 核心命令接口（设计级）

下表路径均相对于 `/api/v1`：

| 接口 | 用途 | 关键约束 |
|---|---|---|
| `GET /auth/csrf` | 匿名或已登录 Session 签发同步 CSRF Token | 响应 no-store；数据库只存哈希；登录成功轮换 Session 与全部 Token |
| `GET /users` | 读取启用用户目录，供创建项目时选择初始成员 | 仅返回 `id`、`name`、`avatarUrl`、`isAdmin`；只返回启用且未停用用户，最多 300 条；响应 no-store；不返回登录名、邮箱或凭据字段 |
| `POST /projects` | 创建项目 | Workflow 单事务完成项目、成员、未分类模块、审计、通知、活动与搜索投影 |
| `POST /tasks` | 创建任务 | 归属校验，负责人必须是项目活跃成员 |
| `POST /tasks/{taskId}/complete` | 完成任务（含可选记录） | 请求携带 `mode`；`WITH_RECORD` 时传 `recordDraftId` 或内联 `record`，由 TaskCompletionWorkflow 单事务完成 |
| `POST /tasks/{taskId}/cancel`、`/reopen` | 取消 / 恢复任务 | 保留历史，不物理删除 |
| `POST /task-groups/merge` | 合并任务 | 同项目、禁止循环、唯一活跃组 |
| `POST /task-groups/{groupId}/members/{memberId}/detach` | 解除合并 | 仅允许活跃来源分支，记录原因；`memberId` 为来源任务成员标识 |
| `POST /change-records/{id}/publish` | 发布独立记录或已完成任务所关联的记录 | 若记录关联未完成任务，返回 409，提示使用任务完成组合命令；不允许客户端分两步保证一致性 |
| `POST /change-records/{id}/versions` | 修改已发布记录 | 生成新版本，旧版本保留 |
| `POST /change-records/{id}/leftover-task` | 遗留问题转任务 | 由 Workflow 编排，幂等，防止重复建任务 |
| `POST /change-records/{id}/void` | 作废记录 | 仅系统管理员，需重认证，保留作废原因 |
| `POST /change-records/{id}/restore` | 恢复已作废记录 | 仅系统管理员，需重认证、原因与预期版本；按 [ADR-024](docs/adr/ADR-024.md) 恢复为 PUBLISHED 并保留作废历史与正式版本 |
| `GET /projects/{projectId}/activity` | 项目动态 | 读取脱敏 ActivityProjection，带项目权限过滤 |
| `GET /audit-logs` | 原始审计查询 | AuditQueryService 使用 audit_reader；仅系统管理员且需重认证 + MFA；读取行为通过 audit_writer 产生 `AUDIT_LOG_READ` |
| `GET /search` | 全局搜索 | 读搜索投影，权限过滤、分页 |
| `GET /notifications` | 通知轮询 | 游标增量拉取 |

所有 POST、PUT、PATCH、DELETE 等非安全写接口默认必须在 Route Registry 登记 `idempotencyRequired` 并支持幂等键；普通 GET、HEAD 与纯校验接口登记 `none`。一次性认证材料的签发/消费流程按 [ADR-023](docs/adr/ADR-023.md) 登记 `securityFlow`（`GET /auth/csrf` 的 `issueCsrfToken` 属此例外），禁止通过通用幂等记录保存或重放 Secret；其他例外必须经新 ADR 评审。`idempotencyRequired` 的摘要必须覆盖幂等契约版本及所有影响命令语义的 path、query、Content-Type、body 和请求头，使用版本头的路由必须包含 `If-Match`，摘要以版本化专用 HMAC 密钥计算；每个可缓存 2xx 响应还必须登记带版本的互斥 `body`/`noBody` 重放策略和结果资源授权策略，通用记录只重放允许的 body，不保存或重放响应头。请求、安全重放或授权策略变化必须升级契约版本；重放前重新验证当前认证、原操作权限、缓存响应所指每个资源的当前读取权限与所需高风险重认证新鲜度。涉及聚合根更新的接口携带期望版本号，冲突返回 409。请求/响应数据校验来自 Zod Schema，HTTP 操作元数据来自 Route Registry；OpenAPI 与前端客户端由二者共同生成。详见 [ADR-019](docs/adr/ADR-019.md)。

---
# 7. 本期开发阶段与验收要点

## 7.1 阶段划分

| 阶段 | 内容 | 对应本期能力 |
|---|---|---|
| 阶段 0 架构跑通 | monorepo、版本基线、Zod + Route Registry、迁移、Session/CSRF、nonce CSP、搜索可行性 PoC、容器构建 | 工程基座 |
| 阶段 1 项目骨架 | 用户与角色、项目与成员、模块与功能、权限矩阵、审计基础设施 | 登录、角色、项目、成员、模块、功能 |
| 阶段 2 任务与记录 | 任务、状态流转、三段式记录、草稿 / 发布 / 版本 / 作废 / 恢复、遗留问题转任务 | 任务执行、迭代记录 |
| 阶段 3 合并与历史 | 任务组、主任务与来源分支、解除合并、历史保留 | 合并、来源分支、历史查询 |
| 阶段 4 搜索与运维 | 全局搜索峰值容量验收、动态、通知、审计展示、远端锚定、生产运维、恢复演练 | 搜索、动态、通知、审计 |

## 7.2 各阶段验收要点

### 阶段 0

- 主版本与补丁版本、基础镜像 digest 已固定并可复现构建；Nginx 使用已批准的 1.30.x 补丁版本；
- NestJS 12 的 ESM、CLI、核心插件与构建链兼容性验证已完成，ADR-026 明确本期继续暂留 11.x、锁定 11.2.3 及后续升级触发条件；
- Zod 数据 Schema + Route Registry → OpenAPI → TypeScript Client 生成链路跑通，Registry/Controller 一一对应，CI 检查无漂移；
- 空数据库可完成全部迁移，应用启动不自动执行迁移；
- 匿名 CSRF 签发 → 登录 → Session/Token 轮换 → 页面刷新重新签发全链路通过；Session 与 CSRF 数据库均仅存 Token 哈希；
- nonce CSP 在强制模式下通过 AntD/Vite E2E，script/style 均无 `unsafe-inline`；安全响应头、CORS 同源与可信代理策略通过验证；
- dependency-cruiser / ESLint 代码依赖规则生效，数据库 FK 图由迁移测试验证；
- PGroonga PoC 已完成 V1 语义、Recall@20、跨项目、边界与特殊输入验证；PostgreSQL 18.6 探针镜像的构建、扩展、`0003-0005` 迁移与逐迁移回滚、旧 `pg_trgm` contract 清理、默认计划和逻辑恢复也已通过；`search_projection` PGroonga bootstrap 与 `0003-0005` 迁移已通过真实 PostgreSQL 集成测试，SearchQueryService 服务层与真实 PostgreSQL 权限过滤测试已落地，生产适配器、API 与生产加密备份恢复仍待阶段 0 完成。
- Compose 使用非 root 容器、独立迁移任务、健康检查、资源限制、Secrets 文件与固定 digest。

### 阶段 1

- 系统管理员、项目成员两类角色权限生效；
- 创建项目、选择初始成员、创建表单中创建者不可取消、未分类模块在同一事务内完成；创建后管理员可移除创建者成员关系，但 `projects.created_by` 不变且不授予额外权限；
- 非项目成员无法访问项目及其下级数据；普通成员被移除后立即失去成员关系派生的项目权限，但不影响其他项目；系统管理员仍保留全局权限；
- 系统管理员受限 MFA 注册、Secret 加密、恢复码、防重放、重置吊销 Session 与高风险密码 + TOTP 重认证可用；
- 项目、模块、功能可创建、编辑、归档，归档后禁止新建下级数据；
- 接口级权限矩阵自动化测试通过，审计日志运行时角色权限正确。

### 阶段 2

- 功能级 / 模块级任务创建与归属正确，模块级任务可关联多个影响功能；
- 指派、完成、取消、恢复状态流转正确，历史可查询；
- `POST /tasks/{taskId}/complete` 支持“仅完成”和“完成+记录”，同事务失败则全部回滚；
- 三段式记录必填校验生效，版本、草稿、发布、作废与恢复规则正确；
- 遗留问题一键转任务幂等，重复点击不会创建重复任务；
- 审计哈希链、项目动态脱敏投影与通知同事务写入验证通过。

### 阶段 3

- 任务合并形成主任务 + 来源分支，不复制、不删除原任务与记录；
- 已有正式迭代记录的任务仍可合并；
- 解除合并恢复独立展示，原状态、负责人、记录不变；
- 并发合并同一任务只能成功一次；
- TaskGroupMember 是组归属与 MAIN/SOURCE 角色唯一真相，数据库拒绝第二个主任务及同一任务加入第二个活跃组；
- 依赖图检查确认无 Projects/Modules、Tasks/ChangeRecords 循环依赖；
- GitHub 仅接受 HTTPS；类型化链接关联的跨项目 FK 与并发去重约束通过数据库测试。

### 阶段 4

- 全局搜索按 5 年容量模型 1.2 倍且不少于 100,000 条投影、30 并发与固定金标集通过召回、响应时间和权限过滤指标；
- 项目动态与操作审计可查询；普通成员只能看脱敏动态，管理员查看原始审计需重认证 + MFA；
- 通知对象、未读数和跳转正确；
- 搜索、通知、备份、恢复、升级回滚在生产环境验证通过；
- 每小时远端 WORM 检查点、每日加密审计明细、并发无分叉哈希链校验与恢复演练通过；
- 依赖漏洞扫描、镜像扫描、安全响应头、生产 Compose 约束全部通过。

## 7.3 上线整体验收

- **业务闭环**：创建项目 → 模块 / 功能 → 任务 → 指派 → 完成（含可选记录）→ 版本修订 → 遗留问题转任务 → 合并 / 解除 → 搜索、通知、活动、审计全流程走通；
- **数据一致**：跨项目数据无法串联，组合命令失败全部回滚，所有要求幂等的写接口重放不产生重复数据，并发冲突返回明确结果；
- **安全达标**：第 3.9 节安全验收清单全部通过，接口级权限矩阵测试全部通过；
- **交付质量**：类型检查、单测、集成测试、E2E、契约漂移、依赖边界、构建与安全扫描全部通过；
- **可运维**：镜像与编排可重复部署，备份可恢复，RPO/RTO 达标，审计归档可校验。

---
# 8. 相关文档索引

| 文档 | 说明 |
|---|---|
| [《功能设计 V1.1》](功能设计v1.1.md) | 业务对象、业务规则、流程、交互与验收标准 |
| [《技术设计 V1.2.2》](技术设计v1.2.2.md) | 数据库设计、事务处理、部署运维、测试与 CI、AI 协作规范（与本文同步） |
| [ADR 索引](docs/adr/README.md) | 架构决策编号、状态、背景、决策和影响的唯一权威来源 |
| [权限矩阵](docs/permissions.md) | 身份、操作、响应与高风险条件的权威基线 |
| [测试矩阵](docs/test-matrix.md) | 各阶段必须实现的自动化验证和验收标准 |
| 《技术选型对比分析》 | TypeScript / Python / Java 多方案对比、权重评分与边界条件 |
| 《系统设计审核整改对照表》 | 审核意见逐条映射、整改决定与文档落点 |
| 《系统设计文档 V1.0》 | 首轮审核前版本，仅作历史对照 |
| 《系统设计文档 V1.0.1》 | 首轮整改版本，仅作历史对照，不再作为开发基线 |
| 本文（系统设计 V1.0.2） | 第二轮整改后的技术选型、模块划分与系统安全候选开发基线 |

## F-14 功能级任务实施补充（2026-09-09）

F-14（2026-09-09）本地实现提供功能级任务 create/edit/read 纵切片；不代表上述任务状态流转和模块级影响关系已经实现。TasksManagementService 在 IdempotencyHttpService 提供的单一事务内复用公开 ProjectAccessQueryPort、ProjectCodePort、ProjectMembersQueryPort、ModuleQueryPort、FeatureQueryPort 和 FeatureReadPort；不访问其他领域内部 Repository。ProjectMembersQueryPort 在同一 tx 重新授权成员列表，真正指派时按用户→成员锁定并复查 ACTIVE；历史未变负责人不重新当作新指派校验。任务、初始状态历史、审计、活动、搜索和指派通知一起提交或回滚。记录/任务组关联读取仍待对应领域提供公开能力。

## F-15 模块级任务实施补充（2026-09-09）

F-15 复用 F-14 TasksManagementService/Repository/HTTP/前端任务面板，新增 MODULE scope 的五个读取/创建/编辑 operationId，模块页管理同模块多影响功能，功能页以 EXISTS 当前关系展示单份引用。模块任务真实父级为项目和模块；归档影响功能的已有关系可保留/移除，新加或重加要求 ACTIVE。关系变更与任务版本、审计、活动、搜索和必要指派通知同事务。

task_feature_impacts 是当前集合，其删除权限仅用于本任务明确移除的当前关系；完整前后身份、relation_type、created_at 及增删差异保存在不可变审计，任务及状态/审计历史不删除。现有复合 FK、关系 PK 与 0002 的 MODULE scope 触发器兜底，没有新增迁移或放宽权限。新增模块页面/联合读取 DTO/生成客户端需要 C 复核，详见 [F-15 交审](docs/f15-local-handoff.md)。本补充不声明任务组、完成/取消/重开或迭代记录能力已实现。
