# 软件研发功能迭代记录与任务协作系统
## V1.2 技术设计方案

**文档状态：** 建议实施版  
**对应产品文档：** 《功能设计 V1.1》  
**目标团队：** 1～3 人，以 AI 编码代理为主要生产力，人工负责架构、评审与验收  
**目标部署：** 国内、单企业、单实例，预计 10～300 名用户  

---

# 0. V1.2 核心结论

本系统采用**模块化单体 + 前后端分离 SPA + 单一关系数据库**，V1.2 不引入微服务、Redis、Elasticsearch、WebSocket 和 Kubernetes。

推荐技术栈：

| 层次 | 选择 | 决策说明 |
|---|---|---|
| 主语言 | TypeScript，严格模式 | 前后端统一；编译反馈适合 AI 迭代 |
| Monorepo | pnpm workspace | 单仓、单 lockfile、统一脚本 |
| 前端 | React + Vite | 内部重交互 SPA，无 SSR/SEO 需求 |
| UI | Ant Design | 表单、表格、树、抽屉和弹窗覆盖完整 |
| 前端数据 | TanStack Query | 服务端数据缓存、失效和重试统一 |
| 前端表单 | React Hook Form + Zod | 表单状态与运行时校验 |
| 后端 | NestJS，默认 Express Adapter | 模块、依赖注入、Guard、Pipe、Interceptor 约定明确；Session 生态成熟 |
| API | REST JSON，`/api/v1` | 明确的外部边界，便于测试和后续集成 |
| API 契约 | OpenAPI 3.1 + 生成式 TypeScript Client | 线上的 HTTP 契约是唯一真相，不直接复用数据库实体 |
| 数据库 | PostgreSQL 18 | 事务、约束、行锁、JSONB、`pg_trgm` |
| 数据访问 | Drizzle ORM + 显式 SQL 迁移 | 类型安全，同时保留 SQL、锁和索引控制力 |
| 认证 | 服务端 Session + HttpOnly Cookie | 支持主动失效，适合内部系统 |
| Session Store | PostgreSQL | V1.2 不为 Session 单独引入 Redis |
| 审计 | 应用层业务审计 + 数据库只追加保护 | 保存业务语义，避免触发器与应用双写 |
| 通知 | 数据库通知表 + 轮询 | V1.2 先保证可靠性；后续可升级 SSE |
| 搜索 | PostgreSQL `pg_trgm` | 不依赖第三方中文分词镜像；规模扩大后再评估分词插件 |
| 测试 | Vitest + PostgreSQL 集成测试 + Playwright | 覆盖逻辑、真实约束、并发与关键用户路径 |
| 部署 | 多阶段镜像 + Docker Compose + Nginx | 可重复构建，无宿主机前端产物依赖 |
| 日志 | pino 输出 stdout | 由 Docker 日志驱动轮转和采集 |

## 0.1 版本策略

技术设计只规定受支持的主版本基线，不使用 `5.7+` 这类不可复现写法：

1. Node.js 使用当前 Active LTS，初始基线为 Node.js 24；
2. React 使用 19.x；
3. Zod 使用 4.x；
4. PostgreSQL 使用 18.x；
5. 其他依赖在项目初始化时选择兼容的稳定版本；
6. `pnpm-lock.yaml` 必须提交；生产镜像必须使用 frozen lockfile；
7. Renovate/Dependabot 只创建升级 PR，不自动合并；
8. 每次升级必须通过类型检查、集成测试、E2E 和生产构建。

## 0.2 V1.1 技术方案的主要调整

1. 后端由 Hono 调整为 NestJS，换取更强的工程约定和成熟的认证/鉴权集成；
2. 不把共享 TypeScript 实体当作 HTTP 契约，改为 OpenAPI 生成客户端；
3. 重构任务聚合模型，删除三处重复的 `task_group_id/main_task_id` 真相；
4. 数据库增加 `NOT NULL`、状态 `CHECK`、复合外键和部分唯一索引；
5. 乐观锁改为“条件更新 + 版本递增”的原子实现；
6. Session 改为可持久化、可轮换、可吊销的完整方案；
7. 资源权限根据资源真实归属判断，不信任客户端传入的 `projectId`；
8. 审计只由应用层写业务事件，数据库负责禁止修改和删除；
9. 事务内只写数据库通知/outbox，网络推送必须在提交后执行；
10. Compose 增加构建、健康检查、迁移任务、Secrets 接线和正确缓存策略；
11. 备份以“能够恢复”为验收标准，而不以“生成了备份文件”为标准；
12. AI 规范从提示建议升级为仓库规则和 CI 强制门禁。

---

# 1. 架构目标与非目标

## 1.1 架构目标

按优先级排序：

1. **数据不丢、不串项目、不产生半完成状态；**
2. **任务、迭代记录、合并关系和审计可追溯；**
3. **并发冲突可检测并返回明确的 409；**
4. **AI 生成代码可被类型、数据库约束和测试快速验证；**
5. **单机部署、备份、升级和回滚过程可重复；**
6. **1～3 人能够长期维护，不依赖某个个人的隐含知识。**

## 1.2 非目标

V1.2 不建设：

- 微服务；
- 多租户平台；
- Kubernetes；
- Redis 缓存；
- Elasticsearch；
- WebSocket 实时协作；
- Event Sourcing；
- CQRS 框架；
- 自研权限 DSL；
- GitHub API/Webhook 深度集成；
- 附件对象存储。

## 1.3 质量属性

| 属性 | V1.2 目标 |
|---|---|
| 一致性 | 核心业务命令使用单数据库事务 |
| 可用性 | 单实例可维护窗口；不承诺高可用 |
| 性能 | 常用列表 P95 API 响应小于 500ms，复杂搜索小于 1s（在验收数据集上） |
| 安全 | 后端强制鉴权；防 IDOR、CSRF、XSS；密码和 Session 安全存储 |
| 可恢复 | 每日备份；每月至少一次自动或人工恢复演练 |
| 可观测 | 请求 ID、结构化日志、健康检查、慢查询分析 |
| 可维护 | 模块化单体；禁止跨模块直接访问内部 Repository |

性能数字是初始 SLO，需要通过基准数据集验证，不根据“用户数”直接推断服务器规格。

---

# 2. 总体架构

## 2.1 运行时拓扑

```text
浏览器
  │ HTTPS
  ▼
Nginx
  ├── /assets/*  -> React 静态资源
  ├── /          -> index.html
  └── /api/v1/*  -> NestJS API
                         │
                         ├── PostgreSQL（业务数据、Session、通知、Outbox）
                         └── GitHub（V1.2 仅解析链接，不主动请求）
```

生产环境只有 Nginx 暴露宿主机端口。API 和 PostgreSQL 只在 Compose 内部网络可见。

## 2.2 后端分层

```text
Controller
  ↓ 解析 HTTP、调用 Pipe/Guard、映射响应
Application Service
  ↓ 权限、状态机、事务、幂等、业务审计
Domain Policy
  ↓ 可复用的纯业务判断
Repository
  ↓ Drizzle/SQL、锁、持久化
PostgreSQL
  ↓ CHECK/FK/UNIQUE 作为最终防线
```

约束：

- Controller 不直接访问数据库；
- Repository 不决定业务状态流转；
- Application Service 不拼接原始 SQL；
- 模块对外只暴露 Application Service；
- 外部网络请求不得发生在数据库事务内；
- 跨模块写操作由一个上层 Application Service 编排。

## 2.3 模块划分

```text
IdentityModule           用户、登录、Session
ProjectsModule           项目、项目成员、未分类模块
ModulesModule            模块
FeaturesModule           功能、当前说明、归档
TasksModule              任务、状态、影响功能
TaskGroupsModule         合并、来源分支、解除合并
ChangeRecordsModule      迭代记录、发布、版本、作废
ExternalLinksModule      GitHub 链接解析与管理
SearchModule             全局搜索
NotificationsModule      站内通知与读取状态
AuditModule              业务审计
OutboxModule             提交后副作用
HealthModule             liveness/readiness
```

这是模块化单体。除非未来出现独立扩缩容、独立故障域或独立团队边界，不拆微服务。

---

# 3. 仓库结构

```text
project-root/
├── apps/
│   ├── api/
│   │   ├── src/
│   │   │   ├── modules/
│   │   │   ├── common/
│   │   │   │   ├── auth/
│   │   │   │   ├── errors/
│   │   │   │   ├── http/
│   │   │   │   └── observability/
│   │   │   ├── database/
│   │   │   ├── main.ts
│   │   │   └── app.module.ts
│   │   ├── test/
│   │   └── package.json
│   └── web/
│       ├── src/
│       │   ├── app/
│       │   ├── features/
│       │   ├── pages/
│       │   ├── shared/
│       │   └── generated/api/
│       ├── e2e/
│       └── package.json
├── packages/
│   ├── api-contract/         OpenAPI 文档与客户端生成配置
│   ├── domain-constants/     仅共享稳定枚举和纯常量
│   ├── eslint-config/
│   ├── tsconfig/
│   └── test-utils/
├── database/
│   ├── schema/
│   ├── migrations/
│   └── seeds/
├── infra/
│   ├── api.Dockerfile
│   ├── web.Dockerfile
│   ├── nginx.conf
│   └── compose.yaml
├── scripts/
│   ├── backup/
│   ├── restore/
│   └── smoke/
├── docs/
│   ├── architecture/
│   ├── adr/
│   └── runbooks/
├── AGENTS.md
├── pnpm-workspace.yaml
├── pnpm-lock.yaml
├── package.json
└── README.md
```

禁止建立万能 `shared/utils` 垃圾目录。共享包只放确实被两个应用使用、语义稳定且无环境副作用的内容。

---

# 4. API 与类型契约

## 4.1 契约原则

1. API 使用 `/api/v1` 前缀；
2. 请求 DTO、响应 DTO、数据库实体三者分离；
3. OpenAPI 是跨进程 HTTP 契约；
4. 前端客户端从 OpenAPI 自动生成，不手写 URL 和响应类型；
5. 前端表单 Zod Schema 可以复用生成类型，但不直接导入数据库 Schema；
6. 日期统一使用 ISO 8601 UTC 字符串；
7. ID 在当前规模下使用 PostgreSQL `INTEGER IDENTITY`，JSON 中为 number；
8. 金额等精确数值如果后续出现，API 中使用字符串，不使用浮点数；
9. OpenAPI 生成结果必须在 CI 中检查无未提交差异。

## 4.2 统一错误模型

```json
{
  "code": "TASK_VERSION_CONFLICT",
  "message": "该任务已被其他用户更新，请刷新后重试",
  "details": {},
  "requestId": "01J..."
}
```

| HTTP 状态 | 使用场景 |
|---|---|
| 400 | 请求格式或业务参数错误 |
| 401 | 未登录或 Session 失效 |
| 403 | 已登录但无全局权限；必要时资源接口统一返回 404 防止枚举 |
| 404 | 资源不存在或当前用户不可访问 |
| 409 | 版本冲突、重复操作、状态冲突 |
| 422 | 字段校验错误 |
| 429 | 登录或敏感接口触发限流 |
| 500 | 未预期错误，不向客户端泄露堆栈 |

## 4.3 关键命令接口

```text
POST /api/v1/projects
POST /api/v1/tasks
POST /api/v1/tasks/{taskId}/complete
POST /api/v1/tasks/{taskId}/cancel
POST /api/v1/tasks/{taskId}/reopen
POST /api/v1/task-groups/merge
POST /api/v1/task-groups/{groupId}/members/{taskId}/detach
POST /api/v1/change-records/{recordId}/publish
POST /api/v1/change-records/{recordId}/versions
POST /api/v1/change-records/{recordId}/leftover-task
POST /api/v1/change-records/{recordId}/void
```

所有修改接口必须携带：

- `X-Request-Id`：链路跟踪；
- `Idempotency-Key`：完成、合并、发布、转任务等关键命令；
- `expectedRowVersion` 或 `If-Match`：更新已有聚合根时。

幂等表保存 `(actor_id, command_type, idempotency_key)` 唯一键、请求摘要、执行状态和结果引用。相同 key、不同请求摘要返回 409。

---

# 5. 数据库设计

## 5.1 通用规则

1. 主键使用 `INTEGER GENERATED BY DEFAULT AS IDENTITY`；
2. 业务必填字段统一 `NOT NULL`；
3. 状态字段使用 `TEXT + CHECK`，避免无约束 VARCHAR；
4. `created_at/updated_at` 使用 `TIMESTAMPTZ NOT NULL`；
5. 所有跨层级关系必须在数据库中防止跨项目串联；
6. 业务历史默认软归档/作废，不通过级联物理删除清理；
7. 核心聚合根拥有 `row_version INTEGER NOT NULL DEFAULT 1`；
8. 数据库迁移文件必须人工评审，禁止应用启动时自动生成 Schema；
9. 迁移使用 expand/contract 原则，避免代码回滚后无法读取新 Schema。

## 5.2 核心关系结构

```text
Project
├── ProjectMember
├── Module
│   ├── Feature
│   ├── Task
│   │   └── TaskFeatureRelation
│   └── ChangeRecord
│       ├── ChangeRecordVersion
│       └── ChangeRecordFeatureRelation
└── TaskGroup
    └── TaskGroupMember（MAIN / SOURCE，唯一关系真相）

Task / Feature / ChangeRecord / Project
└── ExternalLink

业务命令
├── AuditLog
├── Notification
└── OutboxEvent
```

## 5.3 层级一致性约束

以下 SQL 表达约束意图，Drizzle Schema 和迁移 SQL 必须生成等价约束：

```sql
CREATE TABLE modules (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  name TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'ARCHIVED')),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, project_id)
);

CREATE TABLE features (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL,
  module_id INTEGER NOT NULL,
  code TEXT NOT NULL,
  name TEXT NOT NULL,
  current_behavior TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'ARCHIVED')),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (project_id, code),
  UNIQUE (id, project_id),
  UNIQUE (id, module_id, project_id),
  FOREIGN KEY (module_id, project_id)
    REFERENCES modules(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE tasks (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL,
  module_id INTEGER NOT NULL,
  feature_id INTEGER,
  source_change_record_id INTEGER,
  scope_type TEXT NOT NULL
    CHECK (scope_type IN ('FEATURE', 'MODULE')),
  code TEXT NOT NULL,
  title TEXT NOT NULL CHECK (length(title) BETWEEN 1 AND 500),
  description TEXT NOT NULL DEFAULT '',
  assignee_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  creator_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  priority TEXT NOT NULL DEFAULT 'NORMAL'
    CHECK (priority IN ('LOW', 'NORMAL', 'HIGH', 'URGENT')),
  work_status TEXT NOT NULL DEFAULT 'TODO'
    CHECK (work_status IN ('TODO', 'DONE', 'CANCELED')),
  lifecycle_status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (lifecycle_status IN ('ACTIVE', 'ARCHIVED', 'INVALID')),
  completion_note TEXT,
  completed_at TIMESTAMPTZ,
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (project_id, code),
  UNIQUE (id, project_id),
  UNIQUE (id, module_id, project_id),
  FOREIGN KEY (module_id, project_id)
    REFERENCES modules(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (feature_id, module_id, project_id)
    REFERENCES features(id, module_id, project_id) ON DELETE RESTRICT,
  CHECK (
    (scope_type = 'FEATURE' AND feature_id IS NOT NULL)
    OR
    (scope_type = 'MODULE' AND feature_id IS NULL)
  ),
  CHECK (
    (work_status = 'DONE' AND completed_at IS NOT NULL)
    OR
    (work_status <> 'DONE' AND completed_at IS NULL)
  )
);
```

注意：给任务增加 `source_change_record_id` 时需要在 `change_records` 创建后通过 `ALTER TABLE` 添加外键，避免循环建表顺序问题。也可以改为独立的 `task_origins` 关系表。

## 5.4 项目成员历史

项目成员关系保留加入/移除历史：

```sql
CREATE UNIQUE INDEX ux_project_member_active
ON project_members(project_id, user_id)
WHERE status = 'ACTIVE';
```

重新加入项目时新增一条记录，而不是覆盖上一次 `joined_at/removed_at`。项目创建者不可移除由 Application Service 校验，并通过集成测试保证。

## 5.5 任务聚合模型

删除以下冗余字段：

- `tasks.task_group_id`；
- `task_groups.main_task_id`。

统一使用：

```sql
CREATE TABLE task_groups (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  name TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'CLOSED')),
  created_by INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, project_id)
);

CREATE TABLE task_group_members (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id INTEGER NOT NULL,
  task_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('MAIN', 'SOURCE')),
  mode TEXT NOT NULL CHECK (mode IN ('ACTIVE', 'HISTORICAL')),
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'DETACHED')),
  original_work_status TEXT,
  original_assignee_id INTEGER REFERENCES users(id) ON DELETE RESTRICT,
  joined_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  detached_at TIMESTAMPTZ,
  FOREIGN KEY (group_id, project_id)
    REFERENCES task_groups(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (task_id, project_id)
    REFERENCES tasks(id, project_id) ON DELETE RESTRICT,
  CHECK (role = 'SOURCE' OR mode = 'ACTIVE'),
  CHECK (
    (status = 'ACTIVE' AND detached_at IS NULL)
    OR
    (status = 'DETACHED' AND detached_at IS NOT NULL)
  )
);

CREATE UNIQUE INDEX ux_task_one_active_group
ON task_group_members(task_id)
WHERE status = 'ACTIVE';

CREATE UNIQUE INDEX ux_group_one_active_main
ON task_group_members(group_id)
WHERE status = 'ACTIVE' AND role = 'MAIN';
```

规则：

1. 部分唯一索引保证活跃组至多一个 MAIN；创建/关闭事务和集成测试保证活跃组至少一个 MAIN；
2. 一个任务最多属于一个活跃组；
3. SOURCE 与 MAIN 必须同项目；
4. V1.2 不允许组嵌套；
5. 最后一个 SOURCE 解除后，组自动 CLOSED，MAIN 成员转为 DETACHED；
6. 解除仅改变关系，不改变任务原 `work_status`；
7. 同一任务以后可以重新加入组，因此不对历史行建立永久唯一约束。

## 5.6 迭代记录版本

所有正式版本，包括 v1，都写入 `change_record_versions`：

```text
change_records：身份、归属、状态、current_version、当前内容投影
change_record_versions：每个版本不可变的完整快照
```

更新已发布记录时：

1. `SELECT ... FOR UPDATE` 锁定记录；
2. 校验 `expectedRowVersion/currentVersion`；
3. 只接收允许修改的内容 DTO，禁止 `Partial<DatabaseEntity>`；
4. 插入新版本 `current_version + 1`；
5. 更新当前内容投影、`current_version` 和 `row_version`；
6. 写业务审计；
7. 提交。

唯一约束：

```sql
UNIQUE (record_id, version_no)
```

## 5.7 外部链接

不使用无法建立外键的 `(owner_type, owner_id)` 多态关系。V1.2 使用显式可空外键并要求恰好一个归属：

```text
external_links
  project_id NOT NULL
  task_id NULL
  feature_id NULL
  change_record_id NULL
  project_owner_id NULL
```

通过 CHECK 保证四个 owner 字段恰好一个非空，并通过带 `project_id` 的复合外键保证不跨项目。URL 需要唯一性策略，至少避免同一对象重复添加完全相同 URL。

## 5.8 编号生成

不为每个项目动态创建数据库 Sequence。使用单表原子 UPSERT：

```sql
INSERT INTO code_sequences(project_id, entity_type, last_number)
VALUES ($1, $2, 1)
ON CONFLICT (project_id, entity_type)
DO UPDATE SET last_number = code_sequences.last_number + 1
RETURNING last_number;
```

编号只保证唯一和单调，不承诺无空洞。事务回滚、数据修复和未来导入均可能产生跳号。

---

# 6. 关键业务事务

## 6.1 通用事务规则

- 事务默认使用 PostgreSQL `READ COMMITTED`；
- 修改聚合根使用原子条件更新或 `SELECT FOR UPDATE`；
- 事务内不得调用 GitHub、邮件、Webhook、WebSocket 等网络服务；
- 事务内不得执行长时间 CPU 工作；
- 唯一约束冲突必须映射为明确的 409 业务错误；
- 所有关键命令必须支持幂等 key；
- 数据库异常不得原样返回客户端。

## 6.2 创建项目

同一事务完成：

1. 校验项目编码；
2. 创建项目；
3. 创建者加入项目；
4. 批量加入初始成员；
5. 创建“未分类模块”；
6. 写审计日志；
7. 创建站内通知和 outbox 事件。

若任一成员无效、停用或重复，整个操作回滚。创建者永远在初始成员列表中。

## 6.3 完成任务并发布迭代记录

事务顺序：

```text
幂等请求占位
  ↓
按 taskId 查询任务并校验当前用户项目权限
  ↓
原子更新任务：
  WHERE id = ?
    AND row_version = ?
    AND work_status = 'TODO'
    AND lifecycle_status = 'ACTIVE'
  SET work_status = 'DONE',
      completed_at = now(),
      row_version = row_version + 1
  ↓
生成迭代记录编号
  ↓
创建 change_record
  ↓
创建 change_record_version v1
  ↓
写 audit_log
  ↓
写 notification；如需提交后副作用则写 outbox_event
  ↓
保存幂等结果并提交
```

条件更新影响 0 行时，重新读取并区分：不存在/无权限、版本冲突、状态冲突。

“无功能变化直接完成”使用同一状态条件，但不创建迭代记录，必须保存 `completion_note`。

## 6.4 任务合并

事务内：

1. 幂等请求占位；
2. 按固定 ID 顺序锁定 source 和 main，避免死锁；
3. 校验 `source != main`；
4. 校验用户有项目权限；
5. 校验两任务同项目；
6. 校验两任务生命周期允许合并；
7. source 不得属于任何活跃组；
8. main 如果属于活跃组，必须在该组中为 MAIN；
9. main 无组时创建组和 MAIN 成员；
10. 创建 SOURCE 成员并保存原状态/负责人快照；
11. 写审计、通知；如需提交后副作用则写 outbox；
12. 保存幂等结果并提交。

数据库的部分唯一索引负责处理并发竞态。业务代码的“先查后写”不能替代唯一约束。

## 6.5 解除合并

事务内锁定组和成员：

1. 仅允许解除活跃 SOURCE；
2. 标记成员 `DETACHED` 并记录时间、原因；
3. 不修改任务工作状态和负责人；
4. 如果已无活跃 SOURCE，关闭组并解除 MAIN；
5. 写审计、通知；如需提交后副作用则写 outbox；
6. 提交。

## 6.6 遗留问题转任务

同一事务完成：

1. 锁定迭代记录并确认当前版本仍包含遗留问题；
2. 校验用户权限；
3. 生成任务编号；
4. 创建任务并写 `source_change_record_id`；
5. 写审计和通知；
6. 保存幂等结果。

前端重复点击或请求超时重试不得产生两个任务。

## 6.7 记录作废、归档和恢复

- 作废已发布记录需要管理员权限、原因和预期版本；
- 作废不删除版本；
- 项目/模块/功能归档后禁止创建下级新数据；
- 恢复时必须先验证所有父级处于可用状态；
- 普通用户不获得任何物理删除接口。

---

# 7. 认证、鉴权与安全

## 7.1 Session 认证

使用 NestJS 默认 Express Adapter、`express-session` 和 PostgreSQL Session Store。必须形成完整闭环：

1. 密码使用 Argon2id；参数根据生产机器基准测试设置；
2. 登录成功后调用 Session regenerate，防止 Session Fixation；
3. Cookie 名在 HTTPS 环境使用 `__Host-session`；
4. Cookie 设置 `HttpOnly`、`Secure`、`SameSite=Lax`、`Path=/`；
5. Session ID 必须使用密码学安全随机数；
6. Session 数据只保存 `userId/sessionVersion` 等最小信息；
7. 空闲超时和绝对超时分别控制；建议 8 小时空闲、7 天绝对上限；
8. 用户停用、修改密码或管理员强制退出时递增 `session_version` 并吊销现有 Session；
9. 登录失败进行账号+IP 双维度限流，日志不得记录密码；
10. Session Store 表纳入迁移、备份和清理任务；
11. 多实例扩展时仍可共享 PostgreSQL Session，不依赖进程内内存。
12. Nginx 后使用 Secure Cookie 时，Express 必须只信任明确的反向代理跳数，并正确读取 `X-Forwarded-Proto`。

## 7.2 CSRF

所有状态变更请求必须同时满足：

- SameSite Cookie；
- 校验 `Origin`，无 Origin 时按策略校验 Referer；
- 使用同步 Token 或双提交 Token；
- GET/HEAD 不执行状态变更；
- 登录、登出和敏感管理操作同样覆盖 CSRF。

## 7.3 资源鉴权

禁止信任请求中的 `projectId` 来证明资源归属。

正确查询模式：

```sql
SELECT t.*
FROM tasks t
WHERE t.id = $taskId
  AND (
    $isAdmin
    OR EXISTS (
      SELECT 1
      FROM project_members pm
      WHERE pm.project_id = t.project_id
        AND pm.user_id = $actorId
        AND pm.status = 'ACTIVE'
    )
  );
```

Repository 的资源方法必须显式接收 `ActorContext`，不得同时提供无鉴权的同名公共查询。跨资源操作必须对所有资源做授权与同项目校验。

## 7.4 审计

审计记录业务动作，而不是简单复制每一次 SQL UPDATE：

```text
PROJECT_CREATED
PROJECT_MEMBER_REMOVED
TASK_COMPLETED
TASK_MERGED
TASK_DETACHED
CHANGE_RECORD_PUBLISHED
CHANGE_RECORD_VERSION_CREATED
CHANGE_RECORD_VOIDED
```

每条审计至少包含：

- actor_user_id；
- action；
- target_type/target_id/project_id；
- before_data/after_data（脱敏）；
- reason；
- request_id；
- ip_address/user_agent；
- created_at。

应用数据库角色只有 audit_logs 的 INSERT/SELECT 权限，没有 UPDATE/DELETE 权限。不要同时启用“通用审计触发器”和应用业务审计，避免重复及语义冲突。

## 7.5 XSS 与浏览器安全

- Markdown 保存原文；
- 使用 `react-markdown + rehype-sanitize`，不启用原始 HTML；
- 链接协议只允许 `https/http`，外链添加 `rel="noopener noreferrer"`；
- Nginx/NestJS 设置 CSP、`X-Content-Type-Options`、Referrer-Policy 等安全头；
- API JSON 响应不拼接 HTML；
- 错误信息不回显 SQL、堆栈、Secret 和内部路径。

## 7.6 SSRF

V1.2 只解析并保存 GitHub URL，不由服务端抓取远程内容。

未来启用 GitHub 请求时必须：

1. 仅允许 `https`；
2. 精确允许 `github.com/api.github.com`；
3. DNS 解析后拒绝 loopback、私网、链路本地、保留地址和 IPv6 私网；
4. 禁止自动重定向，或对每次重定向重新执行全部校验；
5. 设置连接/响应超时、响应体大小上限；
6. 使用专用低权限 Token；
7. 不允许用户控制代理地址和请求头。

## 7.7 Secrets

- 开发环境使用不提交 Git 的 `.env`；
- `.env.example` 只放变量名和非敏感示例；
- 生产环境优先读取 `/run/secrets/*` 文件；
- Compose 中 PostgreSQL 使用 `POSTGRES_PASSWORD_FILE`；
- 应用实现 `readSecret(name)`，优先读取文件、其次读取环境变量；
- Session 方案不再保留无用途的 `JWT_SECRET`；
- 日志、异常和审计快照统一脱敏。

---

# 8. 通知与提交后副作用

## 8.1 V1.2 通知

V1.2 使用前端 30～60 秒轮询：

```text
GET /api/v1/notifications?after=<cursor>
```

用户操作事务内只创建 `notifications` 数据库行。事务提交后前端下一次轮询即可读取，不需要 WebSocket。

## 8.2 Outbox

需要外部副作用时，才在业务事务内写入 `outbox_events`：

```text
id / event_type / aggregate_type / aggregate_id
payload / status / attempts / available_at / created_at
```

启用外部副作用后，独立 worker 使用 `FOR UPDATE SKIP LOCKED` 拉取事件。失败指数退避，达到阈值转 DEAD 并告警。发送成功后标记 SENT。V1.2 如果没有任何外部副作用，可以先建立表和 Repository 而不启动 worker，也不得创建无消费者的事件。

启用 worker 后，即使 worker 暂停，也不影响核心业务事务提交；恢复后可继续处理。

---

# 9. 搜索设计

## 9.1 V1.2 搜索范围

覆盖：

- 功能名称和当前说明；
- 任务编号、标题和描述；
- 迭代记录编号、标题、问题、方案、验证结果和遗留问题；
- GitHub Issue/PR/Commit 标识。

## 9.2 初始实现

启用官方 PostgreSQL 扩展：

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

对规范化搜索文本建立 GIN trigram 索引。应用层对输入做 trim、长度限制和词项拆分，使用参数化查询。

不直接把用户输入拼入 `to_tsquery`，避免语法错误和查询放大。只有当验收数据证明 trigram 无法满足中文召回和性能时，才通过 ADR 引入 pg_jieba/zhparser；扩展必须自行构建、固定源码版本和镜像 digest，不直接依赖未知维护状态的第三方数据库镜像。

---

# 10. 前端设计约束

## 10.1 状态边界

- TanStack Query：项目、模块、功能、任务、记录、通知等服务端状态；
- React Hook Form：表单草稿和校验；
- URL：分页、筛选、排序、当前 Tab 等可分享状态；
- React local state：弹窗、抽屉和一次性 UI 状态；
- Zustand：仅在确有跨页面客户端状态时引入，不保存服务端实体副本。

默认不创建 `authStore/projectStore` 保存重复数据。当前用户由 `/me` Query 管理，当前项目尽量由路由参数表达。

## 10.2 路由与数据获取

React Router 负责路由和访问入口，TanStack Query 负责服务器数据。不得同时为同一资源混用 Router Loader 缓存与 Query 缓存。

所有 API 调用通过生成客户端进入；禁止组件直接裸写 `fetch/axios`。

## 10.3 更新冲突

编辑表单带回读取时的 `rowVersion`。API 返回 409 时：

1. 保留用户输入；
2. 提示数据已被修改；
3. 支持重新加载最新数据；
4. 不自动覆盖他人修改；
5. 需要时展示字段差异。

## 10.4 静态资源缓存

- `index.html`：`Cache-Control: no-cache`；
- 带内容哈希的 `/assets/*`：`public, max-age=31536000, immutable`；
- API：默认 `no-store`，具体查询按需求设置；
- 版本发布后执行 smoke test，确认新 index 引用了已存在的静态资源。

---

# 11. 部署与运维

## 11.1 镜像

采用两个多阶段镜像：

```text
api image
  builder: pnpm install --frozen-lockfile + build
  runtime: 仅生产依赖、非 root 用户、healthcheck

web image
  builder: pnpm install --frozen-lockfile + Vite build
  runtime: nginx，COPY dist 到镜像
```

禁止生产 Compose 挂载开发机的 `frontend/dist`。镜像使用不可变 tag 和 digest，发布记录保存 Git commit SHA。

## 11.2 Compose 拓扑

```yaml
services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
    secrets: [db_password]
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [internal]

  migrate:
    image: ${API_IMAGE}
    command: ["pnpm", "db:migrate"]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: app
    depends_on:
      db:
        condition: service_healthy
    secrets: [db_password]
    networks: [internal]
    restart: "no"

  api:
    image: ${API_IMAGE}
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: app
      NODE_ENV: production
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    secrets: [db_password, session_secret]
    networks: [internal]
    healthcheck:
      test: ["CMD", "node", "healthcheck.mjs"]
      interval: 15s
      timeout: 5s
      retries: 5

  web:
    image: ${WEB_IMAGE}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    networks: [internal]

networks:
  internal:

volumes:
  pg_data:

secrets:
  db_password:
    file: ./secrets/db_password
  session_secret:
    file: ./secrets/session_secret
```

开发环境可以通过 override 文件暴露 PostgreSQL 端口；生产配置不得暴露。

## 11.3 健康检查

- `/health/live`：进程事件循环正常，不访问数据库；
- `/health/ready`：数据库可连接、迁移版本正确；
- readiness 失败时 Nginx 不应继续转发新请求；
- 健康接口不得泄露连接串、版本细节和堆栈。

## 11.4 日志

- pino 输出 JSON 到 stdout；
- 每个请求包含 requestId、route、status、duration；
- 可记录 actorId/projectId，但不得记录密码、Cookie、CSRF Token、Secret 和完整敏感正文；
- Docker 日志驱动设置 `max-size/max-file`；
- 审计日志不是应用错误日志的替代品；
- 数据库启用 `pg_stat_statements` 并设置慢查询观察规则。

## 11.5 备份与恢复

备份要求：

1. 每日使用 `pg_dump --format=custom`；
2. 先写临时文件，成功后原子重命名；
3. 生成 SHA-256 校验值；
4. 本机保留 7 天，异机/对象存储保留 30 天；
5. 异地备份加密；
6. 备份任务失败必须告警；
7. 每月至少把备份恢复到临时数据库并运行 smoke test；
8. 记录最近一次成功备份和最近一次成功恢复时间；
9. Session 可以不恢复，但业务数据和审计必须恢复。

只有通过恢复演练的文件才称为有效备份。

## 11.6 升级与回滚

```text
构建并测试镜像
  ↓
备份并验证可读
  ↓
运行向后兼容的 expand migration
  ↓
启动新 API 并通过 readiness
  ↓
切换流量/替换容器
  ↓
部署新 Web
  ↓
运行 smoke test
  ↓
观察错误率和慢查询
```

代码回滚不等于数据库回滚。破坏性字段删除放在后续 contract migration，确认旧代码不再使用后执行。

## 11.7 容量规划

初始建议可以从 4 vCPU、8GB RAM、SSD 开始，但这不是容量承诺。上线前用接近真实的数据量验证：

- 项目、任务和迭代记录数量；
- 同时在线与写并发；
- 全局搜索耗时；
- 合并和完成事务锁等待；
- PostgreSQL 连接池大小；
- 备份时间与恢复时间；
- 日志增长速度。

只有实测后才能给出 100/300 用户的资源结论。

---

# 12. 测试与 CI

## 12.1 测试金字塔

| 层次 | 工具 | 必测内容 |
|---|---|---|
| 纯逻辑单元测试 | Vitest | 状态机、权限 Policy、编号格式、URL 解析 |
| Repository 集成测试 | 真实 PostgreSQL | FK/CHECK/UNIQUE、查询过滤、迁移 |
| Application 集成测试 | 真实 PostgreSQL | 事务回滚、审计、通知、幂等 |
| API 测试 | Nest 测试应用 | 认证、CSRF、错误模型、OpenAPI 一致性 |
| E2E | Playwright | 登录、创建项目、创建任务、完成、合并、解除、版本查看 |
| 运维测试 | Compose + smoke | 构建、启动、迁移、健康检查、备份恢复 |

不要只 Mock Repository 测 Service；这无法验证真实事务、锁和数据库约束。

## 12.2 必须存在的并发测试

1. 两个请求同时完成同一任务，只能一个成功；
2. 两个请求同时把同一任务合并到不同组，只能一个成功；
3. 两人同时编辑已发布记录，只能一个版本成功，另一方收到 409；
4. 相同幂等 key 重放不产生重复记录；
5. 相同幂等 key 携带不同请求内容返回 409；
6. 并发创建编号不重复；
7. 解除合并与新增来源任务并发时，组状态仍满足不变量。

## 12.3 安全测试

- 普通成员访问其他项目的任务 ID；
- 普通成员调用管理员接口；
- 移除成员后继续使用旧 Session；
- CSRF 请求；
- Markdown 脚本、危险协议和恶意链接；
- 登录爆破限流；
- Session Fixation；
- 审计表 UPDATE/DELETE 权限；
- 日志与错误响应泄密检查。

## 12.4 CI 门禁

每个 PR 必须依次通过：

```text
依赖安装（frozen lockfile）
  → lint
  → format check
  → typecheck
  → unit tests
  → database migrations from empty DB
  → integration tests
  → OpenAPI/client drift check
  → web/api production build
  → Playwright critical-path tests
  → container image build
```

数据库迁移、权限规则、认证配置、Dockerfile 和 Compose 变更必须人工审核。

---

# 13. AI 编码协作规范

## 13.1 AI 的角色

AI 可以：

- 生成模块骨架；
- 按既有范例实现端点；
- 补充测试；
- 执行类型检查、测试和构建；
- 在失败后定位并修复；
- 更新 OpenAPI Client 和文档。

AI 不得自行决定：

- 新增核心依赖；
- 修改数据库不变量；
- 放宽鉴权；
- 删除或重写历史迁移；
- 跳过失败测试；
- 修改生产 Secrets；
- 将网络请求移入数据库事务；
- 物理删除业务历史。

## 13.2 仓库级约束

根目录 `AGENTS.md` 至少写明：

1. 模块和依赖方向；
2. Controller/Service/Repository 职责；
3. 当前技术栈和允许的库；
4. 错误码、鉴权和事务规范；
5. 数据库迁移不可重写；
6. 必须执行的验证命令；
7. 禁止读取或输出 Secrets；
8. 关键命令的幂等与并发要求；
9. 修改 API 后必须重新生成客户端；
10. 完成定义（Definition of Done）。

## 13.3 AI 任务模板

每次任务应包含：

```text
目标：要实现的用户结果
范围：允许修改的模块/文件
不变量：权限、事务、状态和历史规则
接口：请求、响应和错误码
数据库：涉及的表、锁和约束
验收：必须新增/通过的测试
非目标：本次明确不处理的内容
验证命令：lint/typecheck/test/build
```

## 13.4 AI 开发节奏

```text
先读 PRD/ADR/相邻实现
  ↓
写或更新失败测试
  ↓
小步实现
  ↓
运行最小相关测试
  ↓
运行类型检查和模块测试
  ↓
运行完整 CI 门禁
  ↓
人工审查数据库、安全和业务不变量
```

禁止一次让 AI 生成整个系统后再集中调试。每次 PR 应保持可审查，优先一个业务纵切片。

## 13.5 Git 工作流

1～3 人团队采用 trunk-based：

- `main` 始终可发布；
- 使用短生命周期 `feature/*` 或 `fix/*` 分支；
- 不维护长期 `develop` 分支；
- 小 PR、强 CI、至少一次人工审查；
- 合并使用 squash；
- 发布使用不可变 tag 并记录 commit SHA。

---

# 14. 实施顺序

## 阶段 0：架构跑通

- 初始化 monorepo；
- 建立 NestJS/React 基础应用；
- PostgreSQL、迁移、Session、OpenAPI Client；
- Compose 构建与健康检查；
- CI 最小链路。

验收：空数据库可以一键迁移，用户可以登录，前端可以调用 `/me`，镜像可重复构建。

## 阶段 1：项目骨架

- 用户与管理员；
- 创建项目、初始成员、未分类模块单事务；
- 项目成员访问控制；
- 项目/模块/功能 CRUD 和归档；
- 审计基础设施。

## 阶段 2：任务与记录

- 功能级/模块级任务；
- 影响功能关系；
- 完成、取消、恢复；
- 迭代记录草稿、发布、版本；
- 遗留问题转任务；
- 幂等和乐观锁。

## 阶段 3：合并与历史

- 任务组和 MAIN/SOURCE；
- 历史/活动来源分支；
- 解除合并；
- 聚合视图；
- 并发不变量测试。

## 阶段 4：搜索、通知与运维

- 全局搜索；
- 站内通知轮询；
- Docker 生产配置；
- 备份、恢复、升级 Runbook；
- Playwright 完整关键路径。

---

# 15. 上线前架构验收清单

## 数据一致性

- [ ] 空数据库迁移成功；
- [ ] 所有必填归属列为 NOT NULL；
- [ ] 数据库不能插入跨项目的模块、功能、任务和记录关系；
- [ ] 每个活跃任务最多属于一个活跃组；
- [ ] 每个活跃组恰好一个 MAIN；
- [ ] 所有乐观锁更新都会递增 row_version；
- [ ] 关键命令重放不会生成重复数据。

## 安全

- [ ] Session 可持久化、轮换、失效和清理；
- [ ] 密码使用 Argon2id；
- [ ] CSRF 测试通过；
- [ ] 资源 ID 无法越权访问；
- [ ] Markdown XSS 测试通过；
- [ ] 审计记录不可修改和删除；
- [ ] 日志和错误响应不泄露 Secret。

## 部署

- [ ] 前端产物构建进镜像；
- [ ] PostgreSQL 不暴露公网端口；
- [ ] DB/migration/API healthcheck 有效；
- [ ] index.html 不做长期 immutable 缓存；
- [ ] Secrets 文件与应用读取逻辑真实连通；
- [ ] 新环境可以只依赖镜像、Compose 和 Secrets 启动；
- [ ] 备份恢复演练成功。

## 交付质量

- [ ] OpenAPI 与生成客户端无漂移；
- [ ] 类型检查、单测、集成测试、E2E 和构建全部通过；
- [ ] 并发测试覆盖完成、合并和版本编辑；
- [ ] 核心 ADR 已记录；
- [ ] Runbook 覆盖部署、回滚、备份、恢复和 Session Secret 轮换。

---

# 16. 必需 ADR

项目开始前建立以下架构决策记录：

| ADR | 决策 |
|---|---|
| ADR-001 | 使用模块化单体，不拆微服务 |
| ADR-002 | React SPA，不使用 Next.js |
| ADR-003 | NestJS + Express Adapter，不使用 Hono |
| ADR-004 | OpenAPI 作为 HTTP 契约，不共享数据库实体 |
| ADR-005 | PostgreSQL + Drizzle + 显式迁移 |
| ADR-006 | PostgreSQL Session Store，不引入 Redis |
| ADR-007 | TaskGroupMember 为任务聚合唯一真相 |
| ADR-008 | 应用层业务审计，数据库禁止修改/删除 |
| ADR-009 | 事务内写通知/Outbox，提交后执行副作用 |
| ADR-010 | V1.2 使用 pg_trgm，暂不引入第三方中文分词镜像 |

---

# 17. 最终结论

V1.2 的目标不是选择最“轻”的框架，而是建立一条 AI 可以反复执行、机器可以验证、人工可以审查的研发流水线。

最终架构为：

```text
React SPA
  ↓ OpenAPI 生成客户端
NestJS 模块化单体
  ↓ Drizzle + 显式事务/约束
PostgreSQL
  ↓
Docker Compose 单实例部署
```

项目成功的关键不在 React、NestJS 或 Drizzle 的单项评分，而在以下不变量是否同时成立：

1. 数据库阻止非法关系；
2. Application Service 守住权限、状态与事务；
3. API 契约可生成、可验证；
4. 并发冲突和重复请求有明确结果；
5. 审计、通知和外部副作用边界清楚；
6. 部署、备份和恢复实际跑通；
7. AI 的每次修改都必须通过自动化门禁。

满足上述条件后，该方案能够支持 1～3 人 AI 优先团队稳定交付 V1.1 产品范围，并为后续 GitHub 集成、SSE 通知和容量扩展保留清晰演进路径。
