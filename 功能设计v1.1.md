## 👨‍💼 产品经理

# 软件研发功能迭代记录与任务协作系统
## V1.1 功能规格说明书（PRD）

**文档版本：** V1.1

**产品类型：** 软件研发项目管理 / 功能迭代记录系统

**适用范围：** 单企业、单系统部署

**关联设计：** [系统设计 V1.0.2](系统设计文档v1.0.2.md)、[技术设计 V1.2.2](技术设计v1.2.2.md)、[正式 ADR 索引](docs/adr/README.md)

**本版本核心定位：**

> 以项目为范围、以模块为分类、以功能为长期档案、以任务推动执行、以迭代记录沉淀变化、以 GitHub 链接保留代码证据。

---

# 0. V1.1 相比上一版的确认调整

根据本次确认，V1.1 做出以下最终调整：

1. **不设置工作空间 / 组织层级；**
2. 顶层业务结构为：

   ```text
   项目 → 模块 → 功能
   ```

3. 任务既可以属于某个功能，也可以属于某个模块并影响多个功能；
4. 不强制将一项跨多个功能的技术工作拆成多个任务；
5. 系统只保留两种角色：
   - 系统管理员；
   - 项目成员。
6. **系统管理员和项目成员都可以创建项目；**
7. 创建项目时增加“初始项目成员”多选框；
8. 创建项目时，创建者自动成为初始项目成员且不可在表单中取消；项目创建后，系统管理员可按普通成员规则将其移除，创建人记录仍永久保留且不产生额外权限；
9. 项目成员拥有项目内全部普通研发操作权限；
10. 只有用户管理、成员管理、项目/模块/功能归档或恢复、迭代记录作废或恢复、原始审计等核心高风险功能由系统管理员负责；
11. 迭代记录字段精简为三项核心内容：
    - 为什么改、发现了什么问题；
    - 改了什么、怎么改的；
    - 改完效果如何、如何验证。
12. 迭代记录中的时间、人员、项目、模块、功能、任务、版本等信息尽量自动生成；
13. 已完成、已取消、已合并的任务不物理删除，作为历史资产保留；
14. 已有正式迭代记录的任务仍然可以合并；
15. 合并后形成“主任务 + 来源任务分支”，每条分支保留自己的任务和迭代记录；
16. 模块级任务只保存一份数据，通过“影响功能”关系展示到相关功能页面；
17. GitHub 支持多个 Issue、PR、Commit 等链接。

---

# 1. 业务背景

## 1.1 研发过程背景

软件系统上线后，会持续经历以下过程：

```text
需求提出
  ↓
任务创建
  ↓
人员指派
  ↓
开发实现
  ↓
测试验证
  ↓
上线运行
  ↓
用户反馈 / 线上问题 / 性能问题
  ↓
再次修改
  ↓
持续迭代
```

同一个功能可能在数月甚至数年内被多次修改。

例如：

```text
微信支付退款回调
├── 第一次：实现退款回调
├── 第二次：修复重复回调导致的重复退款
├── 第三次：增加失败重试
├── 第四次：优化异常日志
└── 第五次：升级支付 SDK
```

如果系统只记录任务状态，就只能看到：

```text
修复重复回调：已完成
```

但无法回答：

- 为什么需要修改？
- 当时发现了什么问题？
- 问题根因是什么？
- 最后采用了什么方案？
- 修改后是否验证成功？
- 还有哪些风险？
- 对应哪个 PR 或 Commit？
- 以前是否处理过类似问题？

## 1.2 现有研发管理中的问题

### 问题一：功能和任务混在一起

任务通常是一次性工作的记录，功能则是长期存在的业务能力。

如果把两者混在一起：

```text
修复退款问题
优化退款接口
增加退款重试
```

每次都创建成一个“功能”，后续会产生大量重复功能。

反过来，如果所有任务都挂到一个特别大的功能下：

```text
支付系统
```

又会导致所有支付相关历史堆积在一起，无法快速定位。

### 问题二：研发知识散落

重要信息可能分别存在于：

- GitHub Issue；
- Pull Request；
- Commit；
- 企业微信或钉钉聊天；
- 个人笔记；
- 测试报告；
- 产品文档；
- 某个开发人员的记忆中。

人员变动后，历史知识难以传承。

### 问题三：重复任务较多

多个员工可能分别创建：

```text
T-101：修复重复退款
T-108：处理重复回调
T-112：解决退款重复入账
```

实际上三个任务可能描述的是同一个问题。

如果简单删除其中两个任务，会丢失原始描述、原负责人和已经产生的迭代记录。

### 问题四：跨功能技术工作录入成本高

例如一次支付 SDK 升级同时影响：

- 微信支付下单；
- 微信支付退款；
- 微信支付退款回调。

如果强制拆成三条任务、三条记录，会造成：

- 重复填写；
- 重复关联 GitHub；
- 重复统计；
- 研发人员不愿意记录。

## 1.3 产品要解决的问题

本系统需要建立如下完整链路：

```text
项目归属
  ↓
模块分类
  ↓
功能档案
  ↓
任务执行
  ↓
迭代记录
  ↓
遗留问题
  ↓
后续任务
  ↓
GitHub 代码证据
```

---

# 2. 产品定位

## 2.1 一句话定位

> **以任务驱动研发执行，以功能沉淀知识，以迭代记录保存变化，以 GitHub 关联代码证据的轻量化研发记录系统。**

## 2.2 产品目标

### 目标一：建立功能级研发档案

用户可以围绕一个长期功能查看：

- 当前功能说明；
- 所有历史修改；
- 相关任务；
- 遗留问题；
- GitHub 证据。

### 目标二：降低研发记录成本

通过以下方式减少填写负担：

- 从任务自动带入信息；
- 三段式核心表单；
- 自动生成时间和人员；
- GitHub 链接自动识别；
- 遗留问题一键转任务。

### 目标三：保留任务和迭代历史

任务完成、取消或合并后不删除，保证：

- 任务可追溯；
- 历史记录不丢失；
- 合并前后关系清楚；
- 责任和时间可查询。

### 目标四：支持跨功能研发工作

不强制将共同技术工作拆成大量重复任务，允许：

```text
模块级任务
  ↓
影响多个功能
  ↓
生成一条模块级迭代记录
  ↓
在多个功能页面引用展示
```

## 2.3 非目标

V1.1 暂不建设：

- 工作空间 / 组织 / 多租户；
- Scrum Sprint；
- 工时填报；
- 甘特图；
- 复杂资源排期；
- 完整缺陷管理流程；
- 代码托管；
- 自动判断 PR 是否代表功能完成；
- GitHub 双向修改任务状态；
- 复杂的项目成本管理。

---

# 3. 核心业务层级

## 3.1 总体结构

V1.1 不设置“工作空间 / 组织”层级。

```text
系统
├── 用户
│   ├── 系统管理员
│   └── 项目成员
│
└── 项目 Project
    ├── 项目成员
    └── 模块 Module
        ├── 功能 Feature
        │   ├── 功能级任务 Task
        │   │   └── 迭代记录 ChangeRecord
        │   │
        │   └── 功能历史
        │
        └── 模块级任务 Task
            └── 模块级迭代记录 ChangeRecord
                └── 影响多个功能
```

## 3.2 完整业务结构图

```text
项目：商城系统
│
├── 模块：支付
│   │
│   ├── 功能：微信支付下单
│   │   ├── 任务：优化下单接口响应速度
│   │   └── 迭代记录：增加下单参数缓存
│   │
│   ├── 功能：微信支付退款
│   │   ├── 任务：增加退款失败重试机制
│   │   └── 迭代记录：增加退款失败自动重试
│   │
│   ├── 功能：微信支付退款回调
│   │   ├── 任务：修复重复回调导致的重复退款
│   │   │   └── 迭代记录：增加商户订单号幂等校验
│   │   │
│   │   └── 任务：补充异常回调日志
│   │
│   └── 模块级任务：升级微信支付 SDK
│       └── 模块级迭代记录：完成 SDK 升级及接口适配
│           ├── 影响功能：微信支付下单
│           ├── 影响功能：微信支付退款
│           └── 影响功能：微信支付退款回调
│
└── 模块：订单
    └── 功能：订单批量导出
```

---

# 4. 核心对象定义

| 对象 | 定义 | 示例 | 是否长期保留 |
|---|---|---|---|
| 项目 | 一个完整的软件系统、产品或研发项目 | 商城系统 | 是 |
| 模块 | 项目下的业务区域或分类 | 支付、订单、会员 | 是 |
| 功能 | 可独立描述、独立维护、持续迭代的具体能力 | 微信支付退款回调 | 是 |
| 任务 | 针对某个功能或模块需要执行的一次工作 | 修复重复回调导致的重复退款 | 是 |
| 迭代记录 | 一次已经发生的功能修改或技术变化 | 增加商户订单号幂等校验 | 是 |
| 任务聚合组 | 多个重复或相关任务的统一展示关系 | 退款回调重复处理 | 是 |
| 来源任务分支 | 合并后保留的原始任务线 | T-108 来源任务 | 是 |
| GitHub 链接 | Issue、PR、Commit 等代码证据 | PR #245 | 是 |
| 操作日志 | 系统操作和数据修改记录 | 李四将 T-108 合并到 T-101 | 是 |

---

# 5. 模块、功能、任务、迭代记录的区别

## 5.1 模块 Module

模块回答：

> 这个能力属于哪个业务区域？

示例：

```text
支付
订单
会员
库存
营销
权限
报表
```

模块是分类容器，不应承载所有具体业务细节。

## 5.2 功能 Feature

功能回答：

> 系统具体提供什么可独立维护的能力？

例如：

```text
微信支付下单
微信支付退款
微信支付退款回调
订单批量导出
会员等级计算
优惠券自动核销
```

## 5.3 任务 Task

任务回答：

> 当前要执行什么工作？

例如：

```text
修复重复回调导致的重复退款
增加退款失败重试机制
优化大数据量订单导出速度
```

## 5.4 迭代记录 ChangeRecord

迭代记录回答：

> 这次实际改了什么，为什么改，结果如何？

例如：

```text
增加商户订单号幂等校验
```

---

# 6. 功能粒度规则

## 6.1 什么叫功能粒度统一

功能粒度统一不是要求所有功能大小相同，而是要求在同一个项目中遵循一致的划分逻辑：

> **模块负责分类，功能负责长期维护的具体能力，任务负责一次执行工作，迭代记录负责已经发生的实际变化。**

## 6.2 判断一个事项属于哪一层

```text
待录入事项
    │
    ├── 主要用于对业务进行归类？
    │       └── 是 → 模块
    │
    ├── 是系统长期提供的一项具体能力？
    │       └── 是 → 功能
    │
    ├── 是当前需要执行的一项工作？
    │       └── 是 → 任务
    │
    └── 是已经完成并发生的实际变化？
            └── 是 → 迭代记录
```

## 6.3 功能应满足的标准

一个适合创建为功能的对象，应尽量满足：

1. 能独立说明当前是如何工作的；
2. 有明确的业务边界；
3. 有明确的输入、处理和输出；
4. 后续可能产生多个任务；
5. 后续可能被多次修改；
6. 用户能够单独搜索和理解它。

## 6.4 粒度过大的例子

不推荐：

```text
功能：支付
```

因为支付可能包括：

- 微信支付下单；
- 支付宝支付下单；
- 退款；
- 回调；
- 对账；
- 支付通知；
- 风控校验。

更合理的结构：

```text
模块：支付
├── 功能：微信支付下单
├── 功能：微信支付退款
├── 功能：微信支付退款回调
├── 功能：支付宝支付下单
└── 功能：支付对账
```

## 6.5 粒度过小的例子

不推荐：

```text
功能：增加商户订单号幂等校验
```

它更适合是：

```text
任务：修复重复回调导致的重复退款
迭代记录：增加商户订单号幂等校验
```

## 6.6 “微信支付”属于哪个层级

没有绝对固定的层级，需要根据项目范围判断。

### 场景一：项目是商城系统

```text
项目：商城系统
└── 模块：支付
    └── 功能：微信支付退款回调
```

### 场景二：项目是支付中台

```text
项目：支付中台
└── 模块：渠道接入
    └── 功能：微信支付
        ├── 任务：实现微信支付下单
        └── 任务：实现微信退款回调
```

判断标准不是名称本身，而是：

> 模块负责归类，功能负责描述可独立维护的能力。

---

# 7. 业务规则总览

## BR-001：系统层级规则

V1.1 不设置工作空间、组织或租户层级。

```text
系统 → 项目 → 模块 → 功能
```

## BR-002：项目访问规则

- 系统管理员可以查看所有项目；
- 项目成员只能查看自己加入的项目；
- 创建项目的用户自动成为该项目成员；
- 被加入项目的用户可以访问该项目；
- 被移出项目后不能继续访问项目，但历史数据中的创建人、负责人和作者信息仍保留。

## BR-003：项目创建规则

- 系统管理员可以创建项目；
- 项目成员也可以创建项目；
- 创建者在项目创建时自动成为项目成员；
- 创建项目时可以选择初始项目成员；
- 初始成员只能从系统中已经存在且状态正常的用户中选择；
- 创建表单中不能取消创建者的初始成员资格；项目创建后，系统管理员可以按普通成员规则移除创建者的成员记录；
- 项目的创建人字段只用于溯源、保持不可变，移除成员不会清空或改写该字段；
- 项目创建者不自动获得特殊项目权限；
- V1.1 不设置项目管理员角色。

## BR-004：层级归属规则

- 模块必须属于一个项目；
- 功能必须属于一个项目和一个模块；
- 功能所属模块必须属于同一个项目；
- 功能级任务必须属于一个功能；
- 模块级任务必须属于一个模块；
- 迭代记录必须属于一个项目；
- 任何对象不能跨项目直接挂接。

## BR-005：模块级事项规则

- 一项工作影响多个功能时，不强制拆分；
- 可以创建模块级任务；
- 模块级任务可以关联多个影响功能；
- 模块级任务只生成一份任务数据；
- 模块级迭代记录只保存一份；
- 相关功能页面通过引用方式展示该记录；
- 引用展示不复制数据、不重复计数；
- 只有在不同功能需要独立负责人、状态、验收、上线或回滚时，才建议拆分。

## BR-006：功能档案规则

- 功能是长期存在的档案；
- 所有任务完成后，功能仍然保留；
- 功能不会因为当前没有未完成任务而自动归档；
- 功能归档后历史仍可查看；
- 已归档功能默认不能创建新任务和新迭代记录。

## BR-007：任务保留规则

> **任务不是“有历史才保留”，而是所有任务默认都保留。任务完成、取消或合并后，只改变它的展示位置和可继续操作范围，不删除原始任务。**

保留内容包括：

- 任务编号；
- 任务标题；
- 任务描述；
- 项目、模块、功能归属；
- 任务范围；
- 创建人；
- 负责人；
- 创建时间；
- 完成时间；
- 原始状态；
- GitHub 链接；
- 迭代记录；
- 合并关系；
- 评论和附件（如果后续启用）；
- 全部操作日志。

## BR-008：工作状态和合并状态分离

任务工作状态：

```text
未完成
已完成
已取消
```

任务合并关系：

```text
独立任务
主任务
来源任务
```

不能直接使用“已合并”替换任务原工作状态。

例如：

```text
任务 T-108
工作状态：已完成
合并关系：来源任务
主任务：T-101
```

## BR-009：迭代记录规则

- 迭代记录只描述已经发生或已经确认的变化；
- 计划做但尚未完成的工作不能直接写成正式迭代记录；
- 任务完成时可以自动生成迭代记录；
- 一个任务可以没有迭代记录；
- 一个任务可以产生多条迭代记录；
- 已发布记录不能被静默覆盖；
- 记录修改必须形成新版本；
- 状态只允许 DRAFT → PUBLISHED → VOID，以及管理员重认证后的 VOID → PUBLISHED；完整规则见 [ADR-024](docs/adr/ADR-024.md)。

## BR-010：任务合并规则

- 同一项目内的任务可以合并；
- 是否已有正式迭代记录不影响合并；
- 跨功能、跨模块任务也可以合并；
- 跨项目任务不能直接合并；
- 合并不改变原任务项目、模块或功能归属；
- 合并不删除、不复制、不覆盖任务和迭代记录；
- 合并后由主任务作为统一入口；
- 来源任务作为独立历史分支保留。

## BR-011：删除规则

- 系统不提供普通用户物理删除；
- 项目、模块、功能只能归档；
- 任务只能取消或由系统管理员标记无效；
- 有正式迭代记录的任务不能物理删除；
- 已发布迭代记录只能作废，不能物理删除；
- 已作废迭代记录只能按 [ADR-024](docs/adr/ADR-024.md) 恢复为已发布，不能恢复为草稿或物理删除；
- 审计日志不允许删除。

## BR-012：审计规则

以下操作必须记录：

- 创建、编辑；
- 指派、改派；
- 完成、重新打开、取消；
- 合并、解除合并；
- 迭代记录发布和版本修改；
- 项目成员变更；
- 归档、恢复；
- GitHub 链接添加和删除；
- 管理员作废或恢复历史数据。

---

# 8. 角色和权限设计

## 8.1 角色定义

### 系统管理员

拥有全系统权限，负责：

- 用户管理；
- 项目成员管理；
- 全部项目访问；
- 项目归档和恢复；
- 模块、功能归档和恢复；
- 高风险历史操作；
- 查看原始审计快照；
- 系统设置。

### 项目成员

拥有所加入项目内的全部普通研发操作权限：

- 查看项目；
- 创建项目；
- 创建和编辑模块；
- 创建和编辑功能；
- 创建和编辑任务；
- 指派任务；
- 完成和重新打开任务；
- 创建和发布迭代记录；
- 修改已发布记录并生成新版本；
- 合并和解除合并任务；
- 添加 GitHub 链接；
- 查看项目动态。

项目成员之间不再根据以下身份限制权限：

- 创建人；
- 指派人；
- 记录作者；
- 功能创建人。

也就是说，项目成员可以编辑同一个项目内其他成员创建的普通内容。

## 8.2 权限矩阵

| 功能 | 系统管理员 | 项目成员 | 规则说明 |
|---|---:|---:|---|
| 查看所有项目 | √ | — | 项目成员只能看已加入项目 |
| 查看已加入项目 | √ | √ | — |
| 创建项目 | √ | √ | 本次新增确认 |
| 创建项目时选择初始成员 | √ | √ | 创建表单中创建者自动勾选且不可取消 |
| 编辑项目名称和描述 | √ | √ | 项目成员需已加入项目 |
| 修改项目编码 | — | — | 编码创建后不可修改 |
| 添加/移除项目成员 | √ | — | 高风险权限 |
| 归档/恢复项目 | √ | — | 高风险权限 |
| 创建模块 | √ | √ | — |
| 编辑模块 | √ | √ | — |
| 归档/恢复模块 | √ | — | 高风险权限 |
| 创建功能 | √ | √ | — |
| 编辑功能 | √ | √ | — |
| 编辑当前功能说明 | √ | √ | 保存操作日志 |
| 归档/恢复功能 | √ | — | 高风险权限 |
| 创建任务 | √ | √ | — |
| 编辑任务 | √ | √ | 普通项目成员均可 |
| 指派/改派任务 | √ | √ | 必须指派给项目成员 |
| 完成/重新打开任务 | √ | √ | 记录操作人 |
| 取消/恢复任务 | √ | √ | 取消原因建议填写 |
| 合并任务 | √ | √ | 有迭代记录也允许 |
| 解除合并 | √ | √ | 必须二次确认 |
| 创建迭代记录 | √ | √ | — |
| 编辑迭代草稿 | √ | √ | — |
| 发布迭代记录 | √ | √ | 必填字段校验 |
| 编辑已发布记录 | √ | √ | 必须生成新版本 |
| 作废/恢复迭代记录 | √ | — | 高风险权限 |
| 添加/删除 GitHub 链接 | √ | √ | 操作留痕 |
| 配置 GitHub 应用或 Token | — | — | V1.1 不接入 GitHub API，也不持有 Token |
| 查看项目动态 | √ | √ | — |
| 查看原始审计快照 | √ | — | 项目成员看可读动态 |
| 用户账号管理 | √ | — | — |
| 系统配置 | √ | — | — |

## 8.3 创建项目时选择项目成员

### 表单示意

```text
┌─────────────────────────────────────┐
│ 新建项目                              │
├─────────────────────────────────────┤
│ 项目名称 *                            │
│ [商城系统________________________]    │
│                                     │
│ 项目编码                              │
│ [SHOP______________ 自动生成]        │
│                                     │
│ 项目描述                              │
│ [用于商城业务、支付和订单管理……]      │
│                                     │
│ 初始项目成员                          │
│ [搜索姓名_______________________]    │
│                                     │
│ ☑ 当前创建人：李四（不可取消）         │
│ ☐ 张三                                │
│ ☐ 王五                                │
│ ☐ 赵六                                │
│                                     │
│                [取消] [创建项目]       │
└─────────────────────────────────────┘
```

### 创建规则

1. 创建者自动加入项目；
2. 创建者的勾选框在创建表单中默认选中且不可取消；
3. 可以一次选择多个初始成员；
4. 初始成员只能是已注册且未停用的用户；
5. 初始成员不分配额外项目角色；
6. 系统管理员自动拥有项目访问权，即使未在成员列表中；
7. 选择的成员创建成功后收到项目加入通知；
8. 项目创建和成员初始化在一个事务中完成；
9. 项目创建成功后自动创建“未分类模块”；
10. 项目成员后续增删由系统管理员处理。

### 项目成员移除规则

系统管理员移除成员时：

- 移除规则同样适用于项目创建者；
- 移除创建者只关闭其成员关系，不修改项目中永久保留的创建人字段，也不授予其额外访问权；
- 已完成任务保留原负责人；
- 已发布迭代记录保留原作者；
- 未完成任务需要提示是否改派；
- 如果未完成任务不改派，可以保留原负责人，但该用户不能继续处理；
- GitHub 关联和审计记录不受影响；
- 移除成员不会删除其创建的任何数据。

---

# 9. 项目管理

## 9.1 项目功能结构

```text
项目管理
├── 项目列表
├── 新建项目
├── 项目概览
├── 项目信息编辑
├── 初始成员选择
├── 成员管理
├── 项目归档
└── 项目恢复
```

## 9.2 项目字段

| 字段 | 是否必填 | 生成方式 |
|---|---:|---|
| 项目名称 | 是 | 用户填写 |
| 项目编码 | 是 | 系统自动生成，可在创建时修改 |
| 项目描述 | 否 | 用户填写 |
| 初始项目成员 | 至少包含创建者 | 用户多选 |
| 创建人 | 是 | 系统自动生成 |
| 创建时间 | 是 | 系统自动生成 |
| 项目状态 | 是 | 默认正常 |

## 9.3 项目编码规则

示例：

```text
SHOP
CRM
PAYMENT
```

规则：

- 同一系统内唯一；
- 默认根据项目名称生成；
- 允许用户在创建时修改；
- 创建后不可修改；
- 用于生成功能、任务和迭代记录编号。

示例：

```text
SHOP-F-001
SHOP-T-1
SHOP-CR-001
```

## 9.4 项目状态

```text
正常
已归档
```

### 归档规则

- 项目归档后所有下级数据只读；
- 已归档项目不能创建新模块、功能、任务和迭代记录；
- 项目历史仍可搜索和查看；
- 系统管理员可以恢复项目；
- 归档操作必须填写或确认原因；
- 有未完成任务时，归档前必须显示提醒。

## 9.5 项目概览

### 展示内容

```text
商城系统
状态：正常
成员：8 人

┌────────────┬────────────┬────────────┬────────────┐
│ 活跃模块数 │ 活跃功能数 │ 未完成任务 │ 迭代记录数 │
│     5      │     38     │     21     │    245     │
└────────────┴────────────┴────────────┴────────────┘

最近迭代
├── 微信支付退款回调：增加幂等校验
├── 订单批量导出：改为异步导出
└── 会员等级计算：修正边界条件

待处理遗留问题
├── 大数据量导出内存占用较高
└── 退款异常告警策略不完善
```

---

# 10. 模块管理

## 10.1 模块定位

模块是项目内的业务分类和导航容器。

示例：

```text
支付
订单
库存
会员
营销
权限
报表
```

## 10.2 模块规则

1. 一个模块只能属于一个项目；
2. V1.1 只支持一级模块；
3. 模块下不能再创建子模块；
4. 模块名称在同一项目内不能重复；
5. 项目成员可以创建和编辑模块；
6. 只有系统管理员可以归档和恢复模块；
7. 默认自动创建“未分类模块”；
8. 未分类模块允许修改名称、描述，遵循普通模块的项目权限、父级可写、名称唯一与版本规则；其 `kind='UNCLASSIFIED'` 身份不可改变，不能物理删除（2026-09-09 人工确认）；
9. 模块归档后不能新建功能或模块级任务；
10. 模块下已有历史数据时不能物理删除。

## 10.3 模块页面

```text
商城系统 / 支付

模块说明：
支付相关下单、退款、回调和对账能力。

功能列表：
┌────────┬────────────────────┬────────┬────────┬────────────┐
│ 编号   │ 功能名称           │ 待办数 │ 记录数 │ 最近迭代   │
├────────┼────────────────────┼────────┼────────┼────────────┤
│ F-001  │ 微信支付下单       │   2    │   8    │ 04-12      │
│ F-002  │ 微信支付退款       │   1    │   6    │ 04-10      │
│ F-003  │ 微信支付退款回调   │   4    │  16    │ 04-15      │
└────────┴────────────────────┴────────┴────────┴────────────┘

模块级任务：
├── T-201 升级微信支付 SDK
└── T-208 统一支付错误码
```

---

# 11. 功能管理

## 11.1 功能定位

功能是系统长期维护和知识沉淀的核心对象。

例如：

```text
微信支付退款回调
```

它可以长期挂载：

```text
多个任务
多条迭代记录
多个 GitHub 链接
多个遗留问题
```

## 11.2 功能字段

| 字段 | 必填 | 说明 |
|---|---:|---|
| 功能编号 | 自动 | 例如 SHOP-F-3 |
| 功能名称 | 是 | 具体业务能力 |
| 所属项目 | 自动或必填 | 从页面继承 |
| 所属模块 | 是 | 必须选择 |
| 当前功能说明 | 否 | 描述当前实际行为 |
| 标签 | 否 | 可选 |
| 创建人 | 自动 | 当前用户 |
| 创建时间 | 自动 | 系统生成 |
| 功能状态 | 自动 | 正常/已归档 |

## 11.3 新建功能流程

```text
进入项目或模块
      ↓
点击“新建功能”
      ↓
填写功能名称
      ↓
系统检查相似功能
      ↓
选择已有功能 / 继续创建
      ↓
填写当前功能说明
      ↓
保存功能
```

## 11.4 相似功能提示

用户输入：

```text
微信支付退款
```

系统提示：

```text
可能已存在相似功能：

F-003 微信支付退款回调
F-006 微信支付退款失败处理
F-012 退款结果通知

[查看已有功能] [继续创建]
```

相似提示只做提醒，不自动阻止创建，也不自动合并。

F-13 当前实现限定当前项目内最多 10 个可读功能候选（含归档），按输入名称进行 PGroonga 关键词检索，候选展示真实模块归属和详情入口；不宣称模糊语义匹配。名称输入 2～200 字时防抖提示，提示失败或存在同名候选仍可继续合法创建。功能名称上限 500 字，当前说明上限 50000 字，标签最多 50 个，与现有 Schema 对齐。编号由服务端事务分配，不补零（2026-09-09 人工确认）；身份字段不可编辑。

## 11.5 当前功能说明

当前功能说明用于回答：

> 现在这个功能是怎么工作的？

推荐内容：

```text
功能目标：
主要流程：
输入与输出：
依赖服务：
当前限制：
异常处理：
```

### 当前说明和迭代记录的区别

```text
当前功能说明：
现在是什么样

迭代记录：
为什么变成现在这样
```

### 更新规则

- 项目成员可以编辑；
- 修改当前说明不会自动生成迭代记录；
- 如果修改代表实际功能变化，建议同时创建迭代记录；
- 修改前后内容写入审计日志；
- 不通过历史迭代记录自动覆盖当前说明；
- 来源任务分支的方案如果没有被采用，不得自动更新当前说明。

## 11.6 功能详情结构

```text
项目 / 模块 / 功能

功能名称：微信支付退款回调
功能编号：SHOP-F-3
状态：正常
[编辑功能] [+ 新建任务] [+ 记录一次迭代]

当前功能说明
┌─────────────────────────────────────┐
│ 接收微信退款通知，完成验签、幂等处理、 │
│ 退款状态更新和异常日志记录。          │
└─────────────────────────────────────┘

统计
├── 未完成任务：4
├── 已完成任务：12
├── 迭代记录：16
└── 遗留问题：2

[任务] [迭代记录] [GitHub] [活动]
```

---

# 12. 任务管理

## 12.1 任务定位

任务是一次具体的执行工作，主要以卡片形式创建和处理。

任务可以包括：

- 新需求；
- 缺陷修复；
- 性能优化；
- 技术重构；
- 技术调研；
- 测试验证；
- 文档补充；
- 配置调整；
- 其他研发工作。

## 12.2 任务范围

V1.1 支持两种任务范围：

```text
功能级任务
模块级任务
```

### 功能级任务

针对一个具体功能。

```text
功能：微信支付退款回调
任务：修复重复回调导致的重复退款
```

### 模块级任务

针对模块或多个功能的共同工作。

```text
模块：支付
任务：升级微信支付 SDK
影响功能：
├── 微信支付下单
├── 微信支付退款
└── 微信支付退款回调
```

## 12.3 新建任务入口

```text
全局新建
├── 从项目页面创建
├── 从模块页面创建
├── 从功能页面创建
└── 从遗留问题转换
```

### 从功能页面创建

系统自动带入：

```text
项目
模块
功能
任务范围：功能级
```

用户无需再次选择。

### 从模块页面创建

系统默认设置：

```text
项目
模块
任务范围：模块级
影响功能：可多选
```

### 从全局创建

用户选择：

```text
任务范围：
○ 某个功能
○ 某个模块 / 多个功能
```

## 12.4 新建任务字段

| 字段 | 是否必填 | 默认/来源 |
|---|---:|---|
| 任务标题 | 是 | 用户填写 |
| 任务描述 | 否 | 用户填写 |
| 所属项目 | 是 | 页面自动带入或选择 |
| 所属模块 | 是 | 由功能或用户选择 |
| 所属功能 | 功能级必填 | 模块级可为空 |
| 影响功能 | 模块级可选 | 可多选 |
| 任务范围 | 是 | 功能级/模块级 |
| 指派人 | 是 | 必须是项目成员 |
| 优先级 | 否 | 普通 |
| 截止时间 | 否 | — |
| GitHub 链接 | 否 | 可添加多个 |
| 创建人 | 自动 | 当前用户 |
| 工作状态 | 自动 | 默认未完成 |

## 12.5 新建任务页面

```text
┌─────────────────────────────────────┐
│ 新建任务                              │
├─────────────────────────────────────┤
│ 任务标题 *                            │
│ [修复重复回调导致的重复退款________]   │
│                                     │
│ 任务范围                              │
│ ● 功能级任务                          │
│ ○ 模块级任务                          │
│                                     │
│ 所属项目：商城系统（自动）             │
│ 所属模块：支付（自动）                 │
│ 所属功能：微信支付退款回调（自动）     │
│                                     │
│ 指派给 *                              │
│ [张三 ▼]                              │
│                                     │
│ 任务描述                              │
│ [微信重复回调可能导致重复退款……]       │
│                                     │
│ 优先级：[普通 ▼]                       │
│ GitHub：[+ 添加链接]                   │
│                                     │
│                    [创建任务]          │
└─────────────────────────────────────┘
```

## 12.6 任务状态

### 工作状态

```text
未完成
已完成
已取消
```

用户界面核心只展示：

```text
未完成 / 已完成
```

“已取消”放在更多筛选中。

### 状态流转

```text
未完成 ─────────────→ 已完成
  ↑                    │
  │                    │ 重新打开
  └────────────────────┘

未完成 ─────────────→ 已取消
  ↑                    │
  └──────恢复───────────┘
```

### 规则

1. 新建任务默认为未完成；
2. 完成任务时自动记录完成时间；
3. 重新打开任务时不删除已有迭代记录；
4. 重新打开时保留原完成时间和重新打开时间；
5. 已取消任务不计入完成率；
6. 已完成任务不会从系统中消失；
7. 合并关系不改变工作状态。

---

# 13. 任务历史保留规则

## 13.1 任务完成后的保留方式

例如：

```text
T-101 修复重复回调导致的重复退款
```

### 完成前

```text
任务状态：未完成
负责人：张三
迭代记录：0 条
```

### 完成并生成记录后

```text
任务状态：已完成
完成时间：2025-04-15
负责人：张三
迭代记录：CR-201
```

任务仍然存在，变化如下：

```text
从“未完成任务”区域移出
进入“已完成任务”区域
在功能历史中显示来源任务
仍可通过编号、搜索和链接访问
```

## 13.2 不同任务状态的展示位置

| 任务状态 | 默认展示位置 | 是否可搜索 |
|---|---|---:|
| 未完成 | 待办区 | √ |
| 已完成 | 已完成区/历史区 | √ |
| 已取消 | 取消区，默认折叠 | √ |
| 历史来源任务 | 合并组来源分支 | √ |
| 活动来源任务 | 待办区和合并组 | √ |
| 已归档任务 | 历史区 | 管理员可查 |

## 13.3 任务保留的具体内容

任务完成或合并后，以下信息全部保留：

```text
任务编号
任务标题
任务描述
任务范围
项目、模块、功能归属
负责人
创建人
创建时间
完成时间
原工作状态
迭代记录
GitHub 链接
合并关系
操作日志
```

## 13.4 没有迭代记录的任务也保留

例如测试任务：

```text
T-105：完成退款回归测试
状态：已完成
迭代记录：无
完成说明：仅完成测试验证，不涉及功能代码变化
```

该任务仍然保留，因为它是研发过程的一部分。

## 13.5 任务删除规则

### 项目成员

不能物理删除任务，但可以：

- 编辑任务；
- 完成任务；
- 重新打开任务；
- 取消任务；
- 合并任务；
- 解除合并。

### 系统管理员

可以将明显错误或垃圾任务标记为无效，但：

- 有正式迭代记录的任务不得删除；
- 已删除任务保留编号和审计快照；
- 默认列表不展示；
- 管理员可以恢复。

## 13.6 任务继续使用还是新建

| 场景 | 处理方式 |
|---|---|
| 原任务只是补充少量实现 | 继续使用原任务 |
| 原任务完成后需要补充验证 | 重新打开原任务 |
| 产生新的独立需求 | 新建任务 |
| 同一功能后续出现新的问题 | 新建任务 |
| 只是补录历史资料 | 直接新建迭代记录 |
| 发现两个任务重复 | 合并任务 |
| 两个任务相关但仍独立推进 | 建立关联，不合并 |

---

# 14. 模块级任务和多功能任务

## 14.1 解决的问题

一次技术工作可能同时影响多个功能：

```text
升级微信支付 SDK
```

它可能同时涉及：

```text
微信支付下单
微信支付退款
微信支付退款回调
```

如果强制拆成三条任务，会造成重复劳动。

## 14.2 推荐结构

```text
项目：商城系统
└── 模块：支付
    ├── 功能：微信支付下单
    ├── 功能：微信支付退款
    ├── 功能：微信支付退款回调
    │
    └── 模块级任务：升级微信支付 SDK
        └── 模块级迭代记录：完成 SDK 升级及接口适配
            ├── 影响功能：微信支付下单
            ├── 影响功能：微信支付退款
            └── 影响功能：微信支付退款回调
```

## 14.3 功能页面展示方式

进入“微信支付退款回调”详情页：

```text
迭代记录

● 2025-04-20
  升级微信支付 SDK 及接口适配
  标签：模块级变更
  来源任务：T-201
  影响功能：3 个
  [查看完整记录]
```

这不是复制三条记录，而是同一条记录的引用展示。

## 14.4 模块级记录统计规则

一个模块级任务影响三个功能：

```text
任务数量：1
迭代记录数量：1
影响功能数量：3
```

不允许统计成：

```text
任务数量：3
迭代记录数量：3
```

避免项目进度失真。

## 14.5 什么时候建议拆分

只有满足以下情况时才建议拆分：

1. 不同功能由不同人员独立负责；
2. 不同功能状态不同；
3. 不同功能验收标准不同；
4. 不同功能上线时间不同；
5. 不同功能需要独立回滚；
6. 不同功能采用不同解决方案；
7. 一个功能完成，另一个功能可能长期延期；
8. 后续必须独立统计某个功能的执行结果。

### 示例一：不拆分

```text
SDK 升级
三个功能统一修改
统一测试
统一上线
同一个负责人
```

结果：

```text
一个模块级任务
一条模块级迭代记录
```

### 示例二：建议拆分

```text
下单接口由张三负责
退款接口由李四负责
回调接口延期处理
三个功能验收时间不同
```

结果：

```text
三个功能级任务
各自产生迭代记录
```

## 14.6 模块级任务的限制

- 模块级任务必须选择模块；
- 影响功能可以为空；
- 如果选择影响功能，所有功能必须属于该模块；
- 任务只保留一份；
- 迭代记录只保留一份；
- 可以后续增加或删除影响功能；
- 影响功能变更必须写入审计日志。

---

# 15. 任务完成与迭代记录流程

## 15.1 完成任务总流程

```text
点击“完成任务”
        ↓
是否产生实际功能变化？
        │
   ┌────┴────┐
   │         │
  是        否
   │         │
   ▼         ▼
填写迭代记录   选择完成说明
   │         │
   ▼         ▼
发布迭代记录   任务直接完成
   │
   ▼
任务状态更新为已完成
```

## 15.2 完成任务弹窗

```text
┌─────────────────────────────────────┐
│ 完成任务：T-101                      │
├─────────────────────────────────────┤
│ 本次工作是否产生了实际功能变化？      │
│                                     │
│ ○ 是，填写迭代记录                    │
│ ○ 否，仅完成任务                      │
│                                     │
│ [取消] [继续]                         │
└─────────────────────────────────────┘
```

## 15.3 选择“是”

系统自动带入：

```text
任务标题
项目
模块
功能或影响功能
任务范围
负责人
任务描述
已有 GitHub 链接
任务分支
```

用户只需要补充三项核心内容和可选信息。

发布成功后系统同时完成：

```text
创建迭代记录
关联任务
记录发布人
记录发布时间
更新任务为已完成
记录完成时间
写入审计日志
发送通知
```

这些操作必须在一个事务中完成。

## 15.4 选择“否”

用户选择完成原因：

```text
测试验证
技术调研
文档补充
环境配置
沟通协调
其他
```

系统记录：

```text
任务状态：已完成
迭代记录：无
完成说明：测试验证，不涉及功能变化
```

## 15.5 事务一致性要求

不允许出现：

```text
任务已完成，但迭代记录保存失败
```

也不允许出现：

```text
迭代记录已发布，但任务仍显示未完成
```

如果记录发布失败：

- 任务保持未完成；
- 返回错误提示；
- 用户可以重试；
- 不生成重复记录。

---

# 16. 迭代记录设计

## 16.1 迭代记录定位

迭代记录是系统的核心知识资产，用于记录：

```text
为什么改
发现了什么问题
改了什么
怎么处理
结果如何
如何验证
还有什么问题
对应什么代码证据
```

## 16.2 V1.1 用户填写字段

最终默认表单只保留以下字段：

| 字段 | 是否必填 | 说明 |
|---|---:|---|
| 迭代标题 | 功能直接创建时必填；任务完成时自动带入 | 默认使用任务标题 |
| 为什么改、发现了什么问题 | 是 | 合并背景、原有问题、根因 |
| 改了什么、怎么改的 | 是 | 合并改动内容和解决方式 |
| 改完效果如何、如何验证 | 是 | 合并结果和验证方式 |
| 还有什么问题 | 否 | 遗留风险或待优化点 |
| GitHub 链接 | 否 | 支持多个 Issue、PR、Commit |

## 16.3 删除或合并的字段

以下字段不再作为默认用户录入项：

| 原字段 | V1.1 处理 |
|---|---|
| 变更类型 | 删除 |
| 实际变更时间 | 自动生成 |
| 实际修改人 | 自动带入任务负责人 |
| 记录作者 | 自动生成 |
| 问题根因 | 合并到“为什么改、发现了什么问题” |
| 解决方式 | 合并到“改了什么、怎么改的” |
| 修改后结果 | 合并到“改完效果如何、如何验证” |
| 遗留问题是否有 | 不单独设置，文本为空即无 |
| 版本/发布批次 | 系统版本自动生成；软件发布批次后续建设 |
| 关联任务 | 从任务进入时自动关联 |

## 16.4 自动字段

以下信息由系统自动生成或带入：

```text
迭代记录编号
项目
模块
功能或影响功能
任务范围
来源任务
来源分支
迭代标题默认值
记录作者
处理人
创建时间
发布时间
记录版本号
记录状态
最后修改人
最后修改时间
```

## 16.5 人员自动带入规则

### 从任务完成进入

```text
处理人：任务指派人
记录作者：当前登录用户
```

如果两者相同，页面显示：

```text
处理人：张三
```

如果不同，显示：

```text
处理人：张三
记录作者：李四
```

### 从功能直接创建

```text
处理人：当前登录用户
记录作者：当前登录用户
```

### 特殊情况

如果实际由多人共同完成，不增加复杂人员选择字段，建议在“改了什么、怎么改的”中说明参与人员。

系统管理员可在高级信息中修正处理人，修正动作必须记录日志。

## 16.6 时间自动生成规则

V1.1 默认使用：

```text
记录创建时间：保存草稿时生成
记录发布时间：发布时生成
任务完成时间：完成任务时生成
```

页面上显示：

```text
记录时间：2025-04-15 16:30
```

不要求普通用户手工填写“实际修改时间”。

如果后续需要补录很久以前的历史资料，可以增加“历史补录”入口，由系统管理员指定原发生时间，并记录审计日志；该字段不放在默认表单中。

## 16.7 迭代记录表单

```text
┌─────────────────────────────────────┐
│ 新增功能迭代记录                      │
├─────────────────────────────────────┤
│ 迭代标题                             │
│ [修复重复回调导致的重复退款________]   │
│                                     │
│ 所属项目：商城系统（自动）             │
│ 所属模块：支付（自动）                 │
│ 所属功能：微信支付退款回调（自动）     │
│ 来源任务：T-101（自动）                │
│ 处理人：张三（自动）                   │
│ 记录人：李四（自动）                   │
│                                     │
│ 1. 为什么改、发现了什么问题 *          │
│ [请填写背景、原有问题和问题原因……]     │
│                                     │
│ 2. 改了什么、怎么改的 *                │
│ [请填写修改内容、处理方式和核心方案…]   │
│                                     │
│ 3. 改完效果如何、如何验证 *             │
│ [请填写结果、测试方式和验证结论……]      │
│                                     │
│ 4. 还有什么问题（选填）                │
│ [没有可不填，有风险请填写……]           │
│                                     │
│ GitHub 链接（选填）                    │
│ [+ 添加 Issue / PR / Commit]           │
│                                     │
│ [保存草稿] [发布并完成任务]             │
└─────────────────────────────────────┘
```

## 16.8 填写示例

```text
迭代标题：
修复重复回调导致的重复退款

为什么改、发现了什么问题：
微信支付可能重复发送退款通知，原系统没有做幂等校验，导致同一笔退款可能生成多条退款流水。

改了什么、怎么改的：
增加商户订单号唯一索引，并在处理回调前增加重复请求判断。

改完效果如何、如何验证：
重复发送 100 次退款回调只生成一条退款流水，自动化测试和回归测试均通过。

还有什么问题：
大批量异常回调场景下的告警策略还需要进一步优化。

GitHub：
PR #245
Commit 8f3a1c
```

## 16.9 记录状态

```text
草稿
已发布
已作废
```

### 草稿

- 保存后不计入正式迭代统计；
- 所有项目成员可查看和补充；
- 任务保持未完成；
- 可以继续编辑。

### 已发布

- 计入功能历史；
- 所有项目成员可查看；
- 不能直接覆盖原版本；
- 修改时自动生成新版本。

### 已作废

- 默认不在时间线展示；
- 系统管理员可以查看；
- 必须保留作废原因；
- 不能物理删除；
- 普通项目成员读取详情时按不可见资源返回 404；
- 只有通过重认证的系统管理员可以填写恢复原因并恢复为已发布状态；恢复后普通成员可重新查看并进入正常搜索、统计和时间线；原作废时间、作废原因和完整状态变更历史必须保留，状态与历史不变量见 [ADR-024](docs/adr/ADR-024.md)。

## 16.10 已发布记录修改规则

### 内容补充或修正

```text
CR-201 v1
  ↓ 编辑
CR-201 v2
```

旧版本仍然保留。

### 新的实际功能变化

不能修改旧记录，而应该新建：

```text
CR-201：增加幂等校验
CR-202：增加退款失败重试
```

### 判断标准

| 场景 | 处理方式 |
|---|---|
| 补充原记录遗漏的说明 | 新版本 |
| 修正原记录中的错误文字 | 新版本 |
| 增加同一次修改的验证结果 | 新版本 |
| 后续发生新的代码修改 | 新建迭代记录 |
| 第二次优化同一功能 | 新建迭代记录 |

---

# 17. 遗留问题闭环

## 17.1 遗留问题设计

V1.1 不单独设计复杂的问题管理实体，迭代记录中保留一个选填文本框：

```text
还有什么问题？
```

用户不填写时，系统显示：

```text
暂无已知遗留问题
```

用户填写后，记录显示：

```text
存在遗留问题
```

## 17.2 转为新任务

```text
迭代记录
  ↓
发现遗留问题
  ↓
点击“转为新任务”
  ↓
自动创建任务
```

### 自动带入内容

```text
任务标题：
跟进遗留问题：大数据量导出内存占用较高

任务描述：
来源迭代记录：SHOP-CR-201
原始遗留问题：大数据量场景下内存占用较高……

所属项目：自动带入
所属模块：自动带入
所属功能：自动带入
任务范围：自动带入
```

### 用户补充内容

- 指派人；
- 优先级；
- 截止时间。

## 17.3 规则

1. 新任务默认为未完成；
2. 原遗留问题文本不被删除；
3. 迭代记录显示关联任务；
4. 同一条遗留问题只能生成一个有效跟进任务；
5. 跟进任务完成后可以生成新的迭代记录；
6. 新记录中可以说明“已解决来源记录 CR-XXX 的遗留问题”。

---

# 18. 任务合并与来源分支

## 18.1 核心定义

任务合并不是：

```text
删除副任务
复制副任务内容
覆盖主任务内容
搬迁迭代记录
```

任务合并是：

> **建立主任务与来源任务之间的聚合关系。**

合并后：

```text
任务聚合组
├── 主任务
├── 来源任务 A
└── 来源任务 B
```

每个来源任务就是一条独立的历史分支。

## 18.2 合并前

```text
功能：微信支付退款回调

T-101：修复重复退款
└── CR-201：增加商户订单号幂等校验

T-108：处理重复回调
├── CR-205：增加回调唯一标识
└── CR-206：修复退款重复入账
```

## 18.3 合并后

```text
功能：微信支付退款回调
└── 任务聚合组 TG-001：退款回调重复处理
    │
    ├── 主任务 T-101：修复重复退款
    │   └── CR-201：增加商户订单号幂等校验
    │
    └── 来源任务分支 T-108：处理重复回调
        ├── CR-205：增加回调唯一标识
        └── CR-206：修复退款重复入账
```

## 18.4 “分支”的准确含义

这里的“分支”是任务历史分支，不是 GitHub 代码分支。

它表示：

- 原任务仍然存在；
- 原任务仍有自己的任务信息；
- 原任务仍有自己的迭代记录；
- 原任务仍有自己的负责人和创建人；
- 原任务通过关系指向主任务；
- 主任务可以统一查看所有分支。

## 18.5 合并范围

### 允许合并

| 场景 | 是否允许 |
|---|---:|
| 同项目、同功能 | √ |
| 同项目、不同功能 | √ |
| 同项目、不同模块 | √ |
| 一个功能级任务和一个模块级任务 | √ |
| 已完成任务和未完成任务 | √ |
| 已有正式迭代记录的任务 | √ |
| 有 GitHub 链接的任务 | √ |

### 不允许直接合并

| 场景 | 处理方式 |
|---|---|
| 不同项目 | 使用相关关系或重新创建公共任务 |
| 不同系统实例 | 不支持 |
| 任务合并到自己 | 禁止 |
| 形成循环关系 | 禁止 |
| 已属于其他聚合组的任务 | 先解除原关系 |
| 聚合组整体嵌套 | V1.1 不支持 |

## 18.6 跨功能合并后的归属

例如：

```text
T-101 属于功能：微信支付退款
T-108 属于功能：退款结果通知
```

合并后：

```text
T-101 仍属于微信支付退款
T-108 仍属于退款结果通知
```

系统不会自动改变任务归属。

任务聚合组可以跨功能，但：

- 原任务归属不变；
- 原迭代记录归属不变；
- 原 GitHub 链接归属不变；
- 功能时间线不被强行合并；
- 聚合组详情统一展示双方信息。

## 18.7 合并分支类型

### 历史来源分支

适用于：

```text
两个任务本质重复，后续工作统一到主任务
```

特点：

- 保留原任务和全部历史；
- 默认不再承接新的执行工作；
- 新工作提示用户转到主任务；
- 原迭代记录仍可查看；
- 已发布记录仍可通过新版本修订；
- 默认从主要待办中折叠。

### 活动来源分支

适用于：

```text
两个任务目标相近，但仍需要并行处理
```

特点：

- 来源任务可以继续执行；
- 可以继续指派；
- 可以继续完成；
- 可以继续生成新的迭代记录；
- 会计入当前待办和工作量统计。

## 18.8 合并操作流程

```text
发现重复或相关任务
        ↓
点击“合并到其他任务”
        ↓
选择主任务
        ↓
系统展示双方任务信息
        ↓
选择分支类型
        ↓
填写合并说明
        ↓
确认合并
        ↓
创建任务聚合组或加入已有聚合组
        ↓
建立来源分支
        ↓
保留双方任务和迭代记录
        ↓
写入审计并发送通知
```

## 18.9 合并弹窗

```text
┌─────────────────────────────────────┐
│ 合并任务                              │
├─────────────────────────────────────┤
│ 当前任务：T-108 处理重复回调           │
│ 当前状态：已完成                       │
│ 现有迭代记录：2 条                     │
│ 所属功能：退款结果通知                 │
│                                     │
│ 合并到主任务：                         │
│ [搜索任务编号或标题________________]   │
│                                     │
│ 主任务：T-101 修复重复退款              │
│ 当前状态：未完成                       │
│ 现有迭代记录：1 条                     │
│ 所属功能：微信支付退款                 │
│                                     │
│ 合并后处理方式：                       │
│ ● 历史来源分支                         │
│ ○ 活动来源分支                         │
│                                     │
│ 合并说明（选填）                       │
│ [两个任务均处理退款重复回调问题……]     │
│                                     │
│ 提示：两条任务的迭代记录不会被删除或覆盖。│
│                                     │
│                 [取消] [确认合并]       │
└─────────────────────────────────────┘
```

## 18.10 合并后的字段处理

| 字段 | 处理方式 |
|---|---|
| 主任务标题 | 保留主任务标题 |
| 来源任务标题 | 原样保留 |
| 主任务描述 | 不被覆盖 |
| 来源任务描述 | 原样保留 |
| 主任务负责人 | 不自动变更 |
| 来源任务负责人 | 原样保留 |
| 主任务状态 | 不受来源任务影响 |
| 来源任务状态 | 原样保留 |
| GitHub 链接 | 各自保留，聚合页统一展示 |
| 迭代记录 | 各自保留，不复制 |
| 创建人 | 永久保留 |
| 创建时间 | 永久保留 |
| 合并说明 | 写入合并关系和审计日志 |

## 18.11 有正式迭代记录的任务合并

允许合并：

```text
T-101 有 CR-201
T-108 有 CR-205、CR-206
```

合并后：

```text
T-101
└── CR-201

T-108 来源分支
├── CR-205
└── CR-206
```

不能把：

```text
CR-205.task_id = T-108
```

改成：

```text
CR-205.task_id = T-101
```

因为这样会丢失真实来源。

## 18.12 聚合任务中的历史展示

```text
任务聚合组：退款回调重复处理

主任务 T-101
├── CR-201 增加商户订单号幂等校验
│   来源：主任务
│
来源分支 T-108
├── CR-205 增加回调请求唯一标识
│   来源：T-108
└── CR-206 修复退款重复入账
    来源：T-108
```

支持筛选：

```text
[全部记录]
[主任务]
[来源任务 T-108]
[来源任务 T-112]
```

## 18.13 合并后的新工作归属

### 历史来源分支

点击新建工作时提示：

```text
当前任务是历史来源分支，后续工作建议归入主任务 T-101。

[去主任务创建] [仍创建独立任务]
```

默认后续工作归入主任务。

### 活动来源分支

可以继续：

- 创建任务；
- 修改负责人；
- 完成任务；
- 创建迭代记录；
- 关联 GitHub。

## 18.14 解除合并

操作名称统一使用：

```text
解除合并
```

不使用“回滚”，避免与代码回滚混淆。

### 解除规则

- 系统管理员和项目成员都可以解除；
- 必须二次确认；
- 建议填写解除原因；
- 原任务恢复独立展示；
- 原工作状态保持不变；
- 原负责人保持不变；
- 原迭代记录不移动；
- 合并关系不删除，而是标记为已解除；
- 主任务历史不受影响。

### 示例

合并前：

```text
T-108：未完成
```

合并为历史来源后：

```text
T-108：
工作状态：未完成
关系状态：历史来源分支
主任务：T-101
```

解除后：

```text
T-108：
工作状态：未完成
关系状态：独立任务
```

不能因为解除合并就统一恢复为“未完成”或“已完成”，必须保持合并前状态。

---

# 19. 功能页面展示设计

## 19.1 功能页总体结构

```text
项目 / 模块 / 功能

┌─────────────────────────────────────────────┐
│ 微信支付退款回调                            │
│ F-003   状态：正常                          │
│ [编辑功能] [+ 新建任务] [+ 记录一次迭代]     │
├─────────────────────────────────────────────┤
│ 当前功能说明                                │
│ 接收退款通知，完成验签、幂等和状态更新。      │
├─────────────────────────────────────────────┤
│ 未完成任务 4 | 已完成任务 12 | 迭代记录 16    │
├─────────────────────────────────────────────┤
│ [任务] [迭代记录] [GitHub] [活动]             │
└─────────────────────────────────────────────┘
```

## 19.2 任务 Tab

```text
任务

未完成（4）
┌─────────────────────────────┐
│ T-101 修复重复退款           │
│ 负责人：张三  优先级：高      │
│ [主任务] [迭代记录 1 条]      │
└─────────────────────────────┘

┌─────────────────────────────┐
│ T-105 增加失败重试机制        │
│ 负责人：李四                  │
└─────────────────────────────┘

已完成（12）
┌─────────────────────────────┐
│ T-098 增加退款日志            │
│ 负责人：王五  迭代记录 1 条    │
└─────────────────────────────┘

模块级任务（影响本功能）
┌─────────────────────────────┐
│ T-201 升级微信支付 SDK        │
│ 影响功能：3 个                │
│ [模块级]                      │
└─────────────────────────────┘

任务聚合组
┌─────────────────────────────┐
│ TG-001 退款回调重复处理       │
│ 主任务：T-101                 │
│ 来源分支：T-108、T-112        │
│ [展开分支]                    │
└─────────────────────────────┘
```

## 19.3 迭代记录 Tab

```text
迭代记录
[全部来源] [主任务] [来源任务] [模块级影响]

● 2025-04-20
  升级微信支付 SDK 及接口适配
  标签：模块级变更
  来源：T-201
  影响功能：3 个
  [展开]

● 2025-04-15
  增加商户订单号幂等校验
  来源：主任务 T-101
  [展开]

● 2025-04-12
  增加回调请求唯一标识
  来源：来源任务 T-108
  [展开]
```

## 19.4 迭代记录展开内容

```text
为什么改、发现了什么问题：
微信重复回调可能造成重复退款，原系统缺少幂等处理。

改了什么、怎么改的：
增加商户订单号唯一索引，并在回调处理前增加重复判断。

改完效果如何、如何验证：
重复发送 100 次回调仅生成一条退款流水，测试通过。

还有什么问题：
异常回调告警策略还需要进一步优化。

来源：
主任务 T-101

GitHub：
PR #245
Commit 8f3a1c
```

---

# 20. 任务详情抽屉

点击任务卡片后，从右侧打开详情抽屉。

```text
┌─────────────────────────────────────┐
│ T-108 处理重复回调              [×] │
│ [已完成] [来源任务] [...]             │
├─────────────────────────────────────┤
│ 负责人：李四                          │
│ 创建人：王五                          │
│ 所属项目：商城系统                    │
│ 所属模块：支付                        │
│ 所属功能：微信支付退款回调            │
│ 任务范围：功能级                      │
│ 创建时间：2025-04-01                  │
│ 完成时间：2025-04-12                  │
├─────────────────────────────────────┤
│ 任务描述                              │
│ 处理微信重复退款通知问题。             │
├─────────────────────────────────────┤
│ 工作状态：已完成                      │
│ 合并关系：来源任务                    │
│ 分支类型：历史来源                    │
│ 主任务：T-101                         │
├─────────────────────────────────────┤
│ 本任务迭代记录                        │
│ CR-205 增加回调请求唯一标识             │
│ CR-206 修复退款重复入账                 │
├─────────────────────────────────────┤
│ 功能最近迭代                           │
│ CR-201 增加商户订单号幂等校验            │
│ CR-205 增加回调请求唯一标识              │
│ [查看完整功能历史]                     │
├─────────────────────────────────────┤
│ GitHub 关联                            │
│ PR #243   Commit 8f3a1c                 │
├─────────────────────────────────────┤
│ [查看主任务] [解除合并]                 │
└─────────────────────────────────────┘
```

### 抽屉展示规则

- 默认展示当前任务信息；
- 展示当前任务产生的迭代记录；
- 展示所属功能最近 3 条迭代记录；
- 不在抽屉中加载全部历史；
- 提供“查看完整功能历史”入口；
- 来源任务必须显示主任务链接；
- 模块级任务必须显示影响功能列表。

---

# 21. 卡片、列表和时间线视图

| 使用场景 | 推荐视图 |
|---|---|
| 单个功能内查看未完成任务 | 卡片/看板 |
| 单个功能内查看已完成任务 | 折叠卡片 |
| 跨项目查看我的任务 | 列表 |
| 功能库 | 列表 |
| 迭代历史 | 时间线 |
| 任务详情 | 右侧抽屉 |
| 任务聚合组 | 分支分组列表 |

## 21.1 为什么不是所有地方都用卡片

卡片适合：

- 快速浏览；
- 状态切换；
- 单个功能下任务数量较少的场景。

列表适合：

- 跨项目查询；
- 批量筛选；
- 查看负责人、模块、功能、时间等多个字段；
- 任务数量较多的场景。

---

# 22. GitHub 关联功能

## 22.1 V1.1 目标

V1.1 中 GitHub 主要作为：

```text
代码证据和研发过程链接
```

本系统负责：

```text
功能上下文
改动原因
问题与方案
验证结果
遗留问题
```

## 22.2 支持的链接类型

```text
Issue
Pull Request
Commit
Branch
Release
Repository
Compare
其他 GitHub 链接
```

## 22.3 支持多链接

一个任务或迭代记录可以关联多个链接：

```text
功能：微信支付退款回调
├── Issue #231
├── PR #245
├── PR #251
├── Commit 8f3a1c
└── Release v2.6.0
```

## 22.4 链接添加方式

```text
[+ 添加 GitHub 链接]
粘贴 URL
  ↓
系统识别链接类型
  ↓
显示类型和编号
  ↓
保存
```

示例：

```text
https://github.com/company/shop/pull/245
```

展示为：

```text
PR #245
```

## 22.5 URL 解析与路径识别范围

系统必须使用标准 URL Parser 解析链接，并按 HTTPS 协议、GitHub 域名白名单和路径段规则进行校验；不得使用正则表达式判断主机名或作安全边界。通过已校验的路径结构可以识别：

- 仓库所有者与仓库名称；
- Issue 编号；
- PR 编号；
- Commit Hash；
- Release Tag。

本地 URL 解析不能获取：

- PR 标题；
- PR 作者；
- PR 是否合并；
- Issue 当前状态；
- 审核状态；
- 代码改动内容。

这些信息只能由未来经 ADR 批准的 GitHub API 集成获取。V1.1 不调用 GitHub API、不保存 GitHub Token，也不在服务端抓取链接内容。

## 22.6 V1.1 GitHub 范围

### 必须支持

- 手动添加多个 GitHub URL；
- Issue、PR、Commit 类型识别；
- URL 格式校验；
- 链接去重；
- 新窗口打开；
- 合并后保留来源任务链接。

### 暂不强制支持

- GitHub OAuth；
- Webhook；
- 自动同步 PR 状态；
- 自动生成迭代记录；
- 自动将 PR 合并转换为任务完成。

## 22.7 GitHub 与任务状态的关系

以下规则必须明确：

```text
PR 合并 ≠ 任务完成
PR 合并 ≠ 测试通过
PR 合并 ≠ 功能上线
```

任务是否完成仍由系统用户确认。

## 22.8 推荐编号关联

任务和功能生成稳定编号：

```text
SHOP-F-3
SHOP-T-101
```

研发人员可以在 GitHub 标题中带编号：

```text
[SHOP-T-101] 修复重复退款
[SHOP-F-3] 增加退款幂等校验
```

后续可根据编号自动提示关联，但需要用户确认。

---

# 23. 全局导航和页面结构

## 23.1 全局导航

```text
工作台
项目
我的任务
迭代动态
全局搜索
通知中心
系统管理
```

## 23.2 项目内导航

```text
项目概览
功能库
模块
我的项目任务
迭代记录
项目成员
GitHub
项目设置
```

## 23.3 左侧树形导航

```text
项目
├── 商城系统
│   ├── 支付
│   │   ├── 微信支付下单
│   │   ├── 微信支付退款
│   │   └── 微信支付退款回调
│   ├── 订单
│   └── 会员
└── CRM 系统
    ├── 客户
    └── 权限
```

### 规则

- 默认展示项目和模块；
- 展开模块后加载功能；
- 不在左侧树中加载所有任务和迭代记录；
- 支持搜索；
- 支持最近访问；
- 功能数量过多时采用懒加载；
- 系统管理员可以查看全部项目；
- 项目成员只显示已加入项目。

---

# 24. 搜索和筛选

## 24.1 搜索范围

```text
项目名称
项目编码
模块名称
功能名称
功能当前说明
任务编号
任务标题
任务描述
迭代记录编号
迭代标题
为什么改
改动内容
结果验证
遗留问题
GitHub 编号
GitHub URL
```

V1 搜索支持中文短词、完整英文缩写、完整代码标识符和完整编号；不保证
任意英文子串或代码标识符内部片段的子串搜索，也不提供正则表达式搜索。
普通搜索输入必须按 PGroonga 普通文本语义转义，不得透传正则或高级查询
语法。正式索引与查询方式见 [ADR-025](docs/adr/ADR-025.md)。

## 24.2 搜索结果分类

```text
搜索：退款回调

功能
├── F-003 微信支付退款回调

任务
├── T-101 修复重复退款
├── T-108 处理重复回调

迭代记录
├── CR-201 增加商户订单号幂等校验
├── CR-205 增加回调唯一标识

遗留问题
└── 异常回调告警策略不完善
```

## 24.3 筛选条件

```text
项目
模块
功能
任务范围
任务状态
负责人
是否有迭代记录
是否有遗留问题
是否有 GitHub
是否为来源任务
分支类型
记录时间
记录作者
```

---

# 25. 通知功能

## 25.1 通知原则

通知只发送给需要采取行动或关注结果的人，避免通知所有项目成员造成打扰。

## 25.2 通知场景

| 事件 | 通知对象 |
|---|---|
| 项目成员被加入 | 被加入用户 |
| 任务被指派 | 新负责人 |
| 任务负责人被修改 | 新负责人，旧负责人可选 |
| 任务完成 | 任务创建人、相关记录作者 |
| 任务重新打开 | 任务负责人、创建人 |
| 任务被合并 | 主任务负责人、来源任务负责人、双方创建人 |
| 任务解除合并 | 相关任务负责人和创建人 |
| 迭代记录发布 | 相关任务负责人、功能相关人员 |
| 迭代记录被修改 | 原记录作者、任务负责人 |
| 遗留问题转为任务 | 新任务负责人 |

## 25.3 通知展示

```text
顶部导航铃铛
  ↓
未读数量
  ↓
通知列表
  ↓
点击跳转任务、功能或迭代记录
```

---

# 26. 活动动态和审计日志

## 26.1 活动动态

面向普通项目成员，使用自然语言：

```text
张三将 T-101 标记为已完成
李四发布了 CR-201
王五将 T-108 合并到 T-101
赵六新增了模块级任务 T-201
```

## 26.2 原始审计

系统管理员可以查看：

```text
操作人
操作时间
操作类型
目标对象
操作前数据
操作后数据
请求结果
```

## 26.3 审计规则

- 审计日志只追加；
- 不能编辑；
- 不能删除；
- 不向项目成员直接展示敏感 JSON；
- Token、密码等敏感字段必须脱敏；
- 合并和解除合并保存双方任务快照；
- 迭代记录版本保存完整快照。

---

# 27. 数据模型设计

## 27.1 实体关系图

```text
User 用户
  │
  ├── ProjectMember 项目成员关系
  │         │
  │         └── Project 项目
  │                │
  │                └── Module 模块
  │                       │
  │                       └── Feature 功能
  │                              │
  │                              ├── Task 功能级任务
  │                              └── ChangeRecord 功能级迭代记录
  │
  └── AuditLog 操作日志

Module
  ├── Task 模块级任务
  └── ChangeRecord 模块级迭代记录
          └── ChangeRecordFeatureRelation 影响功能关系

Task
  └── TaskGroup / TaskBranch 任务聚合和来源分支

Task / Feature / ChangeRecord
  └── ExternalLink GitHub 链接
```

## 27.2 User 用户表

| 字段 | 说明 |
|---|---|
| id | 用户 ID |
| name | 姓名 |
| avatar | 头像 |
| is_admin | 是否系统管理员 |
| status | 正常/停用 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

## 27.3 Project 项目表

| 字段 | 说明 |
|---|---|
| id | 项目 ID |
| code | 项目编码，唯一 |
| name | 项目名称 |
| description | 项目描述 |
| created_by | 创建人 |
| status | ACTIVE / ARCHIVED |
| created_at | 创建时间 |
| updated_at | 更新时间 |
| archived_at | 归档时间 |

## 27.4 ProjectMember 项目成员表

| 字段 | 说明 |
|---|---|
| id | 主键 |
| project_id | 项目 ID |
| user_id | 用户 ID |
| status | ACTIVE / REMOVED |
| joined_at | 加入时间 |
| removed_at | 移除时间 |

不设置：

```text
project_role
is_project_admin
is_readonly
```

## 27.5 Module 模块表

| 字段 | 说明 |
|---|---|
| id | 模块 ID |
| project_id | 所属项目 |
| name | 模块名称 |
| description | 模块说明 |
| status | ACTIVE / ARCHIVED |
| sort_order | 排序 |
| created_by | 创建人 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

## 27.6 Feature 功能表

| 字段 | 说明 |
|---|---|
| id | 功能 ID |
| project_id | 所属项目 |
| module_id | 所属模块 |
| code | 功能编号 |
| name | 功能名称 |
| current_behavior | 当前功能说明 |
| tags | 可选标签，最多 50 个 |
| status | ACTIVE / ARCHIVED |
| created_by | 创建人 |
| created_at | 创建时间 |
| updated_at | 更新时间 |
| archived_at | 归档时间 |
| row_version | 并发控制版本 |

## 27.7 Task 任务表

| 字段 | 说明 |
|---|---|
| id | 任务 ID |
| project_id | 项目 ID |
| module_id | 模块 ID |
| feature_id | 功能 ID，模块级任务可为空 |
| scope_type | FEATURE / MODULE |
| code | 任务编号 |
| title | 任务标题 |
| description | 任务描述 |
| assignee_id | 指派人 |
| creator_id | 创建人 |
| priority | LOW / NORMAL / HIGH / URGENT |
| work_status | TODO / DONE / CANCELED |
| lifecycle_status | ACTIVE / ARCHIVED / INVALID |
| completion_note | 完成说明 |
| completed_at | 完成时间 |
| task_group_id | 任务聚合组，可为空 |
| created_at | 创建时间 |
| updated_at | 更新时间 |
| row_version | 并发控制版本 |

### 任务归属约束

```text
scope_type = FEATURE
→ feature_id 必须有值
→ feature.module_id = task.module_id

scope_type = MODULE
→ feature_id 可以为空
→ module_id 必须有值
```

## 27.8 TaskFeatureRelation 任务影响功能关系表

用于模块级任务影响多个功能：

| 字段 | 说明 |
|---|---|
| task_id | 任务 ID |
| feature_id | 影响功能 ID |
| relation_type | IMPACT |
| created_at | 创建时间 |

## 27.9 TaskGroup 任务聚合组表

| 字段 | 说明 |
|---|---|
| id | 聚合组 ID |
| project_id | 项目 ID |
| main_task_id | 主任务 ID |
| name | 聚合组名称 |
| status | ACTIVE / CLOSED |
| created_by | 创建人 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

## 27.10 TaskBranch 任务分支表

| 字段 | 说明 |
|---|---|
| id | 分支 ID |
| task_group_id | 聚合组 ID |
| task_id | 对应任务 |
| branch_role | MAIN / SOURCE |
| branch_mode | HISTORICAL / ACTIVE |
| original_work_status | 合并前工作状态 |
| original_assignee_id | 合并前负责人 |
| status | ACTIVE / DETACHED |
| joined_at | 加入时间 |
| detached_at | 解除时间 |

### 数据约束

```text
一个有效任务只能属于一个有效聚合组
一个聚合组只能有一个主任务
一个来源任务不能形成循环关系
V1.1 不支持聚合组嵌套
```

## 27.11 ChangeRecord 迭代记录表

| 字段 | 说明 |
|---|---|
| id | 记录 ID |
| project_id | 项目 ID |
| module_id | 模块 ID |
| feature_id | 功能 ID，模块级记录可为空 |
| scope_type | FEATURE / MODULE |
| task_id | 来源任务，可为空 |
| title | 迭代标题 |
| context_problem | 为什么改、发现了什么问题 |
| change_solution | 改了什么、怎么改的 |
| result_verification | 改完效果如何、如何验证 |
| leftover | 遗留问题，可为空 |
| handler_id | 处理人，自动带入 |
| author_id | 记录作者，自动生成 |
| status | DRAFT / PUBLISHED / VOID；当前状态和可见性的唯一真相 |
| current_version | 当前版本 |
| created_at | 创建时间 |
| published_at | 发布时间 |
| voided_at | 最近一次作废时间；恢复后保留，与 void_reason 成对 |
| void_reason | 最近一次作废原因；恢复原因及完整多轮历史进入审计 |
| row_version | 乐观锁版本；每次状态迁移递增 |
| updated_at | 更新时间 |

## 27.12 ChangeRecordFeatureRelation 记录影响功能关系表

用于模块级迭代记录：

| 字段 | 说明 |
|---|---|
| change_record_id | 迭代记录 ID |
| feature_id | 影响功能 ID |
| created_at | 创建时间 |

一条模块级记录可以关联多个功能，但数据库只存一份。

## 27.13 ChangeRecordVersion 记录版本表

| 字段 | 说明 |
|---|---|
| id | 版本 ID |
| record_id | 迭代记录 ID |
| version_no | 版本号 |
| snapshot | 内容快照 |
| created_by | 修改人 |
| created_at | 修改时间 |

## 27.14 ExternalLink 外部链接表

| 字段 | 说明 |
|---|---|
| id | 链接 ID |
| owner_type | Task / Feature / ChangeRecord / Project |
| owner_id | 对象 ID |
| provider | GITHUB |
| link_type | ISSUE / PR / COMMIT / RELEASE 等 |
| url | 原始 URL |
| repository | 仓库 |
| external_number | Issue/PR 编号 |
| external_sha | Commit Hash |
| title_snapshot | 标题缓存，可为空 |
| state_snapshot | 状态缓存，可为空 |
| created_by | 添加人 |
| created_at | 添加时间 |

## 27.15 AuditLog 操作日志表

| 字段 | 说明 |
|---|---|
| id | 日志 ID |
| user_id | 操作人 |
| action | 操作类型 |
| target_type | 对象类型 |
| target_id | 对象 ID |
| before_data | 操作前快照 |
| after_data | 操作后快照 |
| created_at | 操作时间 |

---

# 28. 核心业务流程图

## 28.1 创建项目流程

```text
系统管理员 / 项目成员
        ↓
点击“新建项目”
        ↓
填写项目名称、描述
        ↓
系统生成项目编码
        ↓
选择初始项目成员
        ↓
创建者自动勾选
        ↓
创建项目
        ↓
自动创建未分类模块
        ↓
写入项目成员关系
        ↓
发送加入通知
```

## 28.2 创建功能和任务流程

```text
创建项目
  ↓
创建模块
  ↓
创建功能
  ↓
新建功能级任务
  ↓
选择负责人
  ↓
任务进入未完成状态
```

模块级任务流程：

```text
进入模块
  ↓
新建任务
  ↓
选择模块级任务
  ↓
选择影响功能（可选）
  ↓
选择负责人
  ↓
任务进入未完成状态
```

## 28.3 完成任务流程

```text
点击完成
  ↓
是否产生实际功能变化？
  ├── 否
  │   ├── 选择完成原因
  │   └── 任务变为已完成
  │
  └── 是
      ├── 打开迭代记录表单
      ├── 自动带入任务信息
      ├── 填写三项核心内容
      ├── 发布迭代记录
      ├── 任务变为已完成
      └── 写入日志并通知
```

## 28.4 遗留问题转任务流程

```text
查看迭代记录
  ↓
填写遗留问题
  ↓
点击“转为新任务”
  ↓
系统带入项目、模块、功能和问题内容
  ↓
选择负责人
  ↓
创建未完成任务
  ↓
建立记录与任务的双向关系
```

## 28.5 任务合并流程

```text
发现重复任务
  ↓
选择来源任务
  ↓
选择主任务
  ↓
查看双方任务和记录
  ↓
选择历史来源 / 活动来源
  ↓
确认合并
  ↓
建立任务聚合组
  ↓
保留来源分支
  ↓
保留所有迭代记录
  ↓
通知相关人员
```

---

# 29. 统计口径

## 29.1 有效任务

默认有效任务：

```text
未被标记无效
且未取消
且不是历史来源分支
```

活动来源分支可以计入当前工作量。

## 29.2 任务完成率

```text
已完成有效任务数
÷
（已完成有效任务数 + 未完成有效任务数）
```

不计入：

- 已取消任务；
- 已删除或无效任务；
- 历史来源分支。

## 29.3 模块级任务统计

一个模块级任务影响多个功能：

```text
任务数：1
迭代记录数：1
影响功能数：N
```

## 29.4 功能记录数量

- 功能直接记录：计 1 条；
- 功能级任务产生的记录：计 1 条；
- 模块级记录在多个功能页面引用时：仍计 1 条；
- 记录版本不增加迭代记录数量，只增加版本数量。

---

# 30. 技术实现约束

## 30.1 完成任务事务

以下操作在同一事务执行：

```text
创建迭代记录
更新任务状态
记录完成时间
写入审计日志
```

## 30.2 合并事务

以下操作在同一事务执行：

```text
校验任务关系
创建任务聚合组
创建主分支
创建来源分支
保存合并快照
写入审计日志
```

## 30.3 解除合并事务

```text
关闭来源分支关系
恢复独立展示
保存解除原因
写入审计日志
```

## 30.4 并发控制

两个用户同时修改任务或合并任务时：

- 使用乐观锁 `row_version`；
- 或使用数据库行锁；
- 防止同一任务同时进入两个聚合组；
- 防止重复生成迭代记录；
- 冲突时提示：

```text
该数据已被其他用户更新，请刷新后重试。
```

## 30.5 搜索索引

全文搜索至少覆盖：

```text
功能名称
当前功能说明
任务标题
任务描述
迭代标题
为什么改
改了什么
改动结果
遗留问题
GitHub 编号
```

索引采用 PGroonga `pgroonga_text_full_text_search_ops_v2`（默认
TokenBigram）。普通查询使用：

```sql
normalized_search_text &@~ app.pgroonga_query_escape($1)
```

用户输入不得直接进入未转义的 `&~` 或 Groonga 高级查询语法；任意英文/代码
子串及正则搜索不属于 V1 范围。

## 30.6 数据安全

- 所有接口必须进行后端权限校验；
- 不能只依赖前端隐藏按钮；
- 项目数据必须按照项目成员关系过滤；
- Markdown 内容进行 XSS 过滤；
- V1.1 只保存用户提交的 HTTPS GitHub 链接，不保存 GitHub Token，也不调用 GitHub API；
- V1.1 服务端不向 GitHub URL 发起网络请求；未来远程抓取能力必须另行评审凭据、配额与 SSRF 防护；
- 审计日志中的敏感字段脱敏。

---

# 31. 核心验收标准

## 31.1 创建项目

验收条件：

- 系统管理员可以创建项目；
- 项目成员可以创建项目；
- 创建项目表单包含初始项目成员多选框；
- 创建者在创建表单中默认勾选且不能取消；项目创建后可由系统管理员按普通成员规则移除；
- 移除创建者后，项目创建人记录保持不变，创建者不因历史身份获得额外权限；
- 可以一次选择多个初始成员；
- 创建成功后成员可以访问项目；
- 系统自动创建未分类模块；
- 项目编码唯一；
- 创建动作写入审计日志。

## 31.2 创建任务

验收条件：

- 项目成员可以创建任务；
- 功能级任务必须选择功能；
- 模块级任务可以不选择功能；
- 指派人必须是项目成员；
- 任务默认为未完成；
- 创建后生成唯一任务编号；
- 所属项目、模块、功能关系正确；
- 指派人收到通知。

### F-14 实施边界（2026-09-09）

功能详情内提供任务卡片/列表、新建、编辑与详情抽屉。字段为标题、说明、优先级、截止时间和负责人；负责人必须明确选择，空值返回 422，不自动指派。创建与真正改派只接受当前项目活跃成员；未改变负责人时允许保留已移除或停用的历史负责人，不能因此阻止编辑其他字段。创建通知新负责人，真正改派只通知新负责人，其他编辑不产生指派通知；这些通知与任务、审计、活动和搜索同事务提交。

编号按现有数据库约束使用 `项目编码-T-正整数`，不补零。任务创建固定 `FEATURE/TODO/ACTIVE`，项目、模块、功能、编号和创建人不可在编辑中改变；创建时写入初始状态历史。编辑冲突使用编辑前快照、当前草稿和最新数据三方比较，同字段冲突必须明确选择后再提交最新版本。

本批只实现 F-14 基本任务信息和指派。任务完成、取消、重开属于 F-16；模块级任务属于后续 F-15。记录域尚无真实公开读取能力，抽屉暂不展示任务迭代记录、所属功能最近三条记录或完整历史链接，也不显示假数据、假 0 统计和无效按钮；原抽屉记录要求仍为记录域接入后的验收项。来源任务/主任务关系与其关联入口由任务组纵切片接入，不在本批伪造。

## 31.3 任务完成并生成迭代记录

验收条件：

- 点击完成后出现是否产生实际功能变化的选择；
- 选择“是”时打开记录表单；
- 任务信息自动带入；
- 三项核心字段必填；
- 发布成功后任务变为已完成；
- 发布失败时任务保持未完成；
- 任务和记录不会出现状态不一致；
- 记录出现在功能时间线中。

## 31.4 模块级任务

验收条件：

- 可以在模块页面创建模块级任务；
- 可以关联多个影响功能；
- 一个任务只保存一份；
- 一个迭代记录只保存一份；
- 相关功能页面可以看到该记录；
- 功能页面显示“模块级变更”标签；
- 统计时不重复计算。

## 31.5 任务历史保留

验收条件：

- 已完成任务仍可查询；
- 已取消任务仍可查询；
- 任务编号和链接不变；
- 任务原始负责人和创建人保留；
- 任务已有迭代记录不丢失；
- 任务已有 GitHub 链接不丢失；
- 历史任务默认不干扰活跃待办统计。

## 31.6 有记录任务合并

给定：

```text
T-101 有 1 条正式迭代记录
T-108 有 2 条正式迭代记录
```

合并后必须满足：

- T-101 仍然存在；
- T-108 仍然存在；
- T-101 作为主任务；
- T-108 作为来源任务分支；
- 三条迭代记录全部保留；
- 每条记录显示正确来源；
- GitHub 链接全部保留；
- 原任务状态不被覆盖；
- 主任务详情可以查看来源分支；
- 合并行为写入审计日志。

## 31.7 解除合并

验收条件：

- 项目成员可以发起解除合并；
- 必须二次确认；
- 来源任务恢复独立；
- 原工作状态保持不变；
- 迭代记录不移动；
- 合并和解除事件都保留；
- 相关人员收到通知。

## 31.8 记录版本

验收条件：

- 已发布记录不能直接覆盖；
- 编辑后生成新版本；
- 旧版本仍可查看；
- 可以对比版本差异；
- 作废或恢复记录必须由系统管理员执行；
- 作废原因、恢复原因和状态变更历史被记录。

## 31.9 GitHub

验收条件：

- 任务和迭代记录可以添加多个链接；
- 能识别 Issue、PR、Commit；
- 非法 URL 被拦截；
- 重复 URL 有提示；
- 点击链接可以打开 GitHub；
- 合并任务后链接不丢失；
- GitHub 状态不会直接覆盖任务状态。

---

# 32. V1.1 功能范围

## 32.1 V1.1 必须完成

### 基础结构

- 用户登录；
- 系统管理员；
- 项目成员；
- 项目创建；
- 初始项目成员选择；
- 项目成员管理；
- 项目归档；
- 模块管理；
- 功能管理。

### 任务功能

- 功能级任务；
- 模块级任务；
- 任务创建；
- 任务指派；
- 未完成/已完成；
- 取消和恢复；
- 任务卡片；
- 任务列表；
- 任务详情抽屉；
- 任务历史保留。

### 迭代记录

- 完成任务时生成记录；
- 功能直接新增记录；
- 三项核心字段；
- 遗留问题；
- GitHub 多链接；
- 记录草稿；
- 记录发布；
- 记录版本；
- 记录作废；
- 记录恢复；
- 记录历史时间线。

### 合并和分支

- 任务合并；
- 有迭代记录任务合并；
- 主任务；
- 历史来源分支；
- 活动来源分支；
- 解除合并；
- 合并前后状态保留；
- 分支来源展示。

### 辅助功能

- 全局搜索；
- 项目概览；
- 迭代动态；
- 基础通知；
- 操作日志；
- 系统管理员审计。

## 32.2 后续版本

- GitHub OAuth；
- GitHub App；
- Webhook；
- PR 合并自动生成迭代记录草稿；
- 自动识别任务编号；
- 自动检测重复任务；
- 评论；
- @成员；
- 附件；
- 关注功能；
- 多级模块；
- 版本发布管理；
- GitLab、Bitbucket 等平台接入。

---

# 33. V1.1 最终产品结构

```text
系统
├── 用户
│   ├── 系统管理员
│   └── 项目成员
│
└── 项目：商城系统
    ├── 项目成员：张三、李四、王五
    │
    ├── 模块：支付
    │   ├── 功能：微信支付下单
    │   │   ├── 任务
    │   │   └── 迭代记录
    │   │
    │   ├── 功能：微信支付退款
    │   │   ├── 任务
    │   │   └── 迭代记录
    │   │
    │   ├── 功能：微信支付退款回调
    │   │   ├── 任务
    │   │   ├── 迭代记录
    │   │   └── 模块级任务引用
    │   │
    │   └── 模块级任务：升级微信支付 SDK
    │       └── 模块级迭代记录
    │           ├── 影响功能 A
    │           ├── 影响功能 B
    │           └── 影响功能 C
    │
    └── 任务聚合组
        ├── 主任务 T-101
        │   └── 主分支迭代记录
        │
        ├── 来源任务 T-108
        │   └── 来源分支迭代记录
        │
        └── 来源任务 T-112
            └── 来源分支迭代记录
```

---

# 34. 最终业务结论

V1.1 最终采用以下规则：

1. **项目成员和系统管理员都可以创建项目；**
2. **创建项目时可以勾选初始项目成员；**
3. **创建表单中创建者自动成为初始成员且不能取消；创建完成后可由系统管理员按普通成员规则移除，项目创建人记录保持不可变且不授予额外权限；**
4. **不设置项目管理员和只读成员；**
5. **项目成员拥有项目内所有普通研发操作权限；**
6. **只有高风险、影响全局或可能破坏历史的操作由系统管理员负责；**
7. **项目是顶层业务容器；**
8. **模块负责分类；**
9. **功能负责长期档案；**
10. **任务负责一次执行工作；**
11. **迭代记录负责保存实际变化；**
12. **模块级事项不强制拆分为多个功能任务；**
13. **模块级任务可以影响多个功能，任务本身只保存一份；每条迭代记录独立保存，同一任务可以关联多条记录；**
14. **所有任务完成、取消或合并后都保留；**
15. **保留的具体方式是：保留数据、保留关系、保留历史、从默认待办中移出；**
16. **已有正式迭代记录的任务仍然允许合并；**
17. **合并后形成主任务和来源任务分支；**
18. **来源任务的原始状态、负责人、迭代记录和 GitHub 链接全部保留；**
19. **迭代记录表单精简为三项核心内容加两个选填项；**
20. **人员、时间、项目、模块、功能、任务、版本等信息自动生成；**
21. **GitHub 支持多个链接，但不直接控制任务完成状态；**
22. **所有历史修改、合并、解除合并和作废操作必须留痕。**

最终产品可以概括为：

> **项目负责承载范围，模块负责分类，功能负责沉淀，任务负责推进，迭代记录负责还原变化，来源分支负责保留合并历史。**

## F-15 模块级任务实施补充（2026-09-09）

模块入口支持单份 MODULE 任务创建/编辑/读取及当前影响功能集合，feature_id 为 NULL；影响功能可以为空，最多 1000 个输入 ID，规范化去重排序。功能页通过引用展示，每页按唯一任务行计数，不能把多影响关系计作多份任务。引用入口引导到真实模块路径编辑；不同负责人、状态、验收、上线或回滚仍建议拆分。

依据 §14.6/§27.8 及既有 Schema/权限，协调确认 task_feature_impacts 为当前影响集合，移除只删除本任务明确移除的关系，完整关系前后快照及增删差异写入同事务不可变审计；重加产生新 created_at，旧关系历史保留在审计。任务、状态历史、审计本身不删除，此语义不能泛化到任务组历史成员。MODULE 的真实父级是项目/模块；新增或重加影响要求功能 ACTIVE，已有关联可保留或移除 ARCHIVED 功能，但始终校验同项目同模块。归档功能页仍只读。详细本地范围与验收见 [F-15 交审说明](docs/f15-local-handoff.md)。记录域、任务完成状态流转及记录统计仍不在本批，不显示假统计。


## F-16 状态与历史实施补充（2026-09-10）

FEATURE/MODULE 已本地实现 TODO→DONE、DONE→TODO、TODO→CANCELED、CANCELED→TODO；完成时间、历次完成/重开快照、操作者和可选原因保存在不可变状态历史。默认待办移出完成/取消任务，状态筛选仍可读历史和搜索；本批不展示完成率；未来正式统计必须按 §29.2 排除取消、无效及历史来源分支，当前列表比例不作为正式完成率。MODULE 既有影响功能归档不改变真实父级；引用页到模块入口操作。

当前完成只支持 §15.4 无实际功能变化的六类原因，服务端拒绝 WITH_RECORD。实际变化选择显示尚未开放且不提交完成；F17 草稿保存不完成任务，发布并完成留至 F19 同一事务。详细范围、验证和后续边界见 [F-16 交审说明](docs/f16-local-handoff.md)。
