# ADR-010：pg_trgm 与搜索投影的双阶段门禁

- 状态：Superseded
- 基线固化：2026-09-04
- 被替代：ADR-025

## 背景

系统需要中文、编号和混合字符搜索，但当前规模不足以证明需要独立搜索服务。

## 决策

本期采用 PostgreSQL `pg_trgm` 与只读 SearchProjection。阶段 0 验证中文召回和索引计划，阶段 4 验证五年峰值容量、并发和权限过滤。

## 影响与验证

任一门禁不通过都不得宣称搜索可用；阶段 0 失败必须在阶段 1 前以新 ADR 切换或补充方案。
