# ADR-001：采用模块化单体

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

系统规模较小，但项目、任务、记录、审计和投影之间需要强事务一致性。拆分微服务会提前引入分布式事务与运维成本。

## 决策

后端采用模块化单体。模块通过公开的 CommandPort、QueryPort 或 Workflow 协作，禁止跨模块直接访问 Repository。

## 影响与验证

部署和本地开发保持简单；未来只有在容量或组织边界有实证时才评估拆分。CI 必须检查 import 图、Port 方向及跨模块 Repository 访问。
