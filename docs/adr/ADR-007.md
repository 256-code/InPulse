# ADR-007：TaskGroupMember 是任务组归属唯一真相

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

在 tasks、task_groups 和成员表重复保存组归属会产生不可恢复的双重真相。

## 决策

TaskGroupMember 唯一表达活跃组归属及 MAIN/SOURCE 身份；删除 `tasks.group_id` 和 `task_groups.main_task_id` 冗余字段。

## 影响与验证

合并、解除和主任务查询只读取成员表。部分唯一索引与提交期约束保证每组恰有一个 MAIN、每任务至多属于一个活跃组。
