# ADR-016：系统管理员 TOTP MFA 与重认证

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

成员管理、审计读取、用户启停等高风险动作不能只依赖普通登录态。

## 决策

所有系统管理员强制注册 TOTP MFA；高风险动作要求密码与当前 TOTP 重认证。另一名 MFA 管理员可执行重置；单管理员场景只允许双人审批的离线恢复，重置后撤销全部 Session。

## 影响与验证

TOTP Secret 加密、恢复码只存哈希并防重放。测试覆盖受限注册、重置、恢复、会话撤销和审计记录。
