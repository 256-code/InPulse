# ADR-006：PostgreSQL hash-only Session Store

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

单实例内部系统需要主动失效会话，但无需为此单独引入 Redis。

## 决策

Session 存 PostgreSQL，数据库只保存 Token 哈希；使用用户级 `auth_version` 实现统一失效。Session 数据不进入业务备份，恢复后所有旧会话失效。

## 影响与验证

会话读写增加数据库负载，但减少组件。测试覆盖登录轮换、撤销、停用用户及恢复后的统一失效。
