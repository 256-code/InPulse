# ADR-009：本期不实现通用 Outbox

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

本期没有逐业务事件的可靠外部投递需求，引入 Outbox 会同时引入 worker、租约、积压、死信和顺序治理。

## 决策

不建立通用 Outbox 表或 worker。站内通知、审计、ActivityProjection 与 SearchProjection 在业务事务内同步写入；远端审计归档是读取已提交记录的独立幂等运维任务。

## 影响与验证

跨系统可靠投递不是本期能力。出现明确需求时必须以新 ADR 定义语义和运行保障。
