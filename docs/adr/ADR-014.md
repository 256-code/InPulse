# ADR-014：使用脱敏 ActivityProjection

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

原始审计包含安全与内部快照，不适合作为普通项目成员的动态流。

## 决策

项目动态由字段白名单控制的 ActivityProjection 提供，并与业务事务同步写入。普通成员不得直接读取原始审计。

## 影响与验证

投影必须保存来源链标识并设置唯一约束。查询前通过 `ProjectAccessQueryPort` 取得服务端授权 Scope，测试覆盖移除成员和跨项目隔离。
