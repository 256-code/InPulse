# ADR-002：前端采用 React SPA

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

本系统是无需 SEO 和 SSR 的内部重交互应用。

## 决策

前端采用 React SPA 与 Vite，不引入 Next.js。生产由 Nginx 直接提供构建后的静态资源，并只将 `/api/v1/**` 代理到 API。

## 影响与验证

客户端负责路由和数据获取；必须验证刷新回退、静态资源缓存、API 同源及 CSP。
