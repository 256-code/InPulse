# ADR-005：PostgreSQL 18、Drizzle 与显式迁移

- 状态：Accepted
- 基线固化：2026-09-04

## 背景

核心模型依赖复合外键、部分唯一索引、行锁和可审查的 SQL。

## 决策

采用 PostgreSQL 18 与 Drizzle ORM `>=0.45.2,<0.46`；manifest 和 lockfile 锁定实际补丁。迁移显式生成、提交、人工评审并由独立迁移任务执行，应用启动不得自动迁移。

## 影响与验证

升级必须通过生成结果、空库迁移、回滚和锁行为测试。生产 runtime 无 DDL 权限。
