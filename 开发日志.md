# InPulse 开发日志

本文件记录每次写代码时实际做了什么，包括功能、修复、重构、优化、测试与验证、文档同步和后续事项。开发人员在向远端推送功能分支或创建/更新 Pull Request 前必须更新本文件；评审同时检查日志是否完整、真实。

> 维护原则：日志记录事实，不替代 PR 描述、提交说明、测试报告或 ADR。没有执行过的检查必须写明“未运行”，不得根据设计稿虚构结果。

## 一、记录规则

1. 每次向远端推送岗位固定工作分支（`dev/a`、`dev/b`、`dev/c`）或交付功能分支（首次推送、追加提交、修正推送）前，必须在本文件“记录”区新增或更新对应条目；一个 PR 至少包含一条记录，同一分支多次推送时通过“推送序号”区分。
2. 每条记录至少包含：日期时间、岗位/负责人、分支、推送序号、PR/commit、变更类型、做了什么、新增功能、修复/重构/优化、测试与验证、文档同步、风险与后续事项。
3. 测试与验证必须使用实际执行的命令和结果；失败、跳过和未运行的检查必须如实写明，不得只写“CI 通过”。
4. 日志条目随产生它的 PR 一起提交。PR 合并后，该条目视为历史记录，不再修改；补充内容应新增记录或说明，不重写已经合入的事实。
5. 日志不得包含 Secrets、Token、密码、生产数据、数据库备份、日志中的敏感信息或本地路径。
6. 如果本次推送没有代码可记录，则不应推送；临时调试、未完成且未在 PR 中说明的改动不构成可记录交付。

## 二、记录模板

新记录放在“记录”区最前面，直接复制以下模板：

```markdown
## 日期时间 | 岗位/负责人 | 分支 | 推送序号

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `refactor` / `perf` / `test` / `docs` / `ci` / `chore` |
| 分支 | `dev/a`、`dev/b`、`dev/c` 或 `feature/...`、`fix/...` 等 |
| PR / commit | `#123` 或 `abcdef` |
| 做了什么 | 一句话概括本次交付结果 |
| 新增功能 | 用户可感知的新能力及验收点 |
| 修复 / 重构 / 优化 | 解决的问题、性能或结构改善，以及影响范围 |
| 测试与验证 | 实际执行命令、结果、覆盖点；未执行项明确标注 |
| 文档同步 | 更新的设计、ADR、权限矩阵、测试矩阵、Runbook 等 |
| 风险与后续 | 已知风险、未验证事项、下一步工作 |
| 备注 | 本次推送是否完成、是否有待评审或回填 |
```

## 三、记录

## 2026-09-10 18:36 +08:00 | B / F-22 外部链接 | codex/f22-external-links | 本地交审 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix`、`test`、`docs` |
| 分支 | `codex/f22-external-links`；基线仍为`5087f0acd81220a99501d62bf009160777ccf542` |
| PR / commit | 增量代码`b099b6ad46186c4fbe4b69ec75c26819f92c5182`，未建PR、未推送，文档独立提交 |
| 做了什么 | 修父审核指出的读取/重放使用过期预读状态，以及审计动作声明与实写不一致 |
| 新增功能 | 无扩展功能；四域QueryPort显式共享/更新锁，GET和重放在父锁后锁目标重读并持有至读取/授权结束 |
| 修复 / 重构 / 优化 | 受控作废竞态下普通GET及添加/解除重放统一404，管理员只读；auditAction统一EXTERNAL_LINK_ADDED/REMOVED并由四类CRUD真库断言对齐Registry |
| 测试与验证 | 最终增量代码F22真库36/36（3.87秒），契约2/2（342ms，在代码提交上复跑），API局部测试类型、89路由/5生成物漂移、9个增量源码Prettier API check、diff检查通过；新3条竞态均以真实父锁/F21状态迁移先复现200再修为404，4条审计一致性断言先红后绿 |
| 文档同步 | 三设计、权限矩阵、测试矩阵、[F22交审增量](docs/f22-local-handoff.md)、本日志；OpenAPI由生成器更新2个auditAction，既有指纹payload不含auditAction，指纹/客户端不变 |
| 风险与后续 | 初批收尾误删仍使用的PostgresFeatureReadPort import，故之前“未用import清理不影响已验证行为”的判断错误；本次beforeAll实际失败，已恢复后才完成红绿回归。新增poll选项误写intervals导致类型失败，改interval后通过。历史结果不冒充最终SHA重跑结果；父协调下一步独立复验 |
| 备注 | 本次只做父要求的两个修复与对应测试/文档；最后成功测试后无代码编辑；独占库再次核对后停止，数据保留交父，未推送/合并/关机；前端/浏览器/无关测试本轮未重复 |



## 2026-09-10 18:28 +08:00 | B / F-22 外部链接 | codex/f22-external-links | 本地交审 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat`、`test`、`docs` |
| 分支 | `codex/f22-external-links`，基线 `5087f0acd81220a99501d62bf009160777ccf542` |
| PR / commit | 代码 `45594039a95b778c26bc75b7d403d3a4c1c1d036`；未建PR、未推送，文档独立本地提交 |
| 做了什么 | 完成F22四类真实目标当前GitHub链接纵切片，三条严格类型化路由、四域公开端口、生成客户端、五处前端入口 |
| 新增功能 | 多链接列表/添加/解除确认，Issue/PR/Commit及Release展示、URL安全规范化、项目内实体去重、版本与数据库幂等、链接标识搜索；Release仍存OTHER |
| 修复 / 重构 / 优化 | 解除保留实体/审计，正式版本及遗留不变；共享SearchWriter同事务组合URL但不污染raw_text；容量和仓库元数据由500修为安全422；409刷新失败/关闭重开保留门禁；来源任务合并原关联保留 |
| 测试与验证 | 定向真库4文件71/71（F22 33+搜索4+F21 22+发布12），Web5文件27/27，URL/搜索模块17/17，契约3文件39/39，Edge2/2；89路由/权限、5生成物无漂移、局部类型、48个变更源码Prettier API check及diff检查通过。失败包括初始红测、任务fixture编号/状态历史、容量500、修订头/字段和归档时间fixture、JSON类型、路由快照、浏览器搜索标题误定位/漏负责人；完整顺序保留于交审说明 |
| 文档同步 | 功能/系统/技术设计、权限矩阵、测试矩阵、[F22交审说明](docs/f22-local-handoff.md)、本日志 |
| 风险与后续 | 共享搜索写适配器及容量422映射需父独立复核；本批GitHub CI、全仓静态、全量测试、额外全量构建、无关审计未运行；父/PR任务负责审核、CI与合并 |
| 备注 | 独占真库核对data_directory后正常停止，数据保留；浏览器服务已退出。没有新迁移、枚举/角色/依赖变更；未操作用户主工作区，未关机 |



## 2026-09-10 17:38 +08:00 | A / 平台与访问域（契约平台） | main | 推送 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `main` |
| PR / commit | `5563bcf`（推送 1 的直接推送提交）；无 PR |
| 做了什么 | 回填推送 1 的 GitHub Actions 结果 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | `Documentation` [run 34460580277](https://github.com/256-code/InPulse/actions/runs/34460580277) 成功（8s）；`CI / workspace` [run 34460580275](https://github.com/256-code/InPulse/actions/runs/34460580275) 成功（12m12s，含生产镜像构建、Trivy 扫描与 Browser E2E）；两条均为 `main` push 触发 |
| 文档同步 | 本日志条目 |
| 风险与后续 | CI 绿不等于评审完成：直推 `main` 的裁决仍需非作者人工复核，含工作书 F-32 步骤 1 的处方偏差；四条路由仍未登记 Route Registry，登记须与实现同一个 PR |
| 备注 | 推送 1 的条目按日志规则保持原样不再修改，以本条目补充其结果；本次推送自身的 CI 结果不写入本条目 |

## 2026-09-10 17:17 +08:00 | A / 平台与访问域（契约平台） | main | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `main` |
| PR / commit | 无 PR，按 A 指令直推 `main`；squash 合入前的上游基线为 `fc7bb68` |
| 做了什么 | 落库 F-25 / F-29 / F-32 的 A 契约评审裁决，回应 C 反馈的「C-003 / C-010 与 Q-01 ~ Q-15 未裁决」 |
| 新增功能 | 无运行时行为变化 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | 实际执行 `node scripts/check_docs.mjs`：`Checked Git whitespace state and 67 Markdown files: links and anchors are valid.`；`git diff --check` 无输出。未运行 `pnpm lint`、`pnpm typecheck`、`pnpm build`、单测、集成测试与 E2E——本次未改任何代码、迁移、契约源或生成物 |
| 文档同步 | 新增 [A 的契约评审裁决](docs/a-contract-review-f25-f29-f32.md)；回填 [前端生成客户端消费需求清单](docs/frontend-generated-client-consumption-requirements.md) §4.8.1 / §6 / §7、[C 聚合读契约提案](docs/c-aggregate-read-contract-proposal.md) §2 / §5 / §6、[C 端口扩展提案](docs/c-port-extension-proposal.md) §0 / §7；更新 AGENTS.md 阶段进展 |
| 风险与后续 | 直推 `main` 未经 PR 评审，需按仓库约定补非作者人工复核（含工作书 F-32 步骤 1 的处方偏差）；四条路由尚未登记 Route Registry：`contract:validate` 要求每条登记路由都有 Controller 绑定，而聚合读依赖 B 域尚不存在的只读端口扩展，因此登记、权限矩阵、测试矩阵、OpenAPI 与生成客户端必须与实现同一个 PR 落库，未登记前不是实现依据；工作书 F-32 步骤 1 的处方偏差需非作者人工确认；上游 C-001 / C-004 / C-005 / C-007 / C-008 仍未裁决 |
| 备注 | 本轮只做裁决与状态同步，不注册路由、不修改生成物、不预登记权限矩阵与测试矩阵；未执行 GitHub Actions |

## 2026-09-10 16:44 +08:00 | B / 协调审核与PR交付 | codex/f20-leftover-task-pr | 推送 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/f20-leftover-task-pr` |
| PR / commit | [PR #88](https://github.com/256-code/InPulse/pull/88)；上一 HEAD `0f67bd910eca0831675ae8aef67ca4c869e79212` |
| 做了什么 | 修复 F-20 新增共享模块任务暴露的 F-15 E2E fixture 隔离问题 |
| 新增功能 | 无生产行为变化 |
| 修复 / 重构 / 优化 | F-15 经真实 UI 创建独立唯一模块，所属功能与任务均在该模块；详情点击限定本次精确任务标题卡片。所有原计数1、引用关系、禁用和持久化断言保留，沿用项目 fixture 清理 |
| 测试与验证 | 上一 [workspace](https://github.com/256-code/InPulse/actions/runs/34454990974) 失败12m50s：单测/真实PG/构建/镜像扫描通过，Browser E2E 36通过1失败（4.7m）；F20三例13.3s/12.4s/10.7s通过，F15全页任务详情首次匹配3、retry匹配4导致strict mode。上一 [docs](https://github.com/256-code/InPulse/actions/runs/34454990973) 5s通过。修复后隔离库CSP build+preview、明确 --project=edge 的 F15 单条1/1（用例22.9s，总26.2s），git diff --check通过 |
| 文档同步 | 本标准增量日志 |
| 风险与后续 | 首次本地启动遇旧契约dist缺导出，补必要API/契约构建；临时Edge配置首次带入未安装Chromium项目，该项目启动失败、Edge单条22.5s通过，显式限定Edge后命令全绿。未安装新浏览器、未运行完整本地套件；最新精确HEAD完整CI仍待执行 |
| 备注 | 协调授权独占610b隔离库；启动默认5432后纠正到55424，结束SHOW data_directory核对精确目录并停止，数据保留；不改生产代码，不开展F21 |

## 2026-09-10 16:24 +08:00 | B / 协调审核与PR交付 | codex/f20-leftover-task-pr | 推送 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/f20-leftover-task-pr` |
| PR / commit | [PR #88](https://github.com/256-code/InPulse/pull/88)；首次 HEAD `2903d5d` |
| 做了什么 | 修复首次 CI 暴露的 Ant Design loading 可访问名称时序问题 |
| 新增功能 | 无生产行为变化 |
| 修复 / 重构 / 优化 | 409 与刷新场景三处禁用断言改为 findByRole 等待精确无 loading 名称出现，再保留 toBeDisabled；无固定 sleep、无断言弱化 |
| 测试与验证 | 首次 [workspace](https://github.com/256-code/InPulse/actions/runs/34454749252) 在 Unit tests 失败（1m24s），Web 145 通过、1 失败；错误提示出现后按钮仍名为 loading 创建跟进任务。首次 [docs](https://github.com/256-code/InPulse/actions/runs/34454749306) 6s 通过。本地修正后 ConvertLeftoverTask 单文件 4/4（3.22s），仅该文件格式化；最新 CI 待执行 |
| 文档同步 | 本标准增量日志 |
| 风险与后续 | 协调已确认该最小修复；等新的精确 HEAD 完整 CI，通过后正常合并，不沿用旧 CI |
| 备注 | 不改业务代码或测试门禁，不开展 F-21 |

## 2026-09-10 16:21 +08:00 | B / 协调审核与PR交付 | codex/f20-leftover-task-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `docs` |
| 分支 | `codex/f20-leftover-task-pr`，同步 `origin/main` `935844b` |
| PR / commit | 接收协调审核 `029efb97ec0beaff7eb5f93a205126d92ef09e8b`；本次提交后创建 F-20 PR |
| 做了什么 | 交付稳定遗留项转换新任务纵切片；合并主线 F-09 安全收尾与 F-24 拆组，双方源定义、权限说明及历史日志保留，客户端由工具重生成 |
| 新增功能 | 遗留全文预览、真实范围继承、当前成员指派、可选截止时间、单事务转换及双向来源链接；409 后刷新并显式确认新预览，保留输入 |
| 修复 / 重构 / 优化 | 无额外业务修订；协调确认主线无 F-20 业务交叉，保留路由指纹历史，不手工修改生成物 |
| 测试与验证 | 协调审核原候选：真实 PostgreSQL 4 文件 82/82、契约 4 文件 41/41、最终 Web 3 文件 15/15；实现任务 Edge 初轮 5/5，截止时间增量 1/1，非同轮完整套件。本次同步后：contract:validate 84 条、permissions:check 84/84、contract:drift 5 产物；定向契约 4 文件 48/48（387ms），Web 2 文件 13/13（4.05s）；git diff --check 通过。Web 首次路径误写未找到文件，修正实际路径后通过。未运行全量本地构建、全仓静态检查或重复真库/E2E；最新 GitHub CI 待执行 |
| 文档同步 | 三设计、权限/测试矩阵及 F-20 交审说明随候选同步；本标准交付日志。F-19 最终 CI、合并与授权事实见本批开始日志 |
| 风险与后续 | 最新 PR CI 验证完整装配、真实数据库及截止时间浏览器路径；失败按实际原因定向处理，网络失败持续重试；F-20 合并后本批结束，不开展 F-21 |
| 备注 | 依既有用户授权和协调审核，最新 CI 全绿且无平台阻断后正常 squash；不得记作不存在的人工 Approval，不绕过保护规则 |

## 2026-09-10 16:13 +08:00 | B / Codex | codex/f20-leftover-task | 审核补齐，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `test` / `docs` |
| 分支 | `codex/f20-leftover-task`，增量基于首候选 `4b5fb5ac442e6a3020467c2bed91c74667ea0f47` |
| PR / commit | 代码增量 `4636f0e8340be58c453bd66614fb0a67c167652c`；文档独立提交，本批仍未推送/建PR |
| 做了什么 | 按父审核补齐功能§17.2遗漏的可选截止时间；原候选延后普通编辑的范围缩减已纠正 |
| 新增功能 | 原生本地日期时间输入，按既有任务表单语义转UTC ISO；清空传null，非法/溢出日期安全提示并阻止提交 |
| 修复 / 重构 / 优化 | 失败及409刷新/确认保留截止时间；时间进入请求签名，同请求失败复用Key，改时间或清空换Key；API与数据库不变 |
| 测试与验证 | ConvertLeftoverTask Web单文件4/4（3.19秒），覆盖UTC值、保留、同Key/换Key、清空与非法日期；现有F20 FEATURE Edge单例在CSP build+preview中1/1（16.1秒），填写时间后打开普通任务编辑校验准确回显。原82真库/41契约由父协调独立复跑通过（真库9.71秒），本次未重复；仅相关文件格式与git空白检查 |
| 文档同步 | 功能设计、工作书、测试矩阵、交审说明与本标准日志；删除当前交审说明中推迟截止时间的过时限制 |
| 风险与后续 | 隔离库由父明确释放后仅用于单条Edge，结束已核对目录并停止，55424释放、数据保留。无API/契约/迁移/依赖变更，不全仓静态/全量构建/过度审计；等待父增量审核，不推送、不做F21 |
| 备注 | Web首轮3/4为弹窗可见性等待不足，保留原断言并等待后4/4；Edge首轮截止时间回显已通过，后续“取消”按钮因中文自动空格未匹配超时，修正定位及返回详情流程后1/1；无断言弱化 |


## 2026-09-10 16:05 +08:00 | B / Codex | codex/f20-leftover-task | 本地交审，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f20-leftover-task`；最新主线基线 `c7c68819579a269d057131482c35749bab5cea70` |
| PR / commit | 代码 `73adfdcf7f28c50c8dbb72f2a0bb899db05cca94`；文档独立提交；本批无推送/PR/CI |
| 做了什么 | 完成F20稳定遗留项原子转TODO任务、服务端预览与任务来源读取的完整纵切片 |
| 新增功能 | 当前正式版本/记录行版本/遗留行版本与继承集合确认；当前成员指派，任务说明带来源版本及精确遗留原文；单事务任务/编号/唯一link/CONVERTED/审计/活动/搜索/通知，双向页面链接与409显式刷新确认 |
| 修复 / 重构 / 优化 | 通过公开端口复用任务创建与通知，记录转换更新row_version但不改current_version或历史快照；不同Key重复转换只返回当前有权任务引用，同Key按当前全部资源门禁重放。MODULE按本轮协调确定的实施细节仅继承ACTIVE影响，历史归档影响保留；并发归档改变集合409，未放宽数据库/任务不变量。409后刷新再遇网络错误仍保持确认门禁 |
| 测试与验证 | 真实PostgreSQL API integration：leftover-task、record-publication、published-records、tasks-api四文件82/82（11.16秒），含F20 21场景；Web ConvertLeftoverTask/PublishedRecordsView/TasksPanel 13/13（最终4.21秒）；契约leftover-task/published-records/permissions/validate 41/41。83路由/权限、5生成物漂移通过；API测试/Web/E2E定向类型、仅E2E所需API/契约tsc编译、变更文件eslint/prettier通过；依赖边界486源文件、前端148模块通过。现有CSP build+preview Edge F20三例+F18两例5/5（51.3秒）；之后只补刷新失败仍需确认，由上述Web复跑验证，未重复E2E。check:docs 63 Markdown与git diff检查通过 |
| 文档同步 | 三设计、工作书、权限/测试矩阵、Workflow README、本标准日志与F20交审说明（列明三接口权限/归档读取、版本依据、21场景和实际命令） |
| 风险与后续 | 无迁移、核心依赖或数据库权限变更；无全仓静态/全量构建/无关审计，未执行本批GitHubCI。前端到期时间沿用创建null、可由普通任务编辑设置，API支持dueAt。独占测试库核对目录后已正常停止，端口释放，数据保留；冻结候选交父协调复核→PR任务→CI，不推送、不做F21 |
| 备注 | 首契约测试因Schema不存在失败后实现通过；初次真库归档fixture遗漏archived_at触发CHECK，混合影响fixture遗漏module_id触发NOT NULL，补齐合法数据后82/82，未修改约束/降低断言。Web类型检查发现幂等Key工具缺scope及mock字段不完整，补齐后通过。F19最终合并/CI/无实际GitHub人工Approval事实见本批开工标准记录 |


## 2026-09-10 15:44 +08:00 | B / Codex | codex/f20-leftover-task | 实施开始，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f20-leftover-task`，工作区干净并成功 fetch 后从最新 main `c7c68819579a269d057131482c35749bab5cea70` 新建 |
| PR / commit | 本批无提交/PR；补记 F19 [PR #85](https://github.com/256-code/InPulse/pull/85) 已 squash，merge `c7c68819579a269d057131482c35749bab5cea70`，最终交付 `d3bf4473bc42a1344904bcd12ec268d3faa0135b`，协调确认 merge tree 一致 |
| 做了什么 | 阅读 F20 设计 §6.6/功能 §28.4、ADR-013/019、稳定遗留与任务创建实现，开始遗留转任务纵切片 |
| 新增功能 | 计划稳定遗留项在当前正式版本下原子转 TODO 任务、编号、链接、CONVERTED 与全部副作用；实现前端入口和失败/冲突恢复，当前尚未完成 |
| 修复 / 重构 / 优化 | 保留既有唯一约束/不可变历史，不改历史迁移；真实 scope 服务端推导，重复转换只返回当前有权的既有任务引用 |
| 测试与验证 | 本批尚未运行行为验证。F19 最终 [workspace](https://github.com/256-code/InPulse/actions/runs/34449977136) 成功 11m3s、[docs](https://github.com/256-code/InPulse/actions/runs/34449977129) 成功 11s，CSP build+preview Chromium 34/34，含 F19 三例与 F16 两例；上述最终事实由协调核实 |
| 文档同步 | 本标准开始日志，完成时同步三设计/权限/测试矩阵和交审说明 |
| 风险与后续 | MODULE 历史归档 impact 与新任务禁止新增归档 impact 的继承交集已报协调，未受影响工作继续；F21 不开展，不运行全仓静态/全量构建/过度审计 |
| 备注 | F19 按用户授权正常合并，实际 GitHub 人工 Approval 无，不记作真人评审；F20 只交本地候选，先不推送/建 PR |


## 2026-09-10 15:45 +08:00 | A / Codex | feature/a-f09-security-hardening | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `feature/a-f09-security-hardening`（基于 `origin/main` `77b2d7e`，即 F-09 CSP PR #83 合并之后） |
| PR / commit | 本次交付 PR（推送后创建，编号在 PR 创建后回填） |
| 做了什么 | 补齐 F-09 数据安全专项剩余三项验收：SEC-006 未匹配路由的错误契约净化、SEC-007 数据库 Secret 文件 fail closed、SEC-004 GitHub 外部链接规范化与数据库不变量 |
| 新增功能 | 未匹配路由（含全局前缀外、根路径与 `/api/v1` 本身）统一返回固定文案的 JSON 404，不回显 method、path 或框架文案；`normalizeGitHubUrl` 用标准 URL Parser 规范化 GitHub HTTPS 链接并识别 ISSUE/PULL_REQUEST/COMMIT，拒绝 http/ftp、用户信息、非默认端口、`api.github.com`、混淆域名与编码伪段；数据库 Secret 权限判定抽成可测的 `isPrivateOwnerReadableFile`，生产模式只接受 `/run/secrets` 直接子项且仅属主可读 |
| 修复 / 重构 / 优化 | `database/src/config.ts` 的 Secret 权限判定从内联条件改为导出函数，测试可直接覆盖权限矩阵；未放宽任何鉴权、CSP 或幂等策略，未新增依赖，未改动数据库迁移 |
| 测试与验证 | 实际通过：`apps/api/test/http-error-contract.integration.test.ts` 3 例（含 `X-Request-Id` 与 body 一致、已匹配路由不受影响）、`apps/api/test/github-url.test.ts` 16 例、`database/test/integration/external-links.test.ts` 7 例（同项目规范化 URL 唯一与并发去重、非 https/混淆域名/带 fragment 被 CHECK 拒绝、跨项目与错配关联被复合外键拒绝、`normalized_url` 不可变）、`database/test/unit/config.test.ts` 15 例（权限矩阵、生产路径越界、缺失变量不回退、空内容拒绝）；全量：`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`（web 39 文件 140 例、api 63 文件 309 例、契约 77 例、database 15 例）、真实 PostgreSQL 集成（database 2 文件 20 例、api 40 文件 255 例，本地 55436 集群 PostgreSQL 18.6 + PGroonga）、`pnpm build`、`db:migrations:check`、`contract:drift`、`contract:validate`、`check:deploy:test`、`check:deps`（457 文件）、`check:frontend:boundaries`、`permissions:check`（79/79 路由）、`check:secrets`（704 文件）、`check:docs`（61 文件）、公共 registry `pnpm audit --audit-level=high` 无已知漏洞，整条 `pnpm check` 在公共 registry 下全绿。未运行：`pnpm test:e2e`（本次无前端与路由行为变更）、GitHub Actions、镜像构建与 Trivy 扫描 |
| 文档同步 | `docs/test-matrix.md` F-09 章节改为数据安全专项并新增 4 条验收行（F09-SEC006-API-001、F09-SEC007-DB-001、F09-SEC004-API-001、F09-SEC004-DB-001），「审计与安全」表的 SEC-004/SEC-006/SEC-007 由 `Required` 改为本地通过；本日志与 `AGENTS.md` 阶段进展；未改动 ADR（三项均已由 ADR-022/ADR-026 与既有设计定案） |
| 风险与后续 | 仍未交付：Markdown 白名单（前端当前没有 Markdown 渲染落点，react-markdown/rehype-sanitize 属新增生产依赖，必须独立 PR 并由人工确认）、外部链接的 HTTP 关联接口（F-14 业务纵切片，需同步 Schema/Route Registry/权限矩阵/OpenAPI 与生成客户端）；SEC-007 的真实 POSIX 权限位需 Linux 环境，Windows 本机不可复现，权限判定仅在函数级覆盖；SEC-003/SEC-005 等 `Required` 行不在本次 CSP 纵切片范围；CI 绿前不得声称通过 |
| 备注 | 曾试图用包装 Express adapter `use` 的方式净化未匹配路由，本地 3 例通过但与 ADR-026 的处方（直接修正异常过滤器）不符，且触发 `contract:validate` 的 Controller 绑定扫描失败（`apps/api/src/main.ts: 未找到 setGlobalPrefix 字符串字面量`）；实测确认全局 `ApiExceptionFilter` 已把未匹配路由统一为固定文案后撤掉该包装，只保留回归用例。`check:secrets` 曾把「拒绝用户信息」用例里的字面口令误判为仓库内嵌口令，已改用白名单占位值 |

## 2026-09-10 15:40 +08:00 | C / 聚合与发现域 | codex/c-contract-proposal-f25-f29-f32 | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-contract-proposal-f25-f29-f32`（基于 `origin/main` `77b2d7e`；已 rebase 到 `935844b`，即 F-19 PR #85、F-09 SEC PR #86、F-24 PR #87 合并之后；两份文档引用的代码事实均按 `935844b` 复核） |
| PR / commit | [F-25/F-29/F-32 PR #89](https://github.com/256-code/InPulse/pull/89)；推送提交 `8295761`（rebase 到 `935844b` 后），已 squash 合入 `main` 为 `3ef5553` |
| 做了什么 | 把 C 域三项被上游设计留白的接口（F-25 任务聚合组详情、F-29 项目概览、F-32 我的任务）整理成两份可评审输入：交 A 的 HTTP 契约候选（路由、状态码、DTO）与交 B 的后端端口扩展候选（端口签名、事务约定、索引证据、SQL 骨架） |
| 新增功能 | 无用户可感知的运行时能力。新增 `docs/c-aggregate-read-contract-proposal.md`（三条候选路由 `getTaskGroup` / `getProjectOverview` / `listMyTasks`、完整候选 DTO 与 Q-01~Q-15 待裁决清单）与 `docs/c-port-extension-proposal.md`（`ModuleReadPort` / `FeatureReadPort` / `TaskQueryPort` 扩展与新建 `ChangeRecordReadPort` 共四个提案） |
| 修复 / 重构 / 优化 | 无代码改动。文档记录了两处实现前必须定案的架构冲突：① F-32「是否有迭代记录」筛选若进 `TaskQueryPort`，会与既有 `ChangeRecordsModule -> TaskQueryPort` 形成双向模块依赖环；② 功能设计 §29.1「有效任务」需排除历史来源分支，该口径落在 C 域 `task_group_members`，单靠任一域端口无法表达。给出四条路线对比并推荐「C 聚合域只读适配器 + ADR 备案例外」；复核修订后补充：③ 工作书 F-32 步骤 1 的处方（通过 TasksModule 公开只读 Port）与推荐路线 III 是**替代关系**，须由人工在 ADR 中显式裁定；④ `check:deps` 只按导入边判定环与越界，路线 I 若用裸 SQL 实现同样不被机器拦住，路线 I 与 III 的机器可见性对称；推送前另修正端口提案 §1.1 的端口计数（两个 → 三个，且均非项目维度只读），并按 `.gitattributes`（`* text=auto eol=lf`）把两份新文档行尾由 CRLF 规范为 LF |
| 测试与验证 | 实际执行：`pnpm check:docs` 通过（`Checked Git whitespace state and 65 Markdown files: links and anchors are valid.`，为 rebase 到 `935844b` 后的复跑输出）。未运行：`pnpm lint`、`pnpm typecheck`、`pnpm build`、单元测试、集成测试、E2E —— 本次仅新增两份 Markdown 与本文档条目，未改动任何 TypeScript、SQL、迁移、契约源、依赖或 CI 配置 |
| 文档同步 | 仅新增两份评审输入文档，未修改既有设计文档、ADR、权限矩阵与测试矩阵。理由：两份文档均为待接收方裁决的候选输入，按 `docs/frontend-generated-client-consumption-requirements.md` §6.1 状态机，A 接受前不得据此改动上游设计与 Route Registry |
| 风险与后续 | ① A 对 C-003（聚合接口缺口）与 C-010（候选接口未冻结）的裁决未到，提案发出后需催办；② 冲突 ①/② 裁定前 F-29 / F-32 无法开工；③ F-32 与 A 已有的 `GET /projects/{projectId}/members/{userId}/unfinished-tasks` 功能重叠，已在提案中请 A 明确两者关系；④ 索引证据显示四个提案不需要新增迁移，但若改按 `updated_at` 排序则需 B 补复合索引；⑤ 文档内全部路由与字段名在 A 落库前均属候选，不得作为实现依据；⑥ 本分支已 rebase 到 `origin/main` `935844b`，并被 [PR #89](https://github.com/256-code/InPulse/pull/89) squash 合入 `main`（`3ef5553`）；该 PR 的 `Documentation / docs` 已通过，`CI / workspace`（PR 与 `main` 各一次运行）在本次回填时仍在运行，需继续跟踪；⑦ 端口提案 §7.4 新增的工作书 F-32 处方替代问题未裁定前，F-32 不得按路线 III 开工 |
| 备注 | 本次只落盘文档与日志；本机 `gh` CLI 已认证为 `256-code`（token scopes：`gist`、`read:org`、`repo`、`workflow`），具备推送能力，但按 AGENTS.md §2 仍不得自行推送、创建 PR 或合并，需人工授权；两份文档已经过一轮复核修订（记录域导出面、端口消费方表、索引推论、`check:deps` 门禁边界、§22.3 引用、工作书 F-32 处方替代），契约与端口两条通道仍需非作者岗位评审；PR #89 在推送后由 `256-code` 账号创建并随即 squash 合并、远端分支已删除，本条目为合并后的信息回填 |

## 2026-09-10 15:25 +08:00 | B / 协调审核与PR交付 | codex/f19-task-completion-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | feat / merge / docs |
| 分支 | codex/f19-task-completion-pr；接收审核基线5020c0a并同步最新main77b2d7e |
| PR / commit | 已审核6303f7a455479e3bfa9612b4afcce8f5af6dacfb（代码a69c610220d846084b6da2c7f639a5bfa3648a6f）；本次提交后推送建PR |
| 做了什么 | 交付F19任务完成与记录发布单事务纵切片及旧COMPLETE入口收口；保留F18最终CI/合并事实补记；合入上游成员E2E和F09 nonce CSP |
| 新增功能 | WITHOUT_RECORD六类原因，WITH_RECORD互斥选择内联或明确草稿，完整事务/锁序/副作用/幂等和失败输入保留；MAIN与活动SOURCE允许，历史SOURCE拒绝 |
| 修复 / 重构 / 优化 | 两旧transition操作COMPLETE统一进组合Workflow，幂等及重放授权版本升2.0.0并保留1.0.0指纹；旧Key跨版本409。主线仅日志冲突，完整保留双方；无主线业务/契约冲突，协调已确认22文件运行环境交集可正常同步 |
| 测试与验证 | 父协调独立：6真库文件111/111、5Web文件20/20、5契约文件46/46。交付合并后：CompleteWithRecord+AppProviders两文件4/4（2.70s）；随后按父第一条最小验证要求合跑6Web文件22/22（4.51s），测试环境输出getComputedStyle伪元素未实现提示但无失败；cached diff--check通过。未重复111真库/46契约、未全量本地构建或过度审计。实现Edge及局部验证详见交审说明，来源与父/交付实测区分；新HEAD CI尚未执行 |
| 文档同步 | 本标准交付表及双方历史日志，设计/权限/测试矩阵/交接材料沿用已审核内容 |
| 风险与后续 | 上游Playwright已切vite build+preview并enforce CSP，旧Edge结果不代表新CSP环境通过；等待最新HEAD现有CI的F16/F19实际用例结果。草稿列表仍全量无分页，F20等待F19合并，F21不在范围 |
| 备注 | 无生产依赖、迁移、约束、数据库权限变更；不绕过保护或伪造人工Approval；本次合并时工作区原本干净，未操作他人数据库 |

## 2026-09-10 15:17 +08:00 | B / Codex | codex/f19-task-completion | 本地交审，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `test` / `docs` |
| 分支 | `codex/f19-task-completion`；基线 `origin/main` `5020c0a732a7b63d869f9ff418f5bd779dd951e0` |
| PR / commit | 代码 `a69c610220d846084b6da2c7f639a5bfa3648a6f`；本批未推送或创建 PR |
| 做了什么 | 完成 F19 两模式/两范围的任务完成与记录发布原子组合，并关闭旧状态 API 的 COMPLETE 绕行 |
| 新增功能 | 无变化沿用六类原因；有变化明确内联或引用草稿，来源绑定不改身份/影响快照；同事务完成、编号/v1/稳定遗留、审计/活动/搜索/通知/幂等；前端失败保留输入，冲突显式确认最新草稿，成功可打开正式记录 |
| 修复 / 重构 / 优化 | 完整父级/影响并集锁序与保存点重试；MAIN/ACTIVE SOURCE 允许、HISTORICAL SOURCE 拒绝。旧 transitionTask/transitionModuleTask 保持请求和单任务 DTO，COMPLETE 复用相同 Workflow；其他状态经 Tasks 公开端口保持原业务。两旧 operation 幂等及重放授权升级 2.0.0，原 1.0.0 指纹保留、旧 Key 409；完成通知包含全部相关记录作者 |
| 测试与验证 | API integration 六个相关文件累计 111/111：task-completion 30 + record-drafts/published-records/record-publication/task-groups-merge 40，共五文件 70/70；旧 tasks-api 独立 41/41（5.01 秒）。Web 五文件 20/20；契约五文件 46/46；contract:validate、permissions:check、contract:drift 通过（80 路由，5 产物）。API 测试/Web/E2E 定向类型及 E2E 所需 API/契约编译通过；check:deps 469 文件与前端边界 144 模块通过。Edge 兼容修改前 F16–F19 四文件 10/10（2.0 分钟），修改后 F16/F19 两文件 5/5（1.1 分钟）。无全仓静态/全量构建/无关审计，GitHub CI 未执行 |
| 文档同步 | 三份设计、工作书、权限/测试矩阵、Workflow README、F19 交审说明及本日志；无新 ADR/迁移/核心依赖/数据库权限变化 |
| 风险与后续 | 旧 Key 跨契约升级返回 409，客户端须重新确认当前资源与命令后使用新 Key。草稿列表仍沿用项目全量读取。本地独占 PostgreSQL 已核对目录并停止，端口已释放，数据保留。协调审核→PR 任务→CI→维护者合并；F20 等 F19 合并通知，F21 不在范围 |
| 备注 | 失败记录：草稿 E2E 详情 URL 缺 taskId、旧按钮定位与草稿入口、测试动效/JSON 类型问题均已修复并复跑；兼容首轮 29/30 为测试查询版本表不存在 id 列，修正为 SELECT 1 后 30/30；旧 tasks-api 测试模块漏装迁移后的 Controller 导致 13 个 404 与关联异步失败，补齐真实装配后 41/41；扫描预期重复旧位置修正后契约 46/46。文档末尾额外空行导致 check:docs 失败，已修正；最终 pnpm check:docs（62 个 Markdown 链接/锚点及 Git 空白）与 git diff --check 已通过。未跳过或弱化断言，未推送/建 PR/合并 |


## 2026-09-10 14:36 | B / Codex | codex/f19-task-completion | 实施开始，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f19-task-completion`，从成功 fetch 的 `origin/main` `5020c0a732a7b63d869f9ff418f5bd779dd951e0` 新建 |
| PR / commit | 本批尚无提交或 PR；补记 F18 [PR #82](https://github.com/256-code/InPulse/pull/82) 已由维护者合并，merge `5020c0a`，最终 head `ab96562d6222d2cd75bbdecbf95dd1fb95a0bbc5` |
| 做了什么 | 开始 F19 TaskCompletionWorkflow，核对技术 §6.3 与任务组当前主线；不重放旧 F18 提交 |
| 新增功能 | 计划无变化完成或内联/明确草稿互斥的发布并完成，全部单事务；当前只完成首个严格请求 Schema 和最小端口补齐中的部分代码 |
| 修复 / 重构 / 优化 | 计划预读完整父级/影响并集后按任务→组→记录锁序重读；保留已有草稿归属/作者/处理人/影响快照，拒绝改绑其他任务 |
| 测试与验证 | 新契约测试先因未实现失败，补 Schema 时曾导入不存在的 module-tasks 文件，修正为现有 tasks 导出后 2/2 通过；本批组合真库/前端/E2E 尚未执行。F18 最终 [workspace run](https://github.com/256-code/InPulse/actions/runs/34443392933) 成功 12m35s，E2E 步骤 5m1s（未取得精确用例数）；[docs run](https://github.com/256-code/InPulse/actions/runs/34443393028) 成功 7s，由协调核实 |
| 文档同步 | 本开始记录；完成时同步三设计/工作书/权限/测试矩阵与 F19 交审说明 |
| 风险与后续 | 无核心依赖/迁移/数据库权限变更；不运行全仓静态/全量构建/无关审计；F20 待 F19 审核合并后开展，F21 不在范围 |
| 备注 | F18 PR 任务未点击合并或修改分支保护；本批不推送/建 PR，只交本地提交给协调 |


## 2026-09-10 15:20 +08:00 | A / Codex | feature/a-f09-csp-nonce | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `ci` / `docs` |
| 分支 | `feature/a-f09-csp-nonce`（基于 `origin/main` `e67971b`；已 rebase 到 `5020c0a`，即 F-18 PR #82 合并之后） |
| PR / commit | [F-09 PR #83](https://github.com/256-code/InPulse/pull/83)、commit `4f6e7b9`（rebase 前的提交号，rebased head 以 PR 为准） |
| 做了什么 | 交付 F-09 数据安全专项第一个纵切片：逐响应 nonce CSP 与安全响应头，覆盖本地开发、`vite preview`、生产镜像与 CI 门禁 |
| 新增功能 | 入口 HTML 的 script/style/modulepreload 标签与 `csp-nonce` bootstrap meta 在构建期写入 `__INPULSE_CSP_NONCE__` 占位符，生产 Nginx 用每请求 16 随机字节的 `$request_id` 经 `sub_filter` 逐响应替换；CSP 头（script-src/style-src nonce、script-src-attr/style-src-attr `none`、frame-ancestors `none`、base-uri `none`、object-src `none`、form-action `self`）与 HTML 同值下发；HSTS/nosniff/X-Frame-Options/Referrer-Policy/Permissions-Policy 收敛到独立 snippet 并由每个声明 `add_header` 的 location 显式 include；入口与 SPA 回退 `no-store` 且关闭条件请求与 ETag，避免 304 复用旧 nonce |
| 修复 / 重构 / 优化 | `AppProviders` 的 CSP nonce 读取改为 DOM IDL 属性（`meta.nonce`），修复生产构建下 `getAttribute("nonce")` 返回空串导致 Ant Design cssinjs 样式注入失效；Playwright Web 服务从 `vite dev` 改为 `vite build && vite preview`，使 E2E 覆盖真实构建产物；`INPULSE_WEB_CSP=off` 仅为本地对照开关且非法值 fail closed 到 enforce；未新增依赖、未放宽 CSP、未改动数据库 |
| 测试与验证 | 实际通过：`apps/web/tools/vite-csp.test.ts` 与 `AppProviders.test.tsx`（web 单测 37 文件 136/136）、`apps/e2e/tests/csp.spec.ts` 2/2（逐响应 nonce 不同、CSP 头与 meta/script 一致、无占位符残留、登录/主题/命令面板/通知弹层/懒加载/错误页 `securitypolicyviolation` 零违规）、`scripts/check-web-image-csp.sh inpulse/web:local` 在真实镜像与真实 Nginx 上 7 组断言全部通过、`check:deploy:test` 正例通过且 `.env.deploy.example` 占位符被拒、API 单测 61 文件 291/291、真实 PostgreSQL 集成 37 文件 232/232（含数据库 13 例，本地 55436 集群）、`pnpm check` 全绿（lint、format、typecheck、build、契约漂移、73/73 权限矩阵、依赖边界 440 文件、前端边界、Secret 674 文件、文档 60 文件、公共 registry 审计无已知漏洞）。未运行：全量 `pnpm test:e2e` 最近一次为 19 passed / 8 failed（admin-users、features、module-tasks、tasks、record-drafts×2、task-status×2，均为本机既有失败或波动；`E2E_WEB_CSP=off` 对照下 task-status/record-drafts 仍失败，与本改动无关）、GitHub Actions、Trivy 镜像扫描 |
| 文档同步 | `docs/test-matrix.md` 新增 F-09 章节并把 SEC-002 状态改为已自动化；`.github/workflows/ci.yml` 在「Build production web image」之后新增「Verify web image per-response nonce CSP」步骤；`scripts/check_deploy_refs.mjs` 增加安全头资产与 Nginx CSP 结构校验；本日志与 `AGENTS.md` 阶段进展 |
| 风险与后续 | F-09 剩余切片未交付：Markdown 白名单与 GitHub URL 规范化（SEC-004，react-markdown/rehype-sanitize 属新增依赖，需独立 PR 与人工确认）、Secret 文件 fail-closed 测试补强（SEC-007）、未匹配路由统一 JSON 404（SEC-006）；本地 `deps:audit` 需指向公共 registry；等待非作者审核与 CI 结果，CI 绿前不得声称通过 |
| 备注 | 首次推送后 main 前进到 `5020c0a`（F-18 PR #82）与本分支在 `开发日志.md` 冲突，已重新 rebase 并保留双方条目（本条目在最前）。CI 首次运行（run 34446253241）在新增的「Verify web image per-response nonce CSP」步骤失败：`openssl req` 生成的临时 TLS 私钥为 600，容器内以非 root（uid 101）运行的 Nginx 无法读取（`BIO_new_file ... Permission denied`），本地 Windows Docker Desktop 的挂载忽略 mode 因此未复现；已在脚本内 `chmod 644` 临时文件并修订提交，等待下一次 CI 复跑。脚本首次在 Windows Git Bash 跑通需要 `MSYS_NO_PATHCONV=1` 与原生输出路径，脚本已在内部处理，CI 的 Linux bash 不受影响 |

## 2026-09-10 13:59 +08:00 | B / 协调审核与PR交付 | codex/f18-record-publishing-pr | 推送 4

| 字段 | 内容 |
| --- | --- |
| 类型 | fix / docs |
| 分支 | codex/f18-record-publishing-pr；基线 main e67971b |
| PR / commit | [F-18 PR #82](https://github.com/256-code/InPulse/pull/82)；修复前 HEAD 2de97c07c1c56b3af75b12277930caa02abae865 |
| 做了什么 | 修正 PublishedRecordsModule 两处跨领域模块装配导入，改经 activity/index.js 与 notifications/index.js 既有公开入口 |
| 新增功能 | 无；导入的是相同模块类，provider、imports集合和运行逻辑不变 |
| 修复 / 重构 / 优化 | 未改边界规则或allowlist。最初最后观察到Browser E2E运行，完整job摘要后确认真正失败是其后的Dependency boundary check，不是E2E；本地复现同样两条module-boundary错误 |
| 测试与验证 | 旧HEAD [workspace34441970857](https://github.com/256-code/InPulse/actions/runs/34441970857/job/102758720268) 13m0s失败，Browser E2E步骤耗时4m41s后继续执行部署预检再到边界失败；在线步骤日志正文加载Error，未确认E2E精确用例数。旧HEAD [docs34441970866](https://github.com/256-code/InPulse/actions/runs/34441970866) 成功。本地pnpm check:deps修前两错、修后453源文件/模块节点通过；record-publication-effects单测2/2（1.69s）。不重复真库、全量构建或无关审计；新HEAD CI待运行 |
| 文档同步 | 本标准表格日志，保留此前网络、付款阻塞及上游合并事实 |
| 风险与后续 | GitHub当前另要求至少1位有写权限真实Approval，required Documentation仍Expected而实际成功项为Documentation / docs；已告知协调及维护者，不代审批、不改保护或workflow凑同名。等待增量审核与新HEAD完整CI |
| 备注 | 本次仅2条import路径及日志；无生产依赖、Schema、迁移或权限变化 |

## 2026-09-10 13:38 +08:00 | B / 协调审核与PR交付 | codex/f18-record-publishing-pr | 推送 3

| 字段 | 内容 |
| --- | --- |
| 类型 | merge / docs |
| 分支 | codex/f18-record-publishing-pr；同步 main e67971b（CI 提速 PR #80） |
| PR / commit | [F-18 PR #82](https://github.com/256-code/InPulse/pull/82)；上一交付 HEAD af8904774431365396fe1480b18f56a07605a888 |
| 做了什么 | 无冲突合入上游7文件的CI缓存、buildx和Dockerfile依赖清单分层优化及文档；保留上游原内容与双方日志 |
| 新增功能 | 无新增F18业务或契约范围，无新增本分支CI策略 |
| 修复 / 重构 / 优化 | 账户付款/支出限制曾使旧HEAD两项CI均未启动；用户要求再试后重跑原run attempt2，docs已成功、workspace已实际运行 |
| 测试与验证 | 旧HEAD [docs34438904910](https://github.com/256-code/InPulse/actions/runs/34438904910) attempt2成功4秒；[workspace34438904764](https://github.com/256-code/InPulse/actions/runs/34438904764) attempt2提交本条时仍运行；新合并HEAD的CI尚未启动。cached diff--check通过；协调已审核7文件交集，不涉及F18业务/契约，允许同步；未重复34真库或全仓审计/构建 |
| 文档同步 | 本标准推送表；主线AGENTS/CONTRIBUTING和历史日志原样保留 |
| 风险与后续 | 以新HEAD完整CI验收，不把旧HEAD运行中当作通过；PR曾显示1项expected检查，其名称和保护要求待核实，不推断为上游新增job，不忽略或修改保护设置 |
| 备注 | 上游concurrency自动取消被取代旧run属主线机制；网络失败持续低频重试，未关闭TLS验证；无实质业务改动 |

## 2026-09-10 12:50 +08:00 | B / 协调审核与PR交付 | codex/f18-record-publishing-pr | 推送 2

| 字段 | 内容 |
| --- | --- |
| 类型 | merge / fix / docs |
| 分支 | codex/f18-record-publishing-pr；同步 main fb13d6b（F-23 PR #81） |
| PR / commit | [F-18 PR #82](https://github.com/256-code/InPulse/pull/82)；原交付 HEAD 5353f9aa241c5b2f6477c3886868287d2b8544d3；本次为主线合并 |
| 做了什么 | 网络持续低频重试后创建 PR，发现主线 F-23 前进造成冲突；合并注册与权限/日志双方内容，生成器重生 5 个契约产物 |
| 新增功能 | 无新增 F-18 范围，保留主线 F-23 模块与聚合组编号接口 |
| 修复 / 重构 / 优化 | F-18 RecordPublicationAccess 两处 TaskDraftSource 类型引用随主线改为 TaskReadModel；双编号端口/适配器及 AppModule 自动合并，行为交叉已交协调任务增量复核 |
| 测试与验证 | 父协调独立复跑当前合并工作树真实 PostgreSQL record-publication/published-records/record-drafts 34/34（5.84s），核对 data_directory 后使用55424独占测试库并正常停止；契约 published-records/record-drafts/task-groups/permissions 4 文件 24/24（423ms）；API record-publication-effects/task-groups-http.service 2 文件 12/12（2.62s）；contract:generate 成功、contract:drift 5 产物一致；定向格式通过。首次生成被未解的指纹 JSON 冲突标记阻断，恢复原 F18 指纹历史后由生成器重生成功。交付任务未运行全量本地构建/静态测试/审计，未自行启动数据库；最新合并 HEAD CI 尚未运行 |
| 文档同步 | 本标准表格日志；F-18 与 F-23 权限章节和全部原始日志事实均保留 |
| 风险与后续 | 协调已确认类型引用、来源DTO、双编号和指纹历史合并无阻断，允许提交推送；等待最新 HEAD 全部适用 CI 通过；列表/历史暂无分页，F-19/F-20/F-21 未交付 |
| 备注 | 浏览器曾短暂恢复后提交失败，经 PR 列表确认未创建再重提，现 PR #82 已确认；Git TLS 多次失败后正常 fetch 成功，未关闭证书验证或修改网络配置 |
## 2026-09-10 12:08 +08:00 | B / 协调审核与PR交付 | codex/f18-record-publishing-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | feat / docs |
| 分支 | codex/f18-record-publishing-pr；审核基线ad6476b |
| PR / commit | 审核7a44e7bb938b01f326c7a176e3aa1fff8fcc2e84，代码1e73db68f92cce75f826150609daca6f65e48733；PR待创建 |
| 做了什么 | 准备F18发布与不可变版本交付，最终50文件；保留F17草稿及其最终结果补记 |
| 新增功能 | 独立发布、正式编号与版本历史比较、内容修订和冲突合并、稳定遗留项及版本快照 |
| 修复 / 重构 / 优化 | 交付未修改业务；保留已批准遗留清空/恢复/CONVERTED关联和当前资格通知去重规则 |
| 测试与验证 | 协调独立真库34/34（5.01秒）、Web9/9（3.86秒）、搜索容量2/2、契约43/43；实现报告Edge F17/F18五路径5/5（54.5秒），详见交审。交付本批非生成TS/TSX格式及diff空白通过；未重复真库/浏览器或本地全量构建/静态审计。远端fetch连续TLS握手失败，浏览器核对亦连接关闭，随后正常重试fetch成功，确认远端main仍ad6476b，无新交集；本批CI未运行 |
| 文档同步 | 三份设计、工作书、权限/测试矩阵、生成物、交审说明与本模板日志；F17最终结果补记已存在 |
| 风险与后续 | 列表/版本历史暂未分页；F19/F20和完整阶段0未完成。最新HEAD全部CI通过且审核有效后正常squash；实质行为/主线交集需增量审核 |
| 备注 | 无新增依赖/迁移/权限放宽；网络恢复前不声明推送成功，不改变TLS安全配置。合并后回报协调放行F19 |

## 2026-09-10 12:04 | B / Codex | codex/f18-record-publishing | 本地交审，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `test` / `docs` |
| 分支 | `codex/f18-record-publishing`，基线 `ad6476b7a25bfd504e523908354935ef1dd68949` |
| PR / commit | 本地功能提交 `1e73db68f92cce75f826150609daca6f65e48733`；本批尚无 PR/CI |
| 做了什么 | 完成 F-18 独立发布与不可变版本纵切片，交协调审查；保留 F-17 草稿路径 |
| 新增功能 | 正式编号/v1/发布时间、正式列表详情与历史原文对比、修订新版本、冲突合并；按人工确认保存稳定遗留项及版本全文，清空确认解决、再填恢复、已转换保留原关联 |
| 修复 / 重构 / 优化 | 同事务编号/版本/记录/审计/活动/搜索/通知；修复 nullable 遗留项遗漏重放白名单导致 500；共享内容字段/合并函数，新增公开发布端口供 F-19 使用；完整搜索容量超限 422 并保留输入 |
| 测试与验证 | API 三文件真实数据库 34/34，搜索容量 2/2；Web 三文件 9/9；契约四文件 43/43；Edge F-17/F-18 两文件五路径 5/5（54.5 秒）；受影响 API/契约编译及 API 测试/Web/E2E 类型通过，78 路由/权限、5 生成物无漂移、60 Markdown 链接与 git diff --check 通过。首次 HTTP null 分支 500 已修复；首次契约过渡指纹冲突由恢复已提交基线并重新生成解决，既有历史不变；首次浏览器期间客户端生成引发热更新错误，停止源改动后合跑无此错误。未运行全仓静态/构建/测试/审计、默认 Chromium 或本批 CI |
| 文档同步 | 三份设计、开发工作书、权限矩阵、测试矩阵、F-18 交审说明与本日志同步批准后的遗留/通知口径及实际验证 |
| 风险与后续 | 无生产依赖、迁移或数据库权限变更；列表/历史暂未分页，F-20 转换入口未交付。F-19 须在 F-18 审查/PR/CI/合并完成后开始，F-21 不在本轮范围 |
| 备注 | 本机 fetch 超时/TLS 失败，协调再次成功 fetch 确认远端 main 未变；本任务测试实例已核对目录后正常停止，数据保留。只交本地提交，未推送/建 PR/合并，等协调与 PR 任务接手 |



## 2026-09-10 11:20 +08:00 | B / Codex | codex/f18-record-publishing | F18 开始及 F17 最终结果补记

| 字段 | 内容 |
| --- | --- |
| 类型 | feat / docs |
| 分支 | codex/f18-record-publishing，从 origin/main ad6476b 新建，不重放F17本地提交 |
| PR / commit | F18未提交/推送/建PR；历史 [F17 PR #79](https://github.com/256-code/InPulse/pull/79)，merge ad6476b7a25bfd504e523908354935ef1dd68949，交付HEAD 33b043b6527ed9f229707e6d492fe286d5941c7b |
| 做了什么 | 启动F18发布/版本纵切片，提前核对F20稳定遗留项依赖；补记F17最终已核实结果 |
| 新增功能 | 进行中：正式记录读取与版本原文对比；发布/版本写入待遗留项映射定案，不声明已完成 |
| 修复 / 重构 / 优化 | 发现单文本到稳定遗留项的编辑/清空/转换映射未定义、草稿50000与快照10000长度冲突，已报协调；发布“功能相关人员”通知集合亦未定义，不擅自映射/截断/广播 |
| 测试与验证 | 当前只读真库3/3、Web2/2及必要局部编译/类型检查通过。F17历史证据由协调核实：[workspace34430811569](https://github.com/256-code/InPulse/actions/runs/34430811569)成功16分钟、Chromium25/25含独立/FEATURE/MODULE草稿；[docs34430811576](https://github.com/256-code/InPulse/actions/runs/34430811576)成功6秒。本条未重新运行这些历史CI。F18发布/版本写/E2E/CI未运行，无全量本地构建/测试/审计 |
| 文档同步 | 本日志及 [F18实施记录](docs/f18-local-handoff.md)，批准前不把拟议遗留项规则写成正式基线 |
| 风险与后续 | 等待人工映射/长度/通知收件人定案；继续不受影响读取/版本对比。F18审核、CI、合并完成后才开始F19，再依次F20，不自主开始F21 |
| 备注 | 开工工作区干净；fetch先443超时、重试TLS握手失败，本地ref与协调核实的main一致，交付前需再fetch同步；不修改A/C或既有历史事实 |


## 2026-09-10 12:16 +08:00 | C / Codex | codex/f23-task-groups-merge | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f23-task-groups-merge`（基于 `origin/main` `ad6476b`，F-17 PR #79 合并之后） |
| PR / commit | 代码 commit `a905fcf8061dffcdad21e309d3ce0b6fe8090b34`、文档随本批分支提交；已推送 `codex/f23-task-groups-merge`，未创建 PR |
| 做了什么 | 交付 F-23 任务合并服务端纵切片：`POST /api/v1/task-groups/merge` 契约、`TaskGroupsModule`、锁序与同事务副作用、幂等与 409 映射，并补齐真实 PostgreSQL 集成测试与文档 |
| 新增功能 | 来源任务并入主任务聚合组：服务端推导项目与聚合组编号、复用或新建组、保存原工作状态与原负责人快照、通知四类接收人并深链主任务；来源已属活跃组 409、跨项目与非成员 404、归档父级 409 |
| 修复 / 重构 / 优化 | 修 `TaskGroupRepository.findGroupItem` 把 `timestamptz` 当 Date 使用导致的 500（按仓库约定改 `new Date(...)`）；`TaskQueryPort` 新增 `findByTaskId` 并把 `TaskDraftSource` 统一为 `TaskReadModel`；`ProjectCodePort` 新增聚合组编号分配；无新迁移、无依赖变更、无权限放宽 |
| 测试与验证 | 实际通过：API 集成 `task-groups-merge` 6/6（约 3 秒）、集成全量 37 文件 232/232（56.9 秒）、API 单测 61 文件 291/291（含新增 HTTP 边界单测 10 例）、契约单测 10 文件 75/75、前端单测 35 文件 120 例（完整串行通过；随后两次并行复跑出现既有 debounce 与 MFA 计时用例偶发失败，单文件复跑通过）、`contract:drift` 5 产物无漂移、`contract:validate` 73 条、`permissions:check` 73/73、`lint`、`format:check`、`typecheck`、`build`、`db:migrations:check` 6 个迁移、`check:deps` 431 文件、`check:frontend:boundaries`、`check:secrets` 666 文件、`check:deploy:test`；公共 registry 审计无已知漏洞；rebase 到 `c7c6881` 后复验：`lint`、`typecheck`、`build`、API 单测 63 文件 303/303、契约 12 文件 84/84、`task-group` 集成 2 文件 14/14、`contract:validate` 81 条、`contract:drift` 5 产物、`permissions:check` 81/81、`check:docs` 63 个 Markdown 文件均通过。未运行 `pnpm test:e2e`（无前端改动）与生产镜像构建与扫描；rebase 后强制推送使 PR #87 的 GitHub Actions 已通过（workspace 12m4s，docs 通过） |
| 文档同步 | 权限矩阵与测试矩阵 F-23 章节、[F-23 交审说明](docs/f23-local-handoff.md)、本日志与 `AGENTS.md` 阶段进展；Schema、Route Registry、OpenAPI 与前端生成客户端由生成工具同步更新 |
| 风险与后续 | F-24 解除合并、F-25 前端入口与聚合组读取接口未交付；实际 HTTP 路径上非 JSON 内容类型由 `@ContractBody` 先返回 422（处理器内的 400 分支以直接调用覆盖），本轮未改共享契约管线；等待非作者审核与后续 PR |
| 备注 | 仅改动自有交付短分支；本地验证完成于 11:55，代码与文档分两次提交后于 12:16 推送，未创建 PR；数据库无迁移改动 |

## 2026-09-10 11:01 | 岗位待定 / Codex | codex/ci-speedup | 已推送（PR #80）

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | 交付分支 `codex/ci-speedup`，基于已同步到 `origin/main` `e362d12` 的本地 `main` 工作区 |
| PR / commit | [PR #80](https://github.com/256-code/InPulse/pull/80)，功能提交 `ci(ci): speed up PR pipeline`；`d238fc5` 是 rebase 前的提交号，rebase 到最新 `main` 后提交号以 PR head 为准 |
| 做了什么 | 缩短 PR 推送后的 CI 等待与排队：`.github/workflows/ci.yml` 增加 `concurrency`（同一 ref 的新推送取消被取代的在运行流水线，`main` 不取消）；曾试 `apps/api` 单元测试并行，CI 实测负收益后已回退；为 Playwright Chromium 增加 CI 缓存步骤；生产镜像重排 Dockerfile 层顺序并启用 buildx 的 GHA 层缓存。 |
| 新增功能 | 无用户可感知功能，仅调整 CI 与测试执行方式。 |
| 修复 / 重构 / 优化 | 1）同一 ref 内取消旧运行，避免连续推送排队跑完多条完整门禁；2）曾试把 `apps/api` 的 `test:unit` 改为并行（新增 `apps/api/vitest.unit.config.ts`），但 CI 实测为负收益（`apps/api test:unit` Duration 35.99s -> 40.14s；本机 20 核才是 26.56s -> 4.73s），已在同一 PR 内回退该配置文件与 `package.json` 改动，集成测试继续串行、`pnpm test` 语义不变。曾一并尝试 setup-node 的 pnpm store 缓存，按下面的实测数据撤销；3）为 Playwright Chromium 增加 `actions/cache` 缓存（键绑定 `pnpm-lock.yaml`，action 固定到 `v5.1.0` 的提交），Trivy 漏洞库已由 `trivy-action` 自带缓存，未重复配置；4）`deploy/docker/{api,migration,web}.Dockerfile` 改为先复制依赖清单再 `pnpm install`、最后复制源码，使依赖层不随源码失效；5）CI 的 5 处镜像构建改用 container driver 的 buildx 加 `type=gha` 层缓存（各镜像独立 scope 与 `ignore-error=true`），并新增 `Set up Docker Buildx with GHA layer cache` 步骤。 |
| 测试与验证 | 基线为同步后的 `origin/main` `e362d12`；先前基于 `b6ffc7f` 的分析结论已作废，其中 multer 审计失败已由 `pnpm-workspace.yaml` 的 `multer: "2.3.0"` override 解决。CI 实测数据由已登录的 `gh` 读取（`gh run view --json jobs`）：单次运行约 16.2 分钟（PR 34429662190）与 16.4 分钟（push dev/a 34429802561）；主要耗时依次为 Browser E2E 327s/266s、Unit tests 138s/102s、Integration tests 71s/54s、4 次 Trivy 扫描 133s/192s、4 个生产镜像构建 136s/约 110s、PGroonga 探针镜像 26s/32s、Typecheck 32s/23s，而 `pnpm install --frozen-lockfile` 仅 13s，Install Playwright browser 在两条运行间波动为 25s/116s。据此撤销 pnpm store 缓存（预计仅省约 10s，且恢复 327MB store 可能更慢）。本机实测（Windows，20 逻辑核）：`pnpm --filter @inpulse/api test:unit` 改动前 60 文件 281 例、Duration 26.56s（进程总耗时 27.9s），改动后同样 60 文件 281 例全部通过、Duration 4.73s（进程总耗时 6.1s）。`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`（database 5 例、api-contract 8 文件 67 例、web 34 文件 115 例、api 60 文件 281 例）、`pnpm deps:audit`（No known vulnerabilities found）、`pnpm check:docs`（58 个 Markdown 文件）全部通过。未运行：`pnpm test:integration`、`pnpm test:e2e`、`pnpm check:deploy:test` 与 GitHub Actions 上的本次改动；单测并行在 4 vCPU runner 上的实际收益待 CI 验证。本地另用 Docker Desktop 29.7.2 验证镜像层顺序：改前 API 镜像冷构建 79.5s，改后 93.2s（含首次拉基础镜像的差异），随后修改 `apps/api/src/main.ts` 再次构建时 `RUN pnpm install --frozen-lockfile` 层显示 `CACHED`（总 116.0s，其余层因源码变化重跑），验证后已用 `git checkout` 恢复该文件；buildx 的 `type=gha` 缓存无法本地验证（本机访问 Docker Hub 失败，拉不到 `moby/buildkit`）。PR #80 的 CI 已实际执行（run 34433573390，`CI / workspace` 全绿）：运行墙钟 16.68 min，同代码基线 `ad6476b` 在 `main` 的推送运行 34431935852 为 17.58 min；逐步对比（基线 -> 本次）：Browser E2E 352s -> 360s、Unit tests 123s -> 138s、Integration tests 79s -> 71s、4 个生产镜像构建合计 155s -> 132s（API 67->74、migration 41->24、web 20->8、db-bootstrap 27->26）、4 次 Trivy 扫描合计 181s -> 117s、Install Playwright browser 23s -> 24s、Typecheck 27s -> 31s。Playwright 缓存本次首次为空（日志 `Cache not found for input keys: Linux-playwright-...`），post 步骤写缓存 5s；buildx 日志显示同一次运行内层复用（migration/web 出现多个 `CACHED`），GHA 层缓存为首次写入；Trivy 自带缓存命中（`Cache restored from key: cache-trivy-2026-09-10`）。第二条运行（commit `502461c`，run 34435162099）同样 success、墙钟 16.60 min，warm cache 生效：Install Playwright browser 24s -> 14s。日志进一步显示单测步骤的真正大头是 `apps/web test:unit`（85~96s），而 `apps/api test:unit` 基线串行 35.99s、并行 40.14s，即并行的单测文件在 4 vCPU runner 上受模块导入与 worker 启动开销支配，属负收益，已据此回退。第三条运行（commit `850aa8e`，run 34436861016，回退后 + 热缓存）success、墙钟 16.27 min：Unit tests 136s、Browser E2E 368s、4 个生产镜像构建合计 128s、4 次 Trivy 扫描合计 112s、Install Playwright browser 13s。 |
| 文档同步 | 更新 `.github/workflows/ci.yml` 头部说明、`AGENTS.md` 第 8 节、`CONTRIBUTING.md` 验证章节与本开发日志；并同步记录 concurrency、Playwright Chromium 缓存、生产 Dockerfile 依赖层重排与 buildx GHA 层缓存，以及单测并行在 CI 负收益后回退的实测结论；三份设计文档未改，因为技术设计 §12.4 的门禁顺序与清单未变化。 |
| 风险与后续 | `concurrency` 只影响排队与 Actions 额度，不缩短单次时长。CI 实测（见“测试与验证”）：单测并行为负收益（`apps/api test:unit` 35.99s -> 40.14s），已回退；真正的单测大头是 `apps/web`（85~96s，vitest 自身提示用 `pool: 'vmThreads'` 或 `isolate: false` 可省约 21~23s），属独立改动，未在本 PR 内实施。总墙钟由基线 17.58 min 降到 16.60~16.68 min，收益主要来自 Playwright 缓存（23s -> 14s）、镜像依赖层复用与 Trivy 扫描波动；buildx 的 `type=gha` 层缓存在热运行中已观察到镜像构建合计 128~132s、Trivy 扫描 112~117s（基线 155s/181s），但其中含运行间波动，不宜单独归因于缓存。实测最大的单项是 Browser E2E（约 4.4～5.5 分钟）：`apps/e2e` 现为 `workers: 1`、`fullyParallel: false`，但多个 spec 共享 `runtime.member` 登录、`notifications.spec` 还会对共享账号执行“全部已读”，并行前必须先做 fixture 隔离改造并多轮验证，故本次未动。镜像构建（1 个探针镜像 + 4 个生产镜像）已把 `deploy/docker/*.Dockerfile` 的 `COPY . .` 移到 `RUN pnpm install --frozen-lockfile` 之后，并改用 buildx 的 `type=gha` 层缓存；本地已验证改动源码后 `pnpm install` 层命中 `CACHED`，但 buildx 的 `type=gha` 缓存无法在本机复现（拉不到 `moby/buildkit`），Playwright 缓存的收益已在 CI 实测确认（23s -> 14s），且生产 Dockerfile 属重点审查项，需人工评审。E2E 与 Trivy 属验收/安全门禁，不得为了提速弱化。 |
| 备注 | 已推送 `codex/ci-speedup` 并创建 PR #80；因 `main` 前进到 `ad6476b`，分支已 rebase 到最新 `main` 并强推（同 ref 强推仅作用于本交付分支），冲突只在 `开发日志.md`，已同时保留双方条目。PR #80 的 `CI / workspace` 已实际执行并 success（run 34433573390），第二条运行（run 34435162099）亦 success 并确认 warm cache 收益（Playwright 安装 24s -> 14s）；随后按 CI 实测回退了单测并行改动；第三条运行（run 34436861016）在回退后同样 success、墙钟 16.27 min。本条之后的推送若仅为文档回填，不再逐次改写本记录。 |

## 2026-09-10 12:10 +08:00 | C / F-05 成员管理 E2E | codex/c-member-e2e | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/c-member-e2e`；基于 `origin/main` `ad6476b` 新建 worktree，已 rebase 到 `origin/main` `5020c0a`（含 F-18 记录发布）并已推送到 `origin/codex/c-member-e2e` |
| PR / commit | 已推送 `codex/c-member-e2e`（推送 1），未创建 PR；测试 commit `3f187049e1d69faaccc2e5eb42b7613673c155b2`，文档 commit 为本条目所在提交；rebase 后提交号以 `origin/codex/c-member-e2e` head 为准 |
| 做了什么 | 为 F-05 项目成员管理补齐 Playwright 关键路径用例，关闭测试矩阵中 `F05-MEMBER-E2E-001`「未覆盖」状态 |
| 新增功能 | 无生产行为变更；新增两条 E2E：① 普通成员访问 `/projects/:projectId/members` 被 `RequireAdmin` 拦截，断言 `admin-forbidden`、「无权访问」与「此区域仅限系统管理员访问。」；② 管理员登录后首次进入成员页必然触发管理员安全验证（`upgradeToAuthenticated` 不写 `reauthenticated_at`/`mfa_verified_at`，唯一刷新点是重认证服务），完成密码 + TOTP 后断言成员历史，经页面添加成员出现成功提示与「活跃成员」徽标，移除成员时校验确认对话框与「该成员没有未完成任务。」，确认后仍保留历史卡并标记「已移除」「历史记录已保留」，最后以不存在的 `projectId` 校验前端映射的读取失败空态 |
| 修复 / 重构 / 优化 | 无生产代码改动。用例显式处理三处已知约束：① antd 在两个中文字符间插入空格，移除按钮可访问名为 `移 除`，按 `移除` 精确匹配会等到用例超时，改用 `/移\s*除/`；② `listMembers` 返回全部历史行且不按用户去重（`ORDER BY joined_at ASC, id ASC`），CI retry 会产生重复卡，成员卡断言统一使用 `.last()`；③「添加成员」按钮在页头与弹窗 footer 同名，断言作用域限定在 `addDialog` 内 |
| 测试与验证 | `pnpm --filter @inpulse/e2e typecheck`、`pnpm format`、`pnpm lint` 通过；新用例单跑 2/2 通过（14.2s），`playwright test --list` 为 29 tests / 16 files；分支 rebase 到 `origin/main` `5020c0a` 后执行 `pnpm build` + 全量 `pnpm test:e2e`，29/29 通过（4.7m，PostgreSQL 18.6 探针容器 127.0.0.1:55432 + PGroonga）。本轮首次复跑沿用陈旧 `apps/api/dist`（E2E 以 `node dist/main.js` 启动 API），两条既有 F-18 用例因旧产物缺少发布端点而失败，重新 `pnpm build` 后全绿；此前两次复跑各出现一次既有用例服务端 500（F-17 `record-drafts` 与未含本用例的基线工作区 F-14 `tasks`），均为新用例之外的既有缺陷。未运行 `pnpm check` 全量门禁、`test:integration`、`test:unit` 与 GitHub Actions；分支已推送 `codex/c-member-e2e`（推送 1），GitHub Actions 未运行 |
| 文档同步 | `docs/test-matrix.md`（`F05-MEMBER-E2E-001` 由未覆盖改为本地通过、`F05-READ-E2E-001` 与 `CI-017` 更新为 29/29 并补 F-18 覆盖）、`apps/e2e/README.md`（19 → 29 例、16 个 spec 文件并补充成员管理覆盖与本地跑测前置构建要求）、`README.md`（阶段 1 更新段落）、`AGENTS.md`（§8 阶段 0 CI 链路中的 Playwright 用例数）与本日志 |
| 风险与后续 | 用例未覆盖未完成任务改派路径（需先在新建项目建任务并指派成员，成本较高，建议独立交付）；`runtime.member` 在 `runtime.projectId` 上的成员关系由本用例独占变更，已审计既有 spec 均未在该项目增删成员；全量复跑暴露 Features/Records 写路径间歇性 500（F-17 `record-drafts` 与 F-14 `tasks` 各出现一次，未含本用例的基线同样可复现）且服务端 HTTP 层未留下可定位日志，建议单独立项排查（属任务/记录写路径，非本次用例范围）；`pnpm test:e2e` 依赖 `apps/api/dist`，本地复跑前必须先 `pnpm build`，否则会出现与新代码无关的假失败（本轮实测）；GitHub 网络与 Actions 待恢复后需在新基线复跑 |
| 备注 | 夹具由 `global-setup.ts` 每次运行新插入 user + project，无 TRUNCATE/reset，用例可反复执行；本次未新增依赖、迁移或权限变更；分支已 rebase 到最新 `origin/main`（`5020c0a`），文档冲突处同时保留双方条目 |；推送前已按实际复跑结果更新本条目

## 2026-09-10 10:46 +08:00 | B / 协调审核与PR交付 | codex/f17-record-drafts-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | feat / docs |
| 分支 | codex/f17-record-drafts-pr；最新main e362d12，与审核基线一致 |
| PR / commit | 已审核a94c165e68a84c77faa90f1f55e5325115c6dc57，代码b8bec4e0adca1f36949c2a5ee433bbda54bb4d1d；本次推送后创建PR |
| 做了什么 | 交付F17独立/来源草稿与用户授权的B侧日志格式整理；最终范围44文件 |
| 新增功能 | 多草稿明确新建/选择/继续编辑；来源处理人与影响快照保持，保存草稿不完成任务、不生成正式编号版本 |
| 修复 / 重构 / 优化 | 无交付行为修复；核对9条历史日志表格和F14/F15/F16最终CI/合并补记，保留失败、未运行事实与A/C记录 |
| 测试与验证 | 协调独立真库55/55（6.46秒）、Web14/14（3.73秒）、契约权限17/17（277ms）；实现报告Edge草稿3/3及F16两状态闭环、72路由策略/生成物无漂移，详见交审说明。交付本批非生成TS/TSX格式及diff空白通过。首次fetch因GitHub443连接超时失败，重试成功。未重复数据库/浏览器测试、全量本地构建/静态审计；最新HEAD完整CI待PR运行 |
| 文档同步 | 三份设计、工作书、权限/测试矩阵、契约及生成物、交审说明和本日志已同步 |
| 风险与后续 | F17仅草稿；F18/F19发布能力及完整阶段0未完成。最新HEAD全部适用CI通过且审核有效后正常squash；行为修复/上游实质交集需增量审核 |
| 备注 | 用户已明确同意保留同任务多记录现有不变量；无迁移/新唯一约束/依赖升级/权限放宽。最终F17合并事实待实际完成后另行补记 |

## 2026-09-10 10:39 +08:00 | B / Codex | codex/f17-record-drafts | 本地交审（未推送）

| 字段 | 内容 |
| --- | --- |
| 类型 | feat / test / docs |
| 分支 | codex/f17-record-drafts；从 F16 合并基线创建，交审前同步 origin/main e362d12（F06及最新视觉） |
| PR / commit | 本地代码 b8bec4e0adca1f36949c2a5ee433bbda54bb4d1d；未推送、未创建 PR |
| 做了什么 | 完成 F17 独立与任务来源草稿纵切片，统一 B 侧 F14 之后日志格式 |
| 新增功能 | FEATURE/MODULE 独立草稿、来源任务多草稿列表/明确新建/选择继续编辑；三项核心内容必填、遗留问题选填；F16实际变化入口接真实草稿，保存不完成任务 |
| 修复 / 重构 / 优化 | Workflow→TaskQueryPort/RecordDraftCommandPort，显式同tx、父级→任务→记录锁及有限重试；处理人/作者/影响快照服务端派生且内容编辑不追随任务变化；修复 FEATURE 返回链接多 /tasks 的404；按用户授权将9条B侧历史条目转为模板，保留每条原始事实/失败/待验证状态 |
| 测试与验证 | 真库 F17 14+任务回归41共55/55（6.17秒），Web草稿5+任务9共14/14，契约权限17/17，72路由/5生成物无漂移；Edge草稿3/3（37秒），此前同批F16两条也通过；必要局部编译/类型检查、本批格式/文档链接检查通过；日志专项核对163个非B章节未改、44条原B事实原文保留、11张模板表结构正确。初次缺少测试库配置fail closed、fixture优先级MEDIUM约束失败及浏览器首轮4/5均已修正，未降低断言。未执行本批CI/默认Chromium、全量构建/测试/静态审计 |
| 文档同步 | 三份设计、工作书、权限/测试矩阵、Schema/Registry/Controller/OpenAPI/客户端及 [F17交审](docs/f17-local-handoff.md)；日志最终CI/合并证据另作下条补记 |
| 风险与后续 | 公开Port、关联/快照、锁序、版本与幂等重放待非作者审核；F18发布/F19发布并完成尚未开放。无依赖/迁移/数据库权限变更 |
| 备注 | 2026-09-10人工同意保留同任务多记录的现有不变量，修正工作书旧task_id唯一；不新增每任务最多一DRAFT。历史条目格式整理经用户明确授权，不改A/C或F13之前记录，不编造原缺失时间/推送次数 |

## 2026-09-10 10:39 +08:00 | B / 协调历史证据补记 | codex/f14-feature-tasks-pr、codex/f15-module-tasks-pr、codex/f16-task-status-pr | F14–F16 最终交付结果补记

| 字段 | 内容 |
| --- | --- |
| 类型 | docs |
| 分支 | codex/f14-feature-tasks-pr、codex/f15-module-tasks-pr、codex/f16-task-status-pr（历史交付分支） |
| PR / commit | [F14 PR #71](https://github.com/256-code/InPulse/pull/71) squash 273b27319e7e5c251f32bd8d2b0022d40cfea823；[F15 PR #73](https://github.com/256-code/InPulse/pull/73) squash b569c3d77e9a789c3399f52daaf677cb35247a44；[F16 PR #76](https://github.com/256-code/InPulse/pull/76) squash 694c0ebd5260ea5f7cb2c9393cda25347f775a64 |
| 做了什么 | 补齐下方F14/F15/F16本地交审、各次推送与增量修复条目之后的最终CI/合并事实；原条目当时的未运行/失败/待CI结论保留 |
| 新增功能 | 无新增生产功能；范围分别见 [F14交审](docs/f14-local-handoff.md)、[F15交审](docs/f15-local-handoff.md)、[F16交审](docs/f16-local-handoff.md) |
| 修复 / 重构 / 优化 | F14先经历格式与MFA测试隔离修复；F16删除不完整完成率后才交付。具体失败与修复证据仍保留在原阶段条目 |
| 测试与验证 | 协调已核实的历史结果：F14最终HEAD 6ff039243d510bfbc092dacf5a38542178838ea4，[workspace 34342585945](https://github.com/256-code/InPulse/actions/runs/34342585945)成功（Chromium17/17），[docs 34342585903](https://github.com/256-code/InPulse/actions/runs/34342585903)成功；F15最终HEAD 2f53cfca1622dcb211bcabd2134f52a9415b0def，[workspace 34347523235](https://github.com/256-code/InPulse/actions/runs/34347523235)成功（14分46秒、Chromium20/20），[docs 34347523424](https://github.com/256-code/InPulse/actions/runs/34347523424)成功；F16最终HEAD 639d1b1937ff8600cec0e173a40a0769b2572999，[workspace 34426072504](https://github.com/256-code/InPulse/actions/runs/34426072504)成功（15分钟、Chromium22例含F16两例），[docs 34426072610](https://github.com/256-code/InPulse/actions/runs/34426072610)成功（8秒）。本条没有重新执行这些历史CI或测试 |
| 文档同步 | 仅开发日志最终结果补记；F14-F16原交审说明与历史条目继续作为各阶段证据 |
| 风险与后续 | 历史成功不代表F17或当前完整阶段0完成；F17另行交审及CI |
| 备注 | 按用户“没有按照之前的那种格式”澄清整理；历史事实不改写，A/C及F13之前条目不修改 |

## 2026-09-10（未记录具体时间） | B / 实现与协调交付 | codex/f16-task-status-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f16-task-status-pr` |
| PR / commit | 负责人：B 实现、协调审核、专门交付任务；分支 `codex/f16-task-status-pr`，审核 SHA `816e8f657eab25ad537c08fa13ed09c196deddf2`，最新 main `bce3337`，差异25文件且无上游冲突。 |
| 做了什么 | F-16 任务状态 PR 交付（推送 1） |
| 新增功能 | 功能：FEATURE/MODULE 完成、重开、取消、恢复和不可变状态历史；WITHOUT_RECORD 完成原因白名单、状态筛选、409 保留输入和明确确认。实际功能变化不提交完成命令，F17/F19接入仍待后续；不展示或计算不完整完成率。 |
| 修复 / 重构 / 优化 | 原条目未单列，见本表其他字段。 |
| 测试与验证 | 实际验证：协调独立真库41/41（4.20秒）及增量任务面板9/9（3.41秒）；实现者报告Web25/25、契约权限18/18、Edge状态闭环2/2和契约无漂移，详见 [交审说明](docs/f16-local-handoff.md)。交付仅运行本批非生成TS/TSX格式检查及git diff空白检查，均通过；未重复真库、默认Chromium或全量本地构建/静态套件/审计。本批完整CI待PR最新HEAD运行。 |
| 文档同步 | 审核/文档：协调已复核状态机、锁后数据库时间和历史快照、MODULE真实归属、重放实时权限及四副作用原子回滚，无剩余阻断。三份设计、权限/测试矩阵、契约及生成物已同步，无迁移/依赖/安全放宽。 |
| 风险与后续 | 后续：最新HEAD适用CI全部通过且审核有效后按授权squash合并；行为修复及主线实质交集需增量审核，F17待本批合并后协调放行。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

## 2026-09-10 10:26 +08:00 | A / Codex | feature/a-f06-project-edit-archive-restore | Push 4（同步 main #76、#78 后重推）

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `feature/a-f06-project-edit-archive-restore`（基于 `dev/a` `bce3337`，已合并 `origin/main` `1fb662c`（F-16 #76、视觉迁移 #78）） |
| PR / commit | [PR #77](https://github.com/256-code/InPulse/pull/77) |
| 做了什么 | 完成 F-06 项目编辑与归档/恢复纵切片：F-06.1 编辑 `PATCH /api/v1/projects/{projectId}`、F-06.2 归档 `POST .../archive`、F-06.3 恢复 `POST .../restore`、归档影响预览 `GET .../archive-preview` 的契约、后端服务/控制器、真实 HTTP + PostgreSQL 测试；F-06.4 前端入口（编辑/归档/恢复弹窗、未完成任务提醒、管理员重认证） |
| 新增功能 | 编辑：活跃成员或系统管理员整笔替换名称与描述，编码不可改；`If-Match` 乐观锁（409）、CSRF、数据库级幂等；归档：仅系统管理员，5 分钟内管理员密码 + 当前 TOTP 双重认证，归档前预览未完成任务数（仅计 TODO + ACTIVE），原因必填 1-2000 字，归档后项目及下级只读、历史保留；恢复：仅系统管理员，同样重认证与原因，只恢复项目自身状态、不改动下级归档状态；前端：项目卡片按身份展示编辑/归档/恢复入口，409 保留表单与重载提示，403 `ADMIN_REAUTH_REQUIRED` 打开管理员安全验证 |
| 修复 / 重构 / 优化 | 扩展 `ProjectsWritePort`：`findProjectForChange`（FOR UPDATE + 成员数子查询）、`updateProjectDetails`、`updateProjectStatus`（ACTIVE/ARCHIVED 与 `archived_at` 成对维护）、`countUnfinishedTasks`（TODO + ACTIVE）；新增 `ProjectManagementService`、`ProjectManagementHttpService`、Controller 与 `ProjectManagementModule`；契约新增 `ProjectEditRequest`、`ProjectArchiveRequest`、`ProjectArchivePreviewResponse`、`ProjectMutationHeaders`、`ProjectVersionHeaders`、`ProjectReplayContext` 与 `project-routes.ts`；幂等重放策略复用 `ProjectDetailResponse` 全叶子字段与 `ProjectReplayContext` 资源引用，归档/恢复的 resolve 只做 404 门禁、ACTIVE 前置条件在服务层与重放授权内判定（归档态同 Key 重放不被误拒）；前端新增 `project-management-query.ts`、`ProjectManagementModals.tsx`；同步 #78 视觉迁移后，编辑/归档/恢复入口重放到新视觉项目卡片（`.project-card`），相关测试断言同步新结构 |
| 测试与验证 | 实际通过：`pnpm test:unit`（database 5 例、api-contract 8 文件 67 例、Web 34 文件 114 例、API 60 文件 281 例）；`pnpm test:integration`（database 13 例、API 35 文件 212 例，含 `project-management-api.integration.test.ts` 9/9 与 `project-management.service.test.ts` 7 例，临时 PostgreSQL 18.6 + PGroonga 4.0.8）；`pnpm check` 全绿（lint、format:check、typecheck、db:migrations:check、contract:drift、contract:validate（65 条路由）、build、check:deploy:test、check:deps（412 源文件）、check:frontend:boundaries、permissions:check（65/65）、deps:audit（公共 registry 无已知漏洞）、check:secrets（636 文件）、check:docs（58 个 Markdown 文件））；合并 #78 前本地 `pnpm test:e2e` 16/20：F-06 相关路径（项目创建、视觉迁移等）通过，4 个失败为既有用例（`admin-users`、`features`、`module-tasks`、`tasks` 的定位/竞态问题，与 F-05 轮记录一致且未触及本 diff，本轮未修复）；GitHub Actions：[PR #77](https://github.com/256-code/InPulse/pull/77) 首轮 run（`CI / workspace` 15m44s、`Documentation / docs` 7s）全绿；同步 #76/#78 后的最新 HEAD 待 CI 再次验证 |
| 文档同步 | `docs/permissions.md` 新增归档预览、归档、恢复行；`docs/test-matrix.md` 重写 F-06 小节（F06-CONTRACT-001、F06-EDIT-API-001~004、F06-ARCHIVE-API-001~003、F06-ARCHIVE-UI-001）；本开发日志；`packages/api-contract/test/permissions.test.ts` 绑定清单同步；OpenAPI 与前端客户端由生成工具更新 |
| 风险与后续 | E2E 未覆盖项目归档/恢复路径；本机 4 个既有 E2E 失败待 CI 复核；最新 HEAD 的 CI 待运行；等待 CI 与非作者评审后合并 |
| 备注 | 仅操作自有交付短分支；main 仅经 PR squash 合并，不直接推送 |

## 2026-09-10 09:47 +08:00 | C / Codex | codex/c-page-visual-migration | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` |
| 分支 | `codex/c-page-visual-migration`（基于 `dev/c` = `origin/main` `bce3337`） |
| PR / commit | [PR #78](https://github.com/256-code/InPulse/pull/78)；代码 commit `0fd9e32`、日志 commit `0c59b82` |
| 做了什么 | 将设计稿中纯前端可迁移的页面与公共壳视觉迁移到当前项目：成员与设置页、命令面板、通知铃铛/弹层、账户弹层角色行、项目路由三段式面包屑（含实时项目名）；不迁移设计稿模拟数据与需后端支撑的页签/统计/面板 |
| 新增功能 | 项目路由顶栏面包屑显示 `研发交付中心 › 项目与功能 › 项目名`（经 `useProjectDetail`/`getProject` 实时取名称）；成员与设置页改为设计稿的 shell/rail/panel 与成员行；命令面板分组排序与提示文案对齐设计稿；通知铃铛 hover/展开态与账户弹层角色行对齐设计稿 |
| 修复 / 重构 / 优化 | `project-query.ts` 新增 `useProjectDetail`（`projectId` 为 null 不请求、60s staleTime）；`AppLayout` 新增 `projectClient` 注入点（仅测试），生产走默认 client；同步 `admin-users.spec.ts` 与 `AdminUsersPageView.test.tsx` 选择器到迁移后结构 |
| 测试与验证 | 本地实际通过：`pnpm --filter @inpulse/web typecheck`、`pnpm --filter @inpulse/web test`（33 文件 106 例）、`pnpm exec prettier --check`、`pnpm exec eslint`、`pnpm check:frontend:boundaries`（129 模块无违规）、`pnpm check:docs`（57 个 Markdown）；Playwright 临时截图 spec 1/1（面包屑项目名，截图后已删除）。CI 实际通过：[PR #78](https://github.com/256-code/InPulse/pull/78) head `ac04f15` 的 GitHub Actions `CI / workspace` 18m3s（47/47 步骤，run 34427257646）与 `Documentation / docs` 7s（run 34427257653）。本地未运行：全量 `pnpm check`、API 集成、依赖审计 |
| 文档同步 | 本开发日志；未修改 API 契约、数据库 Schema、权限矩阵、三份设计或 ADR |
| 风险与后续 | design-07 页签/统计/最近迭代/遗留面板、design-02/03/04 整页、侧栏计数徽标、搜索总数、设置页每成员项目计数等因缺后端数据暂缓；GitHub Actions 已全绿，等待非作者人工评审后 squash 合并 |
| 备注 | Push 1 仅推送自有交付短分支，不强推、不推送 `main`/`dev/c`、不合并 PR |
## 2026-09-09 21:44 +08:00 | C / Codex | codex/c-core-workspace-ui | Push 2 (同步 main)

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-core-workspace-ui`（已合并 `origin/main` `b569c3d`） |
| PR / commit | [PR #74](https://github.com/256-code/InPulse/pull/74)；本次为 merge 冲突解决提交，继续沿用原 PR |
| 做了什么 | 将主线 F-15 模块级任务与 F-03 用户管理合入核心工作区 UI 分支，解决 `ModulesPageView.tsx`、`TasksPanel.tsx`、`SettingsPage.tsx` 与开发日志冲突 |
| 新增功能 | 成员与设置页改为真实 `AdminUsersPageView`；模块卡片保留最新视觉并新增“模块任务”入口；任务面板保留新视觉并完整接入模块级任务的创建、编辑、影响功能、详情引用与通知直达 |
| 修复 / 重构 / 优化 | 以 main 的模块任务/冲突重载/幂等逻辑为功能基线，以 HEAD 的 Calm 视觉为 UI 基线合并；同步 `module-tasks.spec.ts` 至迁移后的 `查看功能` 与 `.calm-feature-card` 选择器 |
| 测试与验证 | 实际通过：`pnpm typecheck`、`pnpm format:check`、`pnpm lint`、`pnpm test:web`（31 文件 95 例）、`pnpm build`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm check:frontend:boundaries`、`pnpm check:docs`、`pnpm --filter @inpulse/e2e typecheck`、`git diff --check`；本地 PostgreSQL 18 + PGroonga 下 Playwright 相关关键路径 6/6（`module-tasks` 1/1、`tasks` 1/1、`modules`/`features`/`visual-migration` 4/4）。未重跑全量 E2E、API 集成与依赖审计 |
| 文档同步 | 本开发日志；未修改 API 契约、数据库 Schema、权限矩阵、三份设计或 ADR（上游 F-15/F-03 变更除外） |
| 风险与后续 | merge 已解除，等待 GitHub Actions 与非作者人工评审；完整 CI 以推送后的新 run 为准 |
| 备注 | 仅更新自有交付短分支并正常推送，不强推、不推送 `main`、不合并 PR |
## 2026-09-09 20:31 +08:00 | C / Codex | codex/c-core-workspace-ui | 已推送并创建 PR #74

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-core-workspace-ui`（基于 `dev/c` `273b273`） |
| PR / commit | 本地 commit `2095a25`；已创建 [PR #74](https://github.com/256-code/InPulse/pull/74)，已推送并由 CI 和人工评审 |
| 做了什么 | 参考 `inpulse_help 中 latest-version 设计稿` 的 UI 与交互，把核心工作区视觉迁移到当前项目；不迁移设计稿的模拟数据、Next.js、Tailwind、Base UI、shadcn 或 lucide-react |
| 新增功能 | 模块卡片网格、模块资料折叠、状态徽标和模块编辑/归档/恢复操作；功能卡片与详情双栏工作区、功能档案、功能任务卡片/列表；任务详情 Drawer（使用 Ant Design size 属性）与编辑弹窗新视觉；公共 `Calm` 组件、统一图标与工作区空态 |
| 修复 / 重构 / 优化 | 抽取 `CalmBadge`、`CalmEmptyState`、`CalmSectionTitle`、`CalmSegmented`，扩展 `InpulseIcon`；功能详情使用 `listFeatures` 加载同模块功能导航；保留真实项目行为：CSRF、幂等 Key、If-Match、409 三方合并、管理员安全验证和生成客户端调用；同步更新模块/功能/任务 E2E 选择器 |
| 测试与验证 | 实际通过：本地 PostgreSQL 18 + PGroonga 下 `pnpm test:e2e` 17/17（1.9m）；`pnpm test:unit`：Web 29 文件 85 例、API 54 文件 249 例、contract 7 文件 65 例、database 1 文件 5 例；`pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`git diff --check` 通过。此前 `pnpm build`、`pnpm contract:drift`、`pnpm check:docs`、`pnpm check:frontend:boundaries` 已通过，最终改动为 E2E 选择器和任务详情 Drawer 的 Ant Design 属性 |
| 文档同步 | 更新本日志；未修改三份设计、ADR、权限矩阵、测试矩阵、OpenAPI、生成客户端或数据库 Schema |
| 风险与后续 | 任务中心、迭代记录、遗留问题、成员与设置仍保持诚实空态，因对应查询接口尚未接入，未用设计稿模拟数据冒充真实能力；后续需在接口就绪后逐页接入真实数据。控制台仍存在既有 Ant Design 弃用提示（Space、List 等），非本次阻断项 |
| 备注 | 本地工作区已完整验证；已推送，等待 CI 和人工评审 |

## 2026-09-09（未记录具体时间） | B / 实现与协调交付 | codex/f15-module-tasks-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f15-module-tasks-pr` |
| PR / commit | 负责人：B 实现、协调审核、专门 PR 对话交付；分支 `codex/f15-module-tasks-pr`，已审核代码 `a5ec92b`、交审文档 `6d98934`。fetch 后 main 仍为 `273b273`（F-14 PR #71），最终差异仅本批29文件，无上游冲突。 |
| 做了什么 | F-15 模块级任务 PR 交付（推送 1） |
| 新增功能 | 功能：模块级任务创建/编辑/详情及零至多影响功能，功能页单行引用和真实模块任务入口；复用负责人、版本、幂等及通知直达。关系删除只移除当前集合，同事务审计完整前后身份、createdAt及增删；父锁→排序功能锁→任务锁，锁后重读和savepoint有限重试。新增归档关系拒绝，保留/移除历史归档关系允许。 |
| 修复 / 重构 / 优化 | 原条目未单列，见本表其他字段。 |
| 测试与验证 | 实际验证：协调独立复跑真库28/28（F15 8+F14 20）、Web22/22、契约权限18/18、Edge两任务E2E2/2（54.2秒），代码审核无阻断；实现者完整命令见 [F15交审说明](docs/f15-local-handoff.md)。交付仅检查最终diff空白和本批非生成TS/TSX格式；未使用实现者集群、未全量构建/全仓静态检查/无关审计，完整CI待PR运行。 |
| 文档同步 | 文档/影响：同步三份设计、权限/测试矩阵、Schema/Route Registry及生成客户端/OpenAPI；无迁移、依赖或安全基线变更。关系审计、锁序与重放资源已重点审核。 |
| 风险与后续 | 后续：F16/F19、记录/任务组读取统计仍未交付。最新HEAD全部适用CI成功、审核有效且GitHub保护允许后按授权squash合并；行为修复须增量交审。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

### 2026-09-09 21:56 +08:00 | A / Codex | feature/a-f05-project-read | F-05 成员管理补全（本地未推送）

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-f05-project-read`（已同步 `origin/main` `b569c3d`，F-15 #73；即将推送至此远端分支） |
| PR / commit | [PR #75](https://github.com/256-code/InPulse/pull/75)；本地提交 `6879dbe` |
| 做了什么 | 在前述 F-05.1 项目读取基础上完成 F-05 项目成员管理：新增成员历史、未完成任务、添加成员与移除成员四条路由，并将管理页接入真实 API；成员写操作要求管理员密码+TOTP 5 分钟重认证、CSRF 与数据库级幂等。 |
| 新增功能 | 服务端同事务写成员历史、审计、活动、通知与任务改派；移除成员保留原负责人或真实改派；移除创建者不改 `projects.created_by`；项目归档后旧 Key 重放重新检查可写性并拒绝 409；前端管理员入口、成员选择、未完成任务提示、改派、重认证成功后保留幂等 Key，并失效成员/项目缓存。 |
| 修复 / 重构 / 优化 | 修复 `ProjectMemberManagementController` 先前用 `@ContractBody` 把非 JSON Content-Type 提前转成 422 的问题，改为由服务层按契约返回 400；修复 `projects` 模块直接导入 `tasks/tasks-management.module.ts` 的越界，改为经 `tasks/index.ts` 公开表面导入，`check:deps` 通过；修复成员集合路径幂等摘要中多余 `userId` 的问题。 |
| 测试与验证 | `pnpm check`（公共 registry）通过：lint、format、typecheck、数据库迁移检查、契约漂移/校验（57 条路由）、build、deploy 预检、依赖边界、前端边界、权限矩阵（57/57）、依赖审计（No known vulnerabilities）、Secret 扫描、文档检查；`pnpm test:unit` 数据库 5、api-contract 67、Web 33 文件 104 例、API 59 文件 274 例；完整 `pnpm test:integration` 曾通过数据库 13、API 34 文件 190 例，本轮成员管理 8/8 复跑通过。 |
| 文档同步 | 更新 `docs/test-matrix.md`、`docs/permissions.md` 与本开发日志；Schema、Route Registry、OpenAPI、Web 客户端与权限矩阵已同步；无数据库迁移、依赖或 Secrets 变更。 |
| 风险与后续 | 成员管理页面尚未新增 Playwright E2E；归档后幂等重放暂以服务单测覆盖，尚未单独写成真实 SQL/HTTP 回放用例；PR #75 已创建，待 CI 全绿后按授权 squash 合入 `main`；不直接推送 `main`。 |

### 2026-09-09 20:12 +08:00 | A / Codex | feature/a-f05-project-read | Push 1

> 本条为 F-05.1 项目读取阶段记录；F-05 成员管理已在上述 21:56 条目中补全，最终交付以该条目为准。

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-f05-project-read`（已同步 `origin/main` `b569c3d`（F-15 #73），尚未推送） |
| PR / commit | 尚未创建 |
| 做了什么 | 实现 F-05.1 项目读取纵切片：`GET /api/v1/projects` 与 `GET /api/v1/projects/{projectId}`，并在 `/projects` 页面接入真实项目列表；F-05.2 成员管理、F-06 项目编辑/归档/恢复以及项目概览统计不在本次范围。 |
| 新增功能 | 契约新增 `ProjectPath`、`ProjectItem`、`ProjectListResponse`、`ProjectDetailResponse`；两条 GET 路由声明 Session 认证、CSRF/幂等 `none`，生成 OpenAPI/Web 客户端；后端通过 Session 解析 actor，先取服务端 `AuthorizedProjectScope`，SQL 前限制项目范围；系统管理员返回全部项目，普通成员只返回本人 ACTIVE 成员项目且归档仍可读；无权限/不存在统一 404，匿名/停用 401，非法 `projectId` 422，响应 `no-store`；前端项目卡片展示名称、状态、编码、活跃成员数与描述，创建成功后失效 `["projects"]` 查询。 |
| 修复 / 重构 / 优化 | 修复项目页此前“列表接口尚未接入”的占位，保持创建项目后的动态/搜索/通知入口；服务端与前端均不复用客户端提交的项目范围。 |
| 测试与验证 | 实际通过：`pnpm contract:generate`、`pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm test:unit`（database 5、api-contract 67、web 31 文件 95、api 57 文件 262）、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`（53 条路由）、`pnpm db:migrations:check`、`pnpm build`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm check:secrets`、`pnpm check:docs`、`pnpm check:deploy:test`、`pnpm audit --registry=https://registry.npmjs.org --audit-level=high`（No known vulnerabilities found）、`pnpm test:integration`（database 13、API 33 文件 182，PostgreSQL 18.6 + PGroonga）。`pnpm test:e2e` 16/20：本分支项目创建关键路径通过；`admin-users`、`features` 未出现管理员安全验证对话框，`module-tasks`、`tasks` 新建任务弹窗/抽屉发生严格模式定位冲突，四个失败均为既有用例且未触及本 diff，本轮未修复；`origin/main` 同一基线 CI 已全绿，本地失败以本机/DB 状态解释。`pnpm check` 未重复执行。 |
| 文档同步 | 更新 `docs/permissions.md`、`docs/test-matrix.md`、本开发日志；契约生成物已由工具更新。 |
| 风险与后续 | F-05.2 成员管理、F-06 项目编辑/归档/恢复仍未实现；本地 Playwright 4 个既有失败待 CI 复核；待推送后创建 PR，等待 CI 与非作者评审。 |
| 备注 | 本地未提交、未推送；与 F-15 同步后未直接推送 `main` 或修改远端分支。 |

### 2026-09-09 19:20 +08:00 | A / Codex | feature/a-f03-user-management | Push 5 (同步 main)

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `feature/a-f03-user-management`（已推送 `origin/feature/a-f03-user-management`） |
| PR / commit | [PR #72](https://github.com/256-code/InPulse/pull/72)；本地 merge 同步 `origin/main` `273b273`（PR #71 F-14）后再推送 |
| 做了什么 | 旧基线 CI 已全绿（`CI / workspace` 34343276896、`Documentation / docs` 34343277007），但合并时发现 `main` 已前进到 F-14，GitHub 判定冲突；将最新 `main` 合入 F-03 分支，解决 `app.module.ts`、E2E MFA fixture、生成客户端/指纹与开发日志冲突。F-03 管理员用例统一改用 `main` 的 test-scoped `mfaAdmin` fixture，并在 fixture 中暴露 `userId`、保留 TOTP replay step 重置。 |
| 新增功能 | 无新增用户功能；保留 F-03 用户管理的 6 条路由与前端页面。 |
| 修复 / 重构 / 优化 | 消除 F-03 与 F-14 的合并冲突；同步最新 Task 契约与生成客户端；E2E 管理员登录/重认证适配主线新 fixture。 |
| 测试与验证 | 实际通过：`pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/e2e typecheck`、`pnpm contract:generate`、`pnpm contract:drift`、`pnpm contract:validate`、`git diff --check`；本次未重跑完整本地 `pnpm test:e2e`，完整 CI 以推送后新 run 为准。 |
| 文档同步 | `apps/e2e/README.md`、本开发日志；契约生成物已同步。 |
| 风险与后续 | 合并后必须等新 CI 全绿；用户已授权 CI 绿后合入 PR #72，随后同步 `dev/a` 与 `main`。 |
| 备注 | 仅更新自有交付短分支，不直接推送 `main`。 |

### 2026-09-09 19:15 +08:00 | A / Codex | feature/a-f03-user-management | Push 4 (CI 结果回填)

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/a-f03-user-management`（已推送 `origin/feature/a-f03-user-management`） |
| PR / commit | [PR #72](https://github.com/256-code/InPulse/pull/72)；Push 3 提交 `4cf5854`，本次随日志回填提交 |
| 做了什么 | PR #72 的 GitHub Actions 已全部通过并回填结果：`CI / workspace` run 34343276896 success（13m8s），`Documentation / docs` run 34343277007 success；按用户授权准备合入。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | 本次仅文档回填，未重跑业务门禁；Push 3 的本地完整 E2E 18/18 与 CI 绿结果见上一条。 |
| 文档同步 | 本开发日志。 |
| 风险与后续 | 等待合并后进行 `dev/a` 与 `main` 同步；未手动推送 `main`。 |
| 备注 | 日志回填后不再等待 docs-only 新 CI，按用户明确授权执行 squash 合并。 |

### 2026-09-09 19:05 +08:00 | A / Codex | feature/a-f03-user-management | Push 3 (E2E 稳定性修复)

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `test` / `docs` |
| 分支 | `feature/a-f03-user-management`（已推送 `origin/feature/a-f03-user-management`） |
| PR / commit | [PR #72](https://github.com/256-code/InPulse/pull/72)；本地修复提交（Push 3） |
| 做了什么 | PR #72 首次 CI 的 `Browser E2E` 失败（16 通过 / 2 失败，run 34341533338），根因为多个用例共享同一管理员 TOTP `last_accepted_step`，未来 time-step 与时间边界导致验证码被拒绝；新增 E2E 测试夹具 `resetAdminTotpReplayStep`，在管理员登录及重认证前重置测试专用数据库的 `last_accepted_step`，并统一改用当前 TOTP 码。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 仅修改 E2E 测试夹具与 `admin-users`/`features`/`mfa` 用例；生产认证、权限和业务逻辑未改动。 |
| 测试与验证 | 本地完整 `pnpm test:e2e` 18/18 通过（1.9m），`apps/e2e` typecheck、全 workspace typecheck、`pnpm lint`、`pnpm format:check` 与 `git diff --check` 通过；GitHub Actions 新运行尚未执行。 |
| 文档同步 | `apps/e2e/README.md`、本开发日志。 |
| 风险与后续 | 等待 PR #72 新 CI；若全部成功且用户授权，后续合入；未推送或修改 `main`。 |
| 备注 | 仅更新自有交付短分支，不自动合并。 |

### 2026-09-09 18:40 +08:00 | A / Codex | feature/a-f03-user-management | Push 2 (PR 回填)

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/a-f03-user-management`（已推送 `origin/feature/a-f03-user-management`） |
| PR / commit | [PR #72](https://github.com/256-code/InPulse/pull/72)；Push 1 本地提交 `53a3723`，本次随日志回填提交 |
| 做了什么 | PR #72 已创建，base 为 `main`；本次仅回填 PR 链接与状态，无代码变更。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | 本次仅文档同步，未重跑业务门禁；Push 1 的完整本地验证结果见下一条记录。 |
| 文档同步 | 本开发日志。 |
| 风险与后续 | 等待 GitHub Actions 与非作者人工评审；未自动合并，未推送或修改 `main`。 |
| 备注 | 仅更新自有交付短分支；下次推送前继续按仓库规则回填 CI 结果。 |

### 2026-09-09 18:30 +08:00 | A / Codex | feature/a-f03-user-management | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `feature/a-f03-user-management`（已本地合并 `origin/main` `8386b29`） |
| PR / commit | 本地 merge `1e60f71`；本地功能提交 `0167bbe`；本次推送后创建 PR |
| 做了什么 | 检查 `main` 后确认其已更新（F-13 与 F-27/F-28 E2E）；将 `origin/main` 合并进 F-03 交付分支，解决 `AGENTS.md`、`app.module.ts`、`apps/e2e/README.md` 与生成客户端冲突，重新生成契约，并完成阶段 1 A 域 F-03 用户管理本地交付。 |
| 新增功能 | 新增 6 条 `/api/v1/admin/users` 路由（列表、创建、编辑、停用、启用、强制退出）；管理员列表、创建/编辑/启停/强退页面；写操作要求 5 分钟内密码+当前 TOTP 双因子重认证、CSRF、幂等键，版本化操作带 `If-Match`；停用/强退同事务递增 `auth_version`、撤销 Session 并写审计；禁止自停用/自降级/自强退，并保护最后一名可用 MFA 管理员。 |
| 修复 / 重构 / 优化 | 解决与 F-13/F-27/F-28 的模块装配、文档与生成客户端冲突；`Argon2id` 哈希改为事务外异步生成；E2E 管理员 TOTP 辅助改用下一时间步，避免与 MFA 用例重放冲突。 |
| 测试与验证 | 实际执行：`pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm contract:drift`、`pnpm contract:validate`（41 条路由）、`pnpm permissions:check`（41 条操作）、`pnpm test:unit`（database 5、api-contract 63、web 87、api 256）、`pnpm test:web`（30 文件 87 例）、本地 PostgreSQL 18.6 + PGroonga 下 `pnpm test:integration`（database 13、API 31 文件 152 例）、`pnpm build`、`pnpm check:frontend:boundaries`、`pnpm check:deps`、`pnpm check:secrets`（581 文件）、`pnpm check:docs`（55 Markdown）、`pnpm db:migrations:check`（6 个）、`pnpm check:deploy:test`、`pnpm test:e2e`（18/18，1.8m）与公共 registry `pnpm audit --registry=https://registry.npmjs.org --audit-level=high`（无漏洞）均通过；`git diff --check` 通过。未运行根级 `pnpm check` 聚合；GitHub Actions 尚未执行。 |
| 文档同步 | `README.md`、`AGENTS.md`、`apps/e2e/README.md`、`docs/permissions.md`、`docs/test-matrix.md`、`系统设计文档v1.0.2.md`、`技术设计v1.2.2.md`、本开发日志。 |
| 风险与后续 | 本次推送到自有交付短分支并创建 base 为 `main` 的 PR；禁止直接推送 `main`；需等待 GitHub Actions 与人工评审。 |
| 备注 | 当前阶段 1 A 域 F-03 本地实现和 main 合并验证已完成；Push 1 随本日志提交推送。 |
## 2026-09-09（未记录具体时间） | B / 协调交付与增量审核 | codex/f14-feature-tasks-pr | 推送 3（增量交审）

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` |
| 分支 | `codex/f14-feature-tasks-pr` |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-14 PR #71 MFA 测试隔离（推送 3，增量交审） |
| 新增功能 | 原条目未单列，见本表其他字段。 |
| 修复 / 重构 / 优化 | 协调授权修复已确认的 features/mfa 共享管理员因子状态耦合。新增每次尝试独立的 test-scoped fixture，提取既有真实 API 注册流程，保留真实 UI 登录/重认证和全部断言；移除 global setup/runtime 的共享管理员；finally 清理本账号认证材料、保留用户/审计历史。不清零已消费时间步、不放宽安全规则、不靠延时或跳过门禁。 |
| 测试与验证 | [CI 34340424204](https://github.com/256-code/InPulse/actions/runs/34340424204) 在 HEAD `ff85898` 的 Browser E2E 失败：16 通过、既有 MFA 用例两次登录后断言超时；F-14 Chromium 通过31秒，此前真库/契约/构建和四镜像扫描完成，后续门禁未运行；Documentation 成功6秒。<br>实际验证：独享 PostgreSQL 18/PGroonga 空库应用六迁移及 E2E 必要局部编译通过；修复前顺序合跑 features/mfa 3/3（36.2秒），未复现原 CI；修复后两轮顺序6/6（Edge，1.2分钟），四个不同管理员、结束后因子/Session为0。E2E局部类型检查、六文件格式/lint、diff空白检查通过。最初局部类型检查发现闭包内可空ID收窄问题，改为捕获常量后通过。 |
| 文档同步 | 文档：同步交审说明、测试矩阵及本日志。未取得失败trace/响应实证，该次CI根因仍为推断；repeat-each不是实际失败重试，重试隔离依赖test-scoped生命周期。无生产代码、依赖、数据库Schema或安全规则变更，不需新增ADR。增量审核通过后推送，完整CI由现有工作流验证。 |
| 风险与后续 | 原条目未单列，见本表其他字段。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。<br>增量审核：协调对话已检查独立fixture生命周期、真实enrollment、按自身userId清理及共享runtime移除，确认无生产认证/窗口/断言变化，于本次推送前审核通过；复用上述6/6证据，不重复无变化测试。 |

## 2026-09-09（未记录具体时间） | B / 协调交付 | codex/f14-feature-tasks-pr | 推送 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `style` |
| 分支 | `codex/f14-feature-tasks-pr` |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-14 PR #71 格式修复（推送 2） |
| 新增功能 | 原条目未单列，见本表其他字段。 |
| 修复 / 重构 / 优化 | 交付工作区以 `pnpm install --frozen-lockfile` 安装既有依赖，manifest/lockfile 未变；用已锁定 Prettier 3.9.6 仅格式化该测试文件，单文件 `prettier --check` 通过。修改仅调整换行排版，没有修改断言或业务行为；未运行全仓静态检查、全量构建或数据库测试。 |
| 测试与验证 | 首次 [CI 34340185800](https://github.com/256-code/InPulse/actions/runs/34340185800) 在 Format check 失败，仅 `TasksPanel.test.tsx`；安装和 lint 成功，后续门禁未执行。[Documentation 34340185791](https://github.com/256-code/InPulse/actions/runs/34340185791) 成功（8 秒）。 |
| 文档同步 | 原条目未单列，见本表其他字段。 |
| 风险与后续 | 原条目未单列，见本表其他字段。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。<br>分支 `codex/f14-feature-tasks-pr`；本次仅修复格式并记录实际 CI 结果，重推后继续等待最新 HEAD 的完整门禁。原已审核生产代码不变，后续业务范围仍见上一条及 [交审说明](docs/f14-local-handoff.md)。 |

## 2026-09-09（未记录具体时间） | B / 实现与协调交付 | codex/f14-feature-tasks-pr | 推送 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f14-feature-tasks-pr` |
| PR / commit | 负责人：B 实现、协调对话审核、专门 PR 对话交付；分支 `codex/f14-feature-tasks-pr`。类型 `feat(tasks)`；已审核代码 `1c058ac`、交审文档 `fb270fd`，PR 待创建。 |
| 做了什么 | F-14 功能级任务 PR 交付（推送 1）<br>上游同步：fetch 后 main 为 `8386b29`（PR #67），无冲突合入；上游只增活动/通知 E2E 与文档，F-14 生产代码保持已审核内容，测试矩阵和日志同时保留两批记录。 |
| 新增功能 | 功能：功能级任务创建、列表、详情、编辑与项目活跃成员指派；沿用版本冲突、幂等与父级锁，审计/活动/搜索/指派通知和业务写入同事务。历史未变负责人可保留，真正改派重新验证 ACTIVE。 |
| 修复 / 重构 / 优化 | 原条目未单列，见本表其他字段。 |
| 测试与验证 | 实际验证：实现者证据见 [F-14 交审说明](docs/f14-local-handoff.md)；协调独立复跑真实 PostgreSQL 任务/公开 Port 26/26、Web 任务/功能回归 13/13、契约权限 16/16、Edge 任务 E2E 1/1（25.5 秒），代码审核无阻断。交付工作区 `git diff --check origin/main...HEAD` 通过；未重复数据库测试，未运行本地全量构建、全仓静态检查或无关审计，完整 CI 待 PR 执行。 |
| 文档同步 | 文档/影响：三份设计、权限/测试矩阵、生成契约与客户端及交审说明同步；没有新增依赖、迁移或变更部署。API/权限和跨域公开 Port 为重点审核内容。 |
| 风险与后续 | 后续/风险：F-15 模块任务、F-16 状态流转、F-19、任务组及记录关联读取尚未交付。最新 PR HEAD 全部适用 CI 成功、协调审核有效且 GitHub 保护允许后按用户授权 squash 合并；任何行为修复重新交协调审核。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

### 2026-09-09 17:51 +08:00 | C / Codex | codex/c-activity-notifications-e2e | Push 4 (Rebase)

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-activity-notifications-e2e`（已 rebase 到 `origin/main` `1c2b5bf`） |
| PR / commit | [PR #67](https://github.com/256-code/InPulse/pull/67)；本地提交（Push 4） |
| 做了什么 | 将分支 rebase 到 F-13 合并后的最新 `origin/main` `1c2b5bf`；唯一冲突为开发日志，已保留 F-13 与 F-27/F-28 双方记录，并同步最终 Playwright 计数。 |
| 新增功能 | 无代码变更；rebase 后完整套件包含 F-13 `features.spec.ts` 2 例、搜索边界 5 例及本项目新增的活动/通知用例。 |
| 修复 / 重构 / 优化 | 仅 rebase 与文档同步；未修改产品代码、测试逻辑或 F-13 文件。 |
| 测试与验证 | 实际执行：`pnpm build` 通过；本地 PostgreSQL 18.6 + PGroonga 下 `pnpm test:e2e` 16/16 通过（1.4m），含 F-13、搜索边界、项目动态专属路径与通知已读/未读/全部已读；随后 `pnpm --filter @inpulse/e2e typecheck`、`pnpm format:check`、`pnpm check:docs` 与 `git diff --check` 通过。按用户要求，本次仅文档同步，不等待推送后新 CI。 |
| 文档同步 | `README.md`、`AGENTS.md`、`apps/e2e/README.md`、`docs/test-matrix.md`、本日志。 |
| 风险与后续 | PR #67 已 rebase 到最新 main；等待非作者人工评审，通过后合并并同步 `dev/c`。本次推送后新 CI 未等待/未回填。 |
| 备注 | 仅更新自有交付短分支并要求 force-with-lease；不推送 `main`。 |

### 2026-09-09 17:32 +08:00 | B / Codex | codex/f13-feature-management | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| PR / commit | 本条随 F-13 首次交付提交；计划创建 base 为 main 的 PR，不自行合并 |
| 做了什么 | fetch 后将 dev/b 从 `58abb40` ff-only 同步到最新 origin/main `8b895e1`，再创建交付分支；同步前备份全部已跟踪和未跟踪改动并保留 include-untracked stash，恢复无冲突 |
| 新增功能 | 功能列表、创建、详情、说明/标签编辑、项目内相似关键词提示、管理员重认证归档/恢复；七条接口与生成客户端、OpenAPI、权限/测试矩阵同步 |
| 一致性 | 同事务编号分配、实时授权、幂等、审计、活动/搜索投影；父项目/模块/功能锁序、If-Match 条件更新及三方冲突处理；编号不补零，恢复只恢复自身，无新增迁移、依赖或 lockfile 修改 |
| 测试与验证 | 本地审核已通过且无阻断项；复用审核对话独立复跑证据：真库 21/21、契约/权限 16/16、前端 16/16、Edge E2E 2/2。源码、测试、生成物与同步前备份哈希一致；交审文档仅 Git 换行规范化。上游增加 F-12 审计并发及搜索 E2E fixtures，未改 F-13 业务行为；未无变化重复执行既有验证 |
| 文档同步 | 三份设计、权限矩阵、测试矩阵、功能域 README、本地交审和本日志；保留上游全部记录 |
| 风险与后续 | 默认 Playwright Chromium及最新 PR CI 尚未验证，完整门禁由既有 CI 执行；本地未运行全量构建、全仓静态检查或无关审计。任务、迭代记录、链接、遗留问题及关联统计属后续范围 |
| 评审重点 | A：ProjectCodePort 编号分配和 ProjectsModule 装配；B/C：ModuleReadPort、FeatureCandidatesQueryPort、应用装配和生成客户端；需 A/C 非作者人工评审，不代写审批结论 |
| 备注 | README 原导航和未跟踪协作方案保留原位置及备份，不纳入 PR；只推送 B 交付分支，不推送 main、不强推、不重写共享历史 |

### 2026-09-09 17:25 +08:00 | C / Codex | codex/c-activity-notifications-e2e | Push 3 (Rebase)

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/c-activity-notifications-e2e`（已 rebase 到 `origin/main` `8b895e1`） |
| PR / commit | [PR #67](https://github.com/256-code/InPulse/pull/67)；本地提交 `08e9808`（Push 1 代码）与本日志提交（Push 3） |
| 做了什么 | 在搜索边界 PR #68 合入后 rebase，补齐并同步 F-27/F-28 E2E 文档与日志。 |
| 新增功能 | 用户可感知能力仍来自既有 F-27/F-28；新增 `activity.spec.ts` 专属项目动态路径，`notifications.spec.ts` 扩为已读 ↔ 未读切换、全部已读与铃铛未读数联动。 |
| 修复 / 重构 / 优化 | 仅 rebase 与文档同步，不修改产品代码或测试逻辑；恢复本分支 Push 1/Push 2 日志。 |
| 测试与验证 | 实际通过：`pnpm build`；本地 PostgreSQL 18.6 + PGroonga 下 `pnpm test:e2e` 为 14/14（含搜索边界），新增活动与通知场景均通过；随后执行 `pnpm --filter @inpulse/e2e typecheck`、`pnpm format:check`、`pnpm check:docs` 与 `git diff --check`。PR #67 首次 CI 已通过（workspace 9m58s，docs 通过，run 34328705246/34328705325）；rebase 后 GitHub Actions 已通过（workspace 11m7s，docs 通过，run 34334259877/34334259709）。 |
| 文档同步 | `README.md`、`AGENTS.md`、`apps/e2e/README.md`、`docs/test-matrix.md`、本日志。 |
| 风险与后续 | PR #67 rebase 后 GitHub Actions 已通过；等待非作者人工评审，通过后合并并同步 `dev/c`。 |
| 备注 | 仅更新自有交付短分支并要求 force-with-lease；不推送 `main`。 |

### 2026-09-09 16:20 +08:00 | C / Codex | codex/c-activity-notifications-e2e | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-activity-notifications-e2e`（已跟踪 `origin/codex/c-activity-notifications-e2e`） |
| PR / commit | 尚未创建 PR；Push 1 已推送 `origin/codex/c-activity-notifications-e2e`，本地 Push 1 提交 `dfe1326` |
| 做了什么 | 回填 Push 1 实际推送结果：F-27/F-28 E2E 分支已推送到远端，等待 CI 与人工评审。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | Push 1 前已通过：`apps/e2e` typecheck、完整 `pnpm test:e2e`（9/9）、`pnpm lint`、`pnpm format:check`、`pnpm check:docs`；公共 registry 审计无漏洞。本次仅更新日志，未重新运行其他门禁。 |
| 文档同步 | 本日志 Push 2。 |
| 风险与后续 | GitHub Actions 尚未执行；远端分支待 CI 验证；与搜索分支合并顺序需在 PR 阶段确认。 |
| 备注 | Push 2 随本次日志回填一起推送；尚未创建 PR。 |

### 2026-09-09 16:02 +08:00 | C / Codex | codex/c-activity-notifications-e2e | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` |
| 分支 | `codex/c-activity-notifications-e2e`（基于 `dev/c`） |
| PR / commit | 尚未创建 PR；本地提交（Push 1） |
| 做了什么 | 补齐 F-27 项目动态与 F-28 站内通知 Playwright E2E 关键交互。 |
| 新增功能 | 新增 `activity.spec.ts`，覆盖创建项目后进入专属项目动态页并看到 `project.create` 条目；`notifications.spec.ts` 由页面冒烟扩为已读↔未读切换、全部已读与铃铛未读数联动；抽取 `createProjectViaUi` 供两个场景复用。 |
| 修复 / 重构 / 优化 | 仅扩充测试与测试辅助，无业务代码修改。 |
| 测试与验证 | 实际通过：`pnpm install --frozen-lockfile`、`pnpm build`、`pnpm --filter @inpulse/e2e typecheck`、`pnpm test:e2e`（9/9）。后续实际通过 `pnpm lint`、`pnpm format:check`、`pnpm check:docs`；`pnpm check` 除 `deps:audit` 因本机拉取 npmmirror 无 audit endpoint 失败外均通过，公共 registry 审计返回 `No known vulnerabilities found`；GitHub Actions 尚未执行；验证命令使用 `E2E_DATABASE_URL=postgresql://cluster_bootstrap@127.0.0.1:55432/app`、`E2E_API_PORT=3111`、`E2E_WEB_PORT=4181`。 |
| 文档同步 | `docs/test-matrix.md` 更新 ACT/NOT/FE 覆盖状态；`apps/e2e/README.md`、`README.md`、`AGENTS.md` 同步 9/9 与覆盖范围；本日志。 |
| 风险与后续 | 用例复用 UI 创建项目并产生业务投影，依赖真实 PostgreSQL 与成员 fixture；GitHub Actions、lint、format 与 check:docs 待运行；与 `codex/c-search-e2e-boundaries` 并行分支合并时需处理文档和测试计数冲突。 |
| 备注 | 已提交本地（Push 1）；尚未创建 PR，推送结果待 Push 2 回填。 |


### 2026-09-09 16:36 +08:00 | C / Codex | codex/c-search-e2e-boundaries | Push 5 (Rebase)

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-e2e-boundaries`（已 rebase 到 `origin/main` `eef2df4`） |
| PR / commit | [PR #68](https://github.com/256-code/InPulse/pull/68)；本地提交 `6e51b60`、`343cccb` 与本日志提交 |
| 做了什么 | 将搜索边界 E2E 分支 rebase 到最新 `origin/main`，解决与 PR #66、PR #65、PR #69 在文档与开发日志上的冲突。 |
| 新增功能 | 无代码变更；保留主线审计并发证据与 F-12 日志，当前 Playwright 总数保持 main 9/9 + 搜索边界 4 例 = 13/13。 |
| 修复 / 重构 / 优化 | 仅合并文档记录，不修改产品代码或测试逻辑。 |
| 测试与验证 | PR #68 上一版本完整 CI 已通过（workspace 10m14s，docs 通过，run 34331416114、34331416146）；rebase 后实际通过 `pnpm --filter @inpulse/e2e typecheck`、`pnpm format:check`、`pnpm check:docs` 与 `git diff --check`；本次未重跑完整 E2E，交由推送后的 CI 验证。 |
| 文档同步 | `docs/test-matrix.md`、`README.md`、`AGENTS.md`、`apps/e2e/README.md`、`开发日志.md`；本日志 Push 5。 |
| 风险与后续 | PR #68 等待推送后的 CI 与非作者人工评审；F-27/F-28 活动通知分支仍待本 PR 合并后 rebase，并重新核对最终 E2E 总数。 |
| 备注 | 仅更新自有交付短分支，不推送 `main`。 |


### 2026-09-09 16:46 +08:00 | A / Codex | codex/a-f12-audit | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/a-f12-audit`（基于 `origin/main` `58abb40`） |
| PR / commit | [PR #69](https://github.com/256-code/InPulse/pull/69)；本地提交 `86ff43e`（Push 1）与本日志提交（Push 2） |
| 做了什么 | F-12 合入后补齐 F-08 的“真实业务命令 100 并发同链”证据：在 `ModulesManagementService.createModule` 上并发执行 100 笔业务事务，验证审计、Activity 与 Search 同事务落库。 |
| 新增功能 | 无用户可感知新功能；新增 `MOD-AUDIT-CONC-001`，关闭 F-08 中“业务命令接入后须重跑 100 并发”的验收缺口。 |
| 测试与验证 | 实际执行：`pnpm --filter @inpulse/api exec tsc --noEmit` 通过；本地 PostgreSQL 18.6 + PGroonga 上 `modules-api.integration.test.ts` 10/10 通过（新增 100 并发用例 3.38s、完整文件 4.01s）；`pnpm test:integration` 全量通过（database 13/13，API 29 文件 134/134）。未运行全量 `pnpm check`、E2E 和 GitHub Actions。 |
| 文档同步 | 更新 `docs/test-matrix.md`（MOD-AUDIT-CONC-001、AUDIT-001）与 `README.md`。 |
| 风险与后续 | F-08 的远端 WORM 归档、恢复演练与多 scope 并发链头竞争仍待 F-10.3 与人工定案；本次不宣称完整 F-08 验收通过。 |
| 备注 | 仅推送 A 交付短分支，不直接推送 `main`；PR #69 已创建，CI 结果待后续回填。 |

### 2026-09-09 16:35 +08:00 | B / Codex | codex/f12-modules-management | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `test` / `docs` |
| PR / commit | [PR #65](https://github.com/256-code/InPulse/pull/65)；将最新 origin/main `3a8158f` 合入交付分支，保留共享历史，不重写提交 |
| 做了什么 | 解决 app.module.ts 与开发日志冲突：保留 F-12 模块装配及主线 APP_FILTER/APP_INTERCEPTOR，全量保留双方日志记录；适配主线新增 Controller 元数据要求，为五条模块路由补齐 @Operation |
| 测试与验证 | 局部 API/HTTP 测试 5 文件 18/18；契约/绑定测试首次发现五条路由缺少 @Operation，修复后 2 文件 16/16；模块真实 PostgreSQL/HTTP 9/9（接入全局异常过滤器及响应校验器）通过；契约漂移 5 个产物一致；三个修改的 TS 文件格式检查通过。隔离库启动首次未带 55432 参数导致连接失败，按原参数重启后测试通过，结束后已停止隔离库 |
| 文档同步 | 本日志和 F-12 交审说明；契约数据、生成物、权限规则与数据库不变量不变 |
| 风险与后续 | 用户要求本次改完直接推送，不再等待或跟踪 CI；既有自动触发配置未修改，不声明本次提交 CI 通过。前一提交 `54cfbe1` 的 CI run 34328536541 已通过，但不替代本次验证。未运行全量构建、全仓静态检查或无关审计 |
| 备注 | README 原导航及未跟踪协作方案以限定路径 include-untracked stash 备份，提交后恢复原路径并保留 stash；仅推送 B 交付分支，PR 保持未合并，待 A/C 非作者评审 |

### 2026-09-09 16:20 +08:00 | B / Codex | codex/f12-modules-management | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `docs` |
| PR / commit | [PR #65](https://github.com/256-code/InPulse/pull/65)；修复 `a0cf668` 的 [CI run 34327369577](https://github.com/256-code/InPulse/actions/runs/34327369577) 失败 |
| 做了什么 | CI 发现本次新增 module-routes 对 route-registry 的类型导入构成循环；将共享路由类型及常量原样提取到 route-definition，registry 保持原有导出，两者只依赖共享定义，不改路由或响应行为 |
| 测试与验证 | 修复后契约包单元 5 文件 61/61、契约包 tsc、contract:drift（5 个产物一致）、contract:validate（28 条路由）、git diff --check 通过；仅格式化 3 个改动的契约源码文件。首次提取后局部 tsc 发现 registry 内 apiBasePath 引用未显式导入，补齐后上述验证通过。未运行全仓静态检查或全量构建，完整依赖门禁由新一轮 CI 验证 |
| 已有 CI 证据 | `a0cf668` 的 lint/格式/类型/单测、迁移、真实 PostgreSQL 集成（API 28 文件 129 例，含模块 HTTP 9/9）、契约漂移/完整性、应用与生产镜像构建、全部镜像扫描、默认 Chromium E2E 9/9（含模块双页面竞争）和部署预检通过；随后 Dependency boundary check 因上述循环失败，后续门禁未执行；Documentation workflow 通过。不把此 run 声称为整体通过 |
| 文档同步 | 本日志与 F-12 交审状态、PR 验证说明；纯契约内部依赖整理，未改变 API/权限/数据库规则，生成物未变化 |
| 风险与后续 | 本次缺陷已修复并补相关验证，等待最新提交完整 CI；未弱化断言、跳过测试或修改门禁。最终仍由 A/C 非作者人工评审，不自行合并 |

### 2026-09-09 16:10 +08:00 | B / Codex | codex/f12-modules-management | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| PR / commit | [PR #65](https://github.com/256-code/InPulse/pull/65)，base main；功能提交 `f51ab0d` 已推送，本次追加交付状态记录 |
| 做了什么 | 已使用 B 账号 suikiiovo 创建 PR，回填本地交审说明的 PR 链接与当前状态 |
| 测试与验证 | 本次仅文档状态更新，暂存 diff 检查通过；沿用 Push 1 所列审核验证，无代码或测试行为变化，未重复运行测试、全量构建或全仓检查。远端 CI 已由 PR 触发，结果待实际完成；默认 Chromium 本地仍未验证 |
| 文档同步 | 本日志与 F-12 本地交审说明 |
| 风险与后续 | 跟踪最新提交的 CI；完成后交 A/C 非作者人工评审，不自行合并。README 原导航和原未跟踪协作方案仍在工作区，备份/stash 保留 |

### 2026-09-09 16:08 +08:00 | B / Codex | codex/f12-modules-management | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `test` / `docs` |
| PR / commit | 本条随 F-12 首次交付提交；计划推送 B 的交付分支并创建 base 为 main 的 PR，尚未合并 |
| 做了什么 | fetch 后以实际 origin/main `9ca018e` 为准，将 dev/b 从 `d2c5bbc` ff-only 同步，再创建交付分支；同步前建立全部已跟踪改动和未跟踪文件的目录备份与 include-untracked stash，均保留 |
| 新增功能 | 五条模块列表、创建、编辑、管理员归档/恢复接口及页面，项目/活动入口；未分类名称/描述可编辑，kind 不变；归档历史可读，高风险操作要求管理员重认证、原因及 If-Match |
| 修复 / 重构 / 优化 | 409 保留编辑前快照、用户草稿和服务端最新值；未改字段采用最新值，同字段冲突显式选择完成后才采用最新版本号，再次提交仍受 If-Match 保护；幂等、业务、审计、活动和搜索写入同一事务 |
| 测试与验证 | 复用交审证据：契约/权限 16/16、API 单元 6/6；独立审核复跑前端 7/7、真实 PostgreSQL/HTTP 9/9、Edge E2E 1/1（含双页面竞争）通过。同步仅开发日志冲突，已保留本地四条 F-12 历史记录与上游全部记录；源码、测试、生成物与备份哈希一致，无行为变化，未重复执行已通过测试 |
| 文档同步 | 功能/系统/技术设计、开发工作书、权限矩阵、测试矩阵、模块 README、本地交审与历史接入说明、开发日志 |
| 风险与后续 | 本地代码复审已通过；默认 Playwright 下载版 Chromium、远端 CI 尚未验证，完整门禁由既有 CI 执行。本次未运行全量构建、全仓静态检查或无关审计；不新增依赖、迁移或安全门禁变更 |
| 备注 | 仅选择性提交 F-12；README 原导航修改和未跟踪协作方案继续留在原工作区并有备份，不纳入 PR。推送后跟踪 CI，交 A/C 非作者人工评审；不合并、不推送 main、不强推 |

### 2026-09-09 | B / Codex | dev/b | F-12 409 修复与真库/E2E 交审，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `test` |
| PR / commit | 未创建；工作基线仍为 `d2c5bbc`；fetch 看到 `origin/main` 的 `9ca018e` 为上游验证说明，没有覆盖本地改动 |
| 做了什么 | 修复 409 后仅替换版本号造成旧表单覆盖他人修改的问题；保留编辑前快照、草稿和服务端最新数据，按字段三方合并 |
| 用户行为 | 用户未改字段采用最新值，双方修改且值不同的字段展示三份差异，明确选择全部冲突后才应用最新版本；归档/不可访问时保留草稿并阻止提交；关闭表单后忽略迟到的重新加载结果 |
| 测试与验证 | 新增 3 个回归用例先在旧实现失败，再修复为模块前端 7/7 通过；API 单元 6/6、契约/权限 16/16 通过。用户授权下载官方 PostgreSQL 18.6 + PGroonga 4.0.8 便携包后，按已有初始化步骤执行 6 个迁移成功，真库/HTTP 9/9（2.26 秒）通过；初次测试装饰器加载失败已用等价 Nest 注册修正，未改断言。既有 Playwright 基座使用本机 Edge 执行 F-12 E2E 1/1（15.5 秒）通过，包含双页面不同字段和同字段竞争。完整命令见交审说明 |
| 局部检查 | Web/E2E/API 测试类型检查与变更前端文件 ESLint 通过，contract:drift/validate 通过；仅编译 E2E 启动所需 API、契约及数据库包；未运行全量构建、全仓检查、无关测试或 CI |
| 文档同步 | 更新 [F-12 交审说明](docs/f12-local-handoff.md)、测试矩阵及本日志；仅修复交互，不改变 API、权限或数据库不变量 |
| 风险与后续 | 真库和浏览器缺失阻断已解除；便携测试环境仅限本机隔离验证，默认 Playwright 下载版 Chromium 未运行；待审核对话检查，不代替人工审查 |
| 备注 | 原 README 导航、未跟踪协作方案及其他任务改动保留；未提交、推送、创建 PR 或合并 |

### 2026-09-09 | B / Codex | dev/b | F-12 本地实现交审，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| PR / commit | 未创建；基线 `d2c5bbc` |
| 做了什么 | 新增模块 Schema/Route Registry、五条模块接口、本域 Repository/应用服务、模块页面与表单；普通读取保留归档历史，写入真实归属/父级/版本检查，管理员归档恢复复用 A 的重认证，幂等及审计/活动/搜索共用单事务 |
| 新增功能 | 从项目创建成功卡片、项目动态页进入模块列表，创建普通模块，编辑普通及未分类名称/描述，管理员填写原因归档/恢复；409 保留输入并加载最新版本后继续编辑；无删除或假统计 |
| 修复 / 重构 / 优化 | 未分类可编辑规则已按用户确认同步；生成物由工具产生。新增路由初始 If-Match 行为头大小写修正后从 HEAD 指纹基线重新生成，未改写历史契约；弹窗 jsdom 测试关闭动画以保留可见性断言 |
| 测试与验证 | API 本切片单测 2 文件 6 例通过；契约及权限绑定测试 2 文件 16 例通过；模块前端 4 例与项目页面 1 例通过；contract:generate/drift/validate 通过；API 源码/测试及 Web 局部包类型检查通过；仅格式化本次 TS 文件。真实数据库/HTTP 9 例及 Playwright 1 例已写但未运行，本机缺隔离数据库。未运行全量构建、全仓静态检查或无关审计 |
| 文档同步 | 功能/系统/技术设计、工作书、权限矩阵、测试矩阵、Modules 接入说明、历史阻断状态与 [本地交审说明](docs/f12-local-handoff.md) |
| 风险与后续 | A/C 接入阻断已关闭；真库事务/锁/幂等行为与 E2E 仍待隔离环境运行，不声明完整验收通过。前端导航共享区域与鉴权/契约需审核对话及人工检查 |
| 备注 | README 原导航与未跟踪交付方案保留；日志冲突保留双方记录，stash 暂留；未提交、推送、创建 PR 或合并 |

### 2026-09-09 | B / Codex | dev/b | A 域复核与未分类编辑定案，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| PR / commit | 未创建；快进到最新 `origin/main` / `origin/dev/a` 的 `b8c37d6` |
| 做了什么 | 确认 A 的 MFA/重认证接口、生成客户端与同事务 AdminHighRiskAuthService.verify 已落库，关闭两个后端接入阻断；前端认证交互仍缺失 |
| 新增功能 | 无；F-12 业务实现仍未完成 |
| 文档同步 | 按用户定案允许修改未分类模块名称、描述，保持 kind 不变及禁止物理删除；同步功能设计、技术设计、工作书、权限矩阵、待实现测试项与接入说明 |
| 测试与验证 | `pnpm --filter @inpulse/api exec vitest run test/mfa-enrollment.controller.test.ts test/mfa-verify.controller.test.ts test/mfa-reauthenticate.controller.test.ts`：3 文件 14 测试通过；pnpm 自动同步主线 lockfile 对应本地依赖。未运行真库/HTTP 集成/E2E、构建或全仓静态检查 |
| 风险与后续 | 仍需 A 的 MFA/重认证前端入口；复用已合入 Playwright 基座。主线文档所列 PostgreSQL 安装在本任务机器上未发现，不能引用为本次真库验证 |
| 备注 | README 与原有未跟踪方案保留；日志同步冲突已保留双方记录并解除，stash 安全副本暂留；未提交、推送、创建 PR 或合并远端 PR |

### 2026-09-09 | B / Codex | dev/b | 本地接入核对，未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| PR / commit | 未创建；本地快进同步至 `origin/main` 的 `a2e1084` |
| 做了什么 | 定向核对 F-12 设计与接入边界，记录管理员 MFA/重认证接口、事务内重认证检查、前端认证入口缺项，以及未分类模块改名规则冲突 |
| 新增功能 | 无；F-12 完整纵切片未完成，尚未新增业务实现、契约或页面 |
| 修复 / 重构 / 优化 | 无；原 README 导航增量与未跟踪交付方案完整保留 |
| 测试与验证 | 执行 git status、fetch、提交差异计数与 ff-only 同步；恢复后检查 README diff、交付方案文件哈希一致。未运行行为测试、真库/HTTP/E2E、生成器、静态检查及构建；当前进程缺少测试数据库配置，未重复尝试失败命令 |
| 文档同步 | 新增 [F-12 接入阻断交审](docs/f12-readiness-blockers.md)；未改变设计决策或将权限/测试矩阵标记为已实现 |
| 风险与后续 | 请审核方核对阻断，A 提供真实 MFA/重认证闭环及可复用事务内检查，设计负责人定案未分类改名规则；随后继续本切片实现和针对性验证 |
| 备注 | 未提交、推送、创建 PR 或合并远端 PR；本条不是功能完成声明 |

### 2026-09-09 15:54 +08:00 | A / Codex | codex/a-f01-f08-f11 | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/a-f01-f08-f11`（基于 `origin/main` `9ca018e`） |
| PR / commit | [PR #66](https://github.com/256-code/InPulse/pull/66)；本地提交 `e4ca051`（Push 1）、`d8740f7`（Push 1 日志回填）与本日志提交（Push 2，CI 结果回填） |
| 做了什么 | 补齐本轮核查出的 F-01 Session 分批清理和 F-11 Nest Zod Pipe/Serializer 与统一错误模型缺口；F-08 补齐审计密钥惰性轮换与同事务回滚证据；F-08 业务 Workflow 审计接入与远端 WORM 归档仍不属于本轮已有业务范围。 |
| 新增功能 | F-01：按主键分批清理已撤销超过 30 天或绝对过期超过 7 天的 `user_sessions`、过期 `session_csrf_tokens` 与过期/已消费 `preauth_sessions`，单事务内 `FOR UPDATE SKIP LOCKED` 与可配置批大小；F-11：`@Operation` + `ContractBody/Query/Path/Headers` 运行期绑定、全局 `ContractResponseInterceptor`、统一 `ApiExceptionFilter`、`StrictSameOriginGuard` 与 `X-Request-Id`；F-08：keyring 当前版本驱动逐链惰性轮换，同事务回滚业务、审计行与链头。 |
| 修复 / 重构 / 优化 | 请求头部只校验 Schema 声明字段，避免 Express 附加头误报；响应未知字段剔除；Nest `HttpException` 按状态映射为统一错误；未匹配路由不回显 method/path；Controller 不再手工重复解析业务 Zod Schema；依赖未变更，无 lockfile 修改。 |
| 测试与验证 | 实际通过：`pnpm check` 全绿（lint、format:check、typecheck、单测 database 5 例 / api-contract 59 例 / web 63 例 / api 243 例、迁移检查、contract:drift/validate、build、check:deploy:test、依赖边界、权限矩阵、`deps:audit` 无已知漏洞、Secret 与文档检查）；真实 PostgreSQL database 集成 13 例、API 集成 28 文件 124 例（含审计 4/4、Session 清理 2/2）；`pnpm test:e2e` 8/8；Push 1 的 PR #66 GitHub Actions workspace 9m36s、docs 通过（run 34327641842、34327641970）；Push 2 的 GitHub Actions 待执行。 |
| 文档同步 | 新增 `docs/adr/ADR-029.md`，更新 `docs/adr/README.md`、`ADR-026.md`、`ADR-027.md`、`技术设计v1.2.2.md`、`AGENTS.md`、`docs/test-matrix.md` 与本日志。 |
| 风险与后续 | F-08 仍待各业务 Workflow 接入审计、远端 WORM 归档和最终 100 并发业务事务证据；F-11 待 GitHub Actions 与完整业务 E2E 验证；本轮不替代按阶段验收基线冻结。 |
| 备注 | 本次仅推送交付短分支，不直接推送 `main`；PR 创建与 CI 结果在后续条目回填。 |

### 2026-09-09 16:20 +08:00 | C / Codex | codex/c-search-e2e-boundaries | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-e2e-boundaries`（已跟踪 `origin/codex/c-search-e2e-boundaries`） |
| PR / commit | [PR #68](https://github.com/256-code/InPulse/pull/68)；Push 1 已推送 `origin/codex/c-search-e2e-boundaries`，本地提交 `073029b` |
| 做了什么 | 回填 Push 1 推送结果：搜索边界 E2E 分支已推送到远端，PR #68 已自动创建；首次 CI 已通过。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | Push 1 前已通过：`apps/e2e` typecheck、完整 `pnpm test:e2e`（12/12）、`pnpm lint`、`pnpm format:check`、`pnpm check:docs`；公共 registry 审计无漏洞；PR #68 GitHub Actions workspace 10m44s、docs 通过（run 34328753454、34328753406）。 |
| 文档同步 | 本日志 Push 2。 |
| 风险与后续 | PR #68 与最新 `origin/main` 存在文档冲突，需 rebase 后重跑 CI；F-27/F-28 并行分支待搜索 PR 合入后顺序处理。 |
| 备注 | Push 2 随本次日志回填一起推送；PR #68 已创建。 |

### 2026-09-09 15:54 +08:00 | C / Codex | codex/c-search-e2e-boundaries | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` |
| 分支 | `codex/c-search-e2e-boundaries`（基于 `dev/c`） |
| PR / commit | 尚未创建 PR；本地提交（Push 1） |
| 做了什么 | 补齐 F-26 全局搜索 Playwright E2E 边界：无权限项目隔离、无匹配空态、服务端签名游标分页和 PGroonga 中文短词/特殊标识符语义检索。 |
| 新增功能 | `search.spec.ts` 由 1 个正向用例扩为 5 个；`global-setup` 新增无当前用户成员关系的隐藏项目、25 条分页投影和语义查询 fixture；`global-teardown` 同步清理隐藏项目搜索投影；`E2ERuntime` 暴露对应 fixture。 |
| 修复 / 重构 / 优化 | 按评审 P1 调整：分页用例不再断言 `results.length` 文案，只断言已渲染条目计数与“加载更多”消失。 |
| 测试与验证 | 实际通过：`pnpm --filter @inpulse/e2e typecheck`、`pnpm --filter @inpulse/api build`、评审 P1 调整后 `pnpm test:e2e`（完整 12/12）、`pnpm format:check`。后续实际通过 `pnpm lint`、`pnpm format:check`、`pnpm check:docs`；`pnpm check` 除 `deps:audit` 因本机拉取 npmmirror 无 audit endpoint 失败外均通过，公共 registry 审计返回 `No known vulnerabilities found`；GitHub Actions 尚未执行；验证命令使用 `E2E_DATABASE_URL=postgresql://cluster_bootstrap@127.0.0.1:55432/app`、`E2E_API_PORT=3114`、`E2E_WEB_PORT=4184`。 |
| 文档同步 | `docs/test-matrix.md` 更新 SEARCH-003 与当前搜索状态；`apps/e2e/README.md`、`README.md`、`AGENTS.md` 同步 12/12 与覆盖范围；本日志。 |
| 风险与后续 | 搜索 fixture 依赖真实 PGroonga 实例与数据库 bootstrap 角色；GitHub Actions 尚未执行；F-27/F-28 E2E 已在并行独立 worktree 完成，待本分支合入后 rebase 并更新测试计数。 |
| 备注 | 已提交本地（Push 1）；尚未创建 PR，推送结果待 Push 2 回填。 |

### 2026-09-09 14:55 +08:00 | C / Codex | codex/c-mfa-frontend | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/c-mfa-frontend`（已同步 `origin/main` `c18208d`，Push 3 合并远端 main） |
| PR / commit | [PR #63](https://github.com/256-code/InPulse/pull/63)；本地提交 `944506d`（Push 1）、`185c33b`（Push 2 回填日志）与 `8957fa2`（Push 3 合并 `origin/main`）；CI 结果回填为 Push 4 |
| 做了什么 | 补齐登录页 MFA 前端交互与管理员重认证：登录成功后保留 `LoginResponse.csrfToken`；TOTP 注册、验证、恢复码失败统一映射为安全文案；管理员账户菜单新增“管理员安全验证”弹窗，输入管理员密码和当前 6 位 TOTP 完成重认证。 |
| 新增功能 | `MfaEnrollmentForm`、`MfaChallengeForm`、`MfaRecoveryForm`、`AdminReauthenticateModal`、`auth-errors` 与 Playwright `mfa.spec.ts`；`global-setup` 通过后端 API 预注册第二个 MFA 管理员并注入运行时 TOTP Secret；`global-teardown` 清理相关 Session、因子、恢复码与幂等记录。 |
| 修复 / 重构 / 优化 | 保留受限 MFA Session 只在内存中的生命周期，刷新后回到登录页；注册/验证/恢复码成功后使用响应中的新 CSRF Token；重认证前先签发 CSRF；E2E 注册确认使用上一 30 秒时间步，避免与登录挑战在同一时间步被判定为 replay。 |
| 测试与验证 | 实际通过：`pnpm test:unit`（database 5 例、api-contract 4 文件 59 例、web 25 文件 63 例、api 47 文件 224 例）、`pnpm test:e2e`（8/8）、`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm format:check`、`pnpm check:frontend:boundaries`（101 模块 363 依赖）、`pnpm check:secrets`、`pnpm check:docs`；完整 `pnpm check` 已执行，除本机 npm 镜像无 audit endpoint 外全部通过，`pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 返回 `No known vulnerabilities found`；PR #63 的 GitHub Actions 已通过（workspace 10m2s，docs 通过，run 34321871445）。 |
| 文档同步 | 同步 README、AGENTS.md、测试矩阵与 E2E README；MFA UI 用例由 7/7 更新为 8/8。 |
| 风险与后续 | 生产环境仍需 TOTP KEK 相关 Secret；完整业务关键路径 E2E、真实镜像 Tag/digest 绑定与签名发布清单、生产加密备份恢复仍待补齐；本 PR 已通过 CI，仍需非作者人工评审。 |
| 备注 | 本次推送只更新交付分支 `codex/c-mfa-frontend`，不推送 `main`，不使用 force push；Push 4 仅同步 PR #63 的 CI 通过结果。 |

### 2026-09-09 15:16 +08:00 | C / Codex | codex/c-mfa-ci-docs | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-mfa-ci-docs`（基于已合并的 `origin/main` `d2c5bbc`） |
| PR / commit | [PR #64](https://github.com/256-code/InPulse/pull/64)；本地提交 `8ac4e37`（Push 1）及本次日志补充提交（Push 2） |
| 做了什么 | 在 PR #63 合入后，仅同步 CI 通过结果到 README、AGENTS.md、测试矩阵、E2E README 与开发日志，消除“GitHub Actions 尚未执行”的过时表述。 |
| 修复 / 重构 / 优化 | 无代码、API、数据库或测试行为变更；只更新当前状态文档。 |
| 测试与验证 | `pnpm format:check`、`pnpm check:docs`、`git diff --check` 通过。 |
| 文档同步 | 同步 README、AGENTS.md、测试矩阵、E2E README 与本开发日志。 |
| 风险与后续 | 仅文档状态同步，需非作者人工评审后合并；PR #63 的 GitHub Actions 已通过（workspace 10m2s，docs 通过）。 |
| 备注 | 本次推送只更新交付分支 `codex/c-mfa-ci-docs`，不推送 `main`，不使用 force push。 |

### 2026-09-09 13:51 +08:00 | C / Codex | codex/c-design-visual-members | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `codex/c-design-visual-members`（已合并 `origin/main` `b8c37d6`） |
| PR / commit | PR 61；本地合并提交 `b80cf6f` |
| 做了什么 | 将主分支 F-02 认证纵切片合并进本分支，解决认证模块、契约入口、权限矩阵、权限测试、生成客户端/OpenAPI 与开发日志冲突；重新生成契约与 API 客户端并完成全量验证。 |
| 新增功能 | 无额外新功能；本分支保留用户目录、成员选择与设计师视觉迁移，同时合入主分支 MFA 注册/验证/重认证、恢复码轮换/消费与管理员重置能力。 |
| 修复 / 重构 / 优化 | `AuthModule` 同时注册用户目录与 MFA/管理员高风险控制器；契约入口、权限矩阵和扫描测试同时保留两类路由；生成物由源定义重建，避免手工合并 OpenAPI/客户端。 |
| 测试与验证 | 实际通过：`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm build`、`pnpm test:unit`（database 5 例、api-contract 4 文件 59 例、web 23 文件 52 例、api 47 文件 224 例）、`pnpm test:integration`（database 13 例、api 27 文件 120 例）、`pnpm test:e2e`（7/7）、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`、`pnpm check:frontend:boundaries`（95 模块 329 依赖）、`pnpm check:docs`、`pnpm check:deps`、`pnpm check:secrets`、`pnpm check:deploy:test`、`pnpm db:migrations:check`；GitHub Actions 尚未执行，未运行完整 `pnpm check`。 |
| 文档同步 | 更新本开发日志；现有 PR 61 正文继续使用合并前的范围与验证说明，本次合并结果在后续 PR 描述中补充。 |
| 风险与后续 | 主分支 F-02 属鉴权/安全变更，仍需非作者人工评审；MFA UI 场景尚未加入 Playwright；GitHub Actions 与 PR CI 尚未验证；生产还需提供 TOTP KEK 相关 Secret。 |
| 备注 | 本次推送只更新交付分支 `codex/c-design-visual-members`，不推送 `main`，不使用 force push；推送后由 PR 61 自动反映合并状态。 |

### 2026-09-09 13:35 +08:00 | C / Codex | codex/c-design-visual-members | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/c-design-visual-members`（基于已推送基线，未提交/推送） |
| PR / commit | 本机提交 `10c1e75`（Push 2，已推送） |
| 做了什么 | 完成全局命令面板、通知弹层与活动页的完整设计师视觉迁移：新增命令面板、活动中心/项目动态、自绘图标与活动页项目选择入口；通知弹层展示最近通知并支持目标直达；修复项目创建通知目标路径。 |
| 新增功能 | `Ctrl/Cmd+K` 打开全局命令面板，支持快速操作、生成客户端搜索、按类型分组、方向键/Enter/Esc；通知铃铛弹层展示未读数与最近通知、全部已读、点击直达目标；`/activity` 项目选择入口与 `/projects/:projectId/activity` 动态详情；侧栏启用联合品牌图片；新增 Playwright 视觉迁移用例与截图。 |
| 修复 / 重构 / 优化 | 修复命令面板键盘 Enter 执行后未关闭的问题；将结果分组从“快速操作/搜索结果”改为设计稿的“操作/搜索/实体类型”；项目创建通知 `targetPath` 从 `/projects/{id}` 修正为 `/projects/{id}/activity`，与实际前端路由一致；新增活动中心/遗留问题页面与路由并保留登录保护。 |
| 测试与验证 | 实际通过：`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm build`、`pnpm --filter @inpulse/web test:unit`（23 文件 52 例）、`pnpm --filter @inpulse/api test:unit`（39 文件 180 例）、`TEST_DATABASE_URL` 下真实 PostgreSQL API 集成 22 文件 89 例（含项目创建 4/4）、`pnpm test:e2e`（7/7）、`pnpm check:frontend:boundaries`（95 模块 329 依赖）、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`、`pnpm check:deps`、`pnpm check:secrets`、`pnpm check:deploy:test`；GitHub Actions 尚未执行。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`docs/test-matrix.md` 与本开发日志；未改 ADR、权限矩阵、Schema/Route Registry 或生成物，未新增依赖或迁移。 |
| 风险与后续 | 用户目录仍一次最多 300 条；其余业务关键路径 E2E、生产容器镜像、镜像 digest、镜像扫描与备份恢复仍待交付；本次为视觉/导航与通知跳转修复，不改变业务不变量。 |
| 备注 | 本次已推送至 `origin/codex/c-design-visual-members`，尚未创建 PR；待人工评审后创建 PR。 |

### 2026-09-09 12:25 +08:00 | C / Codex | codex/c-design-visual-members | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/c-design-visual-members`（基于 `main` `bc52fea`） |
| PR / commit | 本机提交 `37eb9c0`；PR 尚未创建 |
| 做了什么 | 完成设计师最新视觉迁移与创建项目成员选择接口：新增 `GET /api/v1/users` 用户目录，前端创建项目表单可加载 ACTIVE 用户并选择其他初始成员；按设计师最新参考迁移公共应用壳、项目页与创建弹窗视觉。 |
| 新增功能 | 新增用户目录契约、OpenAPI、生成客户端、Controller/Service/Repository；服务端只返回 `id/name/avatarUrl/isAdmin` 且响应 `no-store`，最多 300 条；创建表单排除创建者、去重排序并生成 `memberIds`；E2E 覆盖选择第二成员、创建后创建者与成员站内通知；公共壳改为深色侧栏、白色顶栏、面包屑和最新 token。 |
| 修复 / 重构 / 优化 | 未新增依赖或迁移；更新契约权限绑定测试，补齐 `GET /api/v1/users` 预期路由（初跑 `pnpm test:unit` 因此失败 1 例，修复后全量通过）；同步系统设计、技术设计中的接口清单与权限矩阵。 |
| 测试与验证 | 最终实际通过：`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 39 文件 180 例、Web 20 文件 48 例、api-contract 59 例、database 5 例）、`pnpm test:integration`（database 13 例、API 22 文件 89 例）、`pnpm db:migrations:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`、`pnpm build`、`pnpm test:e2e`（6/6）、`pnpm check:deploy:test`、`pnpm check:deps`、`pnpm check:frontend:boundaries`（85 模块 286 依赖）、`pnpm check:secrets`、`pnpm check:docs`；`pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 返回无已知漏洞。未整跑 `pnpm check`：本机 npm 镜像无 audit endpoint，`pnpm deps:audit` 仍会在审计步骤失败；公共 registry 审计已单独通过。GitHub Actions 尚未执行。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`系统设计文档v1.0.2.md`、`技术设计v1.2.2.md`、`docs/permissions.md`、`docs/test-matrix.md` 与本开发日志。 |
| 风险与后续 | 用户目录当前一次最多 300 条且不提供搜索/分页；全局命令面板、通知弹层与活动页的完整设计师视觉迁移仍属后续；E2E 运行时旧页面仍出现 AntD `Space direction`/`List` 弃用警告，本次未扩大范围处理；完成业务 Workflow 事件接入、其余关键路径 E2E、生产容器镜像与备份恢复后仍需基线冻结评审。 |
| 备注 | 本次只提交并推送到交付分支，不合并、不推送 `main`；推送后回填 PR/CI 结果，并请非作者人工评审。 |
### 2026-09-09 12:25 +08:00 | A / Codex | codex/a-f02-mfa-enrollment | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `ci` |
| 分支 | `codex/a-f02-mfa-enrollment`（基于 `dev/a` `8f8941a`；已提交 `origin/main` `bc52fea` 合并，合并提交 `7a84892`） |
| PR / commit | [#60](https://github.com/256-code/InPulse/pull/60)；本地合并提交 `7a84892`，短分支已推送；本轮合并包含 C 的 #54 Playwright 依赖、#56/#57 multer、#58 F-31 E2E 基座与 #59 F-04 项目创建前端 |
| 做了什么 | 完成 F-02.3 管理员密码 + 当前 TOTP 重认证、F-02.4 恢复码轮换与消费、F-02.5 另一名管理员在线重置目标管理员 MFA；同步 `origin/main` 后修复 E2E 的 TOTP KEK 注入、登录恢复码挑战路径和管理员重置 Controller/幂等/鉴权语义，并补齐权限矩阵与测试矩阵。 |
| 新增功能 | `POST /api/v1/auth/mfa/reauthenticate`、`POST /api/v1/auth/mfa/recovery-codes/rotate`、`POST /api/v1/auth/mfa/recovery-codes/consume`、`POST /api/v1/auth/admin/mfa-reset`；重认证原子刷新双时间戳并消费一次性 rotation generation；恢复码只保存带独立 salt 的 Argon2id 哈希；管理员重置与目标 Session 撤销、审计同事务完成。 |
| 修复 / 重构 / 优化 | 修复 `AdminHighRiskAuthService` 数据库时间戳解析；修正恢复码非管理员/未知用户权限语义；增加管理员互重置并发锁序与剩余 MFA 管理员保护；`AuthModule` 引入 `IdempotencyModule`；MFA 请求头 Schema 去掉 `.strict()`；E2E 配置自动生成并注入 TOTP KEK；更新登录恢复码挑战契约与前端字段。 |
| 测试与验证 | 实际执行通过：`pnpm format:check`；`pnpm check` 全绿（lint、format、typecheck、单元测试 database 5 例 / api-contract 4 文件 59 例 / web 19 文件 45 例 / api 46 文件 221 例、迁移检查、契约漂移/校验、build、部署 ref、依赖边界、前端边界、权限矩阵、依赖审计、Secret、文档）；`pnpm test:integration` database 13 例、API 26 文件 118 例；`db:test:local` 默认 55432 因现有实例占用失败，改用 `-Port 55433` 后完成角色/扩展初始化、6 条迁移、数据库单测 5 例与集成 13 例；`pnpm test:e2e` 6/6 通过。GitHub Actions 尚未执行。 |
| 文档同步 | `docs/permissions.md`（补 `resetAdminMfa` 业务矩阵）、`docs/test-matrix.md`（F-02.1～F-02.5 与最终计数）、`apps/e2e/README.md`（6 个冒烟用例与项目创建路径）、生成 OpenAPI/客户端与本日志。 |
| 风险与后续 | F-02 全部纵切片已在本地通过，但属于鉴权/安全变更，仍需非作者人工评审；GitHub Actions 与 PR CI 尚未验证；生产必须提供 `/run/secrets/totp_kek_keyring` 与 `TOTP_KEK_VERSION`；MFA UI 场景尚未加入 Playwright。 |
| 备注 | 已推送短分支并创建 PR #60；GitHub Actions 结果待确认。 |

### 2026-09-09 11:27 +08:00 | C / Codex | codex/c-f04-project-create-frontend | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feature` |
| 分支 | `codex/c-f04-project-create-frontend` |
| PR / commit | 本机提交 `e8ce720`，PR 尚未创建 |
| 做了什么 | 完成 F-04 项目创建前端纵切片：`/projects` 改为登录保护，提供 Ant Design + React Hook Form + Zod 项目创建表单，通过生成客户端先签发 CSRF Token，再携带 `x-csrf-token` 与 `Idempotency-Key` 调用 `createProject`，覆盖 401、403、409、422、429；创建后可打开项目动态并跳转全局搜索，并新增登录 → 创建项目 → 项目动态 → 搜索 → 站内通知的 Playwright 关键路径。 |
| 新增功能 | 新增 `features/projects` 表单、创建查询、弹窗与页面视图；新增项目页测试、控制器测试和项目创建 E2E；`/projects` 路由改为登录保护。 |
| 修复 / 重构 / 优化 | 修复 `ProjectBootstrapController` 把浏览器完整请求头误判为 422 的问题；抽出共享 `createIdempotencyKey`；审计 keyring 仅在 `NODE_ENV=test` 且显式开启临时路径时允许测试临时文件，生产仍只允许 `/run/secrets/*`；按人工审查意见将弹窗开启时重置副作用依赖收敛为 `[open]`。 |
| 测试与验证 | 实际执行并通过：`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（API 38 文件 177 例、Web 19 文件 45 例）、`pnpm test:integration`（database 13 例、API 21 文件 87 例）、`pnpm db:migrations:check`、`pnpm format:check`、`pnpm build`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm check:secrets`、`pnpm check:docs`、`pnpm check:deploy:test`；Playwright 完整套件 6/6 通过；人工审查后复跑 `pnpm lint`、`pnpm typecheck`、项目相关前端单测（2 文件 3 例）与完整 `pnpm test:e2e`（6/6）通过，并核对 E2E 的活动/通知断言文本分别对应 `project-bootstrap.workflow.ts` 的 `创建了项目` 与 `已加入项目`。`pnpm deps:audit` 与整体 `pnpm check` 在本地 npm 镜像缺少 audit endpoint 处失败；改用 `pnpm audit --registry=https://registry.npmjs.org` 通过并返回无已知漏洞，`pnpm check` 的其余步骤、`pnpm check:secrets` 与 `pnpm check:docs` 均通过。`pnpm db:migrate`、`pnpm db:test:local` 未在本 PR 复核，GitHub Actions 尚未执行。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`docs/test-matrix.md` 与本开发日志；未改技术设计、系统设计、功能设计或 ADR。 |
| 风险与后续 | 其余业务完整关键路径 E2E、生产容器镜像、镜像 digest、镜像扫描与备份恢复仍待交付；公共壳、全局命令面板、通知弹层与活动页视觉迁移按设计师最新版本属于后续批次，不在本 PR 内。 |
| 备注 | 本机提交 `e8ce720`；本次为 Push 1，尚未创建 PR，未直接推送 `main`。 |

### 2026-09-09 10:35 +08:00 | A / Codex | codex/a-f02-mfa-enrollment | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/a-f02-mfa-enrollment`（基于 `dev/a`，与 `origin/main` 同步起点 `8f8941a`） |
| PR / commit | 尚未创建 PR、尚未推送 |
| 做了什么 | 完成 F-02.1 管理员 TOTP 注册纵切片：受限注册与确认、AEAD Secret 存储、恢复码 Argon2id 哈希、Session/CSRF 轮换和 MFA 验证限流 |
| 新增功能 | `POST /api/v1/auth/mfa/enrollment/start` 与 `confirm`；管理员登录响应新增 `enrollmentGeneration`；首次注册仅可进入 `MFA_ENROLLMENT`，确认后显式进入 `AUTHENTICATED`；TOTP 使用 RFC 6238 SHA-1/30s/6 位/20 字节 Secret，密文使用版本化 AES-256-GCM KEK；恢复码 10 个 20 字符高熵码，数据库只存独立 salt 的 Argon2id 哈希 |
| 修复 / 重构 / 优化 | 增加 MFA 用户级 `FOR UPDATE` 排他锁，保证首因子创建与重新注册竞争返回 409；增加用户/IP/全局 MFA 验证限流，失败计数使用 HMAC 维度摘要；恢复码哈希在事务外计算，主状态变更保持单事务 |
| 测试与验证 | 本地通过：`pnpm check` 全绿（lint、format、typecheck、单测、迁移检查、契约漂移/校验、构建、部署引用、依赖边界、权限矩阵、依赖审计、Secret、文档）；TOTP/RFC 6238、AEAD keyring、恢复码与 Controller 共 16 例单测通过；完整 API `test:integration` 22 文件 92 例通过；`database/scripts/test-local.ps1` 在真实 PostgreSQL 18.6 + PGroonga 上执行角色/扩展初始化、6 条迁移、数据库单测 5 例与集成 13 例通过。尚未运行 Playwright E2E、GitHub Actions 或生产部署检查 |
| 文档同步 | `docs/permissions.md`、`docs/test-matrix.md`、`deploy/.env.deploy.example`、`deploy/.env.deploy.test`、`deploy/compose.yaml`、OpenAPI/生成客户端与本日志 |
| 风险与后续 | 生产必须提供 `/run/secrets/totp_kek_keyring` 与 `TOTP_KEK_VERSION`；`verifyMfa` 已由下一条 F-02.2 记录完成；`reauthenticateAdmin`、恢复码消费/轮换与管理员重置仍需继续；F-02.5 需要另一名已完成 MFA 的管理员执行；本次为鉴权/安全变更，仍需人工评审 |
| 备注 | 本地工作区未提交、未推送；推送前需回填 PR 与 CI 结果 |

### 2026-09-09 11:07 +08:00 | A / Codex | codex/a-f02-mfa-enrollment | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/a-f02-mfa-enrollment`（基于 `dev/a`，与 `origin/main` 同步起点 `8f8941a`） |
| PR / commit | 尚未创建 PR、尚未推送 |
| 做了什么 | 完成 F-02.2 管理员 TOTP 验证纵切片：`verifyMfa` 条件接受当前 time-step ±1、升级 MFA Challenge Session、刷新防重放 step 并轮换 CSRF |
| 新增功能 | `POST /api/v1/auth/mfa/verify`；请求 `{ code }` 与 `X-CSRF-Token`；成功返回 `{ csrfToken, authState: "AUTHENTICATED" }`；契约 Schema、Route Registry、权限矩阵、OpenAPI 与生成客户端同步 |
| 修复 / 重构 / 优化 | 新增共享 `MfaRateLimitError`，修正真实 MFA 限流异常在验证 Controller 被误映射为 500 的问题；按 user → factor → Session 锁序，错误验证码只在该次事务回滚后写入三层持久化限流 |
| 测试与验证 | 本地通过：API 42 文件 194 例单测；完整 API 集成 23 文件 98 例（F-02.2 真实 PostgreSQL 6 例）；`database/scripts/test-local.ps1` 在 PostgreSQL 18.6 + PGroonga 上执行 6 条迁移、数据库单测 5 例与集成 13 例；`pnpm check` 全绿。尚未运行 Playwright E2E、GitHub Actions 或生产部署检查 |
| 文档同步 | `docs/permissions.md`、`docs/test-matrix.md`、OpenAPI/生成客户端与本日志 |
| 风险与后续 | 限流阈值仍是候选基线，需人工安全评审锁定；生产仍要求 `/run/secrets/totp_kek_keyring` 与 `TOTP_KEK_VERSION`；后续为管理员重认证、恢复码消费/轮换和管理员重置；本次为鉴权/安全变更，仍需人工评审 |
| 备注 | 本地工作区未提交、未推送；推送前需回填 PR 与 CI 结果 |

### 2026-09-09 10:40 +08:00 | C / Codex | dev/c | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/c` |
| PR / commit | 本机提交 `70ba5d0`，日志随本推送提交 |
| 做了什么 | 修正文档状态漂移：明确 F-04 项目创建 Workflow 已接入活动、通知与搜索投影；任务完成、记录作废/恢复、合并等业务事件尚未接入。 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 文档修复：同步 README、AGENTS 与测试矩阵中关于活动/通知事件生成情况的过时描述；未改动代码、契约或数据库。 |
| 测试与验证 | 实际执行：`git diff --check` 通过；`pnpm exec prettier --check README.md AGENTS.md docs/test-matrix.md` 通过；`pnpm check:docs` 通过（51 个 Markdown 文件链接和锚点有效）。本次仅文档变更，未运行 typecheck、单测、集成测试、生成物漂移检查与完整 CI。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`docs/test-matrix.md` 与开发日志。 |
| 风险与后续 | GitHub Actions 尚未在本次推送后执行；任务完成、记录作废/恢复、合并等业务 Workflow 仍待接入活动/通知写端口并补充事务集成测试。 |
| 备注 | 本次为 dev/c 文档同步推送；未直接推送 `main`，未创建 PR。 |

### 2026-09-09 10:30 +08:00 | C / Codex | codex/c-playwright-e2e | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `ci` / `docs` |
| 分支 | `codex/c-playwright-e2e`（基于 `codex/c-playwright-dependency`，已合并 `origin/main` `c3cda82`） |
| PR / commit | 待创建 PR；本机提交 `d217973`、`cd8089e`、`4a8b61d`、`f5b93fc`、`70d2373`、`c59a20e`、`9ee5d00`、`99705be`、`c196f0a`、`5170627` 及本轮日志同步 |
| 做了什么 | 新增 Playwright 浏览器 E2E 基座：API/Vite `webServer`、global setup/teardown、真实 UI 登录辅助、API 启动探针/匿名保护页/全局搜索/站内通知/API Session 5 个用例、根级 `pnpm test:e2e`、Vite 代理与 `CI / workspace` 步骤；本轮同步 #55/#56/#57 后的 `main` 并合并依赖分支。 |
| 新增功能 | 提供可复现的浏览器 E2E 基座与 CI 报告 artifact；完整业务关键路径仍待后续模块交付。 |
| 修复 / 重构 / 优化 | 保持 `workers=1`、失败截图/trace/video、CI 自动安装 Chromium 与上传报告；未改动生产 API/数据库。 |
| 测试与验证 | 本轮合并最新 `main` 与依赖分支后实际执行通过：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`、`pnpm db:migrations:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm build`、`pnpm check:deploy:test`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm check:secrets`、`pnpm check:docs`、公共 registry `pnpm audit --audit-level=high`；Docker PGroonga 18 实例下 `pnpm test:integration` 通过（database 13 例、API 21 文件 87 例）；`pnpm test:e2e` 5/5 通过（9.0s，含 API 启动探针、匿名保护页、登录复用 Session、通知页与全局搜索）。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`CONTRIBUTING.md`、`docs/test-matrix.md`、`apps/e2e/README.md`、`apps/e2e` 配置与 CI。 |
| 风险与后续 | Playwright 完整关键路径仍未覆盖；E2E 新增需非作者人工评审；CI 首次真实执行结果待确认。 |
| 备注 | 未直接推送或合并 `main`；本轮 merge 与本地验证完成，待推送并创建 PR。 |

### 2026-09-09 10:20 +08:00 | C / Codex | codex/c-playwright-dependency | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` / `deps` |
| 分支 | `codex/c-playwright-dependency`（同步 `origin/main` `1905af6`，已包含 #55/#56/#57） |
| PR / commit | [#54](https://github.com/256-code/InPulse/pull/54)；本机提交 `ee8a37d`、`e9ea243`、`ddf338a`、`f7567b3`、`09aa90d` 及本轮日志同步 |
| 做了什么 | 按仓库依赖规则把 Playwright 测试依赖拆成独立分支：新增 `apps/e2e` workspace 包，声明精确版本 `@playwright/test@1.63.0`，并同步 `pnpm-lock.yaml`；本轮再合并 #55/#56/#57 后的 `main`，解决开发日志冲突。 |
| 新增功能 | 无业务功能；为后续 Playwright browser E2E 基座提供可复现的 dev 依赖。 |
| 修复 / 重构 / 优化 | 将原先混在功能分支中的依赖变更拆出，独立提交便于人工评审；未手工编辑 lockfile。 |
| 测试与验证 | 本轮合并 `origin/main` `1905af6` 后实际执行通过：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`（database 5 例、api-contract 4 文件 59 例、web 16 文件 40 例、api 37 文件 174 例）、`pnpm db:migrations:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm build`、`pnpm check:deploy:test`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm check:secrets`、`pnpm check:docs`；公共 registry `pnpm audit --audit-level=high` 返回 `No known vulnerabilities found`；Docker PGroonga 18 实例下 `pnpm test:integration` 通过（database 13 例、API 21 文件 87 例）。 |
| 文档同步 | 同步 `开发日志.md`，保留 #55/#56/#57 已合入记录。 |
| 风险与后续 | 按 AGENTS.md 第 4 节，该依赖变更需独立 PR 与非作者人工确认；功能分支基于本提交，推送顺序应为依赖分支先、功能分支后。 |
| 备注 | PR #54 等待远端 CI；未直接推送或合并 `main`。 |

### 2026-09-09 09:41 +08:00 | C / Codex | codex/c-multer-upgrade | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `ci` / `docs` |
| 分支 | `codex/c-multer-upgrade`（基于 `origin/main` `8f8941a`，已同步 A #56 合入后的 `29da85c` 与 A #55 合入后的部署预检，独立于 Playwright 分支） |
| PR / commit | [#57](https://github.com/256-code/InPulse/pull/57)；本机提交 `2e19990`、`253e4da`、`1bab35d`、`f365eb6`、`701a423`、本轮 merge `origin/main` 及日志同步 |
| 做了什么 | 在 A #56 已合入的 `multer: "^2.3.0"` 基础上，把 `pnpm-workspace.yaml` 收紧为精确版本 `multer: "2.3.0"`，同步 lockfile，并补齐 AGENTS、CONTRIBUTING、测试矩阵与开发日志；再同步 A #55 的 Compose/ref 预检与文档状态，不夹带 Playwright、API 或数据库改动。 |
| 新增功能 | 无用户可感知功能。 |
| 修复 / 重构 / 优化 | `multer@2.2.0` 的 3 个 high（GHSA-wc9g-mqfw-jrwm、GHSA-qfvm-cv95-jqjf、GHSA-535w-7cp7-47q4）已通过 `@nestjs/platform-express@11.2.3 -> multer@2.3.0` 消除；`pnpm why multer` 仅剩 `2.3.0`；将范围覆盖改为精确版本，满足依赖锁定要求；另外在 `apps/web` 测试 setup 清理每个用例遗留的 Ant Design `useDelayState` timer，修复 CI 中 jsdom teardown 后 `window is not defined` 的未处理异常。 |
| 测试与验证 | 实际执行通过：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`（api 37 文件 174 例、database 5 例、api-contract 59 例、web 40 例）、`pnpm db:migrations:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm build`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm check:secrets`、`pnpm check:docs`；`pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 返回 `No known vulnerabilities found`。Docker 探针镜像 `inpulse/pgroonga-pg18.6:repro`（`127.0.0.1:55432`，PGroonga 18.6，`max_connections=150`）下三次 `pnpm test:integration` 全量通过（database 13 例、API 21 文件 87 例，最后一次在同步 `origin/main` 后复跑）；首次全量曾在 activity-notifications 幂等用例出现一次 500，单独重跑通过，未发现与 `multer@2.3.0` 相关的复现证据。`pnpm check` 仍仅因本机 npm 镜像无 audit endpoint 在 `deps:audit` 中断，公共 registry 审计已单独通过；timer 清理后 `apps/web` 连续 3 次 40 例通过，远程 CI 正在重新验证。 |
| 文档同步 | 更新 `AGENTS.md`（阶段 0 依赖漏洞修复状态）、`CONTRIBUTING.md`（`deps:audit`）、`docs/test-matrix.md`（CI-014）、`apps/web/src/test/setup.ts`/`setup.test.ts` 与本开发日志。 |
| 风险与后续 | 这是独立依赖修复 PR，须由非作者人工确认；不得通过调低 `--audit-level`、添加 allowlist 或删除门禁规避；A #56 已先合入 `^2.3.0` 修复主线，本 PR 追加精确版本、文档同步和 Web 测试 timer 基线修复；Web timer 修复属于 CI 稳定性配套，未改变生产行为。 |
| 备注 | 已创建 PR #57；未直接推送或合并 `main`；首次全量集成测试的单次 500 已如实记录，未使用 skip 或降低断言。 |

### 2026-09-09 09:50 +08:00 | A / Codex | codex/a-multer-security-upgrade | 已合入

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix(deps)` |
| 分支 | `codex/a-multer-security-upgrade`（基于 `origin/main` `a2e1084`） |
| PR / commit | [#56](https://github.com/256-code/InPulse/pull/56)，已 squash 合入 `main`（`29da85c`）；功能提交 `db0445d`，日志提交 `811208f` |
| 做了什么 | 在 `pnpm-workspace.yaml` 增加 `multer: "^2.3.0"` override，更新 `pnpm-lock.yaml`，消除 NestJS 11.2.3 经 `@nestjs/platform-express` 间接引入的 `multer@2.2.0` 高危 DoS 漏洞 |
| 新增功能 | 无；仅依赖锁定 |
| 修复 / 重构 / 优化 | 修复 `GHSA-wc9g-mqfw-jrwm`、`GHSA-qfvm-cv95-jqjf`、`GHSA-535w-7cp7-47q4`；不修改 NestJS 版本、API 代码或运行时配置 |
| 测试与验证 | 本地通过：`pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`（API 174、web 39 等）、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm build`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm deps:audit`（0 high/critical）、`pnpm check:secrets`、`pnpm check:docs`。GitHub Actions：`CI / workspace` 最终 run `34300425273` 全部通过（含真实 PostgreSQL 集成，2m44s；此前一次因既有 LoginForm 定时器偶发失败，重跑通过），`Documentation` 通过 |
| 文档同步 | 更新本日志 |
| 风险与后续 | 该 override 属于生产间接依赖升级，已按用户授权合入 `main`；未修改 NestJS 版本或 API 代码 |
| 备注 | 已合入 `main`，下一步同步回 `dev/a` 并让 #55 重新全绿 |

### 2026-09-09 10:00 +08:00 | A / Codex | codex/a-deploy-compose-preflight | Merge #56

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | `codex/a-deploy-compose-preflight` |
| PR / commit | [#55](https://github.com/256-code/InPulse/pull/55)；合并 `origin/main` `29da85c`（#56） |
| 做了什么 | 将 #56 合入后的 `main` 合并到本分支，解析 `开发日志.md` 冲突并保留 #55、#56 两条记录；本 PR 增加 `multer@2.3.0` 依赖基线，解除 `Dependency audit` 阻塞 |
| 新增功能 | 无新增功能 |
| 修复 / 重构 / 优化 | 无代码改动；仅同步依赖与日志 |
| 测试与验证 | 本地 `pnpm deps:audit`（0 high/critical）、`pnpm check:deps`、`pnpm check:docs`、`pnpm check:deploy:test` 与 `git diff --check` 通过；GitHub Actions run `34300892449` 全部通过，包含 Compose 正/负例、真实 PostgreSQL 集成与 `Dependency audit`（workspace 3m16s）；`Documentation` run `34300892471` 通过 |
| 文档同步 | 合并 `开发日志.md` |
| 风险与后续 | 按用户授权在 CI 全绿后合入，并同步 `dev/a`；`deploy/compose.yaml` 仍不能直接 `up` |
| 备注 | CI 全绿；准备 squash 合入 `main` |

### 2026-09-09 09:30 +08:00 | A / Codex | codex/a-deploy-compose-preflight | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | `codex/a-deploy-compose-preflight`（基于 `dev/a` `b8bce16`） |
| PR / commit | [#55](https://github.com/256-code/InPulse/pull/55)，已推送 `eb80894` |
| 做了什么 | 把 `dev/a` 上挂起的 deploy/compose WIP 收敛为阶段 0 结构预检：移除 `backup`/`audit-archive` 及 `OPS_IMAGE_REF`，保留当前代码实际读取的 db/API 与 Web Secret 拓扑；把 compose 顶部过时健康端点注释改为已由 #52 实现的 `/health/live`、`/health/ready` 契约；新增 `deploy/.env.deploy.test` 合成 ref 正例，使 `pnpm check:deploy:test` 校验 compose 渲染、exact-tag@sha256、PostgreSQL 命名卷、非 root/只读/资源限制/健康检查/端口等结构，并保留 `.env.deploy.example` 占位符被拒绝的 CI 负例 |
| 新增功能 | 新增 `pnpm check:deploy:test` 并纳入 `pnpm check`；`check_deploy_refs.mjs` 增加 API/Web healthcheck 与 migrate `restart: no` 结构断言；CI 增加“占位符拒绝”独立负例步骤 |
| 修复 / 重构 / 优化 | 清理 WIP 中的未交付运维服务与未使用 Secret；消除 AGENTS/README/CONTRIBUTING/test-matrix 与 CI 关于“尚无 compose.yaml”的过时描述；本次不交付 production Dockerfile、真实镜像 digest 或 backup/restore |
| 测试与验证 | 本地实际通过：`pnpm check:deploy:test`（4 个合成 ref、compose config 渲染、结构不变量 OK）、`.env.deploy.example` 负例拒绝、`pnpm lint`、`pnpm format:check`、`pnpm typecheck`、`pnpm test:unit`、`pnpm db:migrations:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm build`、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm check:secrets`、`pnpm check:docs`。GitHub Actions：`Documentation` run `34299327153` 通过；`CI / workspace` run `34299327164` 经 `--failed` 重跑后第 1～24 步通过（含真实 PostgreSQL 集成），第 25 步 `Dependency audit` 因当前基线 `multer` 3 个 high（GHSA-wc9g-mqfw-jrwm、GHSA-qfvm-cv95-jqjf、GHSA-535w-7cp7-47q4）失败；该 run 尚未包含 `check:deploy:test` 正例步骤，本轮未修改 `pnpm-lock.yaml`。未运行：生产容器镜像构建/扫描 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`CONTRIBUTING.md`、`docs/test-matrix.md`、本日志；同步 `.gitignore` 允许提交 `deploy/.env.deploy.test` 测试夹具 |
| 风险与后续 | 本 PR 只交付 Compose/ref 预检，`deploy/compose.yaml` 仍不能直接 up；Nginx 生产配置、生产 Dockerfile、真实 digest、Secret `0400`/用户归属仍待 F-10.1/F-10.2；`multer` high 依赖问题必须另建独立依赖 PR 并由人工确认 |
| 备注 | 已推送并创建 PR；请求非作者对 workflow、Compose 和 Secret 相关声明进行人工评审 |

### 2026-09-09 09:45 +08:00 | A / Codex | codex/a-deploy-compose-preflight | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | `codex/a-deploy-compose-preflight` |
| PR / commit | [#55](https://github.com/256-code/InPulse/pull/55)；本 PR 功能提交 `13c8f6b` |
| 做了什么 | 补齐 `CI / workspace` 中“Compose/ref 正例”实际执行步骤：`Build` 后运行 `pnpm check:deploy:test`；保留独立负例步骤，使 CI 不再只依赖本地 `pnpm check` 隐式执行 |
| 新增功能 | `.github/workflows/ci.yml` 新增 `Deploy compose and image ref positive preflight` |
| 修复 / 重构 / 优化 | 同步 workflow 顶部 §12.4 注释与正例步骤；未改变 Compose 内容、镜像 ref 或依赖 |
| 测试与验证 | 本地 `pnpm check:deploy:test` 再次通过（4 个合成 ref、compose config 渲染、结构不变量 OK）；GitHub Actions run `34299960441` 中 `Deploy compose and image ref positive preflight` 与 `Deploy image ref negative preflight` 均通过，第 1～24 步全部成功，仅第 25 步 `Dependency audit` 因基线 `multer` 失败 |
| 文档同步 | 更新本日志 |
| 风险与后续 | CI 仍会因基线 `multer` high 在 `Dependency audit` 失败，已另建 [#56](https://github.com/256-code/InPulse/pull/56) 独立依赖 PR；#56 合入并同步到本分支后本 PR 才可全绿；`deploy/compose.yaml` 仍不能直接 `up` |
| 备注 | 本次推送前已更新日志；等待非作者人工评审，不自动合入 |

### 2026-09-08 23:30 +08:00 | A / Codex | codex/a-f04-project-create | PR #53

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/a-f04-project-create`（基于 `origin/main`） |
| PR / commit | [#53](https://github.com/256-code/InPulse/pull/53)，已 squash 合入 `main`（`a2e1084`） |
| 做了什么 | 实现 F-04 项目创建闭环：契约（Schema/Route Registry/权限矩阵）、单事务 Workflow、写端口、Controller、项目编码派生与模块装配；修复幂等指纹按路由实际引用 schema 计算（避免全局 schema 污染既有路由） |
| 新增功能 | `POST /api/v1/projects`：任一启用用户创建项目，创建者自动成为活跃成员且不可取消，可选初始成员；服务端在单事务内建未分类模块并写审计、搜索/活动投影与通知；项目编码由名称派生或显式 `code` 覆盖；成功返回 200 |
| 修复 / 重构 / 优化 | 幂等契约指纹改为路由级 schema 引用集合；`activity`/`notifications` 新增 `index.ts` 公共出口以满足依赖边界；项目编码派生失败映射为 422 而非 500；日期字段以 `new Date(...).toISOString()` 兼容 postgres-js 返回值 |
| 测试与验证 | PR #53 `CI/workspace` 与 `Documentation` 均通过（workspace 3m8s）；此前本地 `pnpm check`、`pnpm test:integration` 21 文件 87 例与 `PROJ-CREATE-001～005` 均通过 |
| 文档同步 | 更新 `docs/permissions.md`、`docs/test-matrix.md`、OpenAPI、生成客户端与本日志 |
| 风险与后续 | 依赖升级、历史迁移未改动；完成后同步回 `dev/a`；既有前端 flaky `window is not defined` 已记录 |
| 备注 | 已合并到 `main`，本次同步回 `dev/a` |

### 2026-09-08 23:30 +08:00 | A / Codex | dev/a | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 已推送 `origin/dev/a`，无 PR；本机提交 `f8eb975` |
| 做了什么 | 在 `dev/a` 实现 F-04 项目创建闭环：契约（Schema/Route Registry/权限矩阵）、单事务 Workflow、写端口、Controller、项目编码派生与模块装配；修复幂等指纹按路由实际引用 schema 计算（避免全局 schema 污染既有路由） |
| 新增功能 | `POST /api/v1/projects`：任一启用用户创建项目，创建者自动成为活跃成员且不可取消，可选初始成员；服务端在单事务内建未分类模块并写审计、搜索/活动投影与通知；项目编码由名称派生或显式 `code` 覆盖；成功返回 200 |
| 修复 / 重构 / 优化 | 幂等契约指纹改为路由级 schema 引用集合；`activity`/`notifications` 新增 `index.ts` 公共出口以满足依赖边界；项目编码派生失败映射为 422 而非 500；日期字段以 `new Date(...).toISOString()` 兼容 postgres-js 返回值 |
| 测试与验证 | 全量 `pnpm check` 通过：lint、format:check、typecheck、根级 `test:unit`（api 37 文件 174 例、database 5 文件、api-contract 59 例、web 39 例）、`db:migrations:check`、`contract:drift`、`contract:validate`、`build`、`check:deps`（228 文件）、`check:frontend:boundaries`、`permissions:check`、`deps:audit`（公共 registry 零漏洞）、`check:secrets`、`check:docs`；真实 PostgreSQL 18.6 + PGroonga 下 `pnpm test:integration` 21 文件 87 例全通过（含新增 `project-bootstrap.integration.test.ts` 4 例与 `project-code.test.ts` 7 例）。未运行 `db:test`；GitHub Actions 当前分支已通过，功能提交 `f8eb975` 全绿（workspace 2m45s），日志提交 `f220dd7` 首次因既有前端单测偶发 `window is not defined` 失败、重跑后全绿（workspace 3m18s） |
| 文档同步 | 更新 `docs/permissions.md`（`createProject` 行细化）、`docs/test-matrix.md`（新增 PROJ-CREATE-001～005） |
| 风险与后续 | 依赖升级、历史迁移未改动；发现既有 `apps/web` 前端单测偶发未处理错误（`LoginForm.test.tsx` 周边 ReactDOM timer），非 F-04 改动引入，建议后续单独修复；当前分支 CI 已绿，仍待人工评审后进入 `main` |
| 备注 | 已推送到 `origin/dev/a`，未创建 PR；当前远端带头功能提交与日志提交均通过 CI |

### 2026-09-08 22:26 +08:00 | A / Codex | dev/a | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `dev/a` |
| PR / commit | 合并提交（`dev/a` merge `origin/main`）；[#52](https://github.com/256-code/InPulse/pull/52) 已合入 `main` |
| 做了什么 | 把 `origin/main`（含 PR #52 健康端点、PR #51 活动/通知）合并回 `dev/a`；解决 `app.module.ts` 投影模块、`permissions.test.ts` 路由并集、生成客户端与开发日志的冲突。 |
| 新增功能 | 无新业务；将健康端点与活动/通知切片同步进入 `dev/a`。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 实际执行并通过：`pnpm contract:generate`（5 个产物）、`pnpm contract:validate`（14 条路由）、`pnpm permissions:check`（14 条操作 / 14 条路由）、`pnpm check`（lint → format:check → typecheck → test:unit → db:migrations:check → contract:drift → contract:validate → build → check:deps → check:frontend:boundaries → permissions:check → deps:audit → check:secrets → check:docs，0 失败）；真实 PostgreSQL 18.6 + PGroonga 本地集成：健康 3 例、API 集成 20 文件 / 83 例、database 13 例。 |
| 文档同步 | 本日志。 |
| 风险与后续 | 无新增风险；健康端点的“迁移版本精确比对”仍待人工定案。 |
| 备注 | `main` 由 PR #52 squash 合入，未重写；本次仅同步回 `dev/a`。 |

### 2026-09-08 21:12 +08:00 | A / Codex | dev/a | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | `dev/a` |
| PR / commit | 本地提交（待推送）；本次先合并 `origin/main` PR #50（搜索投影写端口）到 `dev/a`，再叠加本部署切片 |
| 做了什么 | 同步 `origin/main` 到 PR #50（搜索投影写端口），并落地生产部署纵切片第一步：新增 `deploy/compose.yaml`（技术设计 §11.2 稳态拓扑）、镜像 ref 预检脚本 `scripts/check_deploy_refs.mjs`、根脚本 `check:deploy`，并在 `.github/workflows/ci.yml` 增加 compose 渲染 + 占位符拒绝门禁 |
| 新增功能 | `check:deploy` 校验所有 `*_IMAGE_REF` 必须为 `repository:tag@sha256:<64hex>`，拒绝空值、占位符与 `latest/next/beta/rc/dev/local`；对 `docker compose config` 渲染结果校验结构不变量（db 挂载 `/var/lib/postgresql`、`read_only`、`cap_drop: ALL`、migrate 一次性、api 等待 migrate `service_completed_successfully`、web 仅暴露 80/443） |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | 实际执行通过：`npx prettier --write scripts/check_deploy_refs.mjs package.json deploy/compose.yaml` 且 `--check` 通过；`pnpm lint`（0 错误）；`pnpm format:check`；`npx eslint scripts/check_deploy_refs.mjs`（0 错误）；`docker compose --project-directory deploy -f deploy/compose.yaml --env-file deploy/.env.deploy.example config --quiet`（exit 0）；预检负例（示例 env 含 `REPLACE_ME` 占位符，exit 1 拒绝）；预检正例（临时格式合法 ref，结构 OK，exit 0，临时文件已删除）；`pnpm check:docs`（49 个 Markdown 链接与锚点有效）；`pnpm check:secrets`（340 个受控文件，对 compose `/run/secrets/*` 引用与示例 env 空版本号放行）。**未运行**：`pnpm check`（全量，含 deps:audit）、GitHub Actions 真实执行、生产容器镜像构建、`/health/live` 与 `/health/ready`（见风险与后续） |
| 文档同步 | 新增 `deploy/.env.deploy.example`；更新 `AGENTS.md` §8/§13 中“仓库尚无 compose.yaml”表述；`.gitignore` 新增 `/deploy/secrets/` 与忽略 `deploy/.env.deploy` |
| 风险与后续 | (1) 生产 Dockerfile 与真实镜像 digest 未定案，`check:deploy` 是发布期 gate，因此未加入 `pnpm check`；(2) 技术设计要求 `/health/live`、`/health/ready`，代码目前只有 `GET /health`，所以 `api` 的 `node healthcheck.mjs` 与 `web` 的 `/health/live` 依赖健康端点契约，待人工定案后再实现；(3) 设计 `DB_PASSWORD_FILE` 与代码 `MIGRATION_DB_PASSWORD_FILE` / `RUNTIME_DB_PASSWORD_FILE` 命名不一致，本 compose 按当前代码实际名称落地；(4) `backup` / `audit-archive` 依赖尚未实现的 `ops:backup` / `ops:audit-archive`，当前只能通过 `config` 渲染校验；(5) 代码对 `/run/secrets/*` 要求仅属主可读（`0o400`、无 group/other 位），Compose 默认 secret 权限为 `0444` 且属主为 root，与代码 fail-closed 检查可能不符，需在镜像与 secret 挂载细节定案时补齐 `mode: 0400` 与容器用户归属 |
| 备注 | 本次推送到 `origin/dev/a`（未创建 PR，仅岗位分支）；compose 中设计-代码冲突均以注释标注，未静默选择 |


### 2026-09-08 21:30 +08:00 | A / Codex | dev/a | 健康端点契约

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 本地提交（待推送）；叠加于 `e4a068f`（deploy compose）之上 |
| 做了什么 | 实现健康端点契约 `/health/live` 与 `/health/ready`，并把就绪数据库检查从 Controller 移入 `HealthService`。新增 `HealthModule`，`AppModule` 改用模块引入。 |
| 新增功能 | `GET /health/live` 返回 200 `{"status":"ok"}` 且不访问数据库；`GET /health/ready` 验证数据库可连接且 `app.schema_migrations` 存在记录，成功返回 200，失败返回 503 统一错误体 `{code:"SERVICE_NOT_READY",message,details,requestId}`。Route Registry 8 条、权限矩阵、OpenAPI 与生成客户端同步。 |
| 修复 / 重构 / 优化 | 移除 Controller 对 `DATABASE_CLIENT` 的直接注入与 SQL，改为依赖 `HealthService`，满足 AGENTS.md 第 3 节“Controller 不得直连数据库层”；新增 `HealthService` 承担只读就绪探测。 |
| 测试与验证 | 实际执行通过 `pnpm check`（lint → format:check → typecheck → test:unit → db:migrations:check → contract:drift → contract:validate → build → check:deps → check:frontend:boundaries → permissions:check → deps:audit → check:secrets → check:docs，0 失败）。其中 `pnpm test:unit` 覆盖 database 5 例、api-contract 58 例、apps/web 32 例、apps/api 33 文件 / 155 例；`check:docs` 校验 50 个 Markdown；`check:secrets` 352 个受控文件；构建为 api tsc + web vite。另单独确认 `contract:generate`（5 个产物）。**真实 PostgreSQL 18.6 + PGroonga 本地集成验证通过**：用 `D:\PostgreSQL\18\pgsql\bin` 临时 initdb 集群，应用 `000_roles.sql` / `020_pgroonga.sql` 与全量 `0000-0005` 迁移（6 applied），健康探针 `health.integration.test.ts` 3 例通过，API 集成 15 文件 / 63 例通过，database 集成 13 例通过。GitHub Actions 真实执行仍待推送后回填。 |
| 文档同步 | `AGENTS.md` §5 状态码新增 `503 服务未就绪（就绪探针未通过）`；`docs/test-matrix.md` 新增“健康探针” OPS-001～003；本日志。 |
| 风险与后续 | (1) 已补充 `/health/ready` 失败路径 HTTP 集成用例（`health-readiness-failure.integration.test.ts`，2 例，用不可达 DB URL 验证 live 200 / ready 503、统一错误体且不泄露内部细节），本地真实与无 DB 场景均通过；(2) 技术设计就绪检查“迁移版本正确”目前以 `schema_migrations` 存在记录近似，完整版本号比对待迁移版本常量定案；(3) GitHub Actions 真实执行已回填：`bc5accd` → run `34232681130` success、`501e805` → run `34233247372` success、`8d4c1ed` → run `34234424471`（run #201）success，`CI / workspace` 全步骤通过（含“Integration tests against real PostgreSQL”）。 |
| 备注 | 本次推送到 `origin/dev/a`（未创建 PR，仅岗位分支）；未改动历史迁移、权限矩阵与生成物之外的源码契约。 |

### 2026-09-08 21:56 +08:00 | C / Codex | codex/c-activity-notifications | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-activity-notifications`（Push 1 commit `5303094` 已推送） |
| PR / commit | 尚未创建 PR；Push 1 `5303094` 已推送 |
| 做了什么 | 回填首次推送的 commit 与远端状态；无业务代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 本次仅修改开发日志；未重跑应用门禁。Push 1 的完整本地验证结果见上一条。 |
| 文档同步 | 仅更新本开发日志。 |
| 风险与后续 | 等待 GitHub Actions 与非作者人工评审；项目创建、任务完成、记录作废/恢复等 Workflow 尚未接入活动/通知写端口。 |
| 备注 | 已推送 `origin/codex/c-activity-notifications`，未直接推送或合并 `main`。 |

### 2026-09-08 21:51 +08:00 | C / Codex | codex/c-activity-notifications | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-activity-notifications`（基于当前 `dev/c` 继承的合并基线，未推送） |
| PR / commit | 尚未创建 PR；尚无 commit |
| 做了什么 | 完成 F-27 项目动态与 F-28 站内通知的 API 读写、真实 PostgreSQL 集成、契约登记和前端页面纵切片。 |
| 新增功能 | 新增 `GET /projects/{projectId}/activity`，通过 `ProjectAccessQueryPort` 取得服务端 `AuthorizedProjectScope`，支持 `MEMBER`/`ADMIN_ONLY` 可见性、签名游标分页和脱敏白名单响应；新增通知列表、未读数、单条已读/未读和全部已读路由，收件人始终来自当前 Session，写操作要求 CSRF 与 `Idempotency-Key`；新增 `ActivityWritePort`、`NotificationWritePort` 及其 PostgreSQL 适配器，显式接收 `TransactionContext` 并做来源事件去重；前端新增 `features/activity`、`features/notifications`、`/projects/:projectId/activity` 与 `/notifications` 页面，AppLayout 接入真实通知铃铛与未读数。 |
| 修复 / 重构 / 优化 | 修复认证 Session 数据库时间字段未转为 `Date` 导致 CSRF 签发失败的问题；幂等执行器改为在同一事务内解析认证与业务写入；通知/活动 path 参数使用 `z.coerce.number()`；修复 `hasMore`/`last` 分页语义；新增可绑定用户、命名空间和项目的签名游标。 |
| 测试与验证 | 本地实际执行并通过：`pnpm lint`；`pnpm format:check`；`pnpm --filter @inpulse/api typecheck`；`pnpm --filter @inpulse/api test:unit`（34 文件 160 例）；`$env:TEST_DATABASE_URL=postgresql://cluster_bootstrap@127.0.0.1:55432/app` 后 `pnpm --filter @inpulse/api test:integration`（18 文件 78 例，真实 PostgreSQL 18.6 + PGroonga）；`pnpm --filter @inpulse/web typecheck`；`pnpm --filter @inpulse/web test`（16 文件 39 例）；`pnpm --filter @inpulse/web build`；`pnpm check:frontend:boundaries`；`pnpm contract:generate`、`contract:drift`、`contract:validate`；`pnpm --filter @inpulse/api-contract test`（4 文件 59 例）；`pnpm permissions:check`；`pnpm check:docs`（50 个 Markdown）；`pnpm check:secrets`（382 个文件）。`pnpm check` 除 `deps:audit` 因当前 npm 镜像无 audit endpoint 失败外其余步骤通过；改用公共 registry 执行 `pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 无已知漏洞。GitHub Actions、Playwright E2E 未执行。 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`docs/permissions.md`、`docs/test-matrix.md`、`docs/frontend-generated-client-consumption-requirements.md` 与本日志；未修改三份设计文档，设计中的已有活动/通知语义与本次实现一致。 |
| 风险与后续 | 项目创建、任务完成、记录作废/恢复等业务 Workflow **尚未**调用新的活动/通知写端口，当前没有生产侧事件生成；后续接入时仍须按事务、审计、投影与幂等规则在同一 `TransactionContext` 中接线，并补充对应 Workflow 集成测试。 |
| 备注 | 尚未推送、未创建 PR；生成客户端/OpenAPI/指纹均由 `contract:generate` 更新。 |

### 2026-09-08 20:47 +08:00 | C / Codex | codex/c-search-projection-write | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-projection-write`（基于 `dev/c` `b6ffc7f` 创建；Push 1 `8955802` 已推送） |
| PR / commit | [#50](https://github.com/256-code/InPulse/pull/50)；Push 1 `8955802` 已推送 |
| 做了什么 | 回填 PR #50 与 Push 1 commit；无业务代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 将日志中的“尚未创建 PR / commit 待回填”更新为已创建 PR #50 和 commit `8955802`。 |
| 测试与验证 | 本次仅修改日志；Push 1 已记录本地门禁与公共 registry 审计结果，未重跑数据库集成测试；`pnpm check:docs` 与 `pnpm check:secrets` 已重新通过。GitHub Actions 尚未确认。 |
| 文档同步 | 仅更新本开发日志。 |
| 风险与后续 | 等待 GitHub Actions 与非作者人工评审；写端口尚未接入业务 Workflow，F-04/F-19/F-21 接线归属对应岗位。 |
| 备注 | 用于日志校正的追加推送；已推送 `origin/codex/c-search-projection-write`，未直接推送或合并 `main`。 |

### 2026-09-08 20:46 +08:00 | C / Codex | codex/c-search-projection-write | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-search-projection-write`（基于 `dev/c` `b6ffc7f` 创建；Push 1 待推送） |
| PR / commit | 尚未创建 PR；Push 1 commit 待回填 |
| 做了什么 | 完成 F-26 的 `SearchProjectionWritePort` 公共写端口及其 PostgreSQL 适配器、Nest 模块与真实数据库集成测试。 |
| 新增功能 | 新增 `SearchProjectionWritePort.upsert(tx, input)`：显式接收同一个 `TransactionContext`，校验输入、调用 `normalizeSearchText` 后按 `(project_id, entity_type, entity_id)` upsert；新增 `SearchProjectionModule` 供业务 Workflow 注入，并导出实体类型与可见性类型。 |
| 修复 / 重构 / 优化 | 投影写入不接收全局 Drizzle Client；`source_row_version` 较旧时不覆盖新投影；空规范化文本在写库前拒绝；`search-projection.reader.ts` 复用统一 `SearchProjectionVisibilityScope`。 |
| 测试与验证 | 本地实际执行：`pnpm --filter @inpulse/api test:unit` 31 文件 148 例通过；`pnpm --filter @inpulse/api test:integration` 14 文件 60 例通过（含新写端口 4 例与模块注入 1 例）；workspace typecheck、lint、format、迁移/契约检查、构建、依赖边界、前端边界、权限矩阵、Secret 与文档检查通过；`pnpm check` 在 `deps:audit` 前全部通过，但本机 `registry.npmmirror.com` 无 audit endpoint，改用 `pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 为无已知漏洞。GitHub Actions 尚未执行。 |
| 文档同步 | 更新 `AGENTS.md` 状态、`docs/permissions.md`、`docs/test-matrix.md`，新增 `apps/api/src/modules/search/README.md`，并同步本日志。 |
| 风险与后续 | 写端口尚未接入具体业务 Workflow；F-04、F-19、F-21 的接线属于对应岗位功能范围，后续由 A/B 在各自 Workflow 内以同一事务调用；仍需 Playwright E2E 与 GitHub Actions。 |
| 备注 | 本次为首次推送准备；未直接推送或合并 `main`；构建输出、本地数据库与 Secret 未提交。 |
### 2026-09-08 20:29 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `dev/a` |
| PR / commit | 同步 `origin/main` 到 `b6ffc7f`（PR #49 认证前端登录流程已合入 main） |
| 做了什么 | 将 `dev/a` 快速前进到 `origin/main` 最新提交 `b6ffc7f`，与 main 内容完全一致 |
| 新增功能 | 无新增；仅同步 C 已合入的 `codex/c-auth-frontend`（登录表单、auth context、AppLayout/login 路由、前端单测、测试矩阵与开发日志） |
| 修复 / 重构 / 优化 | 无 |
| 测试与验证 | `git fetch --all --prune` + `git pull --ff-only origin main` 快速前进成功；`merge-base --is-ancestor origin/dev/a origin/main` 通过；未运行应用测试（本次仅同步远端，无源码改动） |
| 文档同步 | `开发日志.md` 新增本推送记录 |
| 风险与后续 | `origin/dev/a` 由本次推送更新到 `b6ffc7f`；后续 F-04 开发基于此基线 |
| 备注 | 本次推送为同步操作，不改变源码 |


### 2026-09-08 20:09 +08:00 | C / Codex | codex/c-auth-frontend | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-auth-frontend` |
| PR / commit | [#49](https://github.com/256-code/InPulse/pull/49)；Push 3 `0ff5068` 已推送；本次日志校正提交待推送 |
| 做了什么 | 回填 PR #49，修正 Push 3 中“尚未创建 PR”的过时描述；无代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 确认 PR #49 已存在，当前 `mergeable` 为 `MERGEABLE`，冲突解决后的分支已同步远端。 |
| 测试与验证 | 本次仅修改日志；Push 1～3 已记录的本地门禁、公共 registry 漏洞审计和 PGroonga 真库集成结果保持不变；推送前重新执行 `pnpm check:docs`。GitHub Actions 尚未确认。 |
| 文档同步 | 仅更新本开发日志。 |
| 风险与后续 | 等待 GitHub Actions 与非作者人工评审；真实 HTTP 登录/E2E、MFA 后续流程仍需验证。 |
| 备注 | 用于日志校正的追加推送；未直接推送或合并 `main`。 |

### 2026-09-08 20:08 +08:00 | C / Codex | codex/c-auth-frontend | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-auth-frontend` |
| PR / commit | [#49](https://github.com/256-code/InPulse/pull/49)；已合并 `origin/main` `f53b57a`；Push 1 `f07b34b`、Push 2 `b6bcf6e` 已推送 |
| 做了什么 | 将最新 `origin/main` 合并到本分支，解决 `开发日志.md` 的推送记录冲突；无业务代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 保留 A 侧审计/重认证记录与 C 侧认证前端 Push 1/Push 2，并修正 Push 2 中提交号附近的异常控制字符；确认下方历史记录未被删除。 |
| 测试与验证 | `pnpm check:docs` 通过；`pnpm check` 除 `deps:audit` 因 npmmirror 无 audit endpoint 失败外全部通过；改以 `pnpm audit --registry=https://registry.npmjs.org --audit-level=high` 结果为无已知漏洞，`pnpm check:secrets` 与文档检查通过。用 `inpulse/pgroonga-pg18.6:repro` 容器执行空库迁移（6 个迁移）后，`pnpm test:integration` 通过：database 1 文件 13 例、api 13 文件 56 例。GitHub Actions 尚未运行。 |
| 文档同步 | 仅更新本开发日志；其余合并带入的 `AGENTS.md` 与 A 侧实现来自 `origin/main`，未在本分支重写。 |
| 风险与后续 | PR #49 已存在；等待 CI 与非作者人工评审；真实 HTTP 登录/E2E、MFA 后续流程仍需验证。 |
| 备注 | 本次为合并冲突解决后的推送；PR #49 由仓库已有流程维护，未由本次任务创建；未直接推送或合并 `main`。 |

### 2026-09-08 19:05 +08:00 | A / Codex | feature/a-audit-require-reauth | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-audit-require-reauth` |
| PR / commit | [#47](https://github.com/256-code/InPulse/pull/47) |
| 做了什么 | 落地 A 侧写路径前置基础设施：`AuditWritePort`（真实 PostgreSQL 审计追加，含 `RequireReauthGuard` 管理员重认证门禁），并同步 `AGENTS.md` 中本机 PGroonga 状态。 |
| 新增功能 | `AuditWritePort` 在调用方 `TransactionContext` 内写项目/SYSTEM 审计链，先行 `audit_lock_head` 再 `audit_append_locked`；`record_hash` 对 JCS 规范化完整信封做 HMAC-SHA-256，密钥版本化并只在 `/run/secrets/*` fail-closed。`RequireReauthGuard` 校验有效 `AUTHENTICATED` Session、系统管理员及密码/TOTP 重认证均在 5 分钟内。 |
| 修复 / 重构 / 优化 | `RequireReauthGuard` 改为单事务：先 `resolveActorInTransaction` 再读重认证元数据，避免一次认证检查开两个 `UnitOfWork`。 |
| 测试与验证 | `pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（29 文件/140 例）、`pnpm lint`、`pnpm format:check`、`pnpm --filter @inpulse/api build`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check` 均通过；本机 PG 18.6 已内置 PGroonga 4.0.8，用 `database/scripts/test-local.ps1` 与端口 55433 临时实例实测 `pnpm --filter @inpulse/database test:integration`（13 例）与 `pnpm --filter @inpulse/api test:integration`（12 文件/48 例）全部通过，其中含新增 `audit-write.integration.test.ts`（2 例）。 |
| 文档同步 | 同步 `AGENTS.md` §8 两处：本机 PostgreSQL 18.6 已内置 PGroonga 4.0.8，`db:test:local` 与 API `test:integration` 已在本地实测通过，CI 探针镜像保留为可复现隔离门禁。 |
| 风险与后续 | 尚未接入任何业务路由或 Workflow（F-04 `POST /api/v1/projects` 仍待做）；`AUDIT_HMAC_KEYRING_FILE`/`AUDIT_HMAC_KEY_VERSION` 生产密钥就绪前不会真正写审计，但缺口已 fail-closed；PR #47 待 CI 复核。 |
| 备注 | 已推送 `feature/a-audit-require-reauth` 并建立 PR #47，CI 未执行。 |

### 2026-09-08 19:53 +08:00 | C / Codex | codex/c-auth-frontend | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-auth-frontend` |
| PR / commit | 尚未创建 PR；Push 1 commit `f07b34b` 已推送 |
| 做了什么 | 回填首次推送的 commit 与远端分支状态；无代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 本次仅修改日志；Push 1 已实际执行的前端门禁见该记录，GitHub Actions 尚未运行。 |
| 文档同步 | 仅更新本日志。 |
| 风险与后续 | 等待 CI 与非作者人工评审；真实 HTTP 登录/E2E、MFA 后续流程仍未验证。 |
| 备注 | 已推送到 `origin/codex/c-auth-frontend`，未直接推送或合并 `main`。 |

### 2026-09-08 19:50 +08:00 | C / Codex | codex/c-auth-frontend | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-auth-frontend`（基于 `dev/c` 140e142 创建） |
| PR / commit | 尚未创建 PR；Push 1 commit `f07b34b` |
| 做了什么 | 接入真实前端认证：`features/auth` 的 `AuthProvider` 挂载时调用 `getCurrentUser`，匿名时调用 `issueCsrfToken`；登录先签发 CSRF 再调用 `login`，完整登录后刷新当前用户；登出同样先签发 CSRF 再调用 `logout`。 |
| 新增功能 | 新增 `/login` 登录页与登录表单；AppLayout 显示当前用户名、头像首字和登录/退出入口；`/search` 声明为 `requiresAuth`，未登录时由守卫提示登录；MFA 返回态明确提示当前版本尚未接入完整流程。 |
| 修复 / 重构 / 优化 | 将认证上下文从 `app/auth` 移到 `features/auth`，保持 `pages -> features` 依赖边界；`RequireAdmin` 改用生成契约的 `isAdmin`；补充 `error` 登录态，避免异常时误放行受限页面。 |
| 测试与验证 | 本地实际执行：`pnpm --filter @inpulse/web typecheck`、`test:unit`（12 文件 32 例）、`check:boundaries`、`build`、ESLint、`pnpm format:check`、`pnpm check:docs`、`git diff --check` 全部通过。未运行真实 HTTP 登录/E2E、GitHub Actions 和 MFA 后续接口；未新增依赖。 |
| 文档同步 | 更新 `docs/frontend-generated-client-consumption-requirements.md` 与 `docs/test-matrix.md`。 |
| 风险与后续 | 管理员 MFA 注册/验证/恢复码流程尚未接入；真实 HTTP、Playwright E2E 仍需在集成环境验证；下一步建议创建小 PR 等待 CI 与人工评审。 |
| 备注 | Push 1 已推送到 `origin/codex/c-auth-frontend`；未直接推送或合并 `main`。 |

### 2026-09-08 18:23 +08:00 | C / Codex | codex/c-search-page | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/c-search-page`（基于 `dev/c` 64cde5d 创建；Push 1 已推送；commit `dcfb2e4`） |
| PR / commit | [#46](https://github.com/256-code/InPulse/pull/46)；Push 1 commit `dcfb2e4` |
| 做了什么 | 完成 F-26 全局搜索页面最小纵切片，通过生成客户端接入 `GET /api/v1/search`。 |
| 新增功能 | `/search` 页面可按类别展示项目、模块、功能、任务、变更记录、外部链接与任务组结果；顶部全局搜索框与左侧“全局搜索”入口可提交 `q`；使用 TanStack Query `useInfiniteQuery` 和服务端 `nextCursor` 加载更多；覆盖 2～200 字符提示、空结果、401/422/429 通用错误。 |
| 修复 / 重构 / 优化 | 复用 `apps/web/src/generated/api` 生成客户端，未手改契约或生成物；未增加未登记筛选参数，也未解析服务端签名游标。 |
| 测试与验证 | `pnpm --filter @inpulse/web typecheck`、`test:unit` 10 文件 24 例、`check:boundaries`、`build`、ESLint、Prettier 均通过；`pnpm check` 除 `deps:audit` 因当前 npm 镜像无 audit endpoint 失败外全部通过，公共 registry 单独 `pnpm audit --registry=https://registry.npmjs.org` 无漏洞，`check:secrets`、`check:docs` 通过；GitHub Actions 未执行。 |
| 文档同步 | README、AGENTS.md、docs/test-matrix.md、docs/frontend-generated-client-consumption-requirements.md；本日志。 |
| 风险与后续 | 路由暂未声明 `requiresAuth`，页面以 401 提示登录，避免未接入登录态阻断；下一步是接入真实认证/业务领域模块，或建立 Playwright E2E。 |
| 备注 | Push 1 已推送，commit `dcfb2e4`；PR #46 已创建。 |
### 2026-09-08 19:01 +08:00 | C / Codex | codex/c-search-page | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-page`（PR #46；Push 1 commit `dcfb2e4` 已推送） |
| PR / commit | [#46](https://github.com/256-code/InPulse/pull/46)；Push 1 commit `dcfb2e4` |
| 做了什么 | 回填首次推送 commit 与推送状态；无代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 本次仅修改日志；未重跑门禁。推送前已完成的前端门禁见 Push 1 条目。 |
| 文档同步 | 仅更新本日志。 |
| 风险与后续 | PR #46 已创建，等待 CI 与非作者人工评审；后续事项见 PR 描述。 |
| 备注 | 本提交用于回填日志，`dcfb2e4` 已推送到 `origin/codex/c-search-page`；PR #46 已创建。 |
### 2026-09-08 19:03 +08:00 | C / Codex | codex/c-search-page | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-page`（PR #46） |
| PR / commit | [#46](https://github.com/256-code/InPulse/pull/46)；Push 1 `dcfb2e4`、Push 2 `0db5eaf` |
| 做了什么 | 回填 PR #46；无代码变更。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无。 |
| 测试与验证 | 本次仅修改日志；未重跑门禁。Push 1 的验证结果保持不变。 |
| 文档同步 | 仅更新本日志。 |
| 风险与后续 | 等待 GitHub Actions 结果与非作者人工评审；不自动合并。 |
| 备注 | PR #46 已创建并推送，未直接推送或合并 `main`。 |

### 2026-09-08 18:02 +08:00 | C / Codex | codex/c-search-http-integration | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-http-integration` |
| PR / commit | [#45](https://github.com/256-code/InPulse/pull/45)；Push 1 `17c8618`、Push 2 `47b0c1b` |
| 做了什么 | 回填 PR #45 的 GitHub Actions 结果：`CI / workspace` 通过（2m27s），`Documentation / docs` 通过。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码变更；仅维护本日志与 PR 描述。 |
| 测试与验证 | GitHub Actions：`CI / workspace` 全部步骤通过，`Documentation / docs` 通过；真实 PostgreSQL 集成测试已在 CI 执行。本次为仅文档提交，未重跑其他本地门禁。 |
| 文档同步 | 仅更新本开发日志；PR #45 描述同步 CI 结果。 |
| 风险与后续 | 等待非作者人工评审；合并后删除交付分支并把最新 `main` 同步回 `dev/c`。 |
| 备注 | 不直接推送或合并 `main`。 |


### 2026-09-08 18:00 +08:00 | C / Codex | codex/c-search-http-integration | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/c-search-http-integration` |
| PR / commit | [#45](https://github.com/256-code/InPulse/pull/45)；Push 1 commit `17c8618` |
| 做了什么 | 回填 PR #45 编号与首次推送 commit；PR 已创建，等待 GitHub Actions 结果。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码变更；仅维护本日志。 |
| 测试与验证 | 本次未修改实现或断言，不重跑门禁；Push 1 的完整验证见上一条。 |
| 文档同步 | 仅更新本开发日志。 |
| 风险与后续 | 等待 `CI / workspace` 与 `Documentation / docs`；通过后再由非作者人工评审并 squash merge。 |
| 备注 | 不直接推送 `main`；合并后删除交付分支并把最新 `main` 同步回 `dev/c`。 |


### 2026-09-08 17:49 +08:00 | C / Codex | codex/c-search-http-integration | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/c-search-http-integration`（基于同步到 `171373d` 的 `dev/c` 创建） |
| PR / commit | 待创建 |
| 做了什么 | 完成 `GET /api/v1/search` 的真实 HTTP API 集成测试，并补充 Session HMAC keyring 测试路径的显式开关与安全回归单测。 |
| 新增功能 | 无用户可见行为变化；新增 8 个真实 HTTP 集成用例，覆盖匿名 401、有效成员 Scope、跨项目隔离、`ADMIN_ONLY`/`HIDDEN`、`includeVoid` 边界、签名游标分页、短查询/非法游标 422、停用用户与未知 Session 401。 |
| 修复 / 重构 / 优化 | `VersionedHmacKeyring.fromEnv` 改为从传入 env 判定 `NODE_ENV=test` 与 `SESSION_HASH_KEYRING_TEST_PATH=1`，生产仍只允许 `/run/secrets/*`；新增单测确认两个条件缺一即 fail closed。交付前已把 `dev/c` 快进到 `origin/main` 的 `171373d`，并保留双方开发日志条目。 |
| 测试与验证 | 实际执行并全部通过：`pnpm --filter @inpulse/api exec vitest run test/session-auth.test.ts`（1 文件 / 11 例）、`pnpm exec eslint apps/api/src/auth/keyring.ts apps/api/test/session-auth.test.ts apps/api/test/search-api.integration.test.ts`、`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（27 文件 / 135 例）、`pnpm --filter @inpulse/api test:search:db`（2 文件 / 16 例，真实 PostgreSQL 18.6 + PGroonga）、`pnpm --filter @inpulse/api test:integration`（12 文件 / 54 例）、`pnpm check:docs`、`git diff --check`；`pnpm check` 也全部通过（临时用官方 npm registry 覆盖本地 npmmirror 的 audit 端点缺失，最终已恢复，未留下仓库或全局配置改动）。GitHub Actions 与 Playwright E2E 未运行。 |
| 文档同步 | 更新 `README.md`、`CONTRIBUTING.md`、`AGENTS.md`、`docs/test-matrix.md` 与本日志；本次无 ADR、权限矩阵、契约生成物或数据库迁移变更。 |
| 风险与后续 | `test:search:db` 按既定决定未纳入 CI，但新 HTTP 集成文件会被现有 `test:integration` 自动包含，需等 PR 的 GitHub Actions 复核；Playwright E2E、搜索页面和生产备份恢复纵切片仍未交付。 |
| 备注 | 交付分支已创建；本次推送前记录，PR 编号及 CI 结果待回填。 |

### 2026-09-08 18:03 +08:00 | A / Codex | dev/a | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 待推送 `origin/dev/a`；commit 回填见推送结果 |
| 做了什么 | 在 A 侧新增 `WorkflowModule` 与 `ProjectWriteAccessWorkflow`，把 B 已合入的 `ModuleQueryPort` / `FeatureQueryPort` 与 A 的 `SessionAuthService` / `ProjectAccessQueryPort` 接入同一事务内的父级写前检查编排。 |
| 新增功能 | Workflow 可接收调用方 `TransactionContext`，按 `session -> project -> module -> feature` 顺序完成父级 FOR SHARE 检查；返回类型化 `allowed / unauthenticated / project-not-found / project-not-active / module-not-found / module-not-active / feature-not-found / feature-not-active`，供 HTTP 层映射为 401 / 404 / 409。 |
| 修复 / 重构 / 优化 | 避免未来 Workflow 各自重复鉴权和事务内锁序；失败后不调用后续 Port，保证不会在项目或父级不可写时继续业务写入。 |
| 测试与验证 | 实际执行并通过：`pnpm check`（lint、format:check、全仓 typecheck、全仓 unit test：api 28 files / 140 tests、migrations:check、contract drift/validate、build、check:deps、check:frontend:boundaries、permissions:check、deps:audit、check:secrets、check:docs）、`git diff --check`；新增 `apps/api/test/project-write-access.workflow.test.ts` 6 个用例。真实 PostgreSQL 集成测试未在本机运行，父级行锁语义继续由已合入的 B `write-query-ports.integration.test.ts` 与 A `project-access.integration.test.ts` 覆盖。 |
| 文档同步 | 新增 `apps/api/src/workflows/README.md` 与本开发日志；未改变 HTTP 路由、权限矩阵或测试矩阵语义。 |
| 风险与后续 | `WorkflowModule` 暂不挂入 `AppModule`，等待首个业务 Workflow/Controller 接入；下一步是项目创建 Workflow 或首个真实 `idempotencyRequired` 业务路由。 |
| 备注 | 本次按用户指示直接在 `dev/a` 工作；尚未推送。 |


### 2026-09-08 17:30 +08:00 | A / Codex | dev/a 扁平化 | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `dev/a` |
| PR / commit | [#43](https://github.com/256-code/InPulse/pull/43) |
| 做了什么 | 应要求将 `dev/a` 强制重置到 `main`（`6f3d632`），消除 `dev/a` 相对于 `main` 的 6 个历史合并提交；旧 `dev/a` 尖端 `ab9f62d` 已备份到 `backup/dev-a-before-force-20260908`。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 恢复 `dev/a` 与 `main` 1:1 对齐，`origin/main...origin/dev/a` 变为 `0 0`。 |
| 测试与验证 | `git rev-list --left-right --count origin/main...origin/dev/a` 返回 `0 0`；未运行 CI 或测试。 |
| 文档同步 | 新增本开发日志记录。 |
| 风险与后续 | `dev/a` 历史已被强推，需要所有 A 侧工作树重新同步；旧历史仅保留在备份分支。 |
| 备注 | 本次为显式授权下的分支扁平化，不改变 `main` 代码内容。 |


### 2026-09-08 17:22 +08:00 | A / Codex | codex/a-search-readme-cleanup | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/a-search-readme-cleanup` |
| PR / commit | [#41](https://github.com/256-code/InPulse/pull/41) |
| 做了什么 | 收尾 C-006 确认：清理 README 中仍写“C-006 未关闭”的旧表述，把搜索 API 稳定正式契约从“尚未提供”移入“已提供”。 |
| 新增功能 | 无行为变化。 |
| 修复 / 重构 / 优化 | 同步 README 的当前状态、下一步、已提供/尚未提供三处内容，避免与已合并的 C-006 正式契约漂移。 |
| 测试与验证 | 实际执行：`pnpm check:docs`、README 的 Prettier 检查。完整 CI 与真实数据库/HTTP/E2E 未运行。 |
| 文档同步 | 更新 `README.md` 与本开发日志。 |
| 风险与后续 | 合并后需以干净方式把 `main` 同步回 `dev/a`。 |
| 备注 | 仅文档收尾；不直接推送或合并 `main`。 |


### 2026-09-08 17:13 +08:00 | A / Codex | codex/a-search-contract-confirmation | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `codex/a-search-contract-confirmation` |
| PR / commit | 未推送 |
| 做了什么 | A 正式确认 C-006 分页/游标契约：将搜索分页 envelope、不透明游标与 15 分钟 TTL 从“候选/未冻结”状态同步为已正式定案。 |
| 新增功能 | 无行为变化；`GET /api/v1/search` 的 Schema/Route Registry、OpenAPI 与生成客户端已在 `origin/main` 落库。 |
| 修复 / 重构 / 优化 | 清理 `search.zod.ts`、`search-cursor.ts`、`docs/permissions.md`、`docs/test-matrix.md` 与前端消费清单中的 C-006 未决/候选表述，避免文档与 Registry 漂移。 |
| 测试与验证 | 实际执行：`pnpm contract:validate`（6 条路由全部通过）、`pnpm contract:drift`（5 个产物一致）、`pnpm --filter @inpulse/api-contract test:unit`（4 文件 / 58 用例通过）、`pnpm --filter @inpulse/api exec vitest run test/search-cursor.test.ts test/search.controller.test.ts`（2 文件 / 13 用例通过）、`pnpm --filter @inpulse/api typecheck` 通过。完整 `pnpm check`、真实 PostgreSQL 集成、HTTP API 与 Playwright E2E 未运行。 |
| 文档同步 | 更新 `docs/permissions.md` 搜索行、`docs/test-matrix.md` SEARCH-003 状态、`docs/frontend-generated-client-consumption-requirements.md` 的 FC-060/FC-061/C-006 与 4.7 分页说明，以及契约/游标源码注释。 |
| 风险与后续 | 尚未推送；推送前建议以本分支发起 PR 并等待 CI。真实 HTTP API、Playwright E2E、搜索页面与真实 PostgreSQL API 纵切片仍待交付，SEARCH-003 继续为部分自动化。 |
| 备注 | 仅本地记录确认动作，不直接推送或合并 `main`。 |

### 2026-09-08 17:05 +08:00 | A / Codex | feature/a-f01-auth-rate-limit | Delivery 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `feature/a-f01-auth-rate-limit` |
| PR / commit | [#39](https://github.com/256-code/InPulse/pull/39) |
| 做了什么 | 再次合并最新 `origin/main`（已含 PR #38 项目写前检查），解决 `session-auth.service.ts` 与开发日志冲突，保持 PR #39 可合并。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 保留 A 的 `GET /me` 注释与项目写前检查实现；开发日志合并 A/C/B 既有记录与 F-01 交付记录。 |
| 测试与验证 | 本地执行 `pnpm check` 全部通过（API 单测 26 文件 / 133 用例）；真实 PostgreSQL 集成测试未本地运行，待 CI 验证。 |
| 文档同步 | 更新本开发日志。 |
| 风险与后续 | 待 CI 全绿后 squash merge 到 `main`，随后同步 `main` 回 `dev/a`。 |
| 备注 | 本次为 `main` 前进后的冲突同步，不直接推送 `main`。 |

### 2026-09-08 16:59 +08:00 | A / Codex | feature/a-f01-auth-rate-limit | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `feature/a-f01-auth-rate-limit` |
| PR / commit | [#39](https://github.com/256-code/InPulse/pull/39) |
| 做了什么 | 修复 PR #39 第二次 CI 失败：`auth_rate_limit_buckets` 的 `CASE` 表达式需要显式 `::timestamptz` 转换，避免字符串参数与 `timestamp with time zone` 列类型不匹配。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | `PostgresAuthRateLimitRepository` 中所有 `now`、`window_started_at`、`blocked_until` 参数统一使用 `${value}::timestamptz` 显式转换。 |
| 测试与验证 | 本地执行 `pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（26 文件 / 130 用例）、`pnpm format:check` 均通过；真实 PostgreSQL 集成测试未本地运行，待 CI 验证。 |
| 文档同步 | 本文件新增 Delivery 3 记录。 |
| 风险与后续 | 待 CI 的 PGroonga 探针镜像执行真实 PostgreSQL 集成测试确认；其余 F-01 候选阈值与评审事项不变。 |
| 备注 | 与 `origin/main` 的冲突已在 Delivery 2 前合并，本提交仅修复真实 PostgreSQL 类型问题。 |

### 2026-09-08 16:58 +08:00 | A / Codex | feature/a-project-write-access | Delivery 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `feature/a-project-write-access` |
| PR / commit | [#38](https://github.com/256-code/InPulse/pull/38)；合并最新 `origin/main` `8b471e7` |
| 做了什么 | 同步最新 `main`（已含 C 的 PR #37 搜索后续交付），解决 `apps/api/test/search-query.integration.test.ts` 与 `开发日志.md` 的合并冲突；保留 A 与 C 双方日志记录及双方搜索测试新增代码。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 搜索集成测试同时保留 A 侧 `TransactionContext` 导入与 C 侧 `SearchCursorService` 导入；本分支项目写前检查逻辑不变。 |
| 测试与验证 | 实际执行：`pnpm check` 全部通过（含 lint、format:check、全仓 typecheck、全仓 unit tests：api 21/115、web 7/17、contract 4/58、database 1/5、migrations:check、contract drift/validate、build、check:deps、check:frontend:boundaries、permissions:check、deps:audit、check:secrets、check:docs）。真实 PostgreSQL 集成用例待 PR CI 重跑。 |
| 文档同步 | 更新本开发日志，保留 C PR #37 交付记录。 |
| 风险与后续 | 等待 GitHub Actions `CI / workspace` 与 `Documentation / docs` 通过；绿后按规则 squash merge 并同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送同一交付分支；不直接推送或合并 `main`。 |


### 2026-09-08 16:55 +08:00 | A / Codex | feature/a-f01-auth-rate-limit | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `feature/a-f01-auth-rate-limit` |
| PR / commit | [#39](https://github.com/256-code/InPulse/pull/39) |
| 做了什么 | 修复 PR #39 首次 CI `workspace` 失败：真实 PostgreSQL 集成测试中 `postgres` 驱动拒绝 `Date` 参数，改为在 SQL 参数处传入 ISO 字符串。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | `PostgresAuthRateLimitRepository` 在 `findBlocked`/`recordFailures` 中把 `now`、`window_started_at`、`blocked_until` 序列化为 ISO 字符串；`database.helpers.ts` 的禁用用户 fixture 同样传入 ISO 字符串。 |
| 测试与验证 | 本地执行 `pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（26 文件 / 130 用例）、`pnpm format:check` 均通过；真实 PostgreSQL 集成测试未本地运行，待 CI 验证。 |
| 文档同步 | 本文件新增 Delivery 2 记录。 |
| 风险与后续 | 仍待 CI 的 PGroonga 探针镜像执行真实 PostgreSQL 集成测试确认修复；限流阈值与 Argon2 并发数 4 的评审事项不变。 |
| 备注 | 与 `origin/main` 的冲突已合并并推送，本提交为 CI 修复的后续推送。 |

### 2026-09-08 16:43 +08:00 | A / Codex | feature/a-f01-auth-rate-limit | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-f01-auth-rate-limit` |
| PR / commit | [#39](https://github.com/256-code/InPulse/pull/39) |
| 做了什么 | 从 `dev/a` `6ab8ec3` 创建短生命周期交付分支，将 F-01 收口改动通过 PR 合并到 `main`，并合并 `origin/main` 最新内容解决冲突。 |
| 新增功能 | `POST /auth/login` 按账号、IP、全局三类持久化桶计数失败并在触发阈值后返回 429；Argon2 同进程并发上限；`GET /me` 返回当前认证用户资料；`UserAuthInvalidationService` 在同一事务内递增 `auth_version` 并撤销该用户全部 Session。 |
| 修复 / 重构 / 优化 | LoginService 在 Argon2 前检查阻断、401 后记录失败、成功后清除账号桶；登录 IP 只从服务端 socket/代理层获取，不信任用户请求头；Route Registry 为登录路由登记 429 响应。 |
| 测试与验证 | 本地执行 `pnpm check` 全通过；API 单元测试 `23 files / 116 tests` 通过；新增真实 PostgreSQL 集成测试（登录限流桶、Session 统一失效），但本机无 PGroonga/测试库环境，未本地运行，待 CI PGroonga 探针镜像验证。 |
| 文档同步 | `docs/test-matrix.md` 新增 `SEC-008`、`SEC-009`；`docs/permissions.md`、`README.md`、`packages/api-contract` 生成物由 `pnpm contract:generate` 同步；本文件新增交付记录。 |
| 风险与后续 | 限流阈值与 Argon2 并发数 4 为候选基线，尚未形成 ADR，需人工安全/性能评审；账号桶可能被分布式登录名用于锁定目标账号，需评审权衡；真实 PostgreSQL 集成测试未本地运行；生产 Dockerfile/Compose 仍未建立。 |
| 备注 | 遵守仓库规则，不直接合并 `dev/a` 到 `main`；本分支合并删除后同步最新 `main` 回 `dev/a`。 |

### 2026-09-08 16:42 +08:00 | A / Codex | feature/a-project-write-access | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `feature/a-project-write-access` |
| PR / commit | [#38](https://github.com/256-code/InPulse/pull/38)；修正集成测试归档 fixture |
| 做了什么 | 修复 PR #38 `CI / workspace` 中 `project-access.integration.test.ts` 归档用例失败：测试直接设置 `ARCHIVED` 未递增 `row_version`，触发 `require_next_row_version()` 数据库保护。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 归档 fixture 同时设置 `row_version = row_version + 1`，断言 `rowVersion` 为 `2`，与既有行版本递增不变量一致。 |
| 测试与验证 | 实际执行：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（20 文件 109 用例）、`pnpm format:check` 全部通过；真实 PostgreSQL 集成用例待 CI 重跑。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 等待 GitHub Actions 重跑 `CI / workspace`；绿后按规则 squash merge 并同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送同一交付分支；不直接推送或合并 `main`。 |


### 2026-09-08 16:38 +08:00 | A / Codex | feature/a-project-write-access | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/a-project-write-access` |
| PR / commit | [#38](https://github.com/256-code/InPulse/pull/38)；Delivery 1 commit `291b692` |
| 做了什么 | 回填本交付 PR 编号与首次推送 commit；本分支已完成首次推送并创建 PR #38。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码变更，仅开发日志回填。 |
| 测试与验证 | Delivery 1 已实际执行 `pnpm check` 全部通过；本次仅文档回填。`pnpm test:integration` 仍待 PR CI 探针镜像验证。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 等待 GitHub Actions `CI / workspace` 与 `Documentation / docs` 通过；绿后按规则 squash merge 并同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送同一交付分支；不直接推送或合并 `main`。 |


### 2026-09-08 16:40 +08:00 | C / Codex | feature/c-search-api-contract | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/c-search-api-contract`（本次为 A 反馈后的后续交付；首个契约纵切片 PR [#36](https://github.com/256-code/InPulse/pull/36) 已合并） |
| PR / commit | [#37](https://github.com/256-code/InPulse/pull/37)；本次推送 commit `c9958cb` |
| 做了什么 | 按 A 2026-09-08 反馈补齐搜索服务与契约实现：`SearchQueryPage` 返回真实 `hasMore`；原有正整数游标替换为服务端版本化 HMAC 签名、绑定 actor/规范化查询、15 分钟过期的 base64url 不透明 token；`q`/`limit`/`cursor` 校验错误统一映射为 422；Route Registry 增加 422 并移除 400；同步权限矩阵、测试矩阵、前端消费要求与生成物。 |
| 新增功能 | `GET /api/v1/search` 采用 `{ items, nextCursor, hasMore }` 响应；`nextCursor` 不再暴露原始 `search_projection.id`，前端可原样传给下一页；游标签发/校验/过期均在服务端完成。 |
| 修复 / 重构 / 优化 | 修正 Controller 原来用 `nextCursor !== null` 推导 `hasMore` 的错误；修正服务层只捕获 `SearchCursorError` 映射为 422、其他错误继续冒泡为 500；新增 `SearchCursorService` 与单测，覆盖签名篡改、过期、跨用户/跨查询、keyring 版本缺失和畸形 token。 |
| 测试与验证 | 实际执行：`pnpm lint` 通过；`pnpm format:check` 通过；`pnpm typecheck` 通过；`pnpm test:unit` 通过（database 5、api-contract 58、web 17、api 112，共 192 例）；`pnpm contract:generate` 生成 5 个产物；`pnpm contract:drift` 5 个产物无漂移；`pnpm contract:validate` 5 条路由通过；`pnpm permissions:check` 5 操作/5 路由通过；`pnpm build` 通过；`pnpm check:deps` 通过（144 源文件）；`pnpm check:frontend:boundaries` 通过（46 modules）；`pnpm check:secrets` 282 文件通过；`pnpm check:docs` 47 Markdown 通过；`git diff --check` 通过；`pnpm --config.registry=https://registry.npmjs.org audit --audit-level=high` 返回 `No known vulnerabilities found`。 |
| 文档同步 | 更新 `README.md`、`CONTRIBUTING.md`、`AGENTS.md`、`docs/permissions.md`、`docs/test-matrix.md`、`docs/frontend-generated-client-consumption-requirements.md`、`packages/api-contract` Schema/Route Registry 注释与本日志；OpenAPI 与生成客户端由生成工具更新。 |
| 风险与后续 | C-006 仍未关闭，本分支仍是评审输入，不得描述为正式验收；A 随后需在正式 Registry 落库错误 code/message/details，并确认 TTL、游标轮换策略；`test:integration`、`test:search:db`、`db:test:local` 未运行（本机 PostgreSQL 18.6 不含 PGroonga）；真实 HTTP API、Playwright E2E、搜索页面与生产备份恢复仍未交付。 |
| 备注 | 推送后创建新 PR #37 供 A 确认正式契约；未推送或合并 `main`。 |
### 2026-09-08 16:36 +08:00 | A / Codex | feature/a-project-write-access | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-project-write-access`（从 `origin/main` `018bfca` 切出） |
| PR / commit | 待创建；首次推送 commit 回填见 Delivery 2 |
| 做了什么 | 按 B 已确认的接口语义补齐 A 侧事务内项目写前检查：`SessionAuthService.resolveActorInTransaction` 复用调用方事务解析当前用户，`ProjectAccessQueryPort.checkProjectForWrite` 返回 `allowed / not-found / parent-not-active`，并对项目行 `FOR SHARE`。 |
| 新增功能 | Workflow 可在同一事务中解析 Session actor 并校验项目写权限；ACTIVE 成员或系统管理员返回 `allowed` 及 `{ projectId, status, rowVersion, isSystemAdmin }`，缺失/停用/非成员返回 `not-found`，已归档项目返回 `parent-not-active`。 |
| 修复 / 重构 / 优化 | 将 `SessionAuthService` 的 Cookie 解析抽为私有方法，`resolveActor` 复用 `resolveActorInTransaction`；写检查不按 `status = ACTIVE` 过滤项目，先按 ID 锁行再判断状态；普通成员关系也实时检查，管理员不要求成员记录。 |
| 测试与验证 | 实际执行：`pnpm check` 全部通过（含 lint、format:check、全仓 typecheck、全仓 unit tests：api 20/109、web 7/17、contract 4/57、database 1/5、migrations:check、contract drift/validate、build、check:deps、check:frontend:boundaries、permissions:check、deps:audit、check:secrets、check:docs）。另实际执行 `git diff --check` 通过。`pnpm test:integration` 未在本机运行，真实 PostgreSQL 集成用例待 PR CI 探针镜像验证。 |
| 文档同步 | 仅新增本开发日志；未改变 HTTP 路由、权限矩阵或测试矩阵语义。 |
| 风险与后续 | `project-access.integration.test.ts` 新增 7 个真库写前检查用例尚未由本机 PGroonga 环境执行；待 PR CI 通过后同步 `main` 回 `dev/a`。 |
| 备注 | 不直接推送或合并 `main`；首次推送后创建 PR 并在 Delivery 2 回填 PR/commit。 |


### 2026-09-08 16:02 +08:00 | C / Codex | feature/c-search-api-contract | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/c-search-api-contract`（从 `dev/c` `1f10b03` 切出） |
| PR / commit | [#36](https://github.com/256-code/InPulse/pull/36)；已推送 commit `b95f173`、`ad4233f` |
| 做了什么 | 新增 F-26 全局搜索 API 契约纵切片：`SearchQueryRequest`、`SearchItem`、`SearchPage`，Route Registry `getSearch`，权限矩阵、OpenAPI 与生成客户端，以及让 `contract:validate` 通过所需的最小 `SearchController`/`SearchModule`。 |
| 新增功能 | `GET /api/v1/search`，`authPolicy=session`、`csrfPolicy=none`、`idempotencyPolicy=none`、`versionPolicy=none`、`concurrencyPolicy=none`；查询参数 `q/cursor/limit/includeVoid`；响应 `{ items, nextCursor, hasMore }`，`items` 保留 `entityType` 判别字段；生成客户端新增 `getSearch(query, init?)`。 |
| 修复 / 重构 / 优化 | Controller 使用契约导出的 `SearchItem`/`SearchPage` 类型并对 `entityType` 做运行时枚举校验；`projects` 增加模块根 `index.ts`，`SearchModule` 通过公开表面导入 `ProjectsModule`，满足依赖边界；`search-text.ts` 常量与契约同步。 |
| 测试与验证 | 实际执行：`pnpm contract:generate`（5 个生成物）、`pnpm contract:drift`、`pnpm contract:validate`（5 条路由）、`pnpm permissions:check`（5 操作 / 5 路由）、`pnpm --filter @inpulse/api-contract typecheck`、`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（19 文件 105 用例）、`pnpm --filter @inpulse/api-contract test:unit`（4 文件 57 用例）、`pnpm check:deps`、`pnpm check:frontend:boundaries`、`pnpm build`、`pnpm check:secrets`（272 文件）、`pnpm check:docs`（46 Markdown）与 `pnpm format:check`。`pnpm check` 执行到 `deps:audit` 前全部通过，但因仓库 `.npmrc` 的 `registry.npmmirror.com` 不提供审计端点而退出；改用 `pnpm --config.registry=https://registry.npmjs.org audit --audit-level=high` 后结果为 `No known vulnerabilities found`。`pnpm test:integration`、`pnpm db:test:local`、`pnpm test:search:db` 未运行（本机 PostgreSQL 18.6 不含 PGroonga），未声称通过。 |
| 文档同步 | 更新 `README.md`、`CONTRIBUTING.md`、`docs/permissions.md`、`docs/test-matrix.md`、`docs/frontend-generated-client-consumption-requirements.md` 与本开发日志；同步生成客户端与 OpenAPI。 |
| 风险与后续 | 响应 envelope、`includeVoid` 暴露方式和游标签名/过期语义仍是 C 候选，需 A 确认（C-006 未解决）；本分支尚未包含真实 HTTP API 集成、Playwright E2E、搜索页面；后续应等待 A 评审后接页面/API 纵切片。 |
| 备注 | 已推送到 `origin/feature/c-search-api-contract`，等待 A 评审；交付说明不得把未定案内容描述为正式契约。 |

### 2026-09-08 16:37 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | commit `6ab8ec3`；经 PR [#39](https://github.com/256-code/InPulse/pull/39) 交付 |
| 做了什么 | 收口 F-01：补齐登录限流、Argon2 并发门控、可信代理 IP 提取、`GET /me` 与用户 Session 统一失效的业务纵切片。 |
| 新增功能 | `POST /auth/login` 按账号、IP、全局三类持久化桶计数失败并在触发阈值后返回 429；Argon2 同进程并发上限；`GET /me` 返回当前认证用户资料；`UserAuthInvalidationService` 在同一事务内递增 `auth_version` 并撤销该用户全部 Session。 |
| 修复 / 重构 / 优化 | LoginService 在 Argon2 前检查阻断、401 后记录失败、成功后清除账号桶；登录 IP 只从服务端 socket/代理层获取，不信任用户请求头；Route Registry 为登录路由登记 429 响应。 |
| 测试与验证 | 本地执行 `pnpm check` 全通过；API 单元测试 `23 files / 116 tests` 通过；新增真实 PostgreSQL 集成测试（登录限流桶、Session 统一失效），但本机无 PGroonga/测试库环境，未本地运行，待 CI PGroonga 探针镜像验证。 |
| 文档同步 | `docs/test-matrix.md` 新增 `SEC-008`、`SEC-009`；`docs/permissions.md`、`README.md`、`packages/api-contract` 生成物由 `pnpm contract:generate` 同步。 |
| 风险与后续 | 限流阈值与 Argon2 并发数 4 为候选基线，尚未形成 ADR，需人工安全/性能评审；账号桶可能被分布式登录名用于锁定目标账号，需评审权衡；真实 PostgreSQL 集成测试未本地运行；生产 Dockerfile/Compose 仍未建立。 |
| 备注 | 本地工作区同时包含此前未提交的 `GET /me`，建议纳入本次 F-01 收口提交。 |

### 2026-09-08 16:10 +08:00 | C / Codex | feature/c-search-api-contract | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `feature/c-search-api-contract`（合并 `origin/main` `38a979c` 后） |
| PR / commit | [#36](https://github.com/256-code/InPulse/pull/36)；合并提交 `bc3d05c` |
| 做了什么 | 解决 PR #36 的 `开发日志.md` 冲突：保留 C 的搜索契约交付记录与 B 的 Modules/CI 文档记录；并合并 `origin/main` 最新内容（含 B 岗位 modules 实现、测试与文档）。 |
| 新增功能 | 无额外业务功能；本分支包含最新 `main` 的 Modules CommandPort 切片。 |
| 修复 / 重构 / 优化 | 冲突解决不删除任何岗位日志条目；`docs/test-matrix.md` 自动合并，搜索契约与 Modules 测试矩阵条目均保留。 |
| 测试与验证 | 合并后实际执行：`pnpm check` 在 `deps:audit` 前全部通过（lint、format、typecheck、单测、迁移检查、契约漂移/完整性、构建、依赖边界、前端边界、权限矩阵），API 单测 20 文件 106 例、Contract 57 例、Web 17 例、Database 5 例；`pnpm --config.registry=https://registry.npmjs.org audit --audit-level=high` 通过；`check:secrets`（280 文件）与 `check:docs`（47 Markdown）通过。`pnpm test:integration` 未本地运行，待 PR CI 真实 PostgreSQL 探针镜像验证。 |
| 文档同步 | 更新 `开发日志.md`；合并 `docs/test-matrix.md` 与 B 岗位 Modules 文档。 |
| 风险与后续 | 搜索响应 envelope、`includeVoid` 与游标签名语义仍待 A 评审（C-006 未解决）；合并后 PR #36 需重新触发 CI 并由 A/B 非作者评审。 |
| 备注 | 本次为冲突解决与 `main` 同步推送，不直接合并 `main`。 |

### 2026-09-08 15:50 +08:00 | B | codex/b-modules-ci-evidence | 文档补交 Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| PR / commit | 原功能 PR [#34](https://github.com/256-code/InPulse/pull/34) 已由维护者 `256-code` 合并为 `22c6248`；文档补交 PR [#35](https://github.com/256-code/InPulse/pull/35)，创建后以本次文档推送回填编号 |
| 做了什么 | 发现维护者在文档回填推送前已合入功能，故由同步后的 dev/b 建立独立文档交付分支，仅补交接入说明、测试矩阵和本日志；原 PR 正文已回填 5/5 证据 |
| 测试与验证 | 行为代码验证仍为上条所列 e826483 的 CI 成功、真实 PostgreSQL 5/5 通过；本次不改代码或断言。文档 diff --check 通过，未执行本地全仓门禁 |
| 保留与同步 | dev/b 快进到 22c6248；全部修改另存 stash，恢复 README 导航与协作方案，已合入代码自然保留，未合入文档保留在本地；未向 main 推送，代理未执行合并 |
| 风险与后续 | 文档补交 PR 等待检查与人工处理，不自动合并；不在已合入的功能分支继续追加工作 |

### 2026-09-08 15:47 +08:00 | B | codex/b-modules-unclassified | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| PR / commit | [#34](https://github.com/256-code/InPulse/pull/34)，行为代码已验证提交 `e826483` |
| 做了什么 | 回填真实数据库验证证据，未修改实现或测试断言 |
| 测试与验证 | [CI / workspace](https://github.com/256-code/InPulse/actions/runs/34200874889) 成功；[数据库步骤](https://github.com/256-code/InPulse/actions/runs/34200874889/job/101979120660?pr=34#step:17:29) 明确显示 modules-command.integration.test.ts 5 tests 全部通过、155 ms；API 集成合计 7 文件、25 例通过 |
| 环境与命令 | CI 的 PostgreSQL 18.6 + PGroonga 隔离库完成角色 bootstrap、扩展安装与空库迁移，根级 pnpm test:integration 调用 API 相同 Vitest 集成配置；被测写入断言 current_user=app_runtime；本地仍无数据库环境，没有将本地失败或 Mock 算作通过 |
| 文档同步 | 接入说明、测试矩阵由待验证更新为上述提交的 CI 已通过；保留先前失败记录并回填 PR 正文证据 |
| 风险与后续 | 本次仅文档回填，推送后仍需确认最终提交检查；项目创建 Workflow 与 HTTP 映射由 A 后续接入；等待非作者人工评审，不自动合并 |

### 2026-09-08 15:44 +08:00 | B | codex/b-modules-unclassified | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `docs` |
| PR / commit | [#34](https://github.com/256-code/InPulse/pull/34)，首次提交 `4c414e4`、`d0f00f1` 已推送 |
| 做了什么 | 修复首次 CI 的 Repository 输入类型：从 Drizzle inferSelect 提取必填字段，避免 inferInsert.kind 的 undefined 联合类型进入 SQL 参数；没有运行时行为变化 |
| 测试与验证 | [首次 CI](https://github.com/256-code/InPulse/actions/runs/34200609717) 在 typecheck 报 TS1320/TS2345，数据库步骤尚未执行。修复后 `pnpm --filter @inpulse/api exec vitest run test/modules-module.test.ts` 1/1 通过；通过 TypeScript Compiler API 仅检查 Modules index 与两份测试及其导入，零诊断；首次从仓库根运行局部脚本因类型包解析路径失败，改在 apps/api 运行后通过，未改类型检查规则 |
| 文档同步 | 本日志回填 PR 与失败/修复证据；数据库 5 例仍待 CI，不伪称通过 |
| 风险与后续 | 保持原有约束、断言与交付范围；推送后等待 CI 实际数据库执行，非作者人工评审，不自动合并 |

### 2026-09-08 15:37 +08:00 | B | codex/b-modules-unclassified | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/b-modules-unclassified`，由同步至 `1f10b03` 的 `dev/b` 创建 |
| PR / commit | 首次交付准备；PR 编号与 CI 结果待创建后回填，不自动合并 |
| 做了什么 | 仅交付 Modules 未分类模块公开 CommandPort、事务参与、冲突传播、5 个真实 PostgreSQL 测试、Nest 注入测试及必要说明 |
| 新增功能 / 修复 | 无额外行为变更；同一 tx 由 A 的 Workflow 持有，未接入项目创建 HTTP 闭环 |
| 测试与验证 | 同步后 `pnpm --filter @inpulse/api exec vitest run test/modules-module.test.ts` 1/1 通过；数据库测试本地仍无环境，5 例待 CI 实际执行；不把注入测试作为数据库通过证据 |
| 文档同步 | 接入说明、测试矩阵与本日志随切片交付，数据库状态明确为待 CI 验证；只有确认实际 5/5 通过后才能回填 |
| 范围核对 | 原有 README 导航修改、未跟踪协作方案全部保留在 dev/b 工作区，不复制到交付分支；同步前全部修改另有 stash 备份；开发日志冲突保留双方条目 |
| 风险与后续 | CI 使用既有 PostgreSQL 18 + PGroonga 探针镜像；若失败只修本切片问题，不弱化断言或约束；待非作者人工评审，不自动合并 |

### 2026-09-08 14:29 +08:00 | B | dev/b | 数据库验证复查（未推送）

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/b` |
| PR / commit | 未创建提交、未推送、未创建 PR |
| 做了什么 | 保持 Modules 初始化 Port 切片范围，复查隔离数据库环境并重跑指定测试 |
| 新增功能 / 修复 | 无；未修改实现、约束或断言 |
| 测试与验证 | `pnpm --filter @inpulse/api exec vitest run --config vitest.integration.config.ts test/modules-command.integration.test.ts` 退出码 1；beforeAll 报缺少 TEST_DATABASE_URL，5 例未执行（runner 显示 skipped），不视为通过 |
| 环境证据 | TEST_DATABASE_URL、POSTGRES_BIN 均未配置（仅检查是否存在，未输出连接值）；PATH 未找到 docker/podman/psql/postgres，默认 PostgreSQL/Docker 位置未发现工具；`wsl --list --quiet` 返回 1，提示 WSL 未安装 |
| 文档同步 | 更新 Modules 接入说明与测试矩阵，保持数据库行为待验证 |
| 风险与后续 | 需要 PostgreSQL 18 + PGroonga 且已 bootstrap/迁移的明确隔离测试库后重跑，验收仍要求 5/5 实际通过；未连接未知数据库，未使用 Mock 或注入测试替代 |
| 备注 | 保留工作区已有改动；未新增依赖、未改迁移、未运行全仓检查或全量构建 |

### 2026-09-08 | B | dev/b | 本地交付（未推送）

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `dev/b`，开工 HEAD 与本地 origin/main 引用均为 `4de1150`；未 fetch，不声称实时远端已复核 |
| PR / commit | 未创建提交、未推送、未创建 PR |
| 做了什么 | 新增 ModulesCommandPort.createUnclassifiedModule，仅供项目初始化 Workflow 使用调用方 TransactionContext 写未分类模块 |
| 新增功能 | 公开输入/结果与 Nest token、ModulesModule 导出；固定名称“未分类模块”和 UNCLASSIFIED，返回 moduleId，其他字段采用数据库默认值 |
| 修复 / 重构 / 优化 | 唯一约束冲突转换为固定安全的 UNCLASSIFIED_MODULE_CONFLICT（409）；其他异常向外传播，不吞错、不添加幂等或自有事务；未修复 A 侧已知问题 |
| 测试与验证 | 先写数据库测试，首次执行 `pnpm --filter @inpulse/api exec vitest run --config vitest.integration.config.ts test/modules-command.integration.test.ts` 因尚无 Port 入口而失败；实现后同命令在 beforeAll 因缺少 TEST_DATABASE_URL 失败，5 个断言未执行（runner 显示 skipped，源码未使用 skip）；`pnpm --filter @inpulse/api exec vitest run test/modules-module.test.ts` 1/1 通过；`pnpm exec prettier --write apps/api/src/modules/modules apps/api/test/modules-command.integration.test.ts apps/api/test/modules-module.test.ts` 成功 |
| 文档同步 | 新增 [接入说明](./apps/api/src/modules/modules/README.md)，登记 [测试矩阵](./docs/test-matrix.md)；实现遵循已有设计与 ADR，无架构、授权或数据库不变量变化，不改三份设计及 HTTP 权限矩阵 |
| 风险与后续 | 真实 PostgreSQL 行为未验证；未配置测试 URL，已检查的 PostgreSQL 18 默认安装位置不存在，Docker 命令不可用。A 仍需在同一 tx 写项目和初始成员、调用本 Port、补齐审计/通知/投影及外层授权/幂等/HTTP 错误脱敏，在 PostgreSQL 18 + PGroonga 隔离库执行新增测试 |
| 备注 | 已检查新增实现及测试；`git diff --check -- apps/api/src/modules/modules apps/api/test/modules-command.integration.test.ts apps/api/test/modules-module.test.ts docs/test-matrix.md 开发日志.md` 通过（新文件另行阅读核对）。保留 README.md 与 docs/three-person-ai-delivery-plan-v2.md 用户改动。未新增依赖、未修改历史迁移、未接入根 AppModule；未运行全仓静态检查、全量构建、依赖审计或无关数据库门禁 |

### 2026-09-08 15:23 +08:00 | A / Codex | feature/a-project-access-session-auth | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/a-project-access-session-auth` |
| PR / commit | [#33](https://github.com/256-code/InPulse/pull/33)；commit `7733c07` |
| 做了什么 | 回填首次推送后创建的 PR 编号。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码变更。 |
| 测试与验证 | 与 Delivery 1 相同，未重跑；等待 PR CI。 |
| 文档同步 | 更新开发日志的 PR / commit 字段。 |
| 风险与后续 | 等待 PR #33 的 `CI / workspace` 与 `Documentation / docs`，以及 B/C 非作者人工评审；通过后同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送同一交付分支；不直接推送或合并 `main`。 |

### 2026-09-08 15:21 +08:00 | A / Codex | feature/a-project-access-session-auth | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/a-project-access-session-auth`（已 rebase 到 `origin/main` `023d60f`） |
| PR / commit | [#33](https://github.com/256-code/InPulse/pull/33)；commit `c2f4305` |
| 做了什么 | 响应 C 反馈，补齐 A 岗位阶段 0 缺失项：生产 `PostgresProjectAccessQueryPort` 与通用 `SessionAuthService`。 |
| 新增功能 | `ProjectsModule` 提供并导出 `PROJECT_ACCESS_QUERY_PORT` 生产适配器；`AuthModule` 导出 `SessionAuthService`，供后续 `authPolicy: session` 路由从 `__Host-session` 解析 `AUTHENTICATED` 身份。 |
| 修复 / 重构 / 优化 | 生产 Port 实时读取用户启用状态、全局管理员标记与 ACTIVE 成员关系，不再依赖测试夹具实现；Session 身份解析复用现有 HMAC 候选哈希与有效 Session 查询，并拒绝匿名/停用/受限状态。 |
| 测试与验证 | 本地实际执行 `pnpm check` 全部通过（含 lint、format:check、typecheck、全仓 unit test、迁移检查、契约漂移/完整性、生产构建、依赖边界、前端边界、权限矩阵、依赖审计、Secret 与文档检查）；新增 5 个 `SessionAuthService` 单测和 5 个真实 PostgreSQL `ProjectAccessQueryPort` 集成用例。`pnpm test:integration` 未在本机运行，待 PR CI 探针镜像验证。 |
| 文档同步 | 更新 `AGENTS.md`、`README.md`、`CONTRIBUTING.md`、`技术设计v1.2.2.md`、`docs/adr/ADR-025.md`、`docs/test-matrix.md`、`docs/poc/search-pgroonga-v1-result.md`，将生产 `ProjectAccessQueryPort` 适配器从待办状态改为已由 `ProjectsModule` 提供。 |
| 风险与后续 | 搜索 API/Controller、Playwright E2E、生产容器镜像/Compose/镜像扫描与生产加密备份恢复仍待交付；真库集成用例尚未由 CI 执行，不得提前声称通过。 |
| 备注 | 不直接推送或合并 `main`；本记录在首次推送前加入。 |

### 2026-09-08 15:15 +08:00 | C / Codex | dev/c | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/c`（基于最新 `origin/main` `265cd90` rebase 后提交） |
| PR / commit | 前端实现提交 `54c581f`；本开发日志随本分支一起推送 |
| 做了什么 | 按设计参考 v1.0 实现 F-30 前端视觉壳与路由骨架：深色侧栏、白色顶栏、中心工作区；新增项目与功能、我的任务、迭代记录、成员与权限、动态审计五个空态路由；不接入业务 API，不引入 Mock 数据 |
| 新增功能 | 侧栏 / 顶栏 / 全局搜索 / 通知占位 / 用户占位；五个可点击空态路由；公共 `WorkspacePlaceholder` 组件 |
| 修复 / 重构 / 优化 | 保留 `app -> pages -> features -> shared/generated` 依赖方向、路由自动聚合、鉴权壳和错误边界；新增路由暂保持匿名可访问，待正式登录态接入后再启用守卫 |
| 测试与验证 | `pnpm test:web` 7 个测试文件 17 个用例通过；`pnpm typecheck`、`pnpm check:frontend:boundaries`、`pnpm format:check`、`pnpm lint`、`pnpm build` 全部通过；Vite 构建仅有 chunk size 警告，不构成失败；本次未执行数据库或后端集成门禁，未改动后端 |
| 文档同步 | 新增本开发日志；未新增正式 API 契约，Route Registry、OpenAPI、权限矩阵和测试矩阵无需变更 |
| 风险与后续 | 项目、任务、记录、成员和搜索等业务接口契约尚未就绪，现有页面均为空态；路由尚未启用 `RequireAuth`；下一步需先确认项目/任务契约或 F-26 搜索接口，再接真实数据 |
| 备注 | 用户确认后本地提交并 rebase；尚未创建 PR，后续可从 `dev/c` 切交付分支并走人工评审 |

### 2026-09-08 14:49 +08:00 | A / Codex | fix/stage0-runtime-contract-idempotency-login | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `fix/stage0-runtime-contract-idempotency-login` |
| PR / commit | [#31](https://github.com/256-code/InPulse/pull/31) |
| 做了什么 | 回填 PR #31 修复后 CI 最终结果。 |
| 新增功能 | 无。 |
| 修复 / 重构 / 优化 | 无代码变更；仅记录 CI 验证证据。 |
| 测试与验证 | GitHub Actions head `3cf80e2`：`CI / workspace` run `34196166675` 与 `Documentation / docs` run `34196166682` 全部通过，含 frozen lockfile、lint、format、typecheck、单测、迁移、PGroonga 真库集成、契约漂移/完整性、构建、依赖边界、权限矩阵、依赖审计、Secret 与文档检查。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | PR #31 已通过 CI，等待 B/C 非作者人工评审；通过后同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送文档回填；不直接推送或合并 `main`。 |

### 2026-09-08 14:45 +08:00 | A / Codex | fix/stage0-runtime-contract-idempotency-login | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `fix/stage0-runtime-contract-idempotency-login` |
| PR / commit | [#31](https://github.com/256-code/InPulse/pull/31)；commit `8bc9ec0` |
| 做了什么 | 修复 PR #31 CI `workspace / Unit tests` 失败：干净环境在 `pnpm test:unit` 阶段尚未构建 `@inpulse/api-contract` 的 `dist`，API 测试解析不到新的运行期 `default` 出口。 |
| 修复 / 重构 / 优化 | 在 `apps/api` 的 Vitest 单测与集成测试配置中为 `@inpulse/api-contract` 增加源码别名，指向 `packages/api-contract/src/index.ts`，与既有 `@inpulse/database` 的测试别名策略一致；运行期 `node dist` 仍走契约包 `dist` 构建产物。 |
| 测试与验证 | 本地再次实际执行 `pnpm check` 全部通过；`apps/api` 单测 17 文件 93 用例通过。真实 PostgreSQL/PGroonga 集成测试仍由 CI 探针镜像覆盖。 |
| 文档同步 | 仅新增本开发日志；未改变 HTTP 路由、契约、权限矩阵或测试矩阵语义。 |
| 风险与后续 | 等待 PR #31 重跑 `CI / workspace`；若探针镜像真库集成再有失败，继续按日志修复，不修改设计语义。 |
| 备注 | 追加推送同一交付分支；不直接推送或合并 `main`。 |

### 2026-09-08 14:39 +08:00 | A / Codex | fix/stage0-runtime-contract-idempotency-login | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `fix/stage0-runtime-contract-idempotency-login`（自 `dev/a` `4de1150` 切出） |
| PR / commit | [#31](https://github.com/256-code/InPulse/pull/31)；commit `31e9f25`、`967b71c`、`18fde55` |
| 做了什么 | 修复阶段 0 三个可复现的运行期阻断：为 `@inpulse/api-contract` 提供可加载的 `dist` 运行期出口；幂等 HTTP 适配器在 runner 持久化前按 Route Registry `idempotencyReplayPolicy` 校验状态码、响应 Schema 与 body 叶子字段；登录成功显式返回 HTTP 200。 |
| 修复 / 重构 / 优化 | 契约包 `exports` 改为 `types -> src/index.ts`、`default -> dist/index.js` 并新增 `tsconfig.build.json`，根级 `pnpm build` 现按拓扑先构建契约包；幂等执行函数在 `runner.run` 前执行响应白名单校验，拒绝未登记状态、Schema 不匹配、noBody/body 分支不一致及额外叶子字段；`LoginController` 增加 `@HttpCode(200)` 并在成功分支显式 `response.status(200)`。 |
| 测试与验证 | 本地实际执行 `pnpm check` 全部通过：lint、format:check、typecheck、全仓单测（api 17 文件 93 用例、api-contract 3 文件 51 用例、web 15 用例、database 5 用例）、迁移一致性、契约漂移/完整性、api/web 生产构建、依赖边界、前端边界、权限矩阵、依赖审计、Secret 扫描与文档检查；新增 HTTP 层登录回归用例。真实 PostgreSQL/PGroonga 集成测试未在本机运行，由 PR CI 探针镜像覆盖。 |
| 文档同步 | 仅新增本开发日志；未新增或修改 HTTP 路由，Route Registry、OpenAPI、权限矩阵与测试矩阵语义不变。 |
| 风险与后续 | 三个修复已分逻辑提交，等待 PR CI 与 B/C 非作者人工评审；下一步仍缺第一个真实 `idempotencyRequired` 业务路由（建议 F-04 `POST /projects`），以及 `/me`、Nest Zod Pipe/Serializer、Playwright E2E 与生产容器/Compose/镜像扫描门禁。 |
| 备注 | 不直接推送或合并 `main`。 |

### 2026-09-08 13:37 +08:00 | A / Codex | feature/idempotency-runner | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/idempotency-runner`（自最新 `origin/main` `f2d13b9` 切出） |
| PR / commit | [PR #30](https://github.com/256-code/InPulse/pull/30) |
| 做了什么 | 把 `dev/a` 上的幂等 runner WIP（`73f16dc`）补成可在 `main` 交付的工程基座：保留原协议语义，补齐 Route Registry 解析、HTTP 摘要构造、惰性 fingerprint keyring、NestJS 模块接线和真实 PostgreSQL 集成测试。 |
| 新增功能 | 运行期幂等协议容器与 HTTP 适配器，供后续 `idempotencyRequired` 业务路由复用；本次不接入用户可感知业务接口。 |
| 修复 / 重构 / 优化 | 从 Route Registry 按 operationId 解析策略并 fail closed；规范化 query、path 参数、Content-Type 与行为头；Cookie、Authorization、CSRF 与 `Idempotency-Key` 不进入摘要；旧 key version 按记录中的版本 key 重算摘要，无法解析时返回明确的 `key-version` 冲突；`actorOnly` 策略允许无授权器，`resources` 策略缺少授权器时拒绝。 |
| 测试与验证 | 本地实际执行通过：`pnpm check`（lint、format、typecheck、全仓单测、迁移一致、契约漂移/完整性、构建、依赖边界、权限矩阵、依赖审计、Secret、文档检查），`pnpm contract:drift`，`pnpm check:secrets`，ESLint 与 `apps/api` 构建；`apps/api` 单测 15 文件 85 用例。PR #30 首次 CI 的 `workspace` 真库集成因测试业务写未递增 `users.row_version` 失败，已修正该测试；修正后本机 typecheck、单测与 ESLint 通过，真库重跑待 CI。 |
| 文档同步 | 仅新增本开发日志；未新增 HTTP 路由，Route Registry、OpenAPI、权限矩阵与测试矩阵无需变更。 |
| 风险与后续 | 尚无第一个真实 `idempotencyRequired` 业务路由；下一步接入 F-04 `POST /api/v1/projects` 并登记完整 replay 策略。生产密钥轮换仍须按指纹 keyring 文件与版本约定操作。 |
| 备注 | PR 已创建；已推送交付分支，fix 修正后等待 CI 重跑；不直接推送或合并 `main`。 |

### 2026-09-08 13:03 +08:00 | A / Codex | feature/auth-logout | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/auth-logout` |
| PR / commit | [PR #29](https://github.com/256-code/InPulse/pull/29)（最新 head `63952f6`） |
| 做了什么 | 记录 PR #29 最终 CI：`CI / workspace`（run `34189150773`）与 `Documentation / docs`（run `34189150641`）全部成功。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 无代码变更；仅回填 CI 验证证据。 |
| 测试与验证 | GitHub Actions head `63952f6`：lint、format、typecheck、单测、迁移一致、PGroonga 真库集成、契约漂移/完整性、构建、依赖边界、权限矩阵、依赖审计、Secret、文档检查全部通过。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | PR #29 已通过 CI，等待非作者人工评审；通过后同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送 `feature/auth-logout`；不自动合并。 |

### 2026-09-08 13:04 +08:00 | A / Codex | feature/auth-logout | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/auth-logout` |
| PR / commit | [PR #29](https://github.com/256-code/InPulse/pull/29)（追加 docs 回填提交，不改写 Delivery 1） |
| 做了什么 | 回填 PR #29 已创建：交付 `POST /api/v1/auth/logout`，指向 `main` 等待评审，未自动合并。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 无代码变更；仅回填交付记录。 |
| 测试与验证 | 推送后等待 PR #29 CI；本机已执行 `pnpm check` 全部通过，真库集成由 CI 探针镜像覆盖。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 等待非作者人工评审；PR CI 的 `CI / workspace` 与 `Documentation / docs` 通过后再同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送 `feature/auth-logout`；不直接推送或合并 `main`。 |

### 2026-09-08 12:58 +08:00 | A / Codex | feature/auth-logout | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/auth-logout`（自最新 `origin/main` `a6dbdeb` 切出） |
| PR / commit | 待创建（本次交付尚未推送） |
| 做了什么 | 交付 `POST /api/v1/auth/logout` 登出纵切片：有效 Session 必须在同源与 Fetch Metadata 校验通过后验证当前 CSRF Hash，在同一事务条件撤销；无效、已撤销或响应丢失后的重试只清 Cookie 并返回 204，不执行状态写。 |
| 新增功能 | 新增 `logout` 路由、`LogoutHeaders`/Schema Registry/Route Registry/权限矩阵，以及 OpenAPI 与 TypeScript 客户端生成物；生成 `logout()` 客户端方法。 |
| 修复 / 重构 / 优化 | 在 `PostgresUserSessionRepository` 增加条件 `revoke`，在 `PostgresSessionCsrfTokenRepository` 增加有效 CSRF Hash 查询；CSRF 比较使用常量时间比较，覆盖 keyring 多版本候选。 |
| 测试与验证 | 本地实际执行通过：`pnpm check`（lint、format、typecheck、全仓单测、迁移一致、契约漂移/完整性、构建、依赖边界、权限矩阵、依赖审计、Secret、文档检查）；`apps/api` 单测 11 文件 65 用例，`apps/api-contract` 3 文件 51 用例；`git diff --check` 通过。真实 PostgreSQL `logout.integration.test.ts` 未在本机运行（`TEST_DATABASE_URL` 未设置），由 PR CI 的 PGroonga 探针镜像覆盖。 |
| 文档同步 | 仅新增本开发日志；ADR-015/ADR-023、权限矩阵与测试矩阵的既定语义已包含 logout，无需修改。 |
| 风险与后续 | 等待 PR CI 真库集成验证；通过后请求非作者人工评审，再同步 `main` 回 `dev/a`。 |
| 备注 | 本地未提交、未推送；不直接推送 `main`。 |

### 2026-09-08 12:29 +08:00 | A / Codex | feature/auth-login | Delivery 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `feature/auth-login` |
| PR / commit | [PR #28](https://github.com/256-code/InPulse/pull/28)（`cd08ebf`） |
| 做了什么 | 记录 PR #28 最终 CI 结果：真库集成修复后 `CI / workspace` 与 `Documentation / docs` 全部通过。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 无代码变更；仅回填验证证据。 |
| 测试与验证 | GitHub Actions run `34186787126`：lint、format、typecheck、单测、迁移一致、PGroonga 真库集成、OpenAPI/客户端漂移、Route Registry、构建、依赖/前端边界、权限矩阵、依赖审计、Secret、文档检查全部通过。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | PR #28 可被 B/C 评审；通过后同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送 `feature/auth-login`；不自动合并。 |

### 2026-09-08 12:23 +08:00 | A / Codex | feature/auth-login | Delivery 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `feature/auth-login` |
| PR / commit | [PR #28](https://github.com/256-code/InPulse/pull/28)（追加修复提交） |
| 做了什么 | 修复 CI 真库集成失败：postgres-js 原生 SQL 不接受 `Date` 参数，Session 的 `idle_expires_at`/`absolute_expires_at` 与测试夹具 `disabled_at` 改为 ISO 字符串传入，避免 `ERR_INVALID_ARG_TYPE`。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 仅调整时间参数序列化；SQL 与事务语义不变。 |
| 测试与验证 | 本地实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（9 文件 56 用例）、`pnpm format:check`、`git diff --check`；PR CI 真库流程重新运行。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 等待 PR #28 CI 真库通过后由 B/C 非作者评审；通过后同步 `main` 回 `dev/a`。 |
| 备注 | 追加推送 `feature/auth-login`；未直接推送 `main`。 |

### 2026-09-08 12:20 +08:00 | A / Codex | feature/auth-login | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/auth-login`（自最新 `origin/main` `cc8e66f` 切出） |
| PR / commit | [#28](https://github.com/256-code/InPulse/pull/28)（`7ff367a`；不包含未交付幂等 runner `73f16dc`） |
| 做了什么 | 交付 `POST /api/v1/auth/login` 登录纵切片：Argon2id 密码校验、匿名预认证 Session + CSRF 校验、密码成功后同一事务消费预认证、创建显式状态的 `user_sessions` 并轮换 CSRF/`__Host-session` Cookie。 |
| 新增功能 | 新增登录路由、契约 Schema/Route Registry/权限矩阵、OpenAPI 与 TypeScript 客户端生成物；普通用户进入 `AUTHENTICATED`，管理员按 TOTP 因子状态进入 `MFA_ENROLLMENT` 或 `MFA_CHALLENGE`。 |
| 修复 / 重构 / 优化 | 引入 `PasswordService`（Argon2id `m=19456, t=2, p=1`，NodeNext 下使用数值 `2` 避免 ambient const enum）；用户不存在/停用/哈希缺失时执行 dummy Argon2id 降低账号枚举；登录前和签发 Session 前均校验已有 Session/用户状态与 `auth_version`；非安全请求要求 Origin/Referer 至少一个存在。 |
| 测试与验证 | 推送前本地实测通过：`pnpm check`（lint、format、typecheck、单测、迁移一致、契约漂移/完整性、构建、依赖/前端边界、权限矩阵、依赖审计、Secret、文档检查）；`apps/api` 单测 9 文件 56 用例、`packages/api-contract` 3 文件 51 用例。真实 PostgreSQL `login.integration.test.ts` 未在本机运行（本地无 PGroonga 实例），由 PR CI 探针镜像验证。 |
| 文档同步 | 仅新增本开发日志；本次未改变 ADR/三份设计文档/权限矩阵/测试矩阵的既定语义。 |
| 风险与后续 | 登录限流（账号 + IP + 全局）和 Argon2 并发上限未实现，按本切片明确为非目标；Nest 全局异常过滤器/`SEC-006` 尚未实现；后续移动需要在 MFA 完成前接入 `startMfaEnrollment`/`verifyMfa` 等受控步骤。 |
| 备注 | 已创建 PR #28；未直接推送 `main`，等待 PR CI 与 B/C 非作者评审。 |

### 2026-09-08 11:30 +08:00 | A / Codex | feature/deps-argon2 | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `feature/deps-argon2`（自最新 `origin/main` `d854ef0` 切出） |
| PR / commit | [#27](https://github.com/256-code/InPulse/pull/27)（`96b01e8`；base `main`） |
| 做了什么 | 在 `apps/api` 增加生产依赖 `@node-rs/argon2@2.2.0` 并同步 `pnpm-lock.yaml`；不包含 `login` 功能代码。 |
| 新增功能 | 无新增功能；为后续 Argon2id 密码校验提供已批准、精确锁定的实现依赖。 |
| 修复 / 重构 / 优化 | 无功能修复或重构；仅按仓库固定版本策略添加依赖，manifest 使用精确版本 `2.2.0`。 |
| 测试与验证 | 本地实测通过：`pnpm install --frozen-lockfile`；`pnpm check`（lint、format、typecheck、单测、迁移一致、契约漂移/完整性、构建、依赖/前端边界、权限矩阵、依赖审计、Secret、文档检查全部通过）；`@node-rs/argon2` ESM 导入冒烟通过，使用 `memoryCost=19456, timeCost=2, parallelism=1` 生成 `$argon2id$...`，正确密码校验返回 `true`，错误密码返回 `false`。 |
| 文档同步 | 仅本开发日志；Argon2id 算法与参数基线仍由 [ADR-011](./docs/adr/ADR-011.md) 声明，本依赖选型属于其实现细节，未改变已接受架构决策。 |
| 风险与后续 | 生产依赖变更按仓库规则必须独立 PR 并人工确认，不得自动合并；合并后再实现 `POST /api/v1/auth/login` 的 Argon2id 校验、preauth 原子消费、`user_sessions` 签发与 CSRF 轮换。 |
| 备注 | 首次推送该交付分支并已创建 PR #27；本地未运行需 PGroonga 真库的 `test:integration`，由 PR CI 覆盖。 |

### 2026-09-08 11:11 +08:00 | A / Codex | feature/auth-csrf-bootstrap | Delivery 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/auth-csrf-bootstrap`（自最新 `origin/main` 切出） |
| PR / commit | [#26](https://github.com/256-code/InPulse/pull/26)（base `main`）；包含认证基础与 `issueCsrfToken` 纵切片 |
| 做了什么 | 将 `dev/a` 上已通过本地门禁与真库 CI 的认证/会话基础层、CSRF 签发路由及后续修复整理为独立交付分支；不包含尚未接入业务命令的幂等协议 runner。 |
| 新增功能 | `GET /api/v1/auth/csrf` / `issueCsrfToken`，匿名预认证 Session 创建与轮换、有效认证 Session 的 CSRF Hash 存取、契约/权限/生成物同步。 |
| 修复 / 重构 / 优化 | 整理 Drizzle 事务上下文、预认证过期时间边界、Session 行锁查询与当前代码中已确认的真库问题；交付分支从 `main` 樱桃挑选，避免把未交付 WIP 混入。 |
| 测试与验证 | 本地实测通过：`pnpm check`（lint、格式、typecheck、单测、迁移一致、契约漂移与完整性、构建、依赖/前端边界、权限、审计、Secret 扫描、文档检查）。交付分支 CI 与人工评审待创建 PR 后确认。 |
| 文档同步 | 同步 [ADR-019](./docs/adr/ADR-019.md)、[技术设计v1.2.2](./技术设计v1.2.2.md)、[系统设计文档v1.0.2](./系统设计文档v1.0.2.md) 及生成物；开发日志新增本交付条目。 |
| 风险与后续 | 本次只交付认证/CSRF 切片；合并后需同步 `main` 到 `dev/a`，再继续 `@node-rs/argon2@2.2.0` 独立依赖 PR 与 `login` 实现。 |
| 备注 | 未直接推送 `main`；等待 PR CI 与 B/C 非作者评审。 |

### 2026-09-08 10:56 +08:00 | A / Codex | dev/a | Push 11

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（修复 Push 10 后的 CI 第二次失败；未创建 PR） |
| 做了什么 | Push 10 的 CI 已通过 `FOR UPDATE` 语法检查，但随后报 `reset.str` 收到 `Date`：原生 `tx.sql` 不会自动序列化原始 `Date` 参数。改为让 Session 过期条件直接使用数据库 `now()`，不再传 JS 时间参数，同时避免应用与 PostgreSQL 时钟偏差。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 只调整 `findValidByTokenHashes` 的过期比较来源，仍保持同事务、实时用户状态和 `auth_version` 校验。 |
| 测试与验证 | 本地实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（7 文件 48 用例）、`pnpm lint`、`pnpm format:check`；完整 `pnpm check` 与真库 CI 结果在本推送后确认。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更。 |
| 风险与后续 | 待 CI 真库测试最终确认；若仍失败，继续按日志修复，不修改既有设计语义。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:52 +08:00 | A / Codex | dev/a | Push 10

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（修复 Push 9 的 CI；未创建 PR） |
| 做了什么 | 修复真实 PostgreSQL 集成测试失败：`PostgresUserSessionRepository.findValidByTokenHashes` 原 Drizzle `.for("update", { of: userSessions })` 生成 `FOR UPDATE of "app"."user_sessions"`，PostgreSQL 报 `FOR UPDATE must specify unqualified relation names`；改为原生 SQL 使用表别名 `us`，锁定子句为 `FOR UPDATE OF us`。同时补上 Push 9 漏提交的 `apps/web/src/generated/api` 客户端与类型。 |
| 新增功能 | 无新增功能；属上一纵切片的 CI 修复。 |
| 修复 / 重构 / 优化 | 只改变 Session 查回的锁查询实现，保持同一事务、实时用户状态与 `auth_version` 校验语义不变；生成物随本提交进入仓库。 |
| 测试与验证 | Push 9 的 CI 已确认失败于上述 PostgreSQL 语法；本地实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（7 文件 48 用例）、`pnpm lint`、`pnpm format:check`。真库流程待本推送后的 CI 验证；本机 Docker Desktop 未启动，无法本地复跑 `pnpm test:integration`。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更。 |
| 风险与后续 | CI 真库门禁是本次唯一待确认项；原 SQL 手工审查后已按 PostgreSQL 锁语法改写，仍需远端真实库验证。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:47 +08:00 | A / Codex | dev/a | Push 9

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 完成认证支柱第一条纵切片：`GET /api/v1/auth/csrf` / `issueCsrfToken`。实现匿名预认证 Session 的 CSRF 签发与轮换，以及有效认证 Session 的 CSRF Hash 存取基础链路。 |
| 新增功能 | 新增 CSRF 签发端点，匿名访问时创建/复用有效预认证材料并轮换 `__Host-preauth` Cookie；携带有效认证 Session 时在同一 Session 上签发新 CSRF 并清空预认证 Cookie；失效或停用凭据按匿名安全语义处理；契约登记为 ADR-023 `securityFlow`，OpenAPI/TypeScript 客户端同步生成。 |
| 修复 / 重构 / 优化 | 契约校验明确 `issueCsrfToken` 虽是 GET，仍必须且只能登记 `securityFlow`，不能按普通 `none` 处理；`VersionedHmacKeyring` 暴露全部 key 版本，`SessionTokenService` 增加按旧版本生成候选令牌哈希的能力，支持后续 keyring 轮换查询。 |
| 测试与验证 | 本地实测 `pnpm check` 全部通过：lint、format、typecheck、单测（`apps/api` 7 文件 48 用例、`packages/api-contract` 3 文件 51 用例、`database` 5 用例、`apps/web` 15 用例）、迁移一致性、契约漂移、Route Registry/权限矩阵、web/api 构建、依赖与前端边界、依赖审计、Secret 扫描、文档检查。`pnpm test:integration` 未运行：本机 Docker Desktop 未启动，无可用 PGroonga PostgreSQL 18.6 实例；新增真库用例 `apps/api/test/auth-session.integration.test.ts` 待 CI 确认。 |
| 文档同步 | 同步 [ADR-019](./docs/adr/ADR-019.md)、[技术设计v1.2.2](./技术设计v1.2.2.md)、[系统设计文档v1.0.2](./系统设计文档v1.0.2.md) 中 `issueCsrfToken` 的例外说明，以及生成物。测试矩阵未改动：CSRF 完整生命周期仍由 SEC-003/SEC-005 记录为 Required，本次未宣称完成 E2E。 |
| 风险与后续 | 真库 CSRF 存取与并发保留策略尚未在 CI 验证；`AuthModule` 仍在配置 `SESSION_HASH_KEYRING_FILE` 后挂载，生产 Secret 就绪后需正式固定；后续继续密码校验、Argon2id、preauth 原子消费、`user_sessions` 签发与 `/me`，并建立 Nest Zod Pipe/Serializer、限流与生产 Fetch Metadata 加固。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:20 +08:00 | A / Codex | dev/a | Push 8

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 放弃 Push 7 的“数据库时钟”helper：`createDatabaseClient` 内部 `drizzle()` 会把 postgres-js 的 OID 1184 parser 替换为透明解析器（返回字符串），导致 `dbFutureExpiry` 拿到的 `expires_at` 是字符串，Drizzle 插入时 `value.toISOString is not a function`。最终改为用 JS 时钟生成 9 分钟有效期（比数据库 10 分钟上限留 60 秒余量），满足 `preauth_sessions_expiry_check` 且避免跨时钟偏差。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 真库测试有效期生成不再依赖被 Drizzle 改写的 parser；过期用例仍以 `consumeOnce` 的未来 `consumedAt` 参数模拟。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（5 文件 40 用例）、`pnpm lint`。真库 4 用例待 CI 确认。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 待 CI 真库集成结果。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:16 +08:00 | A / Codex | dev/a | Push 7

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 修正 `preauth-session.integration.test.ts` 的时钟来源：改用数据库 `now()` 生成 `expires_at`（9 分钟），避免 JS 与 PostgreSQL 时钟微偏差触发 `preauth_sessions_expiry_check`（`expires_at <= created_at + 10 minutes` 在边界上被拒绝）；过期用例改为向 `consumeOnce` 传入未来 `consumedAt` 模拟已过期，不再用 SQL 覆写 `expires_at` 绕过检查。 |
| 新增功能 | 无新增功能。 |
| 修复 / 重构 / 优化 | 真实库测试不再依赖客户端与数据库时钟一致；有效期计算移到事务外，避免事务内跨连接查询。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（5 文件 40 用例）、`pnpm lint`。真库 4 用例待 CI PGroonga 探针步骤确认。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 同上继待 CI 真库验证。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:12 +08:00 | A / Codex | dev/a | Push 6

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 修复 `PostgresUnitOfWork` 在真实 PostgreSQL 下的事务上下文构造：postgres-js `sql.begin` 回传的 `TransactionSql` 没有顶层 `Sql.options`，Drizzle `drizzle(txSql, { schema })` 会因 `client.options.parsers` 为 undefined 而失败；现在将父连接 `client.sql.options` 注入事务 SQL 后再构造 Drizzle `db`，`run` 把父连接传给事务工厂。 |
| 新增功能 | 无新增功能；使已有 `UnitOfWork/TransactionContext` 真正可用于真实库查询，也是新加的 `preauth_sessions` 集成测试能跑起来的必要修复。 |
| 修复 / 重构 / 优化 | 此前 `unit-of-work.test.ts` 用带 `options` 的假 tx 掩盖了该问题，真实库从未执行过 `PostgresUnitOfWork`；本次同时补充了单测对父连接 options 的断言。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（5 文件 40 用例）、根级 `pnpm build`、`pnpm format:check`、`pnpm lint`、`pnpm check:deps`（95 源文件）、`pnpm check:secrets`（209 文件）、`pnpm check:docs`、`pnpm contract:drift`、`pnpm contract:validate`。Push 5 的 CI 真库集成曾失败在 `TypeError: Cannot read properties of undefined (reading 'parsers')`，本修复后再由 CI 验证。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更。 |
| 风险与后续 | `TransactionContext.sql` 目前仍保持为原始 `TransactionSql`，后续原始 SQL 存储/查询请复用同一事务连接；真实库 `preauth_sessions` 4 用例待 CI 确认。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 10:07 +08:00 | A / Codex | dev/a | Push 5

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 建立认证/会话支柱的基础层：`apps/api/src/auth/keyring.ts`（版本化 Session HMAC keyring，`SESSION_HASH_KEY_VERSION` + `SESSION_HASH_KEYRING_FILE`，仅允许 `/run/secrets/*`，缺配置/坏格式/密钥不足 32 字节 fail closed）、`token.ts`（32 字节 base64url 不透明令牌 + HMAC-SHA-256 32 字节哈希 + 常量时间比较）、`preauth-session.repository.ts`（真实 PostgreSQL `preauth_sessions`：创建、按令牌哈希查回、`consumeOnce` 原子一次性消费）、`session-token.service.ts`（签发一次性的预认证 Session Token 与 CSRF Token，只存哈希）与 `auth.module.ts`。 |
| 新增功能 | 认证/会话支柱的密钥管理与预认证 Session 存取；新增单测 `session-auth.test.ts`（10 用例）与真实 PostgreSQL 集成测试 `preauth-session.integration.test.ts`（4 用例，纳入 `apps/api test:integration`，CI 真库步骤运行）。 |
| 修复 / 重构 / 优化 | `apps/api` 单元测试改为排除全部 `*.integration.test.ts`，集成测试由专用 `vitest.integration.config.ts` 驱动；`test:integration` 显式排除仍在单独 `test:search:db` 跑动的搜索集成测试。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（5 文件 40 用例）、根级 `pnpm build`、`pnpm lint`、`pnpm format:check`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm check:deps`（95 源文件）、`pnpm check:frontend:boundaries`、`pnpm permissions:check`、`pnpm check:secrets`（209 文件）、`pnpm check:docs`（46 Markdown）。真实 PostgreSQL 集成测试本机未运行（无 PGroonga/迁移实例），交由 CI PGroonga 探针步骤验证。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更（未新增 HTTP 路由，`routeRegistry` 仍为 1 条）。 |
| 风险与后续 | `AuthModule` 尚未挂入 `AppModule`（生产 Secret 就绪前保持独立，避免健康探针启动被未配置 keyring 阻断）；下一步是校验预认证材料并落地 `issueCsrfToken`/`login` 端点（需同步 Route Registry + Schema Registry + 权限矩阵 + OpenAPI/客户端）。keyring 文件格式为当前实现定义的 `version:hexkey` 行式格式，尚无正式部署文档，建议后续补 keyring 格式与轮换 Runbook。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 09:56 +08:00 | A / Codex | dev/a | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 实现幂等协议引擎 `apps/api/src/idempotency/runner.ts`：`IdempotencyRunner` 在单个 `UnitOfWork` 内完成 插入 PENDING → 冲突判定 → 同事务执行 → `markSucceeded`（或重放/409），并把业务命令抛错/保存失败映射为整块事务回滚；冲突者按摘要/契约版本/密钥版本判定 `hash`/`contract-version`/`key-version`/`in-progress`，重放前调用 `ReplayAuthorizer` 完成重放授权重验。 |
| 新增功能 | 幂等中间件/写接口幂等接入所需的协议核心；新增 `apps/api/test/idempotency-runner.test.ts`（8 用例）。 |
| 修复 / 重构 / 优化 | 无既有行为变更；为后续 Nest 拦截器与鉴权层提供可注入的协议引擎。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（4 文件 30 用例）、`pnpm --filter @inpulse/api build`、根级 `pnpm lint`、`pnpm check:deps`（89 文件）、`pnpm check:secrets`（200 文件）、`pnpm check:docs`（46 Markdown）。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更（实现性代码，未新增/变更路由）。 |
| 风险与后续 | 协议引擎仅被单测覆盖，尚未接入 Nest 拦截器/真实写路由与认证会话；需先建立鉴权/会话与至少一个 `idempotencyRequired` 写接口（含 Route Registry + Schema Registry + 权限矩阵同步）后才能端到端验证；重放授权 `ReplayAuthorizer` 需按技术设计实现当前认证/资源可读/重认证新鲜度校验。 |
| 备注 | 推送 `dev/a`；未进入 `main`。 |

### 2026-09-08 09:50 +08:00 | A / Codex | main | Merge

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `main`（经 PR #25） |
| PR / commit | [#25](https://github.com/256-code/InPulse/pull/25)、`f2d5e72` Merge pull request #25 from 256-code/dev/a |
| 做了什么 | 将 `dev/a` 上的幂等实现（请求摘要 + Postgres 幂等存储 + 数据库运行期出口修复 + secret 扫描假阳性修复）经 PR #25 合入 `main`；随后把 `main` 同步回 `dev/a`（fast-forward 到 `f2d5e72`）并推送。 |
| 新增功能 | `apps/api` 幂等请求摘要与基于 `app.idempotency_records` 的 Postgres 幂等存储；修复 `@inpulse/database` 运行期 `dist` 出口。 |
| 修复 / 重构 / 优化 | 解除 `node dist/main.js` 无法启动；修复 `check:secrets` 假阳性。 |
| 测试与验证 | PR #25 的 CI：workspace 与 docs 检查均成功；合并到 `main`。 |
| 文档同步 | 仅本开发日志。 |
| 风险与后续 | 合并为 merge commit 而非 squash（由 `256-code` 账号合并）；后续继续幂等中间件与写接口幂等接入。 |
| 备注 | 已并入 `main`；`dev/a` 已同步。 |

### 2026-09-08 09:41 +08:00 | A / Codex | dev/a | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 修复 `check:secrets` 假阳性：`apps/api/test/idempotency.test.ts` 中测试变量 `hmacKey` 被 `scripts/check_secrets.mjs` 的敏感字段正则（`hmac[_-]?key` + 字面量赋值）误判为“敏感字段不得赋字面量值”，重命名为 `hmacMaterial`，并把测试字面量改为 `test-versioned-material` / `other-versioned-material`，消除 CI `check:secrets` 失败。 |
| 新增功能 | 无用户可见业务；纯测试/CI 门禁修复。 |
| 修复 / 重构 / 优化 | 根因：`check_secrets.mjs` 按变量名识别敏感字段，`hmacKey` 命中；测试使用的是假 HMAC 材料，不是真实 Secret。未放宽扫描规则，仅调整非敏感测试命名与字面量。 |
| 测试与验证 | 实测通过：`pnpm check:secrets`（检查 198 个受版本控制/待提交文件）、`pnpm --filter @inpulse/api test:unit`（3 文件 22 用例）、`pnpm exec prettier --check apps/api/test/idempotency.test.ts`。 |
| 文档同步 | 仅本开发日志；无 ADR/设计/权限矩阵/测试矩阵变更。 |
| 风险与后续 | 前两条 `dev/a` 推送的 CI 曾红在 `check:secrets`，本修复应解开该失败；仍待 CI 全绿确认，并继续幂等中间件等后续实现。 |
| 备注 | 推送到 `dev/a`；尚未进入 `main`。 |

### 2026-09-08 09:23 +08:00 | A / Codex | dev/a | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` + `chore` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 解除生产运行期断点：为 `@inpulse/database` 新增 `database/tsconfig.build.json` 与 `build` 脚本，把 `schema/**`、`src/client.ts`、`src/config.ts` 编译到 `database/dist`；`database/package.json` 的 `exports` 改为 `types` 指向源码、`default` 指向 `dist/*.js`（`./migrate` 仍为源码 CLI，未在运行期消费）；在 `apps/api/vitest.config.mjs` 增加 `resolve.alias`，把 `@inpulse/database*` 指向源码，保证 vitest 不依赖 `dist`。 |
| 新增功能 | 无用户可见业务；修复 PR #24 遗留的“typecheck/build 绿但 `node dist/main.js` 起不来”问题，使 `apps/api` 可真正启动并响应 `/api/v1/health`。 |
| 修复 / 重构 / 优化 | 根因修复：此前 `@inpulse/database` 只提供 `.ts` 源码出口，Node 运行期不能把 `.js` 说明符映射到 `.ts`，导致 `ERR_MODULE_NOT_FOUND`。 |
| 测试与验证 | 实测通过：`pnpm install --frozen-lockfile`、`pnpm --filter @inpulse/database build`（产出 `dist/schema/*.js`、`dist/src/client.js`、`dist/src/config.js`）、`pnpm --filter @inpulse/database typecheck`、`pnpm --filter @inpulse/database test:unit`（5 用例）、`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（22 用例）、根级 `pnpm build`（database 先构建）、`pnpm -r typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm check:deps`（88 文件无循环/越界）、`pnpm check:docs`（46 Markdown 有效）；`node dist/main.js` 设置 `DATABASE_URL` 后启动，`GET /api/v1/health -> 200 {"status":"ok"}`（注：`RUNTIME` 用途读取 `DATABASE_URL`，非 `RUNTIME_DATABASE_URL`）。真实 PostgreSQL 集成测试（含 PGroonga）本机未运行。 |
| 文档同步 | 无 ADR/设计文档变更（属 [ADR-028](./docs/adr/ADR-028.md) 既定方向的实现）；仅本开发日志。 |
| 风险与后续 | `database/dist` 为 gitignore 构建产物，CI/部署需先 `pnpm build`（`pnpm -r build` 拓扑序已覆盖）；`./migrate` 仍为源码出口（CLI 由 `tsx src/migrate.ts` 运行，运行期未消费）；本机无 PGroonga，真实库集成测试未运行。幂等 store/中间件仍为下一部分。 |
| 备注 | 推送到 `dev/a`；尚未进入 `main`。 |

### 2026-09-08 09:14 +08:00 | A / Codex | dev/a | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 实现 Postgres 幂等存储 `apps/api/src/idempotency/store.ts`：`PostgresIdempotencyStore` 基于已存在的 `app.idempotency_records` 表，所有方法显式接收 `UnitOfWork/TransactionContext`，使用事务绑定的 `tx.db`（不创建自己的 UoW、不使用全局 Drizzle Client），提供 `insertPending`（PENDING，冲突返回 `undefined`）、`find`、`markSucceeded`（PENDING→SUCCEEDED + 响应/replay 元数据）；纯逻辑 `hexDigestToRequestHash` / `defaultIdempotencyExpiry` / `isReplayMatch` / `validateSuccessResponse`。新增 `apps/api/test/idempotency-store.test.ts`（8 用例）；把 `test:unit` 改为 `vitest run --exclude "**/search-query.integration.test.ts"`，自动纳入所有单元测试并排除集成测试。 |
| 新增功能 | 无用户可见业务；为幂等中间件提供与业务写同一事务的幂等记录存取能力。 |
| 修复 / 重构 / 优化 | `test:unit` 由显式用例清单改为 glob + 排除集成测试，消除后续新增用例需手动同步的维护点。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（22 用例：3 UoW + 11 digest + 8 store）、`pnpm exec prettier --write apps/api/src/idempotency apps/api/test/idempotency-store.test.ts apps/api/package.json`、`pnpm exec eslint apps/api/src/idempotency apps/api/test/idempotency-store.test.ts`。本机无 PGroonga，真实 PostgreSQL 集成测试未运行；`PostgresIdempotencyStore` 的 SQL 执行路径（`onConflictDoNothing` / `returning` / `update returning`）需后续在 CI 的 PGroonga 探针镜像上做真实库集成验证。 |
| 文档同步 | 无 ADR/设计/权限矩阵/测试矩阵变更；仅本开发日志。 |
| 风险与后续 | 本切片仅完成幂等记录数据访问层；尚未接入 Nest 中间件（先鉴权→插入幂等行→执行→同事务 `markSucceeded`→重放再鉴权）、replay 授权重验与语义冲突 `409`（需中间件基于 `isReplayMatch` 判定）。写入 `replayAuthContext` 时严禁含 Cookie/CSRF Token/MFA Secret/验证码/恢复码（约束由写入方遵守）。真实库集成测试未运行。 |
| 备注 | 推送到 `dev/a`；尚未进入 `main`。 |

### 2026-09-08 09:07 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审确认） |
| 做了什么 | 为 F-11 幂等中间件落地第一块地基：新增 `apps/api/src/idempotency/jcs.ts`（RFC 8785 JCS 确定性序列化）与 `digest.ts`（版本化 HMAC-SHA-256 幂等请求摘要）；把 `test:unit` 脚本纳入新测试；新增 `apps/api/test/idempotency.test.ts`（11 用例）。 |
| 新增功能 | 无用户可见业务；提供确定性请求摘要核心。摘要覆盖大写 method、operationId、幂等契约版本、摘要格式、请求 Schema 版本、Schema 解析后的 path 参数、规范 query、规范 Content-Type、Route Registry 声明的行为相关请求头、JCS 规范化 body 与 `If-Match`；明确排除 Cookie、Authorization、CSRF、追踪头、Idempotency-Key。 |
| 修复 / 重构 / 优化 | 引入 JCS 规范化（对象键排序、最短数字表示、字符串转义固定），并 fail-closed 拒绝非有限数字与非 JSON 安全值，避免请求被等效性误判。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api test:unit`（14 用例：3 个 UnitOfWork + 11 个 idempotency）、`pnpm exec prettier --check apps/api/src/idempotency apps/api/test/idempotency.test.ts apps/api/package.json`、`pnpm exec eslint apps/api/src/idempotency apps/api/test/idempotency.test.ts`。本机无 PGroonga，真实 PostgreSQL 集成测试未运行；`node dist/main.js` 仍受 `@inpulse/database` 无 dist 影响未启动（见上一条记录）。 |
| 文档同步 | 无 ADR/设计/权限矩阵/测试矩阵变更；仅本开发日志。 |
| 风险与后续 | 本切片仅为摘要核心，尚未接入 Route Registry / 数据库幂等记录 / Nest 中间件拦截与重放鉴权；后续小步：幂等记录迁移 + Postgres 幂等存储（复用 UnitOfWork/TransactionContext）+ 中间件（先鉴权→幂等行→执行→同事务存响应→重放再鉴权）+ replay 授权重验与语义冲突 409。`test:unit` 为显式用例清单，后续新增用例需同步更新；未推送。 |
| 备注 | 推送到 `dev/a`；尚未进入 `main`。 |

### 2026-09-07 23:20 +08:00 | A / Codex | feature/api-db-uow | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/api-db-uow`（基于 `dev/a`） |
| PR / commit | 待创建 |
| 做了什么 | 将 `@inpulse/database`（`./client`、`./config`）接入 `apps/api`：新增 `DatabaseModule`（提供 `DATABASE_CLIENT` 连接池与 `UnitOfWork`）、`UnitOfWork`/`PostgresUnitOfWork`、`TransactionContext`；把 `DatabaseModule` 挂进 `AppModule`；`@inpulse/database` 增加 `./config` 导出；`apps/api` 增加 `@inpulse/database`、`drizzle-orm` 依赖与 `test:unit` 脚本；新增 `unit-of-work.test.ts`。 |
| 新增功能 | 无用户可见业务功能；为 F-11 打下数据库接入与“一个命令一个 `UnitOfWork`、所有写操作显式传递同一 `TransactionContext`、事务回调禁用全局 Drizzle Client”的事务边界。 |
| 修复 / 重构 / 优化 | 无业务行为变更；引入数据库事务抽象与 NestJS 数据层基础。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`（含 `tsconfig.test.json`）、`pnpm --filter @inpulse/api build`、根级 `pnpm build`、`pnpm lint`、`pnpm -r typecheck`、`pnpm --filter @inpulse/api test:unit`（3 用例全通过，覆盖 `run` 仅调用一次 `sql.begin`、回调抛错传播、默认工厂返回 `db/sql`）。真实 PostgreSQL 集成测试（含 PGroonga）本机未运行——本机 PostgreSQL 18.6 无 PGroonga。`node dist/main.js` 启动失败：`@inpulse/database` 的 `exports` 仍指向 `src/*.ts`/`schema/*.ts`，运行期 `ERR_MODULE_NOT_FOUND: database/schema/index.js`（`.js` 说明符不会映射到 `.ts`）——已知未解决风险。 |
| 文档同步 | 无设计/ADR/权限矩阵/测试矩阵变更；仅本开发日志。 |
| 风险与后续 | 生产运行期 `node dist/main.js` 暂时无法启动，依赖后续为 `@inpulse/database` 产出 `dist` 并切换 `exports`（独立小步，建议先记录/决策，不静默改）。本机未运行真实 PostgreSQL 集成测试。下一步：幂等中间件（按 AGENTS 序：先鉴权→幂等行→执行→同事务存响应→重放再鉴权）。 |
| 备注 | 本次未推送；按用户此前授权“作者可直接并入”，推送前再次确认风险。 |

### 2026-09-07 22:54 +08:00 | A / Codex | refactor/api-esm-nodenext | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `refactor` + `docs` |
| 分支 | `refactor/api-esm-nodenext`（基于 `dev/a`） |
| PR / commit | 待创建 |
| 做了什么 | 将 [ADR-028](./docs/adr/ADR-028.md) 状态由 `Proposed` 改为 `Accepted`（作者按用户指示），并把 `apps/api` 迁移到 ESM/NodeNext：`package.json` 增加 `"type": "module"`、`tsconfig.json` 改 `module: NodeNext`/`moduleResolution: NodeNext`（移除 CommonJS/Node10 覆盖，继承 `verbatimModuleSyntax: true`）、`src` 相对导入补 `.js` 后缀；同步 AGENTS.md 与 ADR 索引。 |
| 新增功能 | 无用户可见业务功能；为后续消费 `@inpulse/database`、`@inpulse/api-contract` 等 ESM 共享包铺路（DatabaseModule + UnitOfWork 仍属下一项）。 |
| 修复 / 重构 / 优化 | 迁移 `apps/api` 模块制式；`dist` 现产出 ESM，`start` 入口 `node dist/main.js` 可启动并响应 `/api/v1/health`。 |
| 测试与验证 | 实测通过：`pnpm --filter @inpulse/api typecheck`、`pnpm --filter @inpulse/api build`、根级 `pnpm build`、`pnpm contract:validate`、`pnpm contract:drift`、`pnpm permissions:check`；`node dist/main.js` 启动后 `GET /api/v1/health -> 200 {"status":"ok"}`。`apps/api` 集成测试（`test:search:db`）需真实 PostgreSQL + PGroonga，本机未运行；消费 `@inpulse/database` 未在本 PR 实现。 |
| 文档同步 | `docs/adr/ADR-028.md`（`Proposed`→`Accepted` + 接受记录）、`docs/adr/README.md`、`AGENTS.md`。 |
| 风险与后续 | 按 ADR-028 第 7 节第 8 项，正式定案仍需非作者（B 或 C）人工确认；本改动为 ESM 化 + 决策定案，未改变 API 行为。后续接入 `DatabaseModule + UnitOfWork/TransactionContext` 时再落地 `@inpulse/database` 消费。 |
| 备注 | 本次为代码 + 文档改动，尚未推送/创建 PR。 |

### 2026-09-07 22:46 +08:00 | A / Codex | docs/adr-028-esm-api | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/adr-028-esm-api`（提案 ADR-028，待建 PR） |
| PR / commit | 待创建 |
| 做了什么 | 按用户选择「第 1 条：apps/api 迁 ESM/NodeNext 以消费共享 ESM 包」形成 [ADR-028](./docs/adr/ADR-028.md) 定案材料。说明背景（`apps/api` 为 CJS+Node10，`@inpulse/database`/`@inpulse/api-contract` 为 ESM-only，Node10 无法解析、生产 `node dist` 会指向 `.ts`，ADR-026 已记录）、决策（`module: NodeNext`、`"type": "module"`、相对导入补 `.js`、生产 `node dist/main.js`）、替代方案与权衡、后果、未决范围、影响与验证（明确标注迁移尚未实施），以及转为 Accepted 前的评审清单；并同步 `docs/adr/README.md` 索引加入 ADR-028（`Proposed`）。 |
| 新增功能 | 无业务功能；属架构决策记录。 |
| 修复 / 重构 / 优化 | 无代码变更，仅文档。 |
| 测试与验证 | `pnpm check:docs` 待本改动后运行；本 PR 为纯文档，CI 由 `Documentation / docs` 覆盖；迁移实现（`.js` 后缀、`typecheck/build/test`）留待本 ADR 转 `Accepted` 后的独立实现 PR。 |
| 文档同步 | 新增 `docs/adr/ADR-028.md`；`docs/adr/README.md` 增加 ADR-028 行。尚未触碰三份设计文档与 `AGENTS.md`/`CONTRIBUTING.md`/`README.md` 的 `Proposed` 表述（待 ADR-028 定案时同步）。 |
| 风险与后续 | ADR-028 为 `Proposed`，需非作者（B 或 C）人工评审；评审通过后再实现 `apps/api` 的 ESM 迁移，随后接入 `DatabaseModule + UnitOfWork/TransactionContext`。 |
| 备注 | 本次仅文档；尚未创建交付分支/PR。 |

### 2026-09-07 22:28 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/a` |
| PR / commit | 直接推送 `dev/a`（未创建 PR；如需进入 `main` 须另建 PR 并由非作者评审人确认） |
| 做了什么 | 按用户指示将 [ADR-027](./docs/adr/ADR-027.md) 状态由 `Proposed` 改为 `Accepted`，并同步 `docs/adr/README.md`、`AGENTS.md`（第 4/13 节）、`README.md`、`CONTRIBUTING.md` 中仍称 `Proposed` 的表述；在 ADR-027 正文补充接受记录与“非作者人工确认”说明。 |
| 新增功能 | 无业务功能；属决策记录状态定案。 |
| 修复 / 重构 / 优化 | 无代码变更，仅文档。 |
| 测试与验证 | 本会话实测通过：`pnpm install --frozen-lockfile`、`pnpm contract:drift`、`pnpm contract:validate`、`pnpm permissions:check`、`pnpm --filter @inpulse/api-contract test:unit`（3 文件 50 用例）；`contract:generate` 未重跑（会写生成物），由 `contract:drift` 按字节比对等效验证；`pnpm check:docs` 已通过（45 个 Markdown 链接/锚点有效）。 |
| 文档同步 | `docs/adr/ADR-027.md`（`Proposed`→`Accepted` + 接受记录）、`docs/adr/README.md`、`AGENTS.md`、`README.md`、`CONTRIBUTING.md`。 |
| 风险与后续 | 按 ADR-027 第 7 节第 8 项，正式定案仍需非作者评审人（B 或 C）人工确认；本项仅为文档状态变更，未改任何代码、生成物或迁移。 |
| 备注 | 本次改动已按用户指示直接推送到 `dev/a`；因作者（A）不得自批准，正式定案仍需非作者评审人（B 或 C）人工确认。 |

### 2026-09-07 21:44 +08:00 | A / Codex | docs/adr-027-contract-generation | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/adr-027-contract-generation`（自 `origin/main` 创建，head `a73fdb8`） |
| PR / commit | 待创建。本分支只含 ADR-027 变更。 |
| 做了什么 | 完善契约生成链路的 ADR-027 定案材料：修正 Controller 绑定表述（按 method+path 匹配，而非字面 operationId），补充替代方案与权衡、后果、本 ADR 未决范围（Nest Zod Pipe/Serializer 与运行参数、运行期 operationId 绑定仍待定案）以及“转为 Accepted 前的评审清单”。 |
| 新增功能 | 无业务功能；属决策记录文档。 |
| 修复 / 重构 / 优化 | 无代码变更，仅文档。 |
| 测试与验证 | 本地 `node scripts/check_docs.mjs` 通过（45 个 Markdown 链接/锚点有效）；非数据库门禁与 GitHub Actions 由本 PR 的 `Documentation / docs` 与 `CI / workspace` 覆盖确认。 |
| 文档同步 | 仅完善 ADR-027 正文；状态保持 `Proposed`，待人工评审后由非作者改为 `Accepted`，再同步 README/CONTRIBUTING/AGENTS.md 中仍称 `Proposed` 的表述。 |
| 风险与后续 | 状态仍为 `Proposed`，需人工评审（契约平台共享基建可由 B 或 C 评审）。 |
| 备注 | 本分支仅承载 ADR-027 变更，与依赖审计修复分开交付。 |

### 2026-09-07 21:39 +08:00 | A / Codex | dev/a | 推送前记录（待建交付分支）

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix`（依赖与 CI 门禁）+ `deps` |
| 分支 | `dev/a`（尚未建交付分支；推送前应同步最新 `main` 并创建短交付分支） |
| PR / commit | 待创建。本次改动尚未推送。 |
| 做了什么 | 修复 `CI / workspace` 在 `Dependency audit` 步骤失败：`apps/web` 测试工具链的传递依赖 `ansi-regex@5.0.0`（GHSA-93q8-gq69-wqmw，high，补丁 `>=5.0.1`）触发 `pnpm deps:audit --audit-level=high` 失败。 |
| 新增功能 | 无新增业务功能。 |
| 修复 / 重构 / 优化 | 在 `pnpm-workspace.yaml` 增加 `overrides` 把 `ansi-regex` 固定到 `^5.0.1`，`pnpm-lock.yaml` 相应由 `5.0.0` 升到 `5.0.1`。该依赖为 `apps/web` devDependencies（`@testing-library/*` -> `@testing-library/dom` -> `pretty-format@27.0.2`）的传递依赖，不进入生产构建。 |
| 测试与验证 | 本地执行并全部通过：`pnpm install --frozen-lockfile`；`pnpm deps:audit`（`No known vulnerabilities found`）；完整非数据库门禁 `pnpm check`（含 lint、format:check、typecheck、test:unit 共 70 例、db:migrations:check、contract:drift、contract:validate、build、check:deps、check:frontend:boundaries、permissions:check、deps:audit、check:secrets、check:docs）；`node scripts/check_docs.mjs`（45 个 Markdown）。`db:migrate`、`test:integration` 与 GitHub Actions 运行本身未在本地重跑，需由交付 PR 在 CI 上确认。 |
| 文档同步 | 更新 `AGENTS.md` §13、`CONTRIBUTING.md`、`docs/test-matrix.md`：把 `deps:audit` 由失败改为已修复（`ansi-regex@5.0.1`）。 |
| 风险与后续 | 该变更为 dev 工具链依赖，按 `AGENTS.md` 第 4 节仍须作为独立依赖升级 PR 并由人工确认，不得自动合并；交付 PR 需在 GitHub Actions 确认 `CI / workspace` 转绿后再合入。另工作树还保留上一条 `docs/adr/ADR-027.md` 文档改动，属独立逻辑变更，应分开交付。 |
| 备注 | 本次改动未推送；本条为推送前记录，待建交付分支时回填 PR/commit 与推送序号。 |

### 2026-09-07 17:14 +08:00 | A / Codex | main | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `main`（维护者显式授权的直接推送）与 `dev/a`（同一提交） |
| PR / commit | 无 PR。`origin/main` 由 `7351c4e` 快进到本条日志提交，其中包含 `586a13b`（§12.4 CI 最小链路）与两次同步合并 `fd542f9`（after PR 15）、`ad834f2`（after PR 18）；推送前交付分支 `ci/phase0-minimal-gate-chain` 指向同一提交 `0545443`，推送后已删除 |
| 做了什么 | 按维护者本次会话中的显式指令（「合并完成以后将其上传到 main 分支」「我同意你进行推送」），把已包含全部 `origin/main` 的 `dev/a` 直接快进推送到 `main`。未使用 `--force`，未重写历史，未移动标签，未修改远端仓库设置 |
| 新增功能 | 无。进入 `main` 的内容为阶段 0 工程基座与 §12.4 CI 最小链路，明细见下方 17:04 记录 |
| 修复 / 重构 / 优化 | 本次仅推送，无新增代码改动 |
| 测试与验证 | 维护者指示「不需要跑门禁了」，故推送前只在最终树上复跑了 `pnpm check:secrets`（186 个文件通过）与 `pnpm format:check`（通过），未再执行其余门禁。其余状态沿用 17:04 记录的实测结果：`lint`、`typecheck`、`test:unit`（70 例）、`db:migrations:check`（6 条）、`contract:drift`（5 个生成物）、`contract:validate`、`build`、`check:deps`（81 文件）、`check:frontend:boundaries`、`permissions:check`、`check:docs`（45 Markdown）本地通过；`pnpm deps:audit` 为 1 个 high 失败（`ansi-regex@5.0.0`）；`db:migrate`、`test:integration`、`db:test:local`、`test:search:db`、`test` 与 GitHub Actions 运行本身均未执行。因此推送后 `main` 上的 `CI / workspace` 预计在 Dependency audit 步骤失败 |
| 文档同步 | 仅本开发日志。三份设计文档、ADR、权限矩阵、测试矩阵无变化 |
| 风险与后续 | 1) 本次推送绕过 `CONTRIBUTING.md` 第 15/18/52/138 行（`main` 禁止直接推送、岗位长期分支不得作为进入 `main` 的 PR 来源、AI 编码代理不得直接合并代码、服务端保护规则完成前不得直接推送或绕过失败检查）与开发工作书 6.2 的非作者人工评审要求，唯一依据是维护者在会话中的显式授权；`main` 的服务端保护状态按 `CONTRIBUTING.md` 第 141 行仍无法核验（接口返回 403）。2) 高风险变更（GitHub workflow、根 `package.json`、`pnpm-lock.yaml`、数据库脚本）未经非作者评审即进入 `main`，建议补一次追溯评审。3) `deps:audit` 仍为红，须由独立依赖 PR 把 `ansi-regex` 提升到 `>=5.0.1`，不得调低阈值规避。4) CI-017 Playwright、CI-018 容器镜像与 Compose 渲染、CI-019 镜像扫描仍未落库。5) 建议管理员尽快启用 `main` 分支保护并把 §12.4 门禁设为 required checks，避免再次出现直接推送 |
| 备注 | 本条如实记录一次治理例外，不代表该做法被批准为常规流程 |
### 2026-09-07 17:04 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `dev/a`（Push 1 已推送至 `origin/dev/a`，为快进推送 `a26e7e4..` 加本条日志提交；按 `CONTRIBUTING.md` 第 15/18/52 行，`main` 禁止直接推送、岗位长期分支不得作为进入 `main` 的 PR 来源、AI 编码代理不得直接合并代码，因此未推送 `main`，也未由代理创建 PR） |
| PR / commit | 上一条 15:25 记录所述工作区改动已作为提交 `586a13b` 落库；随后两个合并提交：`fd542f9`（sync origin/main after PR 15）与 `ad834f2`（sync origin/main after PR 18）。推送前已再次 `git fetch origin` 复核：`origin/main` 仍为 `7351c4e`，且 `git merge-base --is-ancestor origin/main HEAD` 为真，无需第三次同步（下方风险第 6 项已消除）。进入 `main` 仍须由人工走交付分支 + PR + 非作者评审 + squash merge；本机 `gh auth status` 报 keyring 中 token 失效（账号 `shaoxiaoyu-D`），代理无法代为创建 PR，而 `git push` 凭据本身可用 |
| 做了什么 | 按用户要求把 `origin/main` 拉取合并进 `dev/a`。合并分两次完成，因为合并过程中后台 fetch 使 `origin/main` 前进：第一次并入 PR #13/#14/#15（`21e5589`：F-26 搜索服务、`020_pgroonga.sql` 与 `0003-0005` 正式迁移），第二次并入 PR #16/#17/#18（`7351c4e`：前端基座依赖、F-30 前端基座、vite alias 对齐） |
| 新增功能 | 无自有新功能。随 main 并入 `apps/api` 搜索服务与 `ProjectAccessQueryPort` 契约、`database` 的 `0003-0005` 迁移与 PGroonga bootstrap、`apps/web` F-30 前端基座（路由、鉴权守卫、错误边界、Ant Design 主题、路径别名、jsdom 单测）与 dependency-cruiser 规则 |
| 修复 / 重构 / 优化 | 冲突解决（第一次 9 处、第二次 7 处）：`.github/workflows/ci.yml` 保留 §12.4 顺序并新增「Build PostgreSQL 18.6 + PGroonga test image」「Verify PGroonga is available」「Bootstrap PGroonga extension」「Frontend boundary check」四步（迁移 `0003` 缺少 PGroonga 时 fail closed），`timeout-minutes` 30→45，并保留 main 关于 `test:search:db`/`test` 暂不入 CI 的既定决定；`package.json` 并入 `test:search:db`、`test:web`、`check:frontend:boundaries` 并把后者加入 `pnpm check`；`apps/web/package.json` 新增 `test:unit`，使 F-30 前端单测真正进入 §12.4 的 unit tests 门禁（此前 `pnpm -r test:unit` 会跳过 apps/web）；`pnpm-lock.yaml` 两次都用 `pnpm install --lockfile-only` 重新生成，未手工编辑；`scripts/check_dependencies.mjs` 把模块根目录下的 `*.port.ts` 认定为公开表面（第 3 节允许跨域读经稳定 QueryPort），修复 `search` 导入 `projects/project-access.port.ts` 被误判越界；`database/scripts/test-local.ps1` 的 PGroonga 探针误用 `pg_available_extensions.extname`（该视图列名为 `name`），在任何服务器上都会报错且 psql 失败后对 null 调用 `.Trim()` 抛 PowerShell 异常掩盖真因，已改为 `name` 并加固退出码判断，ci.yml 中沿用的同一查询一并修正；`开发日志.md` 用程序化并集重建（28 条条目正文逐字保留、无重复、无丢失），并去除 main 在 PR #18 引入的 UTF-8 BOM（其余 Markdown 与该文自身历史均无 BOM）；对 main 在 Prettier 基线之前提交的 21 个文件执行 `prettier --write`，已 token 级核验为纯格式变更（含 `AppRouter.tsx` 内联类型补 `;`、`HomePage.tsx` JSX 换行处插入 `{" "}` 以保留原有空格，渲染不变） |
| 测试与验证 | 本机 Windows / Node 24.20.0 / pnpm 11.19.0。`pnpm install --frozen-lockfile` 通过（lockfile 509 条目，与全部 manifest 一致）。实际执行并通过：`pnpm lint`、`pnpm format:check`、`pnpm typecheck`（database、api-contract、api 含 `tsconfig.test.json`、web）、`pnpm test:unit`（database 5 + api-contract 50 + apps/web 15 = 70 例）、`pnpm db:migrations:check`（6 条迁移哈希一致）、`pnpm contract:drift`（5 个生成物）、`pnpm contract:validate`（1 条路由）、`pnpm build`（api `tsc`；web `vite` 1549 modules，`index-uK4MO2AW.js` 683.76 kB 并触发 >500 kB chunk 警告）、`pnpm check:deps`（81 个源文件 / 81 个模块节点）、`pnpm check:frontend:boundaries`（dependency-cruiser：33 模块 / 69 依赖，无违规）、`pnpm permissions:check`（1 操作 / 1 路由）、`pnpm check:secrets`（186 个文件）、`pnpm check:docs`（45 个 Markdown）。负向验证：`check:deps` 的 Port 规则在临时新增 `modules/projects/project-access.repository.ts` 并从 `search` 导入后非零退出、删除后恢复；`contract:drift` 在篡改 `generated/openapi.json` 后非零退出、还原后通过。**失败**：`pnpm deps:audit` 报 1 个 high（`ansi-regex@5.0.0`，GHSA-93q8-gq69-wqmw，补丁 `>=5.0.1`），路径为 `apps/web` 的 `@testing-library/{jest-dom,react,user-event}` → `@testing-library/dom` → `pretty-format@27.0.2` → `ansi-regex@5.0.0`；已用 `git show MERGE_HEAD:pnpm-lock.yaml` 核对确认该解析来自 `origin/main` PR #16 提交的 lockfile，合并前 `dev/a` 不含 `@testing-library`。因此 `pnpm check` 整体非零退出。**未运行**：`pnpm db:migrate`、`pnpm test:integration`、`pnpm db:test:local`、`pnpm test:search:db`、`pnpm test`——本机 PostgreSQL 18.6（`POSTGRES_BIN`）不含 PGroonga，`db:test:local` 现按预期以「必须提供 PGroonga 扩展」失败（该失败同时证明上述列名修复已生效）；GitHub Actions 本身未运行，ci.yml 的探针镜像构建、`020_pgroonga.sql` bootstrap、空库迁移与集成测试步骤只有 js-yaml 解析（25 步）与命令级验证；Playwright E2E、生产容器镜像构建、Compose 渲染与 digest 校验、镜像扫描仍无脚本，未声称通过 |
| 文档同步 | `README.md`（状态表并入 F-30 前端基座与 §12.4 CI 链路；命令列表加入 `pnpm check:frontend:boundaries`；辅助命令加入 `pnpm test:web`；`db:migrate`/`test:integration` 注明需要 PGroonga）、`AGENTS.md` 第 8 节（命令清单加入 `test:web`/`check:frontend:boundaries`，如实记录 `deps:audit` 当前失败与本机 PGroonga 缺失）与第 13 节（区分「曾在 `0000-0002` 上本地实测」与「合并 `0003-0005` 后改由 CI 探针镜像覆盖」）、`CONTRIBUTING.md`（验证章节命令表与逐命令说明同步，`check:deps` 补充公开表面定义）、`docs/test-matrix.md`（CI-005 覆盖 apps/web、CI-013 补充 dependency-cruiser 与 Port 公开表面、CI-014 标为「已自动化（当前失败）」并在执行状态说明中给出完整依赖路径与不擅自修复的理由）。三份设计文档、`docs/permissions.md` 与 `docs/adr/*` 未改动：本次只做合并与门禁修正，未改变架构、API 语义、权限规则或事务不变量 |
| 风险与后续 | 1) **`deps:audit` 当前为红**：需人工批准一个独立依赖 PR 把 `ansi-regex` 提升到 `>=5.0.1`（pnpm override 或升级 `@testing-library/dom`/`pretty-format`）；在此之前 `pnpm check` 与 `CI / workspace` 会失败，不得通过调低 `--audit-level`、加 allowlist 或删除门禁规避。2) 本机缺 PGroonga，任何真实数据库门禁都无法本地验证；CI 的探针镜像构建步骤也未经真实 Actions 运行，首个 PR 上可能暴露 `docker build` 访问 `packages.groonga.org`/`apt.postgresql.org`、构建时长或 `docker exec` 交互问题，`timeout-minutes` 已提到 45 但可能仍需调整。3) `apps/web` 生产构建主 chunk 683.76 kB（gzip 223.01 kB）触发 Vite >500 kB 警告，需后续代码分割。4) `apps/web/package.json` 的 devDependencies 使用 `^` 范围（`@testing-library/*`、`dependency-cruiser`、`jsdom`），与仓库其余精确锁版惯例不一致，虽由 lockfile 锁定，仍建议人工决定是否收敛。5) 前端分层现有两套检查（`scripts/check_dependencies.mjs` 与 `apps/web/dependency-cruiser.config.cjs`），职责重叠，需人工决定是否收敛为单一真相。6) `origin/main` 在本次合并窗口内两次前进（PR #16/#17/#18），推送前应再次 fetch 确认是否需要第三次同步。7) ADR-027 仍为 `Proposed`；Route Registry 仍只有 `GET /health` 一条真实路由 |
| 备注 | 严格遵守「不得跳过或弱化失败测试」与「脚本落库前不得声称检查已通过」：未使用 `skip`、未降低断言、未删除用例、未调低审计阈值；`deps:audit` 的失败与全部未运行项均如实记录。上一条 15:25 记录写的是「全部停留在工作区」，事实上该批改动已作为 `586a13b` 提交以完成本次合并；按记录规则第 4 条以本条补充说明，不改写既有条目 |

### 2026-09-07 16:12 +08:00 | C / Codex | chore/web-vite-alias | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `chore/web-vite-alias`（基于最新 `main` 切出） |
| PR / commit | [#18](https://github.com/256-code/InPulse/pull/18)；功能提交 `378acd8` |
| 做了什么 | 将 Vite 的 `@app` / `@pages` / `@features` / `@shared` / `@generated` 路径别名与 `tsconfig` 对齐，并处理 Vite ESM 配置下 `__dirname` 的解析 |
| 新增功能 | 无用户可见业务功能；开发与构建时可正确解析应用层路径别名，修复 Vite 与 `tsconfig` 路径不一致问题 |
| 修复 / 重构 / 优化 | 修复 Vite 配置中缺少路径别名导致的别名解析差异；使用 Node ESM 兼容方式计算配置目录 |
| 测试与验证 | 实际运行通过：`pnpm -r typecheck`（5/6 workspace 执行，全部通过）、`pnpm test:web`（6/6 测试文件、15/15 用例通过）、`pnpm check:frontend:boundaries`（30 modules / 66 dependencies 无违规）、`pnpm check:docs`（44 个 Markdown 链接和锚点有效）、`git diff --check`（通过） |
| 文档同步 | 更新本开发日志；无设计、ADR、权限矩阵或测试矩阵变化 |
| 风险与后续 | PR #18 已创建，等待 CI 结果、非作者人工评审与 squash merge；合并后将最新 `main` 快进同步回 `dev/c`，消除当前 `dev/c` 领先 `main` 的 1 个提交 |
| 备注 | 本条为将 `dev/c` 上 `d9111b5` 的同步提交纳入 `main` 的合规 PR；Push 1 已回填 PR #18 编号 |

### 2026-09-07 16:15 +08:00 | C / Codex | feature/f-30-frontend-foundation | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/f-30-frontend-foundation`（基于最新已同步的 `dev/c` 切出） |
| PR / commit | [#17](https://github.com/256-code/InPulse/pull/17)；commit `a8c276c` |
| 做了什么 | 实现 F-30 前端基础框架与边界治理：搭建自动化路由聚合与声明式权限防重守卫、App Shell（Layout/Providers/ErrorBoundary）、409 数据冲突提示组件、Home 骨架页及 `dependency-cruiser` 分层架构门禁 |
| 新增功能 | 1. 动态路由机制（`@shared/routing`）：`buildRouteObjects` 支持路由转换、重复路径检测阻断及权限守卫包装；`AppRouter` 自动聚合 `pages/**/route.ts`；<br>2. 鉴权与管理员守卫壳（`@app/auth`）：`RequireAuth` / `RequireAdmin` 支持 loading 态、anonymous 引导、非管理员 403 拦截与管理员放行；<br>3. App Shell 基础设施（`@app/layout`、`@app/providers`、`@app/errors`）：集成 QueryClientProvider、ConfigProvider、CSP nonce 支持、全局捕获错误边界 `AppErrorBoundary` 与 409 数据冲突交互组件 `ConflictNotice`；<br>4. 架构防腐边界（`dependency-cruiser.config.cjs`）：严格限制 `app -> pages -> features -> shared/generated` 单向流动并禁止循环依赖 |
| 修复 / 重构 / 优化 | 补充单测运行环境所需的 `ResizeObserver` 与 `matchMedia` mock；适配 ESLint 对 `.cjs` 文件的 Node 运行时全局变量识别；精细调整组件 Props 以兼容 `exactOptionalPropertyTypes: true`；修复 Ant Design 6.x `Spin` 的 `description` 属性迁移 |
| 测试与验证 | 实际运行通过：`pnpm test:web`（6/6 测试套件，15/15 单元测试通过，覆盖路由转换、重复路径异常、鉴权守卫、错误边界重置、409 冲突重载与 AppRouter 挂载集成）、`pnpm check:frontend:boundaries`（30 modules / 66 dependencies 均无违规）、`pnpm -r typecheck`（5/5 workspace 通过）、`pnpm lint`（通过）、`pnpm -r build`（API 与 Web 生产包构建成功，页面 chunk 代码分割正常）、`pnpm db:migrations:check`（6/6 迁移校验通过）、`pnpm check:docs`（44 个文档无断链与空白问题）、`git diff --check`（无非法空白与冲突）；未运行：真实 `/me` 与 API 接口（等待 A 岗位契约定案）、CI 工作流接入（留待 A 岗位 F-11 统一接入） |
| 文档同步 | 更新 `docs/test-matrix.md` 新增 FE-001~FE-005 测试矩阵条目；更新 `技术设计v1.2.2.md` 第 10 章补充路径别名与动态路由约定；更新开发日志记录本次纵切片交付细节 |
| 风险与后续 | 前端鉴权壳与用户信息目前采用测试替身，需等 A 岗位在 `packages/api-contract` 完成契约生成链路与真实 `/me`、CSRF 接口后进行无缝对接；CI 工作流未纳入前端单测与边界检查，后续需合入 CI |
| 备注 | 交付分支 PR 待创建，待 A/B 岗位进行非作者代码审查 |

### 2026-09-07 15:55 +08:00 | C / Codex | chore/web-frontend-foundation-dependencies | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `chore/web-frontend-foundation-dependencies`（基于最新 `dev/c` 切出） |
| PR / commit | [#16](https://github.com/256-code/InPulse/pull/16)；commit 40e811f |
| 做了什么 | 落地前端基础设施与测试依赖：引入 Ant Design、TanStack Query、React Hook Form、Zod、React Router v7，配置 Vitest/jsdom/Testing Library 单测基座与路径别名，并增加 `test:web` 与 `check:frontend:boundaries` 脚本 |
| 新增功能 | 无业务功能；`apps/web` 具备 `pnpm --filter @inpulse/web test` 单元测试运行能力及基础路径别名解析（`@app/*`、`@pages/*`、`@features/*`、`@shared/*`、`@generated/*`） |
| 修复 / 重构 / 优化 | 锁定前端基础运行时依赖（`antd@6.6.2`、`@tanstack/react-query@5.102.8`、`react-hook-form@7.87.0`、`zod@4.5.4`、`react-router-dom@7.18.3`）；增加 `dependency-cruiser` 依赖检查脚本预留；保持单 lockfile 规范与 catalog 约束 |
| 测试与验证 | 实际运行通过：`pnpm test:web`（1/1 通过，包含 DOM 环境与 Testing Library 断言）、`pnpm -r typecheck`、`pnpm lint`、`pnpm -r build`（Web 产物正常构建）、`pnpm db:migrations:check`（6/6 迁移校验通过）、`pnpm check:docs`（44 个文档无断链）、`git diff --check`（无非法空白与冲突）；未运行：数据库集成测试（未启动本地 PostgreSQL）与 CI 工作流配置（CI 保持既有范围，未伪造） |
| 文档同步 | 同步更新 `README.md`、`AGENTS.md`、`CONTRIBUTING.md` 中关于新增 `pnpm test:web` 本地单测命令的说明 |
| 风险与后续 | PR A 合并后将同步回 `dev/c` 并切出 `feature/f-30-frontend-foundation` 推进 F-30 核心公共框架实现；`check:frontend:boundaries` 与 `test:web` 暂未接入 CI 工作流（按计划留待 A 的 F-11 统一接入） |
| 备注 | 交付分支 PR 待创建，待 A 岗位进行非作者代码审查 |
### 2026-09-07 15:25 +08:00 | A / Codex | dev/a（工作区，未提交） | 待推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `ci` |
| 分支 | `dev/a` 工作区；按 `AGENTS.md` 第 2/10 节，编码代理不创建提交、不推送，交付分支与提交时机由人工决定 |
| PR / commit | 尚无；本次改动全部停留在工作区（`git status` 共 70 项，其中 36 个新文件） |
| 做了什么 | 把技术设计 §12.4 的 CI 最小链路真实落库：格式基线、单测/集成测试分层、API 契约生成与漂移检查、依赖边界与 Secret 扫描，并按 §12.4 顺序重写 `.github/workflows/ci.yml` 的 `CI / workspace` job |
| 新增功能 | 无用户可感知功能。新增的是可执行门禁：`pnpm format`/`format:check`（Prettier 3.9.6，`.prettierignore` 排除 Markdown、lockfile、迁移与生成物）、`pnpm test:unit`/`test:integration`（database 拆为 `test/unit` 与 `test/integration`，新增 `vitest.integration.config.ts`）、`pnpm contract:generate`/`contract:drift`/`contract:validate`（Schema Registry/Zod + Route Registry 生成 OpenAPI 3.1、契约指纹与 `apps/web/src/generated/api/*`）、`pnpm permissions:check`（可执行权限矩阵与 `docs/permissions.md` 双向校验）、`pnpm check:deps`（前端分层、跨模块内部访问、循环依赖、Controller 直连数据库、前端裸 `fetch`）、`pnpm check:secrets`、`pnpm deps:audit`，以及一次跑完全部非数据库门禁的 `pnpm check` |
| 修复 / 重构 / 优化 | `.github/workflows/ci.yml` 由「install→typecheck→lint→build→migrations:check→docs」重排为 §12.4 顺序，并新增 digest 固定的 `postgres:18.6` 容器步骤（trust 认证、`max_connections=200`、`000_roles.sql` bootstrap、`app_migrator` 空库迁移、`cluster_bootstrap` 集成测试），`timeout-minutes` 由 15 提到 30，新增 `if: always()` 清理容器。一次性用 Prettier 规范化 34 个既有文件，未改变任何语义。新增 `docs/adr/ADR-027.md`（状态 `Proposed`）记录契约生成工具链决策，避免实现默认成为既成事实 |
| 测试与验证 | 本机 Windows / Node 24.20.0 / pnpm 11.19.0 / PostgreSQL 18.6（`D:\PostgreSQL\18\pgsql\bin`）。实际执行并全部通过：`pnpm install --frozen-lockfile`（Already up to date）、`pnpm lint`（0 错误）、`pnpm format:check`、`pnpm typecheck`（4 个项目）、`pnpm test:unit`（database 4 + api-contract 50 = 54 用例）、`pnpm db:migrations:check`（3 条迁移哈希一致）、`pnpm contract:drift`（5 个生成物一致）、`pnpm contract:validate`（1 条路由全部检查通过）、`pnpm permissions:check`（1 条操作 / 1 条路由）、`pnpm build`（api `tsc` + web `vite`，`dist/assets/index-SyOl7gnN.js` 190.45 kB）、`pnpm check:deps`（57 个源文件 / 57 个模块节点，无循环与越界）、`pnpm deps:audit`（No known vulnerabilities found）、`pnpm check:secrets`（148 个文件零命中）、`pnpm check:docs`（44 个 Markdown 文件链接与锚点有效）、`pnpm check`（一次跑完上述非数据库门禁，exit 0）。数据库门禁：`POSTGRES_BIN=D:\PostgreSQL\18\pgsql\bin pnpm db:test:local` 在空库上应用 3 条迁移并通过 database 4 个单测 + 12 个集成测试（含 100 并发审计链与数据库角色边界探针）。外部事实核验：`postgres:18.6` 的 digest `sha256:4ef4dbc9…c2280` 通过 Docker Hub Registry v2 API 实测一致；`actions/checkout@3d3c42e`（v7.0.1）与 `actions/setup-node@8207627`（v7.0.0）通过 GitHub API 实测与标签一致；`.github/workflows/ci.yml` 与 `docs.yml` 用仓库内 `js-yaml@4.3.2` 解析成功并逐步核对顺序。**未运行**：GitHub Actions 本身（本机无 Actions 运行环境，`CI / workspace` 的容器步骤只有 YAML 与命令级验证）；`pnpm db:poc:search:pgroonga:local` 与 `pnpm db:poc:search:local`（本次未重跑）；Playwright E2E、容器镜像构建、Compose 渲染与 digest 校验、镜像扫描（脚本与产物均不存在，无法运行，也未声称通过）。`check:deps` 与 `check:secrets` 在开发过程中用 5 类越界导入探针与 Secret 探针做过负向验证，确认会非零退出后已删除探针 |
| 文档同步 | `README.md`（状态段、状态表、§12.4 顺序的根级命令与辅助命令）、`AGENTS.md`（第 4 节 ADR-027 引用、第 8 节命令清单与未落库项、第 13 节基线冻结剩余事项）、`CONTRIBUTING.md`（「验证」章节重写为 §12.4 命令表 + 逐命令说明 + required checks 策略）、`docs/test-matrix.md`（新增「工程基座与 CI 门禁」章节 CI-001～CI-019，`SEC-001`/`AUDIT-001` 标为阶段 0 数据库层已自动化，`DEPLOY-001` 标为部分自动化）、`docs/adr/README.md`（新增 ADR-027 行）、新增 `docs/adr/ADR-027.md`。三份设计文档与 `docs/permissions.md` 未改动：本次没有改变架构、API 语义、权限规则、事务或部署行为 |
| 风险与后续 | 1) `.github/workflows/ci.yml` 的容器步骤未经真实 Actions 运行，首个 PR 上可能暴露 `docker exec` 交互、端口或超时问题，需按实际结果修正后再谈 required checks；2) ADR-027 仍是 `Proposed`，人工评审前不得把自研生成器当作已冻结基线，Nest Zod Pipe/Serializer 组合仍未定案；3) §12.4 尚有 4 项未落库：Playwright 关键路径 E2E、容器镜像构建、Compose 渲染与 exact-tag/digest 校验、镜像扫描——仓库还没有 Dockerfile 与 `compose.yaml`，且 ADR-017 要求的 Nginx 1.30.x 补丁与镜像 digest 需人工定案，按 `AGENTS.md` 第 10 节应作为独立 PR；4) Route Registry 目前只登记 `GET /health` 一条真实路由，契约门禁的覆盖面会随业务路由增加而真正生效，不能据此认为契约层已完成；5) 按验收标准，先让门禁在真实 PR 上稳定通过，再由管理员把 §12.4 门禁设为 required checks；6) 未提交、未推送，`pnpm-lock.yaml` 与全部生成物需与源码在同一 PR 内一起提交 |
| 备注 | 本次交付严格遵守「不得跳过或弱化失败测试」与「脚本落库前不得声称检查已通过」：没有使用 `skip`、没有降低断言、没有删除用例；未执行的门禁在上文与 README/AGENTS/CONTRIBUTING/测试矩阵中均显式标注为未落库或未运行 |

### 2026-09-07 13:08 +08:00 | A / Codex | dev/a | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/a` |
| PR / commit | 无新 PR；本次推送 `origin/dev/a`。合并提交 `7172949` 把 `origin/main` 的 `67601cd`（PR [#12](https://github.com/256-code/InPulse/pull/12)，C 的 `dev/c` 同步日志）并入 `dev/a` |
| 做了什么 | Push 3 同步完成后 `origin/main` 又前进到 `67601cd`，因此继续把最新 `main` 同步回 `dev/a`，使岗位分支包含全部已合并内容 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 无代码改动。`git merge origin/main` 由 ort 策略自动合并 `开发日志.md`：两侧新增条目位置不同，无冲突、无人工取舍、未改写任何既有历史条目；合并后 `dev/a` 与 `origin/main` 的差异只剩 A 自己的日志条目 |
| 测试与验证 | 实际执行并通过：`git merge origin/main`（Auto-merging `开发日志.md`，Merge made by the 'ort' strategy，+15 行）、`git merge-base --is-ancestor origin/main dev/a`（exit 0）、`git diff --stat origin/main dev/a`（仅 `开发日志.md`）、`git status --porcelain`（无冲突标记）、`pnpm check:docs`（43 个 Markdown 文件链接与锚点有效）、`git diff --check`。本次推送会触发 `dev/a` 上的 `CI / workspace`，结果以 Actions 页面为准，不再回填。未运行：需要 PostgreSQL 18 或 PGroonga 容器的数据库与搜索脚本；应用层单元测试、E2E 与生成物漂移检查尚未建立 |
| 文档同步 | 仅新增本条开发日志；ADR、设计文档、权限矩阵与测试矩阵无变化 |
| 风险与后续 | `main` 在本次同步窗口内连续前进（`dfacd67` 到 `67601cd`），说明多岗位并行交付；后续每次开始新纵切片前都应先重复本同步，避免基于过期基线开发。ADR-026 的后续事项不变：OpenAPI 生成工具与 Nest Zod Pipe/Serializer 组合仍未定案，`SEC-006` 待实现。本地仍残留 PR #9 的交付分支 `chore/phase0-workspace-skeleton`，本次未代为删除 |
| 备注 | 同步后 `dev/a` 包含 `origin/main` 的全部提交，`7172949` 为合并提交；本条之后不再为纯同步追加日志 |

### 2026-09-07 13:06 +08:00 | A / Codex | dev/a | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/a` |
| PR / commit | 无新 PR；本次提交推送至 `origin/dev/a`。PR [#11](https://github.com/256-code/InPulse/pull/11) 已由 `@256-code`（C，非作者）于 2026-09-07 12:59 +08:00 合并进 `main`，合并提交 `dfacd67`，远端交付分支 `docs/nestjs-adr-026` 已删除 |
| 做了什么 | 把最新 `origin/main` 同步回岗位工作分支 `dev/a`，并删除本地已合并的交付分支 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | `dev/a` 由 `05664e9` fast-forward 到 `dfacd67`：`git merge-base --is-ancestor` 确认 `dev/a` 本就是 `origin/main` 的祖先，因此无冲突、无额外合并提交。同步进来的唯一内容差异是 `开发日志.md` 的 45 行交付分支日志（`docs/nestjs-adr-026` 的 Push 1～3）。本地删除已合并的 `docs/nestjs-adr-026`，保留岗位长期分支 `dev/a`；`chore/phase0-workspace-skeleton`（PR #9 的交付分支）本地仍存在，本次未代为删除 |
| 测试与验证 | 实际执行并通过：`git merge-base --is-ancestor dev/a origin/main`（exit 0）、`git merge --ff-only origin/main`（Fast-forward）、同步后 `git diff --stat dev/a origin/main` 为空、`git branch -d docs/nestjs-adr-026`（仅已合并分支可删）、`pnpm check:docs`（43 个 Markdown 文件链接与锚点有效）、`git diff --check`。PR #11 合并前 head `3bf0ece` 的 `CI / workspace`（31s）与 `Documentation / docs`（7s）均为 pass；同步推送 `05664e9..dfacd67` 已触发 `dev/a` 上的 `CI / workspace`，本次日志提交会再触发一次，结果以 Actions 页面为准，不再回填。未运行：需要 PostgreSQL 18 或 PGroonga 容器的数据库与搜索脚本；应用层单元测试、E2E 与生成物漂移检查尚未建立 |
| 文档同步 | 仅新增本条开发日志；ADR-026、PoC 证据文档、`SEC-006`、两份设计文档与 `AGENTS.md` 的同步内容已随 PR #11 进入 `main`，本次未再改动 |
| 风险与后续 | 事实记录：PR #11 虽由非作者 `@256-code` 合并，但 GitHub 上没有留下正式 review（`reviewDecision` 与 `reviews` 均为空），且使用 merge commit 而非 `CONTRIBUTING.md` 约定的 squash merge；`main` 仍未配置服务端保护规则，`Documentation / docs` 也不是 required check，这类偏差目前只能靠人工遵守，建议管理员按 `CONTRIBUTING.md` 完成仓库设置。ADR-026 的后续事项不变：OpenAPI 生成工具与 Nest Zod Pipe/Serializer 组合仍未定案；`SEC-006` 只是 `Required`，实现错误契约时必须让 11.2.3 的异常过滤器停止回显 `Cannot GET /path` 并补测试 |
| 备注 | 同步后 `dev/a` 与 `origin/main` 内容一致（另加本条日志提交）；交付分支已删除，不再使用。下一纵切片为 Schema Registry + 类型化 Route Registry 与 OpenAPI 3.1/客户端生成链路 |

### 2026-09-07 14:49 +08:00 | C / Codex | feature/search-query-service | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `feature/search-query-service`（基于已同步的 `dev/c` 切出） |
| PR / commit | 功能提交 `2c03775`；本次仅推送交付分支，尚未创建 PR |
| 做了什么 | 实现 F-26 搜索服务纵切片：新增 `ProjectAccessQueryPort`/`AuthorizedProjectScope` 契约草案，收敛生产 `normalizeSearchText`/`validateSearchQuery`，实现 `SearchQueryService` 与 `PostgresSearchProjectionReader`，并补齐真实 PostgreSQL 集成测试与根级入口 |
| 新增功能 | 新增 `apps/api/src/modules/search/*`、`apps/api/src/modules/projects/project-access.port.ts`；搜索 SQL 使用 `normalized_search_text &@~ app.pgroonga_query_escape($1)` 与参数化 `project_id/visibility_scope` 过滤，支持分页、管理员显式 VOID 过滤及输入校验 |
| 修复 / 重构 / 优化 | 将 PoC 搜索归一化收敛为生产模块；服务和测试版权限 Port 保持服务端生成 Scope；修正分页参数校验顺序；测试 fixture token 增加随机后缀；测试版本地 Scope 限定到本次 fixture，避免历史投影干扰金标召回 |
| 测试与验证 | 在本地持久化 PostgreSQL 18.6 + PGroonga 实例（容器 `inpulse-local-dev`，地址 `127.0.0.1:55432`，数据卷 `inpulse-local-pgdata`，`max_connections=150`）上执行：`pnpm test:search:db` 8/8 通过；`pnpm test` 中 API 搜索 8/8、数据库 18/18 全部通过；`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm db:migrations:check`、`pnpm check:docs`（44 个 Markdown 文件）、`git diff --check` 均通过 |
| 文档同步 | 更新 `README.md`、`AGENTS.md`、`CONTRIBUTING.md`、`database/README.md`、`docs/test-matrix.md`、`docs/adr/ADR-025.md`、`docs/poc/search-pgroonga-v1-result.md`、`database/poc/search-pgroonga/README.md`、系统设计、技术设计，并在 `AGENTS.md` 第 13 节补充 ADR-025 与 PoC 证据链接；CI 配置文件注明搜索 DB 测试暂不启用 |
| 风险与后续 | 生产 `ProjectAccessQueryPort` 适配器、搜索 API/Controller、E2E 与生产加密备份恢复仍未交付；搜索 API/前端需等 A 定案 Schema Registry + Route Registry 后再做；`test:search:db`/`pnpm test` 需要 PGroonga 且当前不在 CI 中 |
| 备注 | 本地持久化数据库保留；工作区改动待本次提交并推送 `origin/feature/search-query-service`，提交后回填 commit 短哈希，未创建 PR |

### 2026-09-07 12:46 +08:00 | C / Codex | dev/c | Push 5

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` |
| 分支 | `dev/c` |
| PR / commit | 功能提交 `5d4309f`；本次合并 `origin/main` 后推送至 `origin/dev/c`，PR 待人工创建 |
| 做了什么 | 完成 PostgreSQL 18.6 官方基础镜像上的 PGroonga 阶段 0 纵切片：锁定无 `latest` 探针镜像、`020_pgroonga.sql` 与 `0003-0005` 正式迁移（含旧 `pg_trgm` contract 清理）、17 个数据库测试、15 组 V1 搜索语义、默认查询计划、正式 migration runner 升级/逐迁移回滚、排除 Session 数据的逻辑备份恢复及恢复后烟雾验证 |
| 新增功能 | 无用户可感知业务功能；新增 `database/bootstrap/020_pgroonga.sql`、`database/migrations/0003_search_pgroonga.sql`、`0004_search_projection_contract_pg_trgm_index.sql`、`0005_search_projection_contract_pg_trgm_extension.sql` 与 `database/poc/search-pgroonga/Dockerfile.pgroonga-pg18.6`；一体化 PoC 脚本生成 `pgroonga-report.json`（`version: pgroonga-poc-v3`）、`pgroonga-backup-restore-report.json`（`version: pgroonga-backup-restore-v2`）与 `pgroonga-migration-report.json`（`version: pgroonga-migration-v3`） |
| 修复 / 重构 / 优化 | PGroonga 扩展由 `cluster_bootstrap` 预装并撤销业务角色函数 EXECUTE，仅授予 `app_runtime` 查询/转义所需权限；`0004` 在确认 PGroonga 索引存在后删除旧 GIN，`0005` 在确认无扩展外依赖后删除 `pg_trgm`；修正 PoC 报告中过时的 18.4/未验证表述；Windows PowerShell 5.1 下使用 `cmd.exe` 管道避免 custom dump 损坏；验证查询改用 PostgreSQL `U&` 转义避免中文编码漂移；镜像构建移除 `groonga-apt-source-latest`，改为校验 Groonga 官方 keyring SHA-256，并锁定 `postgres:18.6` digest、PGroonga/Groonga Debian 包版本；升级验证改为插入 `0000-0002` checksum 后由正式 migration runner 应用 `0003`，再由 runner 应用 `0004/0005` |
| 测试与验证 | 实际通过：`docker build --no-cache -f database/poc/search-pgroonga/Dockerfile.pgroonga-pg18.6 -t inpulse/pgroonga-pg18.6:repro database/poc/search-pgroonga`（最终 digest `sha256:bca8248cb90be2b287674741d6d1575b5a87f4f1fbb52259a9aca9055781083a`，运行实例 PostgreSQL 18.6、PGroonga 4.0.8、Groonga 16.1.0）；`powershell -NoProfile -ExecutionPolicy Bypass -File database/scripts/poc-search-pgroonga-local.ps1 -Image inpulse/pgroonga-pg18.6:repro -Port 55436 -RestorePort 55437`（六条迁移、17/17 数据库测试、10 个搜索策略 error 0、90 条金标 base/scale Recall@20=100%、15/15 V1 语义探针、跨项目 0 条、`0000-0002 -> 0003` 由 migration runner 1 applied / 3 already present、保留旧 GIN 时强制计划使用 PGroonga、`0004/0005` 由 runner 2 applied / 4 already present、旧 GIN=0、`pg_trgm`=0、`0003-0005` 各自事务回滚恢复、`object_inspect` 默认查询 1000/15 行 `indexDiskUsage=5283840` 且 101000 行 `29138944`、逻辑恢复后 PGroonga 4.0.8/索引/权限/默认计划可用、中文探针返回 6 行）；空库容器补充验证 `000_roles.sql` + `0000-0002` 三份迁移通过，且不挂载 Secret 执行 `010_passwords.sql` 以 `psql` 退出码 3 fail closed；`pnpm --filter @inpulse/database exec vitest run test/config.test.ts`（5/5，新增缺失 Secret 文件用例）、`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm db:migrations:check`（六条迁移）、`pnpm check:docs`（42 个 Markdown 文件）、`git diff --check` 全部通过 |
| 文档同步 | 更新 `README.md`、`database/README.md`、`CONTRIBUTING.md`、`AGENTS.md`、`docs/permissions.md`、`docs/test-matrix.md`、`docs/adr/ADR-025.md`、`docs/poc/search-pgroonga-v1-result.md`、系统设计、技术设计、PGroonga PoC README 与原 `pg_trgm` PoC README |
| 风险与后续 | `search_projection` PGroonga bootstrap 与 `0003-0005` 迁移已通过真实 PostgreSQL 集成测试，旧 `pg_trgm` GIN 索引和扩展已由独立 contract 迁移清理；仍待 SearchQueryService、权限过滤/参数化查询测试、生产多阶段镜像与 ADR-020/F-10 的加密备份恢复验收；当前恢复验证为本地逻辑恢复，不代表生产 RPO/RTO 或旧 Session 失效 |
| 备注 | 功能提交 `5d4309f` 已创建；本次合并并推送 `origin/dev/c`；未修改历史迁移、生产依赖或已合入的文档条目 |
### 2026-09-07 13:49 +08:00 | C / Codex | docs/frontend-client-consumption-input | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/frontend-client-consumption-input`（基于已同步的 `dev/c` 切出） |
| PR / commit | 本次推送至 `origin/docs/frontend-client-consumption-input`；PR 待人工创建 |
| 做了什么 | 向 A 提交“前端对生成客户端的消费需求清单”，作为 F-11 契约平台评审输入；覆盖生成客户端边界、请求/错误模型、CSRF、幂等、乐观锁、分页游标、C 域接口缺口与阶段 0 验收纵切片 |
| 新增功能 | 无业务功能；新增 `docs/frontend-generated-client-consumption-requirements.md` |
| 修复 / 重构 / 优化 | 按外部评审意见明确候选目录、`fetch`/`baseUrl` 注入边界、`credentials`、统一 `ApiError`、`CursorPage` 与评审状态规则；资源 ID 按正式基线保持 `INTEGER IDENTITY`/`number`，不引入替代 ID 类型；接口路径、字段名和 operationId 均标记为候选输入，未冻结 |
| 测试与验证 | 实际执行通过：`pnpm check:docs`（44 个 Markdown 文件链接与锚点有效）、`git diff --check`、`git diff --cached --check`（提交前）。未运行：`pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm db:migrations:check`；本次仅文档变更，未运行需要数据库或应用构建的门禁 |
| 文档同步 | 新增契约评审输入文档，并更新本开发日志；未变更 ADR、设计文档、权限矩阵或测试矩阵。本清单是评审输入，不是新的契约真相 |
| 风险与后续 | 候选接口、字段与 operationId 在 A 写入 Schema Registry/Route Registry 前不能作为实现依据；若采纳，需同步对应设计、权限矩阵与测试矩阵 |
| 备注 | 本次为交付分支首次推送，PR 待人工创建；本提交不包含工作区中的 PGroonga/搜索 PoC 改动 |
### 2026-09-07 12:31 +08:00 | A / Codex | dev/a | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/a` |
| PR / commit | 本次提交推送至 `origin/dev/a`；上一条为 `176ba83`，如需并入 `main` 由人工创建 PR 并经非作者评审 |
| 做了什么 | 回填 Push 1 的 CI 实际结果，并修正其中关于 GitHub Actions 触发范围的描述 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | Push 1 写的「推送 `origin/dev/a` 会触发 `CI / workspace` 与 `Documentation / docs`」不准确：`.github/workflows/docs.yml` 的触发条件是 `pull_request` 与 `push: main`，不含 `dev/*`，本次只触发 `CI`。已按事实修正，未改动任何 ADR、设计文档、权限矩阵或测试矩阵结论 |
| 测试与验证 | `gh run list --branch dev/a`：`CI` 工作流在 `176ba83` 为 completed/success，`workspace` job 的 Install（frozen lockfile）、Typecheck、Lint、Build、Database migration consistency、Documentation checks 全部通过，用时 25s；`Documentation / docs` 本次未触发，待人工开 PR 后回填。本地 `pnpm check:docs` 通过（43 个 Markdown 文件链接与锚点有效）、`git diff --check` 通过 |
| 文档同步 | 仅维护 `开发日志.md` 自身，无其他文档变更 |
| 风险与后续 | 无新增风险；ADR-026 的剩余事项与未定案项见 Push 1 条目 |
| 备注 | 本次推送完成后 Push 1 与 Push 2 均不再修改 |

### 2026-09-07 12:41 +08:00 | A / Codex | docs/nestjs-adr-026 | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/nestjs-adr-026` |
| PR / commit | [#11](https://github.com/256-code/InPulse/pull/11)；上一条提交 `6972792` |
| 做了什么 | 修正 Push 2 条目时间戳，并回填 PR #11 在 `6972792` 上的最终 check 结果 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | Push 2 时间戳原写 12:41，实际提交时间为 12:39，已按事实修正；Push 2 备注中的“不再修改”指结论性内容，本次属事实性更正。未改动 ADR、设计文档、权限矩阵或测试矩阵 |
| 测试与验证 | `gh pr checks 11`：head `6972792` 上 `CI / workspace` pass（34s）、`Documentation / docs` pass（7s）；`gh pr view 11` 为 OPEN、MERGEABLE，已请求 `@suikiiovo` 与 `@256-code` 评审，暂无批准。本地 `pnpm check:docs` 通过（43 个 Markdown 文件链接与锚点有效）、`git diff --check` 通过。本次更正提交会再触发一次 check，结果以 PR 页面为准，不再回填。未运行：需要 PostgreSQL 18 或 PGroonga 容器的数据库与搜索脚本；应用层单元测试、E2E 与生成物漂移检查尚未建立 |
| 文档同步 | 仅维护 `开发日志.md` 自身 |
| 风险与后续 | 按 `CONTRIBUTING.md`，AI 编码代理不得直接合并代码，作者不得自批；等待 B（`@suikiiovo`）或 C（`@256-code`）批准后由人工 squash merge，合并者需人工确认两个 job 均成功（`Documentation / docs` 不是 required check）。合并并删除交付分支后把最新 `main` 同步回 `dev/a` |
| 备注 | 本条之后不再为时间戳或 CI 结果追加日志提交 |

### 2026-09-07 12:39 +08:00 | A / Codex | docs/nestjs-adr-026 | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/nestjs-adr-026` |
| PR / commit | [#11](https://github.com/256-code/InPulse/pull/11)；base `main`，head `docs/nestjs-adr-026`，上一条提交 `188fac7` |
| 做了什么 | 创建 PR #11 并回填编号，修正 Push 1 条目的时间戳，请求 B 与 C 作为非作者评审 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | Push 1 条目时间戳原写 12:44，实际提交时间为 12:36，已按事实修正；PR 编号由“待创建”回填为 [#11](https://github.com/256-code/InPulse/pull/11)。未改动 ADR、设计文档、权限矩阵或测试矩阵结论 |
| 测试与验证 | PR #11 在提交 `188fac7` 上两个 check 均通过：`CI / workspace` pass（32s，含 frozen lockfile 安装、Typecheck、Lint、Build、Database migration consistency、Documentation checks）、`Documentation / docs` pass（9s）。本地 `pnpm check:docs` 通过（43 个 Markdown 文件链接与锚点有效）、`git diff --check` 通过；`gh pr create --reviewer suikiiovo,256-code` 已请求非作者评审。本次回填提交会再触发一次两个 check，结果以 PR 页面为准。未运行：需要 PostgreSQL 18 或 PGroonga 容器的数据库与搜索脚本；应用层单元测试、E2E 与生成物漂移检查尚未建立 |
| 文档同步 | 仅维护 `开发日志.md` 自身 |
| 风险与后续 | 按 `CONTRIBUTING.md`，AI 编码代理不得直接合并代码，作者不得自批；`main` 未配置服务端保护规则，`Documentation / docs` 也不是 required check，合并者必须人工确认两个 job 均成功，并由 B（`@suikiiovo`）或 C（`@256-code`）批准后 squash merge。合并并删除交付分支后需把最新 `main` 同步回 `dev/a` |
| 备注 | 本次推送完成后 Push 1 与 Push 2 均不再修改；ADR-026 的剩余风险与未定案项见 `dev/a` Push 1 条目 |

### 2026-09-07 12:36 +08:00 | A / Codex | docs/nestjs-adr-026 | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/nestjs-adr-026`（自 `dev/a` 的 `05664e9` 切出；`dev/a` 已同步到 `origin/main` 的 `6bbe432`） |
| PR / commit | PR 待本次推送后创建（base `main`，head `docs/nestjs-adr-026`），编号在下方 Push 2 条目回填 |
| 做了什么 | 按 `CONTRIBUTING.md` 的交付流程，把 ADR-026 定案从岗位工作分支 `dev/a` 转为短生命周期交付分支，准备进入 `main` 的 PR |
| 新增功能 | 无新增能力；交付内容即 `dev/a` Push 1 的 ADR-026、NestJS PoC 证据文档、测试矩阵 `SEC-006` 与两份设计文档及 `AGENTS.md` 的同步 |
| 修复 / 重构 / 优化 | 无代码改动；交付分支与 `origin/main` 的差异只有 9 个文档文件（+277/-11），不含依赖、lockfile、迁移、Dockerfile、Compose 或 workflow 变更 |
| 测试与验证 | 本地：`pnpm check:docs` 通过（43 个 Markdown 文件链接与锚点有效）、`git diff --check` 通过、`git diff --stat origin/main..HEAD` 确认只含文档；此前 `dev/a` 上 `CI / workspace` 对 `176ba83` 与 `05664e9` 均为 completed/success。本次推送交付分支会再次触发 `CI / workspace`，创建 PR 后会触发 `Documentation / docs`，结果待回填。未运行：需要 PostgreSQL 18 或 PGroonga 容器的数据库与搜索脚本；应用层单元测试、E2E 与生成物漂移检查尚未建立 |
| 文档同步 | 仅新增本条开发日志；ADR、设计文档、测试矩阵与仓库规则的同步内容见 `dev/a` Push 1 条目 |
| 风险与后续 | 按 `CONTRIBUTING.md`，AI 编码代理不得直接合并代码，进入 `main` 必须经 PR、squash merge 与非作者评审；本 PR 作者为 A（`@shaoxiaoyu-D`），需 B（`@suikiiovo`）或 C（`@256-code`）评审，作者不得自批。`main` 仍未配置服务端保护规则，合并门禁目前只靠人工遵守。合并并删除交付分支后需把最新 `main` 同步回 `dev/a` |
| 备注 | 交付分支只承载 ADR-026 这一个逻辑变更；合并后删除该分支，不删除岗位长期分支 `dev/a` |

### 2026-09-07 12:17 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/a`（用户明确指示本阶段直接在岗位工作分支进行，不切交付分支） |
| PR / commit | 本次提交推送至 `origin/dev/a`（岗位工作分支）；如需并入 `main` 由人工创建 PR 并经非作者评审 |
| 做了什么 | 完成阶段 0 的 NestJS 11.x 与 12.x 兼容性验证门禁，新增 ADR-026 定案本期继续暂留 11.x 并锁定 11.2.3、替代 ADR-003，同步 ADR 索引、技术设计、系统设计、`AGENTS.md` 与测试矩阵；并按用户要求把 `origin/main` 最新内容合并回 `dev/a` |
| 新增功能 | 无业务功能。新增 [ADR-026](./docs/adr/ADR-026.md) 与证据文档 [NestJS 11.x 与 12.x 兼容性 PoC 结果](./docs/poc/nestjs-11-vs-12-v1-result.md)；测试矩阵新增 `SEC-006`：未匹配路由必须返回统一 JSON 404、固定文案、不回显 method 与 path、保留 `X-Request-Id` 与应用 CSP |
| 修复 / 重构 / 优化 | 无生产代码与依赖改动：`apps/api/package.json` 与 `pnpm-lock.yaml` 保持 11.2.3，未把 12.x 写入任何清单；PoC 全部在仓库外临时目录执行，源码与产物未提交。`origin/main`（PR #10，含 C 的 ADR-025 PGroonga 决策与搜索 PoC）合并回 `dev/a` 为 fast-forward 到 `6bbe432`，无冲突；因 `main` 已占用 ADR-025，本次决策顺延为 ADR-026，并先合并再改文档以避免编号与行号漂移 |
| 测试与验证 | 仓库门禁本地实际执行并通过：`pnpm install --frozen-lockfile`（6 个 workspace 项目，Already up to date）、`pnpm typecheck`（`apps/api`、`apps/web`、`database`、`packages/api-contract` 全部 Done）、`pnpm lint`（`eslint .` 无输出）、`pnpm build`（`apps/api` tsc 产物 + `apps/web` vite 15 modules）、`pnpm db:migrations:check`（3 条迁移 SHA-256 校验通过）、`pnpm check:docs`（43 个 Markdown 文件链接与锚点有效）、`git diff --check`。仓库外 PoC 实际执行并通过：两个变体各自 `pnpm install`、`pnpm exec tsc --noEmit -p tsconfig.json`、`pnpm exec tsc -p tsconfig.json`、`pnpm exec nest build`、`node scripts/runtime-smoke.mjs`（18 项，归一化 `requestId`/nonce/Token/指纹后 17 项逐字段一致）、`node scripts/notfound-probe.mjs`、`node scripts/capability-probe.mjs`、`pnpm exec vitest run`（11.2.3 为 17/17，12.0.1 为 16/17，唯一失败项即未匹配路由 404 用例）、`node esm-probe/dist/main.js`，12.0.1 另执行 `node scripts/native-probe.mjs`；并用 npm registry 实测版本发布时间与 `nestjs-zod@5.5.0` 的 peerDependencies。未运行：`pnpm db:migrate`、`pnpm db:test`、`pnpm db:test:local`、`pnpm db:poc:search:local`、`pnpm db:poc:search:pgroonga:local`（需要 PostgreSQL 18 / PGroonga 容器，搜索门禁属 C 的范围）；应用层单元测试、E2E 与生成物漂移检查尚未建立；PoC 未连接真实数据库、未做浏览器 E2E、未做冷启动与性能长稳测量；GitHub Actions：推送后 `CI / workspace` 在 `176ba83` 为 completed/success，`Documentation / docs` 因 `docs.yml` 只在 `pull_request` 与 `push: main` 触发而未运行，详见下方 Push 2 条目 |
| 文档同步 | 新增 `docs/adr/ADR-026.md`、`docs/poc/nestjs-11-vs-12-v1-result.md`；`docs/adr/ADR-003.md` 标记 Superseded（被替代：ADR-026）；`docs/adr/README.md` 索引同步；`技术设计v1.2.2.md` 版本表、阶段 0 条目与验收、ADR 引用范围改为 ADR-001～ADR-026；`系统设计文档v1.0.2.md` 版本表、阶段 0 验收要点与 ADR 引用范围同步；`AGENTS.md` 第 4 节 NestJS 基线表述与第 13 节基线冻结剩余事项；`docs/test-matrix.md` 新增 `SEC-006`。权限矩阵无变化（未改变任何授权规则）；系统设计第 2 章评审整改历史行按历史事实保留未改 |
| 风险与后续 | ADR-026 只决定 NestJS 主版本与补丁版本，OpenAPI 生成工具与 Nest Zod Pipe/Serializer 组合仍未定案，不得写成已冻结选择；`SEC-006` 目前只是 `Required`，11.2.3 默认异常过滤器会回显 `Cannot GET /path`，实现错误契约时必须修正并补测试；ESM-only 依赖（如 `cookie@2.0.1`）与 `apps/api` 的 `moduleResolution: Node10` 冲突仍未解决，引入前需先决定 moduleResolution 或选用 CJS 版本；12.x 只有 11 天历史与一个补丁版本，升级触发条件与工作清单见 ADR-026，升级只能作为独立依赖升级 PR 由人工确认；PoC 代码未入库，长期复现方式需另行决定；`AGENTS.md` 第 13 节仍未链接 C 的 ADR-025 证据，属搜索门禁范围，本次未代改 |
| 备注 | 本次按用户指示直接在 `dev/a` 工作，经用户确认后提交并推送 `origin/dev/a`；推送前 `dev/a` 已 fast-forward 到 `origin/main`（`6bbe432`），本次提交只含文档与 ADR，不含代码、依赖或 lockfile 变更 |

### 2026-09-07 11:37 +08:00 | C / Codex | dev/c | Push 4

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/c`（同步最新 `origin/main`） |
| PR / commit | [#10](https://github.com/256-code/InPulse/pull/10) 已合并；`main` 合并提交为 `6bbe432` |
| 做了什么 | 评审完成后，`dev/c` 经快进同步到最新 `origin/main`，确认 PR #10 的 PGroonga PoC、ADR-025、文档和阶段 0 工程骨架已进入 `main` |
| 新增功能 | 无新增业务功能 |
| 修复 / 重构 / 优化 | 无代码修改；将长期工作分支 `dev/c` 更新为包含 PR #10 合并结果的最新 `main` |
| 测试与验证 | 本次为纯同步，无新增代码；PR #10 的 GitHub Actions `Documentation / docs` 与 `CI / workspace` 共 3 个检查全部通过；本地 `pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm db:migrations:check`、`pnpm check:docs` 和 `git diff --check` 此前已通过 |
| 文档同步 | 仅更新本条开发日志；ADR、设计文档、POC 证据与测试矩阵已在 PR #10 中交付 |
| 风险与后续 | PostgreSQL 18.6 官方基线的 PGroonga 构建、迁移、默认计划和备份恢复仍未验证；下一步可开始正式搜索索引、迁移和 `SearchQueryService` 实现 |
| 备注 | 代理未自行合并 `main`；PR #10 已由人工评审并合并，本次仅同步岗位工作分支 |

### 2026-09-07 11:32 +08:00 | C / Codex | dev/c | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/c`（同步 `origin/main` 后推送 PR #10） |
| PR / commit | [#10](https://github.com/256-code/InPulse/pull/10)；base `main`，head `dev/c` |
| 做了什么 | 解决 PR #10 的合并冲突：把最新 `origin/main`（阶段 0 工程基座骨架）合并进 `dev/c`，同时保留 PGroonga V1 PoC、ADR-025 和原 `pg_trgm` 失败证据 |
| 新增功能 | 无新增业务功能；合并后保留 pnpm workspace、api/web 工程骨架、CI 与 PGroonga 搜索 PoC 工具链 |
| 修复 / 重构 / 优化 | `AGENTS.md`、`CONTRIBUTING.md`、`README.md` 取两侧并集：保留 `main` 的根级命令与 CI 事实，同时补充 PGroonga 搜索命令、V1 结论和 PostgreSQL 18.6 未验证说明；`开发日志.md` 保留 C 的 09:31 与 A 的 09:28 条目，未删减既有历史；修复三个 PoC 源码的未使用变量/赋值 lint 错误 |
| 测试与验证 | 本地：`pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm lint`（0 error / 0 warning）、`pnpm build`（api tsc + web vite）、`pnpm db:migrations:check`（3 条迁移）、`pnpm check:docs`（41 个 Markdown 文件）、`git diff --check` 全部通过；`pnpm db:test` 未运行（需要 PostgreSQL 18 实例）；`database/scripts/poc-search-pgroonga-local.ps1 -Port 55435` 此前已端到端通过。远端：PR #10 的 `Documentation / docs`、`CI / workspace`（push 与 pull_request）共 3 个检查全部通过 |
| 文档同步 | 更新 `AGENTS.md`、`CONTRIBUTING.md`、`README.md`、`开发日志.md`；ADR、设计文档、POC 证据与测试矩阵保持已有内容 |
| 风险与后续 | PostgreSQL 18.6 官方基线的 PGroonga 构建、迁移、默认计划和备份恢复尚未验证；PR #10 仍需非作者人工评审后合并 |
| 备注 | 用户已授权推送 `dev/c`；本次仅解决冲突并同步工作分支，不合并 PR，也不推送 `main` |

### 2026-09-07 11:26 +08:00 | C / Codex | dev/c | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/c`（提交链 `e5fd6a3` 后追加本次日志提交） |
| PR / commit | [#10](https://github.com/256-code/InPulse/pull/10)；本地提交 `e5fd6a3`，推送 `dev/c` |
| 做了什么 | 采纳 PGroonga 作为 V1 搜索方案：新增 ADR-025（替代 ADR-010，不修改 ADR-011），提交 PGroonga PoC 证据，并同步功能、系统、技术、开发工作书、测试矩阵和仓库执行规则 |
| 新增功能 | 新增 `pnpm db:poc:search:pgroonga` 与 `pnpm db:poc:search:pgroonga:local`；PGroonga PoC 覆盖 9 组 V1 语义探针、101000 行索引和 90 条金标 Recall@20 |
| 修复 / 重构 / 优化 | 明确 V1 只要求中文短词、完整英文缩写、完整代码标识符和完整编号；任意英文/代码子串与正则搜索不在 V1 门禁；普通查询必须使用 `pgroonga_query_escape`；保留原 `pg_trgm` 门禁失败证据 |
| 测试与验证 | `pnpm check:docs` 通过（41 个 Markdown 文件）；`pnpm --filter @inpulse/database typecheck` 通过；`git diff --check` 通过；`database/scripts/poc-search-pgroonga-local.ps1 -Port 55435` 已端到端通过，迁移、16 个数据库测试和 10 个搜索策略完成；原 `db:poc:search:local` 仍会因 GIN 默认计划非零退出；PostgreSQL 18.6 官方基线的 PGroonga 构建、迁移、默认计划和备份恢复未运行 |
| 文档同步 | 新增 `docs/adr/ADR-025.md`、`docs/poc/search-pgroonga-v1-result.md`、`database/poc/search-pgroonga/**`；更新 ADR 索引、功能设计、系统设计、技术设计、开发工作书、测试矩阵、README、CONTRIBUTING、AGENTS、database README 与开发日志 |
| 风险与后续 | 当前 PoC 镜像内置 PostgreSQL 18.4，不能证明 18.6 官方镜像兼容；正式迁移、SearchQueryService、权限矩阵接口测试、Dockerfile/Compose 和备份恢复尚未实现 |
| 备注 | 用户要求改用 ADR-025、标记 ADR-010 被替代且不修改 ADR-011；用户已确认推送 `dev/c`；PR #10 已创建 |
### 2026-09-07 09:59 +08:00 | A / Codex | chore/phase0-workspace-skeleton | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `chore/phase0-workspace-skeleton`（自同步后的 `dev/a` 切出），同时推送岗位工作分支 `dev/a` |
| PR / commit | PR 待人工创建（base `main`）；交付内容来自 `2080a1f`、`94d1ac8`、`9507427` 及合并提交 `7fa8313`、`b0474cc` |
| 做了什么 | 用户要求把 `dev/a` 的阶段 0 工程基座骨架推入 `main`；因 `AGENTS.md` 第 10 节与 `CONTRIBUTING.md` 禁止直接推送 `main`、也禁止把 `dev/<role>` 作为 PR 来源，改为先把 `origin/main` 同步进 `dev/a`，再切短生命周期交付分支推送，由人工开 PR、非作者评审后 squash merge |
| 新增功能 | 本条不新增能力；交付内容（pnpm workspace、根级 `typecheck`/`lint`/`build`、`apps/api` 与 `GET /api/v1/health`、`apps/web` SPA 骨架、`packages/api-contract` 入口、`CI / workspace` job）见 2026-09-07 09:28 条目 |
| 修复 / 重构 / 优化 | 合并 `origin/main`（PR #8 仅开发日志）时 `开发日志.md` 冲突按并集解决：C 的 09:31 条目在前、A 的 09:28 条目在后，未删减或改写任何既有历史条目；本次无代码改动 |
| 测试与验证 | 本地实际执行并通过：`pnpm install --frozen-lockfile`（6 个 workspace 项目，Already up to date）、`pnpm typecheck`（`apps/api`、`apps/web`、`database`、`packages/api-contract` 全部 Done）、`pnpm lint`（`eslint .` 无告警输出）、`pnpm build`（`apps/api` tsc 产物 + `apps/web` vite 15 modules）、`pnpm db:migrations:check`（3 条迁移校验通过）、`pnpm check:docs`（37 个 Markdown 文件链接与锚点有效）。未运行：`pnpm db:migrate`、`pnpm db:test`、`pnpm db:test:local`（需要 PostgreSQL 18）；应用层单元测试、E2E 与生成物漂移检查尚未建立；GitHub Actions 的 `CI / workspace` 与 `Documentation / docs` 需 PR 创建后才有结果，本次未运行 |
| 文档同步 | 仅新增本条开发日志；`README.md`、`AGENTS.md`、`CONTRIBUTING.md` 已随骨架在 09:28 条目同步，本次无其他文档、ADR、权限矩阵或测试矩阵变更 |
| 风险与后续 | `.github/workflows/ci.yml` 属高风险变更，必须非作者人工评审；`main` 尚未配置服务端保护规则，禁止直接推送当前只靠人工遵守；阶段 0 搜索门禁未通过待 ADR（见 09:31 条目），NestJS 12.x、Drizzle 补丁版本、OpenAPI 生成工具与 CSP 兼容性验证仍未定案；PR 合并后需删除交付分支并把最新 `main` 同步回 `dev/a` |
| 备注 | 代理未直接推送 `main`、未合并、未打标签；PR 创建、评审与 squash merge 由人工完成，PR 编号确定后按新增记录补充，不改写本条 |

### 2026-09-07 09:31 +08:00 | C / Codex | dev/c | 未推送

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `dev/c`（本次仅提交并推送开发日志） |
| PR / commit | 未创建 PR；本次仅提交日志并推送 `dev/c` |
| 做了什么 | 按用户明确指令，将阶段 0 中文搜索 PoC 结果写入开发日志；PoC 包含 1000 条仿真搜索投影、100 条冻结金标、参数化 SQL、边界输入、跨项目隔离和 GIN 查询计划证据 |
| 新增功能 | 新增 `pnpm db:poc:search` 与 `pnpm db:poc:search:local`；本地 Docker 脚本可重建 PostgreSQL 18.6 并执行迁移、现有数据库测试与 PoC |
| 修复 / 重构 / 优化 | 修正金标数据中通用词“搜索”污染召回、`%`/`_`/`\` 特殊字符未转义、跨项目用例目标不唯一，以及 `similarity` 权限边界；明确 GIN 强制计划不等于默认门禁通过 |
| 测试与验证 | `pnpm install --frozen-lockfile` 通过；`pnpm typecheck` 通过；`pnpm db:poc:search:local` 最终非零退出，其中迁移校验、3 条迁移应用、16/16 数据库测试通过；PoC 1000 行、100 条金标、Recall@20=100%、无结果/边界/跨项目通过，GIN 默认计划未通过、强制 GIN 可用；报告见 `database/poc/search/artifacts/phase0-report.json`；`git diff --check` 需在文档同步后复核 |
| 文档同步 | 更新根 `README.md`、`CONTRIBUTING.md`、`database/README.md`、`AGENTS.md`、`docs/test-matrix.md`，新增 `database/poc/search/README.md` |
| 风险与后续 | 阶段 0 搜索门禁未通过，按 ADR-010 需人工决定并新增 ADR；远端仍缺 `dev/b`，`dev/a` 落后 `main`；本次仅推送日志，工作区其他 PoC/文档改动未提交、未推送 |
| 备注 | 按用户授权只提交并推送开发日志；不把强制 GIN 计划或索引存在性写成“搜索方案已通过” |

### 2026-09-07 09:28 +08:00 | A / Codex | dev/a | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `chore` |
| 分支 | `dev/a` |
| PR / commit | `2080a1f`（工程基座骨架）、`7fa8313`（合并 `origin/main` 数据库基线）；本次为岗位工作分支推送，尚未开 PR |
| 做了什么 | 在 `dev/a` 落地阶段 0 工程基座骨架，并把 `origin/main` 的 PostgreSQL 数据库基线合并进同一工作区，使仓库可一次安装、统一类型检查、lint、构建与文档校验 |
| 新增功能 | 尚无业务功能。新增 pnpm workspace（`apps/*`、`packages/*`、`database`）、根级 `typecheck`/`lint`/`build` 脚本与共享 ESLint 扁平配置；`apps/api` 提供 NestJS 最小应用与 `GET /api/v1/health`；`apps/web` 提供 Vite + React 19 SPA 骨架并按 `app -> pages -> features -> shared/generated` 建目录；`packages/api-contract` 提供空契约入口；`.github/workflows/ci.yml` 新增 `CI / workspace` job（corepack → frozen install → typecheck → lint → build → db:migrations:check → check:docs，actions 按 commit SHA 固定） |
| 修复 / 重构 / 优化 | 合并冲突处理：`README.md`、`package.json`、`pnpm-workspace.yaml`、`pnpm-lock.yaml` 四处以 `main` 为共享基线解决，保留 `packageManager: pnpm@11.19.0`、既有 `engines` 与根 `tsconfig.base.json`，不做未批准版本升级；删除冗余的 `packages/tsconfig`，共享编译选项改由根 `tsconfig.base.json` 提供；`apps/api` 覆盖 CommonJS/Node10/装饰器并关闭 `verbatimModuleSyntax`，`apps/web` 的 `lib` 对齐 ES2024；应用 manifest 统一用 `catalog:` 引用 `typescript` 与 `@types/node`；`pnpm-lock.yaml` 由 `pnpm install` 重新生成，未手工编辑；修复 README 合并残留的重复代码围栏；CI job 去掉自定义 `name`，使检查名与文档中的 `CI / workspace` 一致 |
| 测试与验证 | 实际执行并通过：`pnpm install`（Node v24.20.0 / pnpm 11.19.0，6 个 workspace 项目）、`pnpm typecheck`（`apps/api`、`apps/web`、`database`、`packages/api-contract`）、`pnpm lint`（ESLint 覆盖 29 个文件，其中 `database` 19 个，0 error / 0 warning）、`pnpm build`（`apps/api` tsc 产物 + `apps/web` vite 构建 15 modules）、`pnpm db:migrations:check`（3 条迁移 SHA-256 校验）、`pnpm check:docs`（37 个 Markdown 文件链接与锚点）、`git diff --check`；API 冒烟：本地启动 `apps/api` 构建产物，`GET /api/v1/health` 返回 200 `{"status":"ok"}`。未运行：`pnpm test`、`pnpm db:test`、`pnpm db:test:local` 需要 PostgreSQL 18 实例，本次未启动数据库；E2E、OpenAPI/客户端漂移检查、依赖边界与容器检查尚未建立；GitHub Actions 实际结果待本次推送后回填 |
| 文档同步 | 更新 `README.md`（阶段状态、仓库状态表、根级命令清单、数据库说明链接）、`AGENTS.md`（第 1 节阶段描述、第 8 节根级命令清单与 CI 保持一致）、`CONTRIBUTING.md`（验证章节命令与 CI 覆盖范围）、`.github/workflows/ci.yml`；同时消除 `main` 上既有的阶段描述漂移（README 已写“阶段 0 实施中”，AGENTS 仍写“正式开发前的设计阶段”）。本次未改变授权规则、数据库不变量或测试覆盖范围，权限矩阵与测试矩阵无需变更；未新增或替代 ADR |
| 风险与后续 | NestJS 按 [ADR-003](./docs/adr/ADR-003.md) 保持 11.x 基线并锁定 11.2.3，12.x 升级仍待阶段 0 兼容性门禁；OpenAPI 生成工具与 Nest Zod Pipe/Serializer 组合尚未选型，`packages/api-contract` 目前只是空入口，不得视为已冻结；数据库集成测试未纳入 CI（需要 PostgreSQL 18 服务），阶段 0 需补 service container 方案；ESLint 首次覆盖 `database/**`，后续该目录变更可能触发规则冲突，必须按规则处理而不得静默放宽；下一纵切片为 Schema Registry + 类型化 Route Registry 与 OpenAPI 3.1/客户端生成链路（含无漂移检查） |
| 备注 | `dev/a` 为长期工作分支，不得直接合并到 `main`；交付时切短生命周期分支并开 PR。本次推送完成后回填 `CI / workspace` 与 `Documentation / docs` 的实际结果 |

### 2026-09-04 18:07 +08:00 | A / Codex | main | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `main`（用户明确授权直接推送） |
| PR / commit | 本次数据库基线提交 |
| 做了什么 | 建立 PostgreSQL 18 数据库实现基线，包括 Drizzle 类型模型、只追加 SQL 迁移、受限迁移器、数据库角色与真实数据库集成测试 |
| 新增功能 | 新增 34 张业务与安全表、项目复合外键、状态与版本不变量、幂等记录、任务组成员真相、搜索投影及串行 HMAC 审计链基础设施 |
| 修复 / 重构 / 优化 | 通过复合外键阻止跨项目串联；隔离运行、迁移、审计查询、归档和备份权限；迁移历史增加 advisory lock 与 SHA-256 漂移检查 |
| 测试与验证 | `pnpm install --frozen-lockfile --offline --ignore-scripts`、`pnpm typecheck`、`pnpm db:migrations:check`、`node scripts/check_docs.mjs` 均通过；使用 PostgreSQL 18.6 从空库执行角色初始化和 3 条迁移，2 个测试文件共 16/16 通过，包含生产 Secret 路径拒绝及同一审计 scope 的 100 个并发业务事务；`git diff --check` 通过 |
| 文档同步 | 更新根 `README.md` 与 `database/README.md`；数据库行为按既有功能设计、系统设计、技术设计、ADR、权限矩阵和测试矩阵实现，未改变已接受决策 |
| 风险与后续 | 本次按用户明确指令直推 `main`，未经过仓库规则要求的 PR 与非作者人工评审；Compose、备份恢复演练及其余阶段 0 CI 仍待完成 |
| 备注 | 数据库基线完成；功能开发必须遵守 `database/README.md` 的事务契约，API 不得启动时迁移 |

### 2026-09-04 | C | docs/role-account-map | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/role-account-map` |
| PR / commit | [#7](https://github.com/256-code/InPulse/pull/7) |
| 做了什么 | 回填岗位账号映射 PR 编号，并记录 Push 2 |
| 新增功能 | 无新增业务功能；保留 A=`@shaoxiaoyu-D`、B=`@suikiiovo`、C=`@256-code` 映射 |
| 修复 / 重构 / 优化 | 无代码修复或重构；同步开发日志引用 |
| 测试与验证 | 推送前执行 `node scripts/check_docs.mjs`，通过；PR #7 的 `Documentation / docs` 结果在本次推送后确认 |
| 文档同步 | 无新增同步；本条维护开发日志自身 |
| 风险与后续 | 等待非作者审批后 squash merge；A/B 随后用自己的账号创建并推送 `dev/a`、`dev/b` |
| 备注 | Push 2 在 Pull Request #7 创建后同步日志；合入后本条不再修改 |

### 2026-09-04 | C | docs/role-account-map | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/role-account-map` |
| PR / commit | [#7](https://github.com/256-code/InPulse/pull/7) |
| 做了什么 | 在贡献指南中记录 A/B/C 岗位对应的 GitHub 账号与固定工作分支 |
| 新增功能 | 明确 A=`@shaoxiaoyu-D` 使用 `dev/a`，B=`@suikiiovo` 使用 `dev/b`，C=`@256-code` 使用 `dev/c` |
| 修复 / 重构 / 优化 | 消除“分支名看起来属于某岗位但实际由谁推送”的歧义；禁止借用其他岗位账号或代推 |
| 测试与验证 | 本地执行 `node scripts/check_docs.mjs`，通过：检查 Git 空白状态及 36 个 Markdown 文件的链接和锚点 |
| 文档同步 | 更新 [CONTRIBUTING.md](./CONTRIBUTING.md) 和本文件 |
| 风险与后续 | A/B 需按映射关系用自己的账号创建并推送 `dev/a`、`dev/b`；若账号对应关系变化必须同步更新 |
| 备注 | 本条目由 C 岗位维护；Push 1 提交账号映射规则 |

### 2026-09-04 | C | docs/role-workflow | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/role-workflow` |
| PR / commit | [#6](https://github.com/256-code/InPulse/pull/6) |
| 做了什么 | 回填分支模型 PR 编号，并记录 Push 2 |
| 新增功能 | 无新增业务功能；已创建并推送 `dev/a`、`dev/b`、`dev/c` |
| 修复 / 重构 / 优化 | 无代码修复或重构；同步开发日志引用 |
| 测试与验证 | 推送前执行 `node scripts/check_docs.mjs`，通过；PR #6 的 `Documentation / docs` 结果在本次推送后确认 |
| 文档同步 | 无新增同步；本条维护开发日志自身 |
| 风险与后续 | 等待非作者审批后 squash merge，再将最新 `main` 同步到 `dev/c` |
| 备注 | Push 2 在 Pull Request #6 创建后同步日志；合入后本条不再修改 |

### 2026-09-04 | C | docs/role-workflow | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/role-workflow` |
| PR / commit | [#6](https://github.com/256-code/InPulse/pull/6) |
| 做了什么 | 将协作模型调整为“`main` + 三条岗位长期工作分支 + 临时交付分支” |
| 新增功能 | 新增 `dev/a`、`dev/b`、`dev/c` 固定工作分支；每岗位日常开发推送到自己的分支，不直接推送 `main` |
| 修复 / 重构 / 优化 | 修正分支管理流程：保留三条长期工作分支，交付仍走独立 PR 分支，避免每个任务都产生长期分支 |
| 测试与验证 | 本地执行 `node scripts/check_docs.mjs`，通过：检查 Git 空白状态及 36 个 Markdown 文件的链接和锚点 |
| 文档同步 | 更新 [CONTRIBUTING.md](./CONTRIBUTING.md)、[AGENTS.md](./AGENTS.md) 和本文件 |
| 风险与后续 | 需要每个岗位严格遵守“工作分支同步 + 交付分支 PR + 合并后回同步”；后续创建分支时只增删交付分支 |
| 备注 | 本条目由 C 岗位维护；Push 1 先推送分支模型规则，随后创建三条工作分支 |

### 2026-09-04 | C | docs/dev-log | Push 3

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/dev-log` |
| PR / commit | [#5](https://github.com/256-code/InPulse/pull/5) |
| 做了什么 | 回填 Pull Request #5 的 `Documentation / docs` 最终通过结果 |
| 新增功能 | 无新增业务功能 |
| 修复 / 重构 / 优化 | 无代码修复或重构；更新日志中的验证证据 |
| 测试与验证 | Push 2 后 Pull Request #5 的 GitHub Actions `Documentation / docs` 检查通过；推送前再次执行 `node scripts/check_docs.mjs`，通过；Push 3 的远端检查在推送后重新确认 |
| 文档同步 | 无新增同步；本条维护开发日志自身 |
| 风险与后续 | 等待非作者评审批准后走 squash merge；合入 `main` 后本条不再修改 |
| 备注 | Push 3 写入最终 CI 证据；合入后本条视为历史记录 |

### 2026-09-04 | C | docs/dev-log | Push 2

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/dev-log` |
| PR / commit | [#5](https://github.com/256-code/InPulse/pull/5) |
| 做了什么 | 回填开发日志的 PR 编号，并记录 Push 2 |
| 新增功能 | 无新增业务功能 |
| 修复 / 重构 / 优化 | 无代码修复或重构；确保日志中引用到实际 Pull Request |
| 测试与验证 | 推送前本地执行 `node scripts/check_docs.mjs`，通过：检查 Git 空白状态及 36 个 Markdown 文件的链接和锚点；PR #5 的 GitHub Actions 结果在本次推送后查看 |
| 文档同步 | 无新增同步；本条是开发日志自身维护记录 |
| 风险与后续 | 待 PR #5 的 `Documentation / docs` 运行完成后确认最终通过；后续岗位均需按同一规则维护 |
| 备注 | Push 2 在 Pull Request #5 创建后同步日志引用；合入后本条不再修改 |

### 2026-09-04 | C | docs/dev-log | Push 1

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` |
| 分支 | `docs/dev-log` |
| PR / commit | [#5](https://github.com/256-code/InPulse/pull/5) |
| 做了什么 | 建立“开发日志”维护规范，并把它加入贡献指南、仓库开发规则、README 导航和 PR 模板 |
| 新增功能 | 新增 [开发日志](./开发日志.md)，作为每次写代码、优化、修复和测试验证的集中记录入口 |
| 修复 / 重构 / 优化 | 无代码修复或重构；优化交付可追溯性：每次推送前强制记录变更事实 |
| 测试与验证 | 本地执行 `node scripts/check_docs.mjs`，通过：检查 Git 空白状态及 36 个 Markdown 文件的链接和锚点；GitHub Actions 的 `Documentation / docs` 结果在 Pull Request 创建后回填 |
| 文档同步 | 更新 [CONTRIBUTING.md](./CONTRIBUTING.md)、[AGENTS.md](./AGENTS.md)、[README.md](./README.md) 和 [Pull Request 模板](./.github/pull_request_template.md) |
| 风险与后续 | 需要所有岗位在后续每次推送前遵守并人工检查；后续新增 lint/typecheck/test/build/Playwright 检查后，必须在记录中写明实际命令和结果 |
| 备注 | 本条目在 Push 1 前记录本地验证结果；Pull Request 创建后回填编号和最终 CI 结果，合入后不再修改 |

## 2026-09-08：B 的 ModuleQueryPort / FeatureQueryPort 本地交付待审核

- 范围：增加两个公开事务内写前检查 Port 与 Nest 注入绑定；只读本域表，以完整 ID/归属条件取得 FOR SHARE 后判断 ACTIVE/ARCHIVED。结果沿用 A 的 kind/resource 判别结构，无资源时不返回摘要。未新增 HTTP、CRUD、认证、成员查询或事务基础设施；适配器无额外 Repository 空层，数据库错误继续传播。
- 基线：执行 `git status --short --branch`、`git fetch origin`。首次 `git merge --ff-only origin/main` 被原 README 改动阻止，未覆盖文件；随后 `git stash push -u -m 'preserve user docs before query ports baseline sync'`、`git merge --ff-only origin/main`、`git stash apply 'stash@{0}'` 成功。当前 dev/b 为 a520f52；A 的 checkProjectForWrite 和 resolveActorInTransaction 已在代码中确认。README 仍只多原导航链接，协作方案 Git blob 哈希仍为 2d3753f5d516e0d3c992203c8581da0e469218d0，保护 stash 保留。
- 测试先行：新增 write-query-ports.integration.test.ts（6 个用例），复用 database.helpers.ts 初始化完整项目/成员/未分类模块；使用 app_runtime 和两个独立连接，pg_blocking_pids 验证真实归档 UPDATE 被阻塞，提交/回滚后验证状态与版本。新增 write-query-ports.test.ts 覆盖公开 Nest 注入。未使用 Mock 验证数据库，未弱化约束或清理业务历史。
- 实际验证：`pnpm --filter @inpulse/api exec vitest run test/write-query-ports.test.ts test/modules-module.test.ts`，2 文件、2 用例通过。
- 实际局部验证：`pnpm exec prettier --write` 仅传入本次新增/修改的两个域 TS 文件和两个测试文件，成功；`git diff --check` 通过。使用 apps/api 目录的 `node` 标准输入调用 TypeScript Compiler API，读取 tsconfig.test.json，createProgram roots 限定为 src/modules/modules/index.ts、src/modules/features/index.ts、test/write-query-ports.test.ts、test/write-query-ports.integration.test.ts；getPreEmitDiagnostics 与配置诊断合计 0（测试归属断言补强后重跑仍为 0）。未运行全仓静态检查、全量构建或依赖审计。
- 未验证：`Test-Path Env:TEST_DATABASE_URL` 为 False，`Get-Command docker,podman,psql -ErrorAction SilentlyContinue` 未发现工具。因此未运行真实数据库命令，不重复已知环境失败。待隔离 PostgreSQL 18 + PGroonga 环境执行 `pnpm --filter @inpulse/api exec vitest run --config vitest.integration.config.ts test/write-query-ports.integration.test.ts`；不能将旧 CommandPort CI 5/5 当作本次验证结果。
- 文档：同步 Modules 与 Features 接入说明、测试矩阵。行为落实技术设计既有父级 FOR SHARE 锁序，未改变设计不变量，无需改动 ADR/三份设计或权限矩阵。调用方必须先在同一 tx 解析身份并完成 A 的项目授权，再按项目 → 模块 → 功能检查；HTTP 错误映射、实际业务写入和归档 FOR UPDATE 流程由外层完成。
- 交付约束：本轮仅本地实现待审核；未新增依赖、修改历史迁移、创建提交、推送或创建 PR。原 README 修改和未跟踪协作方案不属于本切片。

## 2026-09-08：B 写前检查 Port 独立交付启动

- 用户授权本切片进入 CI 并交 A/C 人工评审；创建交付分支 codex/b-write-query-ports，基于已同步的 dev/b（6f3d632）。只复制 14 个本切片文件到独立 worktree，不含根 README 导航和协作方案；原工作区全部改动与保护 stash 保留。
- 本地已通过两项 Nest 测试、局部 TypeScript 检查及 diff 空白检查；真实数据库 6 个用例仍待 CI 验证。计划提交代码/测试及必要文档并推送本岗位交付分支，创建 PR；只有日志确认 write-query-ports.integration.test.ts 6/6 实际通过后回填证据，再请求 A/C 人工评审，不自动合并。

## 2026-09-08：B 写前检查 Port 的 CI 数据库证据回填

- [PR #42](https://github.com/256-code/InPulse/pull/42) 仅包含本切片 14 个文件；代码 9bae30b、初始文档 58acbe7 已推送。推送前暂存区检查发现新 SQL 模板一行尾随空格，已删除后重新通过 `git diff --cached --check`；未改查询语义或断言。
- 提交 58acbe7 的 [CI / workspace](https://github.com/256-code/InPulse/actions/runs/34210258609) 成功。[真实数据库日志](https://github.com/256-code/InPulse/actions/runs/34210258609/job/102009300977?pr=42#step:17:28) 显示 `write-query-ports.integration.test.ts (6 tests) 334 ms`，6/6 全部实际通过。API 集成总计 11 文件、46 用例通过；database 包 13 用例通过。CI 使用既有 PostgreSQL 18.6 + PGroonga 镜像、角色 bootstrap 与空库迁移，再执行 `pnpm test:integration`，不使用 Mock、不跳过用例、不改约束。
- 本次同步两个 Port 接入说明及测试矩阵的验证状态并添加直达日志证据；原本地环境不足和待验证历史保留。此次仅文档回填，不改生产代码和测试；推送后检查最终提交 CI，再请求 A/C 人工评审，不自动合并。数据库自动化通过不等同于完整业务 Workflow 或人工验收完成。

## 2026-09-08：PR #42 合并后的验证文档补交

- GitHub 显示 suikiiovo 将 PR #42 合并为 666d0dd（原代码与初始文档两提交），页面无正式 review 记录；代理没有执行合并。数据库证据提交 8d2bf1e 晚于合并，未进入该 PR，因此从同步后的 dev/b 创建 codex/b-write-query-evidence，移植为 11f0066，仅补交四份验证文档。
- 保留所有本地修改后将 main 快进同步到本地 dev/b。stash apply 提示旧未跟踪代码现已由主线跟踪、不能重复恢复；这些文件内容已由合并保留，原 README 导航与未跟踪协作方案仍在原工作区，stash 未删除。
- 验证证据继续指向 58acbe7 的真实 PostgreSQL 18.6 + PGroonga 6/6 日志。本次无代码、测试或数据库变更；`git diff --check` 通过。计划推送文档补交 PR，待 CI 通过后请求 A/C 对补交文档及 #42 代码人工评审，仍不自动合并。
- 文档补交已创建为 [PR #44](https://github.com/256-code/InPulse/pull/44) 草稿，等待最终 CI 后转为待评审并请求 A/C；不自动合并。该 PR 仅四份验证文档，不夹带原 README 导航或协作方案。

## 2026-09-09：A 的 F-10.1 生产部署与镜像闭环 PR #62

- 范围：从当前 `origin/main`（b8c37d6）创建交付分支 `codex/a-f10-deploy`，实现技术设计 v1.2.2 §11.1/§11.2 的生产镜像闭环：API、Migration/ops、Web、DB-bootstrap 四个多阶段镜像；生产 Nginx 配置（非 root 8080/8443、TLS1.2+、80→443、仅透传 API/health、`/api/v1` no-store）；API 内置 `healthcheck.mjs`；migration 镜像改用编译后 `dist/src/migrate.js` 运行，避免在运行时保留 `tsx`。
- 基础镜像 digest 已按 [ADR-017](./docs/adr/ADR-017.md) 记录并由受信镜像仓库解析：`node:24.20.0-bookworm-slim`、`nginxinc/nginx-unprivileged:1.30.4`、`postgres:18.6`。生产 `*_IMAGE_REF` 仍是发布清单占位，未伪造自建镜像 digest。
- 门禁：`scripts/check_deploy_refs.mjs` 新增对四个 Dockerfile 与 `nginx.conf`/`healthcheck.mjs` 存在性、`FROM` 固定 digest、runtime 非 root 数值 `USER` 的校验，并校验每个服务级 secret 使用长语法（`mode=0400`、`uid/gid` 与容器数值 user 一致、`target` 为 `/run/secrets` 直接子项），`pnpm check:deploy:test` 通过；`.github/workflows/ci.yml` 在 §12.4 顺序中插入四个生产镜像构建与 Trivy 扫描（CRITICAL/HIGH、`ignore-unfixed=true`、`exit-code=1`）。
- 文档：同步 `README.md`、`AGENTS.md`、`CONTRIBUTING.md`、`docs/adr/ADR-017.md` 与 `docs/test-matrix.md`（测试矩阵见本机未合并改动）。
- 实际验证：`pnpm --filter @inpulse/database build` 输出 `dist/src/migrate.js` 与 `dist/src/migrations.js`；`pnpm check:deploy:test` 通过，`.env.deploy.example` 负例确实被拒绝，secrets 短语法负例也被拒绝；`pnpm typecheck`、`pnpm format:check`、`pnpm check:deps`、`pnpm check:secrets`、`pnpm check:docs`、`git diff --check` 均通过。
- PR / 推送：2026-09-09 首次推送到 `codex/a-f10-deploy` 并创建 [PR #62](https://github.com/256-code/InPulse/pull/62)（`feat(deploy): implement F-10.1 production images and CI gates`）。首次 CI 的 `workspace` 在 Build production web image 失败：`nginxinc/nginx-unprivileged:1.30.4` 默认以 UID/GID 101 运行，runtime `apt-get` 无权限写 `/var/lib/apt/lists/partial`（exit code 100）。已修复 `deploy/docker/web.Dockerfile`：安装 curl 前临时 `USER root`，装完恢复 `USER 101:101`。修复推送为 `fix(deploy): install web healthcheck curl as root before restoring non-root user`。
- 同步：C 的 [PR #61](https://github.com/256-code/InPulse/pull/61)（设计师最新视觉迁移与成员选择）已于 2026-09-09 合入 `main`（`e45799b`）。按“等 C 先推”要求，`codex/a-f10-deploy` 已 rebase 到该提交；README.md 与 `docs/test-matrix.md` 自动合并冲突已解决（保留 C 的视觉迁移事实与我们的生产镜像/Trivy 落库事实），`check:docs`、`check:deploy:test`、`format:check`、`check:secrets`、`git diff --check` 均在 rebase 后通过。
- CI / 本次实际结果：rebase 后首次 CI（Run `34317233380`）的 `workspace` 在 Trivy 扫描 API 镜像时失败。根因是 `node:24.20.0-bookworm-slim` 自带 npm 11.19.0 全局工具链中的 `brace-expansion`、`ip-address`、`tar` 共 3 个 HIGH，不是应用生产依赖；Docker Hub 当时尚未发布含修复 npm 的 `node:24.21.0-bookworm-slim`。为维持“不降低安全基线、不忽略漏洞”的约束，已在 API/Migration runtime 删除 `/usr/local/lib/node_modules` 与 `npm/npx/corepack`，并让 `scripts/check_deploy_refs.mjs` 校验该不变量；同时更新 ADR-017 与测试矩阵。本机仍无 Docker daemon，实际镜像构建与扫描以 CI 重跑为准。
- CI 重跑结果：Run `34318020055` 中 API、Migration、Web 镜像及四镜像构建均通过，但 DB-bootstrap 的 Trivy 扫描失败。根因是官方 `postgres:18.6` 镜像自带的 `/usr/local/bin/gosu`（Go 1.24.6，22 个 HIGH/CRITICAL）及 Debian OpenSSL 3.5.6（1 个 HIGH，修复版 `3.5.7-1~deb13u2`）。生产 Compose 以 `user: postgres` 运行，官方入口不会调用 `gosu`，因此 DB-bootstrap 构建时删除该二进制，并执行 `apt-get install -y --only-upgrade libssl3t64 openssl openssl-provider-legacy`；基础镜像仍为 `postgres:18.6`，未升级 Postgres 主版本，也未降低扫描门禁。`scripts/check_deploy_refs.mjs` 已新增两条回归校验，ADR-017 与测试矩阵同步更新。
- 未验证 / 后续：`deploy/compose.yaml` 仍是稳态拓扑预检，启动前需人工准备 `/run/secrets/*`、TLS 证书与首次建库 `compose.init` 纵切片；本轮变更在 CI 验证通过前不得声明生产镜像闭环完成。

## 2026-09-09 · B · F-13 功能档案本地实现交审（未提交/推送）

- 基线：dev/b、origin/dev/b、origin/main 本地引用均为 58abb407；本轮不提交、推送、开 PR 或合并。README 原导航增量与未跟踪协作计划原样保留。
- 功能：模块卡片→功能列表→创建→详情→编辑当前说明/标签→管理员归档/恢复；七条契约及生成客户端、实际权限、审计/活动/搜索同事务写入。相似提示只查当前项目关键词候选，允许继续同名创建；没有虚假统计、假数据、无效关联业务入口或凭空通知。
- 一致性：复用项目/模块公开写前锁与功能行版本；新增历史模块读取、项目编号基础设施及搜索候选公开 Port。编号采用既有 code_sequences 原子 UPSERT，不补零（用户定案），不新增迁移或依赖；碰撞安全 409 且整笔回滚。恢复不改变下级历史状态。
- 前端：复用 F-12 三方冲突规则、真实管理员安全验证与不确定失败幂等 Key；同字段冲突解决后才使用最新 If-Match，查询过期响应隔离，成功刷新相关缓存。
- 实测：真库 HTTP/公开 Port 21/21（F-13 15 + 原 Port 6）；契约/权限/Controller 绑定 16/16；前端 16/16（F-13 9 + F-12 7）；Edge E2E 2/2（最终合跑 34.7 秒）；契约生成与 5 产物漂移、35 路由完整性通过；E2E 必要依赖包/API 局部编译、API 测试/Web/E2E 局部类型检查通过。
- 验证过程：先补缺失 Schema/组件失败测试；编号碰撞从 500 回归修成安全 409。fixture 按数据库不变量补用户版本和任务初始状态历史。管理员 E2E 首次因误向 Vite 继承 NODE_ENV=test 产生重复测试 ID 失败，按现有配置仅 API 使用 test 后最终完整通过；没有弱化断言或改公共弹窗。默认 Chromium、CI、全量构建、全仓静态检查及无关审计未运行。
- 文档：同步三份设计的功能/编号相关内容、权限矩阵、测试矩阵、FeatureQueryPort README 和 [F-13 本地交审说明](docs/f13-local-handoff.md)。最初 tags 缺项误判已纠正，现有 Schema 支持标签，以后一次用户确认继续实现。
- 审核/后续：A 审编号公开 Port，C 审候选 Port、AppModule/客户端及 E2E；待非作者审核。任务/记录/GitHub/遗留问题关联业务和统计留待对应功能，不声称已经实现。无数据库迁移待审；隔离 PostgreSQL/Edge 复跑方法和本轮失败记录见交审说明。

## 2026-09-09（未记录具体时间） | B / Codex | codex/f14-feature-tasks | 本地交审

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f14-feature-tasks`<br>基线：fetch origin 后 1c2b5bf，独立工作区 610b/InPulse，分支 codex/f14-feature-tasks；未修改 D:\InPulse，不自行推送/合并。 |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-14 功能级任务本地交审 |
| 新增功能 | 新增功能：功能任务创建/指派/编辑/卡片列表/详情抽屉，真实项目成员选择，必选负责人，历史负责人保留；编辑三方冲突处理与同语义不确定失败幂等重试；指派通知直达任务。 |
| 修复 / 重构 / 优化 | 事务与安全：项目/模块/功能父级锁、任务版本、任务序列/初始历史与审计/活动/搜索/通知原子提交、重放重新授权；新公开 ProjectCodePort 任务编号、ProjectMembersQueryPort 与 FeatureReadPort 已获协调授权，交 A/C 重点审核。无新依赖/迁移/历史迁移修改。 |
| 测试与验证 | 实际验证：F-14 HTTP 真库20/20 + 公开Port回归6/6；Web 5/5；Edge真实UI 1/1；契约生成、40路由验证以及局部API/依赖编译和API/Web/E2E类型检查通过。完整最终命令结果见 docs/f14-local-handoff.md；未执行全仓静态检查、全量构建、全量测试、依赖审计或CI。 |
| 文档同步 | 文档同步：三份设计、权限/测试矩阵、开发日志与F14交审说明；Schema、Route Registry及生成OpenAPI/客户端同步。编号遵守不补零约束，成员移除/停用后普通编辑可保留原负责人。 |
| 风险与后续 | 风险与后续：跨域Port及安全契约待非作者审核；本机默认Chromium缺少安装，Edge通过，不声称默认浏览器通过。记录域无真实公开读取，最近三条/任务记录/完整历史入口均待接入，不展示假统计或无效入口。F-15须F-14合入并收到协调通知后开始，F-16/F-19未实现。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

## 2026-09-09（未记录具体时间） | B / Codex | codex/f15-module-tasks | 本地交审

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f15-module-tasks`<br>基线为fetch后的origin/main 273b27319e7e5c251f32bd8d2b0022d40cfea823（F14 PR71），新分支codex/f15-module-tasks；保留主线最终测试/MFA修复，未重复cherry-pick旧F14。未修改D:\InPulse，不自行推送或合并。 |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-15 模块级任务本地交审 |
| 新增功能 | 新增模块级任务创建/列表/详情/编辑、多影响功能增删与功能页单份引用；复用F14任务事务/HTTP/前端，按唯一任务计数，影响关系不复制任务。影响集合参与三方冲突与同语义幂等重试；指派通知直达真实模块路径。 |
| 修复 / 重构 / 优化 | 依据正式设计及协调解释，task_feature_impacts为当前集合，明确移除关系DELETE但同tx不可变审计保存完整前后身份/类型/时间及增删差异；重加新created_at。项目/模块是真实父级，新增影响必须ACTIVE，既有关联可保留/移除归档功能。当前∪目标功能ID升序锁后再锁任务，锁后重读，必要时同tx savepoint从头有限重试。没有新迁移、权限放宽、历史迁移修改或依赖变更。 |
| 测试与验证 | 实际验证：真库tasks-api.integration 28/28（F15 8+F14 20）；前端任务/模块/功能22/22；契约/权限18/18；生成物无漂移及45路由策略/权限检查通过；Edge F14/F15合跑2/2，55.7秒；局部编译/API测试/Web/E2E类型检查通过。未运行全仓静态检查、全量构建、全量测试、依赖审计或本批CI/Chromium。 |
| 文档同步 | 同步Schema/Route Registry/Controller/OpenAPI/客户端、三份设计、权限/测试矩阵与docs/f15-local-handoff.md。后续为非作者审查与PR/CI，关系历史/锁序/模块重放资源为重点；F16/F19/任务组与记录统计仍未实现，不显示假记录数据。 |
| 风险与后续 | 原条目未单列，见本表其他字段。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

## 2026-09-10（未记录具体时间） | B / Codex | codex/f16-task-status | 本地交审

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` |
| 分支 | `codex/f16-task-status`<br>从 fetch 确认的 origin/main bce3337 创建 codex/f16-task-status，独立610b工作区，不重放旧F14/F15，不修改D:\InPulse。 |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-16 任务状态与历史本地交审 |
| 新增功能 | 新增FEATURE/MODULE完成（WITHOUT_RECORD六原因）、重开、取消、恢复及不可变状态历史；默认待办过滤、历史/搜索保留、取消排除完成率、409保留输入。实际变化入口明确尚未开放，不先完成后补记录。 |
| 修复 / 重构 / 优化 | 复用父级锁、任务锁和MODULE影响重读策略，一个tx内条件更新版本/状态、完成快照与历史、审计/活动/搜索/适用通知。使用锁后数据库时间避免旧事务起始时间倒退；不更改负责人及既有关联。无依赖/迁移/数据库权限变更。 |
| 测试与验证 | 实际验证：真库任务41/41、任务/模块/功能前端25/25（任务9）、契约/权限18/18、Edge状态闭环2/2（1.1分钟）、61路由与生成物无漂移、必要局部编译/类型检查通过。未全量构建/测试/静态审计，未执行本批CI/默认Chromium。 |
| 文档同步 | 同步三份设计、权限/测试矩阵、Schema/Registry/Controller/生成物与 docs/f16-local-handoff.md。交审后冻结，由协调安排PR；F17须收到合并通知后新建分支，草稿不完成任务，WITH_RECORD组合发布保留F19。 |
| 风险与后续 | 原条目未单列，见本表其他字段。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |

## 2026-09-10（未记录具体时间） | B / Codex | codex/f16-task-status | 增量审核修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` |
| 分支 | `codex/f16-task-status` |
| PR / commit | 原条目未单列，见本表其他字段。 |
| 做了什么 | F-16 增量审核修复（完成率） |
| 新增功能 | 原条目未单列，见本表其他字段。 |
| 修复 / 重构 / 优化 | 按协调 P2 审核移除任务面板不完整的完成率计算/展示，纠正上一条交审记录中的“取消排除完成率”实现说明。列表返回全部生命周期且 DTO 不含历史来源分支身份，原比例不能代表功能设计 §29.2 的有效任务完成率。未来正式统计须排除取消、无效和历史来源分支。 |
| 测试与验证 | 回归断言先复现原错误比例67%，移除实现后任务面板定向测试9/9、git diff --check通过。本轮未操作协调正在复核的55424数据库。<br>协调独立真库复核反馈：tasks-api.integration.test.ts 41/41、4.20秒；核对本任务data_directory后正常停止其恢复的55424实例，数据保留。本次增量未重复数据库测试。 |
| 文档同步 | 保留状态筛选及所有历史读取；不新增跨域查询，不扩大到任务组或统计。新增 INVALID 历史 fixture，断言不显示完成率且全部状态仍可查看所有任务。同步交审、功能/系统设计和测试矩阵。 |
| 风险与后续 | 原条目未单列，见本表其他字段。 |
| 备注 | 按用户 2026-09-10 明确授权仅整理格式；原条目事实、当时验证状态与先后顺序保留。 |


## 2026-09-10 15:36 +08:00 | C / Codex | codex/f24-task-groups-unmerge | 本地交审

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f24-task-groups-unmerge`（已两次 rebase：`origin/main` `c7c6881` 与 `14ce2ef`；原基于 `3995cd0`，F-23 PR #81、成员 E2E、F-19 PR #85 与 F-09 加固 PR #86 合并之后） |
| PR / commit | 代码 commit `4bbd142`（`feat(task-groups): add F-24 task group unmerge`）、文档 commit `14230df`/`fc804d9`/`4b96a15`/`d12e07e`（两轮 rebase 已多次改写提交号，最终以 PR head 为准）；已推送 `codex/f24-task-groups-unmerge` 并创建 [PR #87](https://github.com/256-code/InPulse/pull/87)（`feat(task-groups): implement F-24 task group unmerge`） |
| 做了什么 | 交付 F-24 解除合并服务端纵切片：`POST /api/v1/task-groups/unmerge` 契约、`TaskGroupsService.unmerge` 锁序与同事务副作用、幂等与重放授权、真实 PostgreSQL 集成测试与文档 |
| 新增功能 | 解除活跃 SOURCE：成员关系 `DETACHED` + 时间 + 原因、组版本递增；最后一个来源解除时同事务关闭组并解除 MAIN；任务工作状态/负责人/迭代记录与行版本不变；通知去重接收人并深链来源任务；未填写原因回落固定文案；响应含 `closedAt` 与解除成员快照 |
| 修复 / 重构 / 优化 | 合并与解除共享 `TaskGroupCommandError`/`CommandErrors` 错误映射；`TaskGroupRepository` 新增 `detachMember`/`closeGroup` 与解除元数据读取；`task-groups-merge` 集成测试注册 `TaskGroupUnmergeHttpService` Provider 以适配 Nest DI；无新迁移、无依赖变更、无权限放宽 |
| 测试与验证 | 实际通过：解除集成 8/8、`task-group` 2 文件 14/14（约 6.4 秒）、集成全量 40 文件 260 例（两轮各 1 例既有偶发失败：`project-member-management-api`、`preauth-session`，单文件复跑 8/8 与 4/4 通过，均与 F-24 无关）、API 单测 63 文件 303/303、契约 11 文件 81/81、前端 37 文件 124 例（`SimilarFeatures`/`CreateProjectModal` 两个既有计时敏感用例在并行负载下偶发失败，单跑 4/4 通过）、`contract:drift` 5 产物无漂移、`contract:validate` 80 条、`permissions:check` 80/80、`lint`、`format:check`、`typecheck`、`build`、`db:migrations:check` 6 个迁移、`check:deps` 461 文件、`check:frontend:boundaries` 142 模块、`check:secrets` 698 文件、`check:deploy:test`；公共 registry 审计无已知漏洞。未运行 `pnpm test:e2e`（无前端改动）、GitHub Actions、生产镜像构建与扫描 |
| 文档同步 | 权限矩阵与测试矩阵 F-24 章节、[F-24 交审说明](docs/f24-local-handoff.md)、本日志与 `AGENTS.md` 阶段进展；Schema、Route Registry、OpenAPI 与前端生成客户端由生成工具同步更新 |
| 风险与后续 | F-25 聚合组视图与前端解除入口（二次确认 + 原因）未交付；任务组读取接口未交付；等待非作者审核与后续 PR/CI |
| 备注 | 仅改动自有交付短分支；本地验证完成后按用户 2026-09-10 明确授权提交、推送并创建 PR；同日按同一授权 rebase 到 `origin/main` `c7c6881`（冲突按双侧保留处理：`task-groups.module.ts` 模块装配、`AGENTS.md`、`docs/permissions.md`；生成物由 `contract:generate` 重生成），随后强制推送使 PR #87 重新触发 CI |

## 2026-09-10 17:29 +08:00 | B / Codex | codex/f21-record-lifecycle | 本地交审

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `test` / `docs` |
| 分支 | `codex/f21-record-lifecycle`，隔离worktree从 `fc7bb681944942cdba49454e130023a16f778222` 创建，收尾无冲突rebase到纯文档主线 `5563bcf4db3ead1103dbf7c04be98160fcd4bff3` |
| PR / commit | 代码 `9b5805805bbee6f0ff964fba75d5732701570943`；文档随后独立提交；未推送/建PR/合并 |
| 做了什么 | 按Accepted ADR-024交付管理员作废/恢复记录完整纵切片，双时间戳重认证、非空原因、If-Match与数据库幂等 |
| 新增功能 | voidChangeRecord/restoreChangeRecord；管理员VOID列表、详情及全部旧版本；前端原因/重认证/409重载和恢复后成员重新可读 |
| 修复 / 重构 / 优化 | 迁移锁等待后再次检查重认证；同事务恢复全部历史Activity及Search同键UPSERT；PUBLISHED DTO不含原因。F20两个GET明确query=none，保持原契约；版本、遗留与原跟进链接不变 |
| 测试与验证 | 真库四文件63/63（F21 22、F18发布12/读取8、F20 21）；契约四文件42/42；Web三文件7/7；86路由/权限及5生成物漂移通过；最终Edge F21 1/1（11.5秒），相邻F18 2/2；必要运行时编译与受影响类型通过。未跑全仓静态/全量测试/额外全量构建/审计/本批CI |
| 文档同步 | 三份设计、权限与测试矩阵、[F21交审](docs/f21-local-handoff.md)、AGENTS进展与本日志 |
| 风险与后续 | 列表/版本仍无分页，未实现F22/统计聚合及#89候选接口；待父协调审核与PR任务执行最新SHA的CI，本地通过不代表人工Approval |
| 备注 | 保留契约首红、DTO白名单、fixture版本/状态历史、旧数据库路径、局部编译配置和Edge NODE_ENV/定位失败历史，详情见交审。独占55426经SHOW data_directory确认后已停库，数据保留；未触碰README用户修改及三人计划 |

## 2026-09-10 17:35 +08:00 | B / Codex | codex/f21-record-lifecycle | 父审核增量修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `test` / `docs` |
| 分支 | `codex/f21-record-lifecycle`；基线 `5563bcf4db3ead1103dbf7c04be98160fcd4bff3` |
| PR / commit | 增量代码 `92f1463c85f96966ae8fda81086e5689308506a1`；文档独立提交，无推送/PR/合并 |
| 做了什么 | 修复父审核发现的409后刷新失败或关闭重开会启用旧提交问题，首个候选3cde38b未被当作最终批准 |
| 新增功能 | 无范围扩展 |
| 修复 / 重构 / 优化 | 独立needsRefresh门禁仅在成功加载最新记录后解除；失败及关闭重开保留原因/门禁/重新加载入口；必须再次明确确认后使用新版本与新Key |
| 测试与验证 | 新增回归先红，复现500刷新后旧确认按钮可用；修改过程遗漏conflict引用致4例失败、取消按钮中文空格定位再失败，修正后单文件4/4（3.04秒）。父协调独立F21真库22/22（3.46秒）、契约四文件42/42（391ms），未冒充本任务复跑。未扩大检查或重跑数据库/E2E/构建 |
| 文档同步 | [F21交审说明](docs/f21-local-handoff.md)、测试矩阵及本日志补记 |
| 风险与后续 | 待父协调复验最终冻结SHA；本地通过不等于人工Approval或CI |
| 备注 | 父协调首次pg_ctl默认5432导致55426拒连，核对本任务postmaster.pid后重启55426并通过复验；再经SHOW data_directory确认后正常停库、数据保留。增量期间本任务未启动或访问其独占数据库 |

父协调最终本地审核：已检查92f1463的独立刷新门禁实现，RecordLifecycleButton/PublishedRecordsView/EditPublishedRecord三文件独立8/8（3.27秒）通过；结合独立真库22/22、契约42/42，本地审核通过。业务代码自92f1463后保持冻结，后续PR/CI/合并由PR任务处理；本地审核不伪称GitHub人工Approval。

## 2026-09-10 17:38 +08:00 | B / Codex | codex/f21-record-lifecycle-pr | F21 PR交付

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `test` / `docs` |
| 分支 | `codex/f21-record-lifecycle-pr`；从最终冻结 `929f086955e5dd14843dfa17cea846d07f6611ac` 承接，最新主线 `5563bcf4db3ead1103dbf7c04be98160fcd4bff3` 已包含 |
| PR / commit | 本条为首次推送/创建PR前记录；业务代码 `92f1463c85f96966ae8fda81086e5689308506a1`，后续仅审核与交付文档；PR地址与最终CI结果在交付消息记录 |
| 做了什么 | 在独立3d43工作区建立B岗F21交付短分支，完整保留主线及双方日志；父协调已确认最终冻结版本本地审核通过 |
| 新增功能 | 管理员记录作废/恢复、VOID读取与状态筛选、原因与重认证交互、同事务审计及搜索/活动可见性联动 |
| 修复 / 重构 / 优化 | 独立needsRefresh保留409冲突门禁，刷新失败及关闭重开不得启用旧版本提交；恢复保留正式版本和作废快照 |
| 测试与验证 | 沿用实现真库四文件63/63、契约42/42、Edge F21 1/1及F18 2/2、86路由/权限与5生成物无漂移；父独立真库22/22、契约42/42、最终Web三文件8/8（3.27秒）。本交付不重复全量构建/静态/测试；CI尚未运行，所有失败历史保留于交审说明 |
| 文档同步 | 三份设计、权限/测试矩阵、AGENTS及F21交审已随冻结版本承接；新增本条交付记录 |
| 风险与后续 | 等待最新交付SHA的既有CI门禁及平台允许后按用户授权正常squash；不伪造人工Approval、不绕过保护或失败检查；不开展F22 |
| 备注 | gh未登录，已核验既有Git账号列表suikiiovo与GitHub浏览器登录会话；未读取或输出凭据。未修改D:\InPulse或实现工作区，未启动数据库 |

## 2026-09-10 17:41 +08:00 | B / Codex | codex/f21-record-lifecycle-pr | PR #91同步最新主线

| 字段 | 内容 |
| --- | --- |
| 类型 | `docs` / `merge` |
| 分支 | `codex/f21-record-lifecycle-pr`；同步 `origin/main` `197193e0992e3635909493e4fe44ed37adfc8c40` |
| PR / commit | [PR #91](https://github.com/256-code/InPulse/pull/91)；上一head `dd75bce530313032ce45b3a43eda0b25f90172b4` |
| 做了什么 | PR创建后fetch发现主线新增A的CI结果日志，正常合并并保留双方全部记录，无冲突 |
| 新增功能 | 无，F21业务代码继续冻结于父审核版本 |
| 修复 / 重构 / 优化 | 无业务变动 |
| 测试与验证 | 上一head Documentation run 34461848577 success；CI run 34461848584 当时in_progress，不能视为通过；本次同步将触发最终head新CI。未重复本地构建/静态/全套测试 |
| 文档同步 | 开发日志保留A的新增事实并补记本条 |
| 风险与后续 | 等最终head所有适用CI通过及平台允许后正常squash；平台人工Approval如缺失则报告阻断 |
| 备注 | 不更新旧F20分支、不开展F22、不触碰D:\InPulse；CI结果在交付消息中报告 |

## 2026-09-10 17:41 +08:00 | B / Codex | codex/f21-record-lifecycle-pr | CI格式失败修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `style` / `docs` |
| 分支 | `codex/f21-record-lifecycle-pr` |
| PR / commit | [PR #91](https://github.com/256-code/InPulse/pull/91)；失败head `dd75bce` / `bf3c1b3` |
| 做了什么 | 定位CI格式检查唯一失败文件RecordLifecycleButton.test.tsx，用锁定Prettier API与Node fs.writeFileSync修正一处调用换行 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | 三行fireEvent.click合并为一行，断言、定位、调用及业务实现均不变 |
| 测试与验证 | CI run34461848584（dd75bce）与34461920888（bf3c1b3）失败；前者job102821270943日志明确Format check唯一文件，Lint已过。两个head的Documentation 34461848577 / 34461920881 success。本地目标文件Prettier.check true，纯格式diff已核对；未重跑业务测试或全量检查 |
| 文档同步 | 本条保留失败事实；最终CI以本次推送的新head为准 |
| 风险与后续 | 等待CI重新完整执行，未盲重跑旧失败run或弱化门禁 |
| 备注 | 已向父协调说明失败原因及最小增量；无业务或测试语义变更 |

## 2026-09-10 18:39 +08:00 | B / Codex | codex/f22-external-links-pr | F22 PR交付

| 字段 | 内容 |
| --- | --- |
| 类型 | `feat` / `fix` / `test` / `docs` |
| 分支 | `codex/f22-external-links-pr`；最新基线 `5087f0acd81220a99501d62bf009160777ccf542`，从干净3d43工作区新建并完整承接冻结 `2bad40d13483fddeb469cbec2a7750fb9779871e` |
| PR / commit | 首次推送/创建PR前记录；功能代码 `45594039a95b778c26bc75b7d403d3a4c1c1d036`、审核修复 `b099b6ad46186c4fbe4b69ec75c26819f92c5182`；最终交付head/PR/CI与合并结果在交付消息报告 |
| 做了什么 | 交付F22项目/功能/任务/记录GitHub多链接列表、添加与解除；父协调已按最终冻结SHA独立审核通过 |
| 新增功能 | 三条严格目标路由、四域公开端口、真实归属鉴权、父锁/行版本/数据库幂等与同事务审计/活动/搜索；URL标准Parser本地规范化，Release存OTHER派生标签；解除保留实体及历史 |
| 修复 / 重构 / 优化 | GET/重放在父锁后重新取目标锁并读取实时状态，三个真实竞态红转绿；Registry与实际EXTERNAL_LINK_ADDED/REMOVED一致。搜索同tx组合当前URL而保留raw_text正文，容量超限安全422回滚 |
| 测试与验证 | 父独立最终2bad40d：F22+Search写真库40/40（4.75秒），契约三文件40/40（410ms），链接弹窗Web2/2（2.86秒）。实现最后编辑后F22真库36/36；前轮相邻真库71/71、Web27/27、URL+Search模块17/17、Edge2/2；89路由89权限5产物漂移、受影响类型/必要运行时编译及变更源码Prettier API check通过。交付不重复全量本地构建/静态/无关审计或真库，CI尚未运行 |
| 文档同步 | 三份设计、权限/测试矩阵、F22交审与原开发日志完整承接，新增本条父审核及交付记录 |
| 风险与后续 | 共享搜索适配器与容量映射已列重点审核；等最终head全部适用CI通过及平台允许后按用户授权正常squash，不伪造Approval或绕过保护。完成后同步dev/b，由父协调独立核验并关机 |
| 备注 | 保留误删fixture import导致beforeAll失败及所有红测历史，前轮71/71不冒充当时冻结代码可运行证据；最终修复后已重验。本任务未启动任何测试服务，0e4e实现工作区及停库数据保留；不碰D:\InPulse，不开展其他功能 |

## 2026-09-10 18:45 +08:00 | B / Codex | codex/f22-external-links-pr | PR #93 CI时序修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/f22-external-links-pr`；基线5087f0a保持不变 |
| PR / commit | [PR #93](https://github.com/256-code/InPulse/pull/93)；首轮head `5894128709378871fb8797f91b5b64394a40e8c3` |
| 做了什么 | 定位CI34467415499/job102839130984在Unit tests失败，唯一失败为既有RecordLifecycleButton刷新门禁回归第206行同步精确名称定位；Documentation34467415414 success |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | Ant Design退出loading动画尚在，按钮可访问名暂为loading 确认作废记录且仍disabled；同一回归中三个disabled断言使用waitFor等待原精确名称稳定，不改断言目标、调用次数、版本、Key或业务实现，不加固定sleep |
| 测试与验证 | 交付工作区pnpm install --frozen-lockfile通过（仅复用缓存，无lockfile变更）；RecordLifecycleButton单文件4/4（3.03秒，18:45:33），锁定Prettier API目标文件check true，diff已核对。未重复真库或全量本地门禁；首轮后续数据库/浏览器门禁未执行，不称其失败或成功 |
| 文档同步 | 本条保留真实失败原因与最小修复；最终完整CI以本次推送新SHA为准 |
| 风险与后续 | 父协调已同意最小测试时序修复，推送后核对增量；等待新SHA全部适用CI与正常合并 |
| 备注 | 不是业务状态错误解禁，不弱化断言或盲重跑旧CI；本任务未启动数据库/服务，未关机 |

## 2026-09-10 19:06 +08:00 | B / Codex | codex/f22-external-links | F22 搜索夹具回归修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/f22-external-links`；基于冻结版本 `2bad40d` 的最小增量，供 PR 任务 cherry-pick |
| 做了什么 | PROJECT 搜索夹具原文补入原 searchQuery，normalized_search_text 使用现有 normalizeSearchText 从同一原文生成；F22 项目添加链接后立即断言原搜索词仍命中该项目 |
| 修复 / 范围 | 仅共享夹具、F22 浏览器回归及本条日志；search.spec.ts 全部原有断言未改，未同步 PR 分支或覆盖其 F21 等待修复，无生产行为变化 |
| 失败事实 | 父协调报告 PR head e5e5ded 第二轮 CI 搜索失败；本地先新增回归断言，旧夹具在添加项目链接后搜索原词失败（1/1 failed，12.5 秒），证实投影刷新会丢失仅存在于规范化字段的种子词 |
| 测试与验证 | 修复后 Edge 联跑 external-links.spec.ts 与 search.spec.ts，7/7 passed（32.8 秒）；两份改动 TS 用锁定 Prettier API 格式化。未做全量构建、静态检查或审计；首次回归命令的既有 Playwright 配置自动执行 Web Vite 构建，最终联跑改用忽略的本地配置复用已有 API/Web 产物，仅启动 preview |
| 风险与后续 | 本地结果不代表新 PR head CI 已通过；交付增量 SHA，由 PR 任务保留现有变更并执行既有 CI |
| 备注 | 独占重启 55427 数据库；联跑结束后通过 SHOW data_directory 核对本工作区 .data/f22/pg，再 fast stop，保留数据。未推送、合并或关机 |

## 2026-09-10 19:07 +08:00 | B / Codex | codex/f22-external-links-pr | PR #93承接搜索夹具修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `test` / `docs` |
| 分支 | `codex/f22-external-links-pr`；承接基线e5e5ded，不反复同步主线纯日志更新 |
| PR / commit | [PR #93](https://github.com/256-code/InPulse/pull/93)；实现578ab3c cherry-pick为7e6d9fd，F21waitFor增量保留 |
| 做了什么 | 定位第二轮CI34467795626/job102840343710仅Browser E2E失败：search.spec.ts原searchQuery无结果；Documentation34467795718 success，前序单元/真库/契约/构建/镜像扫描通过 |
| 新增功能 | 无生产功能变化，新增F22添加项目链接后原搜索词仍命中项目的浏览器回归 |
| 修复 / 重构 / 优化 | 共享fixture原raw_text未包含仅在normalized字段的随机词，正常链接投影重建后丢失；现seed原文包含该词并由现有normalizeSearchText派生索引，search.spec断言完全不变 |
| 测试与验证 | 实现先新增回归使旧fixture1/1失败（12.5秒），修复后Edge external-links+search两文件7/7（32.8秒）。父核对增量通过。交付未重复本地编译/测试，仅diff --check及承接差异核对；新head完整CI待执行 |
| 文档同步 | cherry-pick开发日志追加冲突按原双方内容完整拼接，未改写历史失败；新增本条交付事实 |
| 风险与后续 | 主线9f9de6a仅回填C历史日志，合并时保留；最终合并树需包含该主线日志与本批代码。只在新SHA所有适用CI通过及平台允许后正常squash |
| 备注 | 0e4e实现库已核对data_directory并停库、数据保留；本任务未启动服务。首次实现红测配置触发Web Vite构建的事实已在实现日志保留，不冒充全程未构建 |

## 2026-09-10 19:23 +08:00 | B / Codex | codex/f22-external-links-pr | PR #93公开边界修复

| 字段 | 内容 |
| --- | --- |
| 类型 | `fix` / `docs` |
| 分支 | `codex/f22-external-links-pr`；承接1338011并保留此前时序/搜索夹具修复 |
| PR / commit | [PR #93](https://github.com/256-code/InPulse/pull/93)；实现527e9a1仅cherry-pick为64bd9d6 |
| 做了什么 | 第三轮CI34469631179/job102846235694的Browser E2E已通过，随后Dependency boundary check失败；Documentation34469631168 success。四个领域HTTP服务直接导入Search内部文件，按已有规则修正公开消费路径 |
| 新增功能 | 无 |
| 修复 / 重构 / 优化 | search/public/search-projection-errors.ts仅重导出原SearchProjectionCapacityError，四处导入改为该公开路径，原class、instanceof和422分支不变；避免大index导出的SearchModule与ProjectsModule形成环 |
| 测试与验证 | 实现一次pnpm check:deps通过（520源文件/520模块节点，无环或越界）；五文件定向TS语法/语义及锁定Prettier检查通过。首次定向TS从根目录执行缺少API node类型上下文而报node:crypto TS2307，改在apps/api上下文后通过，未弱化检查。父diff审核通过，交付未重复本地门禁，最终CI待新SHA完整执行 |
| 文档同步 | 新增本条第三轮失败与公开边界修复记录，历史失败原样保留 |
| 风险与后续 | 不修改边界扫描规则、不跳过失败门禁；等最终head全部适用CI通过及平台允许后正常squash并同步dev/b |
| 备注 | 本次修复未启动数据库或浏览器，无全量本地构建/无关审计；只承接527e9a1，不重放旧历史，最终关机由父协调执行 |
