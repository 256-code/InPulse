# InPulse 备份调度宿主配置示例（技术设计 v1.2.2 §11.5 / ADR-020）。
#
# 安装位置：/etc/inpulse/backup.env（mode 0640，root:root）。
# 本文件只放非敏感运维配置：任何凭据都必须留在 *_FILE 指向的受限文件里，
# 不得把凭据值写进环境变量。启用前由
# `deploy/backup/backupctl.sh enable --confirm-go-live` 逐项校验。
#
# 红线：备份必须加密、必须异机存放、且至少有一份通过完整恢复演练的证据；
# 上线前不部署、不运行定时备份任务（启用动作属于生产上线门禁）。
# 处理步骤见 docs/runbooks/backup-restore.md。

# 生产稳态 Compose 与镜像 ref 环境文件（由发布清单提供）。
INPULSE_COMPOSE_FILE=/srv/inpulse/deploy/compose.yaml
INPULSE_COMPOSE_ENV_FILE=/srv/inpulse/deploy/.env.deploy
INPULSE_COMPOSE_PROJECT=inpulse

# 告警接收方（地址写入 Runbook）：这里只保存文件路径，
# 文件内容为告警 Webhook URL，权限 mode 0400、属主 root。
INPULSE_BACKUP_ALERT_WEBHOOK_FILE=/etc/inpulse/secrets/backup-alert-webhook

# 超过该小时数没有成功备份记录即告警（技术设计 §11.5 固定 18 小时）。
INPULSE_BACKUP_MAX_AGE_HOURS=18
# 同一未恢复故障的最小重复告警间隔（小时），避免每小时重复刷屏。
INPULSE_BACKUP_ALERT_REPEAT_HOURS=6

# 保留策略：本机 7 天、异机至少 30 天。实际清理由 backup 服务与异机存储侧执行，
# 这里只作为启用校验与状态输出的依据（红线之一：异机副本必须存在）。
INPULSE_BACKUP_LOCAL_RETENTION_DAYS=7
INPULSE_BACKUP_REMOTE_RETENTION_DAYS=30

# 宿主状态目录：记录最近一次成功备份时间，供 watchdog 与运维状态页读取。
INPULSE_BACKUP_STATE_DIR=/var/lib/inpulse-backup