# 本地开发统一身份认证（SSO）配置模板（ADR-032 / 技术设计 §7）。
# 用法：
#   1. 复制本文件为 deploy/.env.dev.local（该文件名被 .gitignore 的 .env.* 规则忽略，不会进入仓库）；
#   2. 填写 SSO_CLIENT_ID，并把 Casdoor 应用签发的 Client Secret 保存到仓库外的本地文件，
#      在 SSO_CLIENT_SECRET_FILE 填写该文件的绝对路径；
#   3. 在 Casdoor 应用中登记回调地址 http://127.0.0.1:5173/api/v1/auth/sso/callback；
#   4. 运行 node scripts/dev-start.mjs。
# 本文件只放变量名与公开地址，不含任何 Secret 值；Client Secret 一律不得提交到仓库。
SSO_ENABLED=1
SSO_ISSUER=https://authtest.libiaorobot.com
SSO_CLIENT_ID=
SSO_CLIENT_SECRET_FILE=
SSO_REDIRECT_URI=http://127.0.0.1:5173/api/v1/auth/sso/callback
# 本地数据库连接；留空时 scripts/dev-start.mjs 使用 app_runtime@127.0.0.1:55432/app 与 audit_reader@127.0.0.1:55432/app。
DATABASE_URL=
AUDIT_DATABASE_URL=
