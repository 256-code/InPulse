/** NestJS DI token：版本化 Session HMAC keyring。 */
export const SESSION_HMAC_KEYRING = Symbol("SESSION_HMAC_KEYRING");

/** NestJS DI token：版本化 TOTP AEAD KEK keyring。 */
export const TOTP_KEK_KEYRING = Symbol("TOTP_KEK_KEYRING");
