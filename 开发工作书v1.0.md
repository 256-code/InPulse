## 💻 开发工程师
---

# 软件研发功能迭代记录与任务协作系统

# 开发工作书

**文件编号：** DEV-WB-001
**版本：** V1.0
**编制依据：**《功能设计 V1.1》《系统设计 V1.0.2》《技术设计 V1.2.2》《三人+AI 协作与交付方案 V2.1》
**性质：** 正式执行文件。本工作书规定各功能的开发责任人、作业步骤、验收标准与禁止事项；不规定完成工期，工期由阶段门禁另行管理。

---

## 第一章 总则

**1.1 目的**
规范系统全部功能的开发作业，使三名开发人员（A：平台与访问域；B：内容与任务执行域；C：聚合与发现域）及其 AI 编码代理在同一标准下逐项实施、逐项验收。

**1.2 通用作业规程（每项功能开工前必须先满足）**

1.2.1 契约先行：先编写或修改 `packages/api-contract` 中的 Zod Schema 与 Route Registry（请求、响应、错误码、权限、CSRF、幂等、并发策略），生成 OpenAPI 与客户端，经消费方评审后进入实现。
1.2.2 测试先行：先编写失败测试（含正常、越权、冲突、回滚、并发场景），再实现。
1.2.3 小步实现：一次 PR 只解决一个功能或一个可独立验收的子项；禁止一次性生成整个系统。
1.2.4 人工评审：数据库迁移、权限规则、鉴权配置、容器与编排变更必须由非作者人工评审。
1.2.5 完成定义：功能通过验收标准、CI 全绿、契约无漂移、权限矩阵用例齐全、评审意见全部解决后，方可标记完成。

**1.3 通用禁止事项（适用于全部功能）**

1.3.1 禁止修改数据库不变量（外键、唯一约束、CHECK、部分唯一索引）以绕过业务规则。
1.3.2 禁止重写已合入主分支的迁移文件；已发布迁移只增不改。
1.3.3 禁止删除、覆盖、搬迁任务、迭代记录、审计等业务历史数据。
1.3.4 禁止将网络请求放入数据库事务；禁止事务内执行长耗时操作。
1.3.5 禁止在接口中信任客户端传入的项目归属参数；禁止跨项目挂接数据。
1.3.6 禁止 AI 编码代理自行决定：新增依赖、放宽鉴权、跳过失败测试、修改 Secrets、直接合并代码。

---

## 第二章 功能清单与责任人总表

| 编号 | 功能 | 责任人岗位 |
|---|---|---|
| F-01 | 用户登录与会话管理 | A |
| F-02 | 系统管理员 MFA 与重认证 | A |
| F-03 | 用户管理 | A |
| F-04 | 项目创建（含初始成员与未分类模块） | A |
| F-05 | 项目成员管理 | A |
| F-06 | 项目编辑、归档与恢复 | A |
| F-07 | 资源鉴权与权限矩阵 | A |
| F-08 | 操作审计与哈希链 | A |
| F-09 | 数据安全专项（数据库角色、CSP、SSRF、Secrets） | A |
| F-10 | 部署、备份与恢复 | A |
| F-11 | 工程基座与 API 契约平台（CI/生成链路/依赖边界） | A |
| F-12 | 模块管理 | B |
| F-13 | 功能档案 | B |
| F-14 | 功能级任务 | B |
| F-15 | 模块级任务（影响多个功能） | B |
| F-16 | 任务状态流转与历史保留 | B |
| F-17 | 迭代记录草稿（三段式内容） | B |
| F-18 | 迭代记录发布与版本管理 | B |
| F-19 | 完成任务并发布迭代记录（组合事务） | B |
| F-20 | 遗留问题转任务 | B |
| F-21 | 迭代记录作废与恢复（管理员） | B |
| F-22 | GitHub 链接管理 | B |
| F-23 | 任务合并（主任务与来源分支） | C |
| F-24 | 解除合并 | C |
| F-25 | 任务聚合组视图 | C |
| F-26 | 全局搜索 | C |
| F-27 | 项目动态 | C |
| F-28 | 站内通知 | C |
| F-29 | 项目概览 | C |
| F-30 | 前端公共框架（路由、布局、错误边界、路由扩展机制） | C |
| F-31 | 浏览器自动化测试基座 | C |
| F-32 | 我的任务（跨项目列表） | C |

> 阶段 1 剩余工作的岗位重分配（本表按功能域固定，不随重分配变动）见本工作书末尾章节：剩余工作清算与岗位重分配（2026-09-11 生效）。

---

## 第三章 A 岗位功能工作书（平台与访问域）

### F-01 用户登录与会话管理

**功能说明：** 用户凭账号密码登录系统，获得持久会话；支持登出、空闲超时、绝对超时、停用即失效。
**开发步骤：**
1. 编写契约：`GET /auth/csrf`、`POST /auth/login`、`POST /auth/logout`、`GET /me`（含 401/429 错误码）。
2. 编写迁移：`users`（含 `auth_version`）、`user_sessions`、`session_csrf_tokens`、`preauth_sessions`。
3. 实现密码校验：Argon2id（M=19MiB、t=2、p=1 起，参数随哈希持久化）。
4. 实现登录限流：按"账号+IP+全局"三层计数，限制 Argon2 并发计算。
5. 实现 Session Store：数据库仅存 Token 的 HMAC-SHA-256 哈希（带 key_version），Cookie 为 `__Host-session; HttpOnly; Secure; SameSite=Lax; Path=/`。
6. 实现登录流程：密码校验 → 消费预认证会话 → 轮换会话与 CSRF Token（防 Session Fixation）。
7. 实现失效机制：用户停用、改密、管理员强退时同一事务递增 `auth_version` 并撤销全部会话。
**验收标准：** 数据库中不存在可还原的原始 Token；停用用户的下一个请求即返回 401；连续错误登录被限流。
**禁止事项：** 禁止使用 JWT 或无用途长效密钥；禁止将原始 Token 写入日志、错误、审计快照。

### F-02 系统管理员 MFA 与重认证

**功能说明：** 系统管理员强制启用 TOTP；高风险操作需密码+当前验证码双重重认证（5 分钟有效期）。
**开发步骤：**
1. 编写迁移：`user_totp_factors`（AEAD 密文存储）、`mfa_recovery_codes`（仅存哈希）。
2. 实现受限注册：管理员首次登录仅可进入 MFA 注册流程，未完成前不得访问业务接口。
3. 实现注册确认：连续验证一次有效验证码后方启用；恢复码一次性签发、使用后原子标记。
4. 实现防重放：验证窗口 ±1 时间步，持久化 `last_accepted_step`，条件更新保证同时间步并发仅一个成功。
5. 实现重认证：高风险操作校验 `reauthenticated_at` 与 `mfa_verified_at` 均在 5 分钟内。
6. 实现重置流程：由另一名已完成 MFA 的管理员执行（填写原因）；重置即递增目标用户 `auth_version` 并撤销全部会话；仅剩一名管理员时走双人审批离线恢复规程。
**验收标准：** 密文不含明文 Secret；恢复码不可反查；同一验证码不能重放；重置行为写入审计。
**禁止事项：** 禁止将 TOTP Secret、二维码、恢复码写入日志；禁止管理员自重置。

### F-03 用户管理

**功能说明：** 系统管理员维护用户账号：新增、编辑、启用/停用、强制退出。
**开发步骤：**
1. 编写契约与权限矩阵用例（仅管理员；停用、强退需重认证）。
2. 编写迁移与仓库：用户 CRUD、状态字段（NORMAL/DISABLED）。
3. 实现停用：递增 `auth_version` 撤销会话；停用用户按未认证处理。
4. 实现强退：批量撤销该用户会话并写审计。
5. 实现管理员列表页面与领域 E2E。
**验收标准：** 普通用户调用管理接口返回 403；停用后所有请求 401；操作全程留痕。

### F-04 项目创建（含初始成员与未分类模块）

**功能说明：** 任一启用用户可创建项目；创建者自动成为成员；可选初始成员；系统自动创建"未分类模块"。
**开发步骤：**
1. 编写契约：`POST /projects`（含幂等键、成员多选）。
2. 实现 `ProjectBootstrapWorkflow`，单个事务内完成：创建项目 → 校验并加入创建者与初始成员 → 创建未分类模块（`kind='UNCLASSIFIED'`）→ 写审计 → 写通知、活动、搜索投影。
3. 实现编号生成：`code_sequences` 单表原子 UPSERT，生成项目编码。
4. 实现页面：新建项目表单（创建者勾选且不可取消）。
**验收标准：** 任一成员无效/停用/重复则整体回滚；创建者永远在成员列表；未分类模块每项目恰好一个；七类写入同事务成功或全部回滚。
**禁止事项：** 禁止创建完成后补写未分类模块；禁止降级为多事务。

### F-05 项目成员管理

**功能说明：** 系统管理员添加/移除项目成员；成员关系保留加入/移除历史。
**开发步骤：**
1. 编写契约与权限矩阵（仅管理员，需重认证）。
2. 编写迁移：`project_members`（ACTIVE/REMOVED + 时间一致性 CHECK；活跃成员部分唯一索引）。
3. 实现添加：发送加入通知；重新加入时新增记录，不覆盖历史。
4. 实现移除：未完成任务提示改派；历史任务保留原负责人；被移除用户立即失去本项目访问权，不影响其他项目。
5. 实现页面（成员列表、增删）与越权测试。
**验收标准：** 被移除成员的旧会话下一次请求即 404；`created_by`/负责人信息不被删除；每次变更写审计。
**禁止事项：** 禁止缓存成员列表于会话；禁止项目成员自行增删成员。

### F-06 项目编辑、归档与恢复

**功能说明：** 项目成员编辑项目名称与描述；系统管理员归档/恢复项目（归档后只读）。
**开发步骤：**
1. 实现编辑（名称/描述，项目编码创建后不可改）。
2. 实现归档流程：校验原因必填、存在未完成任务时提示；归档后禁止创建下级数据；历史仍可搜索。
3. 实现恢复流程：校验父级可用性；恢复审计。
4. 实现页面与权限用例。
**验收标准：** 归档后所有写接口返回对应错误；恢复后功能完好；归档/恢复均记审计并通知。

### F-07 资源鉴权与权限矩阵

**功能说明：** 端点级权限控制，按资源真实归属判断，防止 IDOR 与跨项目串联。
**开发步骤：**
1. 实现 `ProjectAccessQueryPort`：按"项目成员关系实时查询"判断活跃成员/管理员。
2. 实现统一模式：资源查询 SQL 带 `EXISTS (SELECT 1 FROM project_members ...)`；无权时统一 404。
3. 实现 `RequireReauth` Guard（管理员高风险操作：密码+当前 TOTP）。
4. 建立可执行权限矩阵 `permissions.matrix.ts`：每一条 Route Registry 项必须至少匹配一组允许用例与一组拒绝用例（匿名/同项目成员/其他项目成员/已移除成员/停用用户/管理员）。
5. 将矩阵纳入 CI：缺失即失败。
**验收标准：** 权限矩阵覆盖全部端点；跨项目 IDOR 测试全部通过；不存在"前端隐藏按钮"式的唯一防护。
**禁止事项：** 禁止以请求体中的 `projectId` 作为授权依据；禁止管理员代读他人通知。

### F-08 操作审计与哈希链

**功能说明：** 记录全部高风险业务操作；审计可追溯、可检测篡改。
**开发步骤：**
1. 编写迁移：`audit_chain_heads`、`audit_logs`（链 ID、序号、`prev_hash`、`record_hash`、`key_version`、`canonical_version`）。
2. 实现受限追加函数：`audit_lock_head`（链头行锁）、`audit_append_locked`（SECURITY DEFINER、固定 search_path、校验序号与哈希）。
3. 实现写入协议：多 scope 按 UTF-8 字节序排序加锁；JCS 规范化序列化完整信封；HMAC 只对完整 JCS 字节计算。
4. 实现读取留痕：任何原始审计读取先经管理员重认证，再写 `AUDIT_LOG_READ`。
5. 实现密钥轮换：旧密钥先写 `AUDIT_KEY_ROTATED` 标记，下一条起用新版本；旧密钥保留至归档完成。
6. 实现远端归档：至少每小时将链头检查点写入 WORM 存储；每日导出加密明细并签名。
**验收标准：** 100 次同链并发追加无分叉、序号连续；业务事务回滚时审计同步回滚；普通 runtime 角色无审计表读写权限。
**禁止事项：** 禁止使用通用触发器与应用审计双写；禁止先提交业务再补审计。

### F-09 数据安全专项

**功能说明：** 落实数据库角色分离、浏览器安全头、XSS/SSRF 防护与 Secrets 规范。
**开发步骤：**
1. 创建八角色（bootstrap/owner/migrator/runtime/audit_writer/audit_reader/audit_archive_writer/backup）及初始化脚本；编写权限探针测试。
2. 实现 Session 哈希密钥、审计 HMAC 密钥、TOTP KEK 的版本化 keyring，仅从 `/run/secrets/*` 读取。
3. 实现安全响应头与 nonce CSP（动态 nonce 写入 CSP 与入口元数据；无 `unsafe-inline`）。
4. 实现 Markdown 渲染白名单（rehype-sanitize、禁原始 HTML）与 GitHub URL 规范化（仅 HTTPS、标准解析、白名单参数）。
5. 实现 Nginx 配置：仅信任内部代理、TLS1.2+、80→443 跳转、API no-store。
**验收标准：** 安全测试清单全部通过（XSS、CSRF、CSP、SSRF、角色权限、Secrets fail-closed）。
**禁止事项：** 禁止生产环境环境变量回退读取 Secrets；禁止日志输出密钥、Cookie、CSRF Token。

### F-10 部署、备份与恢复

**功能说明：** 建立可重复部署、可恢复、可回滚的生产环境。
**开发步骤：**
1. 编写多阶段镜像（API/Migration/Web/DB bootstrap），固定 `exact-tag@sha256` 与 Git SHA。
2. 编写 Compose：非 root、只读根文件系统、资源限制、独立迁移任务、健康检查、仅 Nginx 暴露端口。
3. 实现备份（生产上线门禁项，上线时才启用调度）：宿主调度每 12 小时 `pg_dump --format=custom`，排除会话表，管道加密，SHA-256 与签名清单，异机保留；上线前只交付可一键启用的调度配置与 Runbook，不部署、不运行定时备份任务。
4. 实现全新主机恢复规程：重建角色 → 解密恢复 → 权限收敛 → 会话清空 → 迁移校验 → smoke test。
5. 编写升级/回滚规程：expand 迁移先行，破坏性删除延后。
**验收标准：** 上线时启用定时备份调度并完成一次全新主机恢复演练，备份能真实恢复并运行 smoke test；实测 RPO≤24h、RTO≤2h；恢复后审计链与远端锚点可校验。
**禁止事项：** 禁止以"生成了备份文件"代替"恢复演练通过"。

### F-11 工程基座与 API 契约平台

**功能说明：** 提供 monorepo、CI、契约生成与依赖边界检查。
**开发步骤：**
1. 初始化 pnpm monorepo；锁定 Node 24/pnpm 11/NestJS/Drizzle/PostgreSQL 18 版本与镜像 digest。
2. 建立 Schema Registry（Zod 为数据结构唯一真相）与 Route Registry（HTTP 元数据唯一真相），共同生成 OpenAPI 3.1 与 TypeScript 客户端。
3. 建立 `UnitOfWork`/`TransactionContext`（单事务回调、显式传 tx、禁止全局客户端）与幂等中间件（先鉴权 → 幂等行 → 执行 → 同事务存响应 → 重放再鉴权）。
4. 建立 CI 门禁：frozen lockfile、lint、typecheck、单测、集成测试、契约漂移、依赖边界、构建、容器检查。
5. 编写 `AGENTS.md`、`CONTRIBUTING.md`、`permissions.md`、`test-matrix.md` 并纳入 CI 引用。
**验收标准：** 空库一键迁移；CI 在私钥缺失时 fail closed；依赖图检查禁止循环与跨模块 Repository 导入。

---

## 第四章 B 岗位功能工作书（内容与任务执行域）

### F-12 模块管理

**功能说明：** 模块是项目内的业务分类容器；仅支持一级模块。
**开发步骤：**
1. 编写契约与迁移：`modules`（`kind` NORMAL/UNCLASSIFIED、每项目唯一未分类部分索引、`UNIQUE(id, project_id)`）。
2. 实现创建/编辑：同一项目内名称不重复；未分类模块由项目创建事务自动生成，不可删除。
3. 实现归档（管理员，需重认证）：归档后不能新建功能与模块级任务；历史保留。
4. 实现模块页：功能列表（待办数/记录数/最近迭代）与模块级任务区。
**验收标准：** 未分类模块允许修改名称、描述（2026-09-09 人工确认），遵循普通模块的项目权限、父级可写、名称唯一与版本规则；`kind='UNCLASSIFIED'` 不可改变，禁止物理删除；跨项目复合外键拒绝非法归属；归档禁止项全部生效。
**禁止事项：** 禁止创建二级模块；禁止物理删除含历史数据的模块。

### F-13 功能档案

**功能说明：** 功能是长期档案：当前说明、任务、迭代记录、GitHub 链接、遗留问题的统一视图。
**开发步骤：**
1. 编写契约与迁移：`features`（编号唯一、`current_behavior`、状态、`row_version`）。
2. 实现创建：功能名称+所属模块；输入时提供**相似功能提示**（仅提醒，不阻止、不自动合并）。
3. 实现当前功能说明编辑：修改记录审计；不自动生成迭代记录。
4. 实现归档（管理员，需重认证）：归档后禁止新建任务与记录。
5. 实现功能详情页：当前说明、统计（未完成/已完成任务、记录数、遗留问题）、任务/迭代记录/GitHub/活动四个页签。
**验收标准：** 相似提示返回候选功能列表；说明修改前后内容入审计；功能编号与其项目绑定唯一。
**禁止事项：** 禁止以迭代记录自动覆盖当前说明；禁止删除功能历史。

### F-14 功能级任务

**功能说明：** 针对单一功能的执行工作：创建、指派、编辑。
**开发步骤：**
1. 编写契约与迁移：`tasks`（`scope_type=FEATURE`、`feature_id` 必填、复合外键、编号唯一）。
2. 实现创建：页面自动带入项目/模块/功能；指派人必须是项目活跃成员；默认未完成。
3. 实现编辑：标题、描述、优先级、截止时间、指派人（改派通知）。
4. 实现任务卡片/列表/详情抽屉（含所属功能最近 3 条迭代记录入口）。
**验收标准：** 指派非成员被拒绝；创建后编号唯一；功能级任务 `feature_id` 非空且与模块一致。
**禁止事项：** 禁止跨项目指派；禁止创建时绕过归属校验。

### F-15 模块级任务（影响多个功能）

**功能说明：** 一项工作影响多个功能时，只创建一份任务，用"影响功能"关系展示。
**开发步骤：**
1. 编写契约与迁移：`tasks`（`scope_type=MODULE`、`feature_id` 为空）、`task_feature_impacts`（UNIQUE 防重复）。
2. 实现创建：从模块页创建；影响功能可多选（须同模块）；可后续增删（写审计）。
3. 实现统计规则：任务数=1、记录数=1、影响功能数=N；功能页以引用方式展示，不复制。
4. 实现"何时建议拆分"提示：不同负责人/状态/验收/上线/回滚时建议改为功能级任务。
**验收标准：** 影响功能跨模块被拒绝；同一功能在多个模块级任务的影响列表中正确去重；统计不重复计数。
**禁止事项：** 禁止为每个功能复制任务数据；禁止任务数据保存多份。

### F-16 任务状态流转与历史保留

**功能说明：** 任务状态含未完成/已完成/已取消；任务完成后不删除，只改变展示位置。
**开发步骤：**
1. 实现完成：记录完成时间；完成时弹出"是否产生实际功能变化"选择（跳转 F-19）。
2. 实现取消/恢复：取消原因建议填写；已取消不计入完成率。
3. 实现重新打开：保留原完成时间与重新打开时间；不删除已有迭代记录。
4. 实现历史保留：任务编号、标题、描述、归属、负责人、创建人、时间、GitHub、合并关系、审计全部保留；默认从待办移出。
**验收标准：** 状态机非法迁移被拒绝；已完成/已取消任务仍可搜索；历史字段完整。
**禁止事项：** 禁止物理删除任务（含取消/合并后的任务）；禁止以"已合并"替换原工作状态。

### F-17 迭代记录草稿（三段式内容）

**功能说明：** 记录"为什么改（发现什么问题）→ 改了什么（怎么改的）→ 效果如何（如何验证）"三项核心内容，附选填"还有什么问题"。
**开发步骤：**
1. 编写契约与迁移：`change_records`（DRAFT/PUBLISHED/VOID、`current_payload` JSONB、`task_id` 可空且为多对一（同一任务可有多条记录，每条记录至多一个同项目来源任务）、三段式字段）。
2. 实现保存草稿：不计入正式统计，不改变来源任务既有 TODO/DONE/CANCELED 状态；可继续编辑。来源入口显式选择已有草稿或新建另一条，同 Key 重放而不同 Key 不自动去重。
3. 实现表单自动带入：项目、模块、功能/影响功能、来源任务、处理人、记录作者、时间。
4. 校验必填：三项核心字段；遗留问题为空时展示"暂无已知遗留问题"。
**验收标准：** 草稿不产生正式编号；无来源任务的独立草稿可创建；必填校验生效。2026-09-10 人工确认保留同任务多记录的既有设计/数据库不变量，修正本工作书旧“task_id 唯一”表述；不新增迁移或隐含每任务最多一个 DRAFT 限制。F-17 本地实现与验证见 [交审说明](docs/f17-local-handoff.md)，发布及发布并完成仍属 F-18/F-19。

### F-18 迭代记录发布与版本管理

**功能说明：** 草稿发布为正式记录；已发布记录修改必须生成新版本。
**开发步骤：**
1. 实现发布：生成正式编号、当前版本=1、发布时间；v1 即写入 `change_record_versions` 快照。
2. 实现独立发布接口：记录未关联任务，或关联任务已完成；关联未完成任务返回 409。
3. 实现版本更新：锁定记录 → 校验版本 → 只接收内容 DTO → 新版本快照 → 更新投影与 `row_version` → 审计。
4. 实现版本查看与差异对比。
**验收标准：** 已发布记录不能被覆盖；旧版本可查；并发编辑仅一方成功，另一方 409。
**禁止事项：** 禁止以 `Partial<数据库实体>` 作为更新入参；禁止修改 `task_id` 归属。

2026-09-10 人工确认的 V1 口径：整段非空遗留文字对应一个跨版本稳定项，同记录沿用 ID；ACTIVE 清空须明确确认解决并转 RESOLVED，再填恢复 ACTIVE；CONVERTED 修订、清空或再填都保留原转换关系，不生成第二个任务。每个非空版本保存当时全文快照，空版本不插入版本关联，旧快照不可改。发布/正式修订遗留文字上限 10000，F-17 草稿仍为 50000；完整搜索正文超出现有容量返回 422，保留输入且全部回滚。发布通知作者、处理人、来源任务当前负责人及真实所属/影响功能的创建者，去重并验证实时项目权限；修订仅通知原作者与来源任务当前负责人。F-18 本地实现及证据见 [交审说明](docs/f18-local-handoff.md)，F-19/F-20 的组合完成与转换入口仍待各自交付。

### F-19 完成任务并发布迭代记录（组合事务）

**功能说明：** 任务完成与迭代记录发布必须是单一事务，不允许出现半完成状态。
**开发步骤：**
1. 编写契约：请求仅两种模式——`WITHOUT_RECORD`（完成说明）与 `WITH_RECORD`（内联记录或草稿引用二选一）。
2. 实现流程：幂等占位 → 校验用户项目权限 → 按统一锁序锁定任务 → 条件更新（`work_status='TODO'` 且 `row_version` 匹配）→ 记录分支（内联：建记录+编号；草稿引用：校验后设置 `task_id`）→ 写入 v1 快照 → 发布 → 审计/通知/活动/搜索投影 → 保存幂等结果。
3. 处理冲突：条件更新影响 0 行时，区分不存在/无权限/版本冲突/状态冲突并返回明确错误。
4. 校验无功能变化场景：保存完成原因（测试验证、技术调研、文档补充等），不创建记录。
**验收标准：** 任一步失败全部回滚（任务保持未完成、无残留记录）；"独立发布"与"完成任务"并发时不会出现"已发布记录关联未完成任务"；幂等重放不产生重复。
**禁止事项：** 禁止分两个事务实现；禁止发布失败后任务仍标记完成。

### F-20 遗留问题转任务

**功能说明：** 迭代记录中的"还有什么问题"可一键转为跟进任务。
**开发步骤：**
1. 编写契约与迁移：`change_record_leftover_items`（跨版本稳定 ID）、`change_record_version_leftovers`（版本快照）、`leftover_task_links`（幂等唯一）。
2. 实现流程：锁定记录 → 锁定遗留项并确认版本关系 → 校验权限 → 生成任务编号 → 创建任务并插入转换关系 → 标记 CONVERTED → 审计/通知/投影。
3. 实现防重：遗留项主键唯一；同一遗留项即使更换幂等键也只转换一次，重复请求返回 409 并附既有任务引用。
**验收标准：** 并发转换只生成一个任务；原遗留文本保留；新任务自动带入来源记录信息。
**禁止事项：** 禁止删除原遗留问题文本。

### F-21 迭代记录作废与恢复（管理员）

**功能说明：** 系统管理员可作废已发布记录；作废不物理删除。
**开发步骤：**
1. 实现作废：要求管理员重认证、原因必填、预期版本；作废后默认不在时间线展示。
2. 实现恢复：恢复前校验父级可用；恢复行为写审计。
3. 实现版本保留：作废不删版本；管理员可查看全部。
**验收标准：** 普通成员调用作废返回 403；作废原因不可省略；恢复后记录回到时间线。
**禁止事项：** 禁止物理删除已作废记录。

### F-22 GitHub 链接管理

**功能说明：** 任务、功能、迭代记录、项目可关联多个 GitHub 链接（Issue/PR/Commit 等）。
**开发步骤：**
1. 编写迁移：`external_links`（规范化 URL 唯一）+ 四张类型化关联表（复合外键）。
2. 实现 URL 规范化：仅 `https://github.com/`；标准解析；移除 fragment；白名单查询参数；拒绝 userinfo、非默认端口、混淆域名。
3. 实现类型识别：Issue、PR、Commit、Release、其他。
4. 实现添加/删除：去重（UNIQUE + upsert）；跨域关联由 ExternalLinkWorkflow 先校验目标归属；操作留痕。
5. 实现展示：多链接列表、新窗口打开、`rel="noopener noreferrer"`。
**验收标准：** 跨项目关联被数据库拒绝；重复 URL 返回提示；合并任务后来源任务链接保留。
**禁止事项：** 禁止服务端抓取远程内容；禁止由客户端指定链接归属项目。

---

## 第五章 C 岗位功能工作书（聚合与发现域）

### F-23 任务合并（主任务与来源分支）

**功能说明：** 重复或相关任务合并为"主任务 + 来源任务分支"；不删除、不复制、不覆盖。
**开发步骤：**
1. 编写契约与迁移：`task_groups`、`task_group_members`（`role=MAIN/SOURCE`、`source_kind`、原状态/原负责人快照；部分唯一索引：任务仅一个活跃组、组仅一个 MAIN）。
2. 实现合并流程：幂等占位 → 预读 source/main → 按 ID 升序锁定 → 校验同项目、非自身、来源任务不在活跃组、主任务若在组须为 MAIN → 创建组与 MAIN 成员 → 创建 SOURCE 成员并保存快照 → 审计/通知/活动/搜索投影。
3. 实现分支类型：历史来源分支（默认不承接新工作、新工作提示转主任务）与活动来源分支（可继续执行、计入待办）。
4. 实现跨功能合并：原任务归属、记录、链接一律不变。
**验收标准：** 有正式记录的任务可合并；并发合并同一任务仅一次成功；数据库拒绝第二个 MAIN 与第二个活跃组；合并不改任务原工作状态。
**禁止事项：** 禁止改绑 `change_records.task_id`；禁止聚合组嵌套；禁止删除来源任务。

### F-24 解除合并

**功能说明：** 解除任务与聚合组的关系（不改任务状态、不移动记录）。
**开发步骤：**
1. 实现流程：预读后按 ID 升序锁定 SOURCE/MAIN 任务 → 锁组与成员 → 仅允许活跃 SOURCE → 标记 DETACHED（记录时间、原因）→ 若已无活跃 SOURCE 则关闭组并解除 MAIN → 审计/通知/投影。
2. 实现二次确认与解除原因（建议填写）。
3. 实现再合并限制：同一任务加入同一历史组返回 409。
**验收标准：** 解除后任务恢复独立且原状态不变；CLOSED 组无活跃成员；并发"解除 + 新增 SOURCE"时组不变量保持。
**禁止事项：** 禁止使用"回滚"表述；禁止解除时修改任务工作状态。

### F-25 任务聚合组视图

**功能说明：** 主任务与各来源分支的统一展示，支持按分支筛选历史。
**开发步骤：**
1. 实现聚合组详情页：主任务、历史/活动分支、各自迭代记录与 GitHub 链接。
2. 实现筛选：全部记录/主任务/来源任务分支。
3. 实现入口：任务卡片显示"主任务/来源任务/迭代记录 n 条"标记；任务详情抽屉提供"查看主任务"。
4. 实现历史来源分支的新建工作引导：提示归入主任务。
**验收标准：** 聚合页信息与各任务原数据一致（不复制）；来源分支记录来源标注正确。
**禁止事项：** 禁止聚合页展示未经脱敏的原始审计内容。

### F-26 全局搜索

**功能说明：** 支持项目、模块、功能、任务、迭代记录、遗留问题、GitHub 标识的全文搜索，结果分类展示。
**开发步骤：**
1. 编写迁移：`search_projection`（`UNIQUE(project_id, entity_type, entity_id)`、`normalized_search_text` + PGroonga 全文索引），并安装 `CREATE EXTENSION pgroonga`。
2. 实现投影写入（UPSERT）：业务事务内调用；含 `project_id`；字段脱敏；归档/恢复/作废时同步可见性。
3. 实现规范化：NFKC、小写、合并空白、统一全半角标点；原文保留用于展示。
4. 实现查询：先经 `ProjectAccessQueryPort` 取得 `AuthorizedProjectScope` 再查询；参数化；查询使用 `&@~ app.pgroonga_query_escape($1)`；最短长度 2 字符；分页；跨项目 0 条。
5. 实现结果分类页（功能/任务/迭代记录/遗留问题）。
**验收标准：** 中文短词、完整英文缩写、完整代码标识符、完整编号金标 Recall@20≥90%；默认配置下目标查询走 PGroonga 索引；无权限项目 0 返回；空/超长/特殊字符不报错；不要求任意英文/代码子串；不提供正则搜索。
**禁止事项：** 禁止全量拉取后前端过滤；禁止由客户端提供授权范围；禁止在查询中拼装业务内部表；禁止使用未转义 `&~` 或直接透传 Groonga 高级查询语法。

### F-27 项目动态

**功能说明：** 面向普通成员的自然语言动态流（脱敏）。
**开发步骤：**
1. 编写迁移：`activity_projection`（来源审计链 ID/序号唯一、脱敏 DTO 字段）。
2. 实现写入：业务事务内经 `ActivityWritePort` 写入；标志归档/恢复/作废可见性。
3. 实现查询：先取 `AuthorizedProjectScope` 再查询；返回白名单字段。
4. 实现页面：项目内动态时间线。
**验收标准：** 动态不含原始审计快照；普通成员仅见所属项目动态；投影与审计来源一一对应。
**禁止事项：** 禁止向普通成员开放原始审计查询接口。

### F-28 站内通知

**功能说明：** 按场景通知相关人员（指派、完成、合并、发布、遗留转任务等），前端轮询读取。
**开发步骤：**
1. 编写迁移：`notifications`（`UNIQUE(source_event_id, recipient_id, notification_type)`）。
2. 实现写入：业务事务内经 `NotificationWritePort`；接收方由服务端按场景计算。
3. 实现查询：游标 `(created_at, id)` 签名编码；`recipient_id` 强制当前用户；分页。
4. 实现已读/未读与未读数；页面跳转目标对象。
5. 实现前端轮询（30～60 秒）。
**验收标准：** 用户只能看到自己的通知；同一业务事件不重复生成；被移除成员不再收到该项目通知。
**禁止事项：** 禁止接受客户端指定收件人；禁止管理员代读。

### F-29 项目概览

**功能说明：** 项目统计与最近动态的汇总页（模块数、功能数、未完成任务、迭代记录数、最近迭代、待处理遗留问题）。
**开发步骤：**
1. 分析数据来源：仅允许来自只读投影与各域公开只读 Port 的聚合结果。
2. 实现服务端聚合接口（如必要）：按依赖方向放在聚合域或由对应领域提供只读 Port；禁止前端跨域拼装。
3. 实现页面：统计卡片 + 最近迭代列表 + 遗留问题入口。
**验收标准：** 统计口径与功能设计 §29 一致（模块级记录不重复计数）；无权限项目不返回。
**禁止事项：** 禁止前端直接请求各域内部接口拼接概览数据。

### F-30 前端公共框架

**功能说明：** 提供路由、布局、登录态控制、错误边界、页面扩展机制与公共组件库。
**开发步骤：**
1. 建立依赖方向：`app → pages → features → shared/generated`；ESLint/dependency-cruiser 阻断反向依赖。
2. 建立路由扩展机制：每个领域页面目录自带路由段，`app/` 自动聚合注册。
3. 建立布局与鉴权壳：未登录跳转、管理员路由守卫、全局错误边界。
4. 建立数据访问规范：服务端数据一律 TanStack Query；表单用 RHF+Zod；URL 承载筛选状态；**禁止创建服务端实体副本**。
5. 建立 409 冲突处理规范：保留输入 → 提示已修改 → 重新加载 → 字段差异。
6. 建立 `shared/` 准入规则：至少两个消费者才可进入。
**验收标准：** 依赖方向 CI 检查通过；领域负责人可新增页面而不修改他人文件；CSP nonce 方案生效。
**禁止事项：** 禁止组件直接裸写 fetch/axios；禁止在 shared 中放业务逻辑。

### F-31 浏览器自动化测试基座

**功能说明：** 提供 Playwright 框架、浏览器矩阵、报告机制与稳定性规范。
**开发步骤：**
1. 搭建 Playwright 基座：登录复用、数据造数、报告与截图。
2. 定义场景归属：各领域 E2E 由领域负责人编写场景与断言；你维护框架与稳定性。
3. 纳入 CI：关键路径（登录→项目→任务→完成记录→合并→解除→搜索→通知）全量执行。
**验收标准：** 关键路径可重复执行；失败可定位（截图+traces）；不稳定归因明确（框架问题归你，业务问题归相应领域）。
**禁止事项：** 禁止用 E2E 替代数据库并发/事务集成测试。

### F-32 我的任务（跨项目列表）

**功能说明：** 当前用户跨项目的任务列表，支持筛选（状态、范围、是否有记录）。
**开发步骤：**
1. 通过 TasksModule 公开的只读 Port 提供服务端聚合与分页（权限过滤由服务端按成员关系执行）。
2. 复用 B 的任务卡片/列表组件；页面由本域维护。
3. 实现筛选：任务状态、任务范围、是否有迭代记录、负责人=我。
**验收标准：** 只返回当前用户有权访问项目的任务；统计口径与功能设计 §29 一致。
**禁止事项：** 禁止在本页面按项目逐次请求后前端合并全量数据。

---

## 第六章 协作接口与评审约定

**6.1 接口责任（提供方 → 消费方）**

| 接口 | 提供方 | 消费方 |
|---|---|---|
| UnitOfWork / TransactionContext / 幂等中间件 | A | A、B、C 全部 |
| ProjectAccessQueryPort | A | B、C（写查询前必调） |
| AuditWritePort | A | 全部命令与 Workflow |
| RequireReauth Guard | A | B（F-12/F-13/F-21 管理员操作）、C（管理侧场景） |
| Modules 创建 CommandPort | B | A（F-04 创建未分类模块） |
| ModuleQueryPort / FeatureQueryPort | B | Tasks、ChangeRecords、Workflow |
| TaskQueryPort | B | C（F-23/F-24/F-32） |
| Notification / Activity / SearchProjection WritePort | C | A、B 全部命令与 Workflow |

**6.2 评审约定**

| 变更类型 | 必选评审 |
|---|---|
| 身份、会话、CSRF、MFA（A） | B |
| 项目与成员、数据库角色、部署与恢复（A） | B |
| 审计哈希链（A） | C |
| 模块、功能、任务、记录（B） | C |
| TaskCompletion / Leftover Workflow（B） | A |
| GitHub 链接（B） | A |
| TaskGroups、合并/解除（C） | B |
| 搜索、动态、通知（C） | A |
| 前端框架与 Playwright 基座（C） | A |

任何高风险变更（数据库迁移/角色、不变量、鉴权、权限、Secrets、API 契约、Dockerfile、Compose、CI 工作流）必须至少一名非作者人工批准；作者不得自批。

**6.3 冲突处理顺序**
ADR → 功能设计/系统设计/技术设计（各自负责的事实）→ 权限矩阵/测试矩阵 → AGENTS.md/CONTRIBUTING.md → 本工作书。发现冲突时暂停受影响工作、记录冲突与候选方案，由人工定案；未受影响工作继续。

---

## 第七章 附则

**7.1** 本工作书由全体开发人员共同维护；功能范围变化时，先修订《功能设计 V1.1》，再同步本工作书。
**7.2** 每项功能完成后，将验收证据（测试结果、评审记录、契约生成物）归档至对应功能记录。
**7.3** 本工作书自发布之日起执行。

**编制：** ＿＿＿＿＿＿　**审核：** ＿＿＿＿＿＿　**批准：** ＿＿＿＿＿＿　**日期：** ＿＿＿＿＿＿

---

这份工作书共 7 章、32 项功能（A 11 项、B 11 项、C 10 项），每项功能均按"功能说明 → 开发步骤 → 验收标准 → 禁止事项"四段式编写，可直接作为任务卡模板和 AI 代理的作业依据。如需把其中某一章（如 F-19 组合事务）再展开为子步骤级详细作业指引，告诉我功能编号即可。

## F-19 本地实施补记（2026-09-10）

已接入严格两模式的 completeTask 组合请求和实际变化 UI；WITHOUT_RECORD 沿用 F-16 六原因，WITH_RECORD 内联或明确草稿引用互斥。公开端口负责同事务条件完成、仅 NULL 来源绑定、v1 发布及全部副作用；预读完整父级/影响并集并按任务→组→记录顺序锁后重读，保留历史记录/快照和活动 SOURCE 执行能力。F-18 已合并后才开始本批，F-20 等本批审查/PR/CI/合并后再开展。验收证据及未完成的检查见 [F-19 交审说明](docs/f19-local-handoff.md)。

F-19 兼容补齐：旧 transitionTask/transitionModuleTask 的 COMPLETE 同步进入组合流程，关闭历史 SOURCE 和相关作者通知绕行；旧请求/响应不变，两旧 operation 幂等/重放授权升级 2.0.0，旧 Key 409。REOPEN/CANCEL/RESTORE 经公开端口保留行为。该项随 F19 一并交审，F20 仍须等待 F19 合并。
### F-20 本地交审补记（2026-09-10）

在F19实际合并c7c6881后从最新main新建独立短分支，完成遗留转任务POST、继承预览GET和来源GET。复用既有三张遗留表和唯一/复合约束，无新迁移；单事务任务/编号/link/CONVERTED/全部副作用，任务TODO且成员指派验证。MODULE继承锁后ACTIVE影响，历史不改，预览后变化409要求确认；整段稳定项在CONVERTED编辑/清空/回填时永不重建任务。前端双向链接、可选截止时间和冲突恢复，21例真库及四相关文件82/82、Web13/13、契约41/41、相关Edge5/5。仅本地交审，未推送/PR/CI；F21不开展，详见[交审说明](docs/f20-local-handoff.md)。

---

## 剩余工作清算与岗位重分配（2026-09-11 生效）

**背景：** 第二章的 F-01～F-32 责任人表按功能域固定，本次不再改动；本节只针对阶段 1 当前尚未交付的剩余工作重新分配执行岗位。截至 2026-09-11，`main` 为 `9d7fad0`，契约已登记 93 操作 / 93 路由，F-01～F-32 的主体功能与 R-1～R-7 聚合读契约均已落库。历史负载集中在 A 与 C（近 20 个已合并 PR 中 A 5 个、C 11 个、B 3 个），B 明显空闲，因此本节把 A 的非核心项与 C 的记录域项转出。

**2026-09-11 修订 D-1：** 产品对「F-25 步骤 3 迭代记录 n 条」定案为**补条数**，不降级为布尔标记。据此新增 A-7 与 B-7，并把 F-06 归档恢复 E2E 调整为 C-6。裁决全文见 [F-25 / F-29 / F-32 契约评审裁决](docs/a-contract-review-f25-f29-f32.md) 第 11 节。

**2026-09-11 修订（B-3）：** 产品对 B-3「`/records` 页对齐设计师稿单页结构」定案为**暂缓**（不列入本轮可执行负载，归属 B 不变）；恢复开工需先定案跨项目记录读路由（`listChangeRecords` 与 `listRecordDrafts` 均先选项目，`RecordListQuery` 只有 `status`），按独立契约纵切片排期。

### 一、剩余工作清单与责任人

**执行顺序（2026-09-11 按阻塞关系排序）：** 三张清单内均按本轮开工顺序排列，排序键：① 能解锁其他岗位工作的项置顶（A-7、B-7、C-5）；② 其余无前置项按量级升序、同量级保持原顺序；③ 受硬前置约束的项标注「前置」，前置落库后立即开工，前置提前完成可插队、届时未就绪则顺延下一项；④ 暂缓项（B-3）与真实环境项（A-6）不占队位。跨岗位关键链：**A-7 ∥ B-7 → C-1**、**C-5 → A-5**。

#### 1.1 A（平台与访问域）

| 编号 | 剩余工作 | 量级 | 事实依据 |
|---|---|---|---|
| A-7 | 契约修订 D-1：R-3 `MyTaskItem` 增加 `publishedRecordCount`；R-5 由「聚合组成员关系」扩为「任务记录标记批量读」（`groupRole` 与 `groupId` 改为可空，响应覆盖请求中每一个有权 taskId）；同步 Route Registry 描述、权限矩阵、OpenAPI 与生成客户端 | S | 裁决 §11；`packages/api-contract/src/contracts/aggregate-read.zod.ts`；2026-09-11 本地落库（R-3 / R-5 契约、服务与生成客户端扩展，R-5 服务改为 TaskQueryPort.listByIds → listGroupRoles + countPublishedByTask 组合并覆盖未入组空值；分支 `feature/a7-contract-d1`，见 [开发日志](./开发日志.md) 2026-09-11 A 条目与 [测试矩阵](./docs/test-matrix.md) A-7 章节） |
| A-2 | 未裁决契约项定案：C-001（生成客户端输出位置文档冲突）、C-004（错误 `details` Schema）、C-005（CSRF 专用错误码）、C-007（生成客户端运行时校验策略）、C-008（`message` 交互语义） | S | 契约裁决记录缺项；2026-09-11 本地落库（五项全部定案并写入 [A 的契约评审裁决](./docs/a-contract-review-frontend-consumption.md)：C-001 目录冻结、C-004 `details` 保留键、C-005 CSRF 四码、C-007 不做运行时校验、C-008 `message` 语义；`error.zod.ts` 字段说明随生成物同步；分支 `feature/a2-contract-rulings`，见 [开发日志](./开发日志.md) 2026-09-11 A 条目与 [测试矩阵](./docs/test-matrix.md) A-2 章节） |
| A-1 | `operations/backup` 服务本体：`deploy/compose.yaml` 目前只有 `audit-archive` 服务，缺 `backup` 服务与镜像入口接线；`deploy/backup/backupctl.sh` 在该服务缺失时 fail-closed | M | `deploy/compose.yaml`、`deploy/backup/backupctl.sh` |
| A-3 | `compose.init` 首次建库纵切片：版本化临时覆盖与灾难恢复离线 Runbook | M | 技术设计 V1.2.2 部署章节；2026-09-11 本地落库（新增 `deploy/compose.init.yaml` 一次性覆盖与 [灾难恢复离线 Runbook](./docs/runbooks/disaster-recovery.md)；`010_passwords.sql` 容器内 Secret 路径与稳态对齐为 `db_*`；`scripts/check_deploy_refs.mjs` 增加 overlay 渲染与 bootstrap 凭据静态防线；本地 Docker 实测首次建库、角色探针与稳态接管通过；分支 `feature/a3-compose-init`，见 [开发日志](./开发日志.md) 与 [测试矩阵](./docs/test-matrix.md) A-3 章节） |
| A-4 | SEC-003 CSRF 完整生命周期 E2E：首登、轮换、刷新、多标签、过期，以及仅未消费状态最多重签一次 | M | 测试矩阵 SEC-003（Required）；2026-09-11 本地落库（新增 `apps/api/test/csrf-lifecycle.integration.test.ts` 与 `apps/e2e/tests/csrf.spec.ts`，见 [测试矩阵](./docs/test-matrix.md) A-4 章节与 [开发日志](./开发日志.md)） |
| A-5 | 阶段 4 搜索容量门禁：搜索投影 ≥ 100,000 条且不低于 5 年容量模型峰值的 1.2 倍、≥ 200 条冻结金标、30 并发持续 10 分钟、预热后 P95 < 500ms 与 P99 < 1s，并单独记录冷缓存 | L | 技术设计 V1.2.2 阶段 4；**前置 C-5，落库后开工** |
| A-6 | 上线门禁（需真实环境，非本轮可执行）：RECOVERY-001 全新主机恢复演练、DEPLOY-003 真实启用与告警投递、systemd 与 WORM 真机联调、CI-018 真实镜像 digest 绑定与签名发布清单、TLS 签发与续期 | L | 测试矩阵 RECOVERY-001、DEPLOY-002、DEPLOY-003 |

#### 1.2 B（内容与任务执行域）

| 编号 | 剩余工作 | 量级 | 事实依据 |
|---|---|---|---|
| B-7 | 记录侧只读端口把「已发布任务 ID 集合」改为「任务 → PUBLISHED 记录数」映射（单条 SQL、分页前过滤、宿主沿用记录侧，不新增依赖边），供 R-3 与 R-5 同时消费 | S | `change-records/my-task-query.port.ts`、`aggregate-read/my-tasks-query.service.ts`；裁决 §11；2026-09-11 已交付（[PR #116](https://github.com/256-code/InPulse/pull/116) CI 通过后 squash 合入 main `3b5fe90`：端口删除 listTaskIdsWithPublishedRecords、统一为 countPublishedByTask 计数映射，R-3 服务改为消费计数；R-3/R-5 的响应字段扩展属 A-7；分支 `codex/b7-task-record-counts`，见 [开发日志](./开发日志.md) 2026-09-11 B 条目与 [测试矩阵](./docs/test-matrix.md) B-7 章节） |
| B-2 | F-12 交接文档尾部「未运行」项收尾 | XS | `docs/f12-local-handoff.md`；2026-09-11 已交付（[PR #117](https://github.com/256-code/InPulse/pull/117) CI 通过后 squash 合入 main `bff1972`：尾部「未运行」清单按后续 CI 运行逐项收尾，见 [F-12 本地交审](./docs/f12-local-handoff.md) 收尾章节；分支 `codex/b2-f12-handoff-closure`，见 [开发日志](./开发日志.md) 2026-09-11 B 条目） |
| B-6 | 过时状态句修订：README 当前状态段「任务完成、记录作废/恢复、合并等业务事件尚未接入」已不成立（相关写端口均已注入）；测试矩阵 CI-017 状态行同样过时 | XS | `README.md`、`docs/test-matrix.md`；由 A 与 C 转入；2026-09-11 已交付（README 当前状态段、「下一步」与「尚未提供」行按已接入事实修订，CI-017 行证据更新为本地全量 45/45 与 main 推送运行 34578707754；分支 `codex/b6-stale-status-sentences`，见 [开发日志](./开发日志.md) 2026-09-11 B 条目） |
| B-1 | F-18 / F-19 正式与草稿记录列表分页：当前契约未登记 cursor 与 limit，属最小纵切片遗留 | M | `published-record-routes.ts`、`record-draft-routes.ts`；2026-09-11 已交付（F-18 正式记录与 F-17 草稿列表改为 C-006 签名游标分页，F-19 完成流程的草稿选择同步接入「加载更多」；分支 `codex/b1-record-pagination`，见 [开发日志](./开发日志.md) 2026-09-11 B 条目与 [测试矩阵](./docs/test-matrix.md) B-1 章节） |
| B-4 | `/audit` 动态审计页前端：后端 `GET /api/v1/audit-logs` 已交付，页面当前仍是 `WorkspacePlaceholder` | M | `apps/web/src/pages/audit/AuditPage.tsx`；由 A 转入；2026-09-11 本地落库（`/audit` 改为管理员页并接入 `getAuditLogs`：链选择、筛选、签名游标分页、原始快照弹窗与 403 重认证后重读；路由 `requiresAdmin: true` 且侧栏入口仅管理员可见；前端单测 66 文件 310 例；分支 `codex/b4-audit-page`，见 [开发日志](./开发日志.md) 2026-09-11 B 条目与 [测试矩阵](./docs/test-matrix.md) F-08 前端验收行） |
| B-5 | Markdown 白名单渲染（迭代记录内容）：需引入 react-markdown 与 rehype-sanitize，按第 4 节走独立依赖 PR | M | 由 A 转入（记录内容属 B 域）；**依赖 PR 可先送审，评审期间继续 B-1 / B-4** |
| B-3 | `/records` 页对齐设计师稿单页结构：「记录一次迭代」CTA、draft-strip、四项筛选 toolbar、按日期分组的 timeline-block，替换当前的「草稿 / 已发布」分段切换 | L | 设计师稿 `views/records.tsx`；由 C 转入（涉及 F-17 / F-18 / F-19 视图，属记录域）。**2026-09-11 定案：暂缓，不列入本轮负载，归属 B 不变**；完整对齐需新增跨项目记录读路由（`listChangeRecords` 与 `listRecordDrafts` 均先选项目，`RecordListQuery` 只有 `status`），恢复时按独立契约纵切片排期 |

#### 1.3 C（聚合与发现域）

| 编号 | 剩余工作 | 量级 | 事实依据 |
|---|---|---|---|
| C-5 | 阶段 4 金标查询集由 100 条扩到 ≥ 200 条，供给 A-5 | S | 技术设计 V1.2.2 阶段 4；2026-09-11 已交付（[PR #119](https://github.com/256-code/InPulse/pull/119) squash 合入 main `8ac285d`：阶段 4 冻结金标 100 → 200（190 normal + 7 no-result + 3 edge，`GOLDEN_QUERY_VERSION=phase4-v1`），API 召回断言同步 200 / ≥180，三份 PoC artifact 重新生成；见 [开发日志](./开发日志.md) 2026-09-11 C 条目） |
| C-4 | `.page-content` 与 `.badge` 全局样式改动后的全站人工视觉复核 | S | PR #105 交付说明；2026-09-11 已交付（[PR #126](https://github.com/256-code/InPulse/pull/126)：17 张视图截图逐张核对（1280 / 1680 两档宽度，含任务详情弹窗三 tab 与项目概览六指标 strip），未发现需改代码的缺陷；证据见 [开发日志](./开发日志.md) 2026-09-11 C-3/C-4 条目与[测试矩阵](./docs/test-matrix.md) C-4 章节） |
| C-6 | F-06 项目归档与恢复 Playwright E2E | S | 测试矩阵 F-06 段注明未覆盖；由 B 调整至 C（E2E 由非功能作者编写，符合非作者评审约定）；2026-09-11 已交付（[PR #119](https://github.com/256-code/InPulse/pull/119)：新增 `apps/e2e/tests/project-archive.spec.ts`，项目创建 helper 增加可选 `successTimeoutMs`，证据见 [开发日志](./开发日志.md) 2026-09-11 C 条目与[测试矩阵](./docs/test-matrix.md) F-06 段） |
| C-1 | F-25 步骤 3 入口标记：任务卡片与详情抽屉的「主任务 / 来源任务」徽章、「查看主任务」与「迭代记录 n 条」。机制为页面级一次批量调用 R-5（`taskIds` 1..100），`groupRole` 为 `null` 时隐藏导航入口，`groupId` 导航 `/task-groups/{groupId}`；条数取自 D-1 落库后的 `publishedRecordCount`；不得扩展任务基础 DTO | M | 裁决 §10.4 与 §11；`docs/c-v1-alignment.md`；**前置 A-7 与 B-7，落库后立即开工**；2026-09-11 本地落库（分支 `dev/c`，未提交未推送：新增 `features/tasks/task-marks.ts` 页面级一次批量调用 R-5；任务列表 / 卡片 / 详情显示「主任务 / 来源任务」徽章与「迭代记录 n 条」、`SOURCE` 详情提供「查看主任务」；任务详情由抽屉改为居中弹窗（`Modal centered width={1000}`），对齐设计师稿；证据见 [开发日志](./开发日志.md) 2026-09-11 C-1 条目与[测试矩阵](./docs/test-matrix.md) C-1 章节） |
| C-2 | F-29 / F-32 适配器第二轮字段接线与降级清零：`my-tasks-server.ts` 当前仍返回 `stats`、`scopeCounts`、`leftoverCount`、`leftoverSample` 为 `null`，`MY_TASKS_V1_FILTER_SUPPORT` 全 false；`project-overview-server.ts` 仍降级 `activeLeftoverTotal` 与 `recordTitle`。接线后同步 `my-tasks-types.ts`、`project-overview-v1.ts` 的缺口注释并补 Playwright 关键路径 | L | 代码与 `docs/c-v1-alignment.md`；2026-09-11 已交付（[PR #119](https://github.com/256-code/InPulse/pull/119)：任务中心与项目概览两页 server adapter 全量接线并移除降级分支，`aggregate-views.spec.ts` 断言由降级翻转为接线，见 [开发日志](./开发日志.md) 2026-09-11 C 条目） |
| C-3 | F-29 / F-32 页面视觉与 mock 数据集收尾 | M | `docs/c-v1-alignment.md`；**衔接 C-2 之后**；2026-09-11 已交付（[PR #126](https://github.com/256-code/InPulse/pull/126)：任务详情弹窗改为设计稿的「动作行 + 标签页（任务信息 / 迭代记录 [n] / 合并与分支 [· #groupId]）+ facts 面板」结构，`TaskStatusPanel` 重写为受控 `action` 面板且页面不再使用抽屉；证据见 [开发日志](./开发日志.md) 2026-09-11 C-3/C-4 条目与[测试矩阵](./docs/test-matrix.md) C-3 章节） |

### 二、待人工决策与已知风险

| 事项 | 说明 |
|---|---|
| B-3 `/records` 单页改造 | 已定案（2026-09-11）：**暂缓**，归属 B，不列入本轮负载；恢复开工时按独立契约纵切片排期（完整对齐需新增跨项目记录读路由：现有 `listChangeRecords` 与 `listRecordDrafts` 均按项目取，`RecordListQuery` 只有 `status`）；折中切片可只做页头创建入口、来源筛选与日期分组 |
| C-1 前置 | 「迭代记录 n 条」须等 A-7 与 B-7 落库；在此之前 C 不得以条数实现标记，也不得按任务逐个请求。2026-09-11 两项前置均已落库（B-7 [PR #116](https://github.com/256-code/InPulse/pull/116)、A-7 [PR #118](https://github.com/256-code/InPulse/pull/118)），C-1 按 D-1 的 `publishedRecordCount` 实现，该约束已满足 |
| 偶发 E2E 失败 | 开发日志记载 `leftover-task` 曾出现 1 次 `POST .../leftover-task` 500（19.2 秒），复跑通过、根因未定位，不得视为已修复；2026-09-11 全量 E2E 46 例中 `module-tasks` F-15 出现 1 次「保存后 `新建任务` 弹窗 10 秒内未关闭」（`expect(create).toBeHidden()` 超时），单文件复跑 21.2 秒通过、根因未定位，同样不得视为已修复；2026-09-11 另定位到一类本机偶发 500 的直接根因：`idempotency_records_retention_check`（`expires_at <= created_at + 30 days`）被时钟偏差的边界值拒绝——JS 宿主 `Date.now() + 30d` 比 Docker PostgreSQL `now()` 快约 0.1ms，实例 `created_at 09:55:51.576903+00` / `expires_at 10:55:51.577+00` 超出 30 天上限 97µs，表现为写请求 500 与保存后弹窗不关闭（Linux / CI 单一时间源不受影响）；根因已定位但不得视为已修复，F-15 与 leftover-task 等既有条目在取得直接证据前仍按未定位处理 |

### 三、重分配后的负载

| 岗位 | 承接项 | 量级合计（XS 0.5 / S 1 / M 2 / L 3） |
|---|---|---|
| A | A-1 至 A-5、A-7 | 约 11 |
| B | B-1、B-2、B-4 至 B-7（B-3 暂缓） | 约 8 |
| C | C-1 至 C-6 | 约 10 |

A-6 为上线门禁，依赖真实环境，不计入本轮可执行负载，但归属不变。开工顺序见 §一 排序键与各表内行序（A-7、B-7、C-5 置顶；A-7 ∥ B-7 → C-1、C-5 → A-5）。B-3 暂缓后不计入 B 的本轮可执行负载。

**生效约定：** 本节只调整剩余工作的执行岗位，不改变第二章的 F-01～F-32 功能归属与第六章的接口提供方、评审约定。本节涉及的数据库迁移与角色、业务不变量、鉴权、权限、Secrets、API 契约、Dockerfile、Compose 及 CI 工作流变更，仍按第 6.2 节由非作者人工评审。
