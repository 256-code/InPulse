# 软件研发功能迭代记录与任务协作系统
## V1.2.2 技术设计方案

**文档状态：** 已完成第二轮架构、安全与运维整改，候选实施版（须通过阶段 0 与上线阻断项）

**对应产品文档：** [《功能设计 V1.1》](功能设计v1.1.md)

**对应系统设计：** [《系统设计文档 V1.0.2》](系统设计文档v1.0.2.md)

**目标团队：** 1～3 人，以 AI 编码代理为主要生产力，人工负责架构、评审与验收

**目标部署：** 国内、单企业、单实例，预计 10～300 名用户

**修订记录：**

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.2.1 | 2026-09 | 首轮系统设计同步整改 |
| V1.2.2 | 2026-09-04 | 修复数据库角色与 PostgreSQL 18 挂载、事务传播、并发锁序、认证审计、部署备份和测试缺口 |

---

# 0. V1.2.2 核心结论与同步整改

本系统采用**模块化单体 + 前后端分离 SPA + 单一关系数据库**，V1.2.2 不引入微服务、Redis、Elasticsearch、WebSocket 和 Kubernetes。

推荐技术栈：

| 层次 | 选择 | 决策说明 |
|---|---|---|
| 主语言 | TypeScript，严格模式 | 前后端统一；编译反馈适合 AI 迭代 |
| Monorepo | pnpm workspace | 单仓、单 lockfile、统一脚本 |
| 前端 | React + Vite | 内部重交互 SPA，无 SSR/SEO 需求 |
| UI | Ant Design | 表单、表格、树、抽屉和弹窗覆盖完整 |
| 前端数据 | TanStack Query | 服务端数据缓存、失效和重试统一 |
| 前端表单 | React Hook Form + Zod | 表单状态与运行时校验 |
| 后端 | NestJS，默认 Express Adapter | 模块、依赖注入、Guard、Pipe、Interceptor 约定明确；Session 生态成熟 |
| API | REST JSON，`/api/v1` | 明确的外部边界，便于测试和后续集成 |
| API 契约 | OpenAPI 3.1 + 生成式 TypeScript Client | 线上的 HTTP 契约是唯一真相，不直接复用数据库实体 |
| 数据库 | PostgreSQL 18 | 事务、约束、行锁、JSONB、PGroonga |
| 数据访问 | Drizzle ORM + 显式 SQL 迁移 | 类型安全，同时保留 SQL、锁和索引控制力 |
| 认证 | 服务端 Session + HttpOnly Cookie | 支持主动失效，适合内部系统 |
| Session Store | PostgreSQL | V1.2.2 不为 Session 单独引入 Redis |
| 审计 | 应用层业务审计 + 数据库只追加保护 | 保存业务语义，避免触发器与应用双写 |
| 通知 | 数据库通知表 + 轮询 | V1.2.2 先保证可靠性；后续可升级 SSE |
| 搜索 | PostgreSQL + PGroonga | V1 支持中文短词、完整英文缩写、完整代码标识符和完整编号；默认 TokenBigram 满足 V1 |
| 测试 | Vitest + PostgreSQL 集成测试 + Playwright | 覆盖逻辑、真实约束、并发与关键用户路径 |
| 部署 | 多阶段镜像 + Docker Compose + Nginx | 可重复构建，无宿主机前端产物依赖 |
| 日志 | pino 输出 stdout | 由 Docker 日志驱动轮转和采集 |

V1.2.2 与《系统设计文档 V1.0.2》同步整改，关键变化：

1. OpenAPI 采用 Code-first：Schema Registry/Zod 是请求与响应数据结构真相，Route Registry 是 HTTP 操作元数据真相；
2. 本期不实现 Outbox，不建 `outbox_events` 表；
3. 新增 WorkflowModule 编排跨模块事务，领域模块不再互相调用；
4. 新增脱敏 ActivityProjection 和 SearchProjection；
5. 审计目标改为“可追溯、抗篡改”，增加哈希链、运行时只读与远端归档；
6. Session 数据库只存 Token 哈希，CSRF 固定同步 Token；
7. 增加接口级权限矩阵、管理员 MFA/重认证和生产部署阻断项。

## 0.1 版本策略

采用“固定主版本 + 锁定补丁版本和镜像 digest”：

| 组件 | 主版本基线 | 锁定要求 |
|---|---|---|
| Node.js | 24.x | `engines` 与 CI 固定；进入 Maintenance 后按补丁安全版本升级 |
| pnpm | 11.x | `packageManager` 固定，lockfile 必须提交 |
| NestJS | 11.x（锁定 11.2.3） | 阶段 0 已完成 11.2.3 与 12.0.1 的同源码兼容性 PoC；ADR-026 决定本期继续暂留 11.x 并锁定 11.2.3，升级触发条件与工作清单见该 ADR |
| React | 19.x | 与 Vite、AntD 兼容矩阵在阶段 0 验证 |
| Vite | 8.x | 使用 Rolldown；验证插件与测试工具兼容 |
| Ant Design | 6.x | 验证 CSS-in-JS 与 CSP 兼容方案 |
| TanStack Query | 5.x | 不建立服务端实体副本 |
| React Hook Form | 7.x | 8 仍为 beta，不采用 |
| Zod | 4.x | API 契约唯一运行时 Schema 来源 |
| Drizzle ORM | >=0.45.2、<0.46 | 0.45.2 为本期安全下限，manifest 与 lockfile 锁定实际补丁；升级须通过迁移生成、回滚与锁行为测试，1.0 GA 后再评估 |
| OpenAPI | 3.1 | 由 Zod Schema 生成，禁止手工维护 |
| PostgreSQL | 18.x | 生产使用当前安全补丁版本（评审时基线 18.6） |
| Nginx | 1.30.x（评审基线 1.30.4） | 生产锁定实际补丁与镜像 digest；切换 1.32.x 等新 stable 系列必须走 ADR 与完整回归 |

执行规则：

1. CI 与生产镜像固定实际补丁版本、lockfile、基础镜像 digest 与 Git commit SHA；
2. 禁止 `latest`、`next`、`beta`、`rc` 进入生产依赖；
3. Renovate/Dependabot 只创建 PR，人工确认后合并；
4. 主版本升级必须新增 ADR，并验证冷启动、构建、类型、集成、E2E、安全头与备份恢复；
5. 依赖漏洞扫描是上线阻断项；
6. PostgreSQL 补丁升级按主版本内安全补丁流程执行，先演练迁移与恢复。

---
## 0.2 V1.1 技术方案的主要调整

1. 后端由 Hono 调整为 NestJS，换取更强的工程约定和成熟的认证/鉴权集成；
2. 不把共享 TypeScript 实体当作 HTTP 契约，改为 OpenAPI 生成客户端；
3. 重构任务聚合模型，删除三处重复的 `task_group_id/main_task_id` 真相；
4. 数据库增加 `NOT NULL`、状态 `CHECK`、复合外键和部分唯一索引；
5. 乐观锁改为“条件更新 + 版本递增”的原子实现；
6. Session 改为可持久化、可轮换、可吊销的完整方案；
7. 资源权限根据资源真实归属判断，不信任客户端传入的 `projectId`；
8. 审计只由应用层写业务事件，数据库负责禁止修改和删除；
9. 本期不实现 Outbox；通知、审计、活动、搜索投影均在业务事务内写入，提交后轮询读取；
10. Compose 增加构建、健康检查、迁移任务、Secrets 接线和正确缓存策略；
11. 备份以“能够恢复”为验收标准，而不以“生成了备份文件”为标准；
12. AI 规范从提示建议升级为仓库规则和 CI 强制门禁；
13. 新增 WorkflowModule 编排跨模块事务，消除 Projects/Modules 与 Tasks/ChangeRecords 循环；
14. 新增 SearchProjection、ActivityProjection，搜索与项目动态不再直接跨模块拼接业务表；
15. Session 数据库只存 Token 哈希，CSRF 固定同步 Token；
16. 审计改为业务事件 + 哈希链 + 运行时只读 + 读取留痕 + 远端归档；
17. 系统管理员启用 MFA，高风险操作重新认证；
18. 版本、镜像、部署约束与权限矩阵作为上线阻断项。
19. PostgreSQL 18 数据卷固定挂载 `/var/lib/postgresql`，镜像必须使用精确补丁 Tag 与真实 digest；
20. 数据库拆分 bootstrap、owner、migrator、runtime、backup 角色与独立 Secret；
21. Workflow 通过显式 UnitOfWork 传播同一个 Drizzle transaction handle；
22. 审计链增加链头行锁、序号、密钥版本和规范序列化版本；
23. Session 使用用户级 `auth_version`，登录前提供匿名 CSRF bootstrap；
24. Route Registry 同时绑定请求、响应、权限、CSRF、幂等与并发策略；
25. 补齐备份调度、全新主机恢复、MFA 生命周期和关键竞态测试。

---

# 1. 架构目标与非目标

## 1.1 架构目标

按优先级排序：

1. **数据不丢、不串项目、不产生半完成状态；**
2. **任务、迭代记录、合并关系和审计可追溯；**
3. **并发冲突可检测并返回明确的 409；**
4. **AI 生成代码可被类型、数据库约束和测试快速验证；**
5. **单机部署、备份、升级和回滚过程可重复；**
6. **1～3 人能够长期维护，不依赖某个个人的隐含知识。**

## 1.2 非目标

V1.2.2 不建设：

- 微服务；
- 多租户平台；
- Kubernetes；
- Redis 缓存；
- Elasticsearch；
- WebSocket 实时协作；
- Event Sourcing；
- CQRS 框架；
- 自研权限 DSL；
- GitHub API/Webhook 深度集成；
- 附件对象存储。

## 1.3 质量属性

| 属性 | V1.2.2 目标 |
|---|---|
| 一致性 | 核心业务命令使用单数据库事务 |
| 可用性 | 单实例可维护窗口；不承诺高可用 |
| 性能 | 常用列表 P95 API 响应小于 500ms，复杂搜索小于 1s（在验收数据集上） |
| 安全 | 后端强制鉴权；防 IDOR、CSRF、XSS；密码和 Session 安全存储 |
| 可恢复 | 每日备份；每月至少一次自动或人工恢复演练 |
| 可观测 | 请求 ID、结构化日志、健康检查、慢查询分析 |
| 可维护 | 模块化单体；禁止跨模块直接访问内部 Repository |

性能数字是初始 SLO，需要通过基准数据集验证，不根据“用户数”直接推断服务器规格。

---

# 2. 总体架构

## 2.1 运行时拓扑

```text
浏览器
  │ HTTPS
  ▼
Nginx
  ├── /assets/*  -> React 静态资源
  ├── /          -> index.html
  └── /api/v1/*  -> NestJS API
                         │
                         ├── PostgreSQL（业务数据、Session、审计、通知、搜索/活动投影）
                         └── GitHub（V1.2.2 仅解析链接，不主动请求）
```

生产环境只有 Nginx 暴露宿主机端口。API 和 PostgreSQL 只在 Compose 内部网络可见。

## 2.2 后端分层

```text
Controller
  ↓ Route Registry 绑定的 Pipe / Guard / Response Serializer
Application Service（单域命令）或 Workflow Application Service（跨域命令）
  ↓ UnitOfWork.run(async tx => ...)
Domain Service（纯业务判断）
  ↓ Public CommandPort / QueryPort；写方法必须显式接收 tx
Repository / Projection Writer
  ↓ 同一个 Drizzle transaction handle、SQL、锁、持久化
PostgreSQL
  ↓ CHECK / FK / UNIQUE / 受限角色作为最终防线
```

约束：

- Controller 不直接访问数据库；
- Repository 不决定业务状态流转；
- 领域模块之间不得互相调用写服务，也不得访问其他模块 Repository；只读校验仅通过稳定 QueryPort；
- 修改其他领域业务聚合的跨域写操作只能由 WorkflowModule 编排，单域 Application Service 不得直接调用其他领域写服务或 Repository；审计、通知、Activity 与 Search WritePort 属于横切同库写入，不视为领域聚合调用；
- 单域 Application Service 与跨域 Workflow 是同一应用层的两类入口，二者不得互相嵌套开启事务；
- `UnitOfWork.run` 只创建一个数据库事务，并向回调传入唯一的 `TransactionContext`；所有 CommandPort、Repository、审计、通知和投影写方法必须显式接收该 `tx`；
- 事务回调内禁止使用全局 Drizzle Client；开发环境可用断言发现遗漏，集成测试通过故障注入验证任一步失败都会整体回滚；
- 跨模块只读允许调用稳定 QueryPort，例如 `ProjectAccessQueryPort`；跨模块写只允许 Workflow 调用目标模块公开的 CommandPort，禁止直接导入目标 Repository；
- 审计、通知、活动、搜索投影通过 WritePort/QueryPort 注入；
- 数据库事务内不得调用网络服务；
- 数据库表级外键可以由统一迁移维护，但代码层禁止跨模块导入内部实现；
- CI 检查依赖图，禁止循环依赖和反向依赖；
- `ProjectAccessQueryPort` 为公共稳定只读端口，由 ProjectsModule 提供适配器，下游模块不依赖其 Repository；
- SearchQueryService 与 ActivityQueryService 必须先调用 `ProjectAccessQueryPort`，由服务端根据 actor 与请求范围生成不可伪造的 `AuthorizedProjectScope`，再把该 Scope 作为参数化谓词约束后续 SQL；未取得 Scope 不得查询投影表，禁止由 Controller 传入或缓存权限 Scope。

---
## 2.3 模块划分

```text
IdentityModule           用户、登录、Session、系统管理员标识
ProjectsModule           项目、项目成员、ProjectAccessQueryPort；不创建未分类模块
ModulesModule            模块
FeaturesModule           功能、当前说明、归档
TasksModule              任务、状态、影响功能
TaskGroupsModule         合并、来源分支、解除合并
ChangeRecordsModule      迭代记录、草稿、版本、作废与可审计恢复
ExternalLinksModule      GitHub 链接解析与管理
WorkflowModule           项目创建、任务完成+记录发布、遗留转任务、合并/解除编排
SearchProjectionModule   搜索投影维护与查询
ActivityProjectionModule 脱敏项目动态投影
NotificationsModule      站内通知写入与读取
AuditModule              业务审计、哈希链、审计读取
HealthModule             liveness/readiness
```

不建立 `OutboxModule`，不创建 `outbox_events` 表。本期所有副作用均为数据库写入；未来出现外部推送时新增 ADR 和 worker。

这是模块化单体。除非未来出现独立扩缩容、独立故障域或独立团队边界，不拆微服务。

---
# 3. 仓库结构

```text
project-root/
├── apps/
│   ├── api/
│   │   ├── src/
│   │   │   ├── modules/
│   │   │   ├── common/
│   │   │   │   ├── auth/
│   │   │   │   ├── errors/
│   │   │   │   ├── http/
│   │   │   │   └── observability/
│   │   │   ├── database/
│   │   │   ├── main.ts
│   │   │   └── app.module.ts
│   │   ├── test/
│   │   └── package.json
│   └── web/
│       ├── src/
│       │   ├── app/
│       │   ├── features/
│       │   ├── pages/
│       │   ├── shared/
│       │   └── generated/api/
│       ├── e2e/
│       └── package.json
├── packages/
│   ├── api-contract/         Zod Schema 唯一真相、OpenAPI 生成配置、客户端
│   ├── domain-constants/     仅共享稳定枚举和纯常量
│   ├── eslint-config/
│   ├── tsconfig/
│   └── test-utils/
├── database/
│   ├── schema/
│   ├── migrations/
│   └── seeds/
├── infra/
│   ├── api.Dockerfile
│   ├── web.Dockerfile
│   ├── nginx.conf
│   └── compose.yaml
├── scripts/
│   ├── backup/
│   ├── restore/
│   └── smoke/
├── docs/
│   ├── architecture/
│   ├── adr/
│   └── runbooks/
├── AGENTS.md
├── pnpm-workspace.yaml
├── pnpm-lock.yaml
├── package.json
└── README.md
```

禁止建立万能 `shared/utils` 垃圾目录。共享包只放确实被两个应用使用、语义稳定且无环境副作用的内容。

---

# 4. API 与类型契约

## 4.1 契约原则

采用 **Code-first 双真相**：

- `packages/api-contract` 中的 Schema Registry/Zod 只负责请求与响应数据结构；
- Route Registry 只负责 method、path、operationId、Content-Type、状态码、安全和并发等 HTTP 操作元数据；
- 两者共同生成 OpenAPI 3.1 与前端 TypeScript Client，任一方都不能单独描述完整 API。

```text
Schema Registry / contracts/*.zod.ts（数据结构真相）
  ├── request/response schemas
  └── NestJS Pipe + Response Serializer 运行时复用

Route Registry（HTTP 操作元数据真相）
  ├── method/path/operationId/content-types/status/security/version/concurrency
  └── 与 Schema Registry 组合生成 OpenAPI 3.1 和 TypeScript Client
```

1. API 使用 `/api/v1` 前缀；
2. 请求 Schema、响应 Schema、数据库实体三者分离；
3. OpenAPI 与前端客户端均为两份 Registry 共同生成的产物，不是手工维护的第三真相；
4. 禁止同时手工维护 OpenAPI YAML、Nest DTO 和前端 Zod 三份定义；
5. NestJS 不再使用与 Zod 重复的 class-validator DTO；需要时由 `contracts` 生成类型；
6. 前端表单可复用生成的请求类型，但不得导入数据库 Schema；
7. 日期统一使用 ISO 8601 UTC 字符串；
8. ID 在当前规模下使用 PostgreSQL `INTEGER IDENTITY`，JSON 中为 number；
9. OpenAPI 与生成客户端提交到仓库，CI 检查 Schema Registry、Route Registry、Controller 和生成物无漂移；
10. 任何 API 变更必须修改对应 Schema/Route Registry、集成测试与权限矩阵并重新生成产物，禁止直接改客户端；
11. 请求由 Zod Pipe 校验；响应由 Route Registry 指定的 Zod Schema 通过 Serializer/Interceptor 校验并剔除未知字段，禁止 Controller 直接返回数据库实体；
12. 响应校验失败一律记录内部错误并返回统一 500，生产响应不得包含校验细节或实体内容；
13. 阶段 0 已按 [ADR-029](docs/adr/ADR-029.md) 定案运行期组合：Zod 4 → OpenAPI 3.1 生成器、OpenAPI TypeScript Client 生成器与自研 Nest Zod Pipe/Serializer；未知字段、nullable、日期、错误响应与 OpenAPI 3.1 由契约测试覆盖。
14. 未通过阶段 0 双向契约测试前，不得把第三方生成工具或 Nest Zod 包装写成既定依赖；变更运行期组合必须新增 ADR。

### 4.1.1 Route Registry

每个路由必须在唯一的 Route Registry 中完整登记；可不适用的策略也必须显式写 `none`，禁止依赖默认值：

```text
method
path
operationId
request:
  path/query/headers/body schemaRefs
  contentTypes[]:
    contentType + schemaRef
responses[status]:
  body:
    contentTypes[]:
      contentType + schemaRef
  | noBody
authPolicy
csrfPolicy
idempotencyPolicy
idempotencyExceptionAdr
idempotencyContractVersion
idempotencyFingerprintVersion + behaviorHeaders[]
idempotencyReplayPolicy:
  version
  success[status]:
    body:
      responseSchemaRef
      safeBodyFieldPaths[]
    | noBody
replayAuthorizationPolicy:
  version
  actorOnly
  | resources:
      contextSchemaRef
      resultRefExtractor
      currentReadAuthorizer
securityFlowPolicy:
  singleConsumptionOrNaturalIdempotency
  clientRecoveryPath
versionPolicy（API/schema/If-Match）
concurrencyPolicy（rowVersion / lock order / retry）
auditAction
```

Nest Controller、OpenAPI 生成、权限矩阵测试、CSRF/幂等 Guard 和响应 Serializer 读取 Route Registry，并通过 `schemaRef` 引用 Schema Registry。Controller 以 `@Operation("operationId")` 绑定稳定操作标识，请求参数统一使用 Contract decorator。CI 对每条 route 强制校验 method/path/operationId 唯一且齐全、请求各 Content-Type 有 Schema、有 body 的响应各 Content-Type 有 Schema、`noBody` 响应没有 OpenAPI `content`、auth/CSRF/idempotency/version/concurrency/audit 策略均显式登记。

### 4.1.2 Nest 运行期绑定

- 请求：`ContractValidationPipe` 从 Route Registry 定位 `operationId + part`，再通过 Schema Registry 执行 Zod `safeParse`；失败统一抛 `ContractValidationError`，由全局 Filter 返回 422，不泄漏 Zod 内部格式。
- 响应：`ContractResponseInterceptor` 从 handler 的 `@Operation` 元数据读取 operationId，按实际 `response.statusCode` 选择响应 Schema；成功只返回 `parsed.data`，因此未知字段被剔除；校验失败抛 `ContractResponseError` 并统一返回 500。
- 请求头：Pipe 只提取 Schema `shape` 声明的字段，Express 自动附加的 `host`、`connection` 等真实头部不参与严格校验。
- 错误：全局 `@Catch()` Filter 输出 `{ code, message, details, requestId }` 并设置 `X-Request-Id`；Nest 默认错误体、堆栈、内部校验文本和未匹配路由的 method/path 不返回客户端。
- 安全顺序：非安全方法先执行 `StrictSameOriginGuard`，再执行 Contract Pipe，403 优先于 422。

`idempotencyRequired` 路由还必须声明不可复用的 `idempotencyContractVersion`、摘要格式版本、行为相关请求头、带版本的 `idempotencyReplayPolicy` 与 `replayAuthorizationPolicy`，逐个覆盖所有可能缓存的 2xx 状态。每项成功策略必须是互斥联合：`body` 分支的 `responseSchemaRef` 与该状态响应的精确 Schema ref 相同，`safeBodyFieldPaths` 穷尽且不得越出 Schema 叶子字段；`noBody` 分支不得声明 Schema、字段或 Content-Type。授权策略的 `actorOnly` 只适用于响应不暴露其他资源的操作；`resources` 必须以版本化类型 Schema 从结果提取最小资源引用，并绑定当前读取授权器，禁止保存显示字段、成员列表或权限快照。Schema 标为 `sensitive`/`writeOnly` 的字段、Secret 命名字段和任何响应头均禁止进入重放策略。CI 对请求 Schema ref、摘要字段、响应 Schema ref、重放策略与授权策略生成契约清单摘要；任一内容改变但 `idempotencyContractVersion` 未升级时失败，历史版本号不得复用。

`none` 与 `securityFlow` 路由的重放及授权策略只能为 `none`。`securityFlow` 还必须填写 `securityFlowPolicy` 的单次消费或自然幂等机制和客户端恢复路径，并引用允许它的 ADR；operationId 集合及两项元数据必须与 ADR-023 精确相等。其他策略的 `securityFlowPolicy` 必须为 `none`。Controller 未注册、缺任一字段、引用不存在或生成物漂移都必须失败。

`idempotencyPolicy` 的默认值由 [ADR-019](docs/adr/ADR-019.md) 固定：POST、PUT、PATCH、DELETE 等非安全写方法必须登记为 `idempotencyRequired`；普通 GET、HEAD 和不改变服务端状态的纯校验路由登记为 `none`。只有 [ADR-023](docs/adr/ADR-023.md) allowlist 中的一次性认证安全流程可以登记 `securityFlow`，其中 `issueCsrfToken` 的 `GET /api/v1/auth/csrf` 必须且只能登记 `securityFlow`；写路由不得隐式豁免，新增例外必须登记原因并以新 ADR 获得批准。

---
## 4.2 统一错误模型

```json
{
  "code": "TASK_VERSION_CONFLICT",
  "message": "该任务已被其他用户更新，请刷新后重试",
  "details": {},
  "requestId": "01J..."
}
```

| HTTP 状态 | 使用场景 |
|---|---|
| 400 | 请求格式或业务参数错误 |
| 401 | 未登录或 Session 失效 |
| 403 | 已登录但无全局权限；必要时资源接口统一返回 404 防止枚举 |
| 404 | 资源不存在或当前用户不可访问 |
| 409 | 版本冲突、重复操作、状态冲突 |
| 422 | 字段校验错误 |
| 429 | 登录或敏感接口触发限流 |
| 500 | 未预期错误，不向客户端泄露堆栈 |

所有异常最终经过全局 `@Catch()` Filter 转换为上述信封。响应契约校验失败和未预期异常统一为 500 `INTERNAL_ERROR`；请求契约校验失败统一为 422 `VALIDATION_FAILED`；Nest `HttpException` 按状态映射到对应固定文案。错误响应同时设置 `X-Request-Id`，未匹配路由不得回显 method 或 path。

## 4.3 写命令接口与请求关联

```text
GET  /api/v1/auth/csrf
GET  /api/v1/users
POST /api/v1/projects
POST /api/v1/tasks
POST /api/v1/tasks/{taskId}/complete
POST /api/v1/tasks/{taskId}/cancel
POST /api/v1/tasks/{taskId}/reopen
POST /api/v1/task-groups/merge
POST /api/v1/task-groups/{groupId}/members/{memberId}/detach
POST /api/v1/change-records/{recordId}/publish
POST /api/v1/change-records/{recordId}/versions
POST /api/v1/change-records/{recordId}/leftover-task
POST /api/v1/change-records/{recordId}/void
POST /api/v1/change-records/{recordId}/restore
GET  /api/v1/projects/{projectId}/activity
GET  /api/v1/audit-logs
GET  /api/v1/search
GET  /api/v1/notifications
```

组合命令语义：

- `POST /projects`：由 ProjectBootstrapWorkflow 单事务创建项目、初始成员、未分类模块、审计、通知、活动与搜索投影；
- `GET /users`：读取创建项目时可选初始成员目录；只返回启用且未停用用户的 `id/name/avatarUrl/isAdmin`，最多 300 条，响应 `no-store`，不暴露登录名、邮箱、密码哈希或停用状态；
- `POST /tasks/{taskId}/complete`：请求体只有 `WITHOUT_RECORD|WITH_RECORD` 两种 mode；`WITH_RECORD` 中 `recordDraftId` 与内联 `record` 必须且只能出现一个，由 TaskCompletionWorkflow 单事务完成任务、设置记录的 `task_id`、发布记录、审计、通知、活动与搜索投影；
- `POST /change-records/{recordId}/publish`：允许发布 `task_id IS NULL` 的独立草稿；有关联任务时，ChangeRecordsModule 只经 `TaskQueryPort` 验证任务已为 `DONE`，未完成返回 409，不访问 Tasks Repository 或写服务；
- `POST /change-records/{recordId}/void`：仅系统管理员且需密码 + 当前 TOTP 重认证、作废原因、预期版本和幂等键；
- `POST /change-records/{recordId}/restore`：仅系统管理员且需密码 + 当前 TOTP 重认证、恢复原因、预期版本和幂等键；只允许 `VOID -> PUBLISHED` 并保留作废历史；
- `POST /change-records/{recordId}/leftover-task`：由 Workflow 编排，ChangeRecords 不依赖 Tasks Repository 或写服务；
- `GET /audit-logs`：仅系统管理员，需重认证 + MFA；每次读取写 `AUDIT_LOG_READ`；
- `GET /projects/{projectId}/activity`：只读脱敏 ActivityProjection；
- `GET /search`：只读 SearchProjection，不跨模块拼接业务表。

请求关联规则：

- 服务端始终生成内部 `requestId` 并回传；客户端可选传 `X-Request-Id`，仅接受 `[A-Za-z0-9._:-]{1,64}`，保存为 `clientRequestId`，不得直接作为可信内部 ID；
- `Idempotency-Key`：所有 Route Registry 登记为 `idempotencyRequired` 的写接口必须携带，长度 16～128；创建项目、成员变更、模块/功能/任务编辑、归档/恢复、完成、合并、发布和遗留转任务均不得遗漏；
- 更新已有聚合根时使用 `If-Match: "rv:<rowVersion>"`；如兼容请求体 `expectedRowVersion`，两者同时出现时必须相等，否则返回 400；
- GET、HEAD、普通查询和纯校验接口不创建幂等记录；ADR-023 列明的认证端点使用 `securityFlow`，其他写接口例外必须新建 ADR 并在 Route Registry 显式登记。

### 4.3.1 幂等协议

通用幂等表只服务于已认证且登记 `idempotencyRequired` 的业务命令，以 Route Registry 的稳定 operationId 作为 `operation_id`，保存 `(actor_id, operation_id, idempotency_key)` 唯一键、幂等契约版本、规范请求摘要、最终 HTTP 状态、精确响应 Schema ref、重放与授权策略版本、可安全重放的脱敏响应体、类型化最小授权上下文、创建与过期时间。Cookie、Authorization、CSRF Token、MFA Secret、验证码和恢复码不得进入该表：

```sql
CREATE TABLE idempotency_records (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  actor_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  operation_id TEXT NOT NULL,
  idempotency_key TEXT NOT NULL CHECK (length(idempotency_key) BETWEEN 16 AND 128),
  idempotency_contract_version TEXT NOT NULL,
  request_hash BYTEA NOT NULL CHECK (octet_length(request_hash) = 32),
  request_hash_key_version SMALLINT NOT NULL CHECK (request_hash_key_version > 0),
  state TEXT NOT NULL DEFAULT 'PENDING'
    CHECK (state IN ('PENDING', 'SUCCEEDED')),
  response_status INTEGER CHECK (response_status BETWEEN 200 AND 299),
  response_schema_ref TEXT,
  replay_policy_version TEXT,
  replay_auth_policy_version TEXT,
  replay_auth_context JSONB,
  response_has_body BOOLEAN,
  response_body JSONB,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL,
  UNIQUE (actor_id, operation_id, idempotency_key),
  CHECK (
    (state = 'PENDING' AND response_status IS NULL
      AND response_schema_ref IS NULL AND replay_policy_version IS NULL
      AND replay_auth_policy_version IS NULL AND replay_auth_context IS NULL
      AND response_has_body IS NULL
      AND response_body IS NULL)
    OR
    (state = 'SUCCEEDED' AND response_status IS NOT NULL
      AND replay_policy_version IS NOT NULL
      AND replay_auth_policy_version IS NOT NULL
      AND replay_auth_context IS NOT NULL
      AND jsonb_typeof(replay_auth_context) = 'object'
      AND response_has_body IS NOT NULL
      AND ((response_has_body AND response_schema_ref IS NOT NULL
            AND response_body IS NOT NULL)
        OR (NOT response_has_body AND response_schema_ref IS NULL
            AND response_body IS NULL)))
  )
);
```

执行算法：

1. 先验证当前认证与原操作权限，再生成带版本的规范请求字节串：大写 method、operationId、`idempotencyContractVersion`、摘要格式与请求 Schema 版本、Schema 解析后的 path 参数、按字段名排序且保留重复值语义的 query、规范 Content-Type、Route Registry 声明的全部行为相关请求头以及 JCS 规范化 body；使用版本头的路由必须包含 `If-Match`。Cookie、Authorization、CSRF、追踪头、`X-Request-Id` 和 `Idempotency-Key` 不进入摘要；
2. `IDEMPOTENCY_FINGERPRINT_KEY_VERSION` 选择 `IDEMPOTENCY_FINGERPRINT_KEYRING_FILE=/run/secrets/idempotency_fingerprint_keyring` 中当前版本的独立密钥并计算 HMAC-SHA-256；版本号是非敏感部署配置，keyring 缺失、为空、版本不存在或权限不合规时进程 fail closed。业务事务内先插入幂等行、`idempotency_contract_version` 及 `request_hash_key_version`；并发相同唯一键会由 PostgreSQL 等待先行事务提交或回滚。若唯一键已存在，必须使用记录中的 key version 对当前规范请求重新计算，并以常量时间比较摘要；
3. 新插入者执行命令并在同一事务保存 2xx 状态、当前 replay/auth policy 版本与 `response_has_body`。`body` 分支还保存精确 `response_schema_ref` 和策略穷尽列出的安全字段，入库前再次按该 Schema 校验；`noBody` 分支保存 `response_has_body=false`、`response_schema_ref=NULL` 与 `response_body=NULL`。授权策略提取器同时生成经 `contextSchemaRef` 校验的最小 `replay_auth_context`；`actorOnly` 保存空对象，`resources` 只保存类型、稳定 ID 与验证归属所需 ID，不保存权限判断结果或显示字段。响应头一律不进入幂等记录；进程在提交前失败时整行回滚，重试可重新执行；
4. 冲突者先只读取摘要与授权上下文元数据；幂等契约版本或摘要不同返回 409。两者相同仍须重新验证当前认证、原操作权限、`replay_auth_context` 中每个结果资源的当前可读权限，以及该路由要求的高风险重认证新鲜度；例如创建项目后已被移除的创建者虽仍能调用“创建项目”，也不得重放原项目响应。任一门禁失败即拒绝且不读取/返回缓存状态码或 body。全部通过后才读取缓存响应，并按 `response_has_body` 重放原状态码和有/无 body 语义，不从已变化的业务表重建响应；原响应头一律不复用，`noBody` 重放不得生成 body 或 Content-Type；
5. 输入校验或初次鉴权失败发生在插入之前。事务内产生的确定性业务 4xx、5xx 或未预期异常均不缓存，并必须回滚业务写与 `PENDING` 行；不得提交永久占位，同 Key 后续请求可重新执行；
6. 幂等保证窗口为 30 天，过期记录每日分批清理；API 文档必须公开该窗口。旧摘要密钥必须随数据库恢复材料保留到使用它的记录全部过期并清理，密钥版本不可回退；请求 Schema、摘要字段、响应 Schema、replay policy 或结果授权策略改变时必须升级 `idempotencyContractVersion`，因此跨部署旧 Key 返回 409，不能按已收紧的旧策略重放；
7. CI 使用两个真实数据库连接和同步屏障验证并发同 Key、业务 4xx/异常与首请求回滚、提交后断线重试、权限被移除/用户停用/Session 失效/重认证过期/结果资源不可读后的拒绝，以及 body/path/query/Content-Type/`If-Match`/行为头/幂等契约版本变化时返回 409；至少覆盖创建项目后移除创建者与记录转为 VOID 后的重放，不得泄露原响应。仅原始 query 顺序不同但解析结果相同的请求必须得到相同摘要。

登记为 `securityFlow` 的端点不创建 `idempotency_records`，而由预认证 Session、CSRF Hash、TOTP time-step、恢复码 Hash 或当前 Session 的条件更新保证单次消费或自然幂等；客户端恢复路径和并发断言见 [ADR-023](docs/adr/ADR-023.md)。

每个接口需要在权限矩阵中明确匿名、同项目成员、其他项目成员、移除成员、停用用户与系统管理员的预期结果。

---
# 5. 数据库设计

## 5.1 通用规则

1. 主键使用 `INTEGER GENERATED BY DEFAULT AS IDENTITY`；
2. 业务必填字段统一 `NOT NULL`；
3. 状态字段使用 `TEXT + CHECK`，避免无约束 VARCHAR；
4. `created_at/updated_at` 使用 `TIMESTAMPTZ NOT NULL`；
5. 所有跨层级关系必须使用复合外键防止跨项目串联：父表提供 `UNIQUE(project_id, id)`，子表写入 `project_id` 和外键 `(project_id, child_id)`；
6. 通用多态 `source_type/source_id` 必须避免直接使用；若使用，必须另外提供可验证的归属约束或专用关联表；
7. 审计、通知、活动、搜索投影均携带 `project_id`；
8. 业务历史默认软归档/作废，不通过级联物理删除清理；
9. 核心聚合根拥有 `row_version INTEGER NOT NULL DEFAULT 1`；
10. 数据库迁移文件必须人工评审，禁止应用启动时自动生成 Schema；
11. 迁移使用 expand/contract 原则，避免代码回滚后无法读取新 Schema；
12. 搜索文本保存原始值与 `normalized_search_text`，PGroonga 全文索引建立在规范化列上；
13. 审计表使用 `chain_id/sequence_no/prev_hash/record_hash/key_version/canonical_version`，只允许经受限追加函数写入；
14. 所有文本、URL、Header 和分页参数都必须在 Zod 与数据库层设置可解释上限；Nginx/API 同时限制请求体，避免审计与搜索投影放大超长输入；
15. owner、migrator、runtime、backup 数据库角色职责分离，runtime 不拥有任何业务对象且不能 DDL。

### 5.1.1 用户认证版本与 Session

```sql
ALTER TABLE users
  ADD COLUMN auth_version INTEGER NOT NULL DEFAULT 1
    CHECK (auth_version > 0);

CREATE TABLE user_sessions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash BYTEA NOT NULL,
  token_hash_key_version SMALLINT NOT NULL,
  auth_version_at_issue INTEGER NOT NULL CHECK (auth_version_at_issue > 0),
  auth_state TEXT NOT NULL
    CHECK (auth_state IN ('AUTHENTICATED', 'MFA_ENROLLMENT',
      'MFA_CHALLENGE', 'RECOVERY_CHALLENGE')),
  recovery_rotation_generation INTEGER NOT NULL DEFAULT 0
    CHECK (recovery_rotation_generation >= 0),
  recovery_rotation_consumed_generation INTEGER NOT NULL DEFAULT 0
    CHECK (recovery_rotation_consumed_generation >= 0),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  last_seen_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  idle_expires_at TIMESTAMPTZ NOT NULL,
  absolute_expires_at TIMESTAMPTZ NOT NULL,
  revoked_at TIMESTAMPTZ,
  reauthenticated_at TIMESTAMPTZ,
  mfa_verified_at TIMESTAMPTZ,
  UNIQUE (token_hash),
  CHECK (idle_expires_at <= absolute_expires_at),
  CHECK (revoked_at IS NULL OR revoked_at >= created_at),
  CHECK (recovery_rotation_consumed_generation <= recovery_rotation_generation)
);

CREATE INDEX ix_user_sessions_user_active
  ON user_sessions(user_id, absolute_expires_at)
  WHERE revoked_at IS NULL;

CREATE INDEX ix_user_sessions_expiry
  ON user_sessions(absolute_expires_at, idle_expires_at);

CREATE TABLE session_csrf_tokens (
  session_id BIGINT NOT NULL REFERENCES user_sessions(id) ON DELETE CASCADE,
  token_hash BYTEA NOT NULL,
  issued_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (session_id, token_hash),
  UNIQUE (token_hash),
  CHECK (expires_at > issued_at)
);

CREATE INDEX ix_session_csrf_tokens_expiry
  ON session_csrf_tokens(expires_at);

CREATE TABLE preauth_sessions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  token_hash BYTEA NOT NULL UNIQUE,
  token_hash_key_version SMALLINT NOT NULL CHECK (token_hash_key_version > 0),
  csrf_token_hash BYTEA NOT NULL UNIQUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL,
  consumed_at TIMESTAMPTZ,
  CHECK (expires_at > created_at),
  CHECK (expires_at <= created_at + INTERVAL '10 minutes'),
  CHECK (consumed_at IS NULL OR consumed_at >= created_at)
);

CREATE INDEX ix_preauth_sessions_expiry
  ON preauth_sessions(expires_at);
```

认证查询必须连接 `users`，同时验证用户启用、Session 未撤销、两个过期时间均未到，并满足 `auth_version_at_issue = users.auth_version`。只有 `AUTHENTICATED` Session 可访问业务路由；其余状态只能调用权限矩阵明确允许的 challenge 操作。停用、改密和管理员强制退出在同一事务递增用户 `auth_version` 并撤销现有 Session。签发认证 CSRF Token 时先 `SELECT ... FOR UPDATE` 锁定 `user_sessions` 行，删除过期 Token、插入新 Hash，再按 `issued_at` 淘汰最早项，使每个 Session 最多保留 4 个有效 Hash；明文只在签发响应中出现一次。清理任务每小时按主键分批删除已撤销超过 30 天或绝对过期超过 7 天的 Session、过期 CSRF Token 和过期/已消费预认证 Session，禁止一次全表删除。

### 5.1.2 项目成员历史约束

`project_members` 除活跃唯一索引外必须具有状态/时间一致性约束：

```sql
CHECK (status IN ('ACTIVE', 'REMOVED')),
CHECK (
  (status = 'ACTIVE' AND removed_at IS NULL)
  OR
  (status = 'REMOVED' AND removed_at IS NOT NULL AND removed_at >= joined_at)
)
```

### 5.1.3 管理员 MFA 数据

```sql
CREATE TABLE user_totp_factors (
  user_id INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  status TEXT NOT NULL CHECK (status IN ('ENROLLING', 'ACTIVE', 'DISABLED')),
  enrollment_generation INTEGER NOT NULL CHECK (enrollment_generation > 0),
  key_version SMALLINT NOT NULL CHECK (key_version > 0),
  nonce BYTEA NOT NULL,
  ciphertext BYTEA NOT NULL,
  auth_tag BYTEA NOT NULL,
  last_accepted_step BIGINT,
  enrolled_at TIMESTAMPTZ,
  disabled_at TIMESTAMPTZ,
  CHECK (
    (status = 'ENROLLING' AND enrolled_at IS NULL AND disabled_at IS NULL)
    OR (status = 'ACTIVE' AND enrolled_at IS NOT NULL AND disabled_at IS NULL)
    OR (status = 'DISABLED' AND disabled_at IS NOT NULL)
  )
);

CREATE TABLE mfa_recovery_codes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  batch_version INTEGER NOT NULL CHECK (batch_version > 0),
  code_hash TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  used_at TIMESTAMPTZ,
  UNIQUE (user_id, code_hash)
);
```

TOTP Secret 只以 AEAD 密文保存；恢复码只保存包含独立随机 salt 与参数的 Argon2id 编码字符串，不允许普通 SHA-256，并对尝试限流。管理员密码阶段签发 `user_sessions` 时必须显式选择 `MFA_ENROLLMENT`、`MFA_CHALLENGE` 或 `RECOVERY_CHALLENGE`，数据库无默认完整态；只有普通用户或已完成 MFA 条件更新的管理员可显式进入 `AUTHENTICATED`。

全部用户级 MFA 状态变更统一按 user → TOTP factor → current Session 的顺序取锁；factor 不存在时先锁 user 再插入，任何路径不得先锁 Session 再回锁 factor。MFA enrollment generation 保存于用户唯一的 `user_totp_factors` 行，不放在 Session。start 与 confirm 都先锁 user、再锁或创建 factor；factor 不存在时 generation 视为 0，start 只有在 expected generation 等于当前值且 factor 非 ACTIVE 时才原子写入 `ENROLLING`、`current+1` 与新密文。confirm 必须携带相同 expected generation，只能把对应 `ENROLLING` 行条件更新为 ACTIVE，并在同一事务撤销受限 Session、轮换 Session ID/CSRF、显式签发 `AUTHENTICATED` Session；start-vs-start、start-vs-confirm 及不同受限 Session 竞争时失败方返回 409，不得用旧 Secret 激活或用新 pending 覆盖 ACTIVE 因子。

接受 TOTP 与更新 `last_accepted_step` 使用同一锁序和条件更新，保证同一时间步并发请求最多一个成功。`reauthenticateAdmin` 每次成功递增 Session 的 `recovery_rotation_generation`；恢复码轮换必须携带并条件消费该 generation，同时更新 `recovery_rotation_consumed_generation` 和新 `batch_version`，同一 generation 的竞争者返回 409。

---
## 5.2 核心关系结构

```text
Project
├── ProjectMember
├── Module
│   ├── Feature
│   ├── Task
│   │   └── TaskFeatureRelation
│   └── ChangeRecord
│       ├── ChangeRecordVersion
│       └── ChangeRecordFeatureRelation
└── TaskGroup
    └── TaskGroupMember（MAIN / SOURCE，唯一关系真相）

Task / Feature / ChangeRecord / Project
└── ExternalLink

业务命令同事务写入
├── AuditLog
├── Notification
├── ActivityProjection
└── SearchProjection
```

## 5.3 层级一致性约束

以下 SQL 表达约束意图，Drizzle Schema 和迁移 SQL 必须生成等价约束：

```sql
CREATE TABLE modules (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  name TEXT NOT NULL,
  kind TEXT NOT NULL DEFAULT 'NORMAL'
    CHECK (kind IN ('NORMAL', 'UNCLASSIFIED')),
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'ARCHIVED')),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, project_id)
);

CREATE UNIQUE INDEX ux_modules_one_unclassified
  ON modules(project_id)
  WHERE kind = 'UNCLASSIFIED';

CREATE TABLE features (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL,
  module_id INTEGER NOT NULL,
  code TEXT NOT NULL,
  name TEXT NOT NULL,
  current_behavior TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'ARCHIVED')),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (project_id, code),
  UNIQUE (id, project_id),
  UNIQUE (id, module_id, project_id),
  FOREIGN KEY (module_id, project_id)
    REFERENCES modules(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE tasks (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL,
  module_id INTEGER NOT NULL,
  feature_id INTEGER,
  scope_type TEXT NOT NULL
    CHECK (scope_type IN ('FEATURE', 'MODULE')),
  code TEXT NOT NULL,
  title TEXT NOT NULL CHECK (length(title) BETWEEN 1 AND 500),
  description TEXT NOT NULL DEFAULT '',
  assignee_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  creator_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  priority TEXT NOT NULL DEFAULT 'NORMAL'
    CHECK (priority IN ('LOW', 'NORMAL', 'HIGH', 'URGENT')),
  work_status TEXT NOT NULL DEFAULT 'TODO'
    CHECK (work_status IN ('TODO', 'DONE', 'CANCELED')),
  lifecycle_status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (lifecycle_status IN ('ACTIVE', 'ARCHIVED', 'INVALID')),
  completion_note TEXT,
  completed_at TIMESTAMPTZ,
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (project_id, code),
  UNIQUE (id, project_id),
  UNIQUE (id, module_id, project_id),
  FOREIGN KEY (module_id, project_id)
    REFERENCES modules(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (feature_id, module_id, project_id)
    REFERENCES features(id, module_id, project_id) ON DELETE RESTRICT,
  CHECK (
    (scope_type = 'FEATURE' AND feature_id IS NOT NULL)
    OR
    (scope_type = 'MODULE' AND feature_id IS NULL)
  ),
  CHECK (
    (work_status = 'DONE' AND completed_at IS NOT NULL)
    OR
    (work_status <> 'DONE' AND completed_at IS NULL)
  )
);
```

“未分类模块”依靠不可变的 `kind='UNCLASSIFIED'` 标识，不依赖可修改或可本地化的名称；允许修改名称、描述，并适用普通模块的项目授权、父级可写检查、名称唯一约束与条件版本更新（2026-09-09 人工确认）。该模块不得物理删除，归档项目时随项目变为不可写。

### 5.3.1 迭代记录、任务绑定与遗留项

```sql
CREATE TABLE change_records (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL,
  task_id INTEGER,
  module_id INTEGER NOT NULL,
  feature_id INTEGER,
  scope_type TEXT NOT NULL CHECK (scope_type IN ('FEATURE', 'MODULE')),
  code TEXT,
  title TEXT NOT NULL CHECK (length(title) BETWEEN 1 AND 500),
  status TEXT NOT NULL DEFAULT 'DRAFT'
    CHECK (status IN ('DRAFT', 'PUBLISHED', 'VOID')),
  current_version INTEGER NOT NULL DEFAULT 0 CHECK (current_version >= 0),
  current_payload JSONB NOT NULL DEFAULT '{}'::jsonb,
  published_at TIMESTAMPTZ,
  voided_at TIMESTAMPTZ,
  void_reason TEXT CHECK (void_reason IS NULL OR btrim(void_reason) <> ''),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (project_id, code),
  UNIQUE (id, project_id),
  UNIQUE (id, module_id, project_id),
  FOREIGN KEY (module_id, project_id)
    REFERENCES modules(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (task_id, project_id)
    REFERENCES tasks(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (feature_id, module_id, project_id)
    REFERENCES features(id, module_id, project_id) ON DELETE RESTRICT,
  CHECK (
    (scope_type = 'FEATURE' AND feature_id IS NOT NULL)
    OR (scope_type = 'MODULE' AND feature_id IS NULL)
  ),
  CHECK (
    (status = 'DRAFT' AND code IS NULL AND current_version = 0
      AND published_at IS NULL AND voided_at IS NULL AND void_reason IS NULL)
    OR (status = 'PUBLISHED' AND code IS NOT NULL AND current_version >= 1
      AND published_at IS NOT NULL
      AND ((voided_at IS NULL AND void_reason IS NULL)
        OR (voided_at IS NOT NULL AND void_reason IS NOT NULL)))
    OR (status = 'VOID' AND current_version >= 1 AND published_at IS NOT NULL
        AND code IS NOT NULL AND voided_at IS NOT NULL AND void_reason IS NOT NULL)
  )
);

CREATE INDEX ix_change_records_project_task
ON change_records(project_id, task_id)
WHERE task_id IS NOT NULL;

CREATE TABLE change_record_versions (
  record_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  version_no INTEGER NOT NULL CHECK (version_no > 0),
  payload JSONB NOT NULL,
  created_by INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (record_id, version_no),
  UNIQUE (record_id, version_no, project_id),
  FOREIGN KEY (record_id, project_id)
    REFERENCES change_records(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE change_record_leftover_items (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  record_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'CONVERTED', 'RESOLVED')),
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, record_id, project_id),
  UNIQUE (id, project_id),
  FOREIGN KEY (record_id, project_id)
    REFERENCES change_records(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE change_record_version_leftovers (
  record_id INTEGER NOT NULL,
  version_no INTEGER NOT NULL,
  leftover_item_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  content_snapshot TEXT NOT NULL CHECK (length(content_snapshot) BETWEEN 1 AND 10000),
  PRIMARY KEY (record_id, version_no, leftover_item_id),
  FOREIGN KEY (record_id, version_no, project_id)
    REFERENCES change_record_versions(record_id, version_no, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (leftover_item_id, record_id, project_id)
    REFERENCES change_record_leftover_items(id, record_id, project_id) ON DELETE RESTRICT
);

CREATE TABLE leftover_task_links (
  leftover_item_id INTEGER PRIMARY KEY,
  task_id INTEGER NOT NULL UNIQUE,
  project_id INTEGER NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  FOREIGN KEY (leftover_item_id, project_id)
    REFERENCES change_record_leftover_items(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (task_id, project_id)
    REFERENCES tasks(id, project_id) ON DELETE RESTRICT
);
```

上述 `change_records` 约束有意允许 `PUBLISHED` 记录同时存在成对的 `voided_at/void_reason`，表示该记录曾作废后恢复；`DRAFT` 必须两者皆空，`VOID` 必须两者皆非空。`status` 是当前状态和详情、搜索、时间线可见性的唯一真相，查询与投影不得以 `voided_at` 判断当前仍为作废；字段只保存最近一次作废快照，完整状态历史由不可变审计保存。状态迁移与列不变量以 [ADR-024](docs/adr/ADR-024.md) 为准。

`change_records.task_id` 是可空的多对一单向关联：一条记录至多关联一个任务，同一任务可以关联多条记录；复合外键保证同项目，非唯一索引 `(project_id, task_id)` 支持任务历史查询。TaskCompletionWorkflow 每次完成命令最多创建或绑定并发布一条记录，但已有历史记录不得阻断本次完成，也不妨碍已完成任务后续关联其他独立记录。TasksModule 不反向持有记录外键；设置或改写 `task_id` 是跨域写，只能由 Workflow 调用 ChangeRecords CommandPort，独立发布时 ChangeRecords 仍可按 4.3 节通过只读 `TaskQueryPort` 校验已关联任务为 DONE。遗留项拥有跨版本稳定 ID；版本表保存当时的内容快照，`leftover_task_links.leftover_item_id` 主键保证即使客户端更换 Idempotency-Key，同一遗留项也只能成功转换一次。runtime 对 `change_record_versions` 只有 `SELECT/INSERT`，无 `UPDATE/DELETE/TRUNCATE`。

## 5.4 项目成员历史

项目成员关系保留加入/移除历史：

```sql
CREATE UNIQUE INDEX ux_project_member_active
ON project_members(project_id, user_id)
WHERE status = 'ACTIVE';
```

重新加入项目时新增一条记录，而不是覆盖上一次 `joined_at/removed_at`。创建项目时，创建者必须作为初始成员且不可在创建表单中取消；创建完成后，系统管理员可按普通成员规则移除其成员记录。`projects.created_by` 只用于溯源、保持不可变，不形成第三种角色或额外权限；成员变更只能由通过双因子重认证的系统管理员执行并写审计。详见 [ADR-012](docs/adr/ADR-012.md)。

## 5.5 任务聚合模型

删除以下冗余字段：

- `tasks.task_group_id`；
- `task_groups.main_task_id`。

统一使用：

```sql
CREATE TABLE task_groups (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  name TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'CLOSED')),
  created_by INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  row_version INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, project_id)
);

CREATE TABLE task_group_members (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id INTEGER NOT NULL,
  task_id INTEGER NOT NULL,
  project_id INTEGER NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('MAIN', 'SOURCE')),
  source_kind TEXT CHECK (source_kind IN ('ACTIVE', 'HISTORICAL')),
  status TEXT NOT NULL DEFAULT 'ACTIVE'
    CHECK (status IN ('ACTIVE', 'DETACHED')),
  original_work_status TEXT
    CHECK (original_work_status IN ('TODO', 'DONE', 'CANCELED')),
  original_assignee_id INTEGER REFERENCES users(id) ON DELETE RESTRICT,
  joined_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  detached_at TIMESTAMPTZ,
  UNIQUE (project_id, group_id, task_id),
  FOREIGN KEY (group_id, project_id)
    REFERENCES task_groups(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (task_id, project_id)
    REFERENCES tasks(id, project_id) ON DELETE RESTRICT,
  CHECK (
    (role = 'MAIN' AND source_kind IS NULL
      AND original_work_status IS NULL AND original_assignee_id IS NULL)
    OR
    (role = 'SOURCE' AND source_kind IS NOT NULL
      AND original_work_status IS NOT NULL AND original_assignee_id IS NOT NULL)
  ),
  CHECK (
    (status = 'ACTIVE' AND detached_at IS NULL)
    OR
    (status = 'DETACHED' AND detached_at IS NOT NULL)
  )
);

CREATE UNIQUE INDEX ux_task_one_active_group
ON task_group_members(task_id)
WHERE status = 'ACTIVE';

CREATE UNIQUE INDEX ux_group_one_active_main
ON task_group_members(group_id)
WHERE status = 'ACTIVE' AND role = 'MAIN';
```

规则：

1. 部分唯一索引保证活跃组至多一个 MAIN；迁移另建 `DEFERRABLE INITIALLY DEFERRED` 约束触发器，在提交时保证 ACTIVE 组恰好一个 ACTIVE MAIN 且至少一个 ACTIVE SOURCE、CLOSED 组没有 ACTIVE 成员；
2. 一个任务最多属于一个活跃组；
3. SOURCE 与 MAIN 必须同项目；
4. V1.2.2 不允许组嵌套；
5. 最后一个 SOURCE 解除后，组自动 CLOSED，MAIN 成员转为 DETACHED；
6. 解除仅改变关系，不改变任务原 `work_status`；
7. 同一任务解除后可以加入其他组，但 `UNIQUE(project_id, group_id, task_id)` 禁止在同一组生成第二条历史成员；重新合并到同一组必须返回既有历史关系的冲突，不得覆盖时间和快照。

约束触发器只校验不变量，不写业务审计、不自动流转状态；创建、合并和关闭仍由 Workflow 明确执行。集成测试必须直接使用 runtime SQL 权限尝试构造无 MAIN、双 MAIN、CLOSED+ACTIVE 成员及缺失来源快照的数据，并确认提交失败。

## 5.6 迭代记录版本

所有正式版本，包括 v1，都写入 `change_record_versions`：

```text
change_records：身份、归属、状态、current_version、当前内容投影
change_record_versions：每个版本不可变的完整快照
```

更新已发布记录时：

1. `SELECT ... FOR UPDATE` 锁定记录；
2. 校验 `expectedRowVersion/currentVersion`；
3. 只接收允许修改的内容 DTO，禁止 `Partial<DatabaseEntity>`；
4. 插入新版本 `current_version + 1`；
5. 更新当前内容投影、`current_version` 和 `row_version`；
6. 写业务审计；
7. 提交。

唯一约束：

```sql
UNIQUE (record_id, version_no)
```

## 5.7 外部链接

链接规范化实体与类型化关联表分离；禁止 `owner_type/owner_id` 或多个可空 owner 字段：

```sql
CREATE TABLE external_links (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  display_url TEXT NOT NULL CHECK (length(display_url) BETWEEN 1 AND 2048),
  normalized_url TEXT NOT NULL CHECK (length(normalized_url) BETWEEN 1 AND 2048),
  provider TEXT NOT NULL CHECK (provider IN ('GITHUB')),
  kind TEXT NOT NULL CHECK (kind IN ('ISSUE', 'PULL_REQUEST', 'COMMIT', 'OTHER')),
  created_by INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (id, project_id),
  UNIQUE (project_id, normalized_url)
);

CREATE TABLE project_external_links (
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  link_id INTEGER NOT NULL,
  UNIQUE (project_id, link_id),
  FOREIGN KEY (link_id, project_id)
    REFERENCES external_links(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE task_external_links (
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  task_id INTEGER NOT NULL,
  link_id INTEGER NOT NULL,
  UNIQUE (project_id, task_id, link_id),
  FOREIGN KEY (task_id, project_id)
    REFERENCES tasks(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (link_id, project_id)
    REFERENCES external_links(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE feature_external_links (
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  feature_id INTEGER NOT NULL,
  link_id INTEGER NOT NULL,
  UNIQUE (project_id, feature_id, link_id),
  FOREIGN KEY (feature_id, project_id)
    REFERENCES features(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (link_id, project_id)
    REFERENCES external_links(id, project_id) ON DELETE RESTRICT
);

CREATE TABLE change_record_external_links (
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE RESTRICT,
  change_record_id INTEGER NOT NULL,
  link_id INTEGER NOT NULL,
  UNIQUE (project_id, change_record_id, link_id),
  FOREIGN KEY (change_record_id, project_id)
    REFERENCES change_records(id, project_id) ON DELETE RESTRICT,
  FOREIGN KEY (link_id, project_id)
    REFERENCES external_links(id, project_id) ON DELETE RESTRICT
);
```

GitHub URL 规范化必须先用标准 URL Parser 解析，只接受 `https://github.com/`，固定 scheme/host、移除默认端口和 fragment，并对允许保留的 query 参数建立白名单；不得用字符串前缀判断域名。规范 URL 使用唯一约束 + upsert 去重，类型化关联由 ExternalLinkWorkflow 先经目标 QueryPort 校验权限/归属后写入；单域模块不得跨域创建关联。集成测试必须拒绝跨项目关联、重复基础 URL 和重复类型化关联。权威决策见 [ADR-022](docs/adr/ADR-022.md)。

## 5.8 编号生成

F-13 本地实现复用 `app.code_sequences`；`ProjectCodePort.allocateFeatureCode(tx, projectId)` 作为项目编号基础设施公开入口，读取项目 code 并在调用者同一事务原子分配 FEATURE 序号，调用者先完成项目授权和父级锁。2026-09-09 人工确认功能编号不补零，例如 `SHOP-F-3`，遵守既有 CHECK；不承诺无空洞。若导入历史与序列表不一致触发唯一冲突，返回安全 409 并整体回滚，由管理员核对序列，不自动改写历史。

不为每个项目动态创建数据库 Sequence。使用单表原子 UPSERT：

```sql
INSERT INTO code_sequences(project_id, entity_type, last_number)
VALUES ($1, $2, 1)
ON CONFLICT (project_id, entity_type)
DO UPDATE SET last_number = code_sequences.last_number + 1
RETURNING last_number;
```

编号只保证唯一和单调，不承诺无空洞。该 UPSERT 若与业务写在同一事务会随事务一起回滚，本身不会因回滚消耗编号；数据修复、导入、显式预留或未来改成独立编号事务时仍可能产生跳号，因此业务和测试都不得依赖连续性。

---

# 6. 关键业务事务

## 6.1 通用事务规则

- 事务默认使用 PostgreSQL `READ COMMITTED`；
- 所有命令通过 `UnitOfWork.run(async tx => ...)` 执行；`tx` 显式传入每个 CommandPort、Repository 和投影 Writer，事务内使用全局 DB Client 属于 CI 阻断错误；
- 修改聚合根使用原子条件更新或 `SELECT FOR UPDATE`；
- 多聚合命令统一锁序：任务按 ID 升序 → 任务组按 ID 升序 → 迭代记录按 ID 升序 → 遗留项按 ID 升序；如果预读结果在获得锁后改变，必须重新读取并从头重试有限次数；
- 需要验证可写父级的命令先按项目 → 模块 → 功能以及各层 ID 升序取得 `FOR SHARE`，归档方按同序取得 `FOR UPDATE`，之后才进入上述聚合锁序；这样父级归档与子级写入不能基于同一旧 ACTIVE 快照同时提交；
- 事务内不得调用 GitHub、邮件、Webhook、WebSocket 等网络服务；
- 事务内不得执行长时间 CPU 工作；
- 唯一约束冲突必须映射为明确的 409 业务错误；
- 所有非安全 HTTP 写方法默认必须支持幂等 key；豁免只能按 ADR-019 在 Route Registry 中显式登记并经 ADR 评审；
- 数据库异常不得原样返回客户端。

## 6.2 创建项目

同一事务完成：

1. 校验项目编码；
2. 创建项目；
3. 创建者加入项目；
4. 批量加入初始成员；
5. 创建“未分类模块”；
6. 写审计日志；
7. 创建站内通知、活动投影和搜索投影。

全部步骤由 ProjectBootstrapWorkflow 编排；若任一成员无效、停用或重复，整个操作回滚。创建者永远在初始成员列表中且创建时不可取消；项目创建完成后可由系统管理员按普通成员规则移除，`projects.created_by` 保持不可变。

## 6.3 TaskCompletionWorkflow：完成任务并发布迭代记录

由 WorkflowModule 编排，ChangeRecordsModule 与 TasksModule 之间没有领域写依赖；ChangeRecords 仅在独立发布关联任务的记录时使用只读 `TaskQueryPort`。请求 Schema 只有两个 mode，`WITH_RECORD` 内再用互斥联合保证草稿引用与内联内容二选一：

```text
WITHOUT_RECORD { mode, completionNote, expectedRowVersion }
WITH_RECORD { mode, expectedRowVersion, record }
          XOR { mode, expectedRowVersion, recordDraftId, recordExpectedRowVersion }
```

事务顺序：

```text
幂等请求占位
  ↓
按 taskId 查询任务并校验当前用户项目权限
  ↓
按统一锁序锁定任务；WITH_RECORD 的草稿引用分支再锁定该记录
  ↓
草稿分支校验：DRAFT、同项目、作用域匹配、task_id 为空或等于当前任务、record rowVersion 匹配
  ↓
原子更新任务：
  WHERE id = ?
    AND row_version = ?
    AND work_status = 'TODO'
    AND lifecycle_status = 'ACTIVE'
  SET work_status = 'DONE',
      completed_at = now(),
      row_version = row_version + 1
  ↓
记录分支：
  INLINE：生成编号并创建 task_id=当前任务的待发布 change_record
  DRAFT：复用已有 change_record，原子设置 task_id=当前任务，生成正式编号并保持行锁
  ↓
创建 change_record_version v1
  ↓
同一 SQL 将记录设置为 PUBLISHED、写入正式编号/current_version=1/published_at
  ↓
由 change_records.task_id 复合外键落实同项目关联；本次命令只绑定这一条记录，既有记录不受影响
  ↓
写 audit_log
  ↓
写 notification、活动投影、搜索投影
  ↓
保存幂等结果并提交
```

条件更新影响 0 行时，重新读取并区分：不存在/无权限、版本冲突、状态冲突。

“无功能变化直接完成”使用同一状态条件，但不创建迭代记录，必须保存 `completion_note`。

`WITH_RECORD` 的草稿引用分支不得新建第二条 `change_records`。草稿校验、`task_id` 设置、发布、v1 快照和任务完成必须使用同一个 `tx`；任一步失败全部回滚。

独立 `POST /change-records/{recordId}/publish` 先预读 `change_records.task_id`：为空时锁记录并在锁内确认仍为空；非空时按“任务 → 记录”顺序锁定关联任务和记录，并在锁内重查 `task_id` 与任务状态。`task_id IS NULL` 或关联任务为 `DONE` 才能发布，关联任务未完成返回 409。TaskCompletion 同样按“任务 → 记录”锁定；发布与完成并发时必须串行并重查，不得提交“已发布记录关联未完成任务”。

## 6.4 任务合并

事务内：

1. 幂等请求占位；
2. 预读 source、main 及 main 当前组；按固定 ID 顺序锁定 source 和 main；
3. 校验 `source != main`；
4. 校验用户有项目权限；
5. 校验两任务同项目；
6. 校验两任务生命周期允许合并；
7. source 不得属于任何活跃组；
8. main 如果属于活跃组，按任务之后的统一锁序锁定该组并重新读取成员，必须确认 main 在该组中为 MAIN 且组仍为 ACTIVE；
9. main 无组时创建组和 MAIN 成员；
10. 创建 SOURCE 成员并保存原状态/负责人快照；
11. 写审计、通知、活动投影、搜索投影；
12. 保存幂等结果并提交。

数据库的部分唯一索引负责处理并发竞态。业务代码的“先查后写”不能替代唯一约束。

## 6.5 解除合并

解除也遵循与合并相同的锁序，不能先持有组锁再等待任务锁：

1. 预读 group、待解除 SOURCE 和 MAIN 的任务 ID；
2. 按 ID 升序锁定 SOURCE/MAIN 任务，再锁 group 和成员并重新校验；
3. 仅允许解除活跃 SOURCE；
4. 标记成员 `DETACHED` 并记录时间、原因；
5. 不修改任务工作状态和负责人；
6. 如果已无活跃 SOURCE，关闭组并解除 MAIN；
7. 写审计、通知、活动投影、搜索投影；
8. 提交。

新增 SOURCE 与解除最后一个 SOURCE 都必须持有同一 group 行锁；获得锁后再次计算活跃 SOURCE 数量。这样不能提交 `CLOSED` 组带 ACTIVE 成员的状态。

## 6.6 遗留问题转任务

同一事务完成：

1. 请求必须携带稳定 `leftoverItemId`、记录版本和预期行版本；锁定迭代记录，再锁定遗留项并确认当前版本关系表仍包含该 ID；
2. 校验用户权限；
3. 生成任务编号；
4. 创建任务并插入 `leftover_task_links`，将遗留项标记为 `CONVERTED`；
5. 写审计、通知、活动投影和搜索投影；
6. 保存幂等结果。

前端重复点击或请求超时重试不得产生两个任务；即使使用不同 Idempotency-Key，`leftover_task_links.leftover_item_id` 主键也必须使第二次转换返回 409 和既有任务引用。

## 6.7 记录作废、归档和恢复

- 作废 PUBLISHED 或恢复 VOID 迭代记录需要管理员权限、密码 + 当前 TOTP 重认证、去除首尾空白后仍非空的原因、预期版本和幂等键；
- 恢复只允许 `VOID -> PUBLISHED`，保留最近一次 `voided_at/void_reason`；恢复原因与操作者写入不可变审计事件 `CHANGE_RECORD_RESTORED`，不删除或改写任何版本；完整状态机与数据库不变量见 [ADR-024](docs/adr/ADR-024.md)；
- 同一 Idempotency-Key、相同摘要和相同幂等契约版本的作废/恢复重试，只有在当前认证、系统管理员权限以及 `reauthenticated_at` / `mfa_verified_at` 的 5 分钟门禁仍通过时才重放原 2xx；门禁失败时拒绝且不泄露已存响应。使用新 Key 再次作废已 VOID 记录、再次恢复已 PUBLISHED 记录，或任何版本不匹配请求返回 409，且不产生部分状态变更；
- 项目/模块/功能归档后禁止创建下级新数据；
- 恢复时必须验证项目与模块为 ACTIVE；FEATURE scope 另验证功能为 ACTIVE，MODULE scope 不要求功能；可空 `task_id` 仅为历史来源，不参与门禁；
- 父级按项目、模块、功能顺序取得 `FOR SHARE`，再以 `WHERE id = ? AND status = 'VOID' AND row_version = ?` 条件更新记录；归档方对相同父级按同序使用 `FOR UPDATE`。恢复的记录行只更新 `status='PUBLISHED'`、`row_version=row_version+1` 与 `updated_at`；零行更新映射为 409；
- VOID 详情对普通项目成员返回 404，不进入普通统计、成员搜索和普通时间线；搜索投影同事务标记为 `ADMIN_ONLY/VOID`，只有管理员显式 VOID 筛选可见。恢复后按 `status='PUBLISHED'` 重新允许成员读取和统计，以同一实体键 UPSERT 搜索投影为 `MEMBER/PUBLISHED` 并追加不含原因的脱敏 Activity，禁止因保留的 `voided_at` 继续隐藏；业务、审计与投影同事务且不得重复，本期不生成作废/恢复通知；
- 普通用户不获得任何物理删除接口。

---

# 7. 认证、鉴权与安全

## 7.1 Session 认证

使用 NestJS 默认 Express Adapter，Session Store 使用 PostgreSQL，并实现 hash-only Store Adapter；不得直接采用会把原始 SID 作为主键的默认 Store 行为。DB 不保存原始 Token：

```text
user_sessions
  id
  user_id
  token_hash              // HMAC-SHA-256(session_hash_secret_vN, Cookie 原始 Token)
  token_hash_key_version
  auth_version_at_issue
  auth_state              // 显式 AUTHENTICATED 或 MFA 受限态；无默认值
  recovery_rotation_generation / recovery_rotation_consumed_generation
  reauthenticated_at / mfa_verified_at
  created_at
  last_seen_at
  idle_expires_at
  absolute_expires_at
  revoked_at

session_csrf_tokens
  session_id
  token_hash
  issued_at
  expires_at

preauth_sessions
  id
  token_hash / token_hash_key_version
  csrf_token_hash
  created_at / expires_at / consumed_at
```

实现要求：

1. Cookie 原始 Token 使用 32 字节密码学随机数，Base64URL；
2. Cookie 固定 `__Host-session; HttpOnly; Secure; SameSite=Lax; Path=/`，不设置 Domain；
3. 数据库只保存带唯一索引的 `token_hash`，不保存原始 Token；审计只引用 Hash 前缀；Hash 密钥轮换时保存 key version，旧 key 只保留到其 Session 全部绝对过期；
4. 密码使用 Argon2id，初始不低于 `m=19 MiB, t=2, p=1`；生产按机器向上调优，参数随哈希持久化；
5. 密码阶段成功后原子消费并废弃 `preauth_sessions`，轮换 Cookie、创建显式状态的 Session 并签发一次新的 CSRF Token，防止 Session Fixation；普通用户可显式为 `AUTHENTICATED`，管理员在 MFA 完成前只能显式为对应受限态，数据库不提供完整态默认值；
6. Session 保存认证状态、MFA/重认证新鲜度及一次性 rotation generation，但不保存项目成员列表或权限快照；
7. 空闲超时 8 小时、绝对超时 7 天；
8. 每次认证按 5.1.1 比较 `auth_version_at_issue = users.auth_version`；用户停用、修改密码、管理员强制退出时在同一事务递增用户级 `auth_version` 并撤销既有 Session；
9. 登录限流按账号 + IP + 全局速率，并对 Argon2 并发计算设置上限；
10. 系统管理员启用 TOTP MFA；
11. 用户管理、成员管理、项目/模块/功能归档或恢复、原始审计、记录作废或恢复等高风险管理员操作必须同时通过密码和当前 TOTP 重新认证，有效期 5 分钟；任一因子缺失均拒绝；
12. Session 表不进入业务备份；物理全库恢复后 API 启动前统一失效；
13. Nginx 后设置明确的 `trust proxy` 跳数，只信任内部代理；
14. `last_seen_at/idle_expires_at` 最多每 5 分钟写回一次，避免每请求写放大；过期/撤销行按 5.1.1 分批清理；
15. 阶段 0 必须验证自定义 Store 从 Cookie 到查询全程只使用 HMAC，数据库、错误、日志和内存快照中不出现原始 Token。

## 7.2 CSRF

固定同步 Token。登录前使用独立 `preauth_sessions`，登录后使用 `user_sessions + session_csrf_tokens`：

1. `GET /api/v1/auth/csrf` 对匿名和已登录用户都可用；每次调用都新签发一次高熵 Token、只在该次 `no-store` 响应返回明文并在数据库保存 Hash。Fetch Metadata 与同源可读策略必须通过；GET 的 Origin/Referer 若存在则精确校验，缺失本身不拒绝。已登录 Token 最长有效 8 小时且 `expires_at <= user_sessions.absolute_expires_at`；在 Session 行锁内清理过期项并淘汰最早项，最多保留 4 个有效 Hash；
2. 匿名场景创建或轮换最长 10 分钟、Cookie 名为 `__Host-preauth` 的独立预认证 Session，并按 IP + 预认证 Cookie + 全局三层限流；其严格状态不含 `user_id/auth_version`，只能用于 CSRF bootstrap 与登录；
3. 登录请求只接受匿名预认证 Session 及其同步 Token；已有普通、MFA 受限或完整认证 Session 调用登录统一返回 409，客户端必须先登出/清 Cookie，再调用 CSRF 签发端点建立新预认证状态。成功登录后在同一事务标记预认证 Session 已消费，轮换为 `__Host-session`，并签发认证 Session 的新 CSRF Token；
4. `/me` 不返回 CSRF Token，也不得尝试从 Hash 恢复旧明文；SPA 刷新或新标签页需要 Token 时重新调用 `GET /api/v1/auth/csrf`，明文只保存在内存；
5. 除无效/已撤销 Session 的 `logout` 恢复分支外，非安全方法必须带 `X-CSRF-Token`，服务端对输入做 HMAC/Hash 后在当前有效 Hash 集合中常量时间比较；仅当服务端明确表示 CSRF 校验失败且业务/安全状态尚未消费时，客户端才可重新签发并最多重试一次，禁止循环刷新或无界重试。`idempotencyRequired` 及版本化路由保留原 Idempotency-Key/If-Match；`securityFlow` 不发送业务幂等键，按 ADR-023 的逐操作机制恢复。有效 Session 首次登出仍须验证 CSRF；若 Session 已无效、已撤销或首次响应丢失，`logout` 只在同源 Origin/Referer 与 Fetch Metadata 通过时清 Cookie、返回 204，不读取身份或执行状态写；
6. 同时校验 `Origin`；缺失时校验 `Referer`；两者都缺失则拒绝。校验 `Sec-Fetch-Site/Mode/Dest`，明确 cross-site 的非安全请求直接拒绝；Fetch Metadata 作为纵深防御，不能替代 Token 与 Origin/Referer；
7. 登录、有效 Session 的首次登出、MFA 与管理操作全部覆盖同步 Token；上述无效 Session 登出是唯一例外，`SameSite=Lax` 只作为纵深防御；
8. 预认证 Session 不得访问业务资源；过期、已消费项由清理任务分批回收。

## 7.3 资源鉴权与权限矩阵

禁止信任请求中的 `projectId` 来证明资源归属。正确查询模式：

```sql
SELECT t.*
FROM tasks t
WHERE t.id = $taskId
  AND (
    $isAdmin
    OR EXISTS (
      SELECT 1
      FROM project_members pm
      WHERE pm.project_id = t.project_id
        AND pm.user_id = $actorId
        AND pm.status = 'ACTIVE'
    )
  );
```

成员关系每次请求实时读取，禁止缓存在 Session 或长生命周期对象中。普通成员被移除后立即失去成员关系派生的项目访问权，但不影响其他项目；系统管理员仍按全局身份授权。停用用户的 Session 按无效处理，不能恢复身份；仅可按匿名安全语义调用 `issueCsrfToken` 或无效 Session 的同源 `logout`。

权限决策如下。项目创建者只是不可变的溯源属性，不形成额外角色；普通成员创建者的权限来自其活跃成员关系，创建完成后管理员可将其移除，随后失去成员关系派生权限。创建者若同时是系统管理员，仍保留与成员关系无关的全局权限。ADR-023 的九个认证 operationId 必须按受限 Session/challenge 前置状态使用 [docs/permissions.md](docs/permissions.md) 的认证安全流程矩阵逐项验证，不得由下表笼统“登录”行代替。完整权威矩阵见 [docs/permissions.md](docs/permissions.md)：

| 端点/动作 | 匿名 | 同项目活跃成员 | 其他项目活跃成员 | 已移除成员 | 系统管理员 | 额外约束 |
|---|---|---|---|---|---|---|
| 创建项目 | 401 | 允许 | 允许 | 允许 | 允许 | 任一启用用户可创建，请求者自动成为成员 |
| 读项目、模块、功能、任务、记录、活动 | 401 | 允许 | 404 | 404 | 允许 | 归档数据仍可读；VOID 记录按下一行 |
| 读取 VOID 迭代记录详情 | 401 | 404 | 404 | 404 | 允许 | 恢复为 PUBLISHED 后成员重新可读 |
| 新建/编辑模块、功能、任务、草稿记录、链接 | 401 | 项目内允许 | 404 | 404 | 允许 | 项目及父级必须 ACTIVE |
| 任务完成/重新打开/取消/恢复、任务合并/解除、遗留转任务 | 401 | 项目内允许 | 404 | 404 | 允许 | 状态机、版本与幂等约束 |
| 成员管理 | 401 | 403 | 404 | 404 | 允许 | 密码 + 当前 TOTP 重认证；写审计 |
| 项目、模块或功能归档/恢复 | 401 | 403 | 404 | 404 | 允许 | 密码 + 当前 TOTP 重认证；写审计 |
| 原始审计查询/导出 | 401 | 403 | 403 | 403 | 允许 | 密码 + 当前 TOTP 重认证；读取留痕 |
| 作废 PUBLISHED / 恢复 VOID 迭代记录 | 401 | 403 | 403 | 403 | 允许 | 密码 + 当前 TOTP 重认证 + 原因 |
| 用户管理（`listAdminUsers`/`createUser`/`updateUser`/`disableUser`/`enableUser`/`forceLogoutUser`，`/api/v1/admin/users`） | 401 | 403 | 403 | 403 | 允许 | 读列表仅需完整管理员 Session；五个写操作要求 5 分钟内密码 + 当前 TOTP 重认证、CSRF 与幂等键，除创建外的版本化操作另要求 `If-Match`；唯一/版本/状态冲突 409，禁止自停用/自降级/自强退，并保护最后一名可用 MFA 管理员；停用/强退同事务递增 `auth_version`、撤销 Session 并写 `admin.user.*` 审计 |
| 搜索 | 401 | 仅返回本人活跃成员项目 | 不返回其他项目 | 不返回 | 可按服务端 Scope 查询 | `ProjectAccessQueryPort` + SQL 层过滤 |
| 通知读取/已读 | 401 | 仅本人 | 仅本人 | 仅本人 | 仅本人 | SQL 强制 recipient_id，管理员不得代读 |

匿名用户访问受保护或业务端点统一 401；停用用户的所有 Session 视为失效，受保护/业务端点及使用停用凭据的登录统一 401。匿名安全例外只有 `issueCsrfToken` 与无效/已撤销 Session 的同源 `logout`：前者只创建无身份预认证状态，后者只清 Cookie 并返回 204。资源型端点对不存在和无权访问统一 404；明确的全局管理端点返回 403。机器可执行矩阵保存在 `apps/api/test/permissions.matrix.ts`，每一条 Route Registry 项必须至少匹配一组允许和一组拒绝用例，CI 检查无遗漏。

### 7.3.1 TOTP MFA 生命周期

1. 首个系统管理员由一次性离线 bootstrap 命令创建，首次登录只能进入 MFA 注册流程；
2. TOTP Secret 使用 160 位以上随机值，以 AEAD 加密保存，行内保存 `key_version/nonce/ciphertext/tag`；加密主密钥来自 `/run/secrets/totp_kek_keyring`；
3. 注册只有在 expected 用户级 enrollment generation 与 pending Secret 匹配且验证码条件更新成功后才启用；同一事务撤销受限 Session，轮换 Session ID 与 CSRF，并显式签发 `AUTHENTICATED` Session。展示二维码、明文 Secret 和首次恢复码的响应为 `no-store` 且不写日志；
4. 验证窗口固定为当前时间步 ±1，并记录 `last_accepted_step`，同一时间步不得重放；登录和验证均限流；
5. 启用时生成高熵一次性恢复码，只保存带独立 salt 与参数的 Argon2id 编码哈希；禁止使用普通 SHA-256。使用后原子标记并轮换全部恢复码，校验尝试必须限流；
6. MFA 重置必须由另一名已完成 MFA 且刚通过密码 + 当前 TOTP 重认证的系统管理员执行，并填写必填原因；重置事务同时递增目标用户 `auth_version`、撤销其全部 Session、失效旧因子/恢复码并写审计。系统仅剩一名管理员时，才允许走双人审批的离线恢复 Runbook，审批人与执行人分离；MFA 禁用、密钥轮换和恢复码使用同样要求重认证并写审计；
7. `reauthenticateAdmin` 在密码与未使用的当前 TOTP 都通过后，使用同一服务端事务时间原子更新当前 Session 的 `reauthenticated_at` 与 `mfa_verified_at`，并递增一次性的 `recovery_rotation_generation`；高风险操作检查两个时间戳均非空且均在 5 分钟内，不能只信任前端时间；
8. MFA 密钥轮换保留旧 key version 直到所有密文重加密完成；备份与灾难恢复演练必须包含密钥可用性验证，但密钥不得与数据库备份同库存放。

## 7.4 审计

审计记录业务动作：

```text
PROJECT_CREATED
PROJECT_MEMBER_REMOVED
TASK_COMPLETED
TASK_MERGED
TASK_DETACHED
CHANGE_RECORD_PUBLISHED
CHANGE_RECORD_VERSION_CREATED
CHANGE_RECORD_VOIDED
CHANGE_RECORD_RESTORED
AUDIT_LOG_READ
```

每条审计包含 actor、action、target_type/target_id/project_id、脱敏前后快照、reason、request_id、client_request_id、ip_address、user_agent、created_at，并具有明确的链、序号、密钥版本和规范化版本：

```sql
CREATE TABLE audit_chain_heads (
  chain_id TEXT PRIMARY KEY,
  last_sequence BIGINT NOT NULL DEFAULT 0 CHECK (last_sequence >= 0),
  last_hash BYTEA NOT NULL,
  key_version SMALLINT NOT NULL CHECK (key_version > 0),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE audit_logs (
  chain_id TEXT NOT NULL REFERENCES audit_chain_heads(chain_id) ON DELETE RESTRICT,
  sequence_no BIGINT NOT NULL CHECK (sequence_no > 0),
  key_version SMALLINT NOT NULL CHECK (key_version > 0),
  canonical_version TEXT NOT NULL,
  actor_id INTEGER REFERENCES users(id) ON DELETE RESTRICT,
  action TEXT NOT NULL,
  target_type TEXT NOT NULL,
  target_id INTEGER,
  project_id INTEGER,
  canonical_payload JSONB NOT NULL,
  prev_hash BYTEA NOT NULL,
  record_hash BYTEA NOT NULL,
  created_at TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (chain_id, sequence_no),
  UNIQUE (chain_id, record_hash)
);

CREATE UNIQUE INDEX ux_audit_one_successor
  ON audit_logs(chain_id, prev_hash)
  WHERE sequence_no > 1;
```

分链规则（字段名 `chain_id` 等同协议中的 chain scope）：

- 项目事件进入 `PROJECT:<project_id>` 链；
- 登录、用户管理、原始审计读取等全局事件进入 `SYSTEM` 链；
- chain ID 由服务端根据真实资源归属生成，客户端不能提供；
- 新链创建使用唯一键竞争，初始 `last_hash` 为固定协议定义的 32 字节零值。

并发安全追加协议必须与业务写入使用同一个 `TransactionContext`：

1. 事务若涉及多个 chain scope，先按 scope 的 UTF-8 字节序升序排序，再依次调用固定 `search_path` 的 `SECURITY DEFINER audit_lock_head(chain_id)` 对链头执行 `SELECT ... FOR UPDATE`；禁止按业务发现顺序加锁；
2. 锁函数返回 `last_sequence/last_hash/key_version`，令 `sequence_no = last_sequence + 1`；
3. 构造固定 envelope：`{chainScope, sequenceNo, keyVersion, prevHash, eventPayload}`；`prevHash` 使用协议规定的 Base64URL 表示，`eventPayload` 承载动作与业务字段，字段集合和编码由 `canonical_version` 固定；
4. 按 RFC 8785/JCS v1 对完整 envelope 生成 UTF-8 bytes，仅计算 `record_hash = HMAC-SHA256(audit_hmac_key_vN, jcs_envelope_bytes)`；禁止再拼接 `prev_hash` 或使用非规范 JSON；
5. 调用 `audit_append_locked(expected_sequence, expected_prev_hash, expected_key_version, envelope, record_hash)`；函数再次核对链头，插入日志并更新 `last_sequence/last_hash/key_version`，不匹配则抛出可重试冲突；
6. `app_runtime` 只拥有两个受限追加函数的 `EXECUTE`，对 `audit_logs/audit_chain_heads` 无直接 SELECT/INSERT/UPDATE/DELETE/TRUNCATE/ALTER/REFERENCES，也不拥有管理员只读函数；追加函数 owner 为 NOLOGIN `audit_writer`，固定 `search_path`，禁止动态 SQL；
7. 同一链上的并发追加串行，不同项目链可并行。测试必须覆盖单 scope 并发及多 scope 反向业务顺序，确认统一 UTF-8 锁序无死锁、序号连续且无分叉。

`canonical_version='JCS-1'` 和 `key_version` 是审计行的一部分。当前及保留旧密钥由 `/run/secrets/audit_hmac_keyring` 按版本提供，`AUDIT_HMAC_KEY_VERSION` 选择当前写入版本。业务调用省略 `nextKeyVersion` 时，由 `PostgresAuditWritePort` 对比 keyring 当前版本与链头版本，若当前版本更高，则在链头锁内先用旧密钥写 `AUDIT_KEY_ROTATED`，其 `eventPayload` 只含新版本号；该记录提交后更新 head 的 `key_version`，下一条记录才使用新密钥。轮换由部署 keyring 当前版本驱动，不新增高风险管理 API/CLI；显式指定 `nextKeyVersion` 仅允许 `AUDIT_KEY_ROTATED`。旧密钥只读保留到对应历史完成归档和保留期，不能与数据库备份同库存放。

原始审计读取先由 `AdminAuthorizationPort` 校验系统管理员的密码 + 当前 TOTP 重认证，再由 AuditQueryService 使用独立 `audit_reader` 连接查询；结果只暂存在服务端，`app_runtime` 必须在返回前通过受限追加函数向 SYSTEM 链提交 `AUDIT_LOG_READ`（查询条件、返回条数和操作者，不记录返回正文），追加失败则丢弃结果并整体失败。`audit_reader` 只读且不提供给普通 Repository。审计数据纳入加密备份；`audit_archive_writer` 至少每小时把 `chain_id/last_sequence/last_hash/key_version/record_count/generated_at` 检查点写入异机对象锁/WORM，每日导出加密审计明细。归档清单使用独立 `audit_archive_signing_key` 或受控 KMS 签名，验证公钥随发布清单分发；归档凭据只允许新建对象，不能覆盖或删除。恢复演练校验序号、Hash、密钥版本、签名和远端锚点。

不使用通用审计触发器与应用审计双写。承诺“可追溯、可检测篡改”；数据库所有者、宿主机或应用运行时被完全控制时，仍需依赖受限访问、密钥管理、远端只读归档和外部审计，不承诺绝对不可抵赖。

## 7.5 XSS 与浏览器安全

- Markdown 保存原文；
- `react-markdown + rehype-sanitize`，不启用原始 HTML；
- 所有可保存或渲染的外部链接协议仅允许 `https`，外链 `rel="noopener noreferrer"`；
- API JSON 响应不拼接 HTML。

安全响应头：

| 头 | 值 |
|---|---|
| `Strict-Transport-Security` | 仅在 HTTPS 响应设置；专用生产域及全部子域确认仅支持 HTTPS 后使用 `max-age=31536000; includeSubDomains`，否则使用不带 `includeSubDomains` 的策略并完成域名清单评审 |
| `Content-Security-Policy` | 每个 HTML 响应动态生成：`default-src 'self'; script-src 'self' 'nonce-{RANDOM}'; script-src-attr 'none'; style-src 'self' 'nonce-{RANDOM}'; style-src-attr 'none'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-src 'none'; frame-ancestors 'none'; base-uri 'none'; object-src 'none'; form-action 'self'` |
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `DENY` |
| `Referrer-Policy` | `no-referrer` |
| `Permissions-Policy` | `camera=(), microphone=(), geolocation=(), payment=(), usb=()` |

- `/api/v1/**`、会话与用户信息 `Cache-Control: no-store`；
- `index.html no-store`，防止动态 nonce 被共享缓存复用；带哈希静态资源 immutable；
- 前后端同源部署，生产不启用跨域 Credential，不设置宽泛 CORS；
- HTML 入口层每次响应生成至少 128 bit 随机 nonce，同时写入 CSP 和受控 bootstrap 元数据；nonce 不记录、不跨响应复用，AntD StyleProvider 的运行时 style 标签必须使用同一 nonce；
- 阶段 0 先以 Report-Only 跑登录、弹窗、表格、主题、懒加载和错误页 E2E，再切强制 CSP；生产禁止 `script-src/style-src 'unsafe-inline'`，无法满足时阻断并以 ADR 更换方案；
- Nginx 仅允许 TLS 1.2/1.3，HTTP 80 对除 ACME challenge 外的请求返回 308 到同主机 HTTPS；HSTS 只在 HTTPS 响应发出；
- Nginx 只信任内部代理，用户直接伪造的 `X-Forwarded-*` 不生效。

CSP 权威决策见 [ADR-021](docs/adr/ADR-021.md)。

## 7.6 SSRF

V1.2.2 只解析并保存 GitHub URL，不保存 GitHub Token、不调用 GitHub API，也不由服务端抓取远程内容。GitHub 链接只允许规范化后的 `https://github.com/`，拒绝用户信息、非默认端口、混淆域名与 HTTP。未来启用请求时，必须通过新 ADR 增加 DNS 私网/环回拒绝、逐跳重定向校验、超时、响应体上限、专用低权限 Token，且用户不能控制代理与请求头。

## 7.7 Secrets

- 开发环境 `.env` 不提交 Git，`.env.example` 只放非敏感变量名；
- 生产只允许读取显式 `*_FILE` 指向的 `/run/secrets/*` 文件；
- PostgreSQL 使用 `POSTGRES_PASSWORD_FILE`；
- `readRequiredSecretFile(path)` 遇到文件缺失、空值、路径越界或权限不合规必须 fail closed 并阻止进程就绪；生产禁止 Secret 的环境变量 fallback，环境变量仅可保存非敏感版本号；
- 环境变量 Secret fallback 只允许在明确的 dev/test profile，生产构建和启动测试必须证明该分支不可达；
- Session HMAC、幂等 fingerprint HMAC、审计 HMAC 与 TOTP KEK 使用相互独立的版本化 keyring Secret；部署清单以非敏感版本号选择当前版本。幂等旧 key 至少保留到引用它的 30 天记录全部过期并清理；其他旧版本分别保留到旧 Session 绝对过期、审计保留期结束或所有 TOTP 密文重加密完成，并通过轮换/恢复测试后才退役；
- 不使用无用途的 `JWT_SECRET`；
- 日志、异常、审计快照脱敏。

---
# 8. 通知、活动与搜索投影

## 8.1 站内通知

V1.2.2 使用前端 30～60 秒轮询：

```text
GET /api/v1/notifications?after=<cursor>
```

用户操作事务内只创建 `notifications` 数据库行。事务提交后前端下一次轮询即可读取，不需要 WebSocket、Message Queue 或 Redis。

通知游标是服务端签名/编码的 `(created_at, id)`，查询必须先用 `recipient_id = current_user.id` 过滤再应用游标和 LIMIT；不得接受客户端指定 recipient。通知表以 `(source_event_id, recipient_id, notification_type)` 唯一，防止同一业务事件重复生成。

## 8.2 同事务投影

本项目“项目动态”和“全局搜索”采用数据库投影：

- `activity_projection`：脱敏动态，普通成员可读；
- `search_projection`：规范化文本，仅 Search 查询读取；
- 两者与业务聚合、审计及该命令契约规定的通知（若有）在同一个数据库事务中写入；
- 事务提交后读模型立即可见，不存在异步延迟；
- 不在应用层额外发送事件，也不建立独立消费者。

`activity_projection` 必须保存来源审计的 `source_chain_id/source_sequence` 并建立唯一约束，同时保存 `source_entity_type/source_entity_id`、派生的 `visibility_scope/source_status/source_row_version`。普通成员可见字段使用白名单 DTO，不得复制原始审计快照。投影 Writer 必须接收发起命令的单域 Application Service 或跨域 Workflow 所使用的同一 `tx`。记录作废时，同事务把该记录全部既有动态及新 `CHANGE_RECORD_VOIDED` 动态更新/写入为 `ADMIN_ONLY/VOID`；恢复时把同一记录的既有动态恢复为 `MEMBER/PUBLISHED`，再追加 `CHANGE_RECORD_RESTORED`。两类事件均不得包含作废或恢复原因，且业务状态仍是唯一真相。其他归档/恢复也必须按 Route Registry 更新派生可见性，不允许留下无权限或重复投影。

ActivityQueryService 在任何投影 SQL 之前必须调用 `ProjectAccessQueryPort` 取得服务端 `AuthorizedProjectScope`，再将 Scope 与 `visibility_scope` 共同作为参数化谓词：普通成员只能读取 `MEMBER`，系统管理员才可读取 `ADMIN_ONLY`。投影一致性测试用 `source_entity_id/source_row_version` 对照业务表，禁止依据过期派生状态放宽权限；Controller 不能直接提供 Scope。Activity/Search 是同库横切投影，不是领域聚合：由发起业务命令的单域 Application Service 或跨域 Workflow 经 WritePort 使用同一事务写入，二者都不得直接访问投影实现或另开事务。

## 8.3 Outbox：V1.2.2 不实现

本期不创建 `outbox_events` 表，不建立 worker。原因：

- 所有副作用都是同库写入，事务一致性可以直接保证；
- 不引入 worker、抢占锁、重试、幂等、死信、顺序性、积压监控与清理等复杂度；
- 只有未来出现 GitHub API 推送、邮件、Webhook 等外部副作用时，才新增 ADR 实现 Outbox。

未来启用时的最低约束：

1. 业务事务内只写 `outbox_events`；
2. 独立 worker 使用 `FOR UPDATE SKIP LOCKED` 抢占；
3. 指数退避、最大次数、`DEAD` 状态与告警；
4. 发送者幂等键、事件顺序与积压监控；
5. 清理与归档策略；
6. 禁止用网络调用替换数据库事务内的关键一致性。

---
# 9. 搜索设计

## 9.1 V1.2.2 搜索范围

覆盖：

- 项目、模块、功能名称和当前说明；
- 任务编号、标题和描述；
- 迭代记录编号、标题、问题、方案、验证结果和遗留问题；
- GitHub Issue/PR/Commit 标识。

## 9.2 搜索投影

单独维护 `search_projection`：

```text
search_projection
  id
  project_id
  entity_type
  entity_id
  title
  summary
  raw_text
  normalized_search_text
  visibility_scope (MEMBER | ADMIN_ONLY | HIDDEN)
  source_status
  source_row_version
  updated_at
  UNIQUE(project_id, entity_type, entity_id)
```

- 同一业务事务内更新投影；业务表状态仍是唯一真相，`visibility_scope/source_status/source_row_version` 只是可校验的派生缓存；
- 每条记录携带 `project_id`；SearchQueryService 必须先调用 `ProjectAccessQueryPort` 取得 `AuthorizedProjectScope`，再把 Scope 作为参数化谓词约束搜索 SQL；
- 投影字段必须脱敏，不包含密码、Token、密钥等敏感内容。
- 更新使用上述唯一键 UPSERT。记录作废同事务设置 `ADMIN_ONLY/VOID`，恢复同键 UPSERT 为 `MEMBER/PUBLISHED`；普通成员查询只允许 `MEMBER`，管理员默认也不返回 `ADMIN_ONLY`，只有显式的 VOID 筛选才允许按当前资源权限返回对应记录。其他完全不可检索实体设为 `HIDDEN` 或移除；投影一致性测试按 `source_row_version` 对照业务表，搜索不得依据旧投影放宽权限。

## 9.3 规范化与索引

```sql
CREATE EXTENSION IF NOT EXISTS pgroonga WITH SCHEMA app;
CREATE INDEX IF NOT EXISTS idx_search_projection_pgroonga
  ON app.search_projection
  USING pgroonga (
    normalized_search_text app.pgroonga_text_full_text_search_ops_v2
  );
```

`USING pgroonga` 的默认 opclass 为
`pgroonga_text_full_text_search_ops_v2`（TokenBigram），已在 V1 PoC 中验证
全量英文缩写、完整代码标识符、中文短词和编号搜索。PGroonga 不是 trusted
extension，实际首次安装由 `database/bootstrap/020_pgroonga.sql` 以
`cluster_bootstrap` 完成；`0003_search_pgroonga.sql` 使用幂等守卫与
schema 限定定义，缺少扩展时 fail closed。bootstrap 同时撤销业务角色的
扩展函数 EXECUTE，仅给 `app_runtime` 授权查询与转义所需函数。

旧 `0001` 的 GIN 索引与 `pg_trgm` 扩展按 expand/contract 单独迁移：
`0004` 在确认 `idx_search_projection_pgroonga` 存在后删除旧 GIN 索引；
`0005` 在确认旧 GIN 已删除且 `pg_trgm` 没有扩展外依赖后删除扩展。
`0003` 不与这两项清理合并，便于先验证默认计划再移除旧能力。

规范化规则：

- Unicode NFKC；
- 转小写、trim、合并连续空白；
- 统一常见全角/半角标点；
- 保留中文、英文、数字和业务必要的短横线；
- 原始文本仍保留，展示时使用原文。

查询规则：

- 参数化查询，禁止拼接用户输入；
- 最短查询长度 2 个字符，最大长度限制，分页；
- 普通查询必须使用 `normalized_search_text &@~ app.pgroonga_query_escape($1)`；
- 禁止使用未转义 `&~`，禁止把用户输入透传给 Groonga 高级查询语法；
- 不提供正则表达式搜索；V1 不保证任意英文或代码子串；
- 权限过滤必须发生在 SQL 层，禁止全量拉取后前端过滤；未取得 `ProjectAccessQueryPort` 返回的服务端 Scope 时查询必须 fail closed。

## 9.4 中文 PoC 验收

V1 采用 PGroonga。验收分为阶段 0 可行性门禁和阶段 4 容量门禁：

| 阶段 | 数据与金标 | 目标 |
|---|---|---|
| 阶段 0 | ≥ 1000 条真实/仿真投影；≥ 100 条人工标注查询，覆盖中文短词、编号、英文、混合字符、全半角标点和无结果 | 固定 Top-K，`Recall@20 ≥ 90%`；`EXPLAIN (ANALYZE, BUFFERS)` 证明目标查询使用 PGroonga 索引且默认计划可用；跨项目返回 0 条；V1 语义探针通过 |
| 阶段 4 | 搜索投影 ≥ 100,000 条且不低于 5 年容量模型峰值的 1.2 倍；≥ 200 条冻结金标查询 | 30 个并发请求持续 10 分钟；预热后 P95 < 500ms、P99 < 1s，另报冷缓存；`Recall@20 ≥ 90%`；跨项目返回 0 条 |

两个阶段都必须验证空、超长、特殊字符和无结果输入不报错、不扩大资源消耗；金标集、随机种子、硬件/容器资源、SQL 与查询计划作为 CI Artifact 保存。

PostgreSQL 18.6 探针镜像已完成 PGroonga 构建、扩展安装、默认查询计划、
`0000-0002 -> 0003-0005` 显式迁移、逐迁移事务内回滚、旧 `pg_trgm`
contract 清理和逻辑恢复后的索引可用性验证；`020_pgroonga.sql` 与
`0003-0005` 已通过真实 PostgreSQL 集成测试。正式阶段 0 已实现
SearchQueryService 服务层、真实 PostgreSQL 权限过滤/参数化查询测试；
生产 `ProjectAccessQueryPort` 适配器已由 `ProjectsModule` 提供；
仍需接入搜索 API/Controller 及生产备份恢复。
若 V2 要求
任意英文/代码子串，再通过 ADR 评估 `TokenNgram`、
`TokenBigramSplitSymbolAlphaDigit` 或独立搜索引擎；不得直接使用未转义
正则查询。

---
# 10. 前端设计约束

## 10.1 状态边界

- TanStack Query：项目、模块、功能、任务、记录、通知等服务端状态；
- React Hook Form：表单草稿和校验；
- URL：分页、筛选、排序、当前 Tab 等可分享状态；
- React local state：弹窗、抽屉和一次性 UI 状态；
- Zustand：仅在确有跨页面客户端状态时引入，不保存服务端实体副本。

默认不创建 `authStore/projectStore` 保存重复数据。当前用户由 `/me` Query 管理，当前项目尽量由路由参数表达。

## 10.2 前端依赖边界与数据获取

固定依赖方向：

```text
app → pages → features → shared / generated
```

- `pages` 只做路由和页面组合，不写业务规则；
- `features` 包含业务组件、hooks、API 使用层；禁止 import `pages`；
- `shared` 不反向引用 `features`；
- `generated` 仅由生成工具修改；
- ESLint/dependency-cruiser 规则在 CI 阻断反向依赖。

React Router 负责路由和访问入口，TanStack Query 负责服务器数据。不得同时为同一资源混用 Router Loader 缓存与 Query 缓存。

在 F-30 基础框架实现中，前端路径别名统一为 `@app/*`、`@pages/*`、`@features/*`、`@shared/*` 与 `@generated/*`。路由采用 `pages/<domain>/route.ts` 声明，并由 `AppRouter` 静态自动聚合；`dependency-cruiser`（执行命令 `pnpm check:frontend:boundaries`）作为本地与 CI 门禁保证上述分层单向流动。

所有 API 调用通过生成客户端进入；禁止组件直接裸写 `fetch/axios`。

## 10.3 更新冲突

编辑表单带回读取时的 `rowVersion`。API 返回 409 时：

1. 保留用户输入；
2. 提示数据已被修改；
3. 支持重新加载最新数据；
4. 不自动覆盖他人修改；
5. 需要时展示字段差异。

## 10.4 静态资源缓存

- `index.html`：`Cache-Control: no-store`；
- 带内容哈希的 `/assets/*`：`public, max-age=31536000, immutable`；
- API：默认 `no-store`，具体查询按需求设置；
- 版本发布后执行 smoke test，确认新 index 引用了已存在的静态资源。

---

# 11. 部署与运维

## 11.0 生产上线阻断项

以下任一项未完成，不得上线：

| 项 | 要求 |
|---|---|
| 版本与镜像 | 每个运行镜像使用 `repository:exact-patch@sha256:<64hex>` 或内部不可变 Tag + digest、记录 Git commit SHA，无 `latest/next/beta/rc` |
| 数据库角色 | bootstrap、NOLOGIN owner/audit_writer、migrator、runtime、backup、audit_reader、audit_archive_writer 分离；所有 LOGIN 角色使用独立 Secret，runtime 非 owner、非超级用户、不可 DDL |
| PostgreSQL 18 卷 | 命名卷挂载 `/var/lib/postgresql`，不得沿用 17 及以下的 `/var/lib/postgresql/data` |
| 容器身份 | 所有运行容器非 root；API/Web 根文件系统只读；迁移、运行和备份使用不同身份 |
| 资源限制 | db/api/web 设置 CPU、内存、`pids_limit` 等上限；数据库 `max_connections` 不无限 |
| 连接池 | API 数据库连接池按并发与 DB `max_connections` 设计，不允许无界创建；阶段 0 压测确定 |
| 迁移 | 独立 `migrate` 任务，API 启动不自动迁移；`service_completed_successfully` 后才启动 API |
| 健康检查 | live 不查 DB；ready 验证 DB 连接与 migration version；Compose health 只负责启动/观测，运行期未就绪由 API readiness gate 返回 503 |
| 网络 | 仅 Nginx 暴露端口；PostgreSQL/API 不映射公网；仅信任内部代理头 |
| Secrets | 各 LOGIN DB 角色凭据、版本化 Session HMAC/幂等 fingerprint HMAC/审计 HMAC/TOTP KEK、备份加密、归档签名/WORM 凭据、TLS 独立使用 `/run/secrets/*`；生产无环境变量 fallback |
| 日志 | pino JSON stdout，设置大小/文件上限；不记录密码、Cookie、CSRF Token、Secret |
| 备份 | 至少每 12 小时生成加密备份，排除 Session 数据，SHA-256 校验并异机保留；RPO ≤ 24h、RTO ≤ 2h，以实测为准 |
| 恢复 | 每月恢复到临时库运行 smoke test；最近成功备份/恢复时间可查询 |
| 回滚 | 数据库采用 expand/contract；代码回滚不等于数据库回滚；破坏性字段删除延后 |
| 扫描 | 依赖漏洞、镜像、Secret、权限矩阵、安全头全部通过 |

## 11.1 镜像

采用独立的 API、migration/ops 和 Web 多阶段镜像；数据库使用基于官方 PostgreSQL 18 精确补丁镜像构建的内部 bootstrap 镜像：

```text
api image
  builder: pnpm install --frozen-lockfile + build
  runtime: 仅生产依赖、非 root 用户、healthcheck

migration/ops image
  builder: 与 API 同 commit 构建
  runtime: 仅迁移/备份命令及所需依赖，非 root；不用于承载 API

web image
  builder: pnpm install --frozen-lockfile + Vite build
  runtime: nginx-unprivileged，COPY dist 到镜像，监听 8080/8443

db bootstrap image
  base: postgres:<exact-patch>@sha256:<verified-digest>
  content: 经评审的角色/bootstrap SQL；不包含任何密码
```

禁止生产 Compose 挂载开发机的 `frontend/dist`。Dockerfile 的每个 `FROM` 也必须固定 digest。发布前 preflight 校验所有 `*_IMAGE_REF` 均匹配 `.+:[^@]+@sha256:[0-9a-f]{64}`，拒绝空 digest 和占位值；本文不伪造实际 digest，真实值由受信镜像仓库解析后写入签名发布清单。发布记录保存镜像 RepoDigest、SBOM、Git commit SHA 和构建时间。

## 11.2 Compose 拓扑

```yaml
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "20m"
    max-file: "5"

services:
  db:
    image: "${DB_IMAGE_REF}"
    user: postgres
    restart: unless-stopped
    init: true
    read_only: true
    volumes:
      - pg_data:/var/lib/postgresql
    tmpfs:
      - /tmp:size=64m,noexec,nosuid,nodev
      - /var/run/postgresql:size=16m,nosuid,nodev
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "2.0"
    mem_limit: 4g
    pids_limit: 256
    stop_grace_period: 60s
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app_runtime -d app"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks: [backend]
    logging: *default-logging

  migrate:
    image: "${MIGRATE_IMAGE_REF}"
    user: "10001:10001"
    command: ["pnpm", "db:migrate"]
    read_only: true
    tmpfs: [/tmp:size=64m,noexec,nosuid,nodev]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: app_migrator
      DB_PASSWORD_FILE: /run/secrets/db_migrator_password
    depends_on:
      db:
        condition: service_healthy
    secrets: [db_migrator_password]
    networks: [backend]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "1.0"
    mem_limit: 1g
    pids_limit: 128
    restart: "no"
    logging: *default-logging

  api:
    image: "${API_IMAGE_REF}"
    user: "10001:10001"
    restart: unless-stopped
    init: true
    read_only: true
    tmpfs: [/tmp:size=128m,noexec,nosuid,nodev]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: app_runtime
      DB_PASSWORD_FILE: /run/secrets/db_runtime_password
      AUDIT_DB_HOST: db
      AUDIT_DB_PORT: "5432"
      AUDIT_DB_NAME: app
      AUDIT_DB_USER: audit_reader
      AUDIT_DB_PASSWORD_FILE: /run/secrets/db_audit_reader_password
      NODE_ENV: production
      SESSION_HASH_KEY_VERSION: "${SESSION_HASH_KEY_VERSION}"
      SESSION_HASH_KEYRING_FILE: /run/secrets/session_hash_keyring
      IDEMPOTENCY_FINGERPRINT_KEY_VERSION: "${IDEMPOTENCY_FINGERPRINT_KEY_VERSION}"
      IDEMPOTENCY_FINGERPRINT_KEYRING_FILE: /run/secrets/idempotency_fingerprint_keyring
      AUDIT_HMAC_KEY_VERSION: "${AUDIT_HMAC_KEY_VERSION}"
      AUDIT_HMAC_KEYRING_FILE: /run/secrets/audit_hmac_keyring
      TOTP_KEK_VERSION: "${TOTP_KEK_VERSION}"
      TOTP_KEK_KEYRING_FILE: /run/secrets/totp_kek_keyring
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    secrets:
      - db_runtime_password
      - db_audit_reader_password
      - session_hash_keyring
      - idempotency_fingerprint_keyring
      - audit_hmac_keyring
      - totp_kek_keyring
    networks: [backend]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "1.5"
    mem_limit: 2g
    pids_limit: 256
    healthcheck:
      test: ["CMD", "node", "healthcheck.mjs"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging: *default-logging

  web:
    image: "${WEB_IMAGE_REF}"
    user: "101:101"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=32m,noexec,nosuid,nodev
      - /var/cache/nginx:size=32m,nosuid,nodev
      - /var/run:size=8m,nosuid,nodev
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "80:8080"
      - "443:8443"
    environment:
      TLS_CERT_FILE: /run/secrets/tls_cert
      TLS_KEY_FILE: /run/secrets/tls_key
    secrets: [tls_cert, tls_key]
    networks: [edge, backend]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "0.5"
    mem_limit: 512m
    pids_limit: 128
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/health/live"]
      interval: 15s
      timeout: 5s
      retries: 5
    logging: *default-logging

  backup:
    image: "${OPS_IMAGE_REF}"
    profiles: [operations]
    user: "10002:10002"
    read_only: true
    tmpfs: [/tmp:size=512m,noexec,nosuid,nodev]
    command: ["pnpm", "ops:backup"]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: app_backup
      DB_PASSWORD_FILE: /run/secrets/db_backup_password
      BACKUP_ENCRYPTION_KEY_FILE: /run/secrets/backup_encryption_key
      OFFSITE_CREDENTIALS_FILE: /run/secrets/offsite_credentials
    secrets:
      - db_backup_password
      - backup_encryption_key
      - offsite_credentials
    volumes:
      - backup_encrypted:/backup
    networks: [backend, edge]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "1.0"
    mem_limit: 1g
    pids_limit: 128
    restart: "no"
    logging: *default-logging

  audit-archive:
    image: "${OPS_IMAGE_REF}"
    profiles: [operations]
    user: "10002:10002"
    read_only: true
    tmpfs: [/tmp:size=256m,noexec,nosuid,nodev]
    command: ["pnpm", "ops:audit-archive"]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: app
      DB_USER: audit_archive_writer
      DB_PASSWORD_FILE: /run/secrets/db_audit_archive_password
      WORM_CREDENTIALS_FILE: /run/secrets/audit_worm_credentials
      ARCHIVE_SIGNING_KEY_FILE: /run/secrets/audit_archive_signing_key
    secrets:
      - db_audit_archive_password
      - audit_worm_credentials
      - audit_archive_signing_key
    networks: [backend, edge]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    cpus: "1.0"
    mem_limit: 1g
    pids_limit: 128
    restart: "no"
    logging: *default-logging

networks:
  backend:
    internal: true
  edge:

volumes:
  pg_data:
  backup_encrypted:

secrets:
  db_bootstrap_password:
    file: ./secrets/db_bootstrap_password
  db_migrator_password:
    file: ./secrets/db_migrator_password
  db_runtime_password:
    file: ./secrets/db_runtime_password
  db_backup_password:
    file: ./secrets/db_backup_password
  db_audit_reader_password:
    file: ./secrets/db_audit_reader_password
  db_audit_archive_password:
    file: ./secrets/db_audit_archive_password
  session_hash_keyring:
    file: ./secrets/session_hash_keyring
  idempotency_fingerprint_keyring:
    file: ./secrets/idempotency_fingerprint_keyring
  audit_hmac_keyring:
    file: ./secrets/audit_hmac_keyring
  totp_kek_keyring:
    file: ./secrets/totp_kek_keyring
  backup_encryption_key:
    file: ./secrets/backup_encryption_key
  offsite_credentials:
    file: ./secrets/offsite_credentials
  audit_worm_credentials:
    file: ./secrets/audit_worm_credentials
  audit_archive_signing_key:
    file: ./secrets/audit_archive_signing_key
  tls_cert:
    file: ./secrets/tls_cert.pem
  tls_key:
    file: ./secrets/tls_key.pem
```

这里的 `*_IMAGE_REF` 必须是已验证的完整 `tag@sha256:digest`，不能只填 Tag。数据库卷对 PostgreSQL 18 固定挂载 `/var/lib/postgresql`。开发环境可以通过 override 文件暴露 PostgreSQL 端口；生产配置不得暴露。Secret 文件目录权限固定为仅部署账户可读，不进入 Git、镜像、备份或 CI Artifact。TLS 证书由受控的宿主机续期任务原子替换，并在校验成功后向 Nginx 发送 reload；失败必须告警。

首次建库使用版本化的 `compose.init.yaml` 临时覆盖：仅该次向 db 服务设置 `POSTGRES_DB=app`、`POSTGRES_USER=cluster_bootstrap`、`POSTGRES_PASSWORD_FILE`，并只读挂载 bootstrap、migrator、runtime、backup、audit_reader、audit_archive_writer 六个独立密码 Secret 供镜像内初始化脚本消费。NOLOGIN `app_owner/audit_writer` 不设密码。初始化和角色探针通过后立即停止该配置，改用上面的稳态 Compose；稳态 db 容器不挂载任何登录密码。禁止把一次性初始化覆盖用于日常 `up`。

### 11.2.1 数据库角色与初始化

| 角色 | LOGIN | 用途与权限 |
|---|---:|---|
| `cluster_bootstrap` | 是，仅初始化/灾难恢复 | 官方镜像初始化超级用户；凭据不提供给 migrate、API、backup 容器 |
| `app_owner` | 否 | 拥有数据库 Schema、表和非审计函数；审计追加函数由 `audit_writer` 拥有；不供进程登录 |
| `app_migrator` | 是 | 仅迁移任务使用，可受控 `SET ROLE app_owner`；无业务流量 |
| `app_runtime` | 是 | Schema USAGE、所需业务表的最小 SELECT/INSERT/UPDATE、序列 USAGE、审计受限追加函数 EXECUTE；无原始审计 SELECT、无管理员读函数、无 DDL、无 owner 成员资格 |
| `audit_writer` | 否 | 仅拥有固定 search_path 的审计追加函数及所需审计对象；普通进程不能 SET ROLE |
| `audit_reader` | 是 | AuditQueryService 独立连接；只读原始审计与链头，无业务写和审计写权限 |
| `audit_archive_writer` | 是 | 运维归档进程读取审计/链头并写远端 WORM；无业务写、历史审计修改和对象删除权限 |
| `app_backup` | 是 | 仅备份所需 CONNECT/USAGE/SELECT；显式无 `user_sessions/session_csrf_tokens/preauth_sessions` SELECT，无写权限、无 owner 成员资格 |

数据库 bootstrap 镜像中的版本化脚本从六个独立密码 Secret 创建 LOGIN 角色，将数据库和 Schema 所有权交给 NOLOGIN `app_owner`、将审计追加函数所有权交给 NOLOGIN `audit_writer`，撤销 `PUBLIC` 的 CREATE/EXECUTE，并设置 `ALTER DEFAULT PRIVILEGES`。`020_pgroonga.sql` 由 `cluster_bootstrap` 预装非 trusted 的 PGroonga，并撤销全部 PGroonga 函数对业务角色的 EXECUTE，仅给 `app_runtime` 授权 `pgroonga_query_escape` 与 `&@~` 底层函数。所有登录角色均显式 `NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION`。初始化结束后日常容器不使用 bootstrap 凭据；灾难恢复时由离线 Runbook 临时提供。

CI 和上线 preflight 必须分别以各 LOGIN 角色连接并查询 `pg_roles/pg_class`：确认 runtime/backup/audit_reader/audit_archive_writer 不是超级用户、不是对象 owner、不能 `CREATE/ALTER/DROP/TRUNCATE`、不能 `SET ROLE app_owner/audit_writer`；runtime 无原始审计读取，backup 无 Session/CSRF/preauth SELECT，reader 无写入，archive writer 无覆盖/删除能力；migrator 只在迁移任务存活期间可用。任一检查失败即阻断。

## 11.3 健康检查

- `/health/live`：进程事件循环正常，不访问数据库；
- `/health/ready`：数据库可连接、迁移版本正确；
- Docker Compose 的 health 状态只用于启动依赖与观测，不会在运行期自动摘除 upstream，也不会因 unhealthy 自动重启容器；
- API 内置 readiness gate：数据库或迁移版本异常时，除 live/ready 外的新业务请求立即返回 503；Nginx 只负责透传该结果，不声称具备动态服务发现；
- 宿主机 watchdog 连续三次探测失败时告警，并按 Runbook 限次重启 API；禁止无限快速重启掩盖数据库故障；
- 健康接口不得泄露连接串、版本细节和堆栈。

## 11.4 日志

- pino 输出 JSON 到 stdout；
- 每个请求包含 requestId、route、status、duration；
- 可记录 actorId/projectId，但不得记录密码、Cookie、CSRF Token、Secret 和完整敏感正文；
- Docker 日志驱动设置 `max-size/max-file`；
- 审计日志不是应用错误日志的替代品；
- 数据库启用 `pg_stat_statements` 并设置慢查询观察规则。

## 11.5 备份与恢复

宿主机受控调度器每 12 小时执行一次 `docker compose --profile operations run --rm backup`；调度配置、并发锁和告警接收方写入 Runbook。一次任务未开始、超时、退出非零、上传失败或超过 18 小时没有成功记录都必须告警。任务使用 `app_backup`，不使用 owner/migrator/runtime 凭据。

备份流程：

1. 执行 `pg_dump --format=custom --no-owner --no-acl --exclude-table-data=app.user_sessions --exclude-table-data=app.session_csrf_tokens --exclude-table-data=app.preauth_sessions`；认证/预认证/CSRF 表结构保留、数据明确排除，`app_backup` 在权限层也无这些表的 SELECT；
2. dump 输出直接流入经过评审的 AEAD/age 加密器，明文只允许存在于受限 tmpfs 和进程管道，不得生成本地明文最终文件；
3. 加密完成并可解密读取后生成密文 SHA-256 和签名清单，清单包含数据库版本、迁移版本、镜像 digest、Git SHA、时间、文件大小和审计链锚点；
4. 在同一文件系统原子重命名为最终文件，再上传异机只读存储；本机和异机副本都必须加密；
5. 本机密文保留 7 天，异机保留至少 30 天；加密密钥与数据副本分库存放并有恢复授权流程；
6. 数据库角色、对象 owner 和密码不依赖 `pg_dump` 恢复。版本化 bootstrap/role/default-privilege 脚本与发布清单单独备份；绝不备份角色明文密码；
7. 宿主机每小时运行 `audit-archive` 写入签名链头检查点；每日另导出加密审计明细到独立 WORM 前缀。归档使用 `audit_archive_writer`、独立 WORM 凭据和归档签名密钥，不挂载在线审计 HMAC；
8. 记录最近一次成功备份、异机上传、审计归档和恢复演练时间，并暴露给仅管理员可读的运维状态页。

全新主机恢复顺序：

1. 校验发布清单签名和所有镜像 digest，创建空卷并用 bootstrap 脚本建立全部 LOGIN/NOLOGIN 数据库角色及默认权限；
2. 校验密文 Hash，在 tmpfs 解密并以 `app_owner` 执行 `pg_restore --no-owner --no-acl`；
3. 重新执行权限收敛脚本，验证 runtime/backup 不拥有对象且不能 DDL；
4. 确认 `user_sessions/session_csrf_tokens/preauth_sessions` 均为空；若恢复来源是物理全库快照，则在 API 启动前轮换 Session HMAC key 并递增所有用户 `auth_version`；
5. 校验迁移版本、扩展、所有约束、审计序号/Hash/远端锚点及 MFA/审计旧密钥可用性；
6. 启动 API/Web，运行登录、项目读取、写入回滚、搜索权限与审计读取 smoke test；
7. 记录实测 RPO/RTO。目标为 RPO ≤ 24h、RTO ≤ 2h；未达标即上线阻断。

每月至少在与生产隔离的临时环境完成一次上述全新主机恢复；每季度模拟丢失原主机。只有通过完整恢复演练的文件才称为有效备份。

## 11.6 升级与回滚

```text
构建并测试镜像
  ↓
备份并验证可读
  ↓
运行向后兼容的 expand migration
  ↓
启动新 API 并通过 readiness
  ↓
切换流量/替换容器
  ↓
部署新 Web
  ↓
运行 smoke test
  ↓
观察错误率和慢查询
```

代码回滚不等于数据库回滚。破坏性字段删除放在后续 contract migration，确认旧代码不再使用后执行。

## 11.7 容量规划

初始建议可以从 4 vCPU、8GB RAM、SSD 开始，但这不是容量承诺。上线前用接近真实的数据量验证：

- 项目、任务和迭代记录数量；
- 同时在线与写并发；
- 全局搜索耗时；
- 合并和完成事务锁等待；
- PostgreSQL 连接池大小；
- 备份时间与恢复时间；
- 日志增长速度。

只有实测后才能给出 100/300 用户的资源结论。

---

# 12. 测试与 CI

## 12.1 测试金字塔

| 层次 | 工具 | 必测内容 |
|---|---|---|
| 纯逻辑单元测试 | Vitest | 状态机、权限 Policy、编号格式、URL 解析 |
| Repository 集成测试 | 真实 PostgreSQL | FK/CHECK/UNIQUE、查询过滤、迁移 |
| Application 集成测试 | 真实 PostgreSQL | 事务回滚、审计、通知、幂等 |
| API 测试 | Nest 测试应用 | 认证、CSRF、错误模型、Route Registry、请求/响应 Schema、OpenAPI 一致性 |
| E2E | Playwright | 登录、创建项目、创建任务、完成、合并、解除、版本查看 |
| 运维测试 | Compose + smoke | 构建、启动、迁移、健康检查、备份恢复 |

不要只 Mock Repository 测 Service；这无法验证真实事务、锁和数据库约束。另需增加：

- 依赖边界测试：`app → pages → features → shared/generated`、模块依赖图、禁止跨模块 Repository 导入；
- 权限矩阵数据驱动测试：按表 7.3 中全部操作者类别执行并获得预期结果；
- 投影测试：业务命令提交后 `search_projection`、`activity_projection` 与审计快照一致；
- 搜索测试：规范化、PGroonga 索引、`pgroonga_query_escape`、V1 边界、权限过滤和指标；
- 生产约束测试：非 root、digest、Secrets 文件 fail-closed、HTTP→HTTPS 308、TLS/HSTS/CSP、健康检查、迁移顺序、备份恢复；
- 迁移升级测试：除空库外，必须从最近一个正式版本的脱敏生产规模快照执行升级，并验证旧代码与 expand migration 的回滚兼容窗口；
- 数据库角色测试：分别以 bootstrap/migrator/runtime/backup/audit_reader/audit_archive_writer 连接，验证 NOLOGIN owner/audit_writer、DDL、原始审计读写、Session 表拒绝和最小权限；
- 全新主机恢复测试：从加密备份、bootstrap 脚本和发布清单恢复角色、扩展、权限、业务数据、审计链及全部版本化 keyring；确认认证 Session、预认证 Session 与 CSRF Token 均为空，同时未过期幂等记录引用的 fingerprint key 全部可用；
- Secret 轮换测试：版本化 Session HMAC、幂等 fingerprint HMAC、审计 HMAC、TOTP KEK 的旧数据在轮换和恢复后仍可验证；缺少当前 selector、keyring 或记录引用的旧 key 时 fail closed，分别满足退役条件后旧 key 才被移除。

## 12.2 必须存在的并发测试

1. 两个请求同时完成同一任务，只能一个成功；
2. 两个请求同时把同一任务合并到不同组，只能一个成功；
3. 两人同时编辑已发布记录，只能一个版本成功，另一方收到 409；
4. 相同幂等 key 重放不产生重复记录；
5. 相同幂等 key 携带不同请求内容返回 409；
6. 并发创建编号不重复；
7. 解除合并与新增来源任务并发时，组状态仍满足不变量；
8. 独立发布草稿与使用该草稿完成任务并发时只能一个成功；
9. 两个连接同时使用相同幂等 Key 只能执行一次；先行事务回滚后后继请求可以执行；
10. 至少 100 个同 scope 并发业务事务追加同一审计链，序号连续、每条只有一个后继且远端锚点可验证（见 [ADR-008](docs/adr/ADR-008.md)）；
11. 同一 TOTP 时间步并发验证只能一个成功；
12. 同一遗留项使用不同幂等 Key 并发转换只能生成一个任务；
13. 用户改密/停用与旧 Session 请求并发后，提交点之后的请求全部得到 401；
14. 合并、解除、完成和发布使用锁等待超时测试，确认统一锁序无死锁。
15. 同一任务解除后可加入其他组，但再次加入同一历史组因 `UNIQUE(project_id, group_id, task_id)` 返回 409。
16. 父级归档与子级 create/edit/publish 并发时，双方按项目 → 模块 → 功能统一锁序串行，不能基于旧 ACTIVE 快照同时提交且无死锁。
17. 同一任务已有一条或多条迭代记录时，后续关联其他记录不受唯一约束阻断；每条记录仍只能关联一个同项目任务。

## 12.3 安全测试

- 普通成员访问其他项目的任务 ID；
- 普通成员调用管理员接口；
- 普通成员被移除后继续使用旧 Session；
- CSRF 请求；
- Markdown 脚本、危险协议和恶意链接；
- 登录爆破限流；
- Session Fixation；
- 审计表 UPDATE/DELETE/TRUNCATE 权限；
- 审计哈希链连续性与读取留痕；
- 管理员 MFA 与高风险操作重认证；
- Session Token 数据库只存哈希；
- CSRF 同步 Token、Origin/Referer 缺失拒绝；
- 匿名 `GET /auth/csrf`、登录后 Session/CSRF 双轮换以及匿名 Session 越权拒绝；
- CSP、HSTS、frame-ancestors、no-store、同源 CORS；
- 日志与错误响应泄密检查；
- 响应 Serializer 拒绝/剔除数据库实体中的内部字段；
- TOTP Secret 密文、恢复码一次性、验证码重放、MFA 重置审计和首管理员 bootstrap；
- runtime/backup 无超级用户、无 owner、无 DDL、无直接审计表写权限；
- GitHub URL 混淆域名、userinfo、HTTP、非默认端口与重定向输入拒绝。

## 12.4 CI 门禁

每个 PR 必须依次通过：

```text
依赖安装（frozen lockfile）
  → lint
  → format check
  → typecheck
  → unit tests
  → database migrations from empty DB
  → integration tests
  → OpenAPI/client drift check
  → Route Registry/permission/response-schema completeness
  → web/api production build
  → Playwright critical-path tests
  → dependency boundary check
  → permission matrix test
  → container image build
  → Compose config render + exact-tag/digest format + PostgreSQL 18 mount check
  → database role/privilege probes
  → dependency/image/secret scan
```

数据库迁移、权限规则、认证配置、Dockerfile 和 Compose 变更必须人工审核。生产发布前另需通过上一版本升级、代码回滚兼容、加密备份全新主机恢复、Session 清空、审计并发链与远端归档校验，失败不得上线。

---

# 13. AI 编码协作规范

## 13.1 AI 的角色

AI 可以：

- 生成模块骨架；
- 按既有范例实现端点；
- 补充测试；
- 执行类型检查、测试和构建；
- 在失败后定位并修复；
- 更新 OpenAPI Client 和文档。

AI 不得自行决定：

- 新增核心依赖；
- 修改数据库不变量；
- 放宽鉴权；
- 删除或重写历史迁移；
- 跳过失败测试；
- 修改生产 Secrets；
- 将网络请求移入数据库事务；
- 物理删除业务历史。

## 13.2 仓库级约束

根目录 `AGENTS.md` 至少写明：

1. 模块和依赖方向；
2. Controller/Service/Repository 职责；
3. 当前技术栈和允许的库；
4. 错误码、鉴权和事务规范；
5. 数据库迁移不可重写；
6. 必须执行的验证命令；
7. 禁止读取或输出 Secrets；
8. 所有写接口的幂等默认值、豁免 ADR 与并发要求；
9. 修改 API 后必须重新生成客户端；
10. 完成定义（Definition of Done）。

## 13.3 AI 任务模板

每次任务应包含：

```text
目标：要实现的用户结果
范围：允许修改的模块/文件
不变量：权限、事务、状态和历史规则
接口：请求、响应和错误码
数据库：涉及的表、锁和约束
验收：必须新增/通过的测试
非目标：本次明确不处理的内容
验证命令：lint/typecheck/test/build
```

## 13.4 AI 开发节奏

```text
先读 PRD/ADR/相邻实现
  ↓
写或更新失败测试
  ↓
小步实现
  ↓
运行最小相关测试
  ↓
运行类型检查和模块测试
  ↓
运行完整 CI 门禁
  ↓
人工审查数据库、安全和业务不变量
```

禁止一次让 AI 生成整个系统后再集中调试。每次 PR 应保持可审查，优先一个业务纵切片。

## 13.5 Git 工作流

1～3 人团队采用 trunk-based：

- `main` 始终可发布；
- 使用短生命周期 `feature/*` 或 `fix/*` 分支；
- 不维护长期 `develop` 分支；
- 小 PR、强 CI、至少一次人工审查；
- 合并使用 squash；
- 发布使用不可变 tag 并记录 commit SHA。

---

# 14. 实施顺序

## 阶段 0：架构跑通

- 初始化 monorepo；
- 建立 NestJS/React 基础应用；
- NestJS 12 的 ESM、CLI、核心插件和构建链兼容性验证已完成，ADR-026 决定本期继续暂留 11.x 并锁定 11.2.3；
- 将 Drizzle 锁定在 `>=0.45.2、<0.46` 的实际补丁，并固定 Nginx 1.30.x 的已批准补丁（评审基线 1.30.4）与镜像 digest；
- PostgreSQL、迁移、Session Token 哈希、CSRF、Code-first OpenAPI 客户端；
- Route Registry、请求/响应 Zod 校验和候选生成工具兼容性验证；
- 全部 LOGIN/NOLOGIN 数据库角色及独立 Secret、PostgreSQL 18 卷挂载、Compose 构建与健康检查；
- 使用 ≥1000 条真实/仿真投影和 ≥100 条冻结金标查询执行搜索门禁：`Recall@20 ≥ 90%`，并用 `EXPLAIN (ANALYZE, BUFFERS)` 证明目标查询使用 PGroonga 索引；正式 `0003-0005` 迁移必须落在已通过 PostgreSQL 18.6 探针验证的 PGroonga 方案上，生产备份恢复由 F-10 纵切片验收；
- CI 最小链路。

验收：NestJS 决策已由 ADR-026 落库并锁定 11.2.3，Drizzle/Nginx 实际补丁和 digest 已锁定；空数据库可以一键迁移，匿名客户端可取得 CSRF Token 并安全登录，前端可以调用 `/me`；runtime 不能 DDL，PostgreSQL 18 数据确实落入目标命名卷，镜像可重复构建且 digest 校验通过；阶段 0 搜索数据量、100 金标、Recall@20 与 PGroonga 计划在 PostgreSQL 18.6 正式基线全部达标。

## 阶段 1：项目骨架

- 用户与管理员；
- 创建项目、初始成员、未分类模块单事务；
- 项目成员访问控制；
- 项目/模块/功能 CRUD 和归档；
- 审计基础设施、管理员 MFA/重认证、接口级权限矩阵。

## 阶段 2：任务与记录

- 功能级/模块级任务；
- 影响功能关系；
- 完成、取消、恢复；
- 迭代记录草稿、发布、版本；
- 迭代记录作废与恢复；
- 遗留问题转任务；
- 幂等和乐观锁；
- TaskCompletionWorkflow 的内联/草稿两分支、稳定遗留项、审计并发哈希链、通知/活动/搜索投影同事务。

## 阶段 3：合并与历史

- 任务组和 MAIN/SOURCE；
- 历史/活动来源分支；
- 解除合并；
- 聚合视图；
- 并发不变量测试；
- 模块依赖图检查；
- GitHub 链接跨项目复合外键测试。

## 阶段 4：搜索、通知与运维

- 全局搜索；
- 站内通知轮询；
- Docker 生产配置；
- 备份、恢复、升级 Runbook；
- Playwright 完整关键路径；
- 搜索投影不少于 100,000 条且不低于 5 年峰值 1.2 倍、≥200 条冻结金标、30 并发持续 10 分钟，预热后 P95 < 500ms/P99 < 1s 并单独记录冷缓存；加密备份调度、全新主机恢复、远端审计归档、RPO/RTO 与恢复演练。

---

# 15. 上线前架构验收清单

## 数据一致性

- [ ] 空数据库迁移成功；
- [ ] 所有必填归属列为 NOT NULL；
- [ ] 数据库不能插入跨项目的模块、功能、任务、记录、链接、通知、投影关系；
- [ ] 未分类模块每项目恰好一个，草稿/发布/作废/恢复记录状态约束有效且作废历史不可丢失；
- [ ] 每个活跃任务最多属于一个活跃组；
- [ ] 每个 ACTIVE 组恰好一个 MAIN 且至少一个 SOURCE，CLOSED 组没有 ACTIVE 成员；
- [ ] 每条迭代记录至多关联一个同项目任务，同一任务可关联多条记录且 `(project_id, task_id)` 查询有索引；同一遗留项最多转换为一个任务；
- [ ] 所有乐观锁更新都会递增 row_version；
- [ ] 所有 `idempotencyRequired` 写接口重放不会生成重复数据，且不存在未备案豁免；
- [ ] 项目创建、任务完成+记录发布、遗留转任务、合并/解除均验证全部回滚。

## 安全

- [ ] Session 可持久化、轮换、用户级 auth_version 失效和分批清理，DB 只存带唯一索引的 Token HMAC；
- [ ] Argon2id 参数达到下限，登录并发与速率限制生效；
- [ ] 系统管理员 MFA 注册、密文、重放防护、恢复码、另一名 MFA 管理员重置/单管理员双人离线恢复、全 Session 撤销和高风险重认证生效；
- [ ] 接口级权限矩阵自动化测试全部通过；
- [ ] 普通成员被移除后旧 Session 立即失去成员关系派生的项目权限，不影响其他项目；系统管理员仍保留全局权限；
- [ ] 匿名 CSRF 的 IP+Cookie+全局限流、认证 Token 8 小时/Session 绝对期限上限、最多一次重签重试、Fetch Metadata 与 Origin/Referer 策略通过；
- [ ] 安全响应头、CSP/AntD 方案、同源 CORS、可信代理策略通过；
- [ ] 资源 ID 无法越权访问；
- [ ] Markdown XSS、危险协议与 SSRF 测试通过；
- [ ] 审计 head key_version、多 scope UTF-8 锁序、固定 JCS envelope、仅对完整 JCS bytes 做 HMAC、旧 key 轮换标记、并发无分叉和读取留痕通过；
- [ ] runtime 无审计表直接 DML、无 DDL、非对象 owner、非超级用户；
- [ ] 审计远端只读归档完成且恢复演练可校验；
- [ ] 日志和错误响应不泄露 Secret。

## 部署

- [ ] 所有运行/基础镜像使用真实 exact-tag@sha256 digest，lockfile 与 commit SHA 固定；
- [ ] 前端产物构建进镜像；
- [ ] PostgreSQL 不暴露公网端口；
- [ ] PostgreSQL 18 命名卷挂载 `/var/lib/postgresql`，重建容器后数据仍存在；
- [ ] bootstrap/owner/migrator/runtime/backup 角色和独立 Secret 验证通过；
- [ ] 所有容器非 root，资源限制与连接池有界；
- [ ] DB/migration/API healthcheck 有效，迁移完成后 API 才启动；
- [ ] index.html 不做长期 immutable 缓存；
- [ ] Secrets 文件与应用读取逻辑真实连通；
- [ ] 非 root Nginx 使用 8080/8443，TLS 签发、续期、reload 和失败告警跑通；
- [ ] 新环境可以只依赖镜像、Compose 和 Secrets 启动；
- [ ] 每 12 小时加密备份调度、Session 排除、SHA-256/签名清单、异机保留和失败告警生效；
- [ ] RPO ≤ 24h、RTO ≤ 2h 以恢复实测为准；
- [ ] 备份恢复演练成功，且记录最近成功备份/恢复时间；
- [ ] 依赖漏洞、镜像与 Secret 扫描通过。

## 交付质量

- [ ] Schema Registry/Zod 数据结构与 Route Registry HTTP 元数据双真相共同生成 OpenAPI/客户端且无漂移；
- [ ] Route Registry 覆盖全部 Controller，逐 route 完整绑定 method/path/operationId、请求/响应 Content-Type+Schema、auth/CSRF/幂等/version/concurrency/audit；
- [ ] 模块依赖图无循环，前端无反向依赖；
- [ ] 类型检查、单测、集成测试、E2E、权限矩阵和构建全部通过；
- [ ] 并发测试覆盖完成、独立发布、合并/解除、版本编辑、遗留转任务、幂等和审计链；
- [ ] 阶段 0 的 1000 数据/100 金标/Recall@20/PGroonga 计划和阶段 4 的 100k+容量、200 金标、30 并发 10 分钟、P95/P99/冷缓存指标全部通过；
- [ ] 核心 ADR 已记录；
- [ ] Runbook 覆盖部署、回滚、备份、恢复、审计归档和 Session 失效。

---
# 16. ADR 引用

ADR 的编号、状态、背景、决策与影响只在 [docs/adr/README.md](docs/adr/README.md) 及其链接的正式记录中维护。本文引用 ADR-001～ADR-026，不再重复保存决策全文；修改已接受决策时必须新增 ADR，不得复用既有编号。

---
# 17. 最终结论（V1.2.2）

V1.2.2 的目标不是选择最“轻”的框架，而是建立一条 AI 可以反复执行、机器可以验证、人工可以审查的研发流水线。

最终架构为：

```text
React SPA
  ↓ Zod 契约 + OpenAPI 生成客户端
NestJS 模块化单体
  ↓ Drizzle + 显式事务/约束
PostgreSQL
  ↓
Docker Compose 单实例部署
```

项目成功的关键不在 React、NestJS 或 Drizzle 的单项评分，而在以下不变量是否同时成立：

1. 数据库阻止非法关系；
2. Application Service 守住权限、状态与事务；
3. API 契约可生成、可验证；
4. 并发冲突和重复请求有明确结果；
5. 审计、通知、活动与搜索投影同事务写入；外部副作用在启用 Outbox 前必须新增 ADR；
6. 部署、备份和恢复实际跑通；
7. AI 的每次修改都必须通过自动化门禁。

满足上述条件后，该方案能够支持 1～3 人 AI 优先团队稳定交付 V1.1 产品范围，并为后续 GitHub 集成、SSE 通知和容量扩展保留清晰演进路径。

## F-14 基本信息纵切片实施说明（2026-09-09）

功能级任务集合路径为 `/api/v1/projects/{projectId}/modules/{moduleId}/features/{featureId}/tasks`：`listTasks` GET、`createTask` POST；`/{taskId}` 提供 `getTask` GET、`updateTask` PATCH；`/assignees` GET 为 `listTaskAssignees`。这些 operationId 的 Schema、Route Registry、Controller 绑定、响应和生成客户端同步维护。读写响应均 no-store；编辑必须 If-Match，所有写须 CSRF、数据库幂等和结果资源重放授权；任务号为项目编码加 `-T-n`，ProjectCodePort 使用 `TASK` 序列原子 UPSERT，失败与业务一同回滚。

锁序为项目→模块→功能 FOR SHARE，编辑时随后任务 FOR UPDATE；仅创建/真正改派再按负责人用户→成员 FOR SHARE 复查用户 ACTIVE/disabled_at NULL、成员 ACTIVE。数据库既有触发器继续兜底；未改派的 UPDATE 不出现在 SET 列表中更新 assignee_id，从而保留失效成员历史负责人。初始状态为 TODO/ACTIVE，同时插入 from NULL→TODO 状态历史；不改历史迁移。版本条件更新后审计、活动、搜索及通知接受同一个 TransactionContext；每次真正指派只通知新负责人。重放重新验证当前 Session/CSRF、项目访问、全部真实归属及父级可写性后才返回已存成功结果。

FeatureReadPort 按项目/模块/功能身份读取归档历史。ProjectMembersQueryPort.listActiveMembers 在 tx 内验证当前 actor 的项目访问，并只返回 ACTIVE 用户/成员的 id/name/avatarUrl；与全局用户目录独立。负责人空值 422，普通编辑保留未变历史负责人。前端以编辑前快照、草稿、最新值进行三方合并；冲突解决后才采用最新 If-Match，相同语义的不确定失败重用 Key。详细本地验证和关联能力边界见 [F-14 交审说明](docs/f14-local-handoff.md)。

## F-15 模块级任务实施补充（2026-09-09）

模块任务集合 `/api/v1/projects/{projectId}/modules/{moduleId}/tasks` 提供 listModuleTasks/createModuleTask，资源 `/{taskId}` 提供 getModuleTask/updateModuleTask，`/assignees` 提供 listModuleTaskAssignees。ModuleTaskItem 明确 MODULE/featureId:null/impactFeatureIds；请求集合最大1000项并去重升序后参与幂等摘要。FEATURE写响应及安全策略不变，listTasks只读响应扩展为TaskItem或ModuleTaskItem，每个任务一次，不复制数据。

MODULE影响关系是当前集合；只DELETE本任务目标集合明确移除的关系。同一tx审计保存关系before/after全快照（task/project/module/feature身份、relation_type、created_at）与added/removed差异；重加使用新created_at。没有新增迁移或修改既有权限。MODULE任务真实父级是项目/模块，新增或重加影响须ACTIVE，保留/移除已有ARCHIVED影响只验证归属。模块重放上下文列出响应中的全部影响功能ID，验证当前actor/任务/父级与结果影响资源的可读归属后才重放。

锁定项目→模块，再预读当前影响关系，按当前∪目标功能ID升序FOR SHARE，然后任务FOR UPDATE，锁后重读关系。版本变化409；关系变化但版本未变则回滚本阶段savepoint释放功能/任务锁，从头重建集合，最多三次，禁止持任务锁再补新功能锁。始终一个UnitOfWork和一个tx，未将网络请求放进事务。具体实现、归档解释依据和真实锁等待验证见 [F-15 交审](docs/f15-local-handoff.md)。


## F-16 状态命令与历史读取（2026-09-10）

真实 FEATURE/MODULE task 资源下新增 POST /{taskId}/status（transitionTask/transitionModuleTask）和 GET /{taskId}/status-history（getTaskStatusHistory/getModuleTaskStatusHistory）。TaskStatusRequest 严格区分 COMPLETE/WITHOUT_RECORD 与 REOPEN/CANCEL/RESTORE；完成原因六类，拒绝实际变化发布模式。新增命令版本1.0.0，复用任务结果 DTO 和安全重放字段，原创建/编辑指纹不变。

Application Service 在既有父级锁/任务锁及 MODULE 影响重读策略下判定状态机，Repository 条件更新并写不可变历史，所有审计/活动/搜索/适用通知在同一 tx。完成快照及重开旧快照直接在数据库 CTE 中保持精度；锁后 clock_timestamp 与旧 updated_at 取较晚值，避免事务起始时间倒退。历史按数值 ID 正序返回，no-store，重放重新校验当前权限和 MODULE 结果影响资源。无迁移或权限变更，F19 仍是后续组合 Workflow。详见 [F16 交审](docs/f16-local-handoff.md)。

## F-17 草稿契约与事务实施补记（2026-09-10）

新增七个 operationId：listRecordDrafts/getRecordDraft/createIndependentRecordDraft/updateIndependentRecordDraft/getTaskRecordDrafts/createTaskRecordDraft/updateTaskRecordDraft。统一使用 Schema Registry、Route Registry、Contract 装饰器和全局响应校验；生成 OpenAPI 与客户端随源契约更新。所有写入均同源/CSRF、数据库级幂等且重放实时授权；来源创建的 If-Match 指任务 row_version，编辑的 If-Match 指记录 row_version。内容 DTO 不接受 authorId/handlerId/taskId/status/code/时间等服务端字段。标题 trim 后 1–500 字符，三项核心内容各 1–50000，遗留问题 0–50000；MODULE 影响集合去重排序且至多 1000 项。

Workflow 先取得项目→模块→功能 ID 升序 FOR SHARE，再取得任务 FOR UPDATE，最后记录锁。所需功能包括任务当前影响和记录已存影响的并集。预读任务身份/版本/关系与锁后重读不同，使用同事务 savepoint 释放本阶段锁，从父级功能锁开始最多三次重试；不持任务锁追加新的功能锁。锁后记录版本变化返回 409；内容编辑不重新复制来源负责人、标题或影响集合。

task_id 继续为可空多对一，无唯一索引和每任务最多一草稿约束。创建只保存当前 payload、DRAFT 和自己的影响快照，同事务追加审计；不占用 CHANGE_RECORD 序列、写正式版本、改变任务或发出发布副作用。2026-09-10 人工确认保留基线并修正工作书，无新迁移/生产依赖/数据库权限变更。接口策略、定向真库/浏览器证据见 [权限矩阵](docs/permissions.md)、[测试矩阵](docs/test-matrix.md)及 [F-17 交审说明](docs/f17-local-handoff.md)。

## F-18 正式发布与版本事务（2026-09-10）

新增 listChangeRecords/getChangeRecord/listChangeRecordVersions/getChangeRecordVersion 四个 GET，以及 publishChangeRecord/createChangeRecordVersion 两个 POST；统一 Contract 装饰器和 Schema/Route Registry，成功 DTO、可空稳定遗留项、版本快照及生成物同步。发布请求为空对象且 If-Match 为记录 row_version；修订仅五项内容和 confirmLeftoverResolved，If-Match 与 X-Record-Version 分别覆盖记录 row_version/current_version，两头均进入幂等摘要。数据库幂等成功缓存白名单覆盖全部叶子字段及 leftoverItem 的 null 分支；重放重新认证、CSRF、项目权限、可写父级、记录和保存的结果资源关联，不以历史任务后来重开拒绝重放。

锁序为项目→模块→排序功能→来源任务（仅发布）→记录→排序遗留项。来源预读与任务锁内结果变化时释放 savepoint 后有限重试三次；最终来源必须 DONE，内容修订不再锁定/检查来源任务工作状态。发布与修订都在记录锁内比较预期版本；版本 INSERT、当前记录条件 UPDATE 和 row_version 递增、CHANGE_RECORD 序列分配（仅首发）、全部副作用在同一事务。RecordPublicationCommandPort 供 F-19 调用，组合 Workflow 须先取得完整父级和聚合锁集合并在同事务完成任务。

遗留文字按人工确认映射为单记录单稳定项；非空版本向 change_record_version_leftovers 追加稳定 ID 与精确全文。ACTIVE 清空需 confirmLeftoverResolved=true 并条件更新 RESOLVED，再填转 ACTIVE；CONVERTED 永不因文字编辑重置或取消 leftover_task_links。空版本不写遗留快照，旧版本不可 UPDATE/DELETE。发布/修订 remainingIssues 最大 10000，草稿保留 50000。完整 code/title/四正文经实际 SearchProjection Writer 原文和 NFKC 规范化后的 JavaScript UTF-16 长度上限 100000 校验（数据库另有字符数防线）；容量失败返回 422，保留输入并回滚编号与全部写入，不截断。无新迁移/生产依赖/数据库权限变化；[权限矩阵](docs/permissions.md)、[测试矩阵](docs/test-matrix.md)与 [F-18 交审说明](docs/f18-local-handoff.md)记录本地证据及未交付范围。

## F-19 TaskCompletionWorkflow 实施接口（2026-09-10）

新增 POST /api/v1/tasks/{taskId}/complete（completeTask），由真实任务标识解析 project/module/feature 后实时授权，不信任客户端项目归属。请求严格联合：WITHOUT_RECORD 带 expectedRowVersion、F-16 completionReason 六类及 note；WITH_RECORD 带 expectedRowVersion，并且 record 内联五项内容与 recordDraftId/recordExpectedRowVersion 引用二选一。原 §6.3 completionNote 在传输层沿用 F-16 的受限原因/补充说明结构，服务端形成落库 completion_note。If-Match 必填且须与 expectedRowVersion 相同；CSRF、同源、数据库幂等与安全响应白名单完整登记，新接口幂等契约 1.0.0。返回任务与可空正式记录 DTO，重放同时验证两类资源及全部结果影响/遗留引用。

预读真实任务、引用 DRAFT 和当前组身份，取得 project→module→全部排序功能→task→当前 taskGroup→record 锁。功能集合为任务当前影响、草稿历史影响及重放结果影响的并集；MODULE 影响不是父级，历史已归档影响可保留，真实 FEATURE 父级必须 ACTIVE。任务/草稿/组身份在锁后重读，与预读不同则回滚保存点释放本次锁，从头最多三次。任务组仅需本任务锁及其组锁：本命令不更改其他任务或成员，合并/解除必须先持有所涉任务锁再持组锁，不允许绕过该约定。历史 SOURCE 拒绝执行，MAIN/活动 SOURCE 可完成。

先以公开草稿端口创建或绑定本次记录，再收集包括本次作者在内的相关作者，通过公开任务完成端口以 TODO/ACTIVE/预期 row_version 条件更新并追加历史；同一事务调用发布端口时来源已 DONE，生成 v1/编号/遗留快照及全部发布副作用。所有前序父级和记录锁已经持有，端口重复校验不会引入未预锁的前序资源。任何异常必须离开 UnitOfWork 回滚，不能捕获后提交半完成。独立发布与组合完成按相同任务→记录顺序串行重查；后续任务重开不禁止历史修订和合法已成功请求重放。

完成和发布分别对应 task.complete / record.publish 业务事件，各自在服务端按当前项目权限过滤、去重；完成通知任务创建人和所有关联记录作者，发布集合延续 F-18。无新迁移、约束、生产依赖或权限变化，详见 [权限矩阵](docs/permissions.md)、[测试矩阵](docs/test-matrix.md)与 [F-19 交审说明](docs/f19-local-handoff.md)。

旧 transitionTask/transitionModuleTask 保留 TaskStatusRequest 和 TaskItem/ModuleTaskItem DTO，由 TaskStatusCompatibilityController 单一调用兼容 Workflow HTTP Use Case。COMPLETE 映射为 WITHOUT_RECORD 并以 If-Match 构造 expectedRowVersion，复用完整组合锁序/事务/通知；REOPEN/CANCEL/RESTORE 经公开 TaskStatusCommandPort 调用原单域服务，端口拒绝 COMPLETE。两 operation 的 idempotencyContractVersion 与 replayAuthorizationPolicy 均升级 2.0.0，生成历史保留原 1.0.0；此前任何状态命令 Key 在新契约下返回 409，不能用升级绕过冲突。TaskStatusCompatibilityReplayContext 保存最小任务/影响引用及 completion 分支标记；完成重放检查当前 MAIN/ACTIVE SOURCE 资格与全部结果资源，其他状态按原门禁重放。成功缓存字段和旧响应包装不变。
## F-20 LeftoverTaskWorkflow 落地接口（2026-09-10）

POST /api/v1/projects/{projectId}/change-records/{recordId}/leftover-task（convertLeftoverToTask）严格接收leftoverItemId、recordVersion、expectedRowVersion、leftoverExpectedRowVersion、expectedImpactFeatureIds及title/assigneeId/priority/dueAt。If-Match必须与记录expectedRowVersion一致；客户端不能提交scope、正文或任务状态。新增完整幂等1.0.0、安全白名单和最小结果授权上下文，原路由版本不变。GET同记录路径的leftover-task-preview给出锁后当前版本全文、继承/排除功能名称和已授权任务引用；GET /api/v1/tasks/{taskId}/leftover-source按实际任务项目实时授权，归档父级可读历史，没有来源或来源当前非PUBLISHED返回source=null。

统一锁序project→module→排序功能并集→record→leftover；记录scope/impact锁后变化释放savepoint，从头最多三次。MODULE集合含全部历史impact，重放另含保存结果impact；FEATURE真实父级必须ACTIVE。协调本轮明确新任务只继承锁后ACTIVE的MODULE影响，expectedImpactFeatureIds与实际集合不符409，不能静默改变用户已确认集合或违反禁止新增归档impact的任务规则。新任务未对外可见，创建端口重检的全部父级/影响锁已持有，无记录锁后的新前序资源锁；已有任务引用仅实时读取验证真实归属与项目权限。

记录当前版本关系必须含指定稳定ID、遗留项ACTIVE且无链接、预期正式/记录/遗留版本匹配；任务编号、TODO创建及活跃成员指派校验由Tasks端口完成；记录端口插入现有唯一链接、条件标记CONVERTED并递增遗留row_version。转换改变PublishedRecord DTO中的状态/链接，按聚合并发规则递增记录row_version与updated_at，但不是内容修订，current_version及版本/遗留快照表不写入。task.create审计/活动/搜索与leftover.convert审计/活动/记录搜索版本更新、负责人通知同事务；通知复用创建路径，将类型设为leftover.convert，不额外发送task.assigned。

不同Key重复转换先实时检查既有任务引用，409返回精确引用；失权404且details为空。同Key重放重新验证当前权限/PUBLISHED记录/CONVERTED稳定链接/任务真实归属/保存影响，允许正式内容后来修订或清空回填后合法重放。前端409后持久保持需要刷新确认状态，刷新再失败也不能提交旧预览；显式确认最新版本/全文/影响后保留任务输入并使用新Key。无新迁移或数据库权限变化，见[F-20交审说明](docs/f20-local-handoff.md)。

### F-21 实施接口补记（2026-09-10）

实际路由为 POST /projects/{projectId}/change-records/{recordId}/void 与 /restore（均位于 /api/v1），operationId 为 voidChangeRecord/restoreChangeRecord。请求严格仅接受去空白后的非空 reason，要求 If-Match、CSRF 与数据库级幂等；响应/缓存只包含 id/projectId/status/rowVersion。每次执行及重放均重新检查完整管理员 Session 的双时间戳五分钟门禁，恢复仅更新 ADR-024 允许的三列。

listChangeRecords 新增显式 status=PUBLISHED|VOID 查询；默认 PUBLISHED，VOID 列表只允许管理员。详情/列表使用 ReadableRecord 可区分联合 DTO，PUBLISHED 沿用原正式字段，VOID 分支额外包含最近 voidedAt/voidReason 且仅管理员可读；版本接口在记录可读门禁后返回原不可变版本。F-20 两个 GET 显式 query=none，避免复用路由模板时继承该筛选。既有发布/修订请求及幂等契约不变，生成器更新 OpenAPI/客户端。见 [F-21 交审说明](docs/f21-local-handoff.md)。

## F-22 本地实施补充（2026-09-10）

第5.7节已用三条类型化目标路由实施：GET/POST `/api/v1/external-links/{targetType}/{targetId}` 与 DELETE 同路径 `/{linkId}`。Schema/Route Registry生成OpenAPI和客户端，targetType严格PROJECT/FEATURE/TASK/CHANGE_RECORD，服务端自行解析真实projectId。写接口If-Match与数据库幂等必需，仅重放最小目标/链接ID/行版本结果，并复核当前认证与结果资源；删除后的合法重放不依赖已删除关联行存在。

复用四表、规范URL UNIQUE与ON CONFLICT；标准Parser过滤userinfo、非默认端口、危险scheme/混淆host，query保留page/q/tab、fragment删除。Release派生tag/label但kind为OTHER。项目/功能自身写直接取FOR UPDATE，其他目标按真实project→module→feature共享锁后取业务目标更新锁，各域CommandPort条件推进row_version；记录current_version和不可变正文/遗留历史不变。MODULE历史影响功能和来源任务不当作归属父级。

第9章目标投影保持业务raw_text，Search写端口用公开ExternalLinkQueryPort同事务取当前URL计算normalized_search_text；F22刷新既有目标投影，发布/修订/作废/恢复自然保持链接与目标可见性。组合超过原100000字符容量抛出安全422并回滚，不截断；对应HTTP边界已同步。无新数据库迁移、依赖或基础设施。实际定向验证、失败记录和数据库收尾见 [F-22 交审说明](docs/f22-local-handoff.md)。

F-22父审核修正（2026-09-10）：GET/重放完成父级共享锁后对目标取FOR SHARE并重读，写入取FOR UPDATE，预读只服务父级路径确定；锁保持到同UnitOfWork完成，阻止作废与链接读取之间的过期状态窗口。审计动作声明与实写为EXTERNAL_LINK_ADDED/EXTERNAL_LINK_REMOVED。auditAction不在既有computeRouteFingerprint payload内，本次Schema/重放策略不变；工具生成仅修改OpenAPI相应扩展，指纹与客户端不变。证据见[F22交审增量](docs/f22-local-handoff.md)。
